unit LibraryValesCupones_Intf;

{----------------------------------------------------------------------------}
{ This unit was automatically generated by the RemObjects SDK after reading  }
{ the RODL file associated with this project .                               }
{                                                                            }
{ Do not modify this unit manually, or your changes will be lost when this   }
{ unit is regenerated the next time you compile the project.                 }
{----------------------------------------------------------------------------}

{$I RemObjects.inc}

interface

uses
  {vcl:} Classes, TypInfo,
  {RemObjects:} uROUri, uROProxy, uROExceptions, uROXMLIntf, uROClasses, uROTypes, uROClientIntf,
  {Used RODLs:} DataAbstract4_Intf;

const
  { Library ID }
  LibraryUID = '{BE2DC080-021D-4410-AE12-47322D2F0A40}';
  TargetNamespace = '';

  { Service Interface ID's }
  IServiceValesCupones_IID : TGUID = '{762597B5-7713-4971-8C5A-32F68BB623AB}';

type
  TSeekOrigin = Classes.TSeekOrigin; // fake declaration
  { Forward declarations }
  IServiceValesCupones = interface;

  AAgrupacion = class;
  AAgrupacionBomba = class;
  aBombas = class;
  aBoolean = class;
  aCambioPrecios = class;
  aConsumo = class;
  aContador = class;
  ADatosCelular = class;
  ADespachadorLiquidacion = class;
  ADetalleExpress = class;
  ADetalleFacturaPemex = class;
  ADetalleLiquidacion = class;
  aDispositivos = class;
  aGas = class;
  AGeneraCupon = class;
  aLecturas = class;
  aMaguera = class;
  aNodos = class;
  aRecepciones = class;
  aRegistroVenta = class;
  ArrayOpcion = class;
  aTanques = class;
  ATCarteraVale = class;
  ATDetalleCupon = class;
  ATDetalleFactura = class;
  ATDetalleRecibo = class;
  ATDetalleTransaccion = class;
  ATDetalleValeCreditoFactura = class;
  ATFacturaElectronicaDetalleImportes = class;
  ATFacturaElectronicaDetalleImpuestos = class;
  ATipoValor = class;
  ATLiquidacionDetalle = class;
  ATLiquidacionProducto = class;
  ATTicketFactura = class;
  aTurnoBomba = class;
  TDocumentosConSaldoArray = class;

  TParametros = class;
  TFactura = class;
  TDetalleFactura = class;
  TTicketFactura = class;
  TDetalleCupon = class;
  TDetalleValeCreditoFactura = class;
  TDatosFactura = class;
  TLiquidacion = class;
  TLiquidacionDetalle = class;
  TLiquidacionProducto = class;
  TTransaccion = class;
  TDetalleTransaccion = class;
  TCarteraVale = class;
  TDatosLiquidacion = class;
  TReporte = class;
  TRecibo = class;
  TDetalleReciboPago = class;
  TDatosReciboPago = class;
  TDatosRecibo = class;
  TLoginInfo = class;
  TOpcion = class;
  TConsumoExpress = class;
  TDatosCliente = class;
  TFacturaExpress = class;
  TDetalleExpress = class;
  TValoresTurno = class;
  TOtroProducto = class;
  TAgrupacion = class;
  TSagarpa = class;
  TDatosPuntos = class;
  TDatosCelular = class;
  TEntregaPremio = class;
  TDatosPremio = class;
  TConfirmaEnvioA = class;
  TDatosCerrarLiquidacion = class;
  TDespachadorLiquidacion = class;
  TDetalleLiquidacion = class;
  TTipoValor = class;
  TPerfilPuntos = class;
  TFacturaElectronica = class;
  TFacturaElectronicaDetalleImportes = class;
  TFacturaElectronicaDetalleImpuestos = class;
  TDatosFacturaPemex = class;
  TDetalleFacturaPemex = class;
  TDataFacturaElectronicaF = class;
  TGeneraCupon = class;
  TDocumentosConSaldo = class;
  TAutoriza = class;
  TCuponValido = class;
  TCupon = class;
  TRegistroVenta = class;
  TConsumo = class;
  TTarjeta = class;
  TTarjetaManual = class;
  TNodo = class;
  TDatosVehiculo = class;
  TDatosVerifica = class;
  TGlobal = class;
  TBomba = class;
  TFactorDecimal = class;
  TFlujos = class;
  TLecturas = class;
  TManguera = class;
  TCanal = class;
  TContador = class;
  TRegistroPuntoVenta = class;
  TCambio = class;
  TCambioPrecios = class;
  TDispositivos = class;
  TGas = class;
  TProtecciones = class;
  TLicencia = class;
  TTanque = class;
  TRecepcion = class;

  { Enumerateds }
  eCodeTrama = (
    eCodeTrama_ctContinue,
    eCodeTrama_ctOut,
    eCodeTrama_ctAkc,
    eCodeTrama_ctNak,
    eCodeTrama_ctFrame,
    eCodeTrama_ctOverRun
  );

  eCodigoProducto = (
    eCodigoProducto_cpPorProducto,
    eCodigoProducto_cpPorPosicion
  );

  eEstadoBomba = (
    eEstadoBomba_ebOffLine,
    eEstadoBomba_ebIdle,
    eEstadoBomba_ebBusy,
    eEstadoBomba_ebPago,
    eEstadoBomba_ebUnknown,
    eEstadoBomba_ebCall,
    eEstadoBomba_ebClose,
    eEstadoBomba_ebError,
    eEstadoBomba_ebStop,
    eEstadoBomba_ebAuto
  );

  eEstadoVenta = (
    eEstadoVenta_evSinAutorizadoConsola,
    eEstadoVenta_evCambio,
    eEstadoVenta_evCobro,
    eEstadoVenta_evFullServer,
    eEstadoVenta_evAutorizadoConsolaVolumen,
    eEstadoVenta_evAutorizadoConsolaEfectivo,
    eEstadoVenta_evAutorizadoConsolaLibre,
    eEstadoVenta_evAutorizadoCistemFlotillas,
    eEstadoVenta_evAutorizadoCistemProtocol,
    eEstadoVenta_evAutorizacionTPV,
    eEstadoVenta_evAutorizacionLDC,
    eEstadoVenta_evAutorizacionPendiente
  );

  eFabricante = (
    eFabricante_fTokheim,
    eFabricante_fWayne,
    eFabricante_fGilbarco,
    eFabricante_fBennett,
    eFabricante_fSchlumberger,
    eFabricante_fCistemGas,
    eFabricante_fCistemGasRed,
    eFabricante_fIGM,
    eFabricante_fTransactor
  );

  eImpresionTicket = (
    eImpresionTicket_itNoImprime,
    eImpresionTicket_itCorteTurno,
    eImpresionTicket_itParcial
  );

  eModoOperacion = (
    eModoOperacion_moFullService,
    eModoOperacion_moConsola,
    eModoOperacion_moDeshabilitada
  );

  eMonitoreo = (
    eMonitoreo_mVeederRoot,
    eMonitoreo_mVeederRoot2,
    eMonitoreo_mAutoStikJR,
    eMonitoreo_mAutoStickII,
    eMonitoreo_mIncon,
    eMonitoreo_mPetrovend,
    eMonitoreo_mEeco
  );

  eProceso = (
    eProceso_pSinProceso,
    eProceso_pPrecio,
    eProceso_pMaximo,
    eProceso_pCorte,
    eProceso_pCambioPrecio,
    eProceso_pMinimo
  );

  eResult = (
    eResult_eOK,
    eResult_eFail
  );

  eTipoAutorizacion = (
    eTipoAutorizacion_taSinAutorizacion,
    eTipoAutorizacion_taAutorizacionPreset,
    eTipoAutorizacion_taTarjetaClub,
    eTipoAutorizacion_taCupon,
    eTipoAutorizacion_taFacturacionExpress,
    eTipoAutorizacion_taTarjetaBancaria
  );

  eTipoBitacora = (
    eTipoBitacora_tbUnkown,
    eTipoBitacora_tbBitacora,
    eTipoBitacora_tbAlertas,
    eTipoBitacora_tbDispensarios,
    eTipoBitacora_tbTanques,
    eTipoBitacora_tbAccesos,
    eTipoBitacora_tbJarreos,
    eTipoBitacora_tbCapturas,
    eTipoBitacora_tbConexiones
  );

  eTipoPago = (
    eTipoPago_tpgoEfectivo,
    eTipoPago_tpgoCheque,
    eTipoPago_tpgoTarjeta
  );

  eTipoPreset = (
    eTipoPreset_tpSinPreset,
    eTipoPreset_tpImporteSinProducto,
    eTipoPreset_tpLitrosSinProducto,
    eTipoPreset_tpLibreSinProducto,
    eTipoPreset_tpImporteConProducto,
    eTipoPreset_tpLitrosConProducto,
    eTipoPreset_tpLibreConProducto
  );

  { TParametros }
  TParametros = class(TROComplexType)
  private
    fFechaIni: DateTime;
    fFechaFin: DateTime;
    fClienteIni: Integer;
    fClienteFin: Integer;
    fEstacion: Integer;
    fTipoMantenimiento: Integer;
    fFactura: Integer;
    fFolioFactura: Integer;
    fSerie: AnsiString;
    fEjercicio: Integer;
    fPeriodo: Integer;
    fDia: Integer;
    fEjercicioFin: Integer;
    fPeriodoFin: Integer;
    fDiaFin: Integer;
    fSecuencia: AnsiString;
    fStatus: AnsiString;
    fAgrupacion: Integer;
    fAlmacen: Integer;
    fSecuenciaIniLiquidacion: Integer;
    fSecuenciaFinLiquidacion: Integer;
    fFacturasdeCompras: Integer;
    fMontoFacturado: Double;
    fTurno: Integer;
    fEmpleadoIni: Integer;
    fEmpleadoFin: Integer;
    fEstacionIni: Integer;
    fEstacionFin: Integer;
    fUsuario: Integer;
    fFacturaIni: Integer;
    fFacturaFin: Integer;
    fIdentificador: Integer;
    fSeleccion: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FechaIni:DateTime read fFechaIni write fFechaIni;
    property FechaFin:DateTime read fFechaFin write fFechaFin;
    property ClienteIni:Integer read fClienteIni write fClienteIni;
    property ClienteFin:Integer read fClienteFin write fClienteFin;
    property Estacion:Integer read fEstacion write fEstacion;
    property TipoMantenimiento:Integer read fTipoMantenimiento write fTipoMantenimiento;
    property Factura:Integer read fFactura write fFactura;
    property FolioFactura:Integer read fFolioFactura write fFolioFactura;
    property Serie:AnsiString read fSerie write fSerie;
    property Ejercicio:Integer read fEjercicio write fEjercicio;
    property Periodo:Integer read fPeriodo write fPeriodo;
    property Dia:Integer read fDia write fDia;
    property EjercicioFin:Integer read fEjercicioFin write fEjercicioFin;
    property PeriodoFin:Integer read fPeriodoFin write fPeriodoFin;
    property DiaFin:Integer read fDiaFin write fDiaFin;
    property Secuencia:AnsiString read fSecuencia write fSecuencia;
    property Status:AnsiString read fStatus write fStatus;
    property Agrupacion:Integer read fAgrupacion write fAgrupacion;
    property Almacen:Integer read fAlmacen write fAlmacen;
    property SecuenciaIniLiquidacion:Integer read fSecuenciaIniLiquidacion write fSecuenciaIniLiquidacion;
    property SecuenciaFinLiquidacion:Integer read fSecuenciaFinLiquidacion write fSecuenciaFinLiquidacion;
    property FacturasdeCompras:Integer read fFacturasdeCompras write fFacturasdeCompras;
    property MontoFacturado:Double read fMontoFacturado write fMontoFacturado;
    property Turno:Integer read fTurno write fTurno;
    property EmpleadoIni:Integer read fEmpleadoIni write fEmpleadoIni;
    property EmpleadoFin:Integer read fEmpleadoFin write fEmpleadoFin;
    property EstacionIni:Integer read fEstacionIni write fEstacionIni;
    property EstacionFin:Integer read fEstacionFin write fEstacionFin;
    property Usuario:Integer read fUsuario write fUsuario;
    property FacturaIni:Integer read fFacturaIni write fFacturaIni;
    property FacturaFin:Integer read fFacturaFin write fFacturaFin;
    property Identificador:Integer read fIdentificador write fIdentificador;
    property Seleccion:Integer read fSeleccion write fSeleccion;
  end;

  { TParametrosCollection }
  TParametrosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TParametros;
    procedure SetItems(aIndex: Integer; const Value: TParametros);
  public
    constructor Create; overload;
    function Add: TParametros; reintroduce;
    property Items[Index: Integer]:TParametros read GetItems write SetItems; default;
  end;

  { TFactura }
  TFactura = class(TROComplexType)
  private
    fSerie: AnsiString;
    fFecha: DateTime;
    fSubtotal: Double;
    fImpuesto: Double;
    fTotal: Double;
    fImpuestoPorcentaje: Double;
    fTurno: Integer;
    fEstacionID: Integer;
    fClienteID: Integer;
    fFormaPagoID: Integer;
    fTipoFacturaID: Integer;
    fFacturaID: Integer;
    fTickets: AnsiString;
    fUsuarioID: Integer;
    fISR: Double;
    fIVARTN: Double;
    fIVA: Double;
    fFolio: Integer;
    fFechaVencimiento: DateTime;
    fFechaImpresion: DateTime;
    fEjercicio: Integer;
    fPeriodo: Integer;
    fSaldo: Double;
    fMovimientoID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Serie:AnsiString read fSerie write fSerie;
    property Fecha:DateTime read fFecha write fFecha;
    property Subtotal:Double read fSubtotal write fSubtotal;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Total:Double read fTotal write fTotal;
    property ImpuestoPorcentaje:Double read fImpuestoPorcentaje write fImpuestoPorcentaje;
    property Turno:Integer read fTurno write fTurno;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property FormaPagoID:Integer read fFormaPagoID write fFormaPagoID;
    property TipoFacturaID:Integer read fTipoFacturaID write fTipoFacturaID;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property Tickets:AnsiString read fTickets write fTickets;
    property UsuarioID:Integer read fUsuarioID write fUsuarioID;
    property ISR:Double read fISR write fISR;
    property IVARTN:Double read fIVARTN write fIVARTN;
    property IVA:Double read fIVA write fIVA;
    property Folio:Integer read fFolio write fFolio;
    property FechaVencimiento:DateTime read fFechaVencimiento write fFechaVencimiento;
    property FechaImpresion:DateTime read fFechaImpresion write fFechaImpresion;
    property Ejercicio:Integer read fEjercicio write fEjercicio;
    property Periodo:Integer read fPeriodo write fPeriodo;
    property Saldo:Double read fSaldo write fSaldo;
    property MovimientoID:Integer read fMovimientoID write fMovimientoID;
  end;

  { TFacturaCollection }
  TFacturaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFactura;
    procedure SetItems(aIndex: Integer; const Value: TFactura);
  public
    constructor Create; overload;
    function Add: TFactura; reintroduce;
    property Items[Index: Integer]:TFactura read GetItems write SetItems; default;
  end;

  { TDetalleFactura }
  TDetalleFactura = class(TROComplexType)
  private
    fCantidad: Double;
    fPrecio: Double;
    fDetalleFacturaId: Integer;
    fImporte: Double;
    fFacturaID: Integer;
    fProductoID: Integer;
    fCodigo: AnsiString;
    fDescripcion: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Cantidad:Double read fCantidad write fCantidad;
    property Precio:Double read fPrecio write fPrecio;
    property DetalleFacturaId:Integer read fDetalleFacturaId write fDetalleFacturaId;
    property Importe:Double read fImporte write fImporte;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property ProductoID:Integer read fProductoID write fProductoID;
    property Codigo:AnsiString read fCodigo write fCodigo;
    property Descripcion:AnsiString read fDescripcion write fDescripcion;
  end;

  { TDetalleFacturaCollection }
  TDetalleFacturaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleFactura;
    procedure SetItems(aIndex: Integer; const Value: TDetalleFactura);
  public
    constructor Create; overload;
    function Add: TDetalleFactura; reintroduce;
    procedure SaveToArray(anArray: ATDetalleFactura);
    procedure LoadFromArray(anArray: ATDetalleFactura);
    property Items[Index: Integer]:TDetalleFactura read GetItems write SetItems; default;
  end;

  { TTicketFactura }
  TTicketFactura = class(TROComplexType)
  private
    fTicketID: Integer;
    fFecha: DateTime;
    fVolumen: Double;
    fPrecio: Double;
    fImporte: Double;
    fProductoID: Integer;
    fFacturaID: Integer;
    fEstacionID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property TicketID:Integer read fTicketID write fTicketID;
    property Fecha:DateTime read fFecha write fFecha;
    property Volumen:Double read fVolumen write fVolumen;
    property Precio:Double read fPrecio write fPrecio;
    property Importe:Double read fImporte write fImporte;
    property ProductoID:Integer read fProductoID write fProductoID;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property EstacionID:Integer read fEstacionID write fEstacionID;
  end;

  { TTicketFacturaCollection }
  TTicketFacturaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTicketFactura;
    procedure SetItems(aIndex: Integer; const Value: TTicketFactura);
  public
    constructor Create; overload;
    function Add: TTicketFactura; reintroduce;
    procedure SaveToArray(anArray: ATTicketFactura);
    procedure LoadFromArray(anArray: ATTicketFactura);
    property Items[Index: Integer]:TTicketFactura read GetItems write SetItems; default;
  end;

  { TDetalleCupon }
  TDetalleCupon = class(TROComplexType)
  private
    fDenominacion: Double;
    fCantidad: Double;
    fReferencia: AnsiString;
    fFacturaID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Denominacion:Double read fDenominacion write fDenominacion;
    property Cantidad:Double read fCantidad write fCantidad;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property FacturaID:Integer read fFacturaID write fFacturaID;
  end;

  { TDetalleCuponCollection }
  TDetalleCuponCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleCupon;
    procedure SetItems(aIndex: Integer; const Value: TDetalleCupon);
  public
    constructor Create; overload;
    function Add: TDetalleCupon; reintroduce;
    procedure SaveToArray(anArray: ATDetalleCupon);
    procedure LoadFromArray(anArray: ATDetalleCupon);
    property Items[Index: Integer]:TDetalleCupon read GetItems write SetItems; default;
  end;

  { TDetalleValeCreditoFactura }
  TDetalleValeCreditoFactura = class(TROComplexType)
  private
    fNoVale: Integer;
    fImporte: Double;
    fFacturaID: Integer;
    fCarteraValeCreditoID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property NoVale:Integer read fNoVale write fNoVale;
    property Importe:Double read fImporte write fImporte;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property CarteraValeCreditoID:Integer read fCarteraValeCreditoID write fCarteraValeCreditoID;
  end;

  { TDetalleValeCreditoFacturaCollection }
  TDetalleValeCreditoFacturaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleValeCreditoFactura;
    procedure SetItems(aIndex: Integer; const Value: TDetalleValeCreditoFactura);
  public
    constructor Create; overload;
    function Add: TDetalleValeCreditoFactura; reintroduce;
    procedure SaveToArray(anArray: ATDetalleValeCreditoFactura);
    procedure LoadFromArray(anArray: ATDetalleValeCreditoFactura);
    property Items[Index: Integer]:TDetalleValeCreditoFactura read GetItems write SetItems; default;
  end;

  { TDatosFactura }
  TDatosFactura = class(TROComplexType)
  private
    fFactura: TFactura;
    fDetalles: ATDetalleFactura;
    fDetalleFactura: ATDetalleFactura;
    fTickets: ATTicketFactura;
    fDetalleCupon: ATDetalleCupon;
    fVales: ATDetalleValeCreditoFactura;
    fValesS: AnsiString;
    fCliente: TDatosCliente;
    function GetFactura: TFactura;
    function GetDetalles: ATDetalleFactura;
    function GetDetalleFactura: ATDetalleFactura;
    function GetTickets: ATTicketFactura;
    function GetDetalleCupon: ATDetalleCupon;
    function GetVales: ATDetalleValeCreditoFactura;
    function GetCliente: TDatosCliente;
  protected
    property int_Factura: TFactura read fFactura;
    property int_Detalles: ATDetalleFactura read fDetalles;
    property int_DetalleFactura: ATDetalleFactura read fDetalleFactura;
    property int_Tickets: ATTicketFactura read fTickets;
    property int_DetalleCupon: ATDetalleCupon read fDetalleCupon;
    property int_Vales: ATDetalleValeCreditoFactura read fVales;
    property int_Cliente: TDatosCliente read fCliente;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Factura:TFactura read GetFactura write fFactura;
    property Detalles:ATDetalleFactura read GetDetalles write fDetalles;
    property DetalleFactura:ATDetalleFactura read GetDetalleFactura write fDetalleFactura;
    property Tickets:ATTicketFactura read GetTickets write fTickets;
    property DetalleCupon:ATDetalleCupon read GetDetalleCupon write fDetalleCupon;
    property Vales:ATDetalleValeCreditoFactura read GetVales write fVales;
    property ValesS:AnsiString read fValesS write fValesS;
    property Cliente:TDatosCliente read GetCliente write fCliente;
  end;

  { TDatosFacturaCollection }
  TDatosFacturaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosFactura;
    procedure SetItems(aIndex: Integer; const Value: TDatosFactura);
  public
    constructor Create; overload;
    function Add: TDatosFactura; reintroduce;
    property Items[Index: Integer]:TDatosFactura read GetItems write SetItems; default;
  end;

  { TLiquidacion }
  TLiquidacion = class(TROComplexType)
  private
    fLiquidacionID: Integer;
    fFecha: DateTime;
    fVentasTotales: Double;
    fAnticipos: Double;
    fDiferencias: Double;
    fSecuencia: Integer;
    fEstacionID: Integer;
    fDespachadorID: Integer;
    fAgrupacionID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property LiquidacionID:Integer read fLiquidacionID write fLiquidacionID;
    property Fecha:DateTime read fFecha write fFecha;
    property VentasTotales:Double read fVentasTotales write fVentasTotales;
    property Anticipos:Double read fAnticipos write fAnticipos;
    property Diferencias:Double read fDiferencias write fDiferencias;
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property DespachadorID:Integer read fDespachadorID write fDespachadorID;
    property AgrupacionID:Integer read fAgrupacionID write fAgrupacionID;
  end;

  { TLiquidacionCollection }
  TLiquidacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TLiquidacion);
  public
    constructor Create; overload;
    function Add: TLiquidacion; reintroduce;
    property Items[Index: Integer]:TLiquidacion read GetItems write SetItems; default;
  end;

  { TLiquidacionDetalle }
  TLiquidacionDetalle = class(TROComplexType)
  private
    fLiquidacionDetalleID: Integer;
    fImporte: Double;
    fSecuencia: Integer;
    fReferencia: AnsiString;
    fTicket: Integer;
    fCupon: AnsiString;
    fLiquidacionID: Integer;
    fTipoValorID: Integer;
    fBancoID: Integer;
    fSalidaID: Integer;
    fClienteID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property LiquidacionDetalleID:Integer read fLiquidacionDetalleID write fLiquidacionDetalleID;
    property Importe:Double read fImporte write fImporte;
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Ticket:Integer read fTicket write fTicket;
    property Cupon:AnsiString read fCupon write fCupon;
    property LiquidacionID:Integer read fLiquidacionID write fLiquidacionID;
    property TipoValorID:Integer read fTipoValorID write fTipoValorID;
    property BancoID:Integer read fBancoID write fBancoID;
    property SalidaID:Integer read fSalidaID write fSalidaID;
    property ClienteID:Integer read fClienteID write fClienteID;
  end;

  { TLiquidacionDetalleCollection }
  TLiquidacionDetalleCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLiquidacionDetalle;
    procedure SetItems(aIndex: Integer; const Value: TLiquidacionDetalle);
  public
    constructor Create; overload;
    function Add: TLiquidacionDetalle; reintroduce;
    procedure SaveToArray(anArray: ATLiquidacionDetalle);
    procedure LoadFromArray(anArray: ATLiquidacionDetalle);
    property Items[Index: Integer]:TLiquidacionDetalle read GetItems write SetItems; default;
  end;

  { TLiquidacionProducto }
  TLiquidacionProducto = class(TROComplexType)
  private
    fPrecioVenta: Double;
    fInventarioInicial: Double;
    fInventarioFinal: Double;
    fCantidad: Double;
    fImporte: Double;
    fSecuencia: Integer;
    fTraspasos: Double;
    fProductoID: Integer;
    fBombaID: Integer;
    fCosto: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property PrecioVenta:Double read fPrecioVenta write fPrecioVenta;
    property InventarioInicial:Double read fInventarioInicial write fInventarioInicial;
    property InventarioFinal:Double read fInventarioFinal write fInventarioFinal;
    property Cantidad:Double read fCantidad write fCantidad;
    property Importe:Double read fImporte write fImporte;
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property Traspasos:Double read fTraspasos write fTraspasos;
    property ProductoID:Integer read fProductoID write fProductoID;
    property BombaID:Integer read fBombaID write fBombaID;
    property Costo:Double read fCosto write fCosto;
  end;

  { TLiquidacionProductoCollection }
  TLiquidacionProductoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLiquidacionProducto;
    procedure SetItems(aIndex: Integer; const Value: TLiquidacionProducto);
  public
    constructor Create; overload;
    function Add: TLiquidacionProducto; reintroduce;
    procedure SaveToArray(anArray: ATLiquidacionProducto);
    procedure LoadFromArray(anArray: ATLiquidacionProducto);
    property Items[Index: Integer]:TLiquidacionProducto read GetItems write SetItems; default;
  end;

  { TTransaccion }
  TTransaccion = class(TROComplexType)
  private
    fTransaccionID: Integer;
    fFolio: Integer;
    fTipo: AnsiString;
    fCredito: Boolean;
    fReferencia: AnsiString;
    fFecha: DateTime;
    fTotal: Double;
    fSubtotal: Double;
    fImpuesto: Double;
    fPlazo: Integer;
    fSecuencia: Integer;
    fEstacionDestinoID: Integer;
    fAlmacenDestinoID: Integer;
    fEstacionID: Integer;
    fProveedorId: Integer;
    fAlmacenID: Integer;
    fMovimientoAlmacen: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property TransaccionID:Integer read fTransaccionID write fTransaccionID;
    property Folio:Integer read fFolio write fFolio;
    property Tipo:AnsiString read fTipo write fTipo;
    property Credito:Boolean read fCredito write fCredito;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Fecha:DateTime read fFecha write fFecha;
    property Total:Double read fTotal write fTotal;
    property Subtotal:Double read fSubtotal write fSubtotal;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Plazo:Integer read fPlazo write fPlazo;
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property EstacionDestinoID:Integer read fEstacionDestinoID write fEstacionDestinoID;
    property AlmacenDestinoID:Integer read fAlmacenDestinoID write fAlmacenDestinoID;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property ProveedorId:Integer read fProveedorId write fProveedorId;
    property AlmacenID:Integer read fAlmacenID write fAlmacenID;
    property MovimientoAlmacen:Integer read fMovimientoAlmacen write fMovimientoAlmacen;
  end;

  { TTransaccionCollection }
  TTransaccionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTransaccion;
    procedure SetItems(aIndex: Integer; const Value: TTransaccion);
  public
    constructor Create; overload;
    function Add: TTransaccion; reintroduce;
    property Items[Index: Integer]:TTransaccion read GetItems write SetItems; default;
  end;

  { TDetalleTransaccion }
  TDetalleTransaccion = class(TROComplexType)
  private
    fDetalleTransaccionID: Integer;
    fCantidad: Double;
    fCosto: Double;
    fTransaccionID: Integer;
    fProductoID: Integer;
    fTipo: AnsiString;
    fOperador: Integer;
    fMovimientoAlmacen: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property DetalleTransaccionID:Integer read fDetalleTransaccionID write fDetalleTransaccionID;
    property Cantidad:Double read fCantidad write fCantidad;
    property Costo:Double read fCosto write fCosto;
    property TransaccionID:Integer read fTransaccionID write fTransaccionID;
    property ProductoID:Integer read fProductoID write fProductoID;
    property Tipo:AnsiString read fTipo write fTipo;
    property Operador:Integer read fOperador write fOperador;
    property MovimientoAlmacen:Integer read fMovimientoAlmacen write fMovimientoAlmacen;
  end;

  { TDetalleTransaccionCollection }
  TDetalleTransaccionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleTransaccion;
    procedure SetItems(aIndex: Integer; const Value: TDetalleTransaccion);
  public
    constructor Create; overload;
    function Add: TDetalleTransaccion; reintroduce;
    procedure SaveToArray(anArray: ATDetalleTransaccion);
    procedure LoadFromArray(anArray: ATDetalleTransaccion);
    property Items[Index: Integer]:TDetalleTransaccion read GetItems write SetItems; default;
  end;

  { TCarteraVale }
  TCarteraVale = class(TROComplexType)
  private
    fCarteraValeCreditoID: Integer;
    fNoVale: Integer;
    fTicket: Integer;
    fImporte: Double;
    fFecha: DateTime;
    fStatus: AnsiString;
    fFacturaID: Integer;
    fEstacionID: Integer;
    fClienteID: Integer;
    fLiquidacionDetalleID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property CarteraValeCreditoID:Integer read fCarteraValeCreditoID write fCarteraValeCreditoID;
    property NoVale:Integer read fNoVale write fNoVale;
    property Ticket:Integer read fTicket write fTicket;
    property Importe:Double read fImporte write fImporte;
    property Fecha:DateTime read fFecha write fFecha;
    property Status:AnsiString read fStatus write fStatus;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property LiquidacionDetalleID:Integer read fLiquidacionDetalleID write fLiquidacionDetalleID;
  end;

  { TCarteraValeCollection }
  TCarteraValeCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCarteraVale;
    procedure SetItems(aIndex: Integer; const Value: TCarteraVale);
  public
    constructor Create; overload;
    function Add: TCarteraVale; reintroduce;
    procedure SaveToArray(anArray: ATCarteraVale);
    procedure LoadFromArray(anArray: ATCarteraVale);
    property Items[Index: Integer]:TCarteraVale read GetItems write SetItems; default;
  end;

  { TDatosLiquidacion }
  TDatosLiquidacion = class(TROComplexType)
  private
    fLiquidacion: TLiquidacion;
    fLiquidacionDetalle: ATLiquidacionDetalle;
    fLiquidacionProducto: ATLiquidacionProducto;
    fLiquidacionCombustible: ATLiquidacionProducto;
    fTraspaso: TTransaccion;
    fVenta: TTransaccion;
    fDetalleTraspaso: ATDetalleTransaccion;
    fDetalleVenta: ATDetalleTransaccion;
    fCarteraVales: ATCarteraVale;
    function GetLiquidacion: TLiquidacion;
    function GetLiquidacionDetalle: ATLiquidacionDetalle;
    function GetLiquidacionProducto: ATLiquidacionProducto;
    function GetLiquidacionCombustible: ATLiquidacionProducto;
    function GetTraspaso: TTransaccion;
    function GetVenta: TTransaccion;
    function GetDetalleTraspaso: ATDetalleTransaccion;
    function GetDetalleVenta: ATDetalleTransaccion;
    function GetCarteraVales: ATCarteraVale;
  protected
    property int_Liquidacion: TLiquidacion read fLiquidacion;
    property int_LiquidacionDetalle: ATLiquidacionDetalle read fLiquidacionDetalle;
    property int_LiquidacionProducto: ATLiquidacionProducto read fLiquidacionProducto;
    property int_LiquidacionCombustible: ATLiquidacionProducto read fLiquidacionCombustible;
    property int_Traspaso: TTransaccion read fTraspaso;
    property int_Venta: TTransaccion read fVenta;
    property int_DetalleTraspaso: ATDetalleTransaccion read fDetalleTraspaso;
    property int_DetalleVenta: ATDetalleTransaccion read fDetalleVenta;
    property int_CarteraVales: ATCarteraVale read fCarteraVales;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Liquidacion:TLiquidacion read GetLiquidacion write fLiquidacion;
    property LiquidacionDetalle:ATLiquidacionDetalle read GetLiquidacionDetalle write fLiquidacionDetalle;
    property LiquidacionProducto:ATLiquidacionProducto read GetLiquidacionProducto write fLiquidacionProducto;
    property LiquidacionCombustible:ATLiquidacionProducto read GetLiquidacionCombustible write fLiquidacionCombustible;
    property Traspaso:TTransaccion read GetTraspaso write fTraspaso;
    property Venta:TTransaccion read GetVenta write fVenta;
    property DetalleTraspaso:ATDetalleTransaccion read GetDetalleTraspaso write fDetalleTraspaso;
    property DetalleVenta:ATDetalleTransaccion read GetDetalleVenta write fDetalleVenta;
    property CarteraVales:ATCarteraVale read GetCarteraVales write fCarteraVales;
  end;

  { TDatosLiquidacionCollection }
  TDatosLiquidacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDatosLiquidacion);
  public
    constructor Create; overload;
    function Add: TDatosLiquidacion; reintroduce;
    property Items[Index: Integer]:TDatosLiquidacion read GetItems write SetItems; default;
  end;

  { TReporte }
  TReporte = class(TROComplexType)
  private
    fSQL1: AnsiString;
    fSQL2: AnsiString;
    fTemplate: AnsiString;
    fCampoJoin: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property SQL1:AnsiString read fSQL1 write fSQL1;
    property SQL2:AnsiString read fSQL2 write fSQL2;
    property Template:AnsiString read fTemplate write fTemplate;
    property CampoJoin:AnsiString read fCampoJoin write fCampoJoin;
  end;

  { TReporteCollection }
  TReporteCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TReporte;
    procedure SetItems(aIndex: Integer; const Value: TReporte);
  public
    constructor Create; overload;
    function Add: TReporte; reintroduce;
    property Items[Index: Integer]:TReporte read GetItems write SetItems; default;
  end;

  { TRecibo }
  TRecibo = class(TROComplexType)
  private
    fSerie: AnsiString;
    fFolio: Integer;
    fFecha: DateTime;
    fSubtotal: Double;
    fEjercicio: Integer;
    fPeriodo: Integer;
    fDia: Integer;
    fImpuesto: Double;
    fTotal: Double;
    fClienteID: Integer;
    fEstacionID: Integer;
    fFormaPagoID: Integer;
    fReciboPagoID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Serie:AnsiString read fSerie write fSerie;
    property Folio:Integer read fFolio write fFolio;
    property Fecha:DateTime read fFecha write fFecha;
    property Subtotal:Double read fSubtotal write fSubtotal;
    property Ejercicio:Integer read fEjercicio write fEjercicio;
    property Periodo:Integer read fPeriodo write fPeriodo;
    property Dia:Integer read fDia write fDia;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Total:Double read fTotal write fTotal;
    property ClienteID:Integer read fClienteID write fClienteID;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property FormaPagoID:Integer read fFormaPagoID write fFormaPagoID;
    property ReciboPagoID:Integer read fReciboPagoID write fReciboPagoID;
  end;

  { TReciboCollection }
  TReciboCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TRecibo;
    procedure SetItems(aIndex: Integer; const Value: TRecibo);
  public
    constructor Create; overload;
    function Add: TRecibo; reintroduce;
    property Items[Index: Integer]:TRecibo read GetItems write SetItems; default;
  end;

  { TDetalleReciboPago }
  TDetalleReciboPago = class(TROComplexType)
  private
    fFacturaID: Integer;
    fTotal: Double;
    fReciboID: Integer;
    fItemNo: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property Total:Double read fTotal write fTotal;
    property ReciboID:Integer read fReciboID write fReciboID;
    property ItemNo:Integer read fItemNo write fItemNo;
  end;

  { TDetalleReciboPagoCollection }
  TDetalleReciboPagoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleReciboPago;
    procedure SetItems(aIndex: Integer; const Value: TDetalleReciboPago);
  public
    constructor Create; overload;
    function Add: TDetalleReciboPago; reintroduce;
    procedure SaveToArray(anArray: ATDetalleRecibo);
    procedure LoadFromArray(anArray: ATDetalleRecibo);
    property Items[Index: Integer]:TDetalleReciboPago read GetItems write SetItems; default;
  end;

  { TDatosReciboPago }
  TDatosReciboPago = class(TROComplexType)
  private
    fRecibo: TRecibo;
    fDetalles: ATDetalleRecibo;
    function GetRecibo: TRecibo;
    function GetDetalles: ATDetalleRecibo;
  protected
    property int_Recibo: TRecibo read fRecibo;
    property int_Detalles: ATDetalleRecibo read fDetalles;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Recibo:TRecibo read GetRecibo write fRecibo;
    property Detalles:ATDetalleRecibo read GetDetalles write fDetalles;
  end;

  { TDatosReciboPagoCollection }
  TDatosReciboPagoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosReciboPago;
    procedure SetItems(aIndex: Integer; const Value: TDatosReciboPago);
  public
    constructor Create; overload;
    function Add: TDatosReciboPago; reintroduce;
    property Items[Index: Integer]:TDatosReciboPago read GetItems write SetItems; default;
  end;

  { TDatosRecibo }
  TDatosRecibo = class(TROComplexType)
  private
    fRecibo: TRecibo;
    fDetalles: ATDetalleRecibo;
    function GetRecibo: TRecibo;
    function GetDetalles: ATDetalleRecibo;
  protected
    property int_Recibo: TRecibo read fRecibo;
    property int_Detalles: ATDetalleRecibo read fDetalles;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Recibo:TRecibo read GetRecibo write fRecibo;
    property Detalles:ATDetalleRecibo read GetDetalles write fDetalles;
  end;

  { TDatosReciboCollection }
  TDatosReciboCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosRecibo;
    procedure SetItems(aIndex: Integer; const Value: TDatosRecibo);
  public
    constructor Create; overload;
    function Add: TDatosRecibo; reintroduce;
    property Items[Index: Integer]:TDatosRecibo read GetItems write SetItems; default;
  end;

  { TLoginInfo }
  TLoginInfo = class(TROComplexType)
  private
    fValida: Boolean;
    fEmpleadoID: Integer;
    fNombreEmpleado: AnsiString;
    fAccesos: ArrayOpcion;
    function GetAccesos: ArrayOpcion;
  protected
    property int_Accesos: ArrayOpcion read fAccesos;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Valida:Boolean read fValida write fValida;
    property EmpleadoID:Integer read fEmpleadoID write fEmpleadoID;
    property NombreEmpleado:AnsiString read fNombreEmpleado write fNombreEmpleado;
    property Accesos:ArrayOpcion read GetAccesos write fAccesos;
  end;

  { TLoginInfoCollection }
  TLoginInfoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLoginInfo;
    procedure SetItems(aIndex: Integer; const Value: TLoginInfo);
  public
    constructor Create; overload;
    function Add: TLoginInfo; reintroduce;
    property Items[Index: Integer]:TLoginInfo read GetItems write SetItems; default;
  end;

  { TOpcion }
  TOpcion = class(TROComplexType)
  private
    fOpcionID: Integer;
    fNombre: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property OpcionID:Integer read fOpcionID write fOpcionID;
    property Nombre:AnsiString read fNombre write fNombre;
  end;

  { TOpcionCollection }
  TOpcionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TOpcion;
    procedure SetItems(aIndex: Integer; const Value: TOpcion);
  public
    constructor Create; overload;
    function Add: TOpcion; reintroduce;
    procedure SaveToArray(anArray: ArrayOpcion);
    procedure LoadFromArray(anArray: ArrayOpcion);
    property Items[Index: Integer]:TOpcion read GetItems write SetItems; default;
  end;

  { TConsumoExpress }
  TConsumoExpress = class(TROComplexType)
  private
    fSecuencia: Integer;
    fEstacionID: Integer;
    fClienteID: Integer;
    fPrecio: Double;
    fVolumen: Double;
    fImporte: Double;
    fFacturado: Boolean;
    fBombaID: Integer;
    fProductoID: Integer;
    fFacturaID: Integer;
    fFechaConsumo: DateTime;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property Precio:Double read fPrecio write fPrecio;
    property Volumen:Double read fVolumen write fVolumen;
    property Importe:Double read fImporte write fImporte;
    property Facturado:Boolean read fFacturado write fFacturado;
    property BombaID:Integer read fBombaID write fBombaID;
    property ProductoID:Integer read fProductoID write fProductoID;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property FechaConsumo:DateTime read fFechaConsumo write fFechaConsumo;
  end;

  { TConsumoExpressCollection }
  TConsumoExpressCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TConsumoExpress;
    procedure SetItems(aIndex: Integer; const Value: TConsumoExpress);
  public
    constructor Create; overload;
    function Add: TConsumoExpress; reintroduce;
    property Items[Index: Integer]:TConsumoExpress read GetItems write SetItems; default;
  end;

  { TDatosCliente }
  TDatosCliente = class(TROComplexType)
  private
    fClienteID: Integer;
    fNombre: AnsiString;
    fRFC: AnsiString;
    fDireccion: AnsiString;
    fTelefono: AnsiString;
    fCP: AnsiString;
    fCiudad: AnsiString;
    fColonia: AnsiString;
    fTelefonos: AnsiString;
    fFacturaExpress: Boolean;
    fSaldo: Double;
    fMsnError: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteID:Integer read fClienteID write fClienteID;
    property Nombre:AnsiString read fNombre write fNombre;
    property RFC:AnsiString read fRFC write fRFC;
    property Direccion:AnsiString read fDireccion write fDireccion;
    property Telefono:AnsiString read fTelefono write fTelefono;
    property CP:AnsiString read fCP write fCP;
    property Ciudad:AnsiString read fCiudad write fCiudad;
    property Colonia:AnsiString read fColonia write fColonia;
    property Telefonos:AnsiString read fTelefonos write fTelefonos;
    property FacturaExpress:Boolean read fFacturaExpress write fFacturaExpress;
    property Saldo:Double read fSaldo write fSaldo;
    property MsnError:AnsiString read fMsnError write fMsnError;
  end;

  { TDatosClienteCollection }
  TDatosClienteCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosCliente;
    procedure SetItems(aIndex: Integer; const Value: TDatosCliente);
  public
    constructor Create; overload;
    function Add: TDatosCliente; reintroduce;
    property Items[Index: Integer]:TDatosCliente read GetItems write SetItems; default;
  end;

  { TFacturaExpress }
  TFacturaExpress = class(TROComplexType)
  private
    fFacturaID: Integer;
    fSerie: AnsiString;
    fFolio: Integer;
    fFecha: DateTime;
    fCliente: TDatosCliente;
    fSubtotal: Double;
    fIVA: Double;
    fTotal: Double;
    fImpuesto: Double;
    fDetalle: ADetalleExpress;
    function GetCliente: TDatosCliente;
    function GetDetalle: ADetalleExpress;
  protected
    property int_Cliente: TDatosCliente read fCliente;
    property int_Detalle: ADetalleExpress read fDetalle;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property Serie:AnsiString read fSerie write fSerie;
    property Folio:Integer read fFolio write fFolio;
    property Fecha:DateTime read fFecha write fFecha;
    property Cliente:TDatosCliente read GetCliente write fCliente;
    property Subtotal:Double read fSubtotal write fSubtotal;
    property IVA:Double read fIVA write fIVA;
    property Total:Double read fTotal write fTotal;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Detalle:ADetalleExpress read GetDetalle write fDetalle;
  end;

  { TFacturaExpressCollection }
  TFacturaExpressCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFacturaExpress;
    procedure SetItems(aIndex: Integer; const Value: TFacturaExpress);
  public
    constructor Create; overload;
    function Add: TFacturaExpress; reintroduce;
    property Items[Index: Integer]:TFacturaExpress read GetItems write SetItems; default;
  end;

  { TDetalleExpress }
  TDetalleExpress = class(TROComplexType)
  private
    fCodigo: AnsiString;
    fDescripcion: AnsiString;
    fCantidad: Double;
    fPrecio: Double;
    fImporte: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Codigo:AnsiString read fCodigo write fCodigo;
    property Descripcion:AnsiString read fDescripcion write fDescripcion;
    property Cantidad:Double read fCantidad write fCantidad;
    property Precio:Double read fPrecio write fPrecio;
    property Importe:Double read fImporte write fImporte;
  end;

  { TDetalleExpressCollection }
  TDetalleExpressCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleExpress;
    procedure SetItems(aIndex: Integer; const Value: TDetalleExpress);
  public
    constructor Create; overload;
    function Add: TDetalleExpress; reintroduce;
    procedure SaveToArray(anArray: ADetalleExpress);
    procedure LoadFromArray(anArray: ADetalleExpress);
    property Items[Index: Integer]:TDetalleExpress read GetItems write SetItems; default;
  end;

  { TValoresTurno }
  TValoresTurno = class(TROComplexType)
  private
    fFecha: DateTime;
    fVenta: Double;
    fEfectivo: Double;
    fCredito: Double;
    fConsumoInterno: Double;
    fCalibraciones: Double;
    fFaltantes: Double;
    fSobrantes: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Fecha:DateTime read fFecha write fFecha;
    property Venta:Double read fVenta write fVenta;
    property Efectivo:Double read fEfectivo write fEfectivo;
    property Credito:Double read fCredito write fCredito;
    property ConsumoInterno:Double read fConsumoInterno write fConsumoInterno;
    property Calibraciones:Double read fCalibraciones write fCalibraciones;
    property Faltantes:Double read fFaltantes write fFaltantes;
    property Sobrantes:Double read fSobrantes write fSobrantes;
  end;

  { TValoresTurnoCollection }
  TValoresTurnoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TValoresTurno;
    procedure SetItems(aIndex: Integer; const Value: TValoresTurno);
  public
    constructor Create; overload;
    function Add: TValoresTurno; reintroduce;
    property Items[Index: Integer]:TValoresTurno read GetItems write SetItems; default;
  end;

  { TOtroProducto }
  TOtroProducto = class(TROComplexType)
  private
    fID: Integer;
    fNombre: AnsiString;
    fCodigo: AnsiString;
    fBarras: AnsiString;
    fPrecio: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ID:Integer read fID write fID;
    property Nombre:AnsiString read fNombre write fNombre;
    property Codigo:AnsiString read fCodigo write fCodigo;
    property Barras:AnsiString read fBarras write fBarras;
    property Precio:Double read fPrecio write fPrecio;
  end;

  { TOtroProductoCollection }
  TOtroProductoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TOtroProducto;
    procedure SetItems(aIndex: Integer; const Value: TOtroProducto);
  public
    constructor Create; overload;
    function Add: TOtroProducto; reintroduce;
    property Items[Index: Integer]:TOtroProducto read GetItems write SetItems; default;
  end;

  { TAgrupacion }
  TAgrupacion = class(TROComplexType)
  private
    fAgrupacionID: Integer;
    fBombas: AAgrupacionBomba;
    function GetBombas: AAgrupacionBomba;
  protected
    property int_Bombas: AAgrupacionBomba read fBombas;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property AgrupacionID:Integer read fAgrupacionID write fAgrupacionID;
    property Bombas:AAgrupacionBomba read GetBombas write fBombas;
  end;

  { TAgrupacionCollection }
  TAgrupacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TAgrupacion;
    procedure SetItems(aIndex: Integer; const Value: TAgrupacion);
  public
    constructor Create; overload;
    function Add: TAgrupacion; reintroduce;
    procedure SaveToArray(anArray: AAgrupacion);
    procedure LoadFromArray(anArray: AAgrupacion);
    property Items[Index: Integer]:TAgrupacion read GetItems write SetItems; default;
  end;

  { TSagarpa }
  TSagarpa = class(TROComplexType)
  private
    fClienteSagarpa: Integer;
    fClienteSagarpaPemex: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteSagarpa:Integer read fClienteSagarpa write fClienteSagarpa;
    property ClienteSagarpaPemex:Integer read fClienteSagarpaPemex write fClienteSagarpaPemex;
  end;

  { TSagarpaCollection }
  TSagarpaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TSagarpa;
    procedure SetItems(aIndex: Integer; const Value: TSagarpa);
  public
    constructor Create; overload;
    function Add: TSagarpa; reintroduce;
    property Items[Index: Integer]:TSagarpa read GetItems write SetItems; default;
  end;

  { TDatosPuntos }
  TDatosPuntos = class(TROComplexType)
  private
    fClienteID: Integer;
    fApellidoPaterno: AnsiString;
    fApellidoMaterno: AnsiString;
    fNombres: AnsiString;
    femail: AnsiString;
    fNacimiento: DateTime;
    fReferencia: AnsiString;
    fTelefonos: AnsiString;
    fFacturaExpress: Boolean;
    fPuntosCategoriaID: Integer;
    fSexo: Integer;
    fFechaRegistro: DateTime;
    fUsuarioID: Integer;
    fPuntosClubID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteID:Integer read fClienteID write fClienteID;
    property ApellidoPaterno:AnsiString read fApellidoPaterno write fApellidoPaterno;
    property ApellidoMaterno:AnsiString read fApellidoMaterno write fApellidoMaterno;
    property Nombres:AnsiString read fNombres write fNombres;
    property email:AnsiString read femail write femail;
    property Nacimiento:DateTime read fNacimiento write fNacimiento;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Telefonos:AnsiString read fTelefonos write fTelefonos;
    property FacturaExpress:Boolean read fFacturaExpress write fFacturaExpress;
    property PuntosCategoriaID:Integer read fPuntosCategoriaID write fPuntosCategoriaID;
    property Sexo:Integer read fSexo write fSexo;
    property FechaRegistro:DateTime read fFechaRegistro write fFechaRegistro;
    property UsuarioID:Integer read fUsuarioID write fUsuarioID;
    property PuntosClubID:Integer read fPuntosClubID write fPuntosClubID;
  end;

  { TDatosPuntosCollection }
  TDatosPuntosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosPuntos;
    procedure SetItems(aIndex: Integer; const Value: TDatosPuntos);
  public
    constructor Create; overload;
    function Add: TDatosPuntos; reintroduce;
    property Items[Index: Integer]:TDatosPuntos read GetItems write SetItems; default;
  end;

  { TDatosCelular }
  TDatosCelular = class(TROComplexType)
  private
    fTelefono: AnsiString;
    fCompania: Integer;
    fDescripcion: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Telefono:AnsiString read fTelefono write fTelefono;
    property Compania:Integer read fCompania write fCompania;
    property Descripcion:AnsiString read fDescripcion write fDescripcion;
  end;

  { TDatosCelularCollection }
  TDatosCelularCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosCelular;
    procedure SetItems(aIndex: Integer; const Value: TDatosCelular);
  public
    constructor Create; overload;
    function Add: TDatosCelular; reintroduce;
    procedure SaveToArray(anArray: ADatosCelular);
    procedure LoadFromArray(anArray: ADatosCelular);
    property Items[Index: Integer]:TDatosCelular read GetItems write SetItems; default;
  end;

  { TEntregaPremio }
  TEntregaPremio = class(TROComplexType)
  private
    fClienteID: Integer;
    fCliente: AnsiString;
    fPremio: AnsiString;
    fCantidad: Integer;
    fPuntos: Double;
    fSaldo: Double;
    fMensaje: AnsiString;
    fValido: Boolean;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteID:Integer read fClienteID write fClienteID;
    property Cliente:AnsiString read fCliente write fCliente;
    property Premio:AnsiString read fPremio write fPremio;
    property Cantidad:Integer read fCantidad write fCantidad;
    property Puntos:Double read fPuntos write fPuntos;
    property Saldo:Double read fSaldo write fSaldo;
    property Mensaje:AnsiString read fMensaje write fMensaje;
    property Valido:Boolean read fValido write fValido;
  end;

  { TEntregaPremioCollection }
  TEntregaPremioCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TEntregaPremio;
    procedure SetItems(aIndex: Integer; const Value: TEntregaPremio);
  public
    constructor Create; overload;
    function Add: TEntregaPremio; reintroduce;
    property Items[Index: Integer]:TEntregaPremio read GetItems write SetItems; default;
  end;

  { TDatosPremio }
  TDatosPremio = class(TROComplexType)
  private
    fPremioID: Integer;
    fNombre: AnsiString;
    fPuntos: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property PremioID:Integer read fPremioID write fPremioID;
    property Nombre:AnsiString read fNombre write fNombre;
    property Puntos:Double read fPuntos write fPuntos;
  end;

  { TDatosPremioCollection }
  TDatosPremioCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosPremio;
    procedure SetItems(aIndex: Integer; const Value: TDatosPremio);
  public
    constructor Create; overload;
    function Add: TDatosPremio; reintroduce;
    property Items[Index: Integer]:TDatosPremio read GetItems write SetItems; default;
  end;

  { TConfirmaEnvioA }
  TConfirmaEnvioA = class(TROComplexType)
  private
    fConsumo: TConsumoExpress;
    fHost: AnsiString;
    fFecha: DateTime;
    function GetConsumo: TConsumoExpress;
  protected
    property int_Consumo: TConsumoExpress read fConsumo;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Consumo:TConsumoExpress read GetConsumo write fConsumo;
    property Host:AnsiString read fHost write fHost;
    property Fecha:DateTime read fFecha write fFecha;
  end;

  { TConfirmaEnvioACollection }
  TConfirmaEnvioACollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TConfirmaEnvioA;
    procedure SetItems(aIndex: Integer; const Value: TConfirmaEnvioA);
  public
    constructor Create; overload;
    function Add: TConfirmaEnvioA; reintroduce;
    property Items[Index: Integer]:TConfirmaEnvioA read GetItems write SetItems; default;
  end;

  { TDatosCerrarLiquidacion }
  TDatosCerrarLiquidacion = class(TROComplexType)
  private
    fLiquidacionID: Integer;
    fVentasTotales: Double;
    fEntregado: Double;
    fDiferencia: Double;
    fDespachadores: ADespachadorLiquidacion;
    fDetalle: ADetalleLiquidacion;
    fTipoValor: ATipoValor;
    fEstacionID: Integer;
    fTurnoID: Integer;
    function GetDespachadores: ADespachadorLiquidacion;
    function GetDetalle: ADetalleLiquidacion;
    function GetTipoValor: ATipoValor;
  protected
    property int_Despachadores: ADespachadorLiquidacion read fDespachadores;
    property int_Detalle: ADetalleLiquidacion read fDetalle;
    property int_TipoValor: ATipoValor read fTipoValor;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property LiquidacionID:Integer read fLiquidacionID write fLiquidacionID;
    property VentasTotales:Double read fVentasTotales write fVentasTotales;
    property Entregado:Double read fEntregado write fEntregado;
    property Diferencia:Double read fDiferencia write fDiferencia;
    property Despachadores:ADespachadorLiquidacion read GetDespachadores write fDespachadores;
    property Detalle:ADetalleLiquidacion read GetDetalle write fDetalle;
    property TipoValor:ATipoValor read GetTipoValor write fTipoValor;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property TurnoID:Integer read fTurnoID write fTurnoID;
  end;

  { TDatosCerrarLiquidacionCollection }
  TDatosCerrarLiquidacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosCerrarLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDatosCerrarLiquidacion);
  public
    constructor Create; overload;
    function Add: TDatosCerrarLiquidacion; reintroduce;
    property Items[Index: Integer]:TDatosCerrarLiquidacion read GetItems write SetItems; default;
  end;

  { TDespachadorLiquidacion }
  TDespachadorLiquidacion = class(TROComplexType)
  private
    fDespachadorLiquidacionID: Integer;
    fDespachadorID: Integer;
    fImporte: Double;
    fEntregado: Double;
    fDiferencia: Double;
    fAgrupacionID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property DespachadorLiquidacionID:Integer read fDespachadorLiquidacionID write fDespachadorLiquidacionID;
    property DespachadorID:Integer read fDespachadorID write fDespachadorID;
    property Importe:Double read fImporte write fImporte;
    property Entregado:Double read fEntregado write fEntregado;
    property Diferencia:Double read fDiferencia write fDiferencia;
    property AgrupacionID:Integer read fAgrupacionID write fAgrupacionID;
  end;

  { TDespachadorLiquidacionCollection }
  TDespachadorLiquidacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDespachadorLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDespachadorLiquidacion);
  public
    constructor Create; overload;
    function Add: TDespachadorLiquidacion; reintroduce;
    procedure SaveToArray(anArray: ADespachadorLiquidacion);
    procedure LoadFromArray(anArray: ADespachadorLiquidacion);
    property Items[Index: Integer]:TDespachadorLiquidacion read GetItems write SetItems; default;
  end;

  { TDetalleLiquidacion }
  TDetalleLiquidacion = class(TROComplexType)
  private
    fCantidad: Double;
    fImporte: Double;
    fReferencia: AnsiString;
    fTicket: Integer;
    fSerie: AnsiString;
    fCuponID: Integer;
    fSalidaID: Integer;
    fClienteID: Integer;
    fBancoID: Integer;
    fProductoID: Integer;
    fAuxiliarID: Integer;
    fDespachadorLiquidacionID: Integer;
    fTipoValorID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Cantidad:Double read fCantidad write fCantidad;
    property Importe:Double read fImporte write fImporte;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Ticket:Integer read fTicket write fTicket;
    property Serie:AnsiString read fSerie write fSerie;
    property CuponID:Integer read fCuponID write fCuponID;
    property SalidaID:Integer read fSalidaID write fSalidaID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property BancoID:Integer read fBancoID write fBancoID;
    property ProductoID:Integer read fProductoID write fProductoID;
    property AuxiliarID:Integer read fAuxiliarID write fAuxiliarID;
    property DespachadorLiquidacionID:Integer read fDespachadorLiquidacionID write fDespachadorLiquidacionID;
    property TipoValorID:Integer read fTipoValorID write fTipoValorID;
  end;

  { TDetalleLiquidacionCollection }
  TDetalleLiquidacionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDetalleLiquidacion);
  public
    constructor Create; overload;
    function Add: TDetalleLiquidacion; reintroduce;
    procedure SaveToArray(anArray: ADetalleLiquidacion);
    procedure LoadFromArray(anArray: ADetalleLiquidacion);
    property Items[Index: Integer]:TDetalleLiquidacion read GetItems write SetItems; default;
  end;

  { TTipoValor }
  TTipoValor = class(TROComplexType)
  private
    fTipoValorID: Integer;
    fNombre: AnsiString;
    fGrupo: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property TipoValorID:Integer read fTipoValorID write fTipoValorID;
    property Nombre:AnsiString read fNombre write fNombre;
    property Grupo:AnsiString read fGrupo write fGrupo;
  end;

  { TTipoValorCollection }
  TTipoValorCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTipoValor;
    procedure SetItems(aIndex: Integer; const Value: TTipoValor);
  public
    constructor Create; overload;
    function Add: TTipoValor; reintroduce;
    procedure SaveToArray(anArray: ATipoValor);
    procedure LoadFromArray(anArray: ATipoValor);
    property Items[Index: Integer]:TTipoValor read GetItems write SetItems; default;
  end;

  { TPerfilPuntos }
  TPerfilPuntos = class(TROComplexType)
  private
    fFechaIni: DateTime;
    fFechaFin: DateTime;
    fCriterio: Double;
    fNewField3: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FechaIni:DateTime read fFechaIni write fFechaIni;
    property FechaFin:DateTime read fFechaFin write fFechaFin;
    property Criterio:Double read fCriterio write fCriterio;
    property NewField3:AnsiString read fNewField3 write fNewField3;
  end;

  { TPerfilPuntosCollection }
  TPerfilPuntosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TPerfilPuntos;
    procedure SetItems(aIndex: Integer; const Value: TPerfilPuntos);
  public
    constructor Create; overload;
    function Add: TPerfilPuntos; reintroduce;
    property Items[Index: Integer]:TPerfilPuntos read GetItems write SetItems; default;
  end;

  { TFacturaElectronica }
  TFacturaElectronica = class(TROComplexType)
  private
    fSerie: AnsiString;
    fFolio: AnsiString;
    fFecha: AnsiString;
    fnoAprobacion: AnsiString;
    fanoAprobacion: AnsiString;
    ftipoComprobante: AnsiString;
    fformaDePago: AnsiString;
    fSubTotal: AnsiString;
    fMunicipioEmisor: AnsiString;
    fTotal: AnsiString;
    fRFCEmisor: AnsiString;
    fNomEmpEmisor: AnsiString;
    fDireccionEm: AnsiString;
    fNoExteriorEm: AnsiString;
    fColoniaEmisor: AnsiString;
    fEstadoEmisor: AnsiString;
    fPaisEmisor: AnsiString;
    fCodigoPostalEmisor: AnsiString;
    fRFCReceptor: AnsiString;
    fNombreReceptor: AnsiString;
    fDomicilioReceptor: AnsiString;
    fNoExteriorReceptor: AnsiString;
    fColoniaReceptor: AnsiString;
    fLocalidadReceptor: AnsiString;
    fMunicipioReceptor: AnsiString;
    fEstadoReceptor: AnsiString;
    fPaisReceptor: AnsiString;
    fCodigoPostalReceptor: AnsiString;
    fFacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes;
    fFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
    fTotalImpuesto: AnsiString;
    fCalleExpedidoEn: AnsiString;
    fNoExterioExpedidoEn: AnsiString;
    fColoniaExpedidoEn: AnsiString;
    fCodigoPostalExpedidoEn: AnsiString;
    fLocalidadExpedidoEn: AnsiString;
    fMunicipioExpedidoEn: AnsiString;
    fEstadoExpedidoEn: AnsiString;
    fPaisExpedidoEn: AnsiString;
    fSucursal: Boolean;
    femail: AnsiString;
    function GetFacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes;
    function GetFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  protected
    property int_FacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes read fFacturaElectronicaDetalleImportes;
    property int_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos read fFacturaElectronicaDetalleImpuestos;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Serie:AnsiString read fSerie write fSerie;
    property Folio:AnsiString read fFolio write fFolio;
    property Fecha:AnsiString read fFecha write fFecha;
    property noAprobacion:AnsiString read fnoAprobacion write fnoAprobacion;
    property anoAprobacion:AnsiString read fanoAprobacion write fanoAprobacion;
    property tipoComprobante:AnsiString read ftipoComprobante write ftipoComprobante;
    property formaDePago:AnsiString read fformaDePago write fformaDePago;
    property SubTotal:AnsiString read fSubTotal write fSubTotal;
    property MunicipioEmisor:AnsiString read fMunicipioEmisor write fMunicipioEmisor;
    property Total:AnsiString read fTotal write fTotal;
    property RFCEmisor:AnsiString read fRFCEmisor write fRFCEmisor;
    property NomEmpEmisor:AnsiString read fNomEmpEmisor write fNomEmpEmisor;
    property DireccionEm:AnsiString read fDireccionEm write fDireccionEm;
    property NoExteriorEm:AnsiString read fNoExteriorEm write fNoExteriorEm;
    property ColoniaEmisor:AnsiString read fColoniaEmisor write fColoniaEmisor;
    property EstadoEmisor:AnsiString read fEstadoEmisor write fEstadoEmisor;
    property PaisEmisor:AnsiString read fPaisEmisor write fPaisEmisor;
    property CodigoPostalEmisor:AnsiString read fCodigoPostalEmisor write fCodigoPostalEmisor;
    property RFCReceptor:AnsiString read fRFCReceptor write fRFCReceptor;
    property NombreReceptor:AnsiString read fNombreReceptor write fNombreReceptor;
    property DomicilioReceptor:AnsiString read fDomicilioReceptor write fDomicilioReceptor;
    property NoExteriorReceptor:AnsiString read fNoExteriorReceptor write fNoExteriorReceptor;
    property ColoniaReceptor:AnsiString read fColoniaReceptor write fColoniaReceptor;
    property LocalidadReceptor:AnsiString read fLocalidadReceptor write fLocalidadReceptor;
    property MunicipioReceptor:AnsiString read fMunicipioReceptor write fMunicipioReceptor;
    property EstadoReceptor:AnsiString read fEstadoReceptor write fEstadoReceptor;
    property PaisReceptor:AnsiString read fPaisReceptor write fPaisReceptor;
    property CodigoPostalReceptor:AnsiString read fCodigoPostalReceptor write fCodigoPostalReceptor;
    property FacturaElectronicaDetalleImportes:ATFacturaElectronicaDetalleImportes read GetFacturaElectronicaDetalleImportes write fFacturaElectronicaDetalleImportes;
    property FacturaElectronicaDetalleImpuestos:ATFacturaElectronicaDetalleImpuestos read GetFacturaElectronicaDetalleImpuestos write fFacturaElectronicaDetalleImpuestos;
    property TotalImpuesto:AnsiString read fTotalImpuesto write fTotalImpuesto;
    property CalleExpedidoEn:AnsiString read fCalleExpedidoEn write fCalleExpedidoEn;
    property NoExterioExpedidoEn:AnsiString read fNoExterioExpedidoEn write fNoExterioExpedidoEn;
    property ColoniaExpedidoEn:AnsiString read fColoniaExpedidoEn write fColoniaExpedidoEn;
    property CodigoPostalExpedidoEn:AnsiString read fCodigoPostalExpedidoEn write fCodigoPostalExpedidoEn;
    property LocalidadExpedidoEn:AnsiString read fLocalidadExpedidoEn write fLocalidadExpedidoEn;
    property MunicipioExpedidoEn:AnsiString read fMunicipioExpedidoEn write fMunicipioExpedidoEn;
    property EstadoExpedidoEn:AnsiString read fEstadoExpedidoEn write fEstadoExpedidoEn;
    property PaisExpedidoEn:AnsiString read fPaisExpedidoEn write fPaisExpedidoEn;
    property Sucursal:Boolean read fSucursal write fSucursal;
    property email:AnsiString read femail write femail;
  end;

  { TFacturaElectronicaCollection }
  TFacturaElectronicaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFacturaElectronica;
    procedure SetItems(aIndex: Integer; const Value: TFacturaElectronica);
  public
    constructor Create; overload;
    function Add: TFacturaElectronica; reintroduce;
    property Items[Index: Integer]:TFacturaElectronica read GetItems write SetItems; default;
  end;

  { TFacturaElectronicaDetalleImportes }
  TFacturaElectronicaDetalleImportes = class(TROComplexType)
  private
    fCantidadReceptor: AnsiString;
    fUnidadReceptor: AnsiString;
    fDescripcionReceptor: AnsiString;
    fValorUnitarioReceptor: AnsiString;
    fImporteReceptor: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property CantidadReceptor:AnsiString read fCantidadReceptor write fCantidadReceptor;
    property UnidadReceptor:AnsiString read fUnidadReceptor write fUnidadReceptor;
    property DescripcionReceptor:AnsiString read fDescripcionReceptor write fDescripcionReceptor;
    property ValorUnitarioReceptor:AnsiString read fValorUnitarioReceptor write fValorUnitarioReceptor;
    property ImporteReceptor:AnsiString read fImporteReceptor write fImporteReceptor;
  end;

  { TFacturaElectronicaDetalleImportesCollection }
  TFacturaElectronicaDetalleImportesCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFacturaElectronicaDetalleImportes;
    procedure SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImportes);
  public
    constructor Create; overload;
    function Add: TFacturaElectronicaDetalleImportes; reintroduce;
    procedure SaveToArray(anArray: ATFacturaElectronicaDetalleImportes);
    procedure LoadFromArray(anArray: ATFacturaElectronicaDetalleImportes);
    property Items[Index: Integer]:TFacturaElectronicaDetalleImportes read GetItems write SetItems; default;
  end;

  { TFacturaElectronicaDetalleImpuestos }
  TFacturaElectronicaDetalleImpuestos = class(TROComplexType)
  private
    fImpuestoReceptor: AnsiString;
    fTasaReceptor: AnsiString;
    fImporteImpReceptor: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ImpuestoReceptor:AnsiString read fImpuestoReceptor write fImpuestoReceptor;
    property TasaReceptor:AnsiString read fTasaReceptor write fTasaReceptor;
    property ImporteImpReceptor:AnsiString read fImporteImpReceptor write fImporteImpReceptor;
  end;

  { TFacturaElectronicaDetalleImpuestosCollection }
  TFacturaElectronicaDetalleImpuestosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFacturaElectronicaDetalleImpuestos;
    procedure SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImpuestos);
  public
    constructor Create; overload;
    function Add: TFacturaElectronicaDetalleImpuestos; reintroduce;
    procedure SaveToArray(anArray: ATFacturaElectronicaDetalleImpuestos);
    procedure LoadFromArray(anArray: ATFacturaElectronicaDetalleImpuestos);
    property Items[Index: Integer]:TFacturaElectronicaDetalleImpuestos read GetItems write SetItems; default;
  end;

  { TDatosFacturaPemex }
  TDatosFacturaPemex = class(TROComplexType)
  private
    fTurnoID: Integer;
    fEstacionID: Integer;
    fFecha: DateTime;
    fNombre: AnsiString;
    fFacturaPemexID: Integer;
    fSerie: AnsiString;
    fFolio: Integer;
    fTotal: Double;
    fDetalleFactura: ADetalleFacturaPemex;
    fProductoID: Integer;
    function GetDetalleFactura: ADetalleFacturaPemex;
  protected
    property int_DetalleFactura: ADetalleFacturaPemex read fDetalleFactura;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property TurnoID:Integer read fTurnoID write fTurnoID;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property Fecha:DateTime read fFecha write fFecha;
    property Nombre:AnsiString read fNombre write fNombre;
    property FacturaPemexID:Integer read fFacturaPemexID write fFacturaPemexID;
    property Serie:AnsiString read fSerie write fSerie;
    property Folio:Integer read fFolio write fFolio;
    property Total:Double read fTotal write fTotal;
    property DetalleFactura:ADetalleFacturaPemex read GetDetalleFactura write fDetalleFactura;
    property ProductoID:Integer read fProductoID write fProductoID;
  end;

  { TDatosFacturaPemexCollection }
  TDatosFacturaPemexCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosFacturaPemex;
    procedure SetItems(aIndex: Integer; const Value: TDatosFacturaPemex);
  public
    constructor Create; overload;
    function Add: TDatosFacturaPemex; reintroduce;
    property Items[Index: Integer]:TDatosFacturaPemex read GetItems write SetItems; default;
  end;

  { TDetalleFacturaPemex }
  TDetalleFacturaPemex = class(TROComplexType)
  private
    fFacturaPemexID: Integer;
    fTipoValorPemexID: Integer;
    fCantidad: Double;
    fImporte: Double;
    fPrecio: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FacturaPemexID:Integer read fFacturaPemexID write fFacturaPemexID;
    property TipoValorPemexID:Integer read fTipoValorPemexID write fTipoValorPemexID;
    property Cantidad:Double read fCantidad write fCantidad;
    property Importe:Double read fImporte write fImporte;
    property Precio:Double read fPrecio write fPrecio;
  end;

  { TDetalleFacturaPemexCollection }
  TDetalleFacturaPemexCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDetalleFacturaPemex;
    procedure SetItems(aIndex: Integer; const Value: TDetalleFacturaPemex);
  public
    constructor Create; overload;
    function Add: TDetalleFacturaPemex; reintroduce;
    procedure SaveToArray(anArray: ADetalleFacturaPemex);
    procedure LoadFromArray(anArray: ADetalleFacturaPemex);
    property Items[Index: Integer]:TDetalleFacturaPemex read GetItems write SetItems; default;
  end;

  { TDataFacturaElectronicaF }
  TDataFacturaElectronicaF = class(TROComplexType)
  private
    fFolio: AnsiString;
    fFecha: AnsiString;
    fnoAprobacion: AnsiString;
    ftipoComprobante: AnsiString;
    fanoAprobacion: AnsiString;
    fFormaPago: AnsiString;
    fSubTotal: AnsiString;
    fTotal: AnsiString;
    fRFCEmisor: AnsiString;
    fNomEmpEmisor: AnsiString;
    fDireccionEm: AnsiString;
    fNoExteriorEm: AnsiString;
    fColoniaEmisor: AnsiString;
    fMunicipioEmisor: AnsiString;
    fEstadoEmisor: AnsiString;
    fPaisEmisor: AnsiString;
    fCodigoPostalEmisor: AnsiString;
    fRFCReceptor: AnsiString;
    fNombreReceptor: AnsiString;
    fDomicilioReceptor: AnsiString;
    fNoExteriorReceptor: AnsiString;
    fColoniaReceptor: AnsiString;
    fLocalidadReceptor: AnsiString;
    fMunicipioReceptor: AnsiString;
    fEstadoReceptor: AnsiString;
    fPaisReceptor: AnsiString;
    fCodigoPostalReceptor: AnsiString;
    fFacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes;
    fFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
    fTotalImpuesto: AnsiString;
    fCalleExpedidoEn: AnsiString;
    fNoExteriorExpedidaEn: AnsiString;
    fColoniaExpedidoEn: AnsiString;
    fCodigoPostalExpedidoEn: AnsiString;
    fLocalidadExpedidoEn: AnsiString;
    fMunicipioExpedidoEn: AnsiString;
    fEstadoExpedidoEn: AnsiString;
    fPaisExpedidoEn: AnsiString;
    fSucursal: Boolean;
    femail: AnsiString;
    function GetFacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes;
    function GetFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  protected
    property int_FacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes read fFacturaElectronicaDetalleImporte;
    property int_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos read fFacturaElectronicaDetalleImpuestos;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Folio:AnsiString read fFolio write fFolio;
    property Fecha:AnsiString read fFecha write fFecha;
    property noAprobacion:AnsiString read fnoAprobacion write fnoAprobacion;
    property tipoComprobante:AnsiString read ftipoComprobante write ftipoComprobante;
    property anoAprobacion:AnsiString read fanoAprobacion write fanoAprobacion;
    property FormaPago:AnsiString read fFormaPago write fFormaPago;
    property SubTotal:AnsiString read fSubTotal write fSubTotal;
    property Total:AnsiString read fTotal write fTotal;
    property RFCEmisor:AnsiString read fRFCEmisor write fRFCEmisor;
    property NomEmpEmisor:AnsiString read fNomEmpEmisor write fNomEmpEmisor;
    property DireccionEm:AnsiString read fDireccionEm write fDireccionEm;
    property NoExteriorEm:AnsiString read fNoExteriorEm write fNoExteriorEm;
    property ColoniaEmisor:AnsiString read fColoniaEmisor write fColoniaEmisor;
    property MunicipioEmisor:AnsiString read fMunicipioEmisor write fMunicipioEmisor;
    property EstadoEmisor:AnsiString read fEstadoEmisor write fEstadoEmisor;
    property PaisEmisor:AnsiString read fPaisEmisor write fPaisEmisor;
    property CodigoPostalEmisor:AnsiString read fCodigoPostalEmisor write fCodigoPostalEmisor;
    property RFCReceptor:AnsiString read fRFCReceptor write fRFCReceptor;
    property NombreReceptor:AnsiString read fNombreReceptor write fNombreReceptor;
    property DomicilioReceptor:AnsiString read fDomicilioReceptor write fDomicilioReceptor;
    property NoExteriorReceptor:AnsiString read fNoExteriorReceptor write fNoExteriorReceptor;
    property ColoniaReceptor:AnsiString read fColoniaReceptor write fColoniaReceptor;
    property LocalidadReceptor:AnsiString read fLocalidadReceptor write fLocalidadReceptor;
    property MunicipioReceptor:AnsiString read fMunicipioReceptor write fMunicipioReceptor;
    property EstadoReceptor:AnsiString read fEstadoReceptor write fEstadoReceptor;
    property PaisReceptor:AnsiString read fPaisReceptor write fPaisReceptor;
    property CodigoPostalReceptor:AnsiString read fCodigoPostalReceptor write fCodigoPostalReceptor;
    property FacturaElectronicaDetalleImporte:ATFacturaElectronicaDetalleImportes read GetFacturaElectronicaDetalleImporte write fFacturaElectronicaDetalleImporte;
    property FacturaElectronicaDetalleImpuestos:ATFacturaElectronicaDetalleImpuestos read GetFacturaElectronicaDetalleImpuestos write fFacturaElectronicaDetalleImpuestos;
    property TotalImpuesto:AnsiString read fTotalImpuesto write fTotalImpuesto;
    property CalleExpedidoEn:AnsiString read fCalleExpedidoEn write fCalleExpedidoEn;
    property NoExteriorExpedidaEn:AnsiString read fNoExteriorExpedidaEn write fNoExteriorExpedidaEn;
    property ColoniaExpedidoEn:AnsiString read fColoniaExpedidoEn write fColoniaExpedidoEn;
    property CodigoPostalExpedidoEn:AnsiString read fCodigoPostalExpedidoEn write fCodigoPostalExpedidoEn;
    property LocalidadExpedidoEn:AnsiString read fLocalidadExpedidoEn write fLocalidadExpedidoEn;
    property MunicipioExpedidoEn:AnsiString read fMunicipioExpedidoEn write fMunicipioExpedidoEn;
    property EstadoExpedidoEn:AnsiString read fEstadoExpedidoEn write fEstadoExpedidoEn;
    property PaisExpedidoEn:AnsiString read fPaisExpedidoEn write fPaisExpedidoEn;
    property Sucursal:Boolean read fSucursal write fSucursal;
    property email:AnsiString read femail write femail;
  end;

  { TDataFacturaElectronicaFCollection }
  TDataFacturaElectronicaFCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDataFacturaElectronicaF;
    procedure SetItems(aIndex: Integer; const Value: TDataFacturaElectronicaF);
  public
    constructor Create; overload;
    function Add: TDataFacturaElectronicaF; reintroduce;
    property Items[Index: Integer]:TDataFacturaElectronicaF read GetItems write SetItems; default;
  end;

  { TGeneraCupon }
  TGeneraCupon = class(TROComplexType)
  private
    fCantidad: Integer;
    fDenominacion: Double;
    fReferencia: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Cantidad:Integer read fCantidad write fCantidad;
    property Denominacion:Double read fDenominacion write fDenominacion;
    property Referencia:AnsiString read fReferencia write fReferencia;
  end;

  { TGeneraCuponCollection }
  TGeneraCuponCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TGeneraCupon;
    procedure SetItems(aIndex: Integer; const Value: TGeneraCupon);
  public
    constructor Create; overload;
    function Add: TGeneraCupon; reintroduce;
    procedure SaveToArray(anArray: AGeneraCupon);
    procedure LoadFromArray(anArray: AGeneraCupon);
    property Items[Index: Integer]:TGeneraCupon read GetItems write SetItems; default;
  end;

  { TDocumentosConSaldo }
  TDocumentosConSaldo = class(TROComplexType)
  private
    fReferencia: AnsiString;
    fImporte: Double;
    fSaldo: Double;
    fFecha: DateTime;
    fVencimiento: DateTime;
    fDocumentoID: Integer;
    fEnRecibo: Double;
    fMovimientoID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Importe:Double read fImporte write fImporte;
    property Saldo:Double read fSaldo write fSaldo;
    property Fecha:DateTime read fFecha write fFecha;
    property Vencimiento:DateTime read fVencimiento write fVencimiento;
    property DocumentoID:Integer read fDocumentoID write fDocumentoID;
    property EnRecibo:Double read fEnRecibo write fEnRecibo;
    property MovimientoID:Integer read fMovimientoID write fMovimientoID;
  end;

  { TDocumentosConSaldoCollection }
  TDocumentosConSaldoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDocumentosConSaldo;
    procedure SetItems(aIndex: Integer; const Value: TDocumentosConSaldo);
  public
    constructor Create; overload;
    function Add: TDocumentosConSaldo; reintroduce;
    procedure SaveToArray(anArray: TDocumentosConSaldoArray);
    procedure LoadFromArray(anArray: TDocumentosConSaldoArray);
    property Items[Index: Integer]:TDocumentosConSaldo read GetItems write SetItems; default;
  end;

  { TAutoriza }
  TAutoriza = class(TROComplexType)
  private
    fEstacionID: Integer;
    fReferencia: AnsiString;
    fDigitoSeguridad: Integer;
    fPosicionCarga: Integer;
    fTerminal: AnsiString;
    fNIP: AnsiString;
    fOdometro: Integer;
    fMagna: Double;
    fPremium: Double;
    fDiesel: Double;
    fSeleccionDeProducto: AnsiString;
    fAutorizacionID: Integer;
    fGasolineroID: Integer;
    fClienteID: Integer;
    fVehiculoID: Integer;
    fCantidad: Double;
    fImporteCantidad: AnsiString;
    fFecha: DateTime;
    fImporte: Double;
    fTipo: Integer;
    fPresetMN: Double;
    fPresetLT: Double;
    fKMS: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property DigitoSeguridad:Integer read fDigitoSeguridad write fDigitoSeguridad;
    property PosicionCarga:Integer read fPosicionCarga write fPosicionCarga;
    property Terminal:AnsiString read fTerminal write fTerminal;
    property NIP:AnsiString read fNIP write fNIP;
    property Odometro:Integer read fOdometro write fOdometro;
    property Magna:Double read fMagna write fMagna;
    property Premium:Double read fPremium write fPremium;
    property Diesel:Double read fDiesel write fDiesel;
    property SeleccionDeProducto:AnsiString read fSeleccionDeProducto write fSeleccionDeProducto;
    property AutorizacionID:Integer read fAutorizacionID write fAutorizacionID;
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property Cantidad:Double read fCantidad write fCantidad;
    property ImporteCantidad:AnsiString read fImporteCantidad write fImporteCantidad;
    property Fecha:DateTime read fFecha write fFecha;
    property Importe:Double read fImporte write fImporte;
    property Tipo:Integer read fTipo write fTipo;
    property PresetMN:Double read fPresetMN write fPresetMN;
    property PresetLT:Double read fPresetLT write fPresetLT;
    property KMS:Integer read fKMS write fKMS;
  end;

  { TAutorizaCollection }
  TAutorizaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TAutoriza;
    procedure SetItems(aIndex: Integer; const Value: TAutoriza);
  public
    constructor Create; overload;
    function Add: TAutoriza; reintroduce;
    property Items[Index: Integer]:TAutoriza read GetItems write SetItems; default;
  end;

  { TCuponValido }
  TCuponValido = class(TROComplexType)
  private
    fCuponID: Integer;
    fImporte: Double;
    fStatus: AnsiString;
    fReferencia: AnsiString;
    fNombre: AnsiString;
    fRazonSocial: AnsiString;
    fDomicilio: AnsiString;
    fRFC: AnsiString;
    fCuentaContable: AnsiString;
    fTarjetaID: Integer;
    fMsnError: AnsiString;
    fValido: Boolean;
    fClienteID: Integer;
    fGasolineroID: Integer;
    fVehiculoID: Integer;
    fGrupo: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property CuponID:Integer read fCuponID write fCuponID;
    property Importe:Double read fImporte write fImporte;
    property Status:AnsiString read fStatus write fStatus;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Nombre:AnsiString read fNombre write fNombre;
    property RazonSocial:AnsiString read fRazonSocial write fRazonSocial;
    property Domicilio:AnsiString read fDomicilio write fDomicilio;
    property RFC:AnsiString read fRFC write fRFC;
    property CuentaContable:AnsiString read fCuentaContable write fCuentaContable;
    property TarjetaID:Integer read fTarjetaID write fTarjetaID;
    property MsnError:AnsiString read fMsnError write fMsnError;
    property Valido:Boolean read fValido write fValido;
    property ClienteID:Integer read fClienteID write fClienteID;
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property Grupo:Integer read fGrupo write fGrupo;
  end;

  { TCuponValidoCollection }
  TCuponValidoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCuponValido;
    procedure SetItems(aIndex: Integer; const Value: TCuponValido);
  public
    constructor Create; overload;
    function Add: TCuponValido; reintroduce;
    property Items[Index: Integer]:TCuponValido read GetItems write SetItems; default;
  end;

  { TCupon }
  TCupon = class(TROComplexType)
  private
    fValido: Boolean;
    fMensaje: AnsiString;
    fNombre: AnsiString;
    fRazonSocial: AnsiString;
    fRFC: AnsiString;
    fReferencia: AnsiString;
    fClienteID: Integer;
    fVehiculoID: Integer;
    fGasolineroID: Integer;
    fGrupo: Integer;
    fImporte: Double;
    fEstacionID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Valido:Boolean read fValido write fValido;
    property Mensaje:AnsiString read fMensaje write fMensaje;
    property Nombre:AnsiString read fNombre write fNombre;
    property RazonSocial:AnsiString read fRazonSocial write fRazonSocial;
    property RFC:AnsiString read fRFC write fRFC;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property Grupo:Integer read fGrupo write fGrupo;
    property Importe:Double read fImporte write fImporte;
    property EstacionID:Integer read fEstacionID write fEstacionID;
  end;

  { TCuponCollection }
  TCuponCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCupon;
    procedure SetItems(aIndex: Integer; const Value: TCupon);
  public
    constructor Create; overload;
    function Add: TCupon; reintroduce;
    property Items[Index: Integer]:TCupon read GetItems write SetItems; default;
  end;

  { TRegistroVenta }
  TRegistroVenta = class(TROComplexType)
  private
    fAvituallamiento: Double;
    fBomba: Integer;
    fCentroCostos: AnsiString;
    fClavePemex: AnsiString;
    fCodigo: AnsiString;
    fComision: Double;
    fCuentaContable: AnsiString;
    fDescripcion: AnsiString;
    fEmpresa: AnsiString;
    fEstacion: Integer;
    fFechaCarga: DateTime;
    fFechaContable: DateTime;
    fFormaCompra: Integer;
    fHora: AnsiString;
    fIdentificador: AnsiString;
    fIdentificacion: AnsiString;
    fImporteServido: Double;
    fImpreso: Integer;
    fImpuesto: Double;
    fKms: Integer;
    fManguera: Integer;
    fMensaje: AnsiString;
    fMovimiento: AnsiString;
    fNip: AnsiString;
    fNoEconomico: AnsiString;
    fNombre: AnsiString;
    fNoTerminal: AnsiString;
    fOdometro: Integer;
    fPrecioServido: Double;
    fPreset: eTipoPreset;
    fRegistro: AnsiString;
    fSaldo: Double;
    fSaldoTarjeta: Double;
    fSecuencia: Integer;
    fSite: AnsiString;
    fTarjeta: AnsiString;
    fTipo: Integer;
    fTipoTarjeta: AnsiString;
    fTipoTrama: AnsiString;
    fTurno: Integer;
    fUnidad: AnsiString;
    fVentaAutorizada: Double;
    fVentaVolumenImporte: AnsiString;
    fVentaExhibida: Double;
    fVolumenServido: Double;
    fGasolineroID: Integer;
    fClienteID: Integer;
    fVehiculoID: Integer;
    fIEPS: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Avituallamiento:Double read fAvituallamiento write fAvituallamiento;
    property Bomba:Integer read fBomba write fBomba;
    property CentroCostos:AnsiString read fCentroCostos write fCentroCostos;
    property ClavePemex:AnsiString read fClavePemex write fClavePemex;
    property Codigo:AnsiString read fCodigo write fCodigo;
    property Comision:Double read fComision write fComision;
    property CuentaContable:AnsiString read fCuentaContable write fCuentaContable;
    property Descripcion:AnsiString read fDescripcion write fDescripcion;
    property Empresa:AnsiString read fEmpresa write fEmpresa;
    property Estacion:Integer read fEstacion write fEstacion;
    property FechaCarga:DateTime read fFechaCarga write fFechaCarga;
    property FechaContable:DateTime read fFechaContable write fFechaContable;
    property FormaCompra:Integer read fFormaCompra write fFormaCompra;
    property Hora:AnsiString read fHora write fHora;
    property Identificador:AnsiString read fIdentificador write fIdentificador;
    property Identificacion:AnsiString read fIdentificacion write fIdentificacion;
    property ImporteServido:Double read fImporteServido write fImporteServido;
    property Impreso:Integer read fImpreso write fImpreso;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Kms:Integer read fKms write fKms;
    property Manguera:Integer read fManguera write fManguera;
    property Mensaje:AnsiString read fMensaje write fMensaje;
    property Movimiento:AnsiString read fMovimiento write fMovimiento;
    property Nip:AnsiString read fNip write fNip;
    property NoEconomico:AnsiString read fNoEconomico write fNoEconomico;
    property Nombre:AnsiString read fNombre write fNombre;
    property NoTerminal:AnsiString read fNoTerminal write fNoTerminal;
    property Odometro:Integer read fOdometro write fOdometro;
    property PrecioServido:Double read fPrecioServido write fPrecioServido;
    property Preset:eTipoPreset read fPreset write fPreset;
    property Registro:AnsiString read fRegistro write fRegistro;
    property Saldo:Double read fSaldo write fSaldo;
    property SaldoTarjeta:Double read fSaldoTarjeta write fSaldoTarjeta;
    property Secuencia:Integer read fSecuencia write fSecuencia;
    property Site:AnsiString read fSite write fSite;
    property Tarjeta:AnsiString read fTarjeta write fTarjeta;
    property Tipo:Integer read fTipo write fTipo;
    property TipoTarjeta:AnsiString read fTipoTarjeta write fTipoTarjeta;
    property TipoTrama:AnsiString read fTipoTrama write fTipoTrama;
    property Turno:Integer read fTurno write fTurno;
    property Unidad:AnsiString read fUnidad write fUnidad;
    property VentaAutorizada:Double read fVentaAutorizada write fVentaAutorizada;
    property VentaVolumenImporte:AnsiString read fVentaVolumenImporte write fVentaVolumenImporte;
    property VentaExhibida:Double read fVentaExhibida write fVentaExhibida;
    property VolumenServido:Double read fVolumenServido write fVolumenServido;
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property IEPS:Double read fIEPS write fIEPS;
  end;

  { TRegistroVentaCollection }
  TRegistroVentaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TRegistroVenta;
    procedure SetItems(aIndex: Integer; const Value: TRegistroVenta);
  public
    constructor Create; overload;
    function Add: TRegistroVenta; reintroduce;
    procedure SaveToArray(anArray: aRegistroVenta);
    procedure LoadFromArray(anArray: aRegistroVenta);
    property Items[Index: Integer]:TRegistroVenta read GetItems write SetItems; default;
  end;

  { TConsumo }
  TConsumo = class(TROComplexType)
  private
    fSecuenciaVenta: Integer;
    fSerie: AnsiString;
    fEstacionID: Integer;
    fFechaMovimiento: DateTime;
    fFechaCarga: DateTime;
    fEjercicio: Integer;
    fPeriodo: Integer;
    fDia: Integer;
    fTurno: Integer;
    fKilometraje: Integer;
    fReferencia: AnsiString;
    fTarjeta: AnsiString;
    fPosicionCarga: Integer;
    fCantidad: Double;
    fPrecio: Double;
    fImporte: Double;
    fImpuestoImporte: Double;
    fImpuestoPorcentaje: Double;
    fCuentaContable: AnsiString;
    fManualAutomatico: AnsiString;
    fFacturado: Integer;
    fEnIngresos: Integer;
    fAuditado: Integer;
    fFacturaID: Integer;
    fProductoID: Integer;
    fGasolineroID: Integer;
    fClienteID: Integer;
    fIEPS: Double;
    fUsuarioID: Integer;
    fVehiculoID: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property SecuenciaVenta:Integer read fSecuenciaVenta write fSecuenciaVenta;
    property Serie:AnsiString read fSerie write fSerie;
    property EstacionID:Integer read fEstacionID write fEstacionID;
    property FechaMovimiento:DateTime read fFechaMovimiento write fFechaMovimiento;
    property FechaCarga:DateTime read fFechaCarga write fFechaCarga;
    property Ejercicio:Integer read fEjercicio write fEjercicio;
    property Periodo:Integer read fPeriodo write fPeriodo;
    property Dia:Integer read fDia write fDia;
    property Turno:Integer read fTurno write fTurno;
    property Kilometraje:Integer read fKilometraje write fKilometraje;
    property Referencia:AnsiString read fReferencia write fReferencia;
    property Tarjeta:AnsiString read fTarjeta write fTarjeta;
    property PosicionCarga:Integer read fPosicionCarga write fPosicionCarga;
    property Cantidad:Double read fCantidad write fCantidad;
    property Precio:Double read fPrecio write fPrecio;
    property Importe:Double read fImporte write fImporte;
    property ImpuestoImporte:Double read fImpuestoImporte write fImpuestoImporte;
    property ImpuestoPorcentaje:Double read fImpuestoPorcentaje write fImpuestoPorcentaje;
    property CuentaContable:AnsiString read fCuentaContable write fCuentaContable;
    property ManualAutomatico:AnsiString read fManualAutomatico write fManualAutomatico;
    property Facturado:Integer read fFacturado write fFacturado;
    property EnIngresos:Integer read fEnIngresos write fEnIngresos;
    property Auditado:Integer read fAuditado write fAuditado;
    property FacturaID:Integer read fFacturaID write fFacturaID;
    property ProductoID:Integer read fProductoID write fProductoID;
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property IEPS:Double read fIEPS write fIEPS;
    property UsuarioID:Integer read fUsuarioID write fUsuarioID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
  end;

  { TConsumoCollection }
  TConsumoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TConsumo;
    procedure SetItems(aIndex: Integer; const Value: TConsumo);
  public
    constructor Create; overload;
    function Add: TConsumo; reintroduce;
    procedure SaveToArray(anArray: aConsumo);
    procedure LoadFromArray(anArray: aConsumo);
    property Items[Index: Integer]:TConsumo read GetItems write SetItems; default;
  end;

  { TTarjeta }
  TTarjeta = class(TROComplexType)
  private
    fGasolineroID: Integer;
    fClienteID: Integer;
    fVehiculoID: Integer;
    fCantidad: Double;
    fLitrosImporte: AnsiString;
    fMsnError: AnsiString;
    fStatus: Integer;
    fValido: Boolean;
    fNombre: AnsiString;
    fRazonSocial: AnsiString;
    fRFC: AnsiString;
    fIdentificacion: AnsiString;
    fSaldo: Double;
    fMensaje: AnsiString;
    fCentroCostos: AnsiString;
    fClasificacion: AnsiString;
    fProductos: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property GasolineroID:Integer read fGasolineroID write fGasolineroID;
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property Cantidad:Double read fCantidad write fCantidad;
    property LitrosImporte:AnsiString read fLitrosImporte write fLitrosImporte;
    property MsnError:AnsiString read fMsnError write fMsnError;
    property Status:Integer read fStatus write fStatus;
    property Valido:Boolean read fValido write fValido;
    property Nombre:AnsiString read fNombre write fNombre;
    property RazonSocial:AnsiString read fRazonSocial write fRazonSocial;
    property RFC:AnsiString read fRFC write fRFC;
    property Identificacion:AnsiString read fIdentificacion write fIdentificacion;
    property Saldo:Double read fSaldo write fSaldo;
    property Mensaje:AnsiString read fMensaje write fMensaje;
    property CentroCostos:AnsiString read fCentroCostos write fCentroCostos;
    property Clasificacion:AnsiString read fClasificacion write fClasificacion;
    property Productos:AnsiString read fProductos write fProductos;
  end;

  { TTarjetaCollection }
  TTarjetaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTarjeta;
    procedure SetItems(aIndex: Integer; const Value: TTarjeta);
  public
    constructor Create; overload;
    function Add: TTarjeta; reintroduce;
    property Items[Index: Integer]:TTarjeta read GetItems write SetItems; default;
  end;

  { TTarjetaManual }
  TTarjetaManual = class(TROComplexType)
  private
    fClienteID: Integer;
    fVehiculoID: Integer;
    fClienteNombre: AnsiString;
    fReferencia: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property ClienteNombre:AnsiString read fClienteNombre write fClienteNombre;
    property Referencia:AnsiString read fReferencia write fReferencia;
  end;

  { TTarjetaManualCollection }
  TTarjetaManualCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTarjetaManual;
    procedure SetItems(aIndex: Integer; const Value: TTarjetaManual);
  public
    constructor Create; overload;
    function Add: TTarjetaManual; reintroduce;
    property Items[Index: Integer]:TTarjetaManual read GetItems write SetItems; default;
  end;

  { TNodo }
  TNodo = class(TROComplexType)
  private
    fNombre: AnsiString;
    fTipo: Integer;
    fGasolinero: Integer;
    fHost: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Nombre:AnsiString read fNombre write fNombre;
    property Tipo:Integer read fTipo write fTipo;
    property Gasolinero:Integer read fGasolinero write fGasolinero;
    property Host:AnsiString read fHost write fHost;
  end;

  { TNodoCollection }
  TNodoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TNodo;
    procedure SetItems(aIndex: Integer; const Value: TNodo);
  public
    constructor Create; overload;
    function Add: TNodo; reintroduce;
    procedure SaveToArray(anArray: aNodos);
    procedure LoadFromArray(anArray: aNodos);
    property Items[Index: Integer]:TNodo read GetItems write SetItems; default;
  end;

  { TDatosVehiculo }
  TDatosVehiculo = class(TROComplexType)
  private
    fClienteID: Integer;
    fVehiculoID: Integer;
    fClienteNombre: AnsiString;
    fReferencia: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ClienteID:Integer read fClienteID write fClienteID;
    property VehiculoID:Integer read fVehiculoID write fVehiculoID;
    property ClienteNombre:AnsiString read fClienteNombre write fClienteNombre;
    property Referencia:AnsiString read fReferencia write fReferencia;
  end;

  { TDatosVehiculoCollection }
  TDatosVehiculoCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosVehiculo;
    procedure SetItems(aIndex: Integer; const Value: TDatosVehiculo);
  public
    constructor Create; overload;
    function Add: TDatosVehiculo; reintroduce;
    property Items[Index: Integer]:TDatosVehiculo read GetItems write SetItems; default;
  end;

  { TDatosVerifica }
  TDatosVerifica = class(TROComplexType)
  private
    fAfectarHistorico: Boolean;
    fKMS: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property AfectarHistorico:Boolean read fAfectarHistorico write fAfectarHistorico;
    property KMS:Integer read fKMS write fKMS;
  end;

  { TDatosVerificaCollection }
  TDatosVerificaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDatosVerifica;
    procedure SetItems(aIndex: Integer; const Value: TDatosVerifica);
  public
    constructor Create; overload;
    function Add: TDatosVerifica; reintroduce;
    property Items[Index: Integer]:TDatosVerifica read GetItems write SetItems; default;
  end;

  { TGlobal }
  TGlobal = class(TROComplexType)
  private
    fAfectarDecimal: Boolean;
    fAfectarLitros: Boolean;
    fAutorizacionPorManguera: Boolean;
    fblnProdArriba: Boolean;
    fbolStrop: Boolean;
    fBombas: aBombas;
    fcAck: AnsiString;
    fCambioPrecio: TCambio;
    fcCR: AnsiString;
    fcEsc: AnsiString;
    fcExt: AnsiString;
    fcLF: AnsiString;
    fcNak: AnsiString;
    fConexiones: aDispositivos;
    fCortarSuministrosSinAut: Boolean;
    feStx: AnsiString;
    fcUsuario: AnsiString;
    fDispositivos: aDispositivos;
    fEstacion: Integer;
    fFabricante: eFabricante;
    fFlujoActual: Integer;
    fGasolinas: aGas;
    fidNivelUsuario: Integer;
    fidUsuario: Integer;
    flPoleoHora: Boolean;
    fMaquina: AnsiString;
    fMetodoOperacion: Integer;
    fMetodoTurno: Integer;
    fMonitoreo: eMonitoreo;
    fMSG_CISTEMGAS: Integer;
    fNombreEstacion: AnsiString;
    fNumPuerto: Integer;
    fNumSite: AnsiString;
    fnVueltasGenerales: Integer;
    fPermitirCorteSinVenta: Boolean;
    fPrecioInferior: Double;
    fPrecioSuperior: Double;
    fProtecciones: TProtecciones;
    fRegistroVenta: Integer;
    fRespuesta: AnsiString;
    fRFC: AnsiString;
    fSIIC: AnsiString;
    fSinAfectar: Boolean;
    fStatusCtrolVol: Integer;
    fStrCmdPendiente: AnsiString;
    fTanques: aTanques;
    fTelemedicion: Integer;
    fTipoEstacion: Integer;
    fTotalBombas: Integer;
    fTotalCiclos: Integer;
    fTurnoActual: Integer;
    fTurnosActuales: Integer;
    fVentaSeleccionada: Integer;
    fLicencia: TLicencia;
    function GetBombas: aBombas;
    function GetCambioPrecio: TCambio;
    function GetConexiones: aDispositivos;
    function GetDispositivos: aDispositivos;
    function GetGasolinas: aGas;
    function GetProtecciones: TProtecciones;
    function GetTanques: aTanques;
    function GetLicencia: TLicencia;
  protected
    property int_Bombas: aBombas read fBombas;
    property int_CambioPrecio: TCambio read fCambioPrecio;
    property int_Conexiones: aDispositivos read fConexiones;
    property int_Dispositivos: aDispositivos read fDispositivos;
    property int_Gasolinas: aGas read fGasolinas;
    property int_Protecciones: TProtecciones read fProtecciones;
    property int_Tanques: aTanques read fTanques;
    property int_Licencia: TLicencia read fLicencia;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property AfectarDecimal:Boolean read fAfectarDecimal write fAfectarDecimal;
    property AfectarLitros:Boolean read fAfectarLitros write fAfectarLitros;
    property AutorizacionPorManguera:Boolean read fAutorizacionPorManguera write fAutorizacionPorManguera;
    property blnProdArriba:Boolean read fblnProdArriba write fblnProdArriba;
    property bolStrop:Boolean read fbolStrop write fbolStrop;
    property Bombas:aBombas read GetBombas write fBombas;
    property cAck:AnsiString read fcAck write fcAck;
    property CambioPrecio:TCambio read GetCambioPrecio write fCambioPrecio;
    property cCR:AnsiString read fcCR write fcCR;
    property cEsc:AnsiString read fcEsc write fcEsc;
    property cExt:AnsiString read fcExt write fcExt;
    property cLF:AnsiString read fcLF write fcLF;
    property cNak:AnsiString read fcNak write fcNak;
    property Conexiones:aDispositivos read GetConexiones write fConexiones;
    property CortarSuministrosSinAut:Boolean read fCortarSuministrosSinAut write fCortarSuministrosSinAut;
    property eStx:AnsiString read feStx write feStx;
    property cUsuario:AnsiString read fcUsuario write fcUsuario;
    property Dispositivos:aDispositivos read GetDispositivos write fDispositivos;
    property Estacion:Integer read fEstacion write fEstacion;
    property Fabricante:eFabricante read fFabricante write fFabricante;
    property FlujoActual:Integer read fFlujoActual write fFlujoActual;
    property Gasolinas:aGas read GetGasolinas write fGasolinas;
    property idNivelUsuario:Integer read fidNivelUsuario write fidNivelUsuario;
    property idUsuario:Integer read fidUsuario write fidUsuario;
    property lPoleoHora:Boolean read flPoleoHora write flPoleoHora;
    property Maquina:AnsiString read fMaquina write fMaquina;
    property MetodoOperacion:Integer read fMetodoOperacion write fMetodoOperacion;
    property MetodoTurno:Integer read fMetodoTurno write fMetodoTurno;
    property Monitoreo:eMonitoreo read fMonitoreo write fMonitoreo;
    property MSG_CISTEMGAS:Integer read fMSG_CISTEMGAS write fMSG_CISTEMGAS;
    property NombreEstacion:AnsiString read fNombreEstacion write fNombreEstacion;
    property NumPuerto:Integer read fNumPuerto write fNumPuerto;
    property NumSite:AnsiString read fNumSite write fNumSite;
    property nVueltasGenerales:Integer read fnVueltasGenerales write fnVueltasGenerales;
    property PermitirCorteSinVenta:Boolean read fPermitirCorteSinVenta write fPermitirCorteSinVenta;
    property PrecioInferior:Double read fPrecioInferior write fPrecioInferior;
    property PrecioSuperior:Double read fPrecioSuperior write fPrecioSuperior;
    property Protecciones:TProtecciones read GetProtecciones write fProtecciones;
    property RegistroVenta:Integer read fRegistroVenta write fRegistroVenta;
    property Respuesta:AnsiString read fRespuesta write fRespuesta;
    property RFC:AnsiString read fRFC write fRFC;
    property SIIC:AnsiString read fSIIC write fSIIC;
    property SinAfectar:Boolean read fSinAfectar write fSinAfectar;
    property StatusCtrolVol:Integer read fStatusCtrolVol write fStatusCtrolVol;
    property StrCmdPendiente:AnsiString read fStrCmdPendiente write fStrCmdPendiente;
    property Tanques:aTanques read GetTanques write fTanques;
    property Telemedicion:Integer read fTelemedicion write fTelemedicion;
    property TipoEstacion:Integer read fTipoEstacion write fTipoEstacion;
    property TotalBombas:Integer read fTotalBombas write fTotalBombas;
    property TotalCiclos:Integer read fTotalCiclos write fTotalCiclos;
    property TurnoActual:Integer read fTurnoActual write fTurnoActual;
    property TurnosActuales:Integer read fTurnosActuales write fTurnosActuales;
    property VentaSeleccionada:Integer read fVentaSeleccionada write fVentaSeleccionada;
    property Licencia:TLicencia read GetLicencia write fLicencia;
  end;

  { TGlobalCollection }
  TGlobalCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TGlobal;
    procedure SetItems(aIndex: Integer; const Value: TGlobal);
  public
    constructor Create; overload;
    function Add: TGlobal; reintroduce;
    property Items[Index: Integer]:TGlobal read GetItems write SetItems; default;
  end;

  { TBomba }
  TBomba = class(TROComplexType)
  private
    fAutoImprimir: Boolean;
    fAutorizacion: eTipoAutorizacion;
    fAutorizacionAutomatica: Integer;
    fClasificacion: AnsiString;
    fCodigoProducto: eCodigoProducto;
    fConectLCD: Integer;
    fDespachador: Integer;
    fDobleTarjeta: Boolean;
    fEstado: eEstadoBomba;
    fEstadoNuevo: eEstadoBomba;
    fEstadoVenta: eEstadoVenta;
    fFactorDecimal: TFactorDecimal;
    fFaltaPago: aBoolean;
    fFlujo: TFlujos;
    fGasolina: Integer;
    fGuardando: Boolean;
    fImporteMinimo: Double;
    fIntentoPrepago: Boolean;
    fLecturasFinales: aLecturas;
    fLecturasIniciales: aLecturas;
    fLecturasTurno: aLecturas;
    fMangueras: aMaguera;
    fMaximoLecturasImporte: Double;
    fMaximoLecturasVolumen: Double;
    fModo: eModoOperacion;
    fNoMangueras: Integer;
    fNombreImpresora: AnsiString;
    fNuevoFlujo: AnsiString;
    fPendiente: Boolean;
    fPendienteGrabar: Boolean;
    fPendientesAplicar: aContador;
    fPosicionamiento: TCanal;
    fPreset: eTipoPreset;
    fProceso: eProceso;
    fPuntosRegistrados: Boolean;
    fPuntoVenta: TRegistroPuntoVenta;
    fRegresaModo: Boolean;
    fServicio: Double;
    fTicket: eImpresionTicket;
    fTiempoAutorizado: Integer;
    fTipoDispensario: Integer;
    fTipoTarjeta: Integer;
    fTotalizacionAutomatica: Integer;
    fTurnoID: Integer;
    fUltimoFlujo: AnsiString;
    fUltimoLimiteImporte: Double;
    fUltimoLimiteVolumen: Double;
    fVentaProceso: TRegistroVenta;
    fVentaUltima: TRegistroVenta;
    fVentaPenultima: TRegistroVenta;
    fVentaAntePenultima: TRegistroVenta;
    fVentasTurno: aContador;
    fHost: AnsiString;
    function GetFactorDecimal: TFactorDecimal;
    function GetFaltaPago: aBoolean;
    function GetFlujo: TFlujos;
    function GetLecturasFinales: aLecturas;
    function GetLecturasIniciales: aLecturas;
    function GetLecturasTurno: aLecturas;
    function GetMangueras: aMaguera;
    function GetPendientesAplicar: aContador;
    function GetPosicionamiento: TCanal;
    function GetPuntoVenta: TRegistroPuntoVenta;
    function GetVentaProceso: TRegistroVenta;
    function GetVentaUltima: TRegistroVenta;
    function GetVentaPenultima: TRegistroVenta;
    function GetVentaAntePenultima: TRegistroVenta;
    function GetVentasTurno: aContador;
  protected
    property int_FactorDecimal: TFactorDecimal read fFactorDecimal;
    property int_FaltaPago: aBoolean read fFaltaPago;
    property int_Flujo: TFlujos read fFlujo;
    property int_LecturasFinales: aLecturas read fLecturasFinales;
    property int_LecturasIniciales: aLecturas read fLecturasIniciales;
    property int_LecturasTurno: aLecturas read fLecturasTurno;
    property int_Mangueras: aMaguera read fMangueras;
    property int_PendientesAplicar: aContador read fPendientesAplicar;
    property int_Posicionamiento: TCanal read fPosicionamiento;
    property int_PuntoVenta: TRegistroPuntoVenta read fPuntoVenta;
    property int_VentaProceso: TRegistroVenta read fVentaProceso;
    property int_VentaUltima: TRegistroVenta read fVentaUltima;
    property int_VentaPenultima: TRegistroVenta read fVentaPenultima;
    property int_VentaAntePenultima: TRegistroVenta read fVentaAntePenultima;
    property int_VentasTurno: aContador read fVentasTurno;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property AutoImprimir:Boolean read fAutoImprimir write fAutoImprimir;
    property Autorizacion:eTipoAutorizacion read fAutorizacion write fAutorizacion;
    property AutorizacionAutomatica:Integer read fAutorizacionAutomatica write fAutorizacionAutomatica;
    property Clasificacion:AnsiString read fClasificacion write fClasificacion;
    property CodigoProducto:eCodigoProducto read fCodigoProducto write fCodigoProducto;
    property ConectLCD:Integer read fConectLCD write fConectLCD;
    property Despachador:Integer read fDespachador write fDespachador;
    property DobleTarjeta:Boolean read fDobleTarjeta write fDobleTarjeta;
    property Estado:eEstadoBomba read fEstado write fEstado;
    property EstadoNuevo:eEstadoBomba read fEstadoNuevo write fEstadoNuevo;
    property EstadoVenta:eEstadoVenta read fEstadoVenta write fEstadoVenta;
    property FactorDecimal:TFactorDecimal read GetFactorDecimal write fFactorDecimal;
    property FaltaPago:aBoolean read GetFaltaPago write fFaltaPago;
    property Flujo:TFlujos read GetFlujo write fFlujo;
    property Gasolina:Integer read fGasolina write fGasolina;
    property Guardando:Boolean read fGuardando write fGuardando;
    property ImporteMinimo:Double read fImporteMinimo write fImporteMinimo;
    property IntentoPrepago:Boolean read fIntentoPrepago write fIntentoPrepago;
    property LecturasFinales:aLecturas read GetLecturasFinales write fLecturasFinales;
    property LecturasIniciales:aLecturas read GetLecturasIniciales write fLecturasIniciales;
    property LecturasTurno:aLecturas read GetLecturasTurno write fLecturasTurno;
    property Mangueras:aMaguera read GetMangueras write fMangueras;
    property MaximoLecturasImporte:Double read fMaximoLecturasImporte write fMaximoLecturasImporte;
    property MaximoLecturasVolumen:Double read fMaximoLecturasVolumen write fMaximoLecturasVolumen;
    property Modo:eModoOperacion read fModo write fModo;
    property NoMangueras:Integer read fNoMangueras write fNoMangueras;
    property NombreImpresora:AnsiString read fNombreImpresora write fNombreImpresora;
    property NuevoFlujo:AnsiString read fNuevoFlujo write fNuevoFlujo;
    property Pendiente:Boolean read fPendiente write fPendiente;
    property PendienteGrabar:Boolean read fPendienteGrabar write fPendienteGrabar;
    property PendientesAplicar:aContador read GetPendientesAplicar write fPendientesAplicar;
    property Posicionamiento:TCanal read GetPosicionamiento write fPosicionamiento;
    property Preset:eTipoPreset read fPreset write fPreset;
    property Proceso:eProceso read fProceso write fProceso;
    property PuntosRegistrados:Boolean read fPuntosRegistrados write fPuntosRegistrados;
    property PuntoVenta:TRegistroPuntoVenta read GetPuntoVenta write fPuntoVenta;
    property RegresaModo:Boolean read fRegresaModo write fRegresaModo;
    property Servicio:Double read fServicio write fServicio;
    property Ticket:eImpresionTicket read fTicket write fTicket;
    property TiempoAutorizado:Integer read fTiempoAutorizado write fTiempoAutorizado;
    property TipoDispensario:Integer read fTipoDispensario write fTipoDispensario;
    property TipoTarjeta:Integer read fTipoTarjeta write fTipoTarjeta;
    property TotalizacionAutomatica:Integer read fTotalizacionAutomatica write fTotalizacionAutomatica;
    property TurnoID:Integer read fTurnoID write fTurnoID;
    property UltimoFlujo:AnsiString read fUltimoFlujo write fUltimoFlujo;
    property UltimoLimiteImporte:Double read fUltimoLimiteImporte write fUltimoLimiteImporte;
    property UltimoLimiteVolumen:Double read fUltimoLimiteVolumen write fUltimoLimiteVolumen;
    property VentaProceso:TRegistroVenta read GetVentaProceso write fVentaProceso;
    property VentaUltima:TRegistroVenta read GetVentaUltima write fVentaUltima;
    property VentaPenultima:TRegistroVenta read GetVentaPenultima write fVentaPenultima;
    property VentaAntePenultima:TRegistroVenta read GetVentaAntePenultima write fVentaAntePenultima;
    property VentasTurno:aContador read GetVentasTurno write fVentasTurno;
    property Host:AnsiString read fHost write fHost;
  end;

  { TBombaCollection }
  TBombaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TBomba;
    procedure SetItems(aIndex: Integer; const Value: TBomba);
  public
    constructor Create; overload;
    function Add: TBomba; reintroduce;
    procedure SaveToArray(anArray: aBombas);
    procedure LoadFromArray(anArray: aBombas);
    property Items[Index: Integer]:TBomba read GetItems write SetItems; default;
  end;

  { TFactorDecimal }
  TFactorDecimal = class(TROComplexType)
  private
    fCambioPrecios: Double;
    fVentaPrecio: Double;
    fVentaVolumen: Double;
    fVentaImporte: Double;
    fLecturasVolumen: Double;
    fLecturasImporte: Double;
    fDespliegaVolumen: Double;
    fDespliegaImporte: Double;
    fPrefijarVolumen: Double;
    fPrefijarImporte: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property CambioPrecios:Double read fCambioPrecios write fCambioPrecios;
    property VentaPrecio:Double read fVentaPrecio write fVentaPrecio;
    property VentaVolumen:Double read fVentaVolumen write fVentaVolumen;
    property VentaImporte:Double read fVentaImporte write fVentaImporte;
    property LecturasVolumen:Double read fLecturasVolumen write fLecturasVolumen;
    property LecturasImporte:Double read fLecturasImporte write fLecturasImporte;
    property DespliegaVolumen:Double read fDespliegaVolumen write fDespliegaVolumen;
    property DespliegaImporte:Double read fDespliegaImporte write fDespliegaImporte;
    property PrefijarVolumen:Double read fPrefijarVolumen write fPrefijarVolumen;
    property PrefijarImporte:Double read fPrefijarImporte write fPrefijarImporte;
  end;

  { TFactorDecimalCollection }
  TFactorDecimalCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFactorDecimal;
    procedure SetItems(aIndex: Integer; const Value: TFactorDecimal);
  public
    constructor Create; overload;
    function Add: TFactorDecimal; reintroduce;
    property Items[Index: Integer]:TFactorDecimal read GetItems write SetItems; default;
  end;

  { TFlujos }
  TFlujos = class(TROComplexType)
  private
    fMinimo: AnsiString;
    fMaximo: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Minimo:AnsiString read fMinimo write fMinimo;
    property Maximo:AnsiString read fMaximo write fMaximo;
  end;

  { TFlujosCollection }
  TFlujosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TFlujos;
    procedure SetItems(aIndex: Integer; const Value: TFlujos);
  public
    constructor Create; overload;
    function Add: TFlujos; reintroduce;
    property Items[Index: Integer]:TFlujos read GetItems write SetItems; default;
  end;

  { TLecturas }
  TLecturas = class(TROComplexType)
  private
    fLecturaCorrecta: Boolean;
    fPrecio: Double;
    fCashMoney: Double;
    fCashVolume: Double;
    fCreditMoney: Double;
    fCreditVolume: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property LecturaCorrecta:Boolean read fLecturaCorrecta write fLecturaCorrecta;
    property Precio:Double read fPrecio write fPrecio;
    property CashMoney:Double read fCashMoney write fCashMoney;
    property CashVolume:Double read fCashVolume write fCashVolume;
    property CreditMoney:Double read fCreditMoney write fCreditMoney;
    property CreditVolume:Double read fCreditVolume write fCreditVolume;
  end;

  { TLecturasCollection }
  TLecturasCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLecturas;
    procedure SetItems(aIndex: Integer; const Value: TLecturas);
  public
    constructor Create; overload;
    function Add: TLecturas; reintroduce;
    procedure SaveToArray(anArray: aLecturas);
    procedure LoadFromArray(anArray: aLecturas);
    property Items[Index: Integer]:TLecturas read GetItems write SetItems; default;
  end;

  { TManguera }
  TManguera = class(TROComplexType)
  private
    fNoGasolina: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property NoGasolina:Integer read fNoGasolina write fNoGasolina;
  end;

  { TMangueraCollection }
  TMangueraCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TManguera;
    procedure SetItems(aIndex: Integer; const Value: TManguera);
  public
    constructor Create; overload;
    function Add: TManguera; reintroduce;
    procedure SaveToArray(anArray: aMaguera);
    procedure LoadFromArray(anArray: aMaguera);
    property Items[Index: Integer]:TManguera read GetItems write SetItems; default;
  end;

  { TCanal }
  TCanal = class(TROComplexType)
  private
    fPendiente: eEstadoVenta;
    fLitros: Double;
    fImporte: Double;
    fPrecio: Double;
    fCanal: Integer;
    fCanalAnterior: Integer;
    fDireccion: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Pendiente:eEstadoVenta read fPendiente write fPendiente;
    property Litros:Double read fLitros write fLitros;
    property Importe:Double read fImporte write fImporte;
    property Precio:Double read fPrecio write fPrecio;
    property Canal:Integer read fCanal write fCanal;
    property CanalAnterior:Integer read fCanalAnterior write fCanalAnterior;
    property Direccion:Integer read fDireccion write fDireccion;
  end;

  { TCanalCollection }
  TCanalCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCanal;
    procedure SetItems(aIndex: Integer; const Value: TCanal);
  public
    constructor Create; overload;
    function Add: TCanal; reintroduce;
    property Items[Index: Integer]:TCanal read GetItems write SetItems; default;
  end;

  { TContador }
  TContador = class(TROComplexType)
  private
    fPendiente: eEstadoVenta;
    fLitros: Double;
    fImporte: Double;
    fPrecio: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Pendiente:eEstadoVenta read fPendiente write fPendiente;
    property Litros:Double read fLitros write fLitros;
    property Importe:Double read fImporte write fImporte;
    property Precio:Double read fPrecio write fPrecio;
  end;

  { TContadorCollection }
  TContadorCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TContador;
    procedure SetItems(aIndex: Integer; const Value: TContador);
  public
    constructor Create; overload;
    function Add: TContador; reintroduce;
    procedure SaveToArray(anArray: aContador);
    procedure LoadFromArray(anArray: aContador);
    property Items[Index: Integer]:TContador read GetItems write SetItems; default;
  end;

  { TRegistroPuntoVenta }
  TRegistroPuntoVenta = class(TROComplexType)
  private
    fNoTerminal: AnsiString;
    fTipoTrama: AnsiString;
    fTipoTarjeta: AnsiString;
    fTarjeta: AnsiString;
    fUnidad: AnsiString;
    fNIP: AnsiString;
    fKms: Integer;
    fComision: AnsiString;
    fVentaAutorizada: Double;
    fSaldo: Double;
    fMensaje: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property NoTerminal:AnsiString read fNoTerminal write fNoTerminal;
    property TipoTrama:AnsiString read fTipoTrama write fTipoTrama;
    property TipoTarjeta:AnsiString read fTipoTarjeta write fTipoTarjeta;
    property Tarjeta:AnsiString read fTarjeta write fTarjeta;
    property Unidad:AnsiString read fUnidad write fUnidad;
    property NIP:AnsiString read fNIP write fNIP;
    property Kms:Integer read fKms write fKms;
    property Comision:AnsiString read fComision write fComision;
    property VentaAutorizada:Double read fVentaAutorizada write fVentaAutorizada;
    property Saldo:Double read fSaldo write fSaldo;
    property Mensaje:AnsiString read fMensaje write fMensaje;
  end;

  { TRegistroPuntoVentaCollection }
  TRegistroPuntoVentaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TRegistroPuntoVenta;
    procedure SetItems(aIndex: Integer; const Value: TRegistroPuntoVenta);
  public
    constructor Create; overload;
    function Add: TRegistroPuntoVenta; reintroduce;
    property Items[Index: Integer]:TRegistroPuntoVenta read GetItems write SetItems; default;
  end;

  { TCambio }
  TCambio = class(TROComplexType)
  private
    fFecha: DateTime;
    fActivo: Boolean;
    fPrecios: aCambioPrecios;
    function GetPrecios: aCambioPrecios;
  protected
    property int_Precios: aCambioPrecios read fPrecios;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Fecha:DateTime read fFecha write fFecha;
    property Activo:Boolean read fActivo write fActivo;
    property Precios:aCambioPrecios read GetPrecios write fPrecios;
  end;

  { TCambioCollection }
  TCambioCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCambio;
    procedure SetItems(aIndex: Integer; const Value: TCambio);
  public
    constructor Create; overload;
    function Add: TCambio; reintroduce;
    property Items[Index: Integer]:TCambio read GetItems write SetItems; default;
  end;

  { TCambioPrecios }
  TCambioPrecios = class(TROComplexType)
  private
    fProductoID: Integer;
    fPrecio: Double;
    fIEPS: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ProductoID:Integer read fProductoID write fProductoID;
    property Precio:Double read fPrecio write fPrecio;
    property IEPS:Double read fIEPS write fIEPS;
  end;

  { TCambioPreciosCollection }
  TCambioPreciosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TCambioPrecios;
    procedure SetItems(aIndex: Integer; const Value: TCambioPrecios);
  public
    constructor Create; overload;
    function Add: TCambioPrecios; reintroduce;
    procedure SaveToArray(anArray: aCambioPrecios);
    procedure LoadFromArray(anArray: aCambioPrecios);
    property Items[Index: Integer]:TCambioPrecios read GetItems write SetItems; default;
  end;

  { TDispositivos }
  TDispositivos = class(TROComplexType)
  private
    fActivo: Boolean;
    fNombre: AnsiString;
    fPuerto: Integer;
    fSpeed: Integer;
    fDataBits: Integer;
    fParity: Integer;
    fStopBits: Integer;
    fEcho: Integer;
    fFlow: AnsiString;
    fHandShake: Integer;
    fDispLector: Integer;
    fDispComunicaciones: Integer;
    fRetardoLectura: Integer;
    fRetardoCiclo: Integer;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Activo:Boolean read fActivo write fActivo;
    property Nombre:AnsiString read fNombre write fNombre;
    property Puerto:Integer read fPuerto write fPuerto;
    property Speed:Integer read fSpeed write fSpeed;
    property DataBits:Integer read fDataBits write fDataBits;
    property Parity:Integer read fParity write fParity;
    property StopBits:Integer read fStopBits write fStopBits;
    property Echo:Integer read fEcho write fEcho;
    property Flow:AnsiString read fFlow write fFlow;
    property HandShake:Integer read fHandShake write fHandShake;
    property DispLector:Integer read fDispLector write fDispLector;
    property DispComunicaciones:Integer read fDispComunicaciones write fDispComunicaciones;
    property RetardoLectura:Integer read fRetardoLectura write fRetardoLectura;
    property RetardoCiclo:Integer read fRetardoCiclo write fRetardoCiclo;
  end;

  { TDispositivosCollection }
  TDispositivosCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TDispositivos;
    procedure SetItems(aIndex: Integer; const Value: TDispositivos);
  public
    constructor Create; overload;
    function Add: TDispositivos; reintroduce;
    procedure SaveToArray(anArray: aDispositivos);
    procedure LoadFromArray(anArray: aDispositivos);
    property Items[Index: Integer]:TDispositivos read GetItems write SetItems; default;
  end;

  { TGas }
  TGas = class(TROComplexType)
  private
    fAplica: Boolean;
    fClavePemex: AnsiString;
    fFactorDecimalPrecios: Integer;
    fIEPS: Double;
    fIEPSProgramado: Double;
    fImpuesto: Double;
    fNombre: AnsiString;
    fPrecio: Double;
    fPrecioProgramado: Double;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Aplica:Boolean read fAplica write fAplica;
    property ClavePemex:AnsiString read fClavePemex write fClavePemex;
    property FactorDecimalPrecios:Integer read fFactorDecimalPrecios write fFactorDecimalPrecios;
    property IEPS:Double read fIEPS write fIEPS;
    property IEPSProgramado:Double read fIEPSProgramado write fIEPSProgramado;
    property Impuesto:Double read fImpuesto write fImpuesto;
    property Nombre:AnsiString read fNombre write fNombre;
    property Precio:Double read fPrecio write fPrecio;
    property PrecioProgramado:Double read fPrecioProgramado write fPrecioProgramado;
  end;

  { TGasCollection }
  TGasCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TGas;
    procedure SetItems(aIndex: Integer; const Value: TGas);
  public
    constructor Create; overload;
    function Add: TGas; reintroduce;
    procedure SaveToArray(anArray: aGas);
    procedure LoadFromArray(anArray: aGas);
    property Items[Index: Integer]:TGas read GetItems write SetItems; default;
  end;

  { TProtecciones }
  TProtecciones = class(TROComplexType)
  private
    fConPreset: Boolean;
    fConLitros: Boolean;
    fConMultiplos: Boolean;
    fConDecimales: Boolean;
    fConLLeno: Boolean;
    fObligarPreset: Boolean;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property ConPreset:Boolean read fConPreset write fConPreset;
    property ConLitros:Boolean read fConLitros write fConLitros;
    property ConMultiplos:Boolean read fConMultiplos write fConMultiplos;
    property ConDecimales:Boolean read fConDecimales write fConDecimales;
    property ConLLeno:Boolean read fConLLeno write fConLLeno;
    property ObligarPreset:Boolean read fObligarPreset write fObligarPreset;
  end;

  { TProteccionesCollection }
  TProteccionesCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TProtecciones;
    procedure SetItems(aIndex: Integer; const Value: TProtecciones);
  public
    constructor Create; overload;
    function Add: TProtecciones; reintroduce;
    property Items[Index: Integer]:TProtecciones read GetItems write SetItems; default;
  end;

  { TLicencia }
  TLicencia = class(TROComplexType)
  private
    fVolumetrico: Boolean;
    fTanques: Boolean;
    fCualli: Boolean;
    fProduccion: Boolean;
    fAdministracion: Boolean;
    fFlotillas: Boolean;
    fNA: Boolean;
    fDefinitiva: Boolean;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property Volumetrico:Boolean read fVolumetrico write fVolumetrico;
    property Tanques:Boolean read fTanques write fTanques;
    property Cualli:Boolean read fCualli write fCualli;
    property Produccion:Boolean read fProduccion write fProduccion;
    property Administracion:Boolean read fAdministracion write fAdministracion;
    property Flotillas:Boolean read fFlotillas write fFlotillas;
    property NA:Boolean read fNA write fNA;
    property Definitiva:Boolean read fDefinitiva write fDefinitiva;
  end;

  { TLicenciaCollection }
  TLicenciaCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TLicencia;
    procedure SetItems(aIndex: Integer; const Value: TLicencia);
  public
    constructor Create; overload;
    function Add: TLicencia; reintroduce;
    property Items[Index: Integer]:TLicencia read GetItems write SetItems; default;
  end;

  { TTanque }
  TTanque = class(TROComplexType)
  private
    fTanqueID: Integer;
    fTanque: Integer;
    fCapacidad: Double;
    fFondaje: Double;
    fProductoID: Integer;
    fInicial: Double;
    fTemperatura: Double;
    fVolumenAgua: Double;
    fVolumenInicial: Double;
    fVolumenDisponible: Double;
    fConexion: Boolean;
    fStringTanques: AnsiString;
    fStringRecepciones: AnsiString;
    fRecepciones: AnsiString;
    fNombre: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property TanqueID:Integer read fTanqueID write fTanqueID;
    property Tanque:Integer read fTanque write fTanque;
    property Capacidad:Double read fCapacidad write fCapacidad;
    property Fondaje:Double read fFondaje write fFondaje;
    property ProductoID:Integer read fProductoID write fProductoID;
    property Inicial:Double read fInicial write fInicial;
    property Temperatura:Double read fTemperatura write fTemperatura;
    property VolumenAgua:Double read fVolumenAgua write fVolumenAgua;
    property VolumenInicial:Double read fVolumenInicial write fVolumenInicial;
    property VolumenDisponible:Double read fVolumenDisponible write fVolumenDisponible;
    property Conexion:Boolean read fConexion write fConexion;
    property StringTanques:AnsiString read fStringTanques write fStringTanques;
    property StringRecepciones:AnsiString read fStringRecepciones write fStringRecepciones;
    property Recepciones:AnsiString read fRecepciones write fRecepciones;
    property Nombre:AnsiString read fNombre write fNombre;
  end;

  { TTanqueCollection }
  TTanqueCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TTanque;
    procedure SetItems(aIndex: Integer; const Value: TTanque);
  public
    constructor Create; overload;
    function Add: TTanque; reintroduce;
    procedure SaveToArray(anArray: aTanques);
    procedure LoadFromArray(anArray: aTanques);
    property Items[Index: Integer]:TTanque read GetItems write SetItems; default;
  end;

  { TRecepcion }
  TRecepcion = class(TROComplexType)
  private
    fFechaRecepcion: DateTime;
    fFechaInicial: DateTime;
    fFechaFinal: DateTime;
    fVolumenInicial: Double;
    fVolumenFinal: Double;
    fTemperaturaInicial: Double;
    fTemperaturaFinal: Double;
    fVolumenRecibido: Double;
    fStringRecepocion: AnsiString;
  public
    procedure Assign(iSource: TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
  published
    property FechaRecepcion:DateTime read fFechaRecepcion write fFechaRecepcion;
    property FechaInicial:DateTime read fFechaInicial write fFechaInicial;
    property FechaFinal:DateTime read fFechaFinal write fFechaFinal;
    property VolumenInicial:Double read fVolumenInicial write fVolumenInicial;
    property VolumenFinal:Double read fVolumenFinal write fVolumenFinal;
    property TemperaturaInicial:Double read fTemperaturaInicial write fTemperaturaInicial;
    property TemperaturaFinal:Double read fTemperaturaFinal write fTemperaturaFinal;
    property VolumenRecibido:Double read fVolumenRecibido write fVolumenRecibido;
    property StringRecepocion:AnsiString read fStringRecepocion write fStringRecepocion;
  end;

  { TRecepcionCollection }
  TRecepcionCollection = class(TROCollection)
  protected
    constructor Create(aItemClass: TCollectionItemClass); overload;
    function GetItems(aIndex: Integer): TRecepcion;
    procedure SetItems(aIndex: Integer; const Value: TRecepcion);
  public
    constructor Create; overload;
    function Add: TRecepcion; reintroduce;
    procedure SaveToArray(anArray: aRecepciones);
    procedure LoadFromArray(anArray: aRecepciones);
    property Items[Index: Integer]:TRecepcion read GetItems write SetItems; default;
  end;

  { AAgrupacion }
  AAgrupacion_TAgrupacion = array of TAgrupacion;
  AAgrupacion = class(TROArray)
  private
    fCount: Integer;
    fItems : AAgrupacion_TAgrupacion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TAgrupacion;
    procedure SetItems(aIndex: Integer; const Value: TAgrupacion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TAgrupacion; overload;
    function Add(const Value: TAgrupacion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TAgrupacion read GetItems write SetItems; default;
    property InnerArray: AAgrupacion_TAgrupacion read fItems;
  end;

  { AAgrupacionBomba }
  AAgrupacionBomba_Integer = array of Integer;
  AAgrupacionBomba = class(TROArray)
  private
    fCount: Integer;
    fItems : AAgrupacionBomba_Integer;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): Integer;
    procedure SetItems(aIndex: Integer; const Value: Integer);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add(const Value:Integer): Integer;
    function GetIndex(const aValue: Integer; const aStartFrom: Integer = 0) : Integer; overload;
    function GetIndex(const aPropertyName : string;
                      const aPropertyValue : Variant;
                      StartFrom : Integer = 0;
                      Options : TROSearchOptions = [soIgnoreCase]) : Integer; override;
    function IndexOf(const aValue: Integer; const aStartFrom: Integer = 0): Integer;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:Integer read GetItems write SetItems; default;
    property InnerArray: AAgrupacionBomba_Integer read fItems;
  end;

  { aBombas }
  aBombas_TBomba = array of TBomba;
  aBombas = class(TROArray)
  private
    fCount: Integer;
    fItems : aBombas_TBomba;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TBomba;
    procedure SetItems(aIndex: Integer; const Value: TBomba);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TBomba; overload;
    function Add(const Value: TBomba):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TBomba read GetItems write SetItems; default;
    property InnerArray: aBombas_TBomba read fItems;
  end;

  { aBoolean }
  aBoolean_Boolean = array of Boolean;
  aBoolean = class(TROArray)
  private
    fCount: Integer;
    fItems : aBoolean_Boolean;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): Boolean;
    procedure SetItems(aIndex: Integer; const Value: Boolean);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add(const Value:Boolean): Integer;
    function GetIndex(const aValue: Boolean; const aStartFrom: Integer = 0) : Integer; overload;
    function GetIndex(const aPropertyName : string;
                      const aPropertyValue : Variant;
                      StartFrom : Integer = 0;
                      Options : TROSearchOptions = [soIgnoreCase]) : Integer; override;
    function IndexOf(const aValue: Boolean; const aStartFrom: Integer = 0): Integer;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:Boolean read GetItems write SetItems; default;
    property InnerArray: aBoolean_Boolean read fItems;
  end;

  { aCambioPrecios }
  aCambioPrecios_TCambioPrecios = array of TCambioPrecios;
  aCambioPrecios = class(TROArray)
  private
    fCount: Integer;
    fItems : aCambioPrecios_TCambioPrecios;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TCambioPrecios;
    procedure SetItems(aIndex: Integer; const Value: TCambioPrecios);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TCambioPrecios; overload;
    function Add(const Value: TCambioPrecios):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TCambioPrecios read GetItems write SetItems; default;
    property InnerArray: aCambioPrecios_TCambioPrecios read fItems;
  end;

  { aConsumo }
  aConsumo_TConsumo = array of TConsumo;
  aConsumo = class(TROArray)
  private
    fCount: Integer;
    fItems : aConsumo_TConsumo;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TConsumo;
    procedure SetItems(aIndex: Integer; const Value: TConsumo);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TConsumo; overload;
    function Add(const Value: TConsumo):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TConsumo read GetItems write SetItems; default;
    property InnerArray: aConsumo_TConsumo read fItems;
  end;

  { aContador }
  aContador_TContador = array of TContador;
  aContador = class(TROArray)
  private
    fCount: Integer;
    fItems : aContador_TContador;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TContador;
    procedure SetItems(aIndex: Integer; const Value: TContador);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TContador; overload;
    function Add(const Value: TContador):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TContador read GetItems write SetItems; default;
    property InnerArray: aContador_TContador read fItems;
  end;

  { ADatosCelular }
  ADatosCelular_TDatosCelular = array of TDatosCelular;
  ADatosCelular = class(TROArray)
  private
    fCount: Integer;
    fItems : ADatosCelular_TDatosCelular;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDatosCelular;
    procedure SetItems(aIndex: Integer; const Value: TDatosCelular);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDatosCelular; overload;
    function Add(const Value: TDatosCelular):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDatosCelular read GetItems write SetItems; default;
    property InnerArray: ADatosCelular_TDatosCelular read fItems;
  end;

  { ADespachadorLiquidacion }
  ADespachadorLiquidacion_TDespachadorLiquidacion = array of TDespachadorLiquidacion;
  ADespachadorLiquidacion = class(TROArray)
  private
    fCount: Integer;
    fItems : ADespachadorLiquidacion_TDespachadorLiquidacion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDespachadorLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDespachadorLiquidacion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDespachadorLiquidacion; overload;
    function Add(const Value: TDespachadorLiquidacion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDespachadorLiquidacion read GetItems write SetItems; default;
    property InnerArray: ADespachadorLiquidacion_TDespachadorLiquidacion read fItems;
  end;

  { ADetalleExpress }
  ADetalleExpress_TDetalleExpress = array of TDetalleExpress;
  ADetalleExpress = class(TROArray)
  private
    fCount: Integer;
    fItems : ADetalleExpress_TDetalleExpress;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleExpress;
    procedure SetItems(aIndex: Integer; const Value: TDetalleExpress);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleExpress; overload;
    function Add(const Value: TDetalleExpress):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleExpress read GetItems write SetItems; default;
    property InnerArray: ADetalleExpress_TDetalleExpress read fItems;
  end;

  { ADetalleFacturaPemex }
  ADetalleFacturaPemex_TDetalleFacturaPemex = array of TDetalleFacturaPemex;
  ADetalleFacturaPemex = class(TROArray)
  private
    fCount: Integer;
    fItems : ADetalleFacturaPemex_TDetalleFacturaPemex;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleFacturaPemex;
    procedure SetItems(aIndex: Integer; const Value: TDetalleFacturaPemex);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleFacturaPemex; overload;
    function Add(const Value: TDetalleFacturaPemex):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleFacturaPemex read GetItems write SetItems; default;
    property InnerArray: ADetalleFacturaPemex_TDetalleFacturaPemex read fItems;
  end;

  { ADetalleLiquidacion }
  ADetalleLiquidacion_TDetalleLiquidacion = array of TDetalleLiquidacion;
  ADetalleLiquidacion = class(TROArray)
  private
    fCount: Integer;
    fItems : ADetalleLiquidacion_TDetalleLiquidacion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleLiquidacion;
    procedure SetItems(aIndex: Integer; const Value: TDetalleLiquidacion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleLiquidacion; overload;
    function Add(const Value: TDetalleLiquidacion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleLiquidacion read GetItems write SetItems; default;
    property InnerArray: ADetalleLiquidacion_TDetalleLiquidacion read fItems;
  end;

  { aDispositivos }
  aDispositivos_TDispositivos = array of TDispositivos;
  aDispositivos = class(TROArray)
  private
    fCount: Integer;
    fItems : aDispositivos_TDispositivos;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDispositivos;
    procedure SetItems(aIndex: Integer; const Value: TDispositivos);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDispositivos; overload;
    function Add(const Value: TDispositivos):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDispositivos read GetItems write SetItems; default;
    property InnerArray: aDispositivos_TDispositivos read fItems;
  end;

  { aGas }
  aGas_TGas = array of TGas;
  aGas = class(TROArray)
  private
    fCount: Integer;
    fItems : aGas_TGas;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TGas;
    procedure SetItems(aIndex: Integer; const Value: TGas);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TGas; overload;
    function Add(const Value: TGas):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TGas read GetItems write SetItems; default;
    property InnerArray: aGas_TGas read fItems;
  end;

  { AGeneraCupon }
  AGeneraCupon_TGeneraCupon = array of TGeneraCupon;
  AGeneraCupon = class(TROArray)
  private
    fCount: Integer;
    fItems : AGeneraCupon_TGeneraCupon;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TGeneraCupon;
    procedure SetItems(aIndex: Integer; const Value: TGeneraCupon);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TGeneraCupon; overload;
    function Add(const Value: TGeneraCupon):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TGeneraCupon read GetItems write SetItems; default;
    property InnerArray: AGeneraCupon_TGeneraCupon read fItems;
  end;

  { aLecturas }
  aLecturas_TLecturas = array of TLecturas;
  aLecturas = class(TROArray)
  private
    fCount: Integer;
    fItems : aLecturas_TLecturas;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TLecturas;
    procedure SetItems(aIndex: Integer; const Value: TLecturas);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TLecturas; overload;
    function Add(const Value: TLecturas):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TLecturas read GetItems write SetItems; default;
    property InnerArray: aLecturas_TLecturas read fItems;
  end;

  { aMaguera }
  aMaguera_TManguera = array of TManguera;
  aMaguera = class(TROArray)
  private
    fCount: Integer;
    fItems : aMaguera_TManguera;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TManguera;
    procedure SetItems(aIndex: Integer; const Value: TManguera);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TManguera; overload;
    function Add(const Value: TManguera):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TManguera read GetItems write SetItems; default;
    property InnerArray: aMaguera_TManguera read fItems;
  end;

  { aNodos }
  aNodos_TNodo = array of TNodo;
  aNodos = class(TROArray)
  private
    fCount: Integer;
    fItems : aNodos_TNodo;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TNodo;
    procedure SetItems(aIndex: Integer; const Value: TNodo);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TNodo; overload;
    function Add(const Value: TNodo):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TNodo read GetItems write SetItems; default;
    property InnerArray: aNodos_TNodo read fItems;
  end;

  { aRecepciones }
  aRecepciones_TRecepcion = array of TRecepcion;
  aRecepciones = class(TROArray)
  private
    fCount: Integer;
    fItems : aRecepciones_TRecepcion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TRecepcion;
    procedure SetItems(aIndex: Integer; const Value: TRecepcion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TRecepcion; overload;
    function Add(const Value: TRecepcion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TRecepcion read GetItems write SetItems; default;
    property InnerArray: aRecepciones_TRecepcion read fItems;
  end;

  { aRegistroVenta }
  aRegistroVenta_TRegistroVenta = array of TRegistroVenta;
  aRegistroVenta = class(TROArray)
  private
    fCount: Integer;
    fItems : aRegistroVenta_TRegistroVenta;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TRegistroVenta;
    procedure SetItems(aIndex: Integer; const Value: TRegistroVenta);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TRegistroVenta; overload;
    function Add(const Value: TRegistroVenta):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TRegistroVenta read GetItems write SetItems; default;
    property InnerArray: aRegistroVenta_TRegistroVenta read fItems;
  end;

  { ArrayOpcion }
  ArrayOpcion_TOpcion = array of TOpcion;
  ArrayOpcion = class(TROArray)
  private
    fCount: Integer;
    fItems : ArrayOpcion_TOpcion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TOpcion;
    procedure SetItems(aIndex: Integer; const Value: TOpcion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TOpcion; overload;
    function Add(const Value: TOpcion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TOpcion read GetItems write SetItems; default;
    property InnerArray: ArrayOpcion_TOpcion read fItems;
  end;

  { aTanques }
  aTanques_TTanque = array of TTanque;
  aTanques = class(TROArray)
  private
    fCount: Integer;
    fItems : aTanques_TTanque;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TTanque;
    procedure SetItems(aIndex: Integer; const Value: TTanque);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TTanque; overload;
    function Add(const Value: TTanque):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TTanque read GetItems write SetItems; default;
    property InnerArray: aTanques_TTanque read fItems;
  end;

  { ATCarteraVale }
  ATCarteraVale_TCarteraVale = array of TCarteraVale;
  ATCarteraVale = class(TROArray)
  private
    fCount: Integer;
    fItems : ATCarteraVale_TCarteraVale;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TCarteraVale;
    procedure SetItems(aIndex: Integer; const Value: TCarteraVale);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TCarteraVale; overload;
    function Add(const Value: TCarteraVale):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TCarteraVale read GetItems write SetItems; default;
    property InnerArray: ATCarteraVale_TCarteraVale read fItems;
  end;

  { ATDetalleCupon }
  ATDetalleCupon_TDetalleCupon = array of TDetalleCupon;
  ATDetalleCupon = class(TROArray)
  private
    fCount: Integer;
    fItems : ATDetalleCupon_TDetalleCupon;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleCupon;
    procedure SetItems(aIndex: Integer; const Value: TDetalleCupon);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleCupon; overload;
    function Add(const Value: TDetalleCupon):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleCupon read GetItems write SetItems; default;
    property InnerArray: ATDetalleCupon_TDetalleCupon read fItems;
  end;

  { ATDetalleFactura }
  ATDetalleFactura_TDetalleFactura = array of TDetalleFactura;
  ATDetalleFactura = class(TROArray)
  private
    fCount: Integer;
    fItems : ATDetalleFactura_TDetalleFactura;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleFactura;
    procedure SetItems(aIndex: Integer; const Value: TDetalleFactura);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleFactura; overload;
    function Add(const Value: TDetalleFactura):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleFactura read GetItems write SetItems; default;
    property InnerArray: ATDetalleFactura_TDetalleFactura read fItems;
  end;

  { ATDetalleRecibo }
  ATDetalleRecibo_TDetalleReciboPago = array of TDetalleReciboPago;
  ATDetalleRecibo = class(TROArray)
  private
    fCount: Integer;
    fItems : ATDetalleRecibo_TDetalleReciboPago;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleReciboPago;
    procedure SetItems(aIndex: Integer; const Value: TDetalleReciboPago);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleReciboPago; overload;
    function Add(const Value: TDetalleReciboPago):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleReciboPago read GetItems write SetItems; default;
    property InnerArray: ATDetalleRecibo_TDetalleReciboPago read fItems;
  end;

  { ATDetalleTransaccion }
  ATDetalleTransaccion_TDetalleTransaccion = array of TDetalleTransaccion;
  ATDetalleTransaccion = class(TROArray)
  private
    fCount: Integer;
    fItems : ATDetalleTransaccion_TDetalleTransaccion;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleTransaccion;
    procedure SetItems(aIndex: Integer; const Value: TDetalleTransaccion);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleTransaccion; overload;
    function Add(const Value: TDetalleTransaccion):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleTransaccion read GetItems write SetItems; default;
    property InnerArray: ATDetalleTransaccion_TDetalleTransaccion read fItems;
  end;

  { ATDetalleValeCreditoFactura }
  ATDetalleValeCreditoFactura_TDetalleValeCreditoFactura = array of TDetalleValeCreditoFactura;
  ATDetalleValeCreditoFactura = class(TROArray)
  private
    fCount: Integer;
    fItems : ATDetalleValeCreditoFactura_TDetalleValeCreditoFactura;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDetalleValeCreditoFactura;
    procedure SetItems(aIndex: Integer; const Value: TDetalleValeCreditoFactura);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDetalleValeCreditoFactura; overload;
    function Add(const Value: TDetalleValeCreditoFactura):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDetalleValeCreditoFactura read GetItems write SetItems; default;
    property InnerArray: ATDetalleValeCreditoFactura_TDetalleValeCreditoFactura read fItems;
  end;

  { ATFacturaElectronicaDetalleImportes }
  ATFacturaElectronicaDetalleImportes_TFacturaElectronicaDetalleImportes = array of TFacturaElectronicaDetalleImportes;
  ATFacturaElectronicaDetalleImportes = class(TROArray)
  private
    fCount: Integer;
    fItems : ATFacturaElectronicaDetalleImportes_TFacturaElectronicaDetalleImportes;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TFacturaElectronicaDetalleImportes;
    procedure SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImportes);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TFacturaElectronicaDetalleImportes; overload;
    function Add(const Value: TFacturaElectronicaDetalleImportes):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TFacturaElectronicaDetalleImportes read GetItems write SetItems; default;
    property InnerArray: ATFacturaElectronicaDetalleImportes_TFacturaElectronicaDetalleImportes read fItems;
  end;

  { ATFacturaElectronicaDetalleImpuestos }
  ATFacturaElectronicaDetalleImpuestos_TFacturaElectronicaDetalleImpuestos = array of TFacturaElectronicaDetalleImpuestos;
  ATFacturaElectronicaDetalleImpuestos = class(TROArray)
  private
    fCount: Integer;
    fItems : ATFacturaElectronicaDetalleImpuestos_TFacturaElectronicaDetalleImpuestos;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TFacturaElectronicaDetalleImpuestos;
    procedure SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImpuestos);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TFacturaElectronicaDetalleImpuestos; overload;
    function Add(const Value: TFacturaElectronicaDetalleImpuestos):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TFacturaElectronicaDetalleImpuestos read GetItems write SetItems; default;
    property InnerArray: ATFacturaElectronicaDetalleImpuestos_TFacturaElectronicaDetalleImpuestos read fItems;
  end;

  { ATipoValor }
  ATipoValor_TTipoValor = array of TTipoValor;
  ATipoValor = class(TROArray)
  private
    fCount: Integer;
    fItems : ATipoValor_TTipoValor;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TTipoValor;
    procedure SetItems(aIndex: Integer; const Value: TTipoValor);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TTipoValor; overload;
    function Add(const Value: TTipoValor):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TTipoValor read GetItems write SetItems; default;
    property InnerArray: ATipoValor_TTipoValor read fItems;
  end;

  { ATLiquidacionDetalle }
  ATLiquidacionDetalle_TLiquidacionDetalle = array of TLiquidacionDetalle;
  ATLiquidacionDetalle = class(TROArray)
  private
    fCount: Integer;
    fItems : ATLiquidacionDetalle_TLiquidacionDetalle;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TLiquidacionDetalle;
    procedure SetItems(aIndex: Integer; const Value: TLiquidacionDetalle);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TLiquidacionDetalle; overload;
    function Add(const Value: TLiquidacionDetalle):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TLiquidacionDetalle read GetItems write SetItems; default;
    property InnerArray: ATLiquidacionDetalle_TLiquidacionDetalle read fItems;
  end;

  { ATLiquidacionProducto }
  ATLiquidacionProducto_TLiquidacionProducto = array of TLiquidacionProducto;
  ATLiquidacionProducto = class(TROArray)
  private
    fCount: Integer;
    fItems : ATLiquidacionProducto_TLiquidacionProducto;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TLiquidacionProducto;
    procedure SetItems(aIndex: Integer; const Value: TLiquidacionProducto);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TLiquidacionProducto; overload;
    function Add(const Value: TLiquidacionProducto):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TLiquidacionProducto read GetItems write SetItems; default;
    property InnerArray: ATLiquidacionProducto_TLiquidacionProducto read fItems;
  end;

  { ATTicketFactura }
  ATTicketFactura_TTicketFactura = array of TTicketFactura;
  ATTicketFactura = class(TROArray)
  private
    fCount: Integer;
    fItems : ATTicketFactura_TTicketFactura;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TTicketFactura;
    procedure SetItems(aIndex: Integer; const Value: TTicketFactura);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TTicketFactura; overload;
    function Add(const Value: TTicketFactura):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TTicketFactura read GetItems write SetItems; default;
    property InnerArray: ATTicketFactura_TTicketFactura read fItems;
  end;

  { aTurnoBomba }
  aTurnoBomba_Integer = array of Integer;
  aTurnoBomba = class(TROArray)
  private
    fCount: Integer;
    fItems : aTurnoBomba_Integer;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): Integer;
    procedure SetItems(aIndex: Integer; const Value: Integer);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add(const Value:Integer): Integer;
    function GetIndex(const aValue: Integer; const aStartFrom: Integer = 0) : Integer; overload;
    function GetIndex(const aPropertyName : string;
                      const aPropertyValue : Variant;
                      StartFrom : Integer = 0;
                      Options : TROSearchOptions = [soIgnoreCase]) : Integer; override;
    function IndexOf(const aValue: Integer; const aStartFrom: Integer = 0): Integer;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:Integer read GetItems write SetItems; default;
    property InnerArray: aTurnoBomba_Integer read fItems;
  end;

  { TDocumentosConSaldoArray }
  TDocumentosConSaldoArray_TDocumentosConSaldo = array of TDocumentosConSaldo;
  TDocumentosConSaldoArray = class(TROArray)
  private
    fCount: Integer;
    fItems : TDocumentosConSaldoArray_TDocumentosConSaldo;
  protected
    procedure Grow; virtual;
    function GetItems(aIndex: Integer): TDocumentosConSaldo;
    procedure SetItems(aIndex: Integer; const Value: TDocumentosConSaldo);
    function GetCount: Integer; override;
  public
    class function GetItemType: PTypeInfo; override;
    class function GetItemClass: System.TClass; override;
    class function GetItemSize: Integer; override;

    function GetItemRef(aIndex: Integer): pointer; override;
    procedure SetItemRef(aIndex: Integer; Ref: pointer); override;
    procedure Clear; override;
    procedure Delete(aIndex: Integer); override;
    procedure Resize(ElementCount: Integer); override;

    procedure Assign(iSource:TPersistent); override;
    procedure ReadComplex(ASerializer: TObject); override;
    procedure WriteComplex(ASerializer: TObject); override;
    function Add: TDocumentosConSaldo; overload;
    function Add(const Value: TDocumentosConSaldo):Integer; overload;

    property Count : Integer read GetCount;
    property Items[Index: Integer]:TDocumentosConSaldo read GetItems write SetItems; default;
    property InnerArray: TDocumentosConSaldoArray_TDocumentosConSaldo read fItems;
  end;

  { IServiceValesCupones }
  IServiceValesCupones = interface(IDataAbstractService)
    ['{762597B5-7713-4971-8C5A-32F68BB623AB}']
    function AbreDataSetReporte(const SQL: AnsiString; const Parametros: TParametros): Binary;
    function FacturaCupon(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                          const SerieFactura: AnsiString): Integer;
    procedure ActualizaCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                             const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Liquidacion: Integer);
    procedure ActualizaCuponConReferencia(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                          const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Referencia: AnsiString);
    procedure ActualizaLiquidacionCorte(const Liquidacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer);
    procedure ActualizarStatusValeCredito(const CarteraValeCreditoID: Integer; const Status: AnsiString; const FacturaID: Integer);
    procedure ActualizaStatusCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                   const LiquidacionID: Integer; const PeriodoConsumo: Integer; const Secuencia: Integer);
    function BuscaHost(const Tipo: Integer; const Gasolinero: Integer): AnsiString;
    function BuscaReporte(const Nombre: AnsiString; const TipoTemplate: Integer): TReporte;
    function CambiaFolioFactura(const Serie: AnsiString; const Folio: Integer; const NewSerie: AnsiString; const NewFolio: Integer): AnsiString;
    function CancelaFactura(const Folio: Integer; const Serie: AnsiString; const Fecha: DateTime; const EstacionID: Integer; const UsuarioID: Integer): AnsiString;
    function CancelarCupones(const Lista: AnsiString; const UsuarioID: Integer): AnsiString;
    function CierraLiquidacion(const LiquidacionID: Integer): AnsiString;
    function CostoProducto(const ProductoID: Integer): Double;
    function DatosCliente(const ClienteID: Integer; const Referencia: AnsiString): TDatosCliente;
    function Existencia(const EstacionID: Integer; const AlmacenID: Integer; const ProductoID: Integer; const Fecha: DateTime): Double;
    function FacturaExpress(const Serie: AnsiString; const Folio: Integer): TFacturaExpress;
    function FacturaID(const Serie: AnsiString; const Folio: Integer): Integer;
    function FacturaYLiquidacion(const ClienteID: Integer): Boolean;
    function Fecha: DateTime;
    function Folio(const Campo: AnsiString; const Serie: AnsiString): Integer;
    function FolioActual(const Campo: AnsiString; const Serie: AnsiString): Integer;
    function FolioActual2(const serie: AnsiString; const folio: Integer): Integer;
    procedure GuardaAccesos(const UsuarioID: Integer; const Lista: AnsiString);
    function GuardaConsumoExpress(const Consumo: TConsumoExpress): Boolean;
    procedure GuardaLimiteFactura(const UsuarioID: Int64; const Cantidad: Double);
    procedure GuardarDatosFactura(const DatosFactura: TDatosFactura);
    procedure GuardarDatosLiquidacion(const DatosLiquidacion: TDatosLiquidacion);
    procedure GuardarDatosReciboPago(const DatosReciboPago: TDatosReciboPago);
    function GuardarFacturaExpress(const ClienteID: Integer; const Serie: AnsiString; const EstacionID: Integer; const TicketID: Integer): TFacturaExpress;
    procedure InsertaDeposito(const DepositoID: Integer; const Cantidad: Double; const Usuario: Integer; const Fecha: DateTime; const Secuencia: Integer;
                              const EstacionID: Integer; const Descripcion: AnsiString; const Ejercicio: Integer; const Periodo: Integer;
                              const Dia: Integer);
    procedure GuardaDatosFactura(const DatosFactura: TDatosFactura);
    procedure InsertaProductoPrecio(const ProductoID: Integer; const EstacionID: Integer; const Precio: Double);
    function Login(const Usuario: AnsiString; const Clave: AnsiString): TLoginInfo;
    function ModificarFolioActual(const Campo: AnsiString; const Serie: AnsiString; const Folio: Integer): AnsiString;
    function ObtenerStatusCupon(const Barras: AnsiString): AnsiString;
    function PrefijaCupon(const nBomba: Integer; const Barras: AnsiString; const Terminal: AnsiString; const Grupo: Integer; const ClienteID: Integer): TCupon;
    function ObtenerTipoCambioIDPorEstacion(const EstacionID: Integer): Integer;
    function ObtenerTipoCambioPorEstacion(const EstacionID: Integer): Double;
    function PrecioProducto(const EstacionID: Integer; const ProductoID: Integer): Double;
    function StatusTicket(const EstacionID: Integer; const TicketID: Integer): Integer;
    function SumaAnticipo(const Estacion: Integer; const Secuencia: Integer): Double;
    function SumaVentasCorte(const Estacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer): Double;
    function ValoresTurno(const EstacionID: Integer; const TurnoID: Integer): TValoresTurno;
    function VersionServer: AnsiString;
    function LiquidacionCerrada(const EstacionID: Integer; const TurnoID: Integer): Boolean;
    function ClienteValido(const ClienteID: Integer; const NewClienteID: Integer): Boolean;
    function BuscaOtroProducto(const EstacionID: Integer; const Codigo: AnsiString): TOtroProducto;
    function TurnoALiquidacionID(const TurnoID: Integer; const EstacionID: Integer): Integer;
    function BuscaSagarpa(const EstacionID: Integer): TSagarpa;
    function PuntosCalculaSaldo(const ClienteID: Integer): Double;
    function PuntosCalculaPuntos(const ClienteID: Integer; const ProductoID: Integer; const Volumen: Double): Double;
    procedure PuntosGuardaDatos(const Datos: TDatosPuntos);
    function PuntosDatos(const ClienteID: Integer): TDatosPuntos;
    function EliminaAutomaticosLiquidacion(const EstacionID: Integer; const TurnoID: Integer): Boolean;
    function BuscaDespachadorLiquidacion(const BombaID: Integer; const EstacionID: Integer; const TurnoID: Integer): Integer;
    function EntregaPremio(const ClienteID: Integer; const PremioID: Integer; const Cantidad: Integer): TEntregaPremio;
    function DatosPremio(const PremioID: Integer): TDatosPremio;
    function ValidaConsumo(const Consumo: TConsumoExpress): Integer;
    function AgrupacionesBomba(const EstacionID: Integer): AAgrupacion;
    function DatosCerrarLiquidacion(const EstacionID: Integer; const TurnoID: Integer): TDatosCerrarLiquidacion;
    procedure ProcesaVentasLiquidacion(const Datos: TDatosCerrarLiquidacion);
    function TicketsLiquidacion(const LiquidacionID: Integer): AnsiString;
    function ValidaFolioFactura(const Serie: AnsiString; const Folio: Integer): Boolean;
    procedure PuntosGuardaCriterios(const PuntosCriterioID: Integer; const Datos: AnsiString);
    function DatosFacturaElectronica(const FacturaID: Integer; const EstacionID: Integer): TFacturaElectronica;
    function LlavePrivaCertificado(const LlavePrivada: AnsiString; const Certificado: AnsiString; const GasolineroID: Integer; const Password: AnsiString;
                                   const EstacionID: Integer): AnsiString;
    function SELLOPEMs(const Texto: UnicodeString; const EstacionID: Integer): AnsiString;
    function CERs(const DIR: AnsiString): AnsiString;
    function ValidaLiquidacionDespachador(const LiquidacionID: Integer): AnsiString;
    function ActualizaLiquidacionProd(const Estacion: Integer; const FechaIni: DateTime; const FechaFin: DateTime; const TurnoID: Integer): Boolean;
    procedure PuntosDespachador(const TurnoID: Integer; const EstacionID: Integer; const UsuarioID: Integer);
    procedure GuardarDatosFacturaPemex(const DatosFacturaPemex: TDatosFacturaPemex);
    function AfectaInventarios(const LiquidacionID: Integer; const UsuarioID: Integer): AnsiString;
    procedure InsertaFacturaElectronica(const FacturaElectronicaID: Integer; const CadenaOriginal: UnicodeString; const SelloDigital: UnicodeString;
                                        const FacturaID: Integer; const Vigencia: Boolean; const Enviado: Boolean; const NoCertificado: AnsiString;
                                        const NoAprobacion: AnsiString; const FechaAprobacion: DateTime);
    function CERsNUM(const DIR: AnsiString): AnsiString;
    function ActualizaFirmaDefault(const UsuarioID: Integer; const FIRMA: Binary): AnsiString;
    function obtendatosempleados(const EstacionID: AnsiString; const Turno: AnsiString): AnsiString;
    procedure ProgramaMantenimiento(const IDPROGRAMAMANTENIMIENTO: Integer; const IDESTACION: Integer; const FECHA: DateTime);
    function obtendatosActividadesProgramadas(const IDESTACION: Integer; const TIPO: Integer): AnsiString;
    function obtenNotificacionesDiarias(const IDESTACION: Integer): AnsiString;
    function registraactividadrealizada(const IDACTIVIDADMANTENIMIENTO: Integer; const NOTA: AnsiString; const STATUS: AnsiString): AnsiString;
    function CancelarLote(const LoteID: Integer; const UsuarioID: Integer): AnsiString;
    function StatusCupon(const Codigo: AnsiString): AnsiString;
    function BuscarReporte(const Nombre: AnsiString): TReporte;
    procedure GeneraCupones(const LoteCuponID: Integer; const ClienteID: Integer; const EstacionID: Integer; const Identificador: Integer;
                            const Cupones: AGeneraCupon; const TipoCupon: Boolean; const Serie: AnsiString; const aFolio: Integer);
    function DocumentosConSaldo(const ClienteID: Integer): TDocumentosConSaldoArray;
    function FacturaRecibo(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                           const SerieFactura: AnsiString; const ProductoID: Integer): Integer;
    function CaracteresCupon: AnsiString;
    function ObtenCuponValido(const Barras: AnsiString; const Grupo: Integer; const CuponAutoriza: TAutoriza): TCuponValido;
    function GuardaAutorizacion(const Autorizacion: TAutoriza): Boolean;
    function DatosVehiculo(const VehiculoID: Integer): TDatosVehiculo;
    function CancelaGrupo(const Grupo: Integer): Boolean;
    function QuemarCupon(const Consumo: TConsumo; const Barras: AnsiString): Boolean;
    function GuardarConsumo(const Consumo: TConsumo): Boolean;
    function DatosVerifica(const VehiculoID: Integer): TDatosVerifica;
    function DatosVerificaReferencia(const Referencia: AnsiString): TDatosVerifica;
    function TarjetaConsumo(const Tarjeta: AnsiString; const Consumo: TConsumo): Boolean;
    procedure ActualizaHistorial(const Consumo: TConsumo);
    function EliminaAutorizacionVehiculo(const Referencia: AnsiString): Boolean;
    function AgregaSecuencias(const Consumos: aConsumo): Boolean;
    function AutorizaTarjeta(const Autoriza: TAutoriza): TTarjeta;
    function BuscaAutorizacionTarjeta(const ClienteID: Integer; const VehiculoID: Integer; const GasolineroID: Integer): Boolean;
    procedure EstadoDispensario(const Dispensario: Integer; const Estado: AnsiString);
    procedure EntradaBitacora(const Descripcion: AnsiString; const Tipo: eTipoBitacora; const Alerta: Boolean);
    procedure AutoCambioPrecio;
    procedure CierraTurno(const BombaID: Integer; const TurnoID: Integer; const FinLecturas: aLecturas);
    procedure CreaTurno(const BombaID: Integer; const TurnoID: Integer; const InicioLecturas: aLecturas);
    function GuardaVenta(const nBomba: Integer): Integer;
    function UltimoTurnoBomba: aTurnoBomba;
    procedure ValidaSecuencias(const TurnoID: Integer);
    function ValidaSecuenciasC(const EstacionID: Integer; const Secuencias: AnsiString): AnsiString;
    function Datos: TGlobal;
    procedure CancelaGrupoC(const BombaID: Integer; const Grupo: Integer);
    function PrefijaGrupo(const Bomba: Integer; const Grupo: Integer; const Terminal: AnsiString; const Cupon: TCupon): AnsiString;
    function PrefijaTarjeta(const nBomba: Integer; const Tarjeta: AnsiString; const NIP: AnsiString; const Odometro: AnsiString;
                            const Terminal: AnsiString): AnsiString;
    procedure AsignaBombas;
    procedure AsignaProductos;
    procedure AsignaDispositivo;
    procedure AsignaTanques;
    procedure IncrementarImpreso(const Secuencia: Integer);
    function PrefijaTarjetaFrecuente(const nBomba: Integer; const Tarjeta: AnsiString; const Odometro: AnsiString; const Terminal: AnsiString): AnsiString;
    function GenerarFacturas(const Serie: AnsiString; const fol: Integer; const FechaCorte: DateTime; const FechaContable: DateTime;
                             const DiaFacturar: Integer; const Magna: Boolean; const Premium: Boolean; const Diesel: Boolean; const EmpleadoID: Integer): Integer;
    function EliminaFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
    function CambioFactura(const Serie: AnsiString; const Folio: Integer; const NuevaSerie: AnsiString; const NuevoFolio: Integer;
                           const FechaImpresion: DateTime): AnsiString;
    function DatosFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
    function GeneraFacturasXCliente(const ClienteID: Integer; const Productos: AnsiString; const FechaCorte: DateTime; const FechaContable: DateTime;
                                    const Serie: AnsiString; const aFolio: Integer; const EmpleadoID: Integer): Integer;
    function GasolineroID: Integer;
  end;

  { CoServiceValesCupones }
  CoServiceValesCupones = class
    class function Create(const aMessage: IROMessage; aTransportChannel: IROTransportChannel): IServiceValesCupones; overload;
    class function Create(const aUri: TROUri): IServiceValesCupones; overload;
    class function Create(const aUrl: string): IServiceValesCupones; overload;
  end;

  { TServiceValesCupones_Proxy }
  TServiceValesCupones_Proxy = class(TDataAbstractService_Proxy, IServiceValesCupones)
  protected
    function __GetInterfaceName:string; override;

    function AbreDataSetReporte(const SQL: AnsiString; const Parametros: TParametros): Binary;
    function FacturaCupon(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                          const SerieFactura: AnsiString): Integer;
    procedure ActualizaCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                             const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Liquidacion: Integer);
    procedure ActualizaCuponConReferencia(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                          const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Referencia: AnsiString);
    procedure ActualizaLiquidacionCorte(const Liquidacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer);
    procedure ActualizarStatusValeCredito(const CarteraValeCreditoID: Integer; const Status: AnsiString; const FacturaID: Integer);
    procedure ActualizaStatusCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                   const LiquidacionID: Integer; const PeriodoConsumo: Integer; const Secuencia: Integer);
    function BuscaHost(const Tipo: Integer; const Gasolinero: Integer): AnsiString;
    function BuscaReporte(const Nombre: AnsiString; const TipoTemplate: Integer): TReporte;
    function CambiaFolioFactura(const Serie: AnsiString; const Folio: Integer; const NewSerie: AnsiString; const NewFolio: Integer): AnsiString;
    function CancelaFactura(const Folio: Integer; const Serie: AnsiString; const Fecha: DateTime; const EstacionID: Integer; const UsuarioID: Integer): AnsiString;
    function CancelarCupones(const Lista: AnsiString; const UsuarioID: Integer): AnsiString;
    function CierraLiquidacion(const LiquidacionID: Integer): AnsiString;
    function CostoProducto(const ProductoID: Integer): Double;
    function DatosCliente(const ClienteID: Integer; const Referencia: AnsiString): TDatosCliente;
    function Existencia(const EstacionID: Integer; const AlmacenID: Integer; const ProductoID: Integer; const Fecha: DateTime): Double;
    function FacturaExpress(const Serie: AnsiString; const Folio: Integer): TFacturaExpress;
    function FacturaID(const Serie: AnsiString; const Folio: Integer): Integer;
    function FacturaYLiquidacion(const ClienteID: Integer): Boolean;
    function Fecha: DateTime;
    function Folio(const Campo: AnsiString; const Serie: AnsiString): Integer;
    function FolioActual(const Campo: AnsiString; const Serie: AnsiString): Integer;
    function FolioActual2(const serie: AnsiString; const folio: Integer): Integer;
    procedure GuardaAccesos(const UsuarioID: Integer; const Lista: AnsiString);
    function GuardaConsumoExpress(const Consumo: TConsumoExpress): Boolean;
    procedure GuardaLimiteFactura(const UsuarioID: Int64; const Cantidad: Double);
    procedure GuardarDatosFactura(const DatosFactura: TDatosFactura);
    procedure GuardarDatosLiquidacion(const DatosLiquidacion: TDatosLiquidacion);
    procedure GuardarDatosReciboPago(const DatosReciboPago: TDatosReciboPago);
    function GuardarFacturaExpress(const ClienteID: Integer; const Serie: AnsiString; const EstacionID: Integer; const TicketID: Integer): TFacturaExpress;
    procedure InsertaDeposito(const DepositoID: Integer; const Cantidad: Double; const Usuario: Integer; const Fecha: DateTime; const Secuencia: Integer;
                              const EstacionID: Integer; const Descripcion: AnsiString; const Ejercicio: Integer; const Periodo: Integer;
                              const Dia: Integer);
    procedure GuardaDatosFactura(const DatosFactura: TDatosFactura);
    procedure InsertaProductoPrecio(const ProductoID: Integer; const EstacionID: Integer; const Precio: Double);
    function Login(const Usuario: AnsiString; const Clave: AnsiString): TLoginInfo;
    function ModificarFolioActual(const Campo: AnsiString; const Serie: AnsiString; const Folio: Integer): AnsiString;
    function ObtenerStatusCupon(const Barras: AnsiString): AnsiString;
    function PrefijaCupon(const nBomba: Integer; const Barras: AnsiString; const Terminal: AnsiString; const Grupo: Integer; const ClienteID: Integer): TCupon;
    function ObtenerTipoCambioIDPorEstacion(const EstacionID: Integer): Integer;
    function ObtenerTipoCambioPorEstacion(const EstacionID: Integer): Double;
    function PrecioProducto(const EstacionID: Integer; const ProductoID: Integer): Double;
    function StatusTicket(const EstacionID: Integer; const TicketID: Integer): Integer;
    function SumaAnticipo(const Estacion: Integer; const Secuencia: Integer): Double;
    function SumaVentasCorte(const Estacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer): Double;
    function ValoresTurno(const EstacionID: Integer; const TurnoID: Integer): TValoresTurno;
    function VersionServer: AnsiString;
    function LiquidacionCerrada(const EstacionID: Integer; const TurnoID: Integer): Boolean;
    function ClienteValido(const ClienteID: Integer; const NewClienteID: Integer): Boolean;
    function BuscaOtroProducto(const EstacionID: Integer; const Codigo: AnsiString): TOtroProducto;
    function TurnoALiquidacionID(const TurnoID: Integer; const EstacionID: Integer): Integer;
    function BuscaSagarpa(const EstacionID: Integer): TSagarpa;
    function PuntosCalculaSaldo(const ClienteID: Integer): Double;
    function PuntosCalculaPuntos(const ClienteID: Integer; const ProductoID: Integer; const Volumen: Double): Double;
    procedure PuntosGuardaDatos(const Datos: TDatosPuntos);
    function PuntosDatos(const ClienteID: Integer): TDatosPuntos;
    function EliminaAutomaticosLiquidacion(const EstacionID: Integer; const TurnoID: Integer): Boolean;
    function BuscaDespachadorLiquidacion(const BombaID: Integer; const EstacionID: Integer; const TurnoID: Integer): Integer;
    function EntregaPremio(const ClienteID: Integer; const PremioID: Integer; const Cantidad: Integer): TEntregaPremio;
    function DatosPremio(const PremioID: Integer): TDatosPremio;
    function ValidaConsumo(const Consumo: TConsumoExpress): Integer;
    function AgrupacionesBomba(const EstacionID: Integer): AAgrupacion;
    function DatosCerrarLiquidacion(const EstacionID: Integer; const TurnoID: Integer): TDatosCerrarLiquidacion;
    procedure ProcesaVentasLiquidacion(const Datos: TDatosCerrarLiquidacion);
    function TicketsLiquidacion(const LiquidacionID: Integer): AnsiString;
    function ValidaFolioFactura(const Serie: AnsiString; const Folio: Integer): Boolean;
    procedure PuntosGuardaCriterios(const PuntosCriterioID: Integer; const Datos: AnsiString);
    function DatosFacturaElectronica(const FacturaID: Integer; const EstacionID: Integer): TFacturaElectronica;
    function LlavePrivaCertificado(const LlavePrivada: AnsiString; const Certificado: AnsiString; const GasolineroID: Integer; const Password: AnsiString;
                                   const EstacionID: Integer): AnsiString;
    function SELLOPEMs(const Texto: UnicodeString; const EstacionID: Integer): AnsiString;
    function CERs(const DIR: AnsiString): AnsiString;
    function ValidaLiquidacionDespachador(const LiquidacionID: Integer): AnsiString;
    function ActualizaLiquidacionProd(const Estacion: Integer; const FechaIni: DateTime; const FechaFin: DateTime; const TurnoID: Integer): Boolean;
    procedure PuntosDespachador(const TurnoID: Integer; const EstacionID: Integer; const UsuarioID: Integer);
    procedure GuardarDatosFacturaPemex(const DatosFacturaPemex: TDatosFacturaPemex);
    function AfectaInventarios(const LiquidacionID: Integer; const UsuarioID: Integer): AnsiString;
    procedure InsertaFacturaElectronica(const FacturaElectronicaID: Integer; const CadenaOriginal: UnicodeString; const SelloDigital: UnicodeString;
                                        const FacturaID: Integer; const Vigencia: Boolean; const Enviado: Boolean; const NoCertificado: AnsiString;
                                        const NoAprobacion: AnsiString; const FechaAprobacion: DateTime);
    function CERsNUM(const DIR: AnsiString): AnsiString;
    function ActualizaFirmaDefault(const UsuarioID: Integer; const FIRMA: Binary): AnsiString;
    function obtendatosempleados(const EstacionID: AnsiString; const Turno: AnsiString): AnsiString;
    procedure ProgramaMantenimiento(const IDPROGRAMAMANTENIMIENTO: Integer; const IDESTACION: Integer; const FECHA: DateTime);
    function obtendatosActividadesProgramadas(const IDESTACION: Integer; const TIPO: Integer): AnsiString;
    function obtenNotificacionesDiarias(const IDESTACION: Integer): AnsiString;
    function registraactividadrealizada(const IDACTIVIDADMANTENIMIENTO: Integer; const NOTA: AnsiString; const STATUS: AnsiString): AnsiString;
    function CancelarLote(const LoteID: Integer; const UsuarioID: Integer): AnsiString;
    function StatusCupon(const Codigo: AnsiString): AnsiString;
    function BuscarReporte(const Nombre: AnsiString): TReporte;
    procedure GeneraCupones(const LoteCuponID: Integer; const ClienteID: Integer; const EstacionID: Integer; const Identificador: Integer;
                            const Cupones: AGeneraCupon; const TipoCupon: Boolean; const Serie: AnsiString; const aFolio: Integer);
    function DocumentosConSaldo(const ClienteID: Integer): TDocumentosConSaldoArray;
    function FacturaRecibo(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                           const SerieFactura: AnsiString; const ProductoID: Integer): Integer;
    function CaracteresCupon: AnsiString;
    function ObtenCuponValido(const Barras: AnsiString; const Grupo: Integer; const CuponAutoriza: TAutoriza): TCuponValido;
    function GuardaAutorizacion(const Autorizacion: TAutoriza): Boolean;
    function DatosVehiculo(const VehiculoID: Integer): TDatosVehiculo;
    function CancelaGrupo(const Grupo: Integer): Boolean;
    function QuemarCupon(const Consumo: TConsumo; const Barras: AnsiString): Boolean;
    function GuardarConsumo(const Consumo: TConsumo): Boolean;
    function DatosVerifica(const VehiculoID: Integer): TDatosVerifica;
    function DatosVerificaReferencia(const Referencia: AnsiString): TDatosVerifica;
    function TarjetaConsumo(const Tarjeta: AnsiString; const Consumo: TConsumo): Boolean;
    procedure ActualizaHistorial(const Consumo: TConsumo);
    function EliminaAutorizacionVehiculo(const Referencia: AnsiString): Boolean;
    function AgregaSecuencias(const Consumos: aConsumo): Boolean;
    function AutorizaTarjeta(const Autoriza: TAutoriza): TTarjeta;
    function BuscaAutorizacionTarjeta(const ClienteID: Integer; const VehiculoID: Integer; const GasolineroID: Integer): Boolean;
    procedure EstadoDispensario(const Dispensario: Integer; const Estado: AnsiString);
    procedure EntradaBitacora(const Descripcion: AnsiString; const Tipo: eTipoBitacora; const Alerta: Boolean);
    procedure AutoCambioPrecio;
    procedure CierraTurno(const BombaID: Integer; const TurnoID: Integer; const FinLecturas: aLecturas);
    procedure CreaTurno(const BombaID: Integer; const TurnoID: Integer; const InicioLecturas: aLecturas);
    function GuardaVenta(const nBomba: Integer): Integer;
    function UltimoTurnoBomba: aTurnoBomba;
    procedure ValidaSecuencias(const TurnoID: Integer);
    function ValidaSecuenciasC(const EstacionID: Integer; const Secuencias: AnsiString): AnsiString;
    function Datos: TGlobal;
    procedure CancelaGrupoC(const BombaID: Integer; const Grupo: Integer);
    function PrefijaGrupo(const Bomba: Integer; const Grupo: Integer; const Terminal: AnsiString; const Cupon: TCupon): AnsiString;
    function PrefijaTarjeta(const nBomba: Integer; const Tarjeta: AnsiString; const NIP: AnsiString; const Odometro: AnsiString;
                            const Terminal: AnsiString): AnsiString;
    procedure AsignaBombas;
    procedure AsignaProductos;
    procedure AsignaDispositivo;
    procedure AsignaTanques;
    procedure IncrementarImpreso(const Secuencia: Integer);
    function PrefijaTarjetaFrecuente(const nBomba: Integer; const Tarjeta: AnsiString; const Odometro: AnsiString; const Terminal: AnsiString): AnsiString;
    function GenerarFacturas(const Serie: AnsiString; const fol: Integer; const FechaCorte: DateTime; const FechaContable: DateTime;
                             const DiaFacturar: Integer; const Magna: Boolean; const Premium: Boolean; const Diesel: Boolean; const EmpleadoID: Integer): Integer;
    function EliminaFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
    function CambioFactura(const Serie: AnsiString; const Folio: Integer; const NuevaSerie: AnsiString; const NuevoFolio: Integer;
                           const FechaImpresion: DateTime): AnsiString;
    function DatosFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
    function GeneraFacturasXCliente(const ClienteID: Integer; const Productos: AnsiString; const FechaCorte: DateTime; const FechaContable: DateTime;
                                    const Serie: AnsiString; const aFolio: Integer; const EmpleadoID: Integer): Integer;
    function GasolineroID: Integer;
  end;

implementation

uses
  {vcl:} SysUtils,
  {RemObjects:} uROEventRepository, uROEventReceiver, uROSerializer, uROClient, uRORes;

{ AAgrupacion }

procedure AAgrupacion.Assign(iSource: TPersistent);
var
  lSource: AAgrupacion;
  i: Integer;
  lItem: TAgrupacion;
begin
  if (iSource is AAgrupacion) then begin
    lSource := AAgrupacion(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TAgrupacion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function AAgrupacion.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TAgrupacion);
end;

class function AAgrupacion.GetItemClass: System.TClass;
begin
  Result := TAgrupacion;
end;

class function AAgrupacion.GetItemSize: Integer;
begin
  Result := System.SizeOf(TAgrupacion);
end;

function AAgrupacion.GetItems(aIndex: Integer): TAgrupacion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function AAgrupacion.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure AAgrupacion.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure AAgrupacion.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure AAgrupacion.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure AAgrupacion.SetItems(aIndex: Integer; const Value: TAgrupacion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure AAgrupacion.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TAgrupacion.Create;
  FCount := ElementCount;
end;

function AAgrupacion.GetCount: Integer;
begin
  Result := FCount;
end;

procedure AAgrupacion.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function AAgrupacion.Add: TAgrupacion;
begin
  Result := TAgrupacion.Create;
  Self.Add(Result);
end;

function AAgrupacion.Add(const Value:TAgrupacion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure AAgrupacion.ReadComplex(ASerializer: TObject);
var
  lval: TAgrupacion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TAgrupacion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure AAgrupacion.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(AAgrupacion);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TAgrupacion, i);
end;

{ AAgrupacionBomba }

procedure AAgrupacionBomba.Assign(iSource: TPersistent);
var
  lSource: AAgrupacionBomba;
  i: Integer;
begin
  if (iSource is AAgrupacionBomba) then begin
    lSource := AAgrupacionBomba(iSource);
    Self.Clear();

    Self.Resize(lSource.Count);
    for i := 0 to lSource.Count-1 do
      Self.Items[i] := lSource.Items[i];
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function AAgrupacionBomba.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(Integer);
end;

class function AAgrupacionBomba.GetItemSize: Integer;
begin
  Result := System.SizeOf(Integer);
end;

function AAgrupacionBomba.GetItems(aIndex: Integer): Integer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function AAgrupacionBomba.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := @fItems[aIndex];
end;

procedure AAgrupacionBomba.Clear;
begin
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure AAgrupacionBomba.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure AAgrupacionBomba.SetItems(aIndex: Integer; const Value: Integer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  fItems[aIndex] := Value;
end;

procedure AAgrupacionBomba.Resize(ElementCount: Integer);
begin
  if fCount = ElementCount then Exit;
  System.SetLength(fItems, ElementCount);
  FCount := ElementCount;
end;

function AAgrupacionBomba.GetCount: Integer;
begin
  Result := FCount;
end;

procedure AAgrupacionBomba.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function AAgrupacionBomba.Add(const Value: Integer): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

function AAgrupacionBomba.GetIndex(const aValue: Integer; const aStartFrom: Integer = 0) : Integer;
begin
  Result := Self.IndexOf(aValue, aStartFrom);
end;

function AAgrupacionBomba.GetIndex(const aPropertyName : string;
  const aPropertyValue : Variant; StartFrom : Integer;
  Options : TROSearchOptions) : Integer;
begin
  Result := -1;
end;

function AAgrupacionBomba.IndexOf(const aValue: Integer; const aStartFrom: Integer = 0): Integer;
begin
  for Result := aStartFrom to Self.Count-1 do
    if Items[Result] = aValue then Exit;
  Result := -1;
end;

procedure AAgrupacionBomba.ReadComplex(ASerializer: TObject);
var
  lval: Integer;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadInteger(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TypInfo.otSLong, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure AAgrupacionBomba.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(AAgrupacionBomba);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteInteger(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TypInfo.otSLong, fItems[i], i);
end;

{ aBombas }

procedure aBombas.Assign(iSource: TPersistent);
var
  lSource: aBombas;
  i: Integer;
  lItem: TBomba;
begin
  if (iSource is aBombas) then begin
    lSource := aBombas(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TBomba(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aBombas.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TBomba);
end;

class function aBombas.GetItemClass: System.TClass;
begin
  Result := TBomba;
end;

class function aBombas.GetItemSize: Integer;
begin
  Result := System.SizeOf(TBomba);
end;

function aBombas.GetItems(aIndex: Integer): TBomba;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aBombas.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aBombas.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aBombas.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aBombas.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aBombas.SetItems(aIndex: Integer; const Value: TBomba);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aBombas.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TBomba.Create;
  FCount := ElementCount;
end;

function aBombas.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aBombas.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aBombas.Add: TBomba;
begin
  Result := TBomba.Create;
  Self.Add(Result);
end;

function aBombas.Add(const Value:TBomba): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aBombas.ReadComplex(ASerializer: TObject);
var
  lval: TBomba;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TBomba, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aBombas.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aBombas);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TBomba, i);
end;

{ aBoolean }

procedure aBoolean.Assign(iSource: TPersistent);
var
  lSource: aBoolean;
  i: Integer;
begin
  if (iSource is aBoolean) then begin
    lSource := aBoolean(iSource);
    Self.Clear();

    Self.Resize(lSource.Count);
    for i := 0 to lSource.Count-1 do
      Self.Items[i] := lSource.Items[i];
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aBoolean.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(Boolean);
end;

class function aBoolean.GetItemSize: Integer;
begin
  Result := System.SizeOf(Boolean);
end;

function aBoolean.GetItems(aIndex: Integer): Boolean;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aBoolean.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := @fItems[aIndex];
end;

procedure aBoolean.Clear;
begin
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aBoolean.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aBoolean.SetItems(aIndex: Integer; const Value: Boolean);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  fItems[aIndex] := Value;
end;

procedure aBoolean.Resize(ElementCount: Integer);
begin
  if fCount = ElementCount then Exit;
  System.SetLength(fItems, ElementCount);
  FCount := ElementCount;
end;

function aBoolean.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aBoolean.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aBoolean.Add(const Value: Boolean): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

function aBoolean.GetIndex(const aValue: Boolean; const aStartFrom: Integer = 0) : Integer;
begin
  Result := Self.IndexOf(aValue, aStartFrom);
end;

function aBoolean.GetIndex(const aPropertyName : string;
  const aPropertyValue : Variant; StartFrom : Integer;
  Options : TROSearchOptions) : Integer;
begin
  Result := -1;
end;

function aBoolean.IndexOf(const aValue: Boolean; const aStartFrom: Integer = 0): Integer;
begin
  for Result := aStartFrom to Self.Count-1 do
    if Items[Result] = aValue then Exit;
  Result := -1;
end;

procedure aBoolean.ReadComplex(ASerializer: TObject);
var
  lval: Boolean;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadEnumerated(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)),System.TypeInfo(boolean), lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aBoolean.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aBoolean);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteEnumerated(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)),System.TypeInfo(boolean), fItems[i], i);
end;

{ aCambioPrecios }

procedure aCambioPrecios.Assign(iSource: TPersistent);
var
  lSource: aCambioPrecios;
  i: Integer;
  lItem: TCambioPrecios;
begin
  if (iSource is aCambioPrecios) then begin
    lSource := aCambioPrecios(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TCambioPrecios(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aCambioPrecios.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TCambioPrecios);
end;

class function aCambioPrecios.GetItemClass: System.TClass;
begin
  Result := TCambioPrecios;
end;

class function aCambioPrecios.GetItemSize: Integer;
begin
  Result := System.SizeOf(TCambioPrecios);
end;

function aCambioPrecios.GetItems(aIndex: Integer): TCambioPrecios;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aCambioPrecios.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aCambioPrecios.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aCambioPrecios.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aCambioPrecios.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aCambioPrecios.SetItems(aIndex: Integer; const Value: TCambioPrecios);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aCambioPrecios.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TCambioPrecios.Create;
  FCount := ElementCount;
end;

function aCambioPrecios.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aCambioPrecios.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aCambioPrecios.Add: TCambioPrecios;
begin
  Result := TCambioPrecios.Create;
  Self.Add(Result);
end;

function aCambioPrecios.Add(const Value:TCambioPrecios): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aCambioPrecios.ReadComplex(ASerializer: TObject);
var
  lval: TCambioPrecios;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TCambioPrecios, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aCambioPrecios.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aCambioPrecios);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TCambioPrecios, i);
end;

{ aConsumo }

procedure aConsumo.Assign(iSource: TPersistent);
var
  lSource: aConsumo;
  i: Integer;
  lItem: TConsumo;
begin
  if (iSource is aConsumo) then begin
    lSource := aConsumo(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TConsumo(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aConsumo.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TConsumo);
end;

class function aConsumo.GetItemClass: System.TClass;
begin
  Result := TConsumo;
end;

class function aConsumo.GetItemSize: Integer;
begin
  Result := System.SizeOf(TConsumo);
end;

function aConsumo.GetItems(aIndex: Integer): TConsumo;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aConsumo.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aConsumo.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aConsumo.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aConsumo.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aConsumo.SetItems(aIndex: Integer; const Value: TConsumo);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aConsumo.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TConsumo.Create;
  FCount := ElementCount;
end;

function aConsumo.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aConsumo.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aConsumo.Add: TConsumo;
begin
  Result := TConsumo.Create;
  Self.Add(Result);
end;

function aConsumo.Add(const Value:TConsumo): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aConsumo.ReadComplex(ASerializer: TObject);
var
  lval: TConsumo;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TConsumo, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aConsumo.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aConsumo);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TConsumo, i);
end;

{ aContador }

procedure aContador.Assign(iSource: TPersistent);
var
  lSource: aContador;
  i: Integer;
  lItem: TContador;
begin
  if (iSource is aContador) then begin
    lSource := aContador(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TContador(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aContador.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TContador);
end;

class function aContador.GetItemClass: System.TClass;
begin
  Result := TContador;
end;

class function aContador.GetItemSize: Integer;
begin
  Result := System.SizeOf(TContador);
end;

function aContador.GetItems(aIndex: Integer): TContador;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aContador.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aContador.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aContador.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aContador.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aContador.SetItems(aIndex: Integer; const Value: TContador);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aContador.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TContador.Create;
  FCount := ElementCount;
end;

function aContador.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aContador.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aContador.Add: TContador;
begin
  Result := TContador.Create;
  Self.Add(Result);
end;

function aContador.Add(const Value:TContador): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aContador.ReadComplex(ASerializer: TObject);
var
  lval: TContador;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TContador, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aContador.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aContador);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TContador, i);
end;

{ ADatosCelular }

procedure ADatosCelular.Assign(iSource: TPersistent);
var
  lSource: ADatosCelular;
  i: Integer;
  lItem: TDatosCelular;
begin
  if (iSource is ADatosCelular) then begin
    lSource := ADatosCelular(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDatosCelular(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ADatosCelular.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDatosCelular);
end;

class function ADatosCelular.GetItemClass: System.TClass;
begin
  Result := TDatosCelular;
end;

class function ADatosCelular.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDatosCelular);
end;

function ADatosCelular.GetItems(aIndex: Integer): TDatosCelular;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ADatosCelular.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ADatosCelular.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ADatosCelular.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ADatosCelular.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ADatosCelular.SetItems(aIndex: Integer; const Value: TDatosCelular);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ADatosCelular.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDatosCelular.Create;
  FCount := ElementCount;
end;

function ADatosCelular.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ADatosCelular.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ADatosCelular.Add: TDatosCelular;
begin
  Result := TDatosCelular.Create;
  Self.Add(Result);
end;

function ADatosCelular.Add(const Value:TDatosCelular): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ADatosCelular.ReadComplex(ASerializer: TObject);
var
  lval: TDatosCelular;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDatosCelular, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ADatosCelular.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ADatosCelular);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDatosCelular, i);
end;

{ ADespachadorLiquidacion }

procedure ADespachadorLiquidacion.Assign(iSource: TPersistent);
var
  lSource: ADespachadorLiquidacion;
  i: Integer;
  lItem: TDespachadorLiquidacion;
begin
  if (iSource is ADespachadorLiquidacion) then begin
    lSource := ADespachadorLiquidacion(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDespachadorLiquidacion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ADespachadorLiquidacion.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDespachadorLiquidacion);
end;

class function ADespachadorLiquidacion.GetItemClass: System.TClass;
begin
  Result := TDespachadorLiquidacion;
end;

class function ADespachadorLiquidacion.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDespachadorLiquidacion);
end;

function ADespachadorLiquidacion.GetItems(aIndex: Integer): TDespachadorLiquidacion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ADespachadorLiquidacion.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ADespachadorLiquidacion.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ADespachadorLiquidacion.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ADespachadorLiquidacion.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ADespachadorLiquidacion.SetItems(aIndex: Integer; const Value: TDespachadorLiquidacion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ADespachadorLiquidacion.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDespachadorLiquidacion.Create;
  FCount := ElementCount;
end;

function ADespachadorLiquidacion.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ADespachadorLiquidacion.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ADespachadorLiquidacion.Add: TDespachadorLiquidacion;
begin
  Result := TDespachadorLiquidacion.Create;
  Self.Add(Result);
end;

function ADespachadorLiquidacion.Add(const Value:TDespachadorLiquidacion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ADespachadorLiquidacion.ReadComplex(ASerializer: TObject);
var
  lval: TDespachadorLiquidacion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDespachadorLiquidacion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ADespachadorLiquidacion.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ADespachadorLiquidacion);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDespachadorLiquidacion, i);
end;

{ ADetalleExpress }

procedure ADetalleExpress.Assign(iSource: TPersistent);
var
  lSource: ADetalleExpress;
  i: Integer;
  lItem: TDetalleExpress;
begin
  if (iSource is ADetalleExpress) then begin
    lSource := ADetalleExpress(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleExpress(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ADetalleExpress.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleExpress);
end;

class function ADetalleExpress.GetItemClass: System.TClass;
begin
  Result := TDetalleExpress;
end;

class function ADetalleExpress.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleExpress);
end;

function ADetalleExpress.GetItems(aIndex: Integer): TDetalleExpress;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ADetalleExpress.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ADetalleExpress.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ADetalleExpress.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ADetalleExpress.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ADetalleExpress.SetItems(aIndex: Integer; const Value: TDetalleExpress);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ADetalleExpress.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleExpress.Create;
  FCount := ElementCount;
end;

function ADetalleExpress.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ADetalleExpress.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ADetalleExpress.Add: TDetalleExpress;
begin
  Result := TDetalleExpress.Create;
  Self.Add(Result);
end;

function ADetalleExpress.Add(const Value:TDetalleExpress): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ADetalleExpress.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleExpress;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleExpress, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ADetalleExpress.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ADetalleExpress);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleExpress, i);
end;

{ ADetalleFacturaPemex }

procedure ADetalleFacturaPemex.Assign(iSource: TPersistent);
var
  lSource: ADetalleFacturaPemex;
  i: Integer;
  lItem: TDetalleFacturaPemex;
begin
  if (iSource is ADetalleFacturaPemex) then begin
    lSource := ADetalleFacturaPemex(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleFacturaPemex(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ADetalleFacturaPemex.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleFacturaPemex);
end;

class function ADetalleFacturaPemex.GetItemClass: System.TClass;
begin
  Result := TDetalleFacturaPemex;
end;

class function ADetalleFacturaPemex.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleFacturaPemex);
end;

function ADetalleFacturaPemex.GetItems(aIndex: Integer): TDetalleFacturaPemex;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ADetalleFacturaPemex.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ADetalleFacturaPemex.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ADetalleFacturaPemex.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ADetalleFacturaPemex.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ADetalleFacturaPemex.SetItems(aIndex: Integer; const Value: TDetalleFacturaPemex);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ADetalleFacturaPemex.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleFacturaPemex.Create;
  FCount := ElementCount;
end;

function ADetalleFacturaPemex.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ADetalleFacturaPemex.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ADetalleFacturaPemex.Add: TDetalleFacturaPemex;
begin
  Result := TDetalleFacturaPemex.Create;
  Self.Add(Result);
end;

function ADetalleFacturaPemex.Add(const Value:TDetalleFacturaPemex): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ADetalleFacturaPemex.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleFacturaPemex;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleFacturaPemex, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ADetalleFacturaPemex.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ADetalleFacturaPemex);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleFacturaPemex, i);
end;

{ ADetalleLiquidacion }

procedure ADetalleLiquidacion.Assign(iSource: TPersistent);
var
  lSource: ADetalleLiquidacion;
  i: Integer;
  lItem: TDetalleLiquidacion;
begin
  if (iSource is ADetalleLiquidacion) then begin
    lSource := ADetalleLiquidacion(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleLiquidacion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ADetalleLiquidacion.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleLiquidacion);
end;

class function ADetalleLiquidacion.GetItemClass: System.TClass;
begin
  Result := TDetalleLiquidacion;
end;

class function ADetalleLiquidacion.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleLiquidacion);
end;

function ADetalleLiquidacion.GetItems(aIndex: Integer): TDetalleLiquidacion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ADetalleLiquidacion.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ADetalleLiquidacion.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ADetalleLiquidacion.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ADetalleLiquidacion.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ADetalleLiquidacion.SetItems(aIndex: Integer; const Value: TDetalleLiquidacion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ADetalleLiquidacion.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleLiquidacion.Create;
  FCount := ElementCount;
end;

function ADetalleLiquidacion.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ADetalleLiquidacion.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ADetalleLiquidacion.Add: TDetalleLiquidacion;
begin
  Result := TDetalleLiquidacion.Create;
  Self.Add(Result);
end;

function ADetalleLiquidacion.Add(const Value:TDetalleLiquidacion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ADetalleLiquidacion.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleLiquidacion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleLiquidacion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ADetalleLiquidacion.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ADetalleLiquidacion);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleLiquidacion, i);
end;

{ aDispositivos }

procedure aDispositivos.Assign(iSource: TPersistent);
var
  lSource: aDispositivos;
  i: Integer;
  lItem: TDispositivos;
begin
  if (iSource is aDispositivos) then begin
    lSource := aDispositivos(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDispositivos(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aDispositivos.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDispositivos);
end;

class function aDispositivos.GetItemClass: System.TClass;
begin
  Result := TDispositivos;
end;

class function aDispositivos.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDispositivos);
end;

function aDispositivos.GetItems(aIndex: Integer): TDispositivos;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aDispositivos.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aDispositivos.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aDispositivos.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aDispositivos.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aDispositivos.SetItems(aIndex: Integer; const Value: TDispositivos);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aDispositivos.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDispositivos.Create;
  FCount := ElementCount;
end;

function aDispositivos.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aDispositivos.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aDispositivos.Add: TDispositivos;
begin
  Result := TDispositivos.Create;
  Self.Add(Result);
end;

function aDispositivos.Add(const Value:TDispositivos): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aDispositivos.ReadComplex(ASerializer: TObject);
var
  lval: TDispositivos;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDispositivos, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aDispositivos.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aDispositivos);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDispositivos, i);
end;

{ aGas }

procedure aGas.Assign(iSource: TPersistent);
var
  lSource: aGas;
  i: Integer;
  lItem: TGas;
begin
  if (iSource is aGas) then begin
    lSource := aGas(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TGas(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aGas.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TGas);
end;

class function aGas.GetItemClass: System.TClass;
begin
  Result := TGas;
end;

class function aGas.GetItemSize: Integer;
begin
  Result := System.SizeOf(TGas);
end;

function aGas.GetItems(aIndex: Integer): TGas;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aGas.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aGas.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aGas.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aGas.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aGas.SetItems(aIndex: Integer; const Value: TGas);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aGas.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TGas.Create;
  FCount := ElementCount;
end;

function aGas.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aGas.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aGas.Add: TGas;
begin
  Result := TGas.Create;
  Self.Add(Result);
end;

function aGas.Add(const Value:TGas): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aGas.ReadComplex(ASerializer: TObject);
var
  lval: TGas;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TGas, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aGas.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aGas);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TGas, i);
end;

{ AGeneraCupon }

procedure AGeneraCupon.Assign(iSource: TPersistent);
var
  lSource: AGeneraCupon;
  i: Integer;
  lItem: TGeneraCupon;
begin
  if (iSource is AGeneraCupon) then begin
    lSource := AGeneraCupon(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TGeneraCupon(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function AGeneraCupon.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TGeneraCupon);
end;

class function AGeneraCupon.GetItemClass: System.TClass;
begin
  Result := TGeneraCupon;
end;

class function AGeneraCupon.GetItemSize: Integer;
begin
  Result := System.SizeOf(TGeneraCupon);
end;

function AGeneraCupon.GetItems(aIndex: Integer): TGeneraCupon;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function AGeneraCupon.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure AGeneraCupon.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure AGeneraCupon.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure AGeneraCupon.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure AGeneraCupon.SetItems(aIndex: Integer; const Value: TGeneraCupon);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure AGeneraCupon.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TGeneraCupon.Create;
  FCount := ElementCount;
end;

function AGeneraCupon.GetCount: Integer;
begin
  Result := FCount;
end;

procedure AGeneraCupon.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function AGeneraCupon.Add: TGeneraCupon;
begin
  Result := TGeneraCupon.Create;
  Self.Add(Result);
end;

function AGeneraCupon.Add(const Value:TGeneraCupon): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure AGeneraCupon.ReadComplex(ASerializer: TObject);
var
  lval: TGeneraCupon;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TGeneraCupon, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure AGeneraCupon.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(AGeneraCupon);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TGeneraCupon, i);
end;

{ aLecturas }

procedure aLecturas.Assign(iSource: TPersistent);
var
  lSource: aLecturas;
  i: Integer;
  lItem: TLecturas;
begin
  if (iSource is aLecturas) then begin
    lSource := aLecturas(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TLecturas(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aLecturas.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TLecturas);
end;

class function aLecturas.GetItemClass: System.TClass;
begin
  Result := TLecturas;
end;

class function aLecturas.GetItemSize: Integer;
begin
  Result := System.SizeOf(TLecturas);
end;

function aLecturas.GetItems(aIndex: Integer): TLecturas;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aLecturas.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aLecturas.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aLecturas.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aLecturas.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aLecturas.SetItems(aIndex: Integer; const Value: TLecturas);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aLecturas.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TLecturas.Create;
  FCount := ElementCount;
end;

function aLecturas.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aLecturas.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aLecturas.Add: TLecturas;
begin
  Result := TLecturas.Create;
  Self.Add(Result);
end;

function aLecturas.Add(const Value:TLecturas): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aLecturas.ReadComplex(ASerializer: TObject);
var
  lval: TLecturas;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TLecturas, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aLecturas.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aLecturas);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TLecturas, i);
end;

{ aMaguera }

procedure aMaguera.Assign(iSource: TPersistent);
var
  lSource: aMaguera;
  i: Integer;
  lItem: TManguera;
begin
  if (iSource is aMaguera) then begin
    lSource := aMaguera(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TManguera(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aMaguera.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TManguera);
end;

class function aMaguera.GetItemClass: System.TClass;
begin
  Result := TManguera;
end;

class function aMaguera.GetItemSize: Integer;
begin
  Result := System.SizeOf(TManguera);
end;

function aMaguera.GetItems(aIndex: Integer): TManguera;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aMaguera.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aMaguera.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aMaguera.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aMaguera.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aMaguera.SetItems(aIndex: Integer; const Value: TManguera);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aMaguera.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TManguera.Create;
  FCount := ElementCount;
end;

function aMaguera.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aMaguera.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aMaguera.Add: TManguera;
begin
  Result := TManguera.Create;
  Self.Add(Result);
end;

function aMaguera.Add(const Value:TManguera): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aMaguera.ReadComplex(ASerializer: TObject);
var
  lval: TManguera;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TManguera, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aMaguera.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aMaguera);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TManguera, i);
end;

{ aNodos }

procedure aNodos.Assign(iSource: TPersistent);
var
  lSource: aNodos;
  i: Integer;
  lItem: TNodo;
begin
  if (iSource is aNodos) then begin
    lSource := aNodos(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TNodo(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aNodos.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TNodo);
end;

class function aNodos.GetItemClass: System.TClass;
begin
  Result := TNodo;
end;

class function aNodos.GetItemSize: Integer;
begin
  Result := System.SizeOf(TNodo);
end;

function aNodos.GetItems(aIndex: Integer): TNodo;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aNodos.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aNodos.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aNodos.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aNodos.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aNodos.SetItems(aIndex: Integer; const Value: TNodo);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aNodos.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TNodo.Create;
  FCount := ElementCount;
end;

function aNodos.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aNodos.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aNodos.Add: TNodo;
begin
  Result := TNodo.Create;
  Self.Add(Result);
end;

function aNodos.Add(const Value:TNodo): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aNodos.ReadComplex(ASerializer: TObject);
var
  lval: TNodo;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TNodo, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aNodos.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aNodos);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TNodo, i);
end;

{ aRecepciones }

procedure aRecepciones.Assign(iSource: TPersistent);
var
  lSource: aRecepciones;
  i: Integer;
  lItem: TRecepcion;
begin
  if (iSource is aRecepciones) then begin
    lSource := aRecepciones(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TRecepcion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aRecepciones.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TRecepcion);
end;

class function aRecepciones.GetItemClass: System.TClass;
begin
  Result := TRecepcion;
end;

class function aRecepciones.GetItemSize: Integer;
begin
  Result := System.SizeOf(TRecepcion);
end;

function aRecepciones.GetItems(aIndex: Integer): TRecepcion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aRecepciones.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aRecepciones.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aRecepciones.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aRecepciones.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aRecepciones.SetItems(aIndex: Integer; const Value: TRecepcion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aRecepciones.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TRecepcion.Create;
  FCount := ElementCount;
end;

function aRecepciones.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aRecepciones.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aRecepciones.Add: TRecepcion;
begin
  Result := TRecepcion.Create;
  Self.Add(Result);
end;

function aRecepciones.Add(const Value:TRecepcion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aRecepciones.ReadComplex(ASerializer: TObject);
var
  lval: TRecepcion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TRecepcion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aRecepciones.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aRecepciones);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TRecepcion, i);
end;

{ aRegistroVenta }

procedure aRegistroVenta.Assign(iSource: TPersistent);
var
  lSource: aRegistroVenta;
  i: Integer;
  lItem: TRegistroVenta;
begin
  if (iSource is aRegistroVenta) then begin
    lSource := aRegistroVenta(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TRegistroVenta(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aRegistroVenta.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TRegistroVenta);
end;

class function aRegistroVenta.GetItemClass: System.TClass;
begin
  Result := TRegistroVenta;
end;

class function aRegistroVenta.GetItemSize: Integer;
begin
  Result := System.SizeOf(TRegistroVenta);
end;

function aRegistroVenta.GetItems(aIndex: Integer): TRegistroVenta;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aRegistroVenta.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aRegistroVenta.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aRegistroVenta.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aRegistroVenta.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aRegistroVenta.SetItems(aIndex: Integer; const Value: TRegistroVenta);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aRegistroVenta.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TRegistroVenta.Create;
  FCount := ElementCount;
end;

function aRegistroVenta.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aRegistroVenta.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aRegistroVenta.Add: TRegistroVenta;
begin
  Result := TRegistroVenta.Create;
  Self.Add(Result);
end;

function aRegistroVenta.Add(const Value:TRegistroVenta): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aRegistroVenta.ReadComplex(ASerializer: TObject);
var
  lval: TRegistroVenta;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TRegistroVenta, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aRegistroVenta.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aRegistroVenta);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TRegistroVenta, i);
end;

{ ArrayOpcion }

procedure ArrayOpcion.Assign(iSource: TPersistent);
var
  lSource: ArrayOpcion;
  i: Integer;
  lItem: TOpcion;
begin
  if (iSource is ArrayOpcion) then begin
    lSource := ArrayOpcion(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TOpcion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ArrayOpcion.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TOpcion);
end;

class function ArrayOpcion.GetItemClass: System.TClass;
begin
  Result := TOpcion;
end;

class function ArrayOpcion.GetItemSize: Integer;
begin
  Result := System.SizeOf(TOpcion);
end;

function ArrayOpcion.GetItems(aIndex: Integer): TOpcion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ArrayOpcion.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ArrayOpcion.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ArrayOpcion.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ArrayOpcion.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ArrayOpcion.SetItems(aIndex: Integer; const Value: TOpcion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ArrayOpcion.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TOpcion.Create;
  FCount := ElementCount;
end;

function ArrayOpcion.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ArrayOpcion.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ArrayOpcion.Add: TOpcion;
begin
  Result := TOpcion.Create;
  Self.Add(Result);
end;

function ArrayOpcion.Add(const Value:TOpcion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ArrayOpcion.ReadComplex(ASerializer: TObject);
var
  lval: TOpcion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TOpcion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ArrayOpcion.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ArrayOpcion);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TOpcion, i);
end;

{ aTanques }

procedure aTanques.Assign(iSource: TPersistent);
var
  lSource: aTanques;
  i: Integer;
  lItem: TTanque;
begin
  if (iSource is aTanques) then begin
    lSource := aTanques(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TTanque(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aTanques.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TTanque);
end;

class function aTanques.GetItemClass: System.TClass;
begin
  Result := TTanque;
end;

class function aTanques.GetItemSize: Integer;
begin
  Result := System.SizeOf(TTanque);
end;

function aTanques.GetItems(aIndex: Integer): TTanque;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aTanques.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure aTanques.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure aTanques.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aTanques.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aTanques.SetItems(aIndex: Integer; const Value: TTanque);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure aTanques.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TTanque.Create;
  FCount := ElementCount;
end;

function aTanques.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aTanques.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aTanques.Add: TTanque;
begin
  Result := TTanque.Create;
  Self.Add(Result);
end;

function aTanques.Add(const Value:TTanque): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure aTanques.ReadComplex(ASerializer: TObject);
var
  lval: TTanque;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TTanque, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aTanques.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aTanques);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TTanque, i);
end;

{ ATCarteraVale }

procedure ATCarteraVale.Assign(iSource: TPersistent);
var
  lSource: ATCarteraVale;
  i: Integer;
  lItem: TCarteraVale;
begin
  if (iSource is ATCarteraVale) then begin
    lSource := ATCarteraVale(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TCarteraVale(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATCarteraVale.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TCarteraVale);
end;

class function ATCarteraVale.GetItemClass: System.TClass;
begin
  Result := TCarteraVale;
end;

class function ATCarteraVale.GetItemSize: Integer;
begin
  Result := System.SizeOf(TCarteraVale);
end;

function ATCarteraVale.GetItems(aIndex: Integer): TCarteraVale;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATCarteraVale.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATCarteraVale.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATCarteraVale.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATCarteraVale.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATCarteraVale.SetItems(aIndex: Integer; const Value: TCarteraVale);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATCarteraVale.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TCarteraVale.Create;
  FCount := ElementCount;
end;

function ATCarteraVale.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATCarteraVale.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATCarteraVale.Add: TCarteraVale;
begin
  Result := TCarteraVale.Create;
  Self.Add(Result);
end;

function ATCarteraVale.Add(const Value:TCarteraVale): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATCarteraVale.ReadComplex(ASerializer: TObject);
var
  lval: TCarteraVale;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TCarteraVale, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATCarteraVale.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATCarteraVale);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TCarteraVale, i);
end;

{ ATDetalleCupon }

procedure ATDetalleCupon.Assign(iSource: TPersistent);
var
  lSource: ATDetalleCupon;
  i: Integer;
  lItem: TDetalleCupon;
begin
  if (iSource is ATDetalleCupon) then begin
    lSource := ATDetalleCupon(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleCupon(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATDetalleCupon.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleCupon);
end;

class function ATDetalleCupon.GetItemClass: System.TClass;
begin
  Result := TDetalleCupon;
end;

class function ATDetalleCupon.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleCupon);
end;

function ATDetalleCupon.GetItems(aIndex: Integer): TDetalleCupon;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATDetalleCupon.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATDetalleCupon.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATDetalleCupon.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATDetalleCupon.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATDetalleCupon.SetItems(aIndex: Integer; const Value: TDetalleCupon);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATDetalleCupon.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleCupon.Create;
  FCount := ElementCount;
end;

function ATDetalleCupon.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATDetalleCupon.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATDetalleCupon.Add: TDetalleCupon;
begin
  Result := TDetalleCupon.Create;
  Self.Add(Result);
end;

function ATDetalleCupon.Add(const Value:TDetalleCupon): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATDetalleCupon.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleCupon;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleCupon, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATDetalleCupon.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATDetalleCupon);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleCupon, i);
end;

{ ATDetalleFactura }

procedure ATDetalleFactura.Assign(iSource: TPersistent);
var
  lSource: ATDetalleFactura;
  i: Integer;
  lItem: TDetalleFactura;
begin
  if (iSource is ATDetalleFactura) then begin
    lSource := ATDetalleFactura(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleFactura(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATDetalleFactura.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleFactura);
end;

class function ATDetalleFactura.GetItemClass: System.TClass;
begin
  Result := TDetalleFactura;
end;

class function ATDetalleFactura.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleFactura);
end;

function ATDetalleFactura.GetItems(aIndex: Integer): TDetalleFactura;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATDetalleFactura.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATDetalleFactura.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATDetalleFactura.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATDetalleFactura.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATDetalleFactura.SetItems(aIndex: Integer; const Value: TDetalleFactura);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATDetalleFactura.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleFactura.Create;
  FCount := ElementCount;
end;

function ATDetalleFactura.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATDetalleFactura.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATDetalleFactura.Add: TDetalleFactura;
begin
  Result := TDetalleFactura.Create;
  Self.Add(Result);
end;

function ATDetalleFactura.Add(const Value:TDetalleFactura): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATDetalleFactura.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleFactura;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleFactura, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATDetalleFactura.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATDetalleFactura);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleFactura, i);
end;

{ ATDetalleRecibo }

procedure ATDetalleRecibo.Assign(iSource: TPersistent);
var
  lSource: ATDetalleRecibo;
  i: Integer;
  lItem: TDetalleReciboPago;
begin
  if (iSource is ATDetalleRecibo) then begin
    lSource := ATDetalleRecibo(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleReciboPago(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATDetalleRecibo.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleReciboPago);
end;

class function ATDetalleRecibo.GetItemClass: System.TClass;
begin
  Result := TDetalleReciboPago;
end;

class function ATDetalleRecibo.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleReciboPago);
end;

function ATDetalleRecibo.GetItems(aIndex: Integer): TDetalleReciboPago;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATDetalleRecibo.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATDetalleRecibo.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATDetalleRecibo.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATDetalleRecibo.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATDetalleRecibo.SetItems(aIndex: Integer; const Value: TDetalleReciboPago);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATDetalleRecibo.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleReciboPago.Create;
  FCount := ElementCount;
end;

function ATDetalleRecibo.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATDetalleRecibo.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATDetalleRecibo.Add: TDetalleReciboPago;
begin
  Result := TDetalleReciboPago.Create;
  Self.Add(Result);
end;

function ATDetalleRecibo.Add(const Value:TDetalleReciboPago): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATDetalleRecibo.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleReciboPago;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleReciboPago, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATDetalleRecibo.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATDetalleRecibo);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleReciboPago, i);
end;

{ ATDetalleTransaccion }

procedure ATDetalleTransaccion.Assign(iSource: TPersistent);
var
  lSource: ATDetalleTransaccion;
  i: Integer;
  lItem: TDetalleTransaccion;
begin
  if (iSource is ATDetalleTransaccion) then begin
    lSource := ATDetalleTransaccion(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleTransaccion(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATDetalleTransaccion.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleTransaccion);
end;

class function ATDetalleTransaccion.GetItemClass: System.TClass;
begin
  Result := TDetalleTransaccion;
end;

class function ATDetalleTransaccion.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleTransaccion);
end;

function ATDetalleTransaccion.GetItems(aIndex: Integer): TDetalleTransaccion;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATDetalleTransaccion.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATDetalleTransaccion.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATDetalleTransaccion.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATDetalleTransaccion.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATDetalleTransaccion.SetItems(aIndex: Integer; const Value: TDetalleTransaccion);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATDetalleTransaccion.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleTransaccion.Create;
  FCount := ElementCount;
end;

function ATDetalleTransaccion.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATDetalleTransaccion.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATDetalleTransaccion.Add: TDetalleTransaccion;
begin
  Result := TDetalleTransaccion.Create;
  Self.Add(Result);
end;

function ATDetalleTransaccion.Add(const Value:TDetalleTransaccion): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATDetalleTransaccion.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleTransaccion;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleTransaccion, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATDetalleTransaccion.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATDetalleTransaccion);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleTransaccion, i);
end;

{ ATDetalleValeCreditoFactura }

procedure ATDetalleValeCreditoFactura.Assign(iSource: TPersistent);
var
  lSource: ATDetalleValeCreditoFactura;
  i: Integer;
  lItem: TDetalleValeCreditoFactura;
begin
  if (iSource is ATDetalleValeCreditoFactura) then begin
    lSource := ATDetalleValeCreditoFactura(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDetalleValeCreditoFactura(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATDetalleValeCreditoFactura.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDetalleValeCreditoFactura);
end;

class function ATDetalleValeCreditoFactura.GetItemClass: System.TClass;
begin
  Result := TDetalleValeCreditoFactura;
end;

class function ATDetalleValeCreditoFactura.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDetalleValeCreditoFactura);
end;

function ATDetalleValeCreditoFactura.GetItems(aIndex: Integer): TDetalleValeCreditoFactura;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATDetalleValeCreditoFactura.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATDetalleValeCreditoFactura.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATDetalleValeCreditoFactura.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATDetalleValeCreditoFactura.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATDetalleValeCreditoFactura.SetItems(aIndex: Integer; const Value: TDetalleValeCreditoFactura);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATDetalleValeCreditoFactura.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDetalleValeCreditoFactura.Create;
  FCount := ElementCount;
end;

function ATDetalleValeCreditoFactura.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATDetalleValeCreditoFactura.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATDetalleValeCreditoFactura.Add: TDetalleValeCreditoFactura;
begin
  Result := TDetalleValeCreditoFactura.Create;
  Self.Add(Result);
end;

function ATDetalleValeCreditoFactura.Add(const Value:TDetalleValeCreditoFactura): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATDetalleValeCreditoFactura.ReadComplex(ASerializer: TObject);
var
  lval: TDetalleValeCreditoFactura;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDetalleValeCreditoFactura, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATDetalleValeCreditoFactura.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATDetalleValeCreditoFactura);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDetalleValeCreditoFactura, i);
end;

{ ATFacturaElectronicaDetalleImportes }

procedure ATFacturaElectronicaDetalleImportes.Assign(iSource: TPersistent);
var
  lSource: ATFacturaElectronicaDetalleImportes;
  i: Integer;
  lItem: TFacturaElectronicaDetalleImportes;
begin
  if (iSource is ATFacturaElectronicaDetalleImportes) then begin
    lSource := ATFacturaElectronicaDetalleImportes(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TFacturaElectronicaDetalleImportes(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATFacturaElectronicaDetalleImportes.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TFacturaElectronicaDetalleImportes);
end;

class function ATFacturaElectronicaDetalleImportes.GetItemClass: System.TClass;
begin
  Result := TFacturaElectronicaDetalleImportes;
end;

class function ATFacturaElectronicaDetalleImportes.GetItemSize: Integer;
begin
  Result := System.SizeOf(TFacturaElectronicaDetalleImportes);
end;

function ATFacturaElectronicaDetalleImportes.GetItems(aIndex: Integer): TFacturaElectronicaDetalleImportes;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATFacturaElectronicaDetalleImportes.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATFacturaElectronicaDetalleImportes.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATFacturaElectronicaDetalleImportes.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATFacturaElectronicaDetalleImportes.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATFacturaElectronicaDetalleImportes.SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImportes);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATFacturaElectronicaDetalleImportes.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TFacturaElectronicaDetalleImportes.Create;
  FCount := ElementCount;
end;

function ATFacturaElectronicaDetalleImportes.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATFacturaElectronicaDetalleImportes.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATFacturaElectronicaDetalleImportes.Add: TFacturaElectronicaDetalleImportes;
begin
  Result := TFacturaElectronicaDetalleImportes.Create;
  Self.Add(Result);
end;

function ATFacturaElectronicaDetalleImportes.Add(const Value:TFacturaElectronicaDetalleImportes): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATFacturaElectronicaDetalleImportes.ReadComplex(ASerializer: TObject);
var
  lval: TFacturaElectronicaDetalleImportes;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TFacturaElectronicaDetalleImportes, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATFacturaElectronicaDetalleImportes.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATFacturaElectronicaDetalleImportes);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TFacturaElectronicaDetalleImportes, i);
end;

{ ATFacturaElectronicaDetalleImpuestos }

procedure ATFacturaElectronicaDetalleImpuestos.Assign(iSource: TPersistent);
var
  lSource: ATFacturaElectronicaDetalleImpuestos;
  i: Integer;
  lItem: TFacturaElectronicaDetalleImpuestos;
begin
  if (iSource is ATFacturaElectronicaDetalleImpuestos) then begin
    lSource := ATFacturaElectronicaDetalleImpuestos(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TFacturaElectronicaDetalleImpuestos(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATFacturaElectronicaDetalleImpuestos.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TFacturaElectronicaDetalleImpuestos);
end;

class function ATFacturaElectronicaDetalleImpuestos.GetItemClass: System.TClass;
begin
  Result := TFacturaElectronicaDetalleImpuestos;
end;

class function ATFacturaElectronicaDetalleImpuestos.GetItemSize: Integer;
begin
  Result := System.SizeOf(TFacturaElectronicaDetalleImpuestos);
end;

function ATFacturaElectronicaDetalleImpuestos.GetItems(aIndex: Integer): TFacturaElectronicaDetalleImpuestos;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATFacturaElectronicaDetalleImpuestos.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATFacturaElectronicaDetalleImpuestos.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATFacturaElectronicaDetalleImpuestos.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATFacturaElectronicaDetalleImpuestos.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATFacturaElectronicaDetalleImpuestos.SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImpuestos);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATFacturaElectronicaDetalleImpuestos.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TFacturaElectronicaDetalleImpuestos.Create;
  FCount := ElementCount;
end;

function ATFacturaElectronicaDetalleImpuestos.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATFacturaElectronicaDetalleImpuestos.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATFacturaElectronicaDetalleImpuestos.Add: TFacturaElectronicaDetalleImpuestos;
begin
  Result := TFacturaElectronicaDetalleImpuestos.Create;
  Self.Add(Result);
end;

function ATFacturaElectronicaDetalleImpuestos.Add(const Value:TFacturaElectronicaDetalleImpuestos): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATFacturaElectronicaDetalleImpuestos.ReadComplex(ASerializer: TObject);
var
  lval: TFacturaElectronicaDetalleImpuestos;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TFacturaElectronicaDetalleImpuestos, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATFacturaElectronicaDetalleImpuestos.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATFacturaElectronicaDetalleImpuestos);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TFacturaElectronicaDetalleImpuestos, i);
end;

{ ATipoValor }

procedure ATipoValor.Assign(iSource: TPersistent);
var
  lSource: ATipoValor;
  i: Integer;
  lItem: TTipoValor;
begin
  if (iSource is ATipoValor) then begin
    lSource := ATipoValor(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TTipoValor(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATipoValor.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TTipoValor);
end;

class function ATipoValor.GetItemClass: System.TClass;
begin
  Result := TTipoValor;
end;

class function ATipoValor.GetItemSize: Integer;
begin
  Result := System.SizeOf(TTipoValor);
end;

function ATipoValor.GetItems(aIndex: Integer): TTipoValor;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATipoValor.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATipoValor.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATipoValor.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATipoValor.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATipoValor.SetItems(aIndex: Integer; const Value: TTipoValor);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATipoValor.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TTipoValor.Create;
  FCount := ElementCount;
end;

function ATipoValor.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATipoValor.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATipoValor.Add: TTipoValor;
begin
  Result := TTipoValor.Create;
  Self.Add(Result);
end;

function ATipoValor.Add(const Value:TTipoValor): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATipoValor.ReadComplex(ASerializer: TObject);
var
  lval: TTipoValor;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TTipoValor, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATipoValor.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATipoValor);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TTipoValor, i);
end;

{ ATLiquidacionDetalle }

procedure ATLiquidacionDetalle.Assign(iSource: TPersistent);
var
  lSource: ATLiquidacionDetalle;
  i: Integer;
  lItem: TLiquidacionDetalle;
begin
  if (iSource is ATLiquidacionDetalle) then begin
    lSource := ATLiquidacionDetalle(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TLiquidacionDetalle(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATLiquidacionDetalle.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TLiquidacionDetalle);
end;

class function ATLiquidacionDetalle.GetItemClass: System.TClass;
begin
  Result := TLiquidacionDetalle;
end;

class function ATLiquidacionDetalle.GetItemSize: Integer;
begin
  Result := System.SizeOf(TLiquidacionDetalle);
end;

function ATLiquidacionDetalle.GetItems(aIndex: Integer): TLiquidacionDetalle;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATLiquidacionDetalle.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATLiquidacionDetalle.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATLiquidacionDetalle.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATLiquidacionDetalle.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATLiquidacionDetalle.SetItems(aIndex: Integer; const Value: TLiquidacionDetalle);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATLiquidacionDetalle.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TLiquidacionDetalle.Create;
  FCount := ElementCount;
end;

function ATLiquidacionDetalle.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATLiquidacionDetalle.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATLiquidacionDetalle.Add: TLiquidacionDetalle;
begin
  Result := TLiquidacionDetalle.Create;
  Self.Add(Result);
end;

function ATLiquidacionDetalle.Add(const Value:TLiquidacionDetalle): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATLiquidacionDetalle.ReadComplex(ASerializer: TObject);
var
  lval: TLiquidacionDetalle;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TLiquidacionDetalle, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATLiquidacionDetalle.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATLiquidacionDetalle);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TLiquidacionDetalle, i);
end;

{ ATLiquidacionProducto }

procedure ATLiquidacionProducto.Assign(iSource: TPersistent);
var
  lSource: ATLiquidacionProducto;
  i: Integer;
  lItem: TLiquidacionProducto;
begin
  if (iSource is ATLiquidacionProducto) then begin
    lSource := ATLiquidacionProducto(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TLiquidacionProducto(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATLiquidacionProducto.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TLiquidacionProducto);
end;

class function ATLiquidacionProducto.GetItemClass: System.TClass;
begin
  Result := TLiquidacionProducto;
end;

class function ATLiquidacionProducto.GetItemSize: Integer;
begin
  Result := System.SizeOf(TLiquidacionProducto);
end;

function ATLiquidacionProducto.GetItems(aIndex: Integer): TLiquidacionProducto;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATLiquidacionProducto.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATLiquidacionProducto.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATLiquidacionProducto.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATLiquidacionProducto.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATLiquidacionProducto.SetItems(aIndex: Integer; const Value: TLiquidacionProducto);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATLiquidacionProducto.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TLiquidacionProducto.Create;
  FCount := ElementCount;
end;

function ATLiquidacionProducto.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATLiquidacionProducto.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATLiquidacionProducto.Add: TLiquidacionProducto;
begin
  Result := TLiquidacionProducto.Create;
  Self.Add(Result);
end;

function ATLiquidacionProducto.Add(const Value:TLiquidacionProducto): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATLiquidacionProducto.ReadComplex(ASerializer: TObject);
var
  lval: TLiquidacionProducto;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TLiquidacionProducto, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATLiquidacionProducto.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATLiquidacionProducto);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TLiquidacionProducto, i);
end;

{ ATTicketFactura }

procedure ATTicketFactura.Assign(iSource: TPersistent);
var
  lSource: ATTicketFactura;
  i: Integer;
  lItem: TTicketFactura;
begin
  if (iSource is ATTicketFactura) then begin
    lSource := ATTicketFactura(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TTicketFactura(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function ATTicketFactura.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TTicketFactura);
end;

class function ATTicketFactura.GetItemClass: System.TClass;
begin
  Result := TTicketFactura;
end;

class function ATTicketFactura.GetItemSize: Integer;
begin
  Result := System.SizeOf(TTicketFactura);
end;

function ATTicketFactura.GetItems(aIndex: Integer): TTicketFactura;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function ATTicketFactura.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure ATTicketFactura.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure ATTicketFactura.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure ATTicketFactura.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure ATTicketFactura.SetItems(aIndex: Integer; const Value: TTicketFactura);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure ATTicketFactura.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TTicketFactura.Create;
  FCount := ElementCount;
end;

function ATTicketFactura.GetCount: Integer;
begin
  Result := FCount;
end;

procedure ATTicketFactura.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function ATTicketFactura.Add: TTicketFactura;
begin
  Result := TTicketFactura.Create;
  Self.Add(Result);
end;

function ATTicketFactura.Add(const Value:TTicketFactura): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure ATTicketFactura.ReadComplex(ASerializer: TObject);
var
  lval: TTicketFactura;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TTicketFactura, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure ATTicketFactura.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(ATTicketFactura);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TTicketFactura, i);
end;

{ aTurnoBomba }

procedure aTurnoBomba.Assign(iSource: TPersistent);
var
  lSource: aTurnoBomba;
  i: Integer;
begin
  if (iSource is aTurnoBomba) then begin
    lSource := aTurnoBomba(iSource);
    Self.Clear();

    Self.Resize(lSource.Count);
    for i := 0 to lSource.Count-1 do
      Self.Items[i] := lSource.Items[i];
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function aTurnoBomba.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(Integer);
end;

class function aTurnoBomba.GetItemSize: Integer;
begin
  Result := System.SizeOf(Integer);
end;

function aTurnoBomba.GetItems(aIndex: Integer): Integer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function aTurnoBomba.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := @fItems[aIndex];
end;

procedure aTurnoBomba.Clear;
begin
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure aTurnoBomba.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure aTurnoBomba.SetItems(aIndex: Integer; const Value: Integer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  fItems[aIndex] := Value;
end;

procedure aTurnoBomba.Resize(ElementCount: Integer);
begin
  if fCount = ElementCount then Exit;
  System.SetLength(fItems, ElementCount);
  FCount := ElementCount;
end;

function aTurnoBomba.GetCount: Integer;
begin
  Result := FCount;
end;

procedure aTurnoBomba.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function aTurnoBomba.Add(const Value: Integer): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

function aTurnoBomba.GetIndex(const aValue: Integer; const aStartFrom: Integer = 0) : Integer;
begin
  Result := Self.IndexOf(aValue, aStartFrom);
end;

function aTurnoBomba.GetIndex(const aPropertyName : string;
  const aPropertyValue : Variant; StartFrom : Integer;
  Options : TROSearchOptions) : Integer;
begin
  Result := -1;
end;

function aTurnoBomba.IndexOf(const aValue: Integer; const aStartFrom: Integer = 0): Integer;
begin
  for Result := aStartFrom to Self.Count-1 do
    if Items[Result] = aValue then Exit;
  Result := -1;
end;

procedure aTurnoBomba.ReadComplex(ASerializer: TObject);
var
  lval: Integer;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadInteger(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TypInfo.otSLong, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure aTurnoBomba.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(aTurnoBomba);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteInteger(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TypInfo.otSLong, fItems[i], i);
end;

{ TDocumentosConSaldoArray }

procedure TDocumentosConSaldoArray.Assign(iSource: TPersistent);
var
  lSource: TDocumentosConSaldoArray;
  i: Integer;
  lItem: TDocumentosConSaldo;
begin
  if (iSource is TDocumentosConSaldoArray) then begin
    lSource := TDocumentosConSaldoArray(iSource);
    Self.Clear();

    for i := 0 to lSource.Count-1 do
      if System.Assigned(lSource.Items[i]) then begin
        lItem := TDocumentosConSaldo(lSource.Items[i].ClassType.Create);
        lItem.Assign(lSource.Items[i]);
        Self.Add(lItem);
      end
      else begin
        Self.Add(nil);
      end;
  end
  else begin
    inherited Assign(iSource);
  end;
end;

class function TDocumentosConSaldoArray.GetItemType: PTypeInfo;
begin
  Result := System.TypeInfo(TDocumentosConSaldo);
end;

class function TDocumentosConSaldoArray.GetItemClass: System.TClass;
begin
  Result := TDocumentosConSaldo;
end;

class function TDocumentosConSaldoArray.GetItemSize: Integer;
begin
  Result := System.SizeOf(TDocumentosConSaldo);
end;

function TDocumentosConSaldoArray.GetItems(aIndex: Integer): TDocumentosConSaldo;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

function TDocumentosConSaldoArray.GetItemRef(aIndex: Integer): pointer;
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  Result := fItems[aIndex];
end;

procedure TDocumentosConSaldoArray.SetItemRef(aIndex: Integer; Ref: pointer);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if Ref <> fItems[aIndex] then begin
    if fItems[aIndex] <> nil then fItems[aIndex].Free;
    fItems[aIndex] := Ref;
  end;
end;

procedure TDocumentosConSaldoArray.Clear;
var i: Integer;
begin
  for i := 0 to (Self.Count-1) do fItems[i].Free();
  System.SetLength(fItems, 0);
  FCount := 0;
end;

procedure TDocumentosConSaldoArray.Delete(aIndex: Integer);
var
  i: Integer;
begin
  if (aIndex >= Self.Count) then uROClasses.RaiseError(err_InvalidIndex, [aIndex]);

  fItems[aIndex].Free;

  if (aIndex < Self.Count-1) then
    for i := aIndex to Self.Count-2 do fItems[i] := fItems[i+1];

  System.SetLength(fItems, Self.Count-1);
  System.Dec(FCount);
end;

procedure TDocumentosConSaldoArray.SetItems(aIndex: Integer; const Value: TDocumentosConSaldo);
begin
  if (aIndex < 0) or (aIndex >= Self.Count) then uROClasses.RaiseError(err_ArrayIndexOutOfBounds,[aIndex]);
  if fItems[aIndex] <> Value then begin
    fItems[aIndex].Free;
    fItems[aIndex] := Value;
  end;
end;

procedure TDocumentosConSaldoArray.Resize(ElementCount: Integer);
var
  i: Integer;
begin
  if fCount = ElementCount then Exit;
  for i := FCount -1 downto ElementCount do
    FItems[i].Free;
  System.SetLength(fItems, ElementCount);
  for i := FCount to ElementCount -1 do
    FItems[i] := TDocumentosConSaldo.Create;
  FCount := ElementCount;
end;

function TDocumentosConSaldoArray.GetCount: Integer;
begin
  Result := FCount;
end;

procedure TDocumentosConSaldoArray.Grow;
var
  lDelta, lCapacity: Integer;
begin
  lCapacity := System.Length(fItems);
  if lCapacity > 64 then
    lDelta := lCapacity div 4
  else
    if lCapacity > 8 then
      lDelta := 16
   else
      lDelta := 4;
  System.SetLength(fItems, lCapacity + lDelta);
end;

function TDocumentosConSaldoArray.Add: TDocumentosConSaldo;
begin
  Result := TDocumentosConSaldo.Create;
  Self.Add(Result);
end;

function TDocumentosConSaldoArray.Add(const Value:TDocumentosConSaldo): Integer;
begin
  Result := Self.Count;
  if System.Length(fItems) = Result then
    Self.Grow;
  fItems[Result] := Value;
  System.Inc(fCount);
end;

procedure TDocumentosConSaldoArray.ReadComplex(ASerializer: TObject);
var
  lval: TDocumentosConSaldo;
  i: Integer;
begin
  for i := 0 to Self.Count-1 do begin
      TROSerializer(ASerializer).ReadStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), TDocumentosConSaldo, lval, i);
    Self.Items[i] := lval;
  end;
end;

procedure TDocumentosConSaldoArray.WriteComplex(ASerializer: TObject);
var
  i: Integer;
begin
  TROSerializer(ASerializer).ChangeClass(TDocumentosConSaldoArray);
  for i := 0 to Self.Count-1 do
      TROSerializer(ASerializer).WriteStruct(TROSerializer(ASerializer).GetArrayElementName(GetItemType, GetItemRef(i)), fItems[i], TDocumentosConSaldo, i);
end;

{ TParametros }

procedure TParametros.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TParametros;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TParametros) then begin
    lSource := LibraryValesCupones_Intf.TParametros(iSource);

    Self.FechaIni := lSource.FechaIni;
    Self.FechaFin := lSource.FechaFin;
    Self.ClienteIni := lSource.ClienteIni;
    Self.ClienteFin := lSource.ClienteFin;
    Self.Estacion := lSource.Estacion;
    Self.TipoMantenimiento := lSource.TipoMantenimiento;
    Self.Factura := lSource.Factura;
    Self.FolioFactura := lSource.FolioFactura;
    Self.Serie := lSource.Serie;
    Self.Ejercicio := lSource.Ejercicio;
    Self.Periodo := lSource.Periodo;
    Self.Dia := lSource.Dia;
    Self.EjercicioFin := lSource.EjercicioFin;
    Self.PeriodoFin := lSource.PeriodoFin;
    Self.DiaFin := lSource.DiaFin;
    Self.Secuencia := lSource.Secuencia;
    Self.Status := lSource.Status;
    Self.Agrupacion := lSource.Agrupacion;
    Self.Almacen := lSource.Almacen;
    Self.SecuenciaIniLiquidacion := lSource.SecuenciaIniLiquidacion;
    Self.SecuenciaFinLiquidacion := lSource.SecuenciaFinLiquidacion;
    Self.FacturasdeCompras := lSource.FacturasdeCompras;
    Self.MontoFacturado := lSource.MontoFacturado;
    Self.Turno := lSource.Turno;
    Self.EmpleadoIni := lSource.EmpleadoIni;
    Self.EmpleadoFin := lSource.EmpleadoFin;
    Self.EstacionIni := lSource.EstacionIni;
    Self.EstacionFin := lSource.EstacionFin;
    Self.Usuario := lSource.Usuario;
    Self.FacturaIni := lSource.FacturaIni;
    Self.FacturaFin := lSource.FacturaFin;
    Self.Identificador := lSource.Identificador;
    Self.Seleccion := lSource.Seleccion;
  end;
end;

procedure TParametros.ReadComplex(ASerializer: TObject);
var
  l_Agrupacion: Integer;
  l_Almacen: Integer;
  l_ClienteFin: Integer;
  l_ClienteIni: Integer;
  l_Dia: Integer;
  l_DiaFin: Integer;
  l_Ejercicio: Integer;
  l_EjercicioFin: Integer;
  l_EmpleadoFin: Integer;
  l_EmpleadoIni: Integer;
  l_Estacion: Integer;
  l_EstacionFin: Integer;
  l_EstacionIni: Integer;
  l_Factura: Integer;
  l_FacturaFin: Integer;
  l_FacturaIni: Integer;
  l_FacturasdeCompras: Integer;
  l_FechaFin: DateTime;
  l_FechaIni: DateTime;
  l_FolioFactura: Integer;
  l_Identificador: Integer;
  l_MontoFacturado: Double;
  l_Periodo: Integer;
  l_PeriodoFin: Integer;
  l_Secuencia: AnsiString;
  l_SecuenciaFinLiquidacion: Integer;
  l_SecuenciaIniLiquidacion: Integer;
  l_Seleccion: Integer;
  l_Serie: AnsiString;
  l_Status: AnsiString;
  l_TipoMantenimiento: Integer;
  l_Turno: Integer;
  l_Usuario: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FechaIni := Self.FechaIni;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaIni', l_FechaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaIni']));
    end;
    Self.FechaIni := l_FechaIni;
    l_FechaFin := Self.FechaFin;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFin', l_FechaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFin']));
    end;
    Self.FechaFin := l_FechaFin;
    l_ClienteIni := Self.ClienteIni;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteIni', TypInfo.otSLong, l_ClienteIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteIni']));
    end;
    Self.ClienteIni := l_ClienteIni;
    l_ClienteFin := Self.ClienteFin;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteFin', TypInfo.otSLong, l_ClienteFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteFin']));
    end;
    Self.ClienteFin := l_ClienteFin;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_TipoMantenimiento := Self.TipoMantenimiento;
    try
      TROSerializer(ASerializer).ReadInteger('TipoMantenimiento', TypInfo.otSLong, l_TipoMantenimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoMantenimiento']));
    end;
    Self.TipoMantenimiento := l_TipoMantenimiento;
    l_Factura := Self.Factura;
    try
      TROSerializer(ASerializer).ReadInteger('Factura', TypInfo.otSLong, l_Factura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Factura']));
    end;
    Self.Factura := l_Factura;
    l_FolioFactura := Self.FolioFactura;
    try
      TROSerializer(ASerializer).ReadInteger('FolioFactura', TypInfo.otSLong, l_FolioFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FolioFactura']));
    end;
    Self.FolioFactura := l_FolioFactura;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_EjercicioFin := Self.EjercicioFin;
    try
      TROSerializer(ASerializer).ReadInteger('EjercicioFin', TypInfo.otSLong, l_EjercicioFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EjercicioFin']));
    end;
    Self.EjercicioFin := l_EjercicioFin;
    l_PeriodoFin := Self.PeriodoFin;
    try
      TROSerializer(ASerializer).ReadInteger('PeriodoFin', TypInfo.otSLong, l_PeriodoFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PeriodoFin']));
    end;
    Self.PeriodoFin := l_PeriodoFin;
    l_DiaFin := Self.DiaFin;
    try
      TROSerializer(ASerializer).ReadInteger('DiaFin', TypInfo.otSLong, l_DiaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DiaFin']));
    end;
    Self.DiaFin := l_DiaFin;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Secuencia', l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_Agrupacion := Self.Agrupacion;
    try
      TROSerializer(ASerializer).ReadInteger('Agrupacion', TypInfo.otSLong, l_Agrupacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Agrupacion']));
    end;
    Self.Agrupacion := l_Agrupacion;
    l_Almacen := Self.Almacen;
    try
      TROSerializer(ASerializer).ReadInteger('Almacen', TypInfo.otSLong, l_Almacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Almacen']));
    end;
    Self.Almacen := l_Almacen;
    l_SecuenciaIniLiquidacion := Self.SecuenciaIniLiquidacion;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaIniLiquidacion', TypInfo.otSLong, l_SecuenciaIniLiquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaIniLiquidacion']));
    end;
    Self.SecuenciaIniLiquidacion := l_SecuenciaIniLiquidacion;
    l_SecuenciaFinLiquidacion := Self.SecuenciaFinLiquidacion;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaFinLiquidacion', TypInfo.otSLong, l_SecuenciaFinLiquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaFinLiquidacion']));
    end;
    Self.SecuenciaFinLiquidacion := l_SecuenciaFinLiquidacion;
    l_FacturasdeCompras := Self.FacturasdeCompras;
    try
      TROSerializer(ASerializer).ReadInteger('FacturasdeCompras', TypInfo.otSLong, l_FacturasdeCompras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturasdeCompras']));
    end;
    Self.FacturasdeCompras := l_FacturasdeCompras;
    l_MontoFacturado := Self.MontoFacturado;
    try
      TROSerializer(ASerializer).ReadDouble('MontoFacturado', TypInfo.ftDouble, l_MontoFacturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MontoFacturado']));
    end;
    Self.MontoFacturado := l_MontoFacturado;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_EmpleadoIni := Self.EmpleadoIni;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoIni', TypInfo.otSLong, l_EmpleadoIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoIni']));
    end;
    Self.EmpleadoIni := l_EmpleadoIni;
    l_EmpleadoFin := Self.EmpleadoFin;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoFin', TypInfo.otSLong, l_EmpleadoFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoFin']));
    end;
    Self.EmpleadoFin := l_EmpleadoFin;
    l_EstacionIni := Self.EstacionIni;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionIni', TypInfo.otSLong, l_EstacionIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionIni']));
    end;
    Self.EstacionIni := l_EstacionIni;
    l_EstacionFin := Self.EstacionFin;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionFin', TypInfo.otSLong, l_EstacionFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionFin']));
    end;
    Self.EstacionFin := l_EstacionFin;
    l_Usuario := Self.Usuario;
    try
      TROSerializer(ASerializer).ReadInteger('Usuario', TypInfo.otSLong, l_Usuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Usuario']));
    end;
    Self.Usuario := l_Usuario;
    l_FacturaIni := Self.FacturaIni;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaIni', TypInfo.otSLong, l_FacturaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaIni']));
    end;
    Self.FacturaIni := l_FacturaIni;
    l_FacturaFin := Self.FacturaFin;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaFin', TypInfo.otSLong, l_FacturaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaFin']));
    end;
    Self.FacturaFin := l_FacturaFin;
    l_Identificador := Self.Identificador;
    try
      TROSerializer(ASerializer).ReadInteger('Identificador', TypInfo.otSLong, l_Identificador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificador']));
    end;
    Self.Identificador := l_Identificador;
    l_Seleccion := Self.Seleccion;
    try
      TROSerializer(ASerializer).ReadInteger('Seleccion', TypInfo.otSLong, l_Seleccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Seleccion']));
    end;
    Self.Seleccion := l_Seleccion;
  end
  else begin
    l_Agrupacion := Self.Agrupacion;
    try
      TROSerializer(ASerializer).ReadInteger('Agrupacion', TypInfo.otSLong, l_Agrupacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Agrupacion']));
    end;
    Self.Agrupacion := l_Agrupacion;
    l_Almacen := Self.Almacen;
    try
      TROSerializer(ASerializer).ReadInteger('Almacen', TypInfo.otSLong, l_Almacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Almacen']));
    end;
    Self.Almacen := l_Almacen;
    l_ClienteFin := Self.ClienteFin;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteFin', TypInfo.otSLong, l_ClienteFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteFin']));
    end;
    Self.ClienteFin := l_ClienteFin;
    l_ClienteIni := Self.ClienteIni;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteIni', TypInfo.otSLong, l_ClienteIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteIni']));
    end;
    Self.ClienteIni := l_ClienteIni;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_DiaFin := Self.DiaFin;
    try
      TROSerializer(ASerializer).ReadInteger('DiaFin', TypInfo.otSLong, l_DiaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DiaFin']));
    end;
    Self.DiaFin := l_DiaFin;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_EjercicioFin := Self.EjercicioFin;
    try
      TROSerializer(ASerializer).ReadInteger('EjercicioFin', TypInfo.otSLong, l_EjercicioFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EjercicioFin']));
    end;
    Self.EjercicioFin := l_EjercicioFin;
    l_EmpleadoFin := Self.EmpleadoFin;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoFin', TypInfo.otSLong, l_EmpleadoFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoFin']));
    end;
    Self.EmpleadoFin := l_EmpleadoFin;
    l_EmpleadoIni := Self.EmpleadoIni;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoIni', TypInfo.otSLong, l_EmpleadoIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoIni']));
    end;
    Self.EmpleadoIni := l_EmpleadoIni;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_EstacionFin := Self.EstacionFin;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionFin', TypInfo.otSLong, l_EstacionFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionFin']));
    end;
    Self.EstacionFin := l_EstacionFin;
    l_EstacionIni := Self.EstacionIni;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionIni', TypInfo.otSLong, l_EstacionIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionIni']));
    end;
    Self.EstacionIni := l_EstacionIni;
    l_Factura := Self.Factura;
    try
      TROSerializer(ASerializer).ReadInteger('Factura', TypInfo.otSLong, l_Factura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Factura']));
    end;
    Self.Factura := l_Factura;
    l_FacturaFin := Self.FacturaFin;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaFin', TypInfo.otSLong, l_FacturaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaFin']));
    end;
    Self.FacturaFin := l_FacturaFin;
    l_FacturaIni := Self.FacturaIni;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaIni', TypInfo.otSLong, l_FacturaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaIni']));
    end;
    Self.FacturaIni := l_FacturaIni;
    l_FacturasdeCompras := Self.FacturasdeCompras;
    try
      TROSerializer(ASerializer).ReadInteger('FacturasdeCompras', TypInfo.otSLong, l_FacturasdeCompras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturasdeCompras']));
    end;
    Self.FacturasdeCompras := l_FacturasdeCompras;
    l_FechaFin := Self.FechaFin;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFin', l_FechaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFin']));
    end;
    Self.FechaFin := l_FechaFin;
    l_FechaIni := Self.FechaIni;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaIni', l_FechaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaIni']));
    end;
    Self.FechaIni := l_FechaIni;
    l_FolioFactura := Self.FolioFactura;
    try
      TROSerializer(ASerializer).ReadInteger('FolioFactura', TypInfo.otSLong, l_FolioFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FolioFactura']));
    end;
    Self.FolioFactura := l_FolioFactura;
    l_Identificador := Self.Identificador;
    try
      TROSerializer(ASerializer).ReadInteger('Identificador', TypInfo.otSLong, l_Identificador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificador']));
    end;
    Self.Identificador := l_Identificador;
    l_MontoFacturado := Self.MontoFacturado;
    try
      TROSerializer(ASerializer).ReadDouble('MontoFacturado', TypInfo.ftDouble, l_MontoFacturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MontoFacturado']));
    end;
    Self.MontoFacturado := l_MontoFacturado;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_PeriodoFin := Self.PeriodoFin;
    try
      TROSerializer(ASerializer).ReadInteger('PeriodoFin', TypInfo.otSLong, l_PeriodoFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PeriodoFin']));
    end;
    Self.PeriodoFin := l_PeriodoFin;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Secuencia', l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_SecuenciaFinLiquidacion := Self.SecuenciaFinLiquidacion;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaFinLiquidacion', TypInfo.otSLong, l_SecuenciaFinLiquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaFinLiquidacion']));
    end;
    Self.SecuenciaFinLiquidacion := l_SecuenciaFinLiquidacion;
    l_SecuenciaIniLiquidacion := Self.SecuenciaIniLiquidacion;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaIniLiquidacion', TypInfo.otSLong, l_SecuenciaIniLiquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaIniLiquidacion']));
    end;
    Self.SecuenciaIniLiquidacion := l_SecuenciaIniLiquidacion;
    l_Seleccion := Self.Seleccion;
    try
      TROSerializer(ASerializer).ReadInteger('Seleccion', TypInfo.otSLong, l_Seleccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Seleccion']));
    end;
    Self.Seleccion := l_Seleccion;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_TipoMantenimiento := Self.TipoMantenimiento;
    try
      TROSerializer(ASerializer).ReadInteger('TipoMantenimiento', TypInfo.otSLong, l_TipoMantenimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoMantenimiento']));
    end;
    Self.TipoMantenimiento := l_TipoMantenimiento;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_Usuario := Self.Usuario;
    try
      TROSerializer(ASerializer).ReadInteger('Usuario', TypInfo.otSLong, l_Usuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Usuario']));
    end;
    Self.Usuario := l_Usuario;
  end;
end;

procedure TParametros.WriteComplex(ASerializer: TObject);
var
  l_Agrupacion: Integer;
  l_Almacen: Integer;
  l_ClienteFin: Integer;
  l_ClienteIni: Integer;
  l_Dia: Integer;
  l_DiaFin: Integer;
  l_Ejercicio: Integer;
  l_EjercicioFin: Integer;
  l_EmpleadoFin: Integer;
  l_EmpleadoIni: Integer;
  l_Estacion: Integer;
  l_EstacionFin: Integer;
  l_EstacionIni: Integer;
  l_Factura: Integer;
  l_FacturaFin: Integer;
  l_FacturaIni: Integer;
  l_FacturasdeCompras: Integer;
  l_FechaFin: DateTime;
  l_FechaIni: DateTime;
  l_FolioFactura: Integer;
  l_Identificador: Integer;
  l_MontoFacturado: Double;
  l_Periodo: Integer;
  l_PeriodoFin: Integer;
  l_Secuencia: AnsiString;
  l_SecuenciaFinLiquidacion: Integer;
  l_SecuenciaIniLiquidacion: Integer;
  l_Seleccion: Integer;
  l_Serie: AnsiString;
  l_Status: AnsiString;
  l_TipoMantenimiento: Integer;
  l_Turno: Integer;
  l_Usuario: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TParametros);
    l_FechaIni := Self.FechaIni;
    TROSerializer(ASerializer).WriteDateTime('FechaIni', l_FechaIni);
    l_FechaFin := Self.FechaFin;
    TROSerializer(ASerializer).WriteDateTime('FechaFin', l_FechaFin);
    l_ClienteIni := Self.ClienteIni;
    TROSerializer(ASerializer).WriteInteger('ClienteIni', TypInfo.otSLong, l_ClienteIni);
    l_ClienteFin := Self.ClienteFin;
    TROSerializer(ASerializer).WriteInteger('ClienteFin', TypInfo.otSLong, l_ClienteFin);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_TipoMantenimiento := Self.TipoMantenimiento;
    TROSerializer(ASerializer).WriteInteger('TipoMantenimiento', TypInfo.otSLong, l_TipoMantenimiento);
    l_Factura := Self.Factura;
    TROSerializer(ASerializer).WriteInteger('Factura', TypInfo.otSLong, l_Factura);
    l_FolioFactura := Self.FolioFactura;
    TROSerializer(ASerializer).WriteInteger('FolioFactura', TypInfo.otSLong, l_FolioFactura);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_EjercicioFin := Self.EjercicioFin;
    TROSerializer(ASerializer).WriteInteger('EjercicioFin', TypInfo.otSLong, l_EjercicioFin);
    l_PeriodoFin := Self.PeriodoFin;
    TROSerializer(ASerializer).WriteInteger('PeriodoFin', TypInfo.otSLong, l_PeriodoFin);
    l_DiaFin := Self.DiaFin;
    TROSerializer(ASerializer).WriteInteger('DiaFin', TypInfo.otSLong, l_DiaFin);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteAnsiString('Secuencia', l_Secuencia);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_Agrupacion := Self.Agrupacion;
    TROSerializer(ASerializer).WriteInteger('Agrupacion', TypInfo.otSLong, l_Agrupacion);
    l_Almacen := Self.Almacen;
    TROSerializer(ASerializer).WriteInteger('Almacen', TypInfo.otSLong, l_Almacen);
    l_SecuenciaIniLiquidacion := Self.SecuenciaIniLiquidacion;
    TROSerializer(ASerializer).WriteInteger('SecuenciaIniLiquidacion', TypInfo.otSLong, l_SecuenciaIniLiquidacion);
    l_SecuenciaFinLiquidacion := Self.SecuenciaFinLiquidacion;
    TROSerializer(ASerializer).WriteInteger('SecuenciaFinLiquidacion', TypInfo.otSLong, l_SecuenciaFinLiquidacion);
    l_FacturasdeCompras := Self.FacturasdeCompras;
    TROSerializer(ASerializer).WriteInteger('FacturasdeCompras', TypInfo.otSLong, l_FacturasdeCompras);
    l_MontoFacturado := Self.MontoFacturado;
    TROSerializer(ASerializer).WriteDouble('MontoFacturado', TypInfo.ftDouble, l_MontoFacturado);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_EmpleadoIni := Self.EmpleadoIni;
    TROSerializer(ASerializer).WriteInteger('EmpleadoIni', TypInfo.otSLong, l_EmpleadoIni);
    l_EmpleadoFin := Self.EmpleadoFin;
    TROSerializer(ASerializer).WriteInteger('EmpleadoFin', TypInfo.otSLong, l_EmpleadoFin);
    l_EstacionIni := Self.EstacionIni;
    TROSerializer(ASerializer).WriteInteger('EstacionIni', TypInfo.otSLong, l_EstacionIni);
    l_EstacionFin := Self.EstacionFin;
    TROSerializer(ASerializer).WriteInteger('EstacionFin', TypInfo.otSLong, l_EstacionFin);
    l_Usuario := Self.Usuario;
    TROSerializer(ASerializer).WriteInteger('Usuario', TypInfo.otSLong, l_Usuario);
    l_FacturaIni := Self.FacturaIni;
    TROSerializer(ASerializer).WriteInteger('FacturaIni', TypInfo.otSLong, l_FacturaIni);
    l_FacturaFin := Self.FacturaFin;
    TROSerializer(ASerializer).WriteInteger('FacturaFin', TypInfo.otSLong, l_FacturaFin);
    l_Identificador := Self.Identificador;
    TROSerializer(ASerializer).WriteInteger('Identificador', TypInfo.otSLong, l_Identificador);
    l_Seleccion := Self.Seleccion;
    TROSerializer(ASerializer).WriteInteger('Seleccion', TypInfo.otSLong, l_Seleccion);
  end
  else begin
    l_Agrupacion := Self.Agrupacion;
    TROSerializer(ASerializer).WriteInteger('Agrupacion', TypInfo.otSLong, l_Agrupacion);
    l_Almacen := Self.Almacen;
    TROSerializer(ASerializer).WriteInteger('Almacen', TypInfo.otSLong, l_Almacen);
    l_ClienteFin := Self.ClienteFin;
    TROSerializer(ASerializer).WriteInteger('ClienteFin', TypInfo.otSLong, l_ClienteFin);
    l_ClienteIni := Self.ClienteIni;
    TROSerializer(ASerializer).WriteInteger('ClienteIni', TypInfo.otSLong, l_ClienteIni);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_DiaFin := Self.DiaFin;
    TROSerializer(ASerializer).WriteInteger('DiaFin', TypInfo.otSLong, l_DiaFin);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_EjercicioFin := Self.EjercicioFin;
    TROSerializer(ASerializer).WriteInteger('EjercicioFin', TypInfo.otSLong, l_EjercicioFin);
    l_EmpleadoFin := Self.EmpleadoFin;
    TROSerializer(ASerializer).WriteInteger('EmpleadoFin', TypInfo.otSLong, l_EmpleadoFin);
    l_EmpleadoIni := Self.EmpleadoIni;
    TROSerializer(ASerializer).WriteInteger('EmpleadoIni', TypInfo.otSLong, l_EmpleadoIni);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_EstacionFin := Self.EstacionFin;
    TROSerializer(ASerializer).WriteInteger('EstacionFin', TypInfo.otSLong, l_EstacionFin);
    l_EstacionIni := Self.EstacionIni;
    TROSerializer(ASerializer).WriteInteger('EstacionIni', TypInfo.otSLong, l_EstacionIni);
    l_Factura := Self.Factura;
    TROSerializer(ASerializer).WriteInteger('Factura', TypInfo.otSLong, l_Factura);
    l_FacturaFin := Self.FacturaFin;
    TROSerializer(ASerializer).WriteInteger('FacturaFin', TypInfo.otSLong, l_FacturaFin);
    l_FacturaIni := Self.FacturaIni;
    TROSerializer(ASerializer).WriteInteger('FacturaIni', TypInfo.otSLong, l_FacturaIni);
    l_FacturasdeCompras := Self.FacturasdeCompras;
    TROSerializer(ASerializer).WriteInteger('FacturasdeCompras', TypInfo.otSLong, l_FacturasdeCompras);
    l_FechaFin := Self.FechaFin;
    TROSerializer(ASerializer).WriteDateTime('FechaFin', l_FechaFin);
    l_FechaIni := Self.FechaIni;
    TROSerializer(ASerializer).WriteDateTime('FechaIni', l_FechaIni);
    l_FolioFactura := Self.FolioFactura;
    TROSerializer(ASerializer).WriteInteger('FolioFactura', TypInfo.otSLong, l_FolioFactura);
    l_Identificador := Self.Identificador;
    TROSerializer(ASerializer).WriteInteger('Identificador', TypInfo.otSLong, l_Identificador);
    l_MontoFacturado := Self.MontoFacturado;
    TROSerializer(ASerializer).WriteDouble('MontoFacturado', TypInfo.ftDouble, l_MontoFacturado);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_PeriodoFin := Self.PeriodoFin;
    TROSerializer(ASerializer).WriteInteger('PeriodoFin', TypInfo.otSLong, l_PeriodoFin);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteAnsiString('Secuencia', l_Secuencia);
    l_SecuenciaFinLiquidacion := Self.SecuenciaFinLiquidacion;
    TROSerializer(ASerializer).WriteInteger('SecuenciaFinLiquidacion', TypInfo.otSLong, l_SecuenciaFinLiquidacion);
    l_SecuenciaIniLiquidacion := Self.SecuenciaIniLiquidacion;
    TROSerializer(ASerializer).WriteInteger('SecuenciaIniLiquidacion', TypInfo.otSLong, l_SecuenciaIniLiquidacion);
    l_Seleccion := Self.Seleccion;
    TROSerializer(ASerializer).WriteInteger('Seleccion', TypInfo.otSLong, l_Seleccion);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_TipoMantenimiento := Self.TipoMantenimiento;
    TROSerializer(ASerializer).WriteInteger('TipoMantenimiento', TypInfo.otSLong, l_TipoMantenimiento);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_Usuario := Self.Usuario;
    TROSerializer(ASerializer).WriteInteger('Usuario', TypInfo.otSLong, l_Usuario);
  end;
end;

{ TParametrosCollection }
constructor TParametrosCollection.Create;
begin
  inherited Create(TParametros);
end;

constructor TParametrosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TParametrosCollection.Add: TParametros;
begin
  Result := TParametros(inherited Add);
end;

function TParametrosCollection.GetItems(aIndex: Integer): TParametros;
begin
  Result := TParametros(inherited Items[aIndex]);
end;

procedure TParametrosCollection.SetItems(aIndex: Integer; const Value: TParametros);
begin
  TParametros(inherited Items[aIndex]).Assign(Value);
end;

{ TFactura }

procedure TFactura.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFactura;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFactura) then begin
    lSource := LibraryValesCupones_Intf.TFactura(iSource);

    Self.Serie := lSource.Serie;
    Self.Fecha := lSource.Fecha;
    Self.Subtotal := lSource.Subtotal;
    Self.Impuesto := lSource.Impuesto;
    Self.Total := lSource.Total;
    Self.ImpuestoPorcentaje := lSource.ImpuestoPorcentaje;
    Self.Turno := lSource.Turno;
    Self.EstacionID := lSource.EstacionID;
    Self.ClienteID := lSource.ClienteID;
    Self.FormaPagoID := lSource.FormaPagoID;
    Self.TipoFacturaID := lSource.TipoFacturaID;
    Self.FacturaID := lSource.FacturaID;
    Self.Tickets := lSource.Tickets;
    Self.UsuarioID := lSource.UsuarioID;
    Self.ISR := lSource.ISR;
    Self.IVARTN := lSource.IVARTN;
    Self.IVA := lSource.IVA;
    Self.Folio := lSource.Folio;
    Self.FechaVencimiento := lSource.FechaVencimiento;
    Self.FechaImpresion := lSource.FechaImpresion;
    Self.Ejercicio := lSource.Ejercicio;
    Self.Periodo := lSource.Periodo;
    Self.Saldo := lSource.Saldo;
    Self.MovimientoID := lSource.MovimientoID;
  end;
end;

procedure TFactura.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_Ejercicio: Integer;
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_FechaImpresion: DateTime;
  l_FechaVencimiento: DateTime;
  l_Folio: Integer;
  l_FormaPagoID: Integer;
  l_Impuesto: Double;
  l_ImpuestoPorcentaje: Double;
  l_ISR: Double;
  l_IVA: Double;
  l_IVARTN: Double;
  l_MovimientoID: Integer;
  l_Periodo: Integer;
  l_Saldo: Double;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Tickets: AnsiString;
  l_TipoFacturaID: Integer;
  l_Total: Double;
  l_Turno: Integer;
  l_UsuarioID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoPorcentaje']));
    end;
    Self.ImpuestoPorcentaje := l_ImpuestoPorcentaje;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_FormaPagoID := Self.FormaPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPagoID']));
    end;
    Self.FormaPagoID := l_FormaPagoID;
    l_TipoFacturaID := Self.TipoFacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoFacturaID', TypInfo.otSLong, l_TipoFacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoFacturaID']));
    end;
    Self.TipoFacturaID := l_TipoFacturaID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Tickets := Self.Tickets;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tickets', l_Tickets);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tickets']));
    end;
    Self.Tickets := l_Tickets;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
    l_ISR := Self.ISR;
    try
      TROSerializer(ASerializer).ReadDouble('ISR', TypInfo.ftDouble, l_ISR);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ISR']));
    end;
    Self.ISR := l_ISR;
    l_IVARTN := Self.IVARTN;
    try
      TROSerializer(ASerializer).ReadDouble('IVARTN', TypInfo.ftDouble, l_IVARTN);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVARTN']));
    end;
    Self.IVARTN := l_IVARTN;
    l_IVA := Self.IVA;
    try
      TROSerializer(ASerializer).ReadDouble('IVA', TypInfo.ftDouble, l_IVA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVA']));
    end;
    Self.IVA := l_IVA;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_FechaVencimiento := Self.FechaVencimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaVencimiento', l_FechaVencimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaVencimiento']));
    end;
    Self.FechaVencimiento := l_FechaVencimiento;
    l_FechaImpresion := Self.FechaImpresion;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaImpresion', l_FechaImpresion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaImpresion']));
    end;
    Self.FechaImpresion := l_FechaImpresion;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_MovimientoID := Self.MovimientoID;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoID']));
    end;
    Self.MovimientoID := l_MovimientoID;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_FechaImpresion := Self.FechaImpresion;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaImpresion', l_FechaImpresion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaImpresion']));
    end;
    Self.FechaImpresion := l_FechaImpresion;
    l_FechaVencimiento := Self.FechaVencimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaVencimiento', l_FechaVencimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaVencimiento']));
    end;
    Self.FechaVencimiento := l_FechaVencimiento;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_FormaPagoID := Self.FormaPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPagoID']));
    end;
    Self.FormaPagoID := l_FormaPagoID;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoPorcentaje']));
    end;
    Self.ImpuestoPorcentaje := l_ImpuestoPorcentaje;
    l_ISR := Self.ISR;
    try
      TROSerializer(ASerializer).ReadDouble('ISR', TypInfo.ftDouble, l_ISR);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ISR']));
    end;
    Self.ISR := l_ISR;
    l_IVA := Self.IVA;
    try
      TROSerializer(ASerializer).ReadDouble('IVA', TypInfo.ftDouble, l_IVA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVA']));
    end;
    Self.IVA := l_IVA;
    l_IVARTN := Self.IVARTN;
    try
      TROSerializer(ASerializer).ReadDouble('IVARTN', TypInfo.ftDouble, l_IVARTN);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVARTN']));
    end;
    Self.IVARTN := l_IVARTN;
    l_MovimientoID := Self.MovimientoID;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoID']));
    end;
    Self.MovimientoID := l_MovimientoID;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Tickets := Self.Tickets;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tickets', l_Tickets);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tickets']));
    end;
    Self.Tickets := l_Tickets;
    l_TipoFacturaID := Self.TipoFacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoFacturaID', TypInfo.otSLong, l_TipoFacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoFacturaID']));
    end;
    Self.TipoFacturaID := l_TipoFacturaID;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
  end;
end;

procedure TFactura.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_Ejercicio: Integer;
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_FechaImpresion: DateTime;
  l_FechaVencimiento: DateTime;
  l_Folio: Integer;
  l_FormaPagoID: Integer;
  l_Impuesto: Double;
  l_ImpuestoPorcentaje: Double;
  l_ISR: Double;
  l_IVA: Double;
  l_IVARTN: Double;
  l_MovimientoID: Integer;
  l_Periodo: Integer;
  l_Saldo: Double;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Tickets: AnsiString;
  l_TipoFacturaID: Integer;
  l_Total: Double;
  l_Turno: Integer;
  l_UsuarioID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFactura);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    TROSerializer(ASerializer).WriteDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_FormaPagoID := Self.FormaPagoID;
    TROSerializer(ASerializer).WriteInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    l_TipoFacturaID := Self.TipoFacturaID;
    TROSerializer(ASerializer).WriteInteger('TipoFacturaID', TypInfo.otSLong, l_TipoFacturaID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Tickets := Self.Tickets;
    TROSerializer(ASerializer).WriteAnsiString('Tickets', l_Tickets);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    l_ISR := Self.ISR;
    TROSerializer(ASerializer).WriteDouble('ISR', TypInfo.ftDouble, l_ISR);
    l_IVARTN := Self.IVARTN;
    TROSerializer(ASerializer).WriteDouble('IVARTN', TypInfo.ftDouble, l_IVARTN);
    l_IVA := Self.IVA;
    TROSerializer(ASerializer).WriteDouble('IVA', TypInfo.ftDouble, l_IVA);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_FechaVencimiento := Self.FechaVencimiento;
    TROSerializer(ASerializer).WriteDateTime('FechaVencimiento', l_FechaVencimiento);
    l_FechaImpresion := Self.FechaImpresion;
    TROSerializer(ASerializer).WriteDateTime('FechaImpresion', l_FechaImpresion);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_MovimientoID := Self.MovimientoID;
    TROSerializer(ASerializer).WriteInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_FechaImpresion := Self.FechaImpresion;
    TROSerializer(ASerializer).WriteDateTime('FechaImpresion', l_FechaImpresion);
    l_FechaVencimiento := Self.FechaVencimiento;
    TROSerializer(ASerializer).WriteDateTime('FechaVencimiento', l_FechaVencimiento);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_FormaPagoID := Self.FormaPagoID;
    TROSerializer(ASerializer).WriteInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    TROSerializer(ASerializer).WriteDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    l_ISR := Self.ISR;
    TROSerializer(ASerializer).WriteDouble('ISR', TypInfo.ftDouble, l_ISR);
    l_IVA := Self.IVA;
    TROSerializer(ASerializer).WriteDouble('IVA', TypInfo.ftDouble, l_IVA);
    l_IVARTN := Self.IVARTN;
    TROSerializer(ASerializer).WriteDouble('IVARTN', TypInfo.ftDouble, l_IVARTN);
    l_MovimientoID := Self.MovimientoID;
    TROSerializer(ASerializer).WriteInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Tickets := Self.Tickets;
    TROSerializer(ASerializer).WriteAnsiString('Tickets', l_Tickets);
    l_TipoFacturaID := Self.TipoFacturaID;
    TROSerializer(ASerializer).WriteInteger('TipoFacturaID', TypInfo.otSLong, l_TipoFacturaID);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
  end;
end;

{ TFacturaCollection }
constructor TFacturaCollection.Create;
begin
  inherited Create(TFactura);
end;

constructor TFacturaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFacturaCollection.Add: TFactura;
begin
  Result := TFactura(inherited Add);
end;

function TFacturaCollection.GetItems(aIndex: Integer): TFactura;
begin
  Result := TFactura(inherited Items[aIndex]);
end;

procedure TFacturaCollection.SetItems(aIndex: Integer; const Value: TFactura);
begin
  TFactura(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleFactura }

procedure TDetalleFactura.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleFactura;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleFactura) then begin
    lSource := LibraryValesCupones_Intf.TDetalleFactura(iSource);

    Self.Cantidad := lSource.Cantidad;
    Self.Precio := lSource.Precio;
    Self.DetalleFacturaId := lSource.DetalleFacturaId;
    Self.Importe := lSource.Importe;
    Self.FacturaID := lSource.FacturaID;
    Self.ProductoID := lSource.ProductoID;
    Self.Codigo := lSource.Codigo;
    Self.Descripcion := lSource.Descripcion;
  end;
end;

procedure TDetalleFactura.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Codigo: AnsiString;
  l_Descripcion: AnsiString;
  l_DetalleFacturaId: Integer;
  l_FacturaID: Integer;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_DetalleFacturaId := Self.DetalleFacturaId;
    try
      TROSerializer(ASerializer).ReadInteger('DetalleFacturaId', TypInfo.otSLong, l_DetalleFacturaId);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFacturaId']));
    end;
    Self.DetalleFacturaId := l_DetalleFacturaId;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_DetalleFacturaId := Self.DetalleFacturaId;
    try
      TROSerializer(ASerializer).ReadInteger('DetalleFacturaId', TypInfo.otSLong, l_DetalleFacturaId);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFacturaId']));
    end;
    Self.DetalleFacturaId := l_DetalleFacturaId;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
  end;
end;

procedure TDetalleFactura.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Codigo: AnsiString;
  l_Descripcion: AnsiString;
  l_DetalleFacturaId: Integer;
  l_FacturaID: Integer;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleFactura);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_DetalleFacturaId := Self.DetalleFacturaId;
    TROSerializer(ASerializer).WriteInteger('DetalleFacturaId', TypInfo.otSLong, l_DetalleFacturaId);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_DetalleFacturaId := Self.DetalleFacturaId;
    TROSerializer(ASerializer).WriteInteger('DetalleFacturaId', TypInfo.otSLong, l_DetalleFacturaId);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
  end;
end;

{ TDetalleFacturaCollection }
constructor TDetalleFacturaCollection.Create;
begin
  inherited Create(TDetalleFactura);
end;

constructor TDetalleFacturaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleFacturaCollection.Add: TDetalleFactura;
begin
  Result := TDetalleFactura(inherited Add);
end;

function TDetalleFacturaCollection.GetItems(aIndex: Integer): TDetalleFactura;
begin
  Result := TDetalleFactura(inherited Items[aIndex]);
end;

procedure TDetalleFacturaCollection.LoadFromArray(anArray: ATDetalleFactura);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleFacturaCollection.SaveToArray(anArray: ATDetalleFactura);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleFactura(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleFacturaCollection.SetItems(aIndex: Integer; const Value: TDetalleFactura);
begin
  TDetalleFactura(inherited Items[aIndex]).Assign(Value);
end;

{ TTicketFactura }

procedure TTicketFactura.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTicketFactura;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTicketFactura) then begin
    lSource := LibraryValesCupones_Intf.TTicketFactura(iSource);

    Self.TicketID := lSource.TicketID;
    Self.Fecha := lSource.Fecha;
    Self.Volumen := lSource.Volumen;
    Self.Precio := lSource.Precio;
    Self.Importe := lSource.Importe;
    Self.ProductoID := lSource.ProductoID;
    Self.FacturaID := lSource.FacturaID;
    Self.EstacionID := lSource.EstacionID;
  end;
end;

procedure TTicketFactura.ReadComplex(ASerializer: TObject);
var
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_TicketID: Integer;
  l_Volumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_TicketID := Self.TicketID;
    try
      TROSerializer(ASerializer).ReadInteger('TicketID', TypInfo.otSLong, l_TicketID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TicketID']));
    end;
    Self.TicketID := l_TicketID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Volumen := Self.Volumen;
    try
      TROSerializer(ASerializer).ReadDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumen']));
    end;
    Self.Volumen := l_Volumen;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
  end
  else begin
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_TicketID := Self.TicketID;
    try
      TROSerializer(ASerializer).ReadInteger('TicketID', TypInfo.otSLong, l_TicketID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TicketID']));
    end;
    Self.TicketID := l_TicketID;
    l_Volumen := Self.Volumen;
    try
      TROSerializer(ASerializer).ReadDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumen']));
    end;
    Self.Volumen := l_Volumen;
  end;
end;

procedure TTicketFactura.WriteComplex(ASerializer: TObject);
var
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_TicketID: Integer;
  l_Volumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTicketFactura);
    l_TicketID := Self.TicketID;
    TROSerializer(ASerializer).WriteInteger('TicketID', TypInfo.otSLong, l_TicketID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Volumen := Self.Volumen;
    TROSerializer(ASerializer).WriteDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
  end
  else begin
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_TicketID := Self.TicketID;
    TROSerializer(ASerializer).WriteInteger('TicketID', TypInfo.otSLong, l_TicketID);
    l_Volumen := Self.Volumen;
    TROSerializer(ASerializer).WriteDouble('Volumen', TypInfo.ftDouble, l_Volumen);
  end;
end;

{ TTicketFacturaCollection }
constructor TTicketFacturaCollection.Create;
begin
  inherited Create(TTicketFactura);
end;

constructor TTicketFacturaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTicketFacturaCollection.Add: TTicketFactura;
begin
  Result := TTicketFactura(inherited Add);
end;

function TTicketFacturaCollection.GetItems(aIndex: Integer): TTicketFactura;
begin
  Result := TTicketFactura(inherited Items[aIndex]);
end;

procedure TTicketFacturaCollection.LoadFromArray(anArray: ATTicketFactura);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TTicketFacturaCollection.SaveToArray(anArray: ATTicketFactura);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TTicketFactura(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TTicketFacturaCollection.SetItems(aIndex: Integer; const Value: TTicketFactura);
begin
  TTicketFactura(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleCupon }

procedure TDetalleCupon.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleCupon;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleCupon) then begin
    lSource := LibraryValesCupones_Intf.TDetalleCupon(iSource);

    Self.Denominacion := lSource.Denominacion;
    Self.Cantidad := lSource.Cantidad;
    Self.Referencia := lSource.Referencia;
    Self.FacturaID := lSource.FacturaID;
  end;
end;

procedure TDetalleCupon.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Denominacion: Double;
  l_FacturaID: Integer;
  l_Referencia: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Denominacion := Self.Denominacion;
    try
      TROSerializer(ASerializer).ReadDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Denominacion']));
    end;
    Self.Denominacion := l_Denominacion;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Denominacion := Self.Denominacion;
    try
      TROSerializer(ASerializer).ReadDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Denominacion']));
    end;
    Self.Denominacion := l_Denominacion;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
  end;
end;

procedure TDetalleCupon.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Denominacion: Double;
  l_FacturaID: Integer;
  l_Referencia: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleCupon);
    l_Denominacion := Self.Denominacion;
    TROSerializer(ASerializer).WriteDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Denominacion := Self.Denominacion;
    TROSerializer(ASerializer).WriteDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
  end;
end;

{ TDetalleCuponCollection }
constructor TDetalleCuponCollection.Create;
begin
  inherited Create(TDetalleCupon);
end;

constructor TDetalleCuponCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleCuponCollection.Add: TDetalleCupon;
begin
  Result := TDetalleCupon(inherited Add);
end;

function TDetalleCuponCollection.GetItems(aIndex: Integer): TDetalleCupon;
begin
  Result := TDetalleCupon(inherited Items[aIndex]);
end;

procedure TDetalleCuponCollection.LoadFromArray(anArray: ATDetalleCupon);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleCuponCollection.SaveToArray(anArray: ATDetalleCupon);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleCupon(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleCuponCollection.SetItems(aIndex: Integer; const Value: TDetalleCupon);
begin
  TDetalleCupon(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleValeCreditoFactura }

procedure TDetalleValeCreditoFactura.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleValeCreditoFactura;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleValeCreditoFactura) then begin
    lSource := LibraryValesCupones_Intf.TDetalleValeCreditoFactura(iSource);

    Self.NoVale := lSource.NoVale;
    Self.Importe := lSource.Importe;
    Self.FacturaID := lSource.FacturaID;
    Self.CarteraValeCreditoID := lSource.CarteraValeCreditoID;
  end;
end;

procedure TDetalleValeCreditoFactura.ReadComplex(ASerializer: TObject);
var
  l_CarteraValeCreditoID: Integer;
  l_FacturaID: Integer;
  l_Importe: Double;
  l_NoVale: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_NoVale := Self.NoVale;
    try
      TROSerializer(ASerializer).ReadInteger('NoVale', TypInfo.otSLong, l_NoVale);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoVale']));
    end;
    Self.NoVale := l_NoVale;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    try
      TROSerializer(ASerializer).ReadInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraValeCreditoID']));
    end;
    Self.CarteraValeCreditoID := l_CarteraValeCreditoID;
  end
  else begin
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    try
      TROSerializer(ASerializer).ReadInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraValeCreditoID']));
    end;
    Self.CarteraValeCreditoID := l_CarteraValeCreditoID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_NoVale := Self.NoVale;
    try
      TROSerializer(ASerializer).ReadInteger('NoVale', TypInfo.otSLong, l_NoVale);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoVale']));
    end;
    Self.NoVale := l_NoVale;
  end;
end;

procedure TDetalleValeCreditoFactura.WriteComplex(ASerializer: TObject);
var
  l_CarteraValeCreditoID: Integer;
  l_FacturaID: Integer;
  l_Importe: Double;
  l_NoVale: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleValeCreditoFactura);
    l_NoVale := Self.NoVale;
    TROSerializer(ASerializer).WriteInteger('NoVale', TypInfo.otSLong, l_NoVale);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    TROSerializer(ASerializer).WriteInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
  end
  else begin
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    TROSerializer(ASerializer).WriteInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_NoVale := Self.NoVale;
    TROSerializer(ASerializer).WriteInteger('NoVale', TypInfo.otSLong, l_NoVale);
  end;
end;

{ TDetalleValeCreditoFacturaCollection }
constructor TDetalleValeCreditoFacturaCollection.Create;
begin
  inherited Create(TDetalleValeCreditoFactura);
end;

constructor TDetalleValeCreditoFacturaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleValeCreditoFacturaCollection.Add: TDetalleValeCreditoFactura;
begin
  Result := TDetalleValeCreditoFactura(inherited Add);
end;

function TDetalleValeCreditoFacturaCollection.GetItems(aIndex: Integer): TDetalleValeCreditoFactura;
begin
  Result := TDetalleValeCreditoFactura(inherited Items[aIndex]);
end;

procedure TDetalleValeCreditoFacturaCollection.LoadFromArray(anArray: ATDetalleValeCreditoFactura);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleValeCreditoFacturaCollection.SaveToArray(anArray: ATDetalleValeCreditoFactura);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleValeCreditoFactura(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleValeCreditoFacturaCollection.SetItems(aIndex: Integer; const Value: TDetalleValeCreditoFactura);
begin
  TDetalleValeCreditoFactura(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosFactura }

procedure TDatosFactura.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosFactura;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosFactura) then begin
    lSource := LibraryValesCupones_Intf.TDatosFactura(iSource);

    if System.Assigned(lSource.fFactura) then
      Self.Factura.Assign(lSource.fFactura)
    else
      SysUtils.FreeAndNil(fFactura);
    if System.Assigned(lSource.fDetalles) then
      Self.Detalles.Assign(lSource.fDetalles)
    else
      SysUtils.FreeAndNil(fDetalles);
    if System.Assigned(lSource.fDetalleFactura) then
      Self.DetalleFactura.Assign(lSource.fDetalleFactura)
    else
      SysUtils.FreeAndNil(fDetalleFactura);
    if System.Assigned(lSource.fTickets) then
      Self.Tickets.Assign(lSource.fTickets)
    else
      SysUtils.FreeAndNil(fTickets);
    if System.Assigned(lSource.fDetalleCupon) then
      Self.DetalleCupon.Assign(lSource.fDetalleCupon)
    else
      SysUtils.FreeAndNil(fDetalleCupon);
    if System.Assigned(lSource.fVales) then
      Self.Vales.Assign(lSource.fVales)
    else
      SysUtils.FreeAndNil(fVales);
    Self.ValesS := lSource.ValesS;
    if System.Assigned(lSource.fCliente) then
      Self.Cliente.Assign(lSource.fCliente)
    else
      SysUtils.FreeAndNil(fCliente);
  end;
end;

function TDatosFactura.GetFactura: TFactura;
begin
  if (fFactura = nil) then fFactura := TFactura.Create();
  Result := fFactura;
end;

function TDatosFactura.GetDetalles: ATDetalleFactura;
begin
  if (fDetalles = nil) then fDetalles := ATDetalleFactura.Create();
  Result := fDetalles;
end;

function TDatosFactura.GetDetalleFactura: ATDetalleFactura;
begin
  if (fDetalleFactura = nil) then fDetalleFactura := ATDetalleFactura.Create();
  Result := fDetalleFactura;
end;

function TDatosFactura.GetTickets: ATTicketFactura;
begin
  if (fTickets = nil) then fTickets := ATTicketFactura.Create();
  Result := fTickets;
end;

function TDatosFactura.GetDetalleCupon: ATDetalleCupon;
begin
  if (fDetalleCupon = nil) then fDetalleCupon := ATDetalleCupon.Create();
  Result := fDetalleCupon;
end;

function TDatosFactura.GetVales: ATDetalleValeCreditoFactura;
begin
  if (fVales = nil) then fVales := ATDetalleValeCreditoFactura.Create();
  Result := fVales;
end;

function TDatosFactura.GetCliente: TDatosCliente;
begin
  if (fCliente = nil) then fCliente := TDatosCliente.Create();
  Result := fCliente;
end;

procedure TDatosFactura.ReadComplex(ASerializer: TObject);
var
  l_Cliente: TDatosCliente;
  l_DetalleCupon: ATDetalleCupon;
  l_DetalleFactura: ATDetalleFactura;
  l_Detalles: ATDetalleFactura;
  l_Factura: TFactura;
  l_Tickets: ATTicketFactura;
  l_Vales: ATDetalleValeCreditoFactura;
  l_ValesS: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Factura := int_Factura;
    try
      TROSerializer(ASerializer).ReadStruct('Factura', TFactura, l_Factura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Factura']));
    end;
    if int_Factura <> l_Factura then Self.Factura.Free;
    Self.Factura := l_Factura;
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleFactura, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Self.Detalles.Free;
    Self.Detalles := l_Detalles;
    l_DetalleFactura := int_DetalleFactura;
    try
      TROSerializer(ASerializer).ReadArray('DetalleFactura', ATDetalleFactura, l_DetalleFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFactura']));
    end;
    if int_DetalleFactura <> l_DetalleFactura then Self.DetalleFactura.Free;
    Self.DetalleFactura := l_DetalleFactura;
    l_Tickets := int_Tickets;
    try
      TROSerializer(ASerializer).ReadArray('Tickets', ATTicketFactura, l_Tickets);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tickets']));
    end;
    if int_Tickets <> l_Tickets then Self.Tickets.Free;
    Self.Tickets := l_Tickets;
    l_DetalleCupon := int_DetalleCupon;
    try
      TROSerializer(ASerializer).ReadArray('DetalleCupon', ATDetalleCupon, l_DetalleCupon);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleCupon']));
    end;
    if int_DetalleCupon <> l_DetalleCupon then Self.DetalleCupon.Free;
    Self.DetalleCupon := l_DetalleCupon;
    l_Vales := int_Vales;
    try
      TROSerializer(ASerializer).ReadArray('Vales', ATDetalleValeCreditoFactura, l_Vales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Vales']));
    end;
    if int_Vales <> l_Vales then Self.Vales.Free;
    Self.Vales := l_Vales;
    l_ValesS := Self.ValesS;
    try
      TROSerializer(ASerializer).ReadAnsiString('ValesS', l_ValesS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ValesS']));
    end;
    Self.ValesS := l_ValesS;
    l_Cliente := int_Cliente;
    try
      TROSerializer(ASerializer).ReadStruct('Cliente', TDatosCliente, l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    if int_Cliente <> l_Cliente then Self.Cliente.Free;
    Self.Cliente := l_Cliente;
  end
  else begin
    l_Cliente := int_Cliente;
    try
      TROSerializer(ASerializer).ReadStruct('Cliente', TDatosCliente, l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    if int_Cliente <> l_Cliente then Cliente.Free;
    Self.Cliente := l_Cliente;
    l_DetalleCupon := int_DetalleCupon;
    try
      TROSerializer(ASerializer).ReadArray('DetalleCupon', ATDetalleCupon, l_DetalleCupon);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleCupon']));
    end;
    if int_DetalleCupon <> l_DetalleCupon then DetalleCupon.Free;
    Self.DetalleCupon := l_DetalleCupon;
    l_DetalleFactura := int_DetalleFactura;
    try
      TROSerializer(ASerializer).ReadArray('DetalleFactura', ATDetalleFactura, l_DetalleFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFactura']));
    end;
    if int_DetalleFactura <> l_DetalleFactura then DetalleFactura.Free;
    Self.DetalleFactura := l_DetalleFactura;
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleFactura, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Detalles.Free;
    Self.Detalles := l_Detalles;
    l_Factura := int_Factura;
    try
      TROSerializer(ASerializer).ReadStruct('Factura', TFactura, l_Factura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Factura']));
    end;
    if int_Factura <> l_Factura then Factura.Free;
    Self.Factura := l_Factura;
    l_Tickets := int_Tickets;
    try
      TROSerializer(ASerializer).ReadArray('Tickets', ATTicketFactura, l_Tickets);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tickets']));
    end;
    if int_Tickets <> l_Tickets then Tickets.Free;
    Self.Tickets := l_Tickets;
    l_Vales := int_Vales;
    try
      TROSerializer(ASerializer).ReadArray('Vales', ATDetalleValeCreditoFactura, l_Vales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Vales']));
    end;
    if int_Vales <> l_Vales then Vales.Free;
    Self.Vales := l_Vales;
    l_ValesS := Self.ValesS;
    try
      TROSerializer(ASerializer).ReadAnsiString('ValesS', l_ValesS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ValesS']));
    end;
    Self.ValesS := l_ValesS;
  end;
end;

procedure TDatosFactura.WriteComplex(ASerializer: TObject);
var
  l_Cliente: TDatosCliente;
  l_DetalleCupon: ATDetalleCupon;
  l_DetalleFactura: ATDetalleFactura;
  l_Detalles: ATDetalleFactura;
  l_Factura: TFactura;
  l_Tickets: ATTicketFactura;
  l_Vales: ATDetalleValeCreditoFactura;
  l_ValesS: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosFactura);
    l_Factura := int_Factura;
    TROSerializer(ASerializer).WriteStruct('Factura', l_Factura, TFactura);
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleFactura);
    l_DetalleFactura := int_DetalleFactura;
    TROSerializer(ASerializer).WriteArray('DetalleFactura', l_DetalleFactura, ATDetalleFactura);
    l_Tickets := int_Tickets;
    TROSerializer(ASerializer).WriteArray('Tickets', l_Tickets, ATTicketFactura);
    l_DetalleCupon := int_DetalleCupon;
    TROSerializer(ASerializer).WriteArray('DetalleCupon', l_DetalleCupon, ATDetalleCupon);
    l_Vales := int_Vales;
    TROSerializer(ASerializer).WriteArray('Vales', l_Vales, ATDetalleValeCreditoFactura);
    l_ValesS := Self.ValesS;
    TROSerializer(ASerializer).WriteAnsiString('ValesS', l_ValesS);
    l_Cliente := int_Cliente;
    TROSerializer(ASerializer).WriteStruct('Cliente', l_Cliente, TDatosCliente);
  end
  else begin
    l_Cliente := int_Cliente;
    TROSerializer(ASerializer).WriteStruct('Cliente', l_Cliente, TDatosCliente);
    l_DetalleCupon := int_DetalleCupon;
    TROSerializer(ASerializer).WriteArray('DetalleCupon', l_DetalleCupon, ATDetalleCupon);
    l_DetalleFactura := int_DetalleFactura;
    TROSerializer(ASerializer).WriteArray('DetalleFactura', l_DetalleFactura, ATDetalleFactura);
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleFactura);
    l_Factura := int_Factura;
    TROSerializer(ASerializer).WriteStruct('Factura', l_Factura, TFactura);
    l_Tickets := int_Tickets;
    TROSerializer(ASerializer).WriteArray('Tickets', l_Tickets, ATTicketFactura);
    l_Vales := int_Vales;
    TROSerializer(ASerializer).WriteArray('Vales', l_Vales, ATDetalleValeCreditoFactura);
    l_ValesS := Self.ValesS;
    TROSerializer(ASerializer).WriteAnsiString('ValesS', l_ValesS);
  end;
end;

{ TDatosFacturaCollection }
constructor TDatosFacturaCollection.Create;
begin
  inherited Create(TDatosFactura);
end;

constructor TDatosFacturaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosFacturaCollection.Add: TDatosFactura;
begin
  Result := TDatosFactura(inherited Add);
end;

function TDatosFacturaCollection.GetItems(aIndex: Integer): TDatosFactura;
begin
  Result := TDatosFactura(inherited Items[aIndex]);
end;

procedure TDatosFacturaCollection.SetItems(aIndex: Integer; const Value: TDatosFactura);
begin
  TDatosFactura(inherited Items[aIndex]).Assign(Value);
end;

{ TLiquidacion }

procedure TLiquidacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLiquidacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLiquidacion) then begin
    lSource := LibraryValesCupones_Intf.TLiquidacion(iSource);

    Self.LiquidacionID := lSource.LiquidacionID;
    Self.Fecha := lSource.Fecha;
    Self.VentasTotales := lSource.VentasTotales;
    Self.Anticipos := lSource.Anticipos;
    Self.Diferencias := lSource.Diferencias;
    Self.Secuencia := lSource.Secuencia;
    Self.EstacionID := lSource.EstacionID;
    Self.DespachadorID := lSource.DespachadorID;
    Self.AgrupacionID := lSource.AgrupacionID;
  end;
end;

procedure TLiquidacion.ReadComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_Anticipos: Double;
  l_DespachadorID: Integer;
  l_Diferencias: Double;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_LiquidacionID: Integer;
  l_Secuencia: Integer;
  l_VentasTotales: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_VentasTotales := Self.VentasTotales;
    try
      TROSerializer(ASerializer).ReadDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTotales']));
    end;
    Self.VentasTotales := l_VentasTotales;
    l_Anticipos := Self.Anticipos;
    try
      TROSerializer(ASerializer).ReadDouble('Anticipos', TypInfo.ftDouble, l_Anticipos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Anticipos']));
    end;
    Self.Anticipos := l_Anticipos;
    l_Diferencias := Self.Diferencias;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencias', TypInfo.ftDouble, l_Diferencias);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencias']));
    end;
    Self.Diferencias := l_Diferencias;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_DespachadorID := Self.DespachadorID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorID']));
    end;
    Self.DespachadorID := l_DespachadorID;
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
    l_Anticipos := Self.Anticipos;
    try
      TROSerializer(ASerializer).ReadDouble('Anticipos', TypInfo.ftDouble, l_Anticipos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Anticipos']));
    end;
    Self.Anticipos := l_Anticipos;
    l_DespachadorID := Self.DespachadorID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorID']));
    end;
    Self.DespachadorID := l_DespachadorID;
    l_Diferencias := Self.Diferencias;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencias', TypInfo.ftDouble, l_Diferencias);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencias']));
    end;
    Self.Diferencias := l_Diferencias;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_VentasTotales := Self.VentasTotales;
    try
      TROSerializer(ASerializer).ReadDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTotales']));
    end;
    Self.VentasTotales := l_VentasTotales;
  end;
end;

procedure TLiquidacion.WriteComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_Anticipos: Double;
  l_DespachadorID: Integer;
  l_Diferencias: Double;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_LiquidacionID: Integer;
  l_Secuencia: Integer;
  l_VentasTotales: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLiquidacion);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_VentasTotales := Self.VentasTotales;
    TROSerializer(ASerializer).WriteDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    l_Anticipos := Self.Anticipos;
    TROSerializer(ASerializer).WriteDouble('Anticipos', TypInfo.ftDouble, l_Anticipos);
    l_Diferencias := Self.Diferencias;
    TROSerializer(ASerializer).WriteDouble('Diferencias', TypInfo.ftDouble, l_Diferencias);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_DespachadorID := Self.DespachadorID;
    TROSerializer(ASerializer).WriteInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    l_Anticipos := Self.Anticipos;
    TROSerializer(ASerializer).WriteDouble('Anticipos', TypInfo.ftDouble, l_Anticipos);
    l_DespachadorID := Self.DespachadorID;
    TROSerializer(ASerializer).WriteInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    l_Diferencias := Self.Diferencias;
    TROSerializer(ASerializer).WriteDouble('Diferencias', TypInfo.ftDouble, l_Diferencias);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_VentasTotales := Self.VentasTotales;
    TROSerializer(ASerializer).WriteDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
  end;
end;

{ TLiquidacionCollection }
constructor TLiquidacionCollection.Create;
begin
  inherited Create(TLiquidacion);
end;

constructor TLiquidacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLiquidacionCollection.Add: TLiquidacion;
begin
  Result := TLiquidacion(inherited Add);
end;

function TLiquidacionCollection.GetItems(aIndex: Integer): TLiquidacion;
begin
  Result := TLiquidacion(inherited Items[aIndex]);
end;

procedure TLiquidacionCollection.SetItems(aIndex: Integer; const Value: TLiquidacion);
begin
  TLiquidacion(inherited Items[aIndex]).Assign(Value);
end;

{ TLiquidacionDetalle }

procedure TLiquidacionDetalle.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLiquidacionDetalle;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLiquidacionDetalle) then begin
    lSource := LibraryValesCupones_Intf.TLiquidacionDetalle(iSource);

    Self.LiquidacionDetalleID := lSource.LiquidacionDetalleID;
    Self.Importe := lSource.Importe;
    Self.Secuencia := lSource.Secuencia;
    Self.Referencia := lSource.Referencia;
    Self.Ticket := lSource.Ticket;
    Self.Cupon := lSource.Cupon;
    Self.LiquidacionID := lSource.LiquidacionID;
    Self.TipoValorID := lSource.TipoValorID;
    Self.BancoID := lSource.BancoID;
    Self.SalidaID := lSource.SalidaID;
    Self.ClienteID := lSource.ClienteID;
  end;
end;

procedure TLiquidacionDetalle.ReadComplex(ASerializer: TObject);
var
  l_BancoID: Integer;
  l_ClienteID: Integer;
  l_Cupon: AnsiString;
  l_Importe: Double;
  l_LiquidacionDetalleID: Integer;
  l_LiquidacionID: Integer;
  l_Referencia: AnsiString;
  l_SalidaID: Integer;
  l_Secuencia: Integer;
  l_Ticket: Integer;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalleID']));
    end;
    Self.LiquidacionDetalleID := l_LiquidacionDetalleID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_Cupon := Self.Cupon;
    try
      TROSerializer(ASerializer).ReadAnsiString('Cupon', l_Cupon);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cupon']));
    end;
    Self.Cupon := l_Cupon;
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
    l_BancoID := Self.BancoID;
    try
      TROSerializer(ASerializer).ReadInteger('BancoID', TypInfo.otSLong, l_BancoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BancoID']));
    end;
    Self.BancoID := l_BancoID;
    l_SalidaID := Self.SalidaID;
    try
      TROSerializer(ASerializer).ReadInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SalidaID']));
    end;
    Self.SalidaID := l_SalidaID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
  end
  else begin
    l_BancoID := Self.BancoID;
    try
      TROSerializer(ASerializer).ReadInteger('BancoID', TypInfo.otSLong, l_BancoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BancoID']));
    end;
    Self.BancoID := l_BancoID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Cupon := Self.Cupon;
    try
      TROSerializer(ASerializer).ReadAnsiString('Cupon', l_Cupon);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cupon']));
    end;
    Self.Cupon := l_Cupon;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalleID']));
    end;
    Self.LiquidacionDetalleID := l_LiquidacionDetalleID;
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_SalidaID := Self.SalidaID;
    try
      TROSerializer(ASerializer).ReadInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SalidaID']));
    end;
    Self.SalidaID := l_SalidaID;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
  end;
end;

procedure TLiquidacionDetalle.WriteComplex(ASerializer: TObject);
var
  l_BancoID: Integer;
  l_ClienteID: Integer;
  l_Cupon: AnsiString;
  l_Importe: Double;
  l_LiquidacionDetalleID: Integer;
  l_LiquidacionID: Integer;
  l_Referencia: AnsiString;
  l_SalidaID: Integer;
  l_Secuencia: Integer;
  l_Ticket: Integer;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLiquidacionDetalle);
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
    l_Cupon := Self.Cupon;
    TROSerializer(ASerializer).WriteAnsiString('Cupon', l_Cupon);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    l_BancoID := Self.BancoID;
    TROSerializer(ASerializer).WriteInteger('BancoID', TypInfo.otSLong, l_BancoID);
    l_SalidaID := Self.SalidaID;
    TROSerializer(ASerializer).WriteInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
  end
  else begin
    l_BancoID := Self.BancoID;
    TROSerializer(ASerializer).WriteInteger('BancoID', TypInfo.otSLong, l_BancoID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Cupon := Self.Cupon;
    TROSerializer(ASerializer).WriteAnsiString('Cupon', l_Cupon);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_SalidaID := Self.SalidaID;
    TROSerializer(ASerializer).WriteInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
  end;
end;

{ TLiquidacionDetalleCollection }
constructor TLiquidacionDetalleCollection.Create;
begin
  inherited Create(TLiquidacionDetalle);
end;

constructor TLiquidacionDetalleCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLiquidacionDetalleCollection.Add: TLiquidacionDetalle;
begin
  Result := TLiquidacionDetalle(inherited Add);
end;

function TLiquidacionDetalleCollection.GetItems(aIndex: Integer): TLiquidacionDetalle;
begin
  Result := TLiquidacionDetalle(inherited Items[aIndex]);
end;

procedure TLiquidacionDetalleCollection.LoadFromArray(anArray: ATLiquidacionDetalle);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TLiquidacionDetalleCollection.SaveToArray(anArray: ATLiquidacionDetalle);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TLiquidacionDetalle(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TLiquidacionDetalleCollection.SetItems(aIndex: Integer; const Value: TLiquidacionDetalle);
begin
  TLiquidacionDetalle(inherited Items[aIndex]).Assign(Value);
end;

{ TLiquidacionProducto }

procedure TLiquidacionProducto.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLiquidacionProducto;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLiquidacionProducto) then begin
    lSource := LibraryValesCupones_Intf.TLiquidacionProducto(iSource);

    Self.PrecioVenta := lSource.PrecioVenta;
    Self.InventarioInicial := lSource.InventarioInicial;
    Self.InventarioFinal := lSource.InventarioFinal;
    Self.Cantidad := lSource.Cantidad;
    Self.Importe := lSource.Importe;
    Self.Secuencia := lSource.Secuencia;
    Self.Traspasos := lSource.Traspasos;
    Self.ProductoID := lSource.ProductoID;
    Self.BombaID := lSource.BombaID;
    Self.Costo := lSource.Costo;
  end;
end;

procedure TLiquidacionProducto.ReadComplex(ASerializer: TObject);
var
  l_BombaID: Integer;
  l_Cantidad: Double;
  l_Costo: Double;
  l_Importe: Double;
  l_InventarioFinal: Double;
  l_InventarioInicial: Double;
  l_PrecioVenta: Double;
  l_ProductoID: Integer;
  l_Secuencia: Integer;
  l_Traspasos: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_PrecioVenta := Self.PrecioVenta;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioVenta', TypInfo.ftDouble, l_PrecioVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioVenta']));
    end;
    Self.PrecioVenta := l_PrecioVenta;
    l_InventarioInicial := Self.InventarioInicial;
    try
      TROSerializer(ASerializer).ReadDouble('InventarioInicial', TypInfo.ftDouble, l_InventarioInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'InventarioInicial']));
    end;
    Self.InventarioInicial := l_InventarioInicial;
    l_InventarioFinal := Self.InventarioFinal;
    try
      TROSerializer(ASerializer).ReadDouble('InventarioFinal', TypInfo.ftDouble, l_InventarioFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'InventarioFinal']));
    end;
    Self.InventarioFinal := l_InventarioFinal;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Traspasos := Self.Traspasos;
    try
      TROSerializer(ASerializer).ReadDouble('Traspasos', TypInfo.ftDouble, l_Traspasos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Traspasos']));
    end;
    Self.Traspasos := l_Traspasos;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_BombaID := Self.BombaID;
    try
      TROSerializer(ASerializer).ReadInteger('BombaID', TypInfo.otSLong, l_BombaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BombaID']));
    end;
    Self.BombaID := l_BombaID;
    l_Costo := Self.Costo;
    try
      TROSerializer(ASerializer).ReadDouble('Costo', TypInfo.ftDouble, l_Costo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Costo']));
    end;
    Self.Costo := l_Costo;
  end
  else begin
    l_BombaID := Self.BombaID;
    try
      TROSerializer(ASerializer).ReadInteger('BombaID', TypInfo.otSLong, l_BombaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BombaID']));
    end;
    Self.BombaID := l_BombaID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Costo := Self.Costo;
    try
      TROSerializer(ASerializer).ReadDouble('Costo', TypInfo.ftDouble, l_Costo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Costo']));
    end;
    Self.Costo := l_Costo;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_InventarioFinal := Self.InventarioFinal;
    try
      TROSerializer(ASerializer).ReadDouble('InventarioFinal', TypInfo.ftDouble, l_InventarioFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'InventarioFinal']));
    end;
    Self.InventarioFinal := l_InventarioFinal;
    l_InventarioInicial := Self.InventarioInicial;
    try
      TROSerializer(ASerializer).ReadDouble('InventarioInicial', TypInfo.ftDouble, l_InventarioInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'InventarioInicial']));
    end;
    Self.InventarioInicial := l_InventarioInicial;
    l_PrecioVenta := Self.PrecioVenta;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioVenta', TypInfo.ftDouble, l_PrecioVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioVenta']));
    end;
    Self.PrecioVenta := l_PrecioVenta;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Traspasos := Self.Traspasos;
    try
      TROSerializer(ASerializer).ReadDouble('Traspasos', TypInfo.ftDouble, l_Traspasos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Traspasos']));
    end;
    Self.Traspasos := l_Traspasos;
  end;
end;

procedure TLiquidacionProducto.WriteComplex(ASerializer: TObject);
var
  l_BombaID: Integer;
  l_Cantidad: Double;
  l_Costo: Double;
  l_Importe: Double;
  l_InventarioFinal: Double;
  l_InventarioInicial: Double;
  l_PrecioVenta: Double;
  l_ProductoID: Integer;
  l_Secuencia: Integer;
  l_Traspasos: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLiquidacionProducto);
    l_PrecioVenta := Self.PrecioVenta;
    TROSerializer(ASerializer).WriteDouble('PrecioVenta', TypInfo.ftDouble, l_PrecioVenta);
    l_InventarioInicial := Self.InventarioInicial;
    TROSerializer(ASerializer).WriteDouble('InventarioInicial', TypInfo.ftDouble, l_InventarioInicial);
    l_InventarioFinal := Self.InventarioFinal;
    TROSerializer(ASerializer).WriteDouble('InventarioFinal', TypInfo.ftDouble, l_InventarioFinal);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Traspasos := Self.Traspasos;
    TROSerializer(ASerializer).WriteDouble('Traspasos', TypInfo.ftDouble, l_Traspasos);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_BombaID := Self.BombaID;
    TROSerializer(ASerializer).WriteInteger('BombaID', TypInfo.otSLong, l_BombaID);
    l_Costo := Self.Costo;
    TROSerializer(ASerializer).WriteDouble('Costo', TypInfo.ftDouble, l_Costo);
  end
  else begin
    l_BombaID := Self.BombaID;
    TROSerializer(ASerializer).WriteInteger('BombaID', TypInfo.otSLong, l_BombaID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Costo := Self.Costo;
    TROSerializer(ASerializer).WriteDouble('Costo', TypInfo.ftDouble, l_Costo);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_InventarioFinal := Self.InventarioFinal;
    TROSerializer(ASerializer).WriteDouble('InventarioFinal', TypInfo.ftDouble, l_InventarioFinal);
    l_InventarioInicial := Self.InventarioInicial;
    TROSerializer(ASerializer).WriteDouble('InventarioInicial', TypInfo.ftDouble, l_InventarioInicial);
    l_PrecioVenta := Self.PrecioVenta;
    TROSerializer(ASerializer).WriteDouble('PrecioVenta', TypInfo.ftDouble, l_PrecioVenta);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Traspasos := Self.Traspasos;
    TROSerializer(ASerializer).WriteDouble('Traspasos', TypInfo.ftDouble, l_Traspasos);
  end;
end;

{ TLiquidacionProductoCollection }
constructor TLiquidacionProductoCollection.Create;
begin
  inherited Create(TLiquidacionProducto);
end;

constructor TLiquidacionProductoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLiquidacionProductoCollection.Add: TLiquidacionProducto;
begin
  Result := TLiquidacionProducto(inherited Add);
end;

function TLiquidacionProductoCollection.GetItems(aIndex: Integer): TLiquidacionProducto;
begin
  Result := TLiquidacionProducto(inherited Items[aIndex]);
end;

procedure TLiquidacionProductoCollection.LoadFromArray(anArray: ATLiquidacionProducto);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TLiquidacionProductoCollection.SaveToArray(anArray: ATLiquidacionProducto);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TLiquidacionProducto(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TLiquidacionProductoCollection.SetItems(aIndex: Integer; const Value: TLiquidacionProducto);
begin
  TLiquidacionProducto(inherited Items[aIndex]).Assign(Value);
end;

{ TTransaccion }

procedure TTransaccion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTransaccion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTransaccion) then begin
    lSource := LibraryValesCupones_Intf.TTransaccion(iSource);

    Self.TransaccionID := lSource.TransaccionID;
    Self.Folio := lSource.Folio;
    Self.Tipo := lSource.Tipo;
    Self.Credito := lSource.Credito;
    Self.Referencia := lSource.Referencia;
    Self.Fecha := lSource.Fecha;
    Self.Total := lSource.Total;
    Self.Subtotal := lSource.Subtotal;
    Self.Impuesto := lSource.Impuesto;
    Self.Plazo := lSource.Plazo;
    Self.Secuencia := lSource.Secuencia;
    Self.EstacionDestinoID := lSource.EstacionDestinoID;
    Self.AlmacenDestinoID := lSource.AlmacenDestinoID;
    Self.EstacionID := lSource.EstacionID;
    Self.ProveedorId := lSource.ProveedorId;
    Self.AlmacenID := lSource.AlmacenID;
    Self.MovimientoAlmacen := lSource.MovimientoAlmacen;
  end;
end;

procedure TTransaccion.ReadComplex(ASerializer: TObject);
var
  l_AlmacenDestinoID: Integer;
  l_AlmacenID: Integer;
  l_Credito: Boolean;
  l_EstacionDestinoID: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Impuesto: Double;
  l_MovimientoAlmacen: Integer;
  l_Plazo: Integer;
  l_ProveedorId: Integer;
  l_Referencia: AnsiString;
  l_Secuencia: Integer;
  l_Subtotal: Double;
  l_Tipo: AnsiString;
  l_Total: Double;
  l_TransaccionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_TransaccionID := Self.TransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TransaccionID']));
    end;
    Self.TransaccionID := l_TransaccionID;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tipo', l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_Credito := Self.Credito;
    try
      TROSerializer(ASerializer).ReadEnumerated('Credito',System.TypeInfo(boolean), l_Credito);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Credito']));
    end;
    Self.Credito := l_Credito;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Plazo := Self.Plazo;
    try
      TROSerializer(ASerializer).ReadInteger('Plazo', TypInfo.otSLong, l_Plazo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Plazo']));
    end;
    Self.Plazo := l_Plazo;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_EstacionDestinoID := Self.EstacionDestinoID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionDestinoID', TypInfo.otSLong, l_EstacionDestinoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionDestinoID']));
    end;
    Self.EstacionDestinoID := l_EstacionDestinoID;
    l_AlmacenDestinoID := Self.AlmacenDestinoID;
    try
      TROSerializer(ASerializer).ReadInteger('AlmacenDestinoID', TypInfo.otSLong, l_AlmacenDestinoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AlmacenDestinoID']));
    end;
    Self.AlmacenDestinoID := l_AlmacenDestinoID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_ProveedorId := Self.ProveedorId;
    try
      TROSerializer(ASerializer).ReadInteger('ProveedorId', TypInfo.otSLong, l_ProveedorId);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProveedorId']));
    end;
    Self.ProveedorId := l_ProveedorId;
    l_AlmacenID := Self.AlmacenID;
    try
      TROSerializer(ASerializer).ReadInteger('AlmacenID', TypInfo.otSLong, l_AlmacenID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AlmacenID']));
    end;
    Self.AlmacenID := l_AlmacenID;
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoAlmacen']));
    end;
    Self.MovimientoAlmacen := l_MovimientoAlmacen;
  end
  else begin
    l_AlmacenDestinoID := Self.AlmacenDestinoID;
    try
      TROSerializer(ASerializer).ReadInteger('AlmacenDestinoID', TypInfo.otSLong, l_AlmacenDestinoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AlmacenDestinoID']));
    end;
    Self.AlmacenDestinoID := l_AlmacenDestinoID;
    l_AlmacenID := Self.AlmacenID;
    try
      TROSerializer(ASerializer).ReadInteger('AlmacenID', TypInfo.otSLong, l_AlmacenID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AlmacenID']));
    end;
    Self.AlmacenID := l_AlmacenID;
    l_Credito := Self.Credito;
    try
      TROSerializer(ASerializer).ReadEnumerated('Credito',System.TypeInfo(boolean), l_Credito);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Credito']));
    end;
    Self.Credito := l_Credito;
    l_EstacionDestinoID := Self.EstacionDestinoID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionDestinoID', TypInfo.otSLong, l_EstacionDestinoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionDestinoID']));
    end;
    Self.EstacionDestinoID := l_EstacionDestinoID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoAlmacen']));
    end;
    Self.MovimientoAlmacen := l_MovimientoAlmacen;
    l_Plazo := Self.Plazo;
    try
      TROSerializer(ASerializer).ReadInteger('Plazo', TypInfo.otSLong, l_Plazo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Plazo']));
    end;
    Self.Plazo := l_Plazo;
    l_ProveedorId := Self.ProveedorId;
    try
      TROSerializer(ASerializer).ReadInteger('ProveedorId', TypInfo.otSLong, l_ProveedorId);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProveedorId']));
    end;
    Self.ProveedorId := l_ProveedorId;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tipo', l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_TransaccionID := Self.TransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TransaccionID']));
    end;
    Self.TransaccionID := l_TransaccionID;
  end;
end;

procedure TTransaccion.WriteComplex(ASerializer: TObject);
var
  l_AlmacenDestinoID: Integer;
  l_AlmacenID: Integer;
  l_Credito: Boolean;
  l_EstacionDestinoID: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Impuesto: Double;
  l_MovimientoAlmacen: Integer;
  l_Plazo: Integer;
  l_ProveedorId: Integer;
  l_Referencia: AnsiString;
  l_Secuencia: Integer;
  l_Subtotal: Double;
  l_Tipo: AnsiString;
  l_Total: Double;
  l_TransaccionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTransaccion);
    l_TransaccionID := Self.TransaccionID;
    TROSerializer(ASerializer).WriteInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteAnsiString('Tipo', l_Tipo);
    l_Credito := Self.Credito;
    TROSerializer(ASerializer).WriteEnumerated('Credito',System.TypeInfo(boolean), l_Credito);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Plazo := Self.Plazo;
    TROSerializer(ASerializer).WriteInteger('Plazo', TypInfo.otSLong, l_Plazo);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_EstacionDestinoID := Self.EstacionDestinoID;
    TROSerializer(ASerializer).WriteInteger('EstacionDestinoID', TypInfo.otSLong, l_EstacionDestinoID);
    l_AlmacenDestinoID := Self.AlmacenDestinoID;
    TROSerializer(ASerializer).WriteInteger('AlmacenDestinoID', TypInfo.otSLong, l_AlmacenDestinoID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_ProveedorId := Self.ProveedorId;
    TROSerializer(ASerializer).WriteInteger('ProveedorId', TypInfo.otSLong, l_ProveedorId);
    l_AlmacenID := Self.AlmacenID;
    TROSerializer(ASerializer).WriteInteger('AlmacenID', TypInfo.otSLong, l_AlmacenID);
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    TROSerializer(ASerializer).WriteInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
  end
  else begin
    l_AlmacenDestinoID := Self.AlmacenDestinoID;
    TROSerializer(ASerializer).WriteInteger('AlmacenDestinoID', TypInfo.otSLong, l_AlmacenDestinoID);
    l_AlmacenID := Self.AlmacenID;
    TROSerializer(ASerializer).WriteInteger('AlmacenID', TypInfo.otSLong, l_AlmacenID);
    l_Credito := Self.Credito;
    TROSerializer(ASerializer).WriteEnumerated('Credito',System.TypeInfo(boolean), l_Credito);
    l_EstacionDestinoID := Self.EstacionDestinoID;
    TROSerializer(ASerializer).WriteInteger('EstacionDestinoID', TypInfo.otSLong, l_EstacionDestinoID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    TROSerializer(ASerializer).WriteInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    l_Plazo := Self.Plazo;
    TROSerializer(ASerializer).WriteInteger('Plazo', TypInfo.otSLong, l_Plazo);
    l_ProveedorId := Self.ProveedorId;
    TROSerializer(ASerializer).WriteInteger('ProveedorId', TypInfo.otSLong, l_ProveedorId);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteAnsiString('Tipo', l_Tipo);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_TransaccionID := Self.TransaccionID;
    TROSerializer(ASerializer).WriteInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
  end;
end;

{ TTransaccionCollection }
constructor TTransaccionCollection.Create;
begin
  inherited Create(TTransaccion);
end;

constructor TTransaccionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTransaccionCollection.Add: TTransaccion;
begin
  Result := TTransaccion(inherited Add);
end;

function TTransaccionCollection.GetItems(aIndex: Integer): TTransaccion;
begin
  Result := TTransaccion(inherited Items[aIndex]);
end;

procedure TTransaccionCollection.SetItems(aIndex: Integer; const Value: TTransaccion);
begin
  TTransaccion(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleTransaccion }

procedure TDetalleTransaccion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleTransaccion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleTransaccion) then begin
    lSource := LibraryValesCupones_Intf.TDetalleTransaccion(iSource);

    Self.DetalleTransaccionID := lSource.DetalleTransaccionID;
    Self.Cantidad := lSource.Cantidad;
    Self.Costo := lSource.Costo;
    Self.TransaccionID := lSource.TransaccionID;
    Self.ProductoID := lSource.ProductoID;
    Self.Tipo := lSource.Tipo;
    Self.Operador := lSource.Operador;
    Self.MovimientoAlmacen := lSource.MovimientoAlmacen;
  end;
end;

procedure TDetalleTransaccion.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Costo: Double;
  l_DetalleTransaccionID: Integer;
  l_MovimientoAlmacen: Integer;
  l_Operador: Integer;
  l_ProductoID: Integer;
  l_Tipo: AnsiString;
  l_TransaccionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_DetalleTransaccionID := Self.DetalleTransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('DetalleTransaccionID', TypInfo.otSLong, l_DetalleTransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleTransaccionID']));
    end;
    Self.DetalleTransaccionID := l_DetalleTransaccionID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Costo := Self.Costo;
    try
      TROSerializer(ASerializer).ReadDouble('Costo', TypInfo.ftDouble, l_Costo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Costo']));
    end;
    Self.Costo := l_Costo;
    l_TransaccionID := Self.TransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TransaccionID']));
    end;
    Self.TransaccionID := l_TransaccionID;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tipo', l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_Operador := Self.Operador;
    try
      TROSerializer(ASerializer).ReadInteger('Operador', TypInfo.otSLong, l_Operador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Operador']));
    end;
    Self.Operador := l_Operador;
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoAlmacen']));
    end;
    Self.MovimientoAlmacen := l_MovimientoAlmacen;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Costo := Self.Costo;
    try
      TROSerializer(ASerializer).ReadDouble('Costo', TypInfo.ftDouble, l_Costo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Costo']));
    end;
    Self.Costo := l_Costo;
    l_DetalleTransaccionID := Self.DetalleTransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('DetalleTransaccionID', TypInfo.otSLong, l_DetalleTransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleTransaccionID']));
    end;
    Self.DetalleTransaccionID := l_DetalleTransaccionID;
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoAlmacen']));
    end;
    Self.MovimientoAlmacen := l_MovimientoAlmacen;
    l_Operador := Self.Operador;
    try
      TROSerializer(ASerializer).ReadInteger('Operador', TypInfo.otSLong, l_Operador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Operador']));
    end;
    Self.Operador := l_Operador;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tipo', l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_TransaccionID := Self.TransaccionID;
    try
      TROSerializer(ASerializer).ReadInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TransaccionID']));
    end;
    Self.TransaccionID := l_TransaccionID;
  end;
end;

procedure TDetalleTransaccion.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Costo: Double;
  l_DetalleTransaccionID: Integer;
  l_MovimientoAlmacen: Integer;
  l_Operador: Integer;
  l_ProductoID: Integer;
  l_Tipo: AnsiString;
  l_TransaccionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleTransaccion);
    l_DetalleTransaccionID := Self.DetalleTransaccionID;
    TROSerializer(ASerializer).WriteInteger('DetalleTransaccionID', TypInfo.otSLong, l_DetalleTransaccionID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Costo := Self.Costo;
    TROSerializer(ASerializer).WriteDouble('Costo', TypInfo.ftDouble, l_Costo);
    l_TransaccionID := Self.TransaccionID;
    TROSerializer(ASerializer).WriteInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteAnsiString('Tipo', l_Tipo);
    l_Operador := Self.Operador;
    TROSerializer(ASerializer).WriteInteger('Operador', TypInfo.otSLong, l_Operador);
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    TROSerializer(ASerializer).WriteInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Costo := Self.Costo;
    TROSerializer(ASerializer).WriteDouble('Costo', TypInfo.ftDouble, l_Costo);
    l_DetalleTransaccionID := Self.DetalleTransaccionID;
    TROSerializer(ASerializer).WriteInteger('DetalleTransaccionID', TypInfo.otSLong, l_DetalleTransaccionID);
    l_MovimientoAlmacen := Self.MovimientoAlmacen;
    TROSerializer(ASerializer).WriteInteger('MovimientoAlmacen', TypInfo.otSLong, l_MovimientoAlmacen);
    l_Operador := Self.Operador;
    TROSerializer(ASerializer).WriteInteger('Operador', TypInfo.otSLong, l_Operador);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteAnsiString('Tipo', l_Tipo);
    l_TransaccionID := Self.TransaccionID;
    TROSerializer(ASerializer).WriteInteger('TransaccionID', TypInfo.otSLong, l_TransaccionID);
  end;
end;

{ TDetalleTransaccionCollection }
constructor TDetalleTransaccionCollection.Create;
begin
  inherited Create(TDetalleTransaccion);
end;

constructor TDetalleTransaccionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleTransaccionCollection.Add: TDetalleTransaccion;
begin
  Result := TDetalleTransaccion(inherited Add);
end;

function TDetalleTransaccionCollection.GetItems(aIndex: Integer): TDetalleTransaccion;
begin
  Result := TDetalleTransaccion(inherited Items[aIndex]);
end;

procedure TDetalleTransaccionCollection.LoadFromArray(anArray: ATDetalleTransaccion);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleTransaccionCollection.SaveToArray(anArray: ATDetalleTransaccion);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleTransaccion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleTransaccionCollection.SetItems(aIndex: Integer; const Value: TDetalleTransaccion);
begin
  TDetalleTransaccion(inherited Items[aIndex]).Assign(Value);
end;

{ TCarteraVale }

procedure TCarteraVale.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCarteraVale;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCarteraVale) then begin
    lSource := LibraryValesCupones_Intf.TCarteraVale(iSource);

    Self.CarteraValeCreditoID := lSource.CarteraValeCreditoID;
    Self.NoVale := lSource.NoVale;
    Self.Ticket := lSource.Ticket;
    Self.Importe := lSource.Importe;
    Self.Fecha := lSource.Fecha;
    Self.Status := lSource.Status;
    Self.FacturaID := lSource.FacturaID;
    Self.EstacionID := lSource.EstacionID;
    Self.ClienteID := lSource.ClienteID;
    Self.LiquidacionDetalleID := lSource.LiquidacionDetalleID;
  end;
end;

procedure TCarteraVale.ReadComplex(ASerializer: TObject);
var
  l_CarteraValeCreditoID: Integer;
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_LiquidacionDetalleID: Integer;
  l_NoVale: Integer;
  l_Status: AnsiString;
  l_Ticket: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    try
      TROSerializer(ASerializer).ReadInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraValeCreditoID']));
    end;
    Self.CarteraValeCreditoID := l_CarteraValeCreditoID;
    l_NoVale := Self.NoVale;
    try
      TROSerializer(ASerializer).ReadInteger('NoVale', TypInfo.otSLong, l_NoVale);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoVale']));
    end;
    Self.NoVale := l_NoVale;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalleID']));
    end;
    Self.LiquidacionDetalleID := l_LiquidacionDetalleID;
  end
  else begin
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    try
      TROSerializer(ASerializer).ReadInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraValeCreditoID']));
    end;
    Self.CarteraValeCreditoID := l_CarteraValeCreditoID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalleID']));
    end;
    Self.LiquidacionDetalleID := l_LiquidacionDetalleID;
    l_NoVale := Self.NoVale;
    try
      TROSerializer(ASerializer).ReadInteger('NoVale', TypInfo.otSLong, l_NoVale);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoVale']));
    end;
    Self.NoVale := l_NoVale;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
  end;
end;

procedure TCarteraVale.WriteComplex(ASerializer: TObject);
var
  l_CarteraValeCreditoID: Integer;
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_LiquidacionDetalleID: Integer;
  l_NoVale: Integer;
  l_Status: AnsiString;
  l_Ticket: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCarteraVale);
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    TROSerializer(ASerializer).WriteInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    l_NoVale := Self.NoVale;
    TROSerializer(ASerializer).WriteInteger('NoVale', TypInfo.otSLong, l_NoVale);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
  end
  else begin
    l_CarteraValeCreditoID := Self.CarteraValeCreditoID;
    TROSerializer(ASerializer).WriteInteger('CarteraValeCreditoID', TypInfo.otSLong, l_CarteraValeCreditoID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_LiquidacionDetalleID := Self.LiquidacionDetalleID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionDetalleID', TypInfo.otSLong, l_LiquidacionDetalleID);
    l_NoVale := Self.NoVale;
    TROSerializer(ASerializer).WriteInteger('NoVale', TypInfo.otSLong, l_NoVale);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
  end;
end;

{ TCarteraValeCollection }
constructor TCarteraValeCollection.Create;
begin
  inherited Create(TCarteraVale);
end;

constructor TCarteraValeCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCarteraValeCollection.Add: TCarteraVale;
begin
  Result := TCarteraVale(inherited Add);
end;

function TCarteraValeCollection.GetItems(aIndex: Integer): TCarteraVale;
begin
  Result := TCarteraVale(inherited Items[aIndex]);
end;

procedure TCarteraValeCollection.LoadFromArray(anArray: ATCarteraVale);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TCarteraValeCollection.SaveToArray(anArray: ATCarteraVale);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TCarteraVale(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TCarteraValeCollection.SetItems(aIndex: Integer; const Value: TCarteraVale);
begin
  TCarteraVale(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosLiquidacion }

procedure TDatosLiquidacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosLiquidacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosLiquidacion) then begin
    lSource := LibraryValesCupones_Intf.TDatosLiquidacion(iSource);

    if System.Assigned(lSource.fLiquidacion) then
      Self.Liquidacion.Assign(lSource.fLiquidacion)
    else
      SysUtils.FreeAndNil(fLiquidacion);
    if System.Assigned(lSource.fLiquidacionDetalle) then
      Self.LiquidacionDetalle.Assign(lSource.fLiquidacionDetalle)
    else
      SysUtils.FreeAndNil(fLiquidacionDetalle);
    if System.Assigned(lSource.fLiquidacionProducto) then
      Self.LiquidacionProducto.Assign(lSource.fLiquidacionProducto)
    else
      SysUtils.FreeAndNil(fLiquidacionProducto);
    if System.Assigned(lSource.fLiquidacionCombustible) then
      Self.LiquidacionCombustible.Assign(lSource.fLiquidacionCombustible)
    else
      SysUtils.FreeAndNil(fLiquidacionCombustible);
    if System.Assigned(lSource.fTraspaso) then
      Self.Traspaso.Assign(lSource.fTraspaso)
    else
      SysUtils.FreeAndNil(fTraspaso);
    if System.Assigned(lSource.fVenta) then
      Self.Venta.Assign(lSource.fVenta)
    else
      SysUtils.FreeAndNil(fVenta);
    if System.Assigned(lSource.fDetalleTraspaso) then
      Self.DetalleTraspaso.Assign(lSource.fDetalleTraspaso)
    else
      SysUtils.FreeAndNil(fDetalleTraspaso);
    if System.Assigned(lSource.fDetalleVenta) then
      Self.DetalleVenta.Assign(lSource.fDetalleVenta)
    else
      SysUtils.FreeAndNil(fDetalleVenta);
    if System.Assigned(lSource.fCarteraVales) then
      Self.CarteraVales.Assign(lSource.fCarteraVales)
    else
      SysUtils.FreeAndNil(fCarteraVales);
  end;
end;

function TDatosLiquidacion.GetLiquidacion: TLiquidacion;
begin
  if (fLiquidacion = nil) then fLiquidacion := TLiquidacion.Create();
  Result := fLiquidacion;
end;

function TDatosLiquidacion.GetLiquidacionDetalle: ATLiquidacionDetalle;
begin
  if (fLiquidacionDetalle = nil) then fLiquidacionDetalle := ATLiquidacionDetalle.Create();
  Result := fLiquidacionDetalle;
end;

function TDatosLiquidacion.GetLiquidacionProducto: ATLiquidacionProducto;
begin
  if (fLiquidacionProducto = nil) then fLiquidacionProducto := ATLiquidacionProducto.Create();
  Result := fLiquidacionProducto;
end;

function TDatosLiquidacion.GetLiquidacionCombustible: ATLiquidacionProducto;
begin
  if (fLiquidacionCombustible = nil) then fLiquidacionCombustible := ATLiquidacionProducto.Create();
  Result := fLiquidacionCombustible;
end;

function TDatosLiquidacion.GetTraspaso: TTransaccion;
begin
  if (fTraspaso = nil) then fTraspaso := TTransaccion.Create();
  Result := fTraspaso;
end;

function TDatosLiquidacion.GetVenta: TTransaccion;
begin
  if (fVenta = nil) then fVenta := TTransaccion.Create();
  Result := fVenta;
end;

function TDatosLiquidacion.GetDetalleTraspaso: ATDetalleTransaccion;
begin
  if (fDetalleTraspaso = nil) then fDetalleTraspaso := ATDetalleTransaccion.Create();
  Result := fDetalleTraspaso;
end;

function TDatosLiquidacion.GetDetalleVenta: ATDetalleTransaccion;
begin
  if (fDetalleVenta = nil) then fDetalleVenta := ATDetalleTransaccion.Create();
  Result := fDetalleVenta;
end;

function TDatosLiquidacion.GetCarteraVales: ATCarteraVale;
begin
  if (fCarteraVales = nil) then fCarteraVales := ATCarteraVale.Create();
  Result := fCarteraVales;
end;

procedure TDatosLiquidacion.ReadComplex(ASerializer: TObject);
var
  l_CarteraVales: ATCarteraVale;
  l_DetalleTraspaso: ATDetalleTransaccion;
  l_DetalleVenta: ATDetalleTransaccion;
  l_Liquidacion: TLiquidacion;
  l_LiquidacionCombustible: ATLiquidacionProducto;
  l_LiquidacionDetalle: ATLiquidacionDetalle;
  l_LiquidacionProducto: ATLiquidacionProducto;
  l_Traspaso: TTransaccion;
  l_Venta: TTransaccion;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Liquidacion := int_Liquidacion;
    try
      TROSerializer(ASerializer).ReadStruct('Liquidacion', TLiquidacion, l_Liquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Liquidacion']));
    end;
    if int_Liquidacion <> l_Liquidacion then Self.Liquidacion.Free;
    Self.Liquidacion := l_Liquidacion;
    l_LiquidacionDetalle := int_LiquidacionDetalle;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionDetalle', ATLiquidacionDetalle, l_LiquidacionDetalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalle']));
    end;
    if int_LiquidacionDetalle <> l_LiquidacionDetalle then Self.LiquidacionDetalle.Free;
    Self.LiquidacionDetalle := l_LiquidacionDetalle;
    l_LiquidacionProducto := int_LiquidacionProducto;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionProducto', ATLiquidacionProducto, l_LiquidacionProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionProducto']));
    end;
    if int_LiquidacionProducto <> l_LiquidacionProducto then Self.LiquidacionProducto.Free;
    Self.LiquidacionProducto := l_LiquidacionProducto;
    l_LiquidacionCombustible := int_LiquidacionCombustible;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionCombustible', ATLiquidacionProducto, l_LiquidacionCombustible);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionCombustible']));
    end;
    if int_LiquidacionCombustible <> l_LiquidacionCombustible then Self.LiquidacionCombustible.Free;
    Self.LiquidacionCombustible := l_LiquidacionCombustible;
    l_Traspaso := int_Traspaso;
    try
      TROSerializer(ASerializer).ReadStruct('Traspaso', TTransaccion, l_Traspaso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Traspaso']));
    end;
    if int_Traspaso <> l_Traspaso then Self.Traspaso.Free;
    Self.Traspaso := l_Traspaso;
    l_Venta := int_Venta;
    try
      TROSerializer(ASerializer).ReadStruct('Venta', TTransaccion, l_Venta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Venta']));
    end;
    if int_Venta <> l_Venta then Self.Venta.Free;
    Self.Venta := l_Venta;
    l_DetalleTraspaso := int_DetalleTraspaso;
    try
      TROSerializer(ASerializer).ReadArray('DetalleTraspaso', ATDetalleTransaccion, l_DetalleTraspaso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleTraspaso']));
    end;
    if int_DetalleTraspaso <> l_DetalleTraspaso then Self.DetalleTraspaso.Free;
    Self.DetalleTraspaso := l_DetalleTraspaso;
    l_DetalleVenta := int_DetalleVenta;
    try
      TROSerializer(ASerializer).ReadArray('DetalleVenta', ATDetalleTransaccion, l_DetalleVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleVenta']));
    end;
    if int_DetalleVenta <> l_DetalleVenta then Self.DetalleVenta.Free;
    Self.DetalleVenta := l_DetalleVenta;
    l_CarteraVales := int_CarteraVales;
    try
      TROSerializer(ASerializer).ReadArray('CarteraVales', ATCarteraVale, l_CarteraVales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraVales']));
    end;
    if int_CarteraVales <> l_CarteraVales then Self.CarteraVales.Free;
    Self.CarteraVales := l_CarteraVales;
  end
  else begin
    l_CarteraVales := int_CarteraVales;
    try
      TROSerializer(ASerializer).ReadArray('CarteraVales', ATCarteraVale, l_CarteraVales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CarteraVales']));
    end;
    if int_CarteraVales <> l_CarteraVales then CarteraVales.Free;
    Self.CarteraVales := l_CarteraVales;
    l_DetalleTraspaso := int_DetalleTraspaso;
    try
      TROSerializer(ASerializer).ReadArray('DetalleTraspaso', ATDetalleTransaccion, l_DetalleTraspaso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleTraspaso']));
    end;
    if int_DetalleTraspaso <> l_DetalleTraspaso then DetalleTraspaso.Free;
    Self.DetalleTraspaso := l_DetalleTraspaso;
    l_DetalleVenta := int_DetalleVenta;
    try
      TROSerializer(ASerializer).ReadArray('DetalleVenta', ATDetalleTransaccion, l_DetalleVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleVenta']));
    end;
    if int_DetalleVenta <> l_DetalleVenta then DetalleVenta.Free;
    Self.DetalleVenta := l_DetalleVenta;
    l_Liquidacion := int_Liquidacion;
    try
      TROSerializer(ASerializer).ReadStruct('Liquidacion', TLiquidacion, l_Liquidacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Liquidacion']));
    end;
    if int_Liquidacion <> l_Liquidacion then Liquidacion.Free;
    Self.Liquidacion := l_Liquidacion;
    l_LiquidacionCombustible := int_LiquidacionCombustible;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionCombustible', ATLiquidacionProducto, l_LiquidacionCombustible);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionCombustible']));
    end;
    if int_LiquidacionCombustible <> l_LiquidacionCombustible then LiquidacionCombustible.Free;
    Self.LiquidacionCombustible := l_LiquidacionCombustible;
    l_LiquidacionDetalle := int_LiquidacionDetalle;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionDetalle', ATLiquidacionDetalle, l_LiquidacionDetalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionDetalle']));
    end;
    if int_LiquidacionDetalle <> l_LiquidacionDetalle then LiquidacionDetalle.Free;
    Self.LiquidacionDetalle := l_LiquidacionDetalle;
    l_LiquidacionProducto := int_LiquidacionProducto;
    try
      TROSerializer(ASerializer).ReadArray('LiquidacionProducto', ATLiquidacionProducto, l_LiquidacionProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionProducto']));
    end;
    if int_LiquidacionProducto <> l_LiquidacionProducto then LiquidacionProducto.Free;
    Self.LiquidacionProducto := l_LiquidacionProducto;
    l_Traspaso := int_Traspaso;
    try
      TROSerializer(ASerializer).ReadStruct('Traspaso', TTransaccion, l_Traspaso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Traspaso']));
    end;
    if int_Traspaso <> l_Traspaso then Traspaso.Free;
    Self.Traspaso := l_Traspaso;
    l_Venta := int_Venta;
    try
      TROSerializer(ASerializer).ReadStruct('Venta', TTransaccion, l_Venta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Venta']));
    end;
    if int_Venta <> l_Venta then Venta.Free;
    Self.Venta := l_Venta;
  end;
end;

procedure TDatosLiquidacion.WriteComplex(ASerializer: TObject);
var
  l_CarteraVales: ATCarteraVale;
  l_DetalleTraspaso: ATDetalleTransaccion;
  l_DetalleVenta: ATDetalleTransaccion;
  l_Liquidacion: TLiquidacion;
  l_LiquidacionCombustible: ATLiquidacionProducto;
  l_LiquidacionDetalle: ATLiquidacionDetalle;
  l_LiquidacionProducto: ATLiquidacionProducto;
  l_Traspaso: TTransaccion;
  l_Venta: TTransaccion;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosLiquidacion);
    l_Liquidacion := int_Liquidacion;
    TROSerializer(ASerializer).WriteStruct('Liquidacion', l_Liquidacion, TLiquidacion);
    l_LiquidacionDetalle := int_LiquidacionDetalle;
    TROSerializer(ASerializer).WriteArray('LiquidacionDetalle', l_LiquidacionDetalle, ATLiquidacionDetalle);
    l_LiquidacionProducto := int_LiquidacionProducto;
    TROSerializer(ASerializer).WriteArray('LiquidacionProducto', l_LiquidacionProducto, ATLiquidacionProducto);
    l_LiquidacionCombustible := int_LiquidacionCombustible;
    TROSerializer(ASerializer).WriteArray('LiquidacionCombustible', l_LiquidacionCombustible, ATLiquidacionProducto);
    l_Traspaso := int_Traspaso;
    TROSerializer(ASerializer).WriteStruct('Traspaso', l_Traspaso, TTransaccion);
    l_Venta := int_Venta;
    TROSerializer(ASerializer).WriteStruct('Venta', l_Venta, TTransaccion);
    l_DetalleTraspaso := int_DetalleTraspaso;
    TROSerializer(ASerializer).WriteArray('DetalleTraspaso', l_DetalleTraspaso, ATDetalleTransaccion);
    l_DetalleVenta := int_DetalleVenta;
    TROSerializer(ASerializer).WriteArray('DetalleVenta', l_DetalleVenta, ATDetalleTransaccion);
    l_CarteraVales := int_CarteraVales;
    TROSerializer(ASerializer).WriteArray('CarteraVales', l_CarteraVales, ATCarteraVale);
  end
  else begin
    l_CarteraVales := int_CarteraVales;
    TROSerializer(ASerializer).WriteArray('CarteraVales', l_CarteraVales, ATCarteraVale);
    l_DetalleTraspaso := int_DetalleTraspaso;
    TROSerializer(ASerializer).WriteArray('DetalleTraspaso', l_DetalleTraspaso, ATDetalleTransaccion);
    l_DetalleVenta := int_DetalleVenta;
    TROSerializer(ASerializer).WriteArray('DetalleVenta', l_DetalleVenta, ATDetalleTransaccion);
    l_Liquidacion := int_Liquidacion;
    TROSerializer(ASerializer).WriteStruct('Liquidacion', l_Liquidacion, TLiquidacion);
    l_LiquidacionCombustible := int_LiquidacionCombustible;
    TROSerializer(ASerializer).WriteArray('LiquidacionCombustible', l_LiquidacionCombustible, ATLiquidacionProducto);
    l_LiquidacionDetalle := int_LiquidacionDetalle;
    TROSerializer(ASerializer).WriteArray('LiquidacionDetalle', l_LiquidacionDetalle, ATLiquidacionDetalle);
    l_LiquidacionProducto := int_LiquidacionProducto;
    TROSerializer(ASerializer).WriteArray('LiquidacionProducto', l_LiquidacionProducto, ATLiquidacionProducto);
    l_Traspaso := int_Traspaso;
    TROSerializer(ASerializer).WriteStruct('Traspaso', l_Traspaso, TTransaccion);
    l_Venta := int_Venta;
    TROSerializer(ASerializer).WriteStruct('Venta', l_Venta, TTransaccion);
  end;
end;

{ TDatosLiquidacionCollection }
constructor TDatosLiquidacionCollection.Create;
begin
  inherited Create(TDatosLiquidacion);
end;

constructor TDatosLiquidacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosLiquidacionCollection.Add: TDatosLiquidacion;
begin
  Result := TDatosLiquidacion(inherited Add);
end;

function TDatosLiquidacionCollection.GetItems(aIndex: Integer): TDatosLiquidacion;
begin
  Result := TDatosLiquidacion(inherited Items[aIndex]);
end;

procedure TDatosLiquidacionCollection.SetItems(aIndex: Integer; const Value: TDatosLiquidacion);
begin
  TDatosLiquidacion(inherited Items[aIndex]).Assign(Value);
end;

{ TReporte }

procedure TReporte.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TReporte;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TReporte) then begin
    lSource := LibraryValesCupones_Intf.TReporte(iSource);

    Self.SQL1 := lSource.SQL1;
    Self.SQL2 := lSource.SQL2;
    Self.Template := lSource.Template;
    Self.CampoJoin := lSource.CampoJoin;
  end;
end;

procedure TReporte.ReadComplex(ASerializer: TObject);
var
  l_CampoJoin: AnsiString;
  l_SQL1: AnsiString;
  l_SQL2: AnsiString;
  l_Template: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_SQL1 := Self.SQL1;
    try
      TROSerializer(ASerializer).ReadAnsiString('SQL1', l_SQL1);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SQL1']));
    end;
    Self.SQL1 := l_SQL1;
    l_SQL2 := Self.SQL2;
    try
      TROSerializer(ASerializer).ReadAnsiString('SQL2', l_SQL2);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SQL2']));
    end;
    Self.SQL2 := l_SQL2;
    l_Template := Self.Template;
    try
      TROSerializer(ASerializer).ReadAnsiString('Template', l_Template);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Template']));
    end;
    Self.Template := l_Template;
    l_CampoJoin := Self.CampoJoin;
    try
      TROSerializer(ASerializer).ReadAnsiString('CampoJoin', l_CampoJoin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CampoJoin']));
    end;
    Self.CampoJoin := l_CampoJoin;
  end
  else begin
    l_CampoJoin := Self.CampoJoin;
    try
      TROSerializer(ASerializer).ReadAnsiString('CampoJoin', l_CampoJoin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CampoJoin']));
    end;
    Self.CampoJoin := l_CampoJoin;
    l_SQL1 := Self.SQL1;
    try
      TROSerializer(ASerializer).ReadAnsiString('SQL1', l_SQL1);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SQL1']));
    end;
    Self.SQL1 := l_SQL1;
    l_SQL2 := Self.SQL2;
    try
      TROSerializer(ASerializer).ReadAnsiString('SQL2', l_SQL2);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SQL2']));
    end;
    Self.SQL2 := l_SQL2;
    l_Template := Self.Template;
    try
      TROSerializer(ASerializer).ReadAnsiString('Template', l_Template);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Template']));
    end;
    Self.Template := l_Template;
  end;
end;

procedure TReporte.WriteComplex(ASerializer: TObject);
var
  l_CampoJoin: AnsiString;
  l_SQL1: AnsiString;
  l_SQL2: AnsiString;
  l_Template: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TReporte);
    l_SQL1 := Self.SQL1;
    TROSerializer(ASerializer).WriteAnsiString('SQL1', l_SQL1);
    l_SQL2 := Self.SQL2;
    TROSerializer(ASerializer).WriteAnsiString('SQL2', l_SQL2);
    l_Template := Self.Template;
    TROSerializer(ASerializer).WriteAnsiString('Template', l_Template);
    l_CampoJoin := Self.CampoJoin;
    TROSerializer(ASerializer).WriteAnsiString('CampoJoin', l_CampoJoin);
  end
  else begin
    l_CampoJoin := Self.CampoJoin;
    TROSerializer(ASerializer).WriteAnsiString('CampoJoin', l_CampoJoin);
    l_SQL1 := Self.SQL1;
    TROSerializer(ASerializer).WriteAnsiString('SQL1', l_SQL1);
    l_SQL2 := Self.SQL2;
    TROSerializer(ASerializer).WriteAnsiString('SQL2', l_SQL2);
    l_Template := Self.Template;
    TROSerializer(ASerializer).WriteAnsiString('Template', l_Template);
  end;
end;

{ TReporteCollection }
constructor TReporteCollection.Create;
begin
  inherited Create(TReporte);
end;

constructor TReporteCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TReporteCollection.Add: TReporte;
begin
  Result := TReporte(inherited Add);
end;

function TReporteCollection.GetItems(aIndex: Integer): TReporte;
begin
  Result := TReporte(inherited Items[aIndex]);
end;

procedure TReporteCollection.SetItems(aIndex: Integer; const Value: TReporte);
begin
  TReporte(inherited Items[aIndex]).Assign(Value);
end;

{ TRecibo }

procedure TRecibo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TRecibo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TRecibo) then begin
    lSource := LibraryValesCupones_Intf.TRecibo(iSource);

    Self.Serie := lSource.Serie;
    Self.Folio := lSource.Folio;
    Self.Fecha := lSource.Fecha;
    Self.Subtotal := lSource.Subtotal;
    Self.Ejercicio := lSource.Ejercicio;
    Self.Periodo := lSource.Periodo;
    Self.Dia := lSource.Dia;
    Self.Impuesto := lSource.Impuesto;
    Self.Total := lSource.Total;
    Self.ClienteID := lSource.ClienteID;
    Self.EstacionID := lSource.EstacionID;
    Self.FormaPagoID := lSource.FormaPagoID;
    Self.ReciboPagoID := lSource.ReciboPagoID;
  end;
end;

procedure TRecibo.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_Dia: Integer;
  l_Ejercicio: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_FormaPagoID: Integer;
  l_Impuesto: Double;
  l_Periodo: Integer;
  l_ReciboPagoID: Integer;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FormaPagoID := Self.FormaPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPagoID']));
    end;
    Self.FormaPagoID := l_FormaPagoID;
    l_ReciboPagoID := Self.ReciboPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('ReciboPagoID', TypInfo.otSLong, l_ReciboPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ReciboPagoID']));
    end;
    Self.ReciboPagoID := l_ReciboPagoID;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_FormaPagoID := Self.FormaPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPagoID']));
    end;
    Self.FormaPagoID := l_FormaPagoID;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_ReciboPagoID := Self.ReciboPagoID;
    try
      TROSerializer(ASerializer).ReadInteger('ReciboPagoID', TypInfo.otSLong, l_ReciboPagoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ReciboPagoID']));
    end;
    Self.ReciboPagoID := l_ReciboPagoID;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
  end;
end;

procedure TRecibo.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_Dia: Integer;
  l_Ejercicio: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_FormaPagoID: Integer;
  l_Impuesto: Double;
  l_Periodo: Integer;
  l_ReciboPagoID: Integer;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TRecibo);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FormaPagoID := Self.FormaPagoID;
    TROSerializer(ASerializer).WriteInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    l_ReciboPagoID := Self.ReciboPagoID;
    TROSerializer(ASerializer).WriteInteger('ReciboPagoID', TypInfo.otSLong, l_ReciboPagoID);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_FormaPagoID := Self.FormaPagoID;
    TROSerializer(ASerializer).WriteInteger('FormaPagoID', TypInfo.otSLong, l_FormaPagoID);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_ReciboPagoID := Self.ReciboPagoID;
    TROSerializer(ASerializer).WriteInteger('ReciboPagoID', TypInfo.otSLong, l_ReciboPagoID);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
  end;
end;

{ TReciboCollection }
constructor TReciboCollection.Create;
begin
  inherited Create(TRecibo);
end;

constructor TReciboCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TReciboCollection.Add: TRecibo;
begin
  Result := TRecibo(inherited Add);
end;

function TReciboCollection.GetItems(aIndex: Integer): TRecibo;
begin
  Result := TRecibo(inherited Items[aIndex]);
end;

procedure TReciboCollection.SetItems(aIndex: Integer; const Value: TRecibo);
begin
  TRecibo(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleReciboPago }

procedure TDetalleReciboPago.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleReciboPago;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleReciboPago) then begin
    lSource := LibraryValesCupones_Intf.TDetalleReciboPago(iSource);

    Self.FacturaID := lSource.FacturaID;
    Self.Total := lSource.Total;
    Self.ReciboID := lSource.ReciboID;
    Self.ItemNo := lSource.ItemNo;
  end;
end;

procedure TDetalleReciboPago.ReadComplex(ASerializer: TObject);
var
  l_FacturaID: Integer;
  l_ItemNo: Integer;
  l_ReciboID: Integer;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_ReciboID := Self.ReciboID;
    try
      TROSerializer(ASerializer).ReadInteger('ReciboID', TypInfo.otSLong, l_ReciboID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ReciboID']));
    end;
    Self.ReciboID := l_ReciboID;
    l_ItemNo := Self.ItemNo;
    try
      TROSerializer(ASerializer).ReadInteger('ItemNo', TypInfo.otSLong, l_ItemNo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ItemNo']));
    end;
    Self.ItemNo := l_ItemNo;
  end
  else begin
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_ItemNo := Self.ItemNo;
    try
      TROSerializer(ASerializer).ReadInteger('ItemNo', TypInfo.otSLong, l_ItemNo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ItemNo']));
    end;
    Self.ItemNo := l_ItemNo;
    l_ReciboID := Self.ReciboID;
    try
      TROSerializer(ASerializer).ReadInteger('ReciboID', TypInfo.otSLong, l_ReciboID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ReciboID']));
    end;
    Self.ReciboID := l_ReciboID;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
  end;
end;

procedure TDetalleReciboPago.WriteComplex(ASerializer: TObject);
var
  l_FacturaID: Integer;
  l_ItemNo: Integer;
  l_ReciboID: Integer;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleReciboPago);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_ReciboID := Self.ReciboID;
    TROSerializer(ASerializer).WriteInteger('ReciboID', TypInfo.otSLong, l_ReciboID);
    l_ItemNo := Self.ItemNo;
    TROSerializer(ASerializer).WriteInteger('ItemNo', TypInfo.otSLong, l_ItemNo);
  end
  else begin
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_ItemNo := Self.ItemNo;
    TROSerializer(ASerializer).WriteInteger('ItemNo', TypInfo.otSLong, l_ItemNo);
    l_ReciboID := Self.ReciboID;
    TROSerializer(ASerializer).WriteInteger('ReciboID', TypInfo.otSLong, l_ReciboID);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
  end;
end;

{ TDetalleReciboPagoCollection }
constructor TDetalleReciboPagoCollection.Create;
begin
  inherited Create(TDetalleReciboPago);
end;

constructor TDetalleReciboPagoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleReciboPagoCollection.Add: TDetalleReciboPago;
begin
  Result := TDetalleReciboPago(inherited Add);
end;

function TDetalleReciboPagoCollection.GetItems(aIndex: Integer): TDetalleReciboPago;
begin
  Result := TDetalleReciboPago(inherited Items[aIndex]);
end;

procedure TDetalleReciboPagoCollection.LoadFromArray(anArray: ATDetalleRecibo);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleReciboPagoCollection.SaveToArray(anArray: ATDetalleRecibo);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleReciboPago(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleReciboPagoCollection.SetItems(aIndex: Integer; const Value: TDetalleReciboPago);
begin
  TDetalleReciboPago(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosReciboPago }

procedure TDatosReciboPago.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosReciboPago;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosReciboPago) then begin
    lSource := LibraryValesCupones_Intf.TDatosReciboPago(iSource);

    if System.Assigned(lSource.fRecibo) then
      Self.Recibo.Assign(lSource.fRecibo)
    else
      SysUtils.FreeAndNil(fRecibo);
    if System.Assigned(lSource.fDetalles) then
      Self.Detalles.Assign(lSource.fDetalles)
    else
      SysUtils.FreeAndNil(fDetalles);
  end;
end;

function TDatosReciboPago.GetRecibo: TRecibo;
begin
  if (fRecibo = nil) then fRecibo := TRecibo.Create();
  Result := fRecibo;
end;

function TDatosReciboPago.GetDetalles: ATDetalleRecibo;
begin
  if (fDetalles = nil) then fDetalles := ATDetalleRecibo.Create();
  Result := fDetalles;
end;

procedure TDatosReciboPago.ReadComplex(ASerializer: TObject);
var
  l_Detalles: ATDetalleRecibo;
  l_Recibo: TRecibo;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Recibo := int_Recibo;
    try
      TROSerializer(ASerializer).ReadStruct('Recibo', TRecibo, l_Recibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recibo']));
    end;
    if int_Recibo <> l_Recibo then Self.Recibo.Free;
    Self.Recibo := l_Recibo;
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleRecibo, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Self.Detalles.Free;
    Self.Detalles := l_Detalles;
  end
  else begin
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleRecibo, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Detalles.Free;
    Self.Detalles := l_Detalles;
    l_Recibo := int_Recibo;
    try
      TROSerializer(ASerializer).ReadStruct('Recibo', TRecibo, l_Recibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recibo']));
    end;
    if int_Recibo <> l_Recibo then Recibo.Free;
    Self.Recibo := l_Recibo;
  end;
end;

procedure TDatosReciboPago.WriteComplex(ASerializer: TObject);
var
  l_Detalles: ATDetalleRecibo;
  l_Recibo: TRecibo;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosReciboPago);
    l_Recibo := int_Recibo;
    TROSerializer(ASerializer).WriteStruct('Recibo', l_Recibo, TRecibo);
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleRecibo);
  end
  else begin
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleRecibo);
    l_Recibo := int_Recibo;
    TROSerializer(ASerializer).WriteStruct('Recibo', l_Recibo, TRecibo);
  end;
end;

{ TDatosReciboPagoCollection }
constructor TDatosReciboPagoCollection.Create;
begin
  inherited Create(TDatosReciboPago);
end;

constructor TDatosReciboPagoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosReciboPagoCollection.Add: TDatosReciboPago;
begin
  Result := TDatosReciboPago(inherited Add);
end;

function TDatosReciboPagoCollection.GetItems(aIndex: Integer): TDatosReciboPago;
begin
  Result := TDatosReciboPago(inherited Items[aIndex]);
end;

procedure TDatosReciboPagoCollection.SetItems(aIndex: Integer; const Value: TDatosReciboPago);
begin
  TDatosReciboPago(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosRecibo }

procedure TDatosRecibo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosRecibo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosRecibo) then begin
    lSource := LibraryValesCupones_Intf.TDatosRecibo(iSource);

    if System.Assigned(lSource.fRecibo) then
      Self.Recibo.Assign(lSource.fRecibo)
    else
      SysUtils.FreeAndNil(fRecibo);
    if System.Assigned(lSource.fDetalles) then
      Self.Detalles.Assign(lSource.fDetalles)
    else
      SysUtils.FreeAndNil(fDetalles);
  end;
end;

function TDatosRecibo.GetRecibo: TRecibo;
begin
  if (fRecibo = nil) then fRecibo := TRecibo.Create();
  Result := fRecibo;
end;

function TDatosRecibo.GetDetalles: ATDetalleRecibo;
begin
  if (fDetalles = nil) then fDetalles := ATDetalleRecibo.Create();
  Result := fDetalles;
end;

procedure TDatosRecibo.ReadComplex(ASerializer: TObject);
var
  l_Detalles: ATDetalleRecibo;
  l_Recibo: TRecibo;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Recibo := int_Recibo;
    try
      TROSerializer(ASerializer).ReadStruct('Recibo', TRecibo, l_Recibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recibo']));
    end;
    if int_Recibo <> l_Recibo then Self.Recibo.Free;
    Self.Recibo := l_Recibo;
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleRecibo, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Self.Detalles.Free;
    Self.Detalles := l_Detalles;
  end
  else begin
    l_Detalles := int_Detalles;
    try
      TROSerializer(ASerializer).ReadArray('Detalles', ATDetalleRecibo, l_Detalles);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalles']));
    end;
    if int_Detalles <> l_Detalles then Detalles.Free;
    Self.Detalles := l_Detalles;
    l_Recibo := int_Recibo;
    try
      TROSerializer(ASerializer).ReadStruct('Recibo', TRecibo, l_Recibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recibo']));
    end;
    if int_Recibo <> l_Recibo then Recibo.Free;
    Self.Recibo := l_Recibo;
  end;
end;

procedure TDatosRecibo.WriteComplex(ASerializer: TObject);
var
  l_Detalles: ATDetalleRecibo;
  l_Recibo: TRecibo;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosRecibo);
    l_Recibo := int_Recibo;
    TROSerializer(ASerializer).WriteStruct('Recibo', l_Recibo, TRecibo);
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleRecibo);
  end
  else begin
    l_Detalles := int_Detalles;
    TROSerializer(ASerializer).WriteArray('Detalles', l_Detalles, ATDetalleRecibo);
    l_Recibo := int_Recibo;
    TROSerializer(ASerializer).WriteStruct('Recibo', l_Recibo, TRecibo);
  end;
end;

{ TDatosReciboCollection }
constructor TDatosReciboCollection.Create;
begin
  inherited Create(TDatosRecibo);
end;

constructor TDatosReciboCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosReciboCollection.Add: TDatosRecibo;
begin
  Result := TDatosRecibo(inherited Add);
end;

function TDatosReciboCollection.GetItems(aIndex: Integer): TDatosRecibo;
begin
  Result := TDatosRecibo(inherited Items[aIndex]);
end;

procedure TDatosReciboCollection.SetItems(aIndex: Integer; const Value: TDatosRecibo);
begin
  TDatosRecibo(inherited Items[aIndex]).Assign(Value);
end;

{ TLoginInfo }

procedure TLoginInfo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLoginInfo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLoginInfo) then begin
    lSource := LibraryValesCupones_Intf.TLoginInfo(iSource);

    Self.Valida := lSource.Valida;
    Self.EmpleadoID := lSource.EmpleadoID;
    Self.NombreEmpleado := lSource.NombreEmpleado;
    if System.Assigned(lSource.fAccesos) then
      Self.Accesos.Assign(lSource.fAccesos)
    else
      SysUtils.FreeAndNil(fAccesos);
  end;
end;

function TLoginInfo.GetAccesos: ArrayOpcion;
begin
  if (fAccesos = nil) then fAccesos := ArrayOpcion.Create();
  Result := fAccesos;
end;

procedure TLoginInfo.ReadComplex(ASerializer: TObject);
var
  l_Accesos: ArrayOpcion;
  l_EmpleadoID: Integer;
  l_NombreEmpleado: AnsiString;
  l_Valida: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Valida := Self.Valida;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valida',System.TypeInfo(boolean), l_Valida);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valida']));
    end;
    Self.Valida := l_Valida;
    l_EmpleadoID := Self.EmpleadoID;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoID', TypInfo.otSLong, l_EmpleadoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoID']));
    end;
    Self.EmpleadoID := l_EmpleadoID;
    l_NombreEmpleado := Self.NombreEmpleado;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreEmpleado', l_NombreEmpleado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreEmpleado']));
    end;
    Self.NombreEmpleado := l_NombreEmpleado;
    l_Accesos := int_Accesos;
    try
      TROSerializer(ASerializer).ReadArray('Accesos', ArrayOpcion, l_Accesos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Accesos']));
    end;
    if int_Accesos <> l_Accesos then Self.Accesos.Free;
    Self.Accesos := l_Accesos;
  end
  else begin
    l_Accesos := int_Accesos;
    try
      TROSerializer(ASerializer).ReadArray('Accesos', ArrayOpcion, l_Accesos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Accesos']));
    end;
    if int_Accesos <> l_Accesos then Accesos.Free;
    Self.Accesos := l_Accesos;
    l_EmpleadoID := Self.EmpleadoID;
    try
      TROSerializer(ASerializer).ReadInteger('EmpleadoID', TypInfo.otSLong, l_EmpleadoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EmpleadoID']));
    end;
    Self.EmpleadoID := l_EmpleadoID;
    l_NombreEmpleado := Self.NombreEmpleado;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreEmpleado', l_NombreEmpleado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreEmpleado']));
    end;
    Self.NombreEmpleado := l_NombreEmpleado;
    l_Valida := Self.Valida;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valida',System.TypeInfo(boolean), l_Valida);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valida']));
    end;
    Self.Valida := l_Valida;
  end;
end;

procedure TLoginInfo.WriteComplex(ASerializer: TObject);
var
  l_Accesos: ArrayOpcion;
  l_EmpleadoID: Integer;
  l_NombreEmpleado: AnsiString;
  l_Valida: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLoginInfo);
    l_Valida := Self.Valida;
    TROSerializer(ASerializer).WriteEnumerated('Valida',System.TypeInfo(boolean), l_Valida);
    l_EmpleadoID := Self.EmpleadoID;
    TROSerializer(ASerializer).WriteInteger('EmpleadoID', TypInfo.otSLong, l_EmpleadoID);
    l_NombreEmpleado := Self.NombreEmpleado;
    TROSerializer(ASerializer).WriteAnsiString('NombreEmpleado', l_NombreEmpleado);
    l_Accesos := int_Accesos;
    TROSerializer(ASerializer).WriteArray('Accesos', l_Accesos, ArrayOpcion);
  end
  else begin
    l_Accesos := int_Accesos;
    TROSerializer(ASerializer).WriteArray('Accesos', l_Accesos, ArrayOpcion);
    l_EmpleadoID := Self.EmpleadoID;
    TROSerializer(ASerializer).WriteInteger('EmpleadoID', TypInfo.otSLong, l_EmpleadoID);
    l_NombreEmpleado := Self.NombreEmpleado;
    TROSerializer(ASerializer).WriteAnsiString('NombreEmpleado', l_NombreEmpleado);
    l_Valida := Self.Valida;
    TROSerializer(ASerializer).WriteEnumerated('Valida',System.TypeInfo(boolean), l_Valida);
  end;
end;

{ TLoginInfoCollection }
constructor TLoginInfoCollection.Create;
begin
  inherited Create(TLoginInfo);
end;

constructor TLoginInfoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLoginInfoCollection.Add: TLoginInfo;
begin
  Result := TLoginInfo(inherited Add);
end;

function TLoginInfoCollection.GetItems(aIndex: Integer): TLoginInfo;
begin
  Result := TLoginInfo(inherited Items[aIndex]);
end;

procedure TLoginInfoCollection.SetItems(aIndex: Integer; const Value: TLoginInfo);
begin
  TLoginInfo(inherited Items[aIndex]).Assign(Value);
end;

{ TOpcion }

procedure TOpcion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TOpcion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TOpcion) then begin
    lSource := LibraryValesCupones_Intf.TOpcion(iSource);

    Self.OpcionID := lSource.OpcionID;
    Self.Nombre := lSource.Nombre;
  end;
end;

procedure TOpcion.ReadComplex(ASerializer: TObject);
var
  l_Nombre: AnsiString;
  l_OpcionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_OpcionID := Self.OpcionID;
    try
      TROSerializer(ASerializer).ReadInteger('OpcionID', TypInfo.otSLong, l_OpcionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'OpcionID']));
    end;
    Self.OpcionID := l_OpcionID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
  end
  else begin
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_OpcionID := Self.OpcionID;
    try
      TROSerializer(ASerializer).ReadInteger('OpcionID', TypInfo.otSLong, l_OpcionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'OpcionID']));
    end;
    Self.OpcionID := l_OpcionID;
  end;
end;

procedure TOpcion.WriteComplex(ASerializer: TObject);
var
  l_Nombre: AnsiString;
  l_OpcionID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TOpcion);
    l_OpcionID := Self.OpcionID;
    TROSerializer(ASerializer).WriteInteger('OpcionID', TypInfo.otSLong, l_OpcionID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
  end
  else begin
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_OpcionID := Self.OpcionID;
    TROSerializer(ASerializer).WriteInteger('OpcionID', TypInfo.otSLong, l_OpcionID);
  end;
end;

{ TOpcionCollection }
constructor TOpcionCollection.Create;
begin
  inherited Create(TOpcion);
end;

constructor TOpcionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TOpcionCollection.Add: TOpcion;
begin
  Result := TOpcion(inherited Add);
end;

function TOpcionCollection.GetItems(aIndex: Integer): TOpcion;
begin
  Result := TOpcion(inherited Items[aIndex]);
end;

procedure TOpcionCollection.LoadFromArray(anArray: ArrayOpcion);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TOpcionCollection.SaveToArray(anArray: ArrayOpcion);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TOpcion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TOpcionCollection.SetItems(aIndex: Integer; const Value: TOpcion);
begin
  TOpcion(inherited Items[aIndex]).Assign(Value);
end;

{ TConsumoExpress }

procedure TConsumoExpress.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TConsumoExpress;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TConsumoExpress) then begin
    lSource := LibraryValesCupones_Intf.TConsumoExpress(iSource);

    Self.Secuencia := lSource.Secuencia;
    Self.EstacionID := lSource.EstacionID;
    Self.ClienteID := lSource.ClienteID;
    Self.Precio := lSource.Precio;
    Self.Volumen := lSource.Volumen;
    Self.Importe := lSource.Importe;
    Self.Facturado := lSource.Facturado;
    Self.BombaID := lSource.BombaID;
    Self.ProductoID := lSource.ProductoID;
    Self.FacturaID := lSource.FacturaID;
    Self.FechaConsumo := lSource.FechaConsumo;
  end;
end;

procedure TConsumoExpress.ReadComplex(ASerializer: TObject);
var
  l_BombaID: Integer;
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_Facturado: Boolean;
  l_FacturaID: Integer;
  l_FechaConsumo: DateTime;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_Secuencia: Integer;
  l_Volumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_Volumen := Self.Volumen;
    try
      TROSerializer(ASerializer).ReadDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumen']));
    end;
    Self.Volumen := l_Volumen;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Facturado := Self.Facturado;
    try
      TROSerializer(ASerializer).ReadEnumerated('Facturado',System.TypeInfo(boolean), l_Facturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Facturado']));
    end;
    Self.Facturado := l_Facturado;
    l_BombaID := Self.BombaID;
    try
      TROSerializer(ASerializer).ReadInteger('BombaID', TypInfo.otSLong, l_BombaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BombaID']));
    end;
    Self.BombaID := l_BombaID;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_FechaConsumo := Self.FechaConsumo;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaConsumo', l_FechaConsumo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaConsumo']));
    end;
    Self.FechaConsumo := l_FechaConsumo;
  end
  else begin
    l_BombaID := Self.BombaID;
    try
      TROSerializer(ASerializer).ReadInteger('BombaID', TypInfo.otSLong, l_BombaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BombaID']));
    end;
    Self.BombaID := l_BombaID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Facturado := Self.Facturado;
    try
      TROSerializer(ASerializer).ReadEnumerated('Facturado',System.TypeInfo(boolean), l_Facturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Facturado']));
    end;
    Self.Facturado := l_Facturado;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_FechaConsumo := Self.FechaConsumo;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaConsumo', l_FechaConsumo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaConsumo']));
    end;
    Self.FechaConsumo := l_FechaConsumo;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Volumen := Self.Volumen;
    try
      TROSerializer(ASerializer).ReadDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumen']));
    end;
    Self.Volumen := l_Volumen;
  end;
end;

procedure TConsumoExpress.WriteComplex(ASerializer: TObject);
var
  l_BombaID: Integer;
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_Facturado: Boolean;
  l_FacturaID: Integer;
  l_FechaConsumo: DateTime;
  l_Importe: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_Secuencia: Integer;
  l_Volumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TConsumoExpress);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_Volumen := Self.Volumen;
    TROSerializer(ASerializer).WriteDouble('Volumen', TypInfo.ftDouble, l_Volumen);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Facturado := Self.Facturado;
    TROSerializer(ASerializer).WriteEnumerated('Facturado',System.TypeInfo(boolean), l_Facturado);
    l_BombaID := Self.BombaID;
    TROSerializer(ASerializer).WriteInteger('BombaID', TypInfo.otSLong, l_BombaID);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_FechaConsumo := Self.FechaConsumo;
    TROSerializer(ASerializer).WriteDateTime('FechaConsumo', l_FechaConsumo);
  end
  else begin
    l_BombaID := Self.BombaID;
    TROSerializer(ASerializer).WriteInteger('BombaID', TypInfo.otSLong, l_BombaID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Facturado := Self.Facturado;
    TROSerializer(ASerializer).WriteEnumerated('Facturado',System.TypeInfo(boolean), l_Facturado);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_FechaConsumo := Self.FechaConsumo;
    TROSerializer(ASerializer).WriteDateTime('FechaConsumo', l_FechaConsumo);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Volumen := Self.Volumen;
    TROSerializer(ASerializer).WriteDouble('Volumen', TypInfo.ftDouble, l_Volumen);
  end;
end;

{ TConsumoExpressCollection }
constructor TConsumoExpressCollection.Create;
begin
  inherited Create(TConsumoExpress);
end;

constructor TConsumoExpressCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TConsumoExpressCollection.Add: TConsumoExpress;
begin
  Result := TConsumoExpress(inherited Add);
end;

function TConsumoExpressCollection.GetItems(aIndex: Integer): TConsumoExpress;
begin
  Result := TConsumoExpress(inherited Items[aIndex]);
end;

procedure TConsumoExpressCollection.SetItems(aIndex: Integer; const Value: TConsumoExpress);
begin
  TConsumoExpress(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosCliente }

procedure TDatosCliente.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosCliente;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosCliente) then begin
    lSource := LibraryValesCupones_Intf.TDatosCliente(iSource);

    Self.ClienteID := lSource.ClienteID;
    Self.Nombre := lSource.Nombre;
    Self.RFC := lSource.RFC;
    Self.Direccion := lSource.Direccion;
    Self.Telefono := lSource.Telefono;
    Self.CP := lSource.CP;
    Self.Ciudad := lSource.Ciudad;
    Self.Colonia := lSource.Colonia;
    Self.Telefonos := lSource.Telefonos;
    Self.FacturaExpress := lSource.FacturaExpress;
    Self.Saldo := lSource.Saldo;
    Self.MsnError := lSource.MsnError;
  end;
end;

procedure TDatosCliente.ReadComplex(ASerializer: TObject);
var
  l_Ciudad: AnsiString;
  l_ClienteID: Integer;
  l_Colonia: AnsiString;
  l_CP: AnsiString;
  l_Direccion: AnsiString;
  l_FacturaExpress: Boolean;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_RFC: AnsiString;
  l_Saldo: Double;
  l_Telefono: AnsiString;
  l_Telefonos: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Direccion := Self.Direccion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Direccion', l_Direccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Direccion']));
    end;
    Self.Direccion := l_Direccion;
    l_Telefono := Self.Telefono;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefono', l_Telefono);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefono']));
    end;
    Self.Telefono := l_Telefono;
    l_CP := Self.CP;
    try
      TROSerializer(ASerializer).ReadAnsiString('CP', l_CP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CP']));
    end;
    Self.CP := l_CP;
    l_Ciudad := Self.Ciudad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Ciudad', l_Ciudad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ciudad']));
    end;
    Self.Ciudad := l_Ciudad;
    l_Colonia := Self.Colonia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Colonia', l_Colonia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Colonia']));
    end;
    Self.Colonia := l_Colonia;
    l_Telefonos := Self.Telefonos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefonos', l_Telefonos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefonos']));
    end;
    Self.Telefonos := l_Telefonos;
    l_FacturaExpress := Self.FacturaExpress;
    try
      TROSerializer(ASerializer).ReadEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaExpress']));
    end;
    Self.FacturaExpress := l_FacturaExpress;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
  end
  else begin
    l_Ciudad := Self.Ciudad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Ciudad', l_Ciudad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ciudad']));
    end;
    Self.Ciudad := l_Ciudad;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Colonia := Self.Colonia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Colonia', l_Colonia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Colonia']));
    end;
    Self.Colonia := l_Colonia;
    l_CP := Self.CP;
    try
      TROSerializer(ASerializer).ReadAnsiString('CP', l_CP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CP']));
    end;
    Self.CP := l_CP;
    l_Direccion := Self.Direccion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Direccion', l_Direccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Direccion']));
    end;
    Self.Direccion := l_Direccion;
    l_FacturaExpress := Self.FacturaExpress;
    try
      TROSerializer(ASerializer).ReadEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaExpress']));
    end;
    Self.FacturaExpress := l_FacturaExpress;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Telefono := Self.Telefono;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefono', l_Telefono);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefono']));
    end;
    Self.Telefono := l_Telefono;
    l_Telefonos := Self.Telefonos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefonos', l_Telefonos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefonos']));
    end;
    Self.Telefonos := l_Telefonos;
  end;
end;

procedure TDatosCliente.WriteComplex(ASerializer: TObject);
var
  l_Ciudad: AnsiString;
  l_ClienteID: Integer;
  l_Colonia: AnsiString;
  l_CP: AnsiString;
  l_Direccion: AnsiString;
  l_FacturaExpress: Boolean;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_RFC: AnsiString;
  l_Saldo: Double;
  l_Telefono: AnsiString;
  l_Telefonos: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosCliente);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Direccion := Self.Direccion;
    TROSerializer(ASerializer).WriteAnsiString('Direccion', l_Direccion);
    l_Telefono := Self.Telefono;
    TROSerializer(ASerializer).WriteAnsiString('Telefono', l_Telefono);
    l_CP := Self.CP;
    TROSerializer(ASerializer).WriteAnsiString('CP', l_CP);
    l_Ciudad := Self.Ciudad;
    TROSerializer(ASerializer).WriteAnsiString('Ciudad', l_Ciudad);
    l_Colonia := Self.Colonia;
    TROSerializer(ASerializer).WriteAnsiString('Colonia', l_Colonia);
    l_Telefonos := Self.Telefonos;
    TROSerializer(ASerializer).WriteAnsiString('Telefonos', l_Telefonos);
    l_FacturaExpress := Self.FacturaExpress;
    TROSerializer(ASerializer).WriteEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
  end
  else begin
    l_Ciudad := Self.Ciudad;
    TROSerializer(ASerializer).WriteAnsiString('Ciudad', l_Ciudad);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Colonia := Self.Colonia;
    TROSerializer(ASerializer).WriteAnsiString('Colonia', l_Colonia);
    l_CP := Self.CP;
    TROSerializer(ASerializer).WriteAnsiString('CP', l_CP);
    l_Direccion := Self.Direccion;
    TROSerializer(ASerializer).WriteAnsiString('Direccion', l_Direccion);
    l_FacturaExpress := Self.FacturaExpress;
    TROSerializer(ASerializer).WriteEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Telefono := Self.Telefono;
    TROSerializer(ASerializer).WriteAnsiString('Telefono', l_Telefono);
    l_Telefonos := Self.Telefonos;
    TROSerializer(ASerializer).WriteAnsiString('Telefonos', l_Telefonos);
  end;
end;

{ TDatosClienteCollection }
constructor TDatosClienteCollection.Create;
begin
  inherited Create(TDatosCliente);
end;

constructor TDatosClienteCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosClienteCollection.Add: TDatosCliente;
begin
  Result := TDatosCliente(inherited Add);
end;

function TDatosClienteCollection.GetItems(aIndex: Integer): TDatosCliente;
begin
  Result := TDatosCliente(inherited Items[aIndex]);
end;

procedure TDatosClienteCollection.SetItems(aIndex: Integer; const Value: TDatosCliente);
begin
  TDatosCliente(inherited Items[aIndex]).Assign(Value);
end;

{ TFacturaExpress }

procedure TFacturaExpress.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFacturaExpress;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFacturaExpress) then begin
    lSource := LibraryValesCupones_Intf.TFacturaExpress(iSource);

    Self.FacturaID := lSource.FacturaID;
    Self.Serie := lSource.Serie;
    Self.Folio := lSource.Folio;
    Self.Fecha := lSource.Fecha;
    if System.Assigned(lSource.fCliente) then
      Self.Cliente.Assign(lSource.fCliente)
    else
      SysUtils.FreeAndNil(fCliente);
    Self.Subtotal := lSource.Subtotal;
    Self.IVA := lSource.IVA;
    Self.Total := lSource.Total;
    Self.Impuesto := lSource.Impuesto;
    if System.Assigned(lSource.fDetalle) then
      Self.Detalle.Assign(lSource.fDetalle)
    else
      SysUtils.FreeAndNil(fDetalle);
  end;
end;

function TFacturaExpress.GetCliente: TDatosCliente;
begin
  if (fCliente = nil) then fCliente := TDatosCliente.Create();
  Result := fCliente;
end;

function TFacturaExpress.GetDetalle: ADetalleExpress;
begin
  if (fDetalle = nil) then fDetalle := ADetalleExpress.Create();
  Result := fDetalle;
end;

procedure TFacturaExpress.ReadComplex(ASerializer: TObject);
var
  l_Cliente: TDatosCliente;
  l_Detalle: ADetalleExpress;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Impuesto: Double;
  l_IVA: Double;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Cliente := int_Cliente;
    try
      TROSerializer(ASerializer).ReadStruct('Cliente', TDatosCliente, l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    if int_Cliente <> l_Cliente then Self.Cliente.Free;
    Self.Cliente := l_Cliente;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_IVA := Self.IVA;
    try
      TROSerializer(ASerializer).ReadDouble('IVA', TypInfo.ftDouble, l_IVA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVA']));
    end;
    Self.IVA := l_IVA;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Detalle := int_Detalle;
    try
      TROSerializer(ASerializer).ReadArray('Detalle', ADetalleExpress, l_Detalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalle']));
    end;
    if int_Detalle <> l_Detalle then Self.Detalle.Free;
    Self.Detalle := l_Detalle;
  end
  else begin
    l_Cliente := int_Cliente;
    try
      TROSerializer(ASerializer).ReadStruct('Cliente', TDatosCliente, l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    if int_Cliente <> l_Cliente then Cliente.Free;
    Self.Cliente := l_Cliente;
    l_Detalle := int_Detalle;
    try
      TROSerializer(ASerializer).ReadArray('Detalle', ADetalleExpress, l_Detalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalle']));
    end;
    if int_Detalle <> l_Detalle then Detalle.Free;
    Self.Detalle := l_Detalle;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_IVA := Self.IVA;
    try
      TROSerializer(ASerializer).ReadDouble('IVA', TypInfo.ftDouble, l_IVA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IVA']));
    end;
    Self.IVA := l_IVA;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Subtotal := Self.Subtotal;
    try
      TROSerializer(ASerializer).ReadDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Subtotal']));
    end;
    Self.Subtotal := l_Subtotal;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
  end;
end;

procedure TFacturaExpress.WriteComplex(ASerializer: TObject);
var
  l_Cliente: TDatosCliente;
  l_Detalle: ADetalleExpress;
  l_FacturaID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Impuesto: Double;
  l_IVA: Double;
  l_Serie: AnsiString;
  l_Subtotal: Double;
  l_Total: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFacturaExpress);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Cliente := int_Cliente;
    TROSerializer(ASerializer).WriteStruct('Cliente', l_Cliente, TDatosCliente);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_IVA := Self.IVA;
    TROSerializer(ASerializer).WriteDouble('IVA', TypInfo.ftDouble, l_IVA);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Detalle := int_Detalle;
    TROSerializer(ASerializer).WriteArray('Detalle', l_Detalle, ADetalleExpress);
  end
  else begin
    l_Cliente := int_Cliente;
    TROSerializer(ASerializer).WriteStruct('Cliente', l_Cliente, TDatosCliente);
    l_Detalle := int_Detalle;
    TROSerializer(ASerializer).WriteArray('Detalle', l_Detalle, ADetalleExpress);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_IVA := Self.IVA;
    TROSerializer(ASerializer).WriteDouble('IVA', TypInfo.ftDouble, l_IVA);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Subtotal := Self.Subtotal;
    TROSerializer(ASerializer).WriteDouble('Subtotal', TypInfo.ftDouble, l_Subtotal);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
  end;
end;

{ TFacturaExpressCollection }
constructor TFacturaExpressCollection.Create;
begin
  inherited Create(TFacturaExpress);
end;

constructor TFacturaExpressCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFacturaExpressCollection.Add: TFacturaExpress;
begin
  Result := TFacturaExpress(inherited Add);
end;

function TFacturaExpressCollection.GetItems(aIndex: Integer): TFacturaExpress;
begin
  Result := TFacturaExpress(inherited Items[aIndex]);
end;

procedure TFacturaExpressCollection.SetItems(aIndex: Integer; const Value: TFacturaExpress);
begin
  TFacturaExpress(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleExpress }

procedure TDetalleExpress.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleExpress;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleExpress) then begin
    lSource := LibraryValesCupones_Intf.TDetalleExpress(iSource);

    Self.Codigo := lSource.Codigo;
    Self.Descripcion := lSource.Descripcion;
    Self.Cantidad := lSource.Cantidad;
    Self.Precio := lSource.Precio;
    Self.Importe := lSource.Importe;
  end;
end;

procedure TDetalleExpress.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Codigo: AnsiString;
  l_Descripcion: AnsiString;
  l_Importe: Double;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end;
end;

procedure TDetalleExpress.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_Codigo: AnsiString;
  l_Descripcion: AnsiString;
  l_Importe: Double;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleExpress);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end;
end;

{ TDetalleExpressCollection }
constructor TDetalleExpressCollection.Create;
begin
  inherited Create(TDetalleExpress);
end;

constructor TDetalleExpressCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleExpressCollection.Add: TDetalleExpress;
begin
  Result := TDetalleExpress(inherited Add);
end;

function TDetalleExpressCollection.GetItems(aIndex: Integer): TDetalleExpress;
begin
  Result := TDetalleExpress(inherited Items[aIndex]);
end;

procedure TDetalleExpressCollection.LoadFromArray(anArray: ADetalleExpress);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleExpressCollection.SaveToArray(anArray: ADetalleExpress);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleExpress(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleExpressCollection.SetItems(aIndex: Integer; const Value: TDetalleExpress);
begin
  TDetalleExpress(inherited Items[aIndex]).Assign(Value);
end;

{ TValoresTurno }

procedure TValoresTurno.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TValoresTurno;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TValoresTurno) then begin
    lSource := LibraryValesCupones_Intf.TValoresTurno(iSource);

    Self.Fecha := lSource.Fecha;
    Self.Venta := lSource.Venta;
    Self.Efectivo := lSource.Efectivo;
    Self.Credito := lSource.Credito;
    Self.ConsumoInterno := lSource.ConsumoInterno;
    Self.Calibraciones := lSource.Calibraciones;
    Self.Faltantes := lSource.Faltantes;
    Self.Sobrantes := lSource.Sobrantes;
  end;
end;

procedure TValoresTurno.ReadComplex(ASerializer: TObject);
var
  l_Calibraciones: Double;
  l_ConsumoInterno: Double;
  l_Credito: Double;
  l_Efectivo: Double;
  l_Faltantes: Double;
  l_Fecha: DateTime;
  l_Sobrantes: Double;
  l_Venta: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Venta := Self.Venta;
    try
      TROSerializer(ASerializer).ReadDouble('Venta', TypInfo.ftDouble, l_Venta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Venta']));
    end;
    Self.Venta := l_Venta;
    l_Efectivo := Self.Efectivo;
    try
      TROSerializer(ASerializer).ReadDouble('Efectivo', TypInfo.ftDouble, l_Efectivo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Efectivo']));
    end;
    Self.Efectivo := l_Efectivo;
    l_Credito := Self.Credito;
    try
      TROSerializer(ASerializer).ReadDouble('Credito', TypInfo.ftDouble, l_Credito);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Credito']));
    end;
    Self.Credito := l_Credito;
    l_ConsumoInterno := Self.ConsumoInterno;
    try
      TROSerializer(ASerializer).ReadDouble('ConsumoInterno', TypInfo.ftDouble, l_ConsumoInterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConsumoInterno']));
    end;
    Self.ConsumoInterno := l_ConsumoInterno;
    l_Calibraciones := Self.Calibraciones;
    try
      TROSerializer(ASerializer).ReadDouble('Calibraciones', TypInfo.ftDouble, l_Calibraciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Calibraciones']));
    end;
    Self.Calibraciones := l_Calibraciones;
    l_Faltantes := Self.Faltantes;
    try
      TROSerializer(ASerializer).ReadDouble('Faltantes', TypInfo.ftDouble, l_Faltantes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Faltantes']));
    end;
    Self.Faltantes := l_Faltantes;
    l_Sobrantes := Self.Sobrantes;
    try
      TROSerializer(ASerializer).ReadDouble('Sobrantes', TypInfo.ftDouble, l_Sobrantes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sobrantes']));
    end;
    Self.Sobrantes := l_Sobrantes;
  end
  else begin
    l_Calibraciones := Self.Calibraciones;
    try
      TROSerializer(ASerializer).ReadDouble('Calibraciones', TypInfo.ftDouble, l_Calibraciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Calibraciones']));
    end;
    Self.Calibraciones := l_Calibraciones;
    l_ConsumoInterno := Self.ConsumoInterno;
    try
      TROSerializer(ASerializer).ReadDouble('ConsumoInterno', TypInfo.ftDouble, l_ConsumoInterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConsumoInterno']));
    end;
    Self.ConsumoInterno := l_ConsumoInterno;
    l_Credito := Self.Credito;
    try
      TROSerializer(ASerializer).ReadDouble('Credito', TypInfo.ftDouble, l_Credito);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Credito']));
    end;
    Self.Credito := l_Credito;
    l_Efectivo := Self.Efectivo;
    try
      TROSerializer(ASerializer).ReadDouble('Efectivo', TypInfo.ftDouble, l_Efectivo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Efectivo']));
    end;
    Self.Efectivo := l_Efectivo;
    l_Faltantes := Self.Faltantes;
    try
      TROSerializer(ASerializer).ReadDouble('Faltantes', TypInfo.ftDouble, l_Faltantes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Faltantes']));
    end;
    Self.Faltantes := l_Faltantes;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Sobrantes := Self.Sobrantes;
    try
      TROSerializer(ASerializer).ReadDouble('Sobrantes', TypInfo.ftDouble, l_Sobrantes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sobrantes']));
    end;
    Self.Sobrantes := l_Sobrantes;
    l_Venta := Self.Venta;
    try
      TROSerializer(ASerializer).ReadDouble('Venta', TypInfo.ftDouble, l_Venta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Venta']));
    end;
    Self.Venta := l_Venta;
  end;
end;

procedure TValoresTurno.WriteComplex(ASerializer: TObject);
var
  l_Calibraciones: Double;
  l_ConsumoInterno: Double;
  l_Credito: Double;
  l_Efectivo: Double;
  l_Faltantes: Double;
  l_Fecha: DateTime;
  l_Sobrantes: Double;
  l_Venta: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TValoresTurno);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Venta := Self.Venta;
    TROSerializer(ASerializer).WriteDouble('Venta', TypInfo.ftDouble, l_Venta);
    l_Efectivo := Self.Efectivo;
    TROSerializer(ASerializer).WriteDouble('Efectivo', TypInfo.ftDouble, l_Efectivo);
    l_Credito := Self.Credito;
    TROSerializer(ASerializer).WriteDouble('Credito', TypInfo.ftDouble, l_Credito);
    l_ConsumoInterno := Self.ConsumoInterno;
    TROSerializer(ASerializer).WriteDouble('ConsumoInterno', TypInfo.ftDouble, l_ConsumoInterno);
    l_Calibraciones := Self.Calibraciones;
    TROSerializer(ASerializer).WriteDouble('Calibraciones', TypInfo.ftDouble, l_Calibraciones);
    l_Faltantes := Self.Faltantes;
    TROSerializer(ASerializer).WriteDouble('Faltantes', TypInfo.ftDouble, l_Faltantes);
    l_Sobrantes := Self.Sobrantes;
    TROSerializer(ASerializer).WriteDouble('Sobrantes', TypInfo.ftDouble, l_Sobrantes);
  end
  else begin
    l_Calibraciones := Self.Calibraciones;
    TROSerializer(ASerializer).WriteDouble('Calibraciones', TypInfo.ftDouble, l_Calibraciones);
    l_ConsumoInterno := Self.ConsumoInterno;
    TROSerializer(ASerializer).WriteDouble('ConsumoInterno', TypInfo.ftDouble, l_ConsumoInterno);
    l_Credito := Self.Credito;
    TROSerializer(ASerializer).WriteDouble('Credito', TypInfo.ftDouble, l_Credito);
    l_Efectivo := Self.Efectivo;
    TROSerializer(ASerializer).WriteDouble('Efectivo', TypInfo.ftDouble, l_Efectivo);
    l_Faltantes := Self.Faltantes;
    TROSerializer(ASerializer).WriteDouble('Faltantes', TypInfo.ftDouble, l_Faltantes);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Sobrantes := Self.Sobrantes;
    TROSerializer(ASerializer).WriteDouble('Sobrantes', TypInfo.ftDouble, l_Sobrantes);
    l_Venta := Self.Venta;
    TROSerializer(ASerializer).WriteDouble('Venta', TypInfo.ftDouble, l_Venta);
  end;
end;

{ TValoresTurnoCollection }
constructor TValoresTurnoCollection.Create;
begin
  inherited Create(TValoresTurno);
end;

constructor TValoresTurnoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TValoresTurnoCollection.Add: TValoresTurno;
begin
  Result := TValoresTurno(inherited Add);
end;

function TValoresTurnoCollection.GetItems(aIndex: Integer): TValoresTurno;
begin
  Result := TValoresTurno(inherited Items[aIndex]);
end;

procedure TValoresTurnoCollection.SetItems(aIndex: Integer; const Value: TValoresTurno);
begin
  TValoresTurno(inherited Items[aIndex]).Assign(Value);
end;

{ TOtroProducto }

procedure TOtroProducto.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TOtroProducto;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TOtroProducto) then begin
    lSource := LibraryValesCupones_Intf.TOtroProducto(iSource);

    Self.ID := lSource.ID;
    Self.Nombre := lSource.Nombre;
    Self.Codigo := lSource.Codigo;
    Self.Barras := lSource.Barras;
    Self.Precio := lSource.Precio;
  end;
end;

procedure TOtroProducto.ReadComplex(ASerializer: TObject);
var
  l_Barras: AnsiString;
  l_Codigo: AnsiString;
  l_ID: Integer;
  l_Nombre: AnsiString;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ID := Self.ID;
    try
      TROSerializer(ASerializer).ReadInteger('ID', TypInfo.otSLong, l_ID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ID']));
    end;
    Self.ID := l_ID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Barras := Self.Barras;
    try
      TROSerializer(ASerializer).ReadAnsiString('Barras', l_Barras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Barras']));
    end;
    Self.Barras := l_Barras;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end
  else begin
    l_Barras := Self.Barras;
    try
      TROSerializer(ASerializer).ReadAnsiString('Barras', l_Barras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Barras']));
    end;
    Self.Barras := l_Barras;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_ID := Self.ID;
    try
      TROSerializer(ASerializer).ReadInteger('ID', TypInfo.otSLong, l_ID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ID']));
    end;
    Self.ID := l_ID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end;
end;

procedure TOtroProducto.WriteComplex(ASerializer: TObject);
var
  l_Barras: AnsiString;
  l_Codigo: AnsiString;
  l_ID: Integer;
  l_Nombre: AnsiString;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TOtroProducto);
    l_ID := Self.ID;
    TROSerializer(ASerializer).WriteInteger('ID', TypInfo.otSLong, l_ID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Barras := Self.Barras;
    TROSerializer(ASerializer).WriteAnsiString('Barras', l_Barras);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end
  else begin
    l_Barras := Self.Barras;
    TROSerializer(ASerializer).WriteAnsiString('Barras', l_Barras);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_ID := Self.ID;
    TROSerializer(ASerializer).WriteInteger('ID', TypInfo.otSLong, l_ID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end;
end;

{ TOtroProductoCollection }
constructor TOtroProductoCollection.Create;
begin
  inherited Create(TOtroProducto);
end;

constructor TOtroProductoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TOtroProductoCollection.Add: TOtroProducto;
begin
  Result := TOtroProducto(inherited Add);
end;

function TOtroProductoCollection.GetItems(aIndex: Integer): TOtroProducto;
begin
  Result := TOtroProducto(inherited Items[aIndex]);
end;

procedure TOtroProductoCollection.SetItems(aIndex: Integer; const Value: TOtroProducto);
begin
  TOtroProducto(inherited Items[aIndex]).Assign(Value);
end;

{ TAgrupacion }

procedure TAgrupacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TAgrupacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TAgrupacion) then begin
    lSource := LibraryValesCupones_Intf.TAgrupacion(iSource);

    Self.AgrupacionID := lSource.AgrupacionID;
    if System.Assigned(lSource.fBombas) then
      Self.Bombas.Assign(lSource.fBombas)
    else
      SysUtils.FreeAndNil(fBombas);
  end;
end;

function TAgrupacion.GetBombas: AAgrupacionBomba;
begin
  if (fBombas = nil) then fBombas := AAgrupacionBomba.Create();
  Result := fBombas;
end;

procedure TAgrupacion.ReadComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_Bombas: AAgrupacionBomba;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
    l_Bombas := int_Bombas;
    try
      TROSerializer(ASerializer).ReadArray('Bombas', AAgrupacionBomba, l_Bombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bombas']));
    end;
    if int_Bombas <> l_Bombas then Self.Bombas.Free;
    Self.Bombas := l_Bombas;
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
    l_Bombas := int_Bombas;
    try
      TROSerializer(ASerializer).ReadArray('Bombas', AAgrupacionBomba, l_Bombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bombas']));
    end;
    if int_Bombas <> l_Bombas then Bombas.Free;
    Self.Bombas := l_Bombas;
  end;
end;

procedure TAgrupacion.WriteComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_Bombas: AAgrupacionBomba;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TAgrupacion);
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    l_Bombas := int_Bombas;
    TROSerializer(ASerializer).WriteArray('Bombas', l_Bombas, AAgrupacionBomba);
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    l_Bombas := int_Bombas;
    TROSerializer(ASerializer).WriteArray('Bombas', l_Bombas, AAgrupacionBomba);
  end;
end;

{ TAgrupacionCollection }
constructor TAgrupacionCollection.Create;
begin
  inherited Create(TAgrupacion);
end;

constructor TAgrupacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TAgrupacionCollection.Add: TAgrupacion;
begin
  Result := TAgrupacion(inherited Add);
end;

function TAgrupacionCollection.GetItems(aIndex: Integer): TAgrupacion;
begin
  Result := TAgrupacion(inherited Items[aIndex]);
end;

procedure TAgrupacionCollection.LoadFromArray(anArray: AAgrupacion);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TAgrupacionCollection.SaveToArray(anArray: AAgrupacion);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TAgrupacion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TAgrupacionCollection.SetItems(aIndex: Integer; const Value: TAgrupacion);
begin
  TAgrupacion(inherited Items[aIndex]).Assign(Value);
end;

{ TSagarpa }

procedure TSagarpa.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TSagarpa;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TSagarpa) then begin
    lSource := LibraryValesCupones_Intf.TSagarpa(iSource);

    Self.ClienteSagarpa := lSource.ClienteSagarpa;
    Self.ClienteSagarpaPemex := lSource.ClienteSagarpaPemex;
  end;
end;

procedure TSagarpa.ReadComplex(ASerializer: TObject);
var
  l_ClienteSagarpa: Integer;
  l_ClienteSagarpaPemex: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteSagarpa := Self.ClienteSagarpa;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteSagarpa', TypInfo.otSLong, l_ClienteSagarpa);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteSagarpa']));
    end;
    Self.ClienteSagarpa := l_ClienteSagarpa;
    l_ClienteSagarpaPemex := Self.ClienteSagarpaPemex;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteSagarpaPemex', TypInfo.otSLong, l_ClienteSagarpaPemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteSagarpaPemex']));
    end;
    Self.ClienteSagarpaPemex := l_ClienteSagarpaPemex;
  end
  else begin
    l_ClienteSagarpa := Self.ClienteSagarpa;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteSagarpa', TypInfo.otSLong, l_ClienteSagarpa);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteSagarpa']));
    end;
    Self.ClienteSagarpa := l_ClienteSagarpa;
    l_ClienteSagarpaPemex := Self.ClienteSagarpaPemex;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteSagarpaPemex', TypInfo.otSLong, l_ClienteSagarpaPemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteSagarpaPemex']));
    end;
    Self.ClienteSagarpaPemex := l_ClienteSagarpaPemex;
  end;
end;

procedure TSagarpa.WriteComplex(ASerializer: TObject);
var
  l_ClienteSagarpa: Integer;
  l_ClienteSagarpaPemex: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TSagarpa);
    l_ClienteSagarpa := Self.ClienteSagarpa;
    TROSerializer(ASerializer).WriteInteger('ClienteSagarpa', TypInfo.otSLong, l_ClienteSagarpa);
    l_ClienteSagarpaPemex := Self.ClienteSagarpaPemex;
    TROSerializer(ASerializer).WriteInteger('ClienteSagarpaPemex', TypInfo.otSLong, l_ClienteSagarpaPemex);
  end
  else begin
    l_ClienteSagarpa := Self.ClienteSagarpa;
    TROSerializer(ASerializer).WriteInteger('ClienteSagarpa', TypInfo.otSLong, l_ClienteSagarpa);
    l_ClienteSagarpaPemex := Self.ClienteSagarpaPemex;
    TROSerializer(ASerializer).WriteInteger('ClienteSagarpaPemex', TypInfo.otSLong, l_ClienteSagarpaPemex);
  end;
end;

{ TSagarpaCollection }
constructor TSagarpaCollection.Create;
begin
  inherited Create(TSagarpa);
end;

constructor TSagarpaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TSagarpaCollection.Add: TSagarpa;
begin
  Result := TSagarpa(inherited Add);
end;

function TSagarpaCollection.GetItems(aIndex: Integer): TSagarpa;
begin
  Result := TSagarpa(inherited Items[aIndex]);
end;

procedure TSagarpaCollection.SetItems(aIndex: Integer; const Value: TSagarpa);
begin
  TSagarpa(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosPuntos }

procedure TDatosPuntos.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosPuntos;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosPuntos) then begin
    lSource := LibraryValesCupones_Intf.TDatosPuntos(iSource);

    Self.ClienteID := lSource.ClienteID;
    Self.ApellidoPaterno := lSource.ApellidoPaterno;
    Self.ApellidoMaterno := lSource.ApellidoMaterno;
    Self.Nombres := lSource.Nombres;
    Self.email := lSource.email;
    Self.Nacimiento := lSource.Nacimiento;
    Self.Referencia := lSource.Referencia;
    Self.Telefonos := lSource.Telefonos;
    Self.FacturaExpress := lSource.FacturaExpress;
    Self.PuntosCategoriaID := lSource.PuntosCategoriaID;
    Self.Sexo := lSource.Sexo;
    Self.FechaRegistro := lSource.FechaRegistro;
    Self.UsuarioID := lSource.UsuarioID;
    Self.PuntosClubID := lSource.PuntosClubID;
  end;
end;

procedure TDatosPuntos.ReadComplex(ASerializer: TObject);
var
  l_ApellidoMaterno: AnsiString;
  l_ApellidoPaterno: AnsiString;
  l_ClienteID: Integer;
  l_email: AnsiString;
  l_FacturaExpress: Boolean;
  l_FechaRegistro: DateTime;
  l_Nacimiento: DateTime;
  l_Nombres: AnsiString;
  l_PuntosCategoriaID: Integer;
  l_PuntosClubID: Integer;
  l_Referencia: AnsiString;
  l_Sexo: Integer;
  l_Telefonos: AnsiString;
  l_UsuarioID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_ApellidoPaterno := Self.ApellidoPaterno;
    try
      TROSerializer(ASerializer).ReadAnsiString('ApellidoPaterno', l_ApellidoPaterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ApellidoPaterno']));
    end;
    Self.ApellidoPaterno := l_ApellidoPaterno;
    l_ApellidoMaterno := Self.ApellidoMaterno;
    try
      TROSerializer(ASerializer).ReadAnsiString('ApellidoMaterno', l_ApellidoMaterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ApellidoMaterno']));
    end;
    Self.ApellidoMaterno := l_ApellidoMaterno;
    l_Nombres := Self.Nombres;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombres', l_Nombres);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombres']));
    end;
    Self.Nombres := l_Nombres;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
    l_Nacimiento := Self.Nacimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('Nacimiento', l_Nacimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nacimiento']));
    end;
    Self.Nacimiento := l_Nacimiento;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Telefonos := Self.Telefonos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefonos', l_Telefonos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefonos']));
    end;
    Self.Telefonos := l_Telefonos;
    l_FacturaExpress := Self.FacturaExpress;
    try
      TROSerializer(ASerializer).ReadEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaExpress']));
    end;
    Self.FacturaExpress := l_FacturaExpress;
    l_PuntosCategoriaID := Self.PuntosCategoriaID;
    try
      TROSerializer(ASerializer).ReadInteger('PuntosCategoriaID', TypInfo.otSLong, l_PuntosCategoriaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosCategoriaID']));
    end;
    Self.PuntosCategoriaID := l_PuntosCategoriaID;
    l_Sexo := Self.Sexo;
    try
      TROSerializer(ASerializer).ReadInteger('Sexo', TypInfo.otSLong, l_Sexo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sexo']));
    end;
    Self.Sexo := l_Sexo;
    l_FechaRegistro := Self.FechaRegistro;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaRegistro', l_FechaRegistro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaRegistro']));
    end;
    Self.FechaRegistro := l_FechaRegistro;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
    l_PuntosClubID := Self.PuntosClubID;
    try
      TROSerializer(ASerializer).ReadInteger('PuntosClubID', TypInfo.otSLong, l_PuntosClubID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosClubID']));
    end;
    Self.PuntosClubID := l_PuntosClubID;
  end
  else begin
    l_ApellidoMaterno := Self.ApellidoMaterno;
    try
      TROSerializer(ASerializer).ReadAnsiString('ApellidoMaterno', l_ApellidoMaterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ApellidoMaterno']));
    end;
    Self.ApellidoMaterno := l_ApellidoMaterno;
    l_ApellidoPaterno := Self.ApellidoPaterno;
    try
      TROSerializer(ASerializer).ReadAnsiString('ApellidoPaterno', l_ApellidoPaterno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ApellidoPaterno']));
    end;
    Self.ApellidoPaterno := l_ApellidoPaterno;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
    l_FacturaExpress := Self.FacturaExpress;
    try
      TROSerializer(ASerializer).ReadEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaExpress']));
    end;
    Self.FacturaExpress := l_FacturaExpress;
    l_FechaRegistro := Self.FechaRegistro;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaRegistro', l_FechaRegistro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaRegistro']));
    end;
    Self.FechaRegistro := l_FechaRegistro;
    l_Nacimiento := Self.Nacimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('Nacimiento', l_Nacimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nacimiento']));
    end;
    Self.Nacimiento := l_Nacimiento;
    l_Nombres := Self.Nombres;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombres', l_Nombres);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombres']));
    end;
    Self.Nombres := l_Nombres;
    l_PuntosCategoriaID := Self.PuntosCategoriaID;
    try
      TROSerializer(ASerializer).ReadInteger('PuntosCategoriaID', TypInfo.otSLong, l_PuntosCategoriaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosCategoriaID']));
    end;
    Self.PuntosCategoriaID := l_PuntosCategoriaID;
    l_PuntosClubID := Self.PuntosClubID;
    try
      TROSerializer(ASerializer).ReadInteger('PuntosClubID', TypInfo.otSLong, l_PuntosClubID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosClubID']));
    end;
    Self.PuntosClubID := l_PuntosClubID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Sexo := Self.Sexo;
    try
      TROSerializer(ASerializer).ReadInteger('Sexo', TypInfo.otSLong, l_Sexo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sexo']));
    end;
    Self.Sexo := l_Sexo;
    l_Telefonos := Self.Telefonos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefonos', l_Telefonos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefonos']));
    end;
    Self.Telefonos := l_Telefonos;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
  end;
end;

procedure TDatosPuntos.WriteComplex(ASerializer: TObject);
var
  l_ApellidoMaterno: AnsiString;
  l_ApellidoPaterno: AnsiString;
  l_ClienteID: Integer;
  l_email: AnsiString;
  l_FacturaExpress: Boolean;
  l_FechaRegistro: DateTime;
  l_Nacimiento: DateTime;
  l_Nombres: AnsiString;
  l_PuntosCategoriaID: Integer;
  l_PuntosClubID: Integer;
  l_Referencia: AnsiString;
  l_Sexo: Integer;
  l_Telefonos: AnsiString;
  l_UsuarioID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosPuntos);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_ApellidoPaterno := Self.ApellidoPaterno;
    TROSerializer(ASerializer).WriteAnsiString('ApellidoPaterno', l_ApellidoPaterno);
    l_ApellidoMaterno := Self.ApellidoMaterno;
    TROSerializer(ASerializer).WriteAnsiString('ApellidoMaterno', l_ApellidoMaterno);
    l_Nombres := Self.Nombres;
    TROSerializer(ASerializer).WriteAnsiString('Nombres', l_Nombres);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
    l_Nacimiento := Self.Nacimiento;
    TROSerializer(ASerializer).WriteDateTime('Nacimiento', l_Nacimiento);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Telefonos := Self.Telefonos;
    TROSerializer(ASerializer).WriteAnsiString('Telefonos', l_Telefonos);
    l_FacturaExpress := Self.FacturaExpress;
    TROSerializer(ASerializer).WriteEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    l_PuntosCategoriaID := Self.PuntosCategoriaID;
    TROSerializer(ASerializer).WriteInteger('PuntosCategoriaID', TypInfo.otSLong, l_PuntosCategoriaID);
    l_Sexo := Self.Sexo;
    TROSerializer(ASerializer).WriteInteger('Sexo', TypInfo.otSLong, l_Sexo);
    l_FechaRegistro := Self.FechaRegistro;
    TROSerializer(ASerializer).WriteDateTime('FechaRegistro', l_FechaRegistro);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    l_PuntosClubID := Self.PuntosClubID;
    TROSerializer(ASerializer).WriteInteger('PuntosClubID', TypInfo.otSLong, l_PuntosClubID);
  end
  else begin
    l_ApellidoMaterno := Self.ApellidoMaterno;
    TROSerializer(ASerializer).WriteAnsiString('ApellidoMaterno', l_ApellidoMaterno);
    l_ApellidoPaterno := Self.ApellidoPaterno;
    TROSerializer(ASerializer).WriteAnsiString('ApellidoPaterno', l_ApellidoPaterno);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
    l_FacturaExpress := Self.FacturaExpress;
    TROSerializer(ASerializer).WriteEnumerated('FacturaExpress',System.TypeInfo(boolean), l_FacturaExpress);
    l_FechaRegistro := Self.FechaRegistro;
    TROSerializer(ASerializer).WriteDateTime('FechaRegistro', l_FechaRegistro);
    l_Nacimiento := Self.Nacimiento;
    TROSerializer(ASerializer).WriteDateTime('Nacimiento', l_Nacimiento);
    l_Nombres := Self.Nombres;
    TROSerializer(ASerializer).WriteAnsiString('Nombres', l_Nombres);
    l_PuntosCategoriaID := Self.PuntosCategoriaID;
    TROSerializer(ASerializer).WriteInteger('PuntosCategoriaID', TypInfo.otSLong, l_PuntosCategoriaID);
    l_PuntosClubID := Self.PuntosClubID;
    TROSerializer(ASerializer).WriteInteger('PuntosClubID', TypInfo.otSLong, l_PuntosClubID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Sexo := Self.Sexo;
    TROSerializer(ASerializer).WriteInteger('Sexo', TypInfo.otSLong, l_Sexo);
    l_Telefonos := Self.Telefonos;
    TROSerializer(ASerializer).WriteAnsiString('Telefonos', l_Telefonos);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
  end;
end;

{ TDatosPuntosCollection }
constructor TDatosPuntosCollection.Create;
begin
  inherited Create(TDatosPuntos);
end;

constructor TDatosPuntosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosPuntosCollection.Add: TDatosPuntos;
begin
  Result := TDatosPuntos(inherited Add);
end;

function TDatosPuntosCollection.GetItems(aIndex: Integer): TDatosPuntos;
begin
  Result := TDatosPuntos(inherited Items[aIndex]);
end;

procedure TDatosPuntosCollection.SetItems(aIndex: Integer; const Value: TDatosPuntos);
begin
  TDatosPuntos(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosCelular }

procedure TDatosCelular.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosCelular;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosCelular) then begin
    lSource := LibraryValesCupones_Intf.TDatosCelular(iSource);

    Self.Telefono := lSource.Telefono;
    Self.Compania := lSource.Compania;
    Self.Descripcion := lSource.Descripcion;
  end;
end;

procedure TDatosCelular.ReadComplex(ASerializer: TObject);
var
  l_Compania: Integer;
  l_Descripcion: AnsiString;
  l_Telefono: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Telefono := Self.Telefono;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefono', l_Telefono);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefono']));
    end;
    Self.Telefono := l_Telefono;
    l_Compania := Self.Compania;
    try
      TROSerializer(ASerializer).ReadInteger('Compania', TypInfo.otSLong, l_Compania);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Compania']));
    end;
    Self.Compania := l_Compania;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
  end
  else begin
    l_Compania := Self.Compania;
    try
      TROSerializer(ASerializer).ReadInteger('Compania', TypInfo.otSLong, l_Compania);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Compania']));
    end;
    Self.Compania := l_Compania;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_Telefono := Self.Telefono;
    try
      TROSerializer(ASerializer).ReadAnsiString('Telefono', l_Telefono);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telefono']));
    end;
    Self.Telefono := l_Telefono;
  end;
end;

procedure TDatosCelular.WriteComplex(ASerializer: TObject);
var
  l_Compania: Integer;
  l_Descripcion: AnsiString;
  l_Telefono: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosCelular);
    l_Telefono := Self.Telefono;
    TROSerializer(ASerializer).WriteAnsiString('Telefono', l_Telefono);
    l_Compania := Self.Compania;
    TROSerializer(ASerializer).WriteInteger('Compania', TypInfo.otSLong, l_Compania);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
  end
  else begin
    l_Compania := Self.Compania;
    TROSerializer(ASerializer).WriteInteger('Compania', TypInfo.otSLong, l_Compania);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_Telefono := Self.Telefono;
    TROSerializer(ASerializer).WriteAnsiString('Telefono', l_Telefono);
  end;
end;

{ TDatosCelularCollection }
constructor TDatosCelularCollection.Create;
begin
  inherited Create(TDatosCelular);
end;

constructor TDatosCelularCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosCelularCollection.Add: TDatosCelular;
begin
  Result := TDatosCelular(inherited Add);
end;

function TDatosCelularCollection.GetItems(aIndex: Integer): TDatosCelular;
begin
  Result := TDatosCelular(inherited Items[aIndex]);
end;

procedure TDatosCelularCollection.LoadFromArray(anArray: ADatosCelular);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDatosCelularCollection.SaveToArray(anArray: ADatosCelular);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDatosCelular(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDatosCelularCollection.SetItems(aIndex: Integer; const Value: TDatosCelular);
begin
  TDatosCelular(inherited Items[aIndex]).Assign(Value);
end;

{ TEntregaPremio }

procedure TEntregaPremio.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TEntregaPremio;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TEntregaPremio) then begin
    lSource := LibraryValesCupones_Intf.TEntregaPremio(iSource);

    Self.ClienteID := lSource.ClienteID;
    Self.Cliente := lSource.Cliente;
    Self.Premio := lSource.Premio;
    Self.Cantidad := lSource.Cantidad;
    Self.Puntos := lSource.Puntos;
    Self.Saldo := lSource.Saldo;
    Self.Mensaje := lSource.Mensaje;
    Self.Valido := lSource.Valido;
  end;
end;

procedure TEntregaPremio.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Integer;
  l_Cliente: AnsiString;
  l_ClienteID: Integer;
  l_Mensaje: AnsiString;
  l_Premio: AnsiString;
  l_Puntos: Double;
  l_Saldo: Double;
  l_Valido: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Cliente := Self.Cliente;
    try
      TROSerializer(ASerializer).ReadAnsiString('Cliente', l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    Self.Cliente := l_Cliente;
    l_Premio := Self.Premio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Premio', l_Premio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Premio']));
    end;
    Self.Premio := l_Premio;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Puntos := Self.Puntos;
    try
      TROSerializer(ASerializer).ReadDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puntos']));
    end;
    Self.Puntos := l_Puntos;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Cliente := Self.Cliente;
    try
      TROSerializer(ASerializer).ReadAnsiString('Cliente', l_Cliente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cliente']));
    end;
    Self.Cliente := l_Cliente;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Premio := Self.Premio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Premio', l_Premio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Premio']));
    end;
    Self.Premio := l_Premio;
    l_Puntos := Self.Puntos;
    try
      TROSerializer(ASerializer).ReadDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puntos']));
    end;
    Self.Puntos := l_Puntos;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
  end;
end;

procedure TEntregaPremio.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Integer;
  l_Cliente: AnsiString;
  l_ClienteID: Integer;
  l_Mensaje: AnsiString;
  l_Premio: AnsiString;
  l_Puntos: Double;
  l_Saldo: Double;
  l_Valido: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TEntregaPremio);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Cliente := Self.Cliente;
    TROSerializer(ASerializer).WriteAnsiString('Cliente', l_Cliente);
    l_Premio := Self.Premio;
    TROSerializer(ASerializer).WriteAnsiString('Premio', l_Premio);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    l_Puntos := Self.Puntos;
    TROSerializer(ASerializer).WriteDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    l_Cliente := Self.Cliente;
    TROSerializer(ASerializer).WriteAnsiString('Cliente', l_Cliente);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Premio := Self.Premio;
    TROSerializer(ASerializer).WriteAnsiString('Premio', l_Premio);
    l_Puntos := Self.Puntos;
    TROSerializer(ASerializer).WriteDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
  end;
end;

{ TEntregaPremioCollection }
constructor TEntregaPremioCollection.Create;
begin
  inherited Create(TEntregaPremio);
end;

constructor TEntregaPremioCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TEntregaPremioCollection.Add: TEntregaPremio;
begin
  Result := TEntregaPremio(inherited Add);
end;

function TEntregaPremioCollection.GetItems(aIndex: Integer): TEntregaPremio;
begin
  Result := TEntregaPremio(inherited Items[aIndex]);
end;

procedure TEntregaPremioCollection.SetItems(aIndex: Integer; const Value: TEntregaPremio);
begin
  TEntregaPremio(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosPremio }

procedure TDatosPremio.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosPremio;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosPremio) then begin
    lSource := LibraryValesCupones_Intf.TDatosPremio(iSource);

    Self.PremioID := lSource.PremioID;
    Self.Nombre := lSource.Nombre;
    Self.Puntos := lSource.Puntos;
  end;
end;

procedure TDatosPremio.ReadComplex(ASerializer: TObject);
var
  l_Nombre: AnsiString;
  l_PremioID: Integer;
  l_Puntos: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_PremioID := Self.PremioID;
    try
      TROSerializer(ASerializer).ReadInteger('PremioID', TypInfo.otSLong, l_PremioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PremioID']));
    end;
    Self.PremioID := l_PremioID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Puntos := Self.Puntos;
    try
      TROSerializer(ASerializer).ReadDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puntos']));
    end;
    Self.Puntos := l_Puntos;
  end
  else begin
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_PremioID := Self.PremioID;
    try
      TROSerializer(ASerializer).ReadInteger('PremioID', TypInfo.otSLong, l_PremioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PremioID']));
    end;
    Self.PremioID := l_PremioID;
    l_Puntos := Self.Puntos;
    try
      TROSerializer(ASerializer).ReadDouble('Puntos', TypInfo.ftDouble, l_Puntos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puntos']));
    end;
    Self.Puntos := l_Puntos;
  end;
end;

procedure TDatosPremio.WriteComplex(ASerializer: TObject);
var
  l_Nombre: AnsiString;
  l_PremioID: Integer;
  l_Puntos: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosPremio);
    l_PremioID := Self.PremioID;
    TROSerializer(ASerializer).WriteInteger('PremioID', TypInfo.otSLong, l_PremioID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Puntos := Self.Puntos;
    TROSerializer(ASerializer).WriteDouble('Puntos', TypInfo.ftDouble, l_Puntos);
  end
  else begin
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_PremioID := Self.PremioID;
    TROSerializer(ASerializer).WriteInteger('PremioID', TypInfo.otSLong, l_PremioID);
    l_Puntos := Self.Puntos;
    TROSerializer(ASerializer).WriteDouble('Puntos', TypInfo.ftDouble, l_Puntos);
  end;
end;

{ TDatosPremioCollection }
constructor TDatosPremioCollection.Create;
begin
  inherited Create(TDatosPremio);
end;

constructor TDatosPremioCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosPremioCollection.Add: TDatosPremio;
begin
  Result := TDatosPremio(inherited Add);
end;

function TDatosPremioCollection.GetItems(aIndex: Integer): TDatosPremio;
begin
  Result := TDatosPremio(inherited Items[aIndex]);
end;

procedure TDatosPremioCollection.SetItems(aIndex: Integer; const Value: TDatosPremio);
begin
  TDatosPremio(inherited Items[aIndex]).Assign(Value);
end;

{ TConfirmaEnvioA }

procedure TConfirmaEnvioA.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TConfirmaEnvioA;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TConfirmaEnvioA) then begin
    lSource := LibraryValesCupones_Intf.TConfirmaEnvioA(iSource);

    if System.Assigned(lSource.fConsumo) then
      Self.Consumo.Assign(lSource.fConsumo)
    else
      SysUtils.FreeAndNil(fConsumo);
    Self.Host := lSource.Host;
    Self.Fecha := lSource.Fecha;
  end;
end;

function TConfirmaEnvioA.GetConsumo: TConsumoExpress;
begin
  if (fConsumo = nil) then fConsumo := TConsumoExpress.Create();
  Result := fConsumo;
end;

procedure TConfirmaEnvioA.ReadComplex(ASerializer: TObject);
var
  l_Consumo: TConsumoExpress;
  l_Fecha: DateTime;
  l_Host: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Consumo := int_Consumo;
    try
      TROSerializer(ASerializer).ReadStruct('Consumo', TConsumoExpress, l_Consumo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Consumo']));
    end;
    if int_Consumo <> l_Consumo then Self.Consumo.Free;
    Self.Consumo := l_Consumo;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
  end
  else begin
    l_Consumo := int_Consumo;
    try
      TROSerializer(ASerializer).ReadStruct('Consumo', TConsumoExpress, l_Consumo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Consumo']));
    end;
    if int_Consumo <> l_Consumo then Consumo.Free;
    Self.Consumo := l_Consumo;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
  end;
end;

procedure TConfirmaEnvioA.WriteComplex(ASerializer: TObject);
var
  l_Consumo: TConsumoExpress;
  l_Fecha: DateTime;
  l_Host: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TConfirmaEnvioA);
    l_Consumo := int_Consumo;
    TROSerializer(ASerializer).WriteStruct('Consumo', l_Consumo, TConsumoExpress);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
  end
  else begin
    l_Consumo := int_Consumo;
    TROSerializer(ASerializer).WriteStruct('Consumo', l_Consumo, TConsumoExpress);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
  end;
end;

{ TConfirmaEnvioACollection }
constructor TConfirmaEnvioACollection.Create;
begin
  inherited Create(TConfirmaEnvioA);
end;

constructor TConfirmaEnvioACollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TConfirmaEnvioACollection.Add: TConfirmaEnvioA;
begin
  Result := TConfirmaEnvioA(inherited Add);
end;

function TConfirmaEnvioACollection.GetItems(aIndex: Integer): TConfirmaEnvioA;
begin
  Result := TConfirmaEnvioA(inherited Items[aIndex]);
end;

procedure TConfirmaEnvioACollection.SetItems(aIndex: Integer; const Value: TConfirmaEnvioA);
begin
  TConfirmaEnvioA(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosCerrarLiquidacion }

procedure TDatosCerrarLiquidacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosCerrarLiquidacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosCerrarLiquidacion) then begin
    lSource := LibraryValesCupones_Intf.TDatosCerrarLiquidacion(iSource);

    Self.LiquidacionID := lSource.LiquidacionID;
    Self.VentasTotales := lSource.VentasTotales;
    Self.Entregado := lSource.Entregado;
    Self.Diferencia := lSource.Diferencia;
    if System.Assigned(lSource.fDespachadores) then
      Self.Despachadores.Assign(lSource.fDespachadores)
    else
      SysUtils.FreeAndNil(fDespachadores);
    if System.Assigned(lSource.fDetalle) then
      Self.Detalle.Assign(lSource.fDetalle)
    else
      SysUtils.FreeAndNil(fDetalle);
    if System.Assigned(lSource.fTipoValor) then
      Self.TipoValor.Assign(lSource.fTipoValor)
    else
      SysUtils.FreeAndNil(fTipoValor);
    Self.EstacionID := lSource.EstacionID;
    Self.TurnoID := lSource.TurnoID;
  end;
end;

function TDatosCerrarLiquidacion.GetDespachadores: ADespachadorLiquidacion;
begin
  if (fDespachadores = nil) then fDespachadores := ADespachadorLiquidacion.Create();
  Result := fDespachadores;
end;

function TDatosCerrarLiquidacion.GetDetalle: ADetalleLiquidacion;
begin
  if (fDetalle = nil) then fDetalle := ADetalleLiquidacion.Create();
  Result := fDetalle;
end;

function TDatosCerrarLiquidacion.GetTipoValor: ATipoValor;
begin
  if (fTipoValor = nil) then fTipoValor := ATipoValor.Create();
  Result := fTipoValor;
end;

procedure TDatosCerrarLiquidacion.ReadComplex(ASerializer: TObject);
var
  l_Despachadores: ADespachadorLiquidacion;
  l_Detalle: ADetalleLiquidacion;
  l_Diferencia: Double;
  l_Entregado: Double;
  l_EstacionID: Integer;
  l_LiquidacionID: Integer;
  l_TipoValor: ATipoValor;
  l_TurnoID: Integer;
  l_VentasTotales: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_VentasTotales := Self.VentasTotales;
    try
      TROSerializer(ASerializer).ReadDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTotales']));
    end;
    Self.VentasTotales := l_VentasTotales;
    l_Entregado := Self.Entregado;
    try
      TROSerializer(ASerializer).ReadDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Entregado']));
    end;
    Self.Entregado := l_Entregado;
    l_Diferencia := Self.Diferencia;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencia']));
    end;
    Self.Diferencia := l_Diferencia;
    l_Despachadores := int_Despachadores;
    try
      TROSerializer(ASerializer).ReadArray('Despachadores', ADespachadorLiquidacion, l_Despachadores);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Despachadores']));
    end;
    if int_Despachadores <> l_Despachadores then Self.Despachadores.Free;
    Self.Despachadores := l_Despachadores;
    l_Detalle := int_Detalle;
    try
      TROSerializer(ASerializer).ReadArray('Detalle', ADetalleLiquidacion, l_Detalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalle']));
    end;
    if int_Detalle <> l_Detalle then Self.Detalle.Free;
    Self.Detalle := l_Detalle;
    l_TipoValor := int_TipoValor;
    try
      TROSerializer(ASerializer).ReadArray('TipoValor', ATipoValor, l_TipoValor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValor']));
    end;
    if int_TipoValor <> l_TipoValor then Self.TipoValor.Free;
    Self.TipoValor := l_TipoValor;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
  end
  else begin
    l_Despachadores := int_Despachadores;
    try
      TROSerializer(ASerializer).ReadArray('Despachadores', ADespachadorLiquidacion, l_Despachadores);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Despachadores']));
    end;
    if int_Despachadores <> l_Despachadores then Despachadores.Free;
    Self.Despachadores := l_Despachadores;
    l_Detalle := int_Detalle;
    try
      TROSerializer(ASerializer).ReadArray('Detalle', ADetalleLiquidacion, l_Detalle);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Detalle']));
    end;
    if int_Detalle <> l_Detalle then Detalle.Free;
    Self.Detalle := l_Detalle;
    l_Diferencia := Self.Diferencia;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencia']));
    end;
    Self.Diferencia := l_Diferencia;
    l_Entregado := Self.Entregado;
    try
      TROSerializer(ASerializer).ReadDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Entregado']));
    end;
    Self.Entregado := l_Entregado;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_LiquidacionID := Self.LiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LiquidacionID']));
    end;
    Self.LiquidacionID := l_LiquidacionID;
    l_TipoValor := int_TipoValor;
    try
      TROSerializer(ASerializer).ReadArray('TipoValor', ATipoValor, l_TipoValor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValor']));
    end;
    if int_TipoValor <> l_TipoValor then TipoValor.Free;
    Self.TipoValor := l_TipoValor;
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
    l_VentasTotales := Self.VentasTotales;
    try
      TROSerializer(ASerializer).ReadDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTotales']));
    end;
    Self.VentasTotales := l_VentasTotales;
  end;
end;

procedure TDatosCerrarLiquidacion.WriteComplex(ASerializer: TObject);
var
  l_Despachadores: ADespachadorLiquidacion;
  l_Detalle: ADetalleLiquidacion;
  l_Diferencia: Double;
  l_Entregado: Double;
  l_EstacionID: Integer;
  l_LiquidacionID: Integer;
  l_TipoValor: ATipoValor;
  l_TurnoID: Integer;
  l_VentasTotales: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosCerrarLiquidacion);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_VentasTotales := Self.VentasTotales;
    TROSerializer(ASerializer).WriteDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
    l_Entregado := Self.Entregado;
    TROSerializer(ASerializer).WriteDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    l_Diferencia := Self.Diferencia;
    TROSerializer(ASerializer).WriteDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    l_Despachadores := int_Despachadores;
    TROSerializer(ASerializer).WriteArray('Despachadores', l_Despachadores, ADespachadorLiquidacion);
    l_Detalle := int_Detalle;
    TROSerializer(ASerializer).WriteArray('Detalle', l_Detalle, ADetalleLiquidacion);
    l_TipoValor := int_TipoValor;
    TROSerializer(ASerializer).WriteArray('TipoValor', l_TipoValor, ATipoValor);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
  end
  else begin
    l_Despachadores := int_Despachadores;
    TROSerializer(ASerializer).WriteArray('Despachadores', l_Despachadores, ADespachadorLiquidacion);
    l_Detalle := int_Detalle;
    TROSerializer(ASerializer).WriteArray('Detalle', l_Detalle, ADetalleLiquidacion);
    l_Diferencia := Self.Diferencia;
    TROSerializer(ASerializer).WriteDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    l_Entregado := Self.Entregado;
    TROSerializer(ASerializer).WriteDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_LiquidacionID := Self.LiquidacionID;
    TROSerializer(ASerializer).WriteInteger('LiquidacionID', TypInfo.otSLong, l_LiquidacionID);
    l_TipoValor := int_TipoValor;
    TROSerializer(ASerializer).WriteArray('TipoValor', l_TipoValor, ATipoValor);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    l_VentasTotales := Self.VentasTotales;
    TROSerializer(ASerializer).WriteDouble('VentasTotales', TypInfo.ftDouble, l_VentasTotales);
  end;
end;

{ TDatosCerrarLiquidacionCollection }
constructor TDatosCerrarLiquidacionCollection.Create;
begin
  inherited Create(TDatosCerrarLiquidacion);
end;

constructor TDatosCerrarLiquidacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosCerrarLiquidacionCollection.Add: TDatosCerrarLiquidacion;
begin
  Result := TDatosCerrarLiquidacion(inherited Add);
end;

function TDatosCerrarLiquidacionCollection.GetItems(aIndex: Integer): TDatosCerrarLiquidacion;
begin
  Result := TDatosCerrarLiquidacion(inherited Items[aIndex]);
end;

procedure TDatosCerrarLiquidacionCollection.SetItems(aIndex: Integer; const Value: TDatosCerrarLiquidacion);
begin
  TDatosCerrarLiquidacion(inherited Items[aIndex]).Assign(Value);
end;

{ TDespachadorLiquidacion }

procedure TDespachadorLiquidacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDespachadorLiquidacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDespachadorLiquidacion) then begin
    lSource := LibraryValesCupones_Intf.TDespachadorLiquidacion(iSource);

    Self.DespachadorLiquidacionID := lSource.DespachadorLiquidacionID;
    Self.DespachadorID := lSource.DespachadorID;
    Self.Importe := lSource.Importe;
    Self.Entregado := lSource.Entregado;
    Self.Diferencia := lSource.Diferencia;
    Self.AgrupacionID := lSource.AgrupacionID;
  end;
end;

procedure TDespachadorLiquidacion.ReadComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_DespachadorID: Integer;
  l_DespachadorLiquidacionID: Integer;
  l_Diferencia: Double;
  l_Entregado: Double;
  l_Importe: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorLiquidacionID']));
    end;
    Self.DespachadorLiquidacionID := l_DespachadorLiquidacionID;
    l_DespachadorID := Self.DespachadorID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorID']));
    end;
    Self.DespachadorID := l_DespachadorID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Entregado := Self.Entregado;
    try
      TROSerializer(ASerializer).ReadDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Entregado']));
    end;
    Self.Entregado := l_Entregado;
    l_Diferencia := Self.Diferencia;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencia']));
    end;
    Self.Diferencia := l_Diferencia;
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AgrupacionID']));
    end;
    Self.AgrupacionID := l_AgrupacionID;
    l_DespachadorID := Self.DespachadorID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorID']));
    end;
    Self.DespachadorID := l_DespachadorID;
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorLiquidacionID']));
    end;
    Self.DespachadorLiquidacionID := l_DespachadorLiquidacionID;
    l_Diferencia := Self.Diferencia;
    try
      TROSerializer(ASerializer).ReadDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diferencia']));
    end;
    Self.Diferencia := l_Diferencia;
    l_Entregado := Self.Entregado;
    try
      TROSerializer(ASerializer).ReadDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Entregado']));
    end;
    Self.Entregado := l_Entregado;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
  end;
end;

procedure TDespachadorLiquidacion.WriteComplex(ASerializer: TObject);
var
  l_AgrupacionID: Integer;
  l_DespachadorID: Integer;
  l_DespachadorLiquidacionID: Integer;
  l_Diferencia: Double;
  l_Entregado: Double;
  l_Importe: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDespachadorLiquidacion);
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    TROSerializer(ASerializer).WriteInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    l_DespachadorID := Self.DespachadorID;
    TROSerializer(ASerializer).WriteInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Entregado := Self.Entregado;
    TROSerializer(ASerializer).WriteDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    l_Diferencia := Self.Diferencia;
    TROSerializer(ASerializer).WriteDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
  end
  else begin
    l_AgrupacionID := Self.AgrupacionID;
    TROSerializer(ASerializer).WriteInteger('AgrupacionID', TypInfo.otSLong, l_AgrupacionID);
    l_DespachadorID := Self.DespachadorID;
    TROSerializer(ASerializer).WriteInteger('DespachadorID', TypInfo.otSLong, l_DespachadorID);
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    TROSerializer(ASerializer).WriteInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    l_Diferencia := Self.Diferencia;
    TROSerializer(ASerializer).WriteDouble('Diferencia', TypInfo.ftDouble, l_Diferencia);
    l_Entregado := Self.Entregado;
    TROSerializer(ASerializer).WriteDouble('Entregado', TypInfo.ftDouble, l_Entregado);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
  end;
end;

{ TDespachadorLiquidacionCollection }
constructor TDespachadorLiquidacionCollection.Create;
begin
  inherited Create(TDespachadorLiquidacion);
end;

constructor TDespachadorLiquidacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDespachadorLiquidacionCollection.Add: TDespachadorLiquidacion;
begin
  Result := TDespachadorLiquidacion(inherited Add);
end;

function TDespachadorLiquidacionCollection.GetItems(aIndex: Integer): TDespachadorLiquidacion;
begin
  Result := TDespachadorLiquidacion(inherited Items[aIndex]);
end;

procedure TDespachadorLiquidacionCollection.LoadFromArray(anArray: ADespachadorLiquidacion);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDespachadorLiquidacionCollection.SaveToArray(anArray: ADespachadorLiquidacion);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDespachadorLiquidacion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDespachadorLiquidacionCollection.SetItems(aIndex: Integer; const Value: TDespachadorLiquidacion);
begin
  TDespachadorLiquidacion(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleLiquidacion }

procedure TDetalleLiquidacion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleLiquidacion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleLiquidacion) then begin
    lSource := LibraryValesCupones_Intf.TDetalleLiquidacion(iSource);

    Self.Cantidad := lSource.Cantidad;
    Self.Importe := lSource.Importe;
    Self.Referencia := lSource.Referencia;
    Self.Ticket := lSource.Ticket;
    Self.Serie := lSource.Serie;
    Self.CuponID := lSource.CuponID;
    Self.SalidaID := lSource.SalidaID;
    Self.ClienteID := lSource.ClienteID;
    Self.BancoID := lSource.BancoID;
    Self.ProductoID := lSource.ProductoID;
    Self.AuxiliarID := lSource.AuxiliarID;
    Self.DespachadorLiquidacionID := lSource.DespachadorLiquidacionID;
    Self.TipoValorID := lSource.TipoValorID;
  end;
end;

procedure TDetalleLiquidacion.ReadComplex(ASerializer: TObject);
var
  l_AuxiliarID: Integer;
  l_BancoID: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_CuponID: Integer;
  l_DespachadorLiquidacionID: Integer;
  l_Importe: Double;
  l_ProductoID: Integer;
  l_Referencia: AnsiString;
  l_SalidaID: Integer;
  l_Serie: AnsiString;
  l_Ticket: Integer;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_CuponID := Self.CuponID;
    try
      TROSerializer(ASerializer).ReadInteger('CuponID', TypInfo.otSLong, l_CuponID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuponID']));
    end;
    Self.CuponID := l_CuponID;
    l_SalidaID := Self.SalidaID;
    try
      TROSerializer(ASerializer).ReadInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SalidaID']));
    end;
    Self.SalidaID := l_SalidaID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_BancoID := Self.BancoID;
    try
      TROSerializer(ASerializer).ReadInteger('BancoID', TypInfo.otSLong, l_BancoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BancoID']));
    end;
    Self.BancoID := l_BancoID;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_AuxiliarID := Self.AuxiliarID;
    try
      TROSerializer(ASerializer).ReadInteger('AuxiliarID', TypInfo.otSLong, l_AuxiliarID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AuxiliarID']));
    end;
    Self.AuxiliarID := l_AuxiliarID;
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorLiquidacionID']));
    end;
    Self.DespachadorLiquidacionID := l_DespachadorLiquidacionID;
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
  end
  else begin
    l_AuxiliarID := Self.AuxiliarID;
    try
      TROSerializer(ASerializer).ReadInteger('AuxiliarID', TypInfo.otSLong, l_AuxiliarID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AuxiliarID']));
    end;
    Self.AuxiliarID := l_AuxiliarID;
    l_BancoID := Self.BancoID;
    try
      TROSerializer(ASerializer).ReadInteger('BancoID', TypInfo.otSLong, l_BancoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'BancoID']));
    end;
    Self.BancoID := l_BancoID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_CuponID := Self.CuponID;
    try
      TROSerializer(ASerializer).ReadInteger('CuponID', TypInfo.otSLong, l_CuponID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuponID']));
    end;
    Self.CuponID := l_CuponID;
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    try
      TROSerializer(ASerializer).ReadInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespachadorLiquidacionID']));
    end;
    Self.DespachadorLiquidacionID := l_DespachadorLiquidacionID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_SalidaID := Self.SalidaID;
    try
      TROSerializer(ASerializer).ReadInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SalidaID']));
    end;
    Self.SalidaID := l_SalidaID;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadInteger('Ticket', TypInfo.otSLong, l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
  end;
end;

procedure TDetalleLiquidacion.WriteComplex(ASerializer: TObject);
var
  l_AuxiliarID: Integer;
  l_BancoID: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_CuponID: Integer;
  l_DespachadorLiquidacionID: Integer;
  l_Importe: Double;
  l_ProductoID: Integer;
  l_Referencia: AnsiString;
  l_SalidaID: Integer;
  l_Serie: AnsiString;
  l_Ticket: Integer;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleLiquidacion);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_CuponID := Self.CuponID;
    TROSerializer(ASerializer).WriteInteger('CuponID', TypInfo.otSLong, l_CuponID);
    l_SalidaID := Self.SalidaID;
    TROSerializer(ASerializer).WriteInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_BancoID := Self.BancoID;
    TROSerializer(ASerializer).WriteInteger('BancoID', TypInfo.otSLong, l_BancoID);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_AuxiliarID := Self.AuxiliarID;
    TROSerializer(ASerializer).WriteInteger('AuxiliarID', TypInfo.otSLong, l_AuxiliarID);
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    TROSerializer(ASerializer).WriteInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
  end
  else begin
    l_AuxiliarID := Self.AuxiliarID;
    TROSerializer(ASerializer).WriteInteger('AuxiliarID', TypInfo.otSLong, l_AuxiliarID);
    l_BancoID := Self.BancoID;
    TROSerializer(ASerializer).WriteInteger('BancoID', TypInfo.otSLong, l_BancoID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_CuponID := Self.CuponID;
    TROSerializer(ASerializer).WriteInteger('CuponID', TypInfo.otSLong, l_CuponID);
    l_DespachadorLiquidacionID := Self.DespachadorLiquidacionID;
    TROSerializer(ASerializer).WriteInteger('DespachadorLiquidacionID', TypInfo.otSLong, l_DespachadorLiquidacionID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_SalidaID := Self.SalidaID;
    TROSerializer(ASerializer).WriteInteger('SalidaID', TypInfo.otSLong, l_SalidaID);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteInteger('Ticket', TypInfo.otSLong, l_Ticket);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
  end;
end;

{ TDetalleLiquidacionCollection }
constructor TDetalleLiquidacionCollection.Create;
begin
  inherited Create(TDetalleLiquidacion);
end;

constructor TDetalleLiquidacionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleLiquidacionCollection.Add: TDetalleLiquidacion;
begin
  Result := TDetalleLiquidacion(inherited Add);
end;

function TDetalleLiquidacionCollection.GetItems(aIndex: Integer): TDetalleLiquidacion;
begin
  Result := TDetalleLiquidacion(inherited Items[aIndex]);
end;

procedure TDetalleLiquidacionCollection.LoadFromArray(anArray: ADetalleLiquidacion);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleLiquidacionCollection.SaveToArray(anArray: ADetalleLiquidacion);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleLiquidacion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleLiquidacionCollection.SetItems(aIndex: Integer; const Value: TDetalleLiquidacion);
begin
  TDetalleLiquidacion(inherited Items[aIndex]).Assign(Value);
end;

{ TTipoValor }

procedure TTipoValor.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTipoValor;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTipoValor) then begin
    lSource := LibraryValesCupones_Intf.TTipoValor(iSource);

    Self.TipoValorID := lSource.TipoValorID;
    Self.Nombre := lSource.Nombre;
    Self.Grupo := lSource.Grupo;
  end;
end;

procedure TTipoValor.ReadComplex(ASerializer: TObject);
var
  l_Grupo: AnsiString;
  l_Nombre: AnsiString;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Grupo', l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
  end
  else begin
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Grupo', l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_TipoValorID := Self.TipoValorID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorID']));
    end;
    Self.TipoValorID := l_TipoValorID;
  end;
end;

procedure TTipoValor.WriteComplex(ASerializer: TObject);
var
  l_Grupo: AnsiString;
  l_Nombre: AnsiString;
  l_TipoValorID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTipoValor);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteAnsiString('Grupo', l_Grupo);
  end
  else begin
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteAnsiString('Grupo', l_Grupo);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_TipoValorID := Self.TipoValorID;
    TROSerializer(ASerializer).WriteInteger('TipoValorID', TypInfo.otSLong, l_TipoValorID);
  end;
end;

{ TTipoValorCollection }
constructor TTipoValorCollection.Create;
begin
  inherited Create(TTipoValor);
end;

constructor TTipoValorCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTipoValorCollection.Add: TTipoValor;
begin
  Result := TTipoValor(inherited Add);
end;

function TTipoValorCollection.GetItems(aIndex: Integer): TTipoValor;
begin
  Result := TTipoValor(inherited Items[aIndex]);
end;

procedure TTipoValorCollection.LoadFromArray(anArray: ATipoValor);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TTipoValorCollection.SaveToArray(anArray: ATipoValor);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TTipoValor(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TTipoValorCollection.SetItems(aIndex: Integer; const Value: TTipoValor);
begin
  TTipoValor(inherited Items[aIndex]).Assign(Value);
end;

{ TPerfilPuntos }

procedure TPerfilPuntos.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TPerfilPuntos;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TPerfilPuntos) then begin
    lSource := LibraryValesCupones_Intf.TPerfilPuntos(iSource);

    Self.FechaIni := lSource.FechaIni;
    Self.FechaFin := lSource.FechaFin;
    Self.Criterio := lSource.Criterio;
    Self.NewField3 := lSource.NewField3;
  end;
end;

procedure TPerfilPuntos.ReadComplex(ASerializer: TObject);
var
  l_Criterio: Double;
  l_FechaFin: DateTime;
  l_FechaIni: DateTime;
  l_NewField3: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FechaIni := Self.FechaIni;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaIni', l_FechaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaIni']));
    end;
    Self.FechaIni := l_FechaIni;
    l_FechaFin := Self.FechaFin;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFin', l_FechaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFin']));
    end;
    Self.FechaFin := l_FechaFin;
    l_Criterio := Self.Criterio;
    try
      TROSerializer(ASerializer).ReadDouble('Criterio', TypInfo.ftDouble, l_Criterio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Criterio']));
    end;
    Self.Criterio := l_Criterio;
    l_NewField3 := Self.NewField3;
    try
      TROSerializer(ASerializer).ReadAnsiString('NewField3', l_NewField3);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NewField3']));
    end;
    Self.NewField3 := l_NewField3;
  end
  else begin
    l_Criterio := Self.Criterio;
    try
      TROSerializer(ASerializer).ReadDouble('Criterio', TypInfo.ftDouble, l_Criterio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Criterio']));
    end;
    Self.Criterio := l_Criterio;
    l_FechaFin := Self.FechaFin;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFin', l_FechaFin);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFin']));
    end;
    Self.FechaFin := l_FechaFin;
    l_FechaIni := Self.FechaIni;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaIni', l_FechaIni);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaIni']));
    end;
    Self.FechaIni := l_FechaIni;
    l_NewField3 := Self.NewField3;
    try
      TROSerializer(ASerializer).ReadAnsiString('NewField3', l_NewField3);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NewField3']));
    end;
    Self.NewField3 := l_NewField3;
  end;
end;

procedure TPerfilPuntos.WriteComplex(ASerializer: TObject);
var
  l_Criterio: Double;
  l_FechaFin: DateTime;
  l_FechaIni: DateTime;
  l_NewField3: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TPerfilPuntos);
    l_FechaIni := Self.FechaIni;
    TROSerializer(ASerializer).WriteDateTime('FechaIni', l_FechaIni);
    l_FechaFin := Self.FechaFin;
    TROSerializer(ASerializer).WriteDateTime('FechaFin', l_FechaFin);
    l_Criterio := Self.Criterio;
    TROSerializer(ASerializer).WriteDouble('Criterio', TypInfo.ftDouble, l_Criterio);
    l_NewField3 := Self.NewField3;
    TROSerializer(ASerializer).WriteAnsiString('NewField3', l_NewField3);
  end
  else begin
    l_Criterio := Self.Criterio;
    TROSerializer(ASerializer).WriteDouble('Criterio', TypInfo.ftDouble, l_Criterio);
    l_FechaFin := Self.FechaFin;
    TROSerializer(ASerializer).WriteDateTime('FechaFin', l_FechaFin);
    l_FechaIni := Self.FechaIni;
    TROSerializer(ASerializer).WriteDateTime('FechaIni', l_FechaIni);
    l_NewField3 := Self.NewField3;
    TROSerializer(ASerializer).WriteAnsiString('NewField3', l_NewField3);
  end;
end;

{ TPerfilPuntosCollection }
constructor TPerfilPuntosCollection.Create;
begin
  inherited Create(TPerfilPuntos);
end;

constructor TPerfilPuntosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TPerfilPuntosCollection.Add: TPerfilPuntos;
begin
  Result := TPerfilPuntos(inherited Add);
end;

function TPerfilPuntosCollection.GetItems(aIndex: Integer): TPerfilPuntos;
begin
  Result := TPerfilPuntos(inherited Items[aIndex]);
end;

procedure TPerfilPuntosCollection.SetItems(aIndex: Integer; const Value: TPerfilPuntos);
begin
  TPerfilPuntos(inherited Items[aIndex]).Assign(Value);
end;

{ TFacturaElectronica }

procedure TFacturaElectronica.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFacturaElectronica;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFacturaElectronica) then begin
    lSource := LibraryValesCupones_Intf.TFacturaElectronica(iSource);

    Self.Serie := lSource.Serie;
    Self.Folio := lSource.Folio;
    Self.Fecha := lSource.Fecha;
    Self.noAprobacion := lSource.noAprobacion;
    Self.anoAprobacion := lSource.anoAprobacion;
    Self.tipoComprobante := lSource.tipoComprobante;
    Self.formaDePago := lSource.formaDePago;
    Self.SubTotal := lSource.SubTotal;
    Self.MunicipioEmisor := lSource.MunicipioEmisor;
    Self.Total := lSource.Total;
    Self.RFCEmisor := lSource.RFCEmisor;
    Self.NomEmpEmisor := lSource.NomEmpEmisor;
    Self.DireccionEm := lSource.DireccionEm;
    Self.NoExteriorEm := lSource.NoExteriorEm;
    Self.ColoniaEmisor := lSource.ColoniaEmisor;
    Self.EstadoEmisor := lSource.EstadoEmisor;
    Self.PaisEmisor := lSource.PaisEmisor;
    Self.CodigoPostalEmisor := lSource.CodigoPostalEmisor;
    Self.RFCReceptor := lSource.RFCReceptor;
    Self.NombreReceptor := lSource.NombreReceptor;
    Self.DomicilioReceptor := lSource.DomicilioReceptor;
    Self.NoExteriorReceptor := lSource.NoExteriorReceptor;
    Self.ColoniaReceptor := lSource.ColoniaReceptor;
    Self.LocalidadReceptor := lSource.LocalidadReceptor;
    Self.MunicipioReceptor := lSource.MunicipioReceptor;
    Self.EstadoReceptor := lSource.EstadoReceptor;
    Self.PaisReceptor := lSource.PaisReceptor;
    Self.CodigoPostalReceptor := lSource.CodigoPostalReceptor;
    if System.Assigned(lSource.fFacturaElectronicaDetalleImportes) then
      Self.FacturaElectronicaDetalleImportes.Assign(lSource.fFacturaElectronicaDetalleImportes)
    else
      SysUtils.FreeAndNil(fFacturaElectronicaDetalleImportes);
    if System.Assigned(lSource.fFacturaElectronicaDetalleImpuestos) then
      Self.FacturaElectronicaDetalleImpuestos.Assign(lSource.fFacturaElectronicaDetalleImpuestos)
    else
      SysUtils.FreeAndNil(fFacturaElectronicaDetalleImpuestos);
    Self.TotalImpuesto := lSource.TotalImpuesto;
    Self.CalleExpedidoEn := lSource.CalleExpedidoEn;
    Self.NoExterioExpedidoEn := lSource.NoExterioExpedidoEn;
    Self.ColoniaExpedidoEn := lSource.ColoniaExpedidoEn;
    Self.CodigoPostalExpedidoEn := lSource.CodigoPostalExpedidoEn;
    Self.LocalidadExpedidoEn := lSource.LocalidadExpedidoEn;
    Self.MunicipioExpedidoEn := lSource.MunicipioExpedidoEn;
    Self.EstadoExpedidoEn := lSource.EstadoExpedidoEn;
    Self.PaisExpedidoEn := lSource.PaisExpedidoEn;
    Self.Sucursal := lSource.Sucursal;
    Self.email := lSource.email;
  end;
end;

function TFacturaElectronica.GetFacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes;
begin
  if (fFacturaElectronicaDetalleImportes = nil) then fFacturaElectronicaDetalleImportes := ATFacturaElectronicaDetalleImportes.Create();
  Result := fFacturaElectronicaDetalleImportes;
end;

function TFacturaElectronica.GetFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
begin
  if (fFacturaElectronicaDetalleImpuestos = nil) then fFacturaElectronicaDetalleImpuestos := ATFacturaElectronicaDetalleImpuestos.Create();
  Result := fFacturaElectronicaDetalleImpuestos;
end;

procedure TFacturaElectronica.ReadComplex(ASerializer: TObject);
var
  l_anoAprobacion: AnsiString;
  l_CalleExpedidoEn: AnsiString;
  l_CodigoPostalEmisor: AnsiString;
  l_CodigoPostalExpedidoEn: AnsiString;
  l_CodigoPostalReceptor: AnsiString;
  l_ColoniaEmisor: AnsiString;
  l_ColoniaExpedidoEn: AnsiString;
  l_ColoniaReceptor: AnsiString;
  l_DireccionEm: AnsiString;
  l_DomicilioReceptor: AnsiString;
  l_email: AnsiString;
  l_EstadoEmisor: AnsiString;
  l_EstadoExpedidoEn: AnsiString;
  l_EstadoReceptor: AnsiString;
  l_FacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes;
  l_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  l_Fecha: AnsiString;
  l_Folio: AnsiString;
  l_formaDePago: AnsiString;
  l_LocalidadExpedidoEn: AnsiString;
  l_LocalidadReceptor: AnsiString;
  l_MunicipioEmisor: AnsiString;
  l_MunicipioExpedidoEn: AnsiString;
  l_MunicipioReceptor: AnsiString;
  l_noAprobacion: AnsiString;
  l_NoExterioExpedidoEn: AnsiString;
  l_NoExteriorEm: AnsiString;
  l_NoExteriorReceptor: AnsiString;
  l_NombreReceptor: AnsiString;
  l_NomEmpEmisor: AnsiString;
  l_PaisEmisor: AnsiString;
  l_PaisExpedidoEn: AnsiString;
  l_PaisReceptor: AnsiString;
  l_RFCEmisor: AnsiString;
  l_RFCReceptor: AnsiString;
  l_Serie: AnsiString;
  l_SubTotal: AnsiString;
  l_Sucursal: Boolean;
  l_tipoComprobante: AnsiString;
  l_Total: AnsiString;
  l_TotalImpuesto: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Folio', l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadAnsiString('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_noAprobacion := Self.noAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('noAprobacion', l_noAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'noAprobacion']));
    end;
    Self.noAprobacion := l_noAprobacion;
    l_anoAprobacion := Self.anoAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('anoAprobacion', l_anoAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'anoAprobacion']));
    end;
    Self.anoAprobacion := l_anoAprobacion;
    l_tipoComprobante := Self.tipoComprobante;
    try
      TROSerializer(ASerializer).ReadAnsiString('tipoComprobante', l_tipoComprobante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'tipoComprobante']));
    end;
    Self.tipoComprobante := l_tipoComprobante;
    l_formaDePago := Self.formaDePago;
    try
      TROSerializer(ASerializer).ReadAnsiString('formaDePago', l_formaDePago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'formaDePago']));
    end;
    Self.formaDePago := l_formaDePago;
    l_SubTotal := Self.SubTotal;
    try
      TROSerializer(ASerializer).ReadAnsiString('SubTotal', l_SubTotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SubTotal']));
    end;
    Self.SubTotal := l_SubTotal;
    l_MunicipioEmisor := Self.MunicipioEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioEmisor']));
    end;
    Self.MunicipioEmisor := l_MunicipioEmisor;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadAnsiString('Total', l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_RFCEmisor := Self.RFCEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCEmisor', l_RFCEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCEmisor']));
    end;
    Self.RFCEmisor := l_RFCEmisor;
    l_NomEmpEmisor := Self.NomEmpEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NomEmpEmisor']));
    end;
    Self.NomEmpEmisor := l_NomEmpEmisor;
    l_DireccionEm := Self.DireccionEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('DireccionEm', l_DireccionEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DireccionEm']));
    end;
    Self.DireccionEm := l_DireccionEm;
    l_NoExteriorEm := Self.NoExteriorEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorEm', l_NoExteriorEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorEm']));
    end;
    Self.NoExteriorEm := l_NoExteriorEm;
    l_ColoniaEmisor := Self.ColoniaEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaEmisor']));
    end;
    Self.ColoniaEmisor := l_ColoniaEmisor;
    l_EstadoEmisor := Self.EstadoEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoEmisor', l_EstadoEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoEmisor']));
    end;
    Self.EstadoEmisor := l_EstadoEmisor;
    l_PaisEmisor := Self.PaisEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisEmisor', l_PaisEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisEmisor']));
    end;
    Self.PaisEmisor := l_PaisEmisor;
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalEmisor']));
    end;
    Self.CodigoPostalEmisor := l_CodigoPostalEmisor;
    l_RFCReceptor := Self.RFCReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCReceptor', l_RFCReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCReceptor']));
    end;
    Self.RFCReceptor := l_RFCReceptor;
    l_NombreReceptor := Self.NombreReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreReceptor', l_NombreReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreReceptor']));
    end;
    Self.NombreReceptor := l_NombreReceptor;
    l_DomicilioReceptor := Self.DomicilioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DomicilioReceptor']));
    end;
    Self.DomicilioReceptor := l_DomicilioReceptor;
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorReceptor']));
    end;
    Self.NoExteriorReceptor := l_NoExteriorReceptor;
    l_ColoniaReceptor := Self.ColoniaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaReceptor']));
    end;
    Self.ColoniaReceptor := l_ColoniaReceptor;
    l_LocalidadReceptor := Self.LocalidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadReceptor']));
    end;
    Self.LocalidadReceptor := l_LocalidadReceptor;
    l_MunicipioReceptor := Self.MunicipioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioReceptor']));
    end;
    Self.MunicipioReceptor := l_MunicipioReceptor;
    l_EstadoReceptor := Self.EstadoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoReceptor', l_EstadoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoReceptor']));
    end;
    Self.EstadoReceptor := l_EstadoReceptor;
    l_PaisReceptor := Self.PaisReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisReceptor', l_PaisReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisReceptor']));
    end;
    Self.PaisReceptor := l_PaisReceptor;
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalReceptor']));
    end;
    Self.CodigoPostalReceptor := l_CodigoPostalReceptor;
    l_FacturaElectronicaDetalleImportes := int_FacturaElectronicaDetalleImportes;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImportes', ATFacturaElectronicaDetalleImportes, l_FacturaElectronicaDetalleImportes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImportes']));
    end;
    if int_FacturaElectronicaDetalleImportes <> l_FacturaElectronicaDetalleImportes then Self.FacturaElectronicaDetalleImportes.Free;
    Self.FacturaElectronicaDetalleImportes := l_FacturaElectronicaDetalleImportes;
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImpuestos', ATFacturaElectronicaDetalleImpuestos, l_FacturaElectronicaDetalleImpuestos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImpuestos']));
    end;
    if int_FacturaElectronicaDetalleImpuestos <> l_FacturaElectronicaDetalleImpuestos then Self.FacturaElectronicaDetalleImpuestos.Free;
    Self.FacturaElectronicaDetalleImpuestos := l_FacturaElectronicaDetalleImpuestos;
    l_TotalImpuesto := Self.TotalImpuesto;
    try
      TROSerializer(ASerializer).ReadAnsiString('TotalImpuesto', l_TotalImpuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalImpuesto']));
    end;
    Self.TotalImpuesto := l_TotalImpuesto;
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CalleExpedidoEn']));
    end;
    Self.CalleExpedidoEn := l_CalleExpedidoEn;
    l_NoExterioExpedidoEn := Self.NoExterioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExterioExpedidoEn', l_NoExterioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExterioExpedidoEn']));
    end;
    Self.NoExterioExpedidoEn := l_NoExterioExpedidoEn;
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaExpedidoEn']));
    end;
    Self.ColoniaExpedidoEn := l_ColoniaExpedidoEn;
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalExpedidoEn']));
    end;
    Self.CodigoPostalExpedidoEn := l_CodigoPostalExpedidoEn;
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadExpedidoEn']));
    end;
    Self.LocalidadExpedidoEn := l_LocalidadExpedidoEn;
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioExpedidoEn']));
    end;
    Self.MunicipioExpedidoEn := l_MunicipioExpedidoEn;
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoExpedidoEn']));
    end;
    Self.EstadoExpedidoEn := l_EstadoExpedidoEn;
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisExpedidoEn']));
    end;
    Self.PaisExpedidoEn := l_PaisExpedidoEn;
    l_Sucursal := Self.Sucursal;
    try
      TROSerializer(ASerializer).ReadEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sucursal']));
    end;
    Self.Sucursal := l_Sucursal;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
  end
  else begin
    l_anoAprobacion := Self.anoAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('anoAprobacion', l_anoAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'anoAprobacion']));
    end;
    Self.anoAprobacion := l_anoAprobacion;
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CalleExpedidoEn']));
    end;
    Self.CalleExpedidoEn := l_CalleExpedidoEn;
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalEmisor']));
    end;
    Self.CodigoPostalEmisor := l_CodigoPostalEmisor;
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalExpedidoEn']));
    end;
    Self.CodigoPostalExpedidoEn := l_CodigoPostalExpedidoEn;
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalReceptor']));
    end;
    Self.CodigoPostalReceptor := l_CodigoPostalReceptor;
    l_ColoniaEmisor := Self.ColoniaEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaEmisor']));
    end;
    Self.ColoniaEmisor := l_ColoniaEmisor;
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaExpedidoEn']));
    end;
    Self.ColoniaExpedidoEn := l_ColoniaExpedidoEn;
    l_ColoniaReceptor := Self.ColoniaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaReceptor']));
    end;
    Self.ColoniaReceptor := l_ColoniaReceptor;
    l_DireccionEm := Self.DireccionEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('DireccionEm', l_DireccionEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DireccionEm']));
    end;
    Self.DireccionEm := l_DireccionEm;
    l_DomicilioReceptor := Self.DomicilioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DomicilioReceptor']));
    end;
    Self.DomicilioReceptor := l_DomicilioReceptor;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
    l_EstadoEmisor := Self.EstadoEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoEmisor', l_EstadoEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoEmisor']));
    end;
    Self.EstadoEmisor := l_EstadoEmisor;
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoExpedidoEn']));
    end;
    Self.EstadoExpedidoEn := l_EstadoExpedidoEn;
    l_EstadoReceptor := Self.EstadoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoReceptor', l_EstadoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoReceptor']));
    end;
    Self.EstadoReceptor := l_EstadoReceptor;
    l_FacturaElectronicaDetalleImportes := int_FacturaElectronicaDetalleImportes;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImportes', ATFacturaElectronicaDetalleImportes, l_FacturaElectronicaDetalleImportes);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImportes']));
    end;
    if int_FacturaElectronicaDetalleImportes <> l_FacturaElectronicaDetalleImportes then FacturaElectronicaDetalleImportes.Free;
    Self.FacturaElectronicaDetalleImportes := l_FacturaElectronicaDetalleImportes;
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImpuestos', ATFacturaElectronicaDetalleImpuestos, l_FacturaElectronicaDetalleImpuestos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImpuestos']));
    end;
    if int_FacturaElectronicaDetalleImpuestos <> l_FacturaElectronicaDetalleImpuestos then FacturaElectronicaDetalleImpuestos.Free;
    Self.FacturaElectronicaDetalleImpuestos := l_FacturaElectronicaDetalleImpuestos;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadAnsiString('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Folio', l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_formaDePago := Self.formaDePago;
    try
      TROSerializer(ASerializer).ReadAnsiString('formaDePago', l_formaDePago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'formaDePago']));
    end;
    Self.formaDePago := l_formaDePago;
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadExpedidoEn']));
    end;
    Self.LocalidadExpedidoEn := l_LocalidadExpedidoEn;
    l_LocalidadReceptor := Self.LocalidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadReceptor']));
    end;
    Self.LocalidadReceptor := l_LocalidadReceptor;
    l_MunicipioEmisor := Self.MunicipioEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioEmisor']));
    end;
    Self.MunicipioEmisor := l_MunicipioEmisor;
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioExpedidoEn']));
    end;
    Self.MunicipioExpedidoEn := l_MunicipioExpedidoEn;
    l_MunicipioReceptor := Self.MunicipioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioReceptor']));
    end;
    Self.MunicipioReceptor := l_MunicipioReceptor;
    l_noAprobacion := Self.noAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('noAprobacion', l_noAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'noAprobacion']));
    end;
    Self.noAprobacion := l_noAprobacion;
    l_NoExterioExpedidoEn := Self.NoExterioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExterioExpedidoEn', l_NoExterioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExterioExpedidoEn']));
    end;
    Self.NoExterioExpedidoEn := l_NoExterioExpedidoEn;
    l_NoExteriorEm := Self.NoExteriorEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorEm', l_NoExteriorEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorEm']));
    end;
    Self.NoExteriorEm := l_NoExteriorEm;
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorReceptor']));
    end;
    Self.NoExteriorReceptor := l_NoExteriorReceptor;
    l_NombreReceptor := Self.NombreReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreReceptor', l_NombreReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreReceptor']));
    end;
    Self.NombreReceptor := l_NombreReceptor;
    l_NomEmpEmisor := Self.NomEmpEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NomEmpEmisor']));
    end;
    Self.NomEmpEmisor := l_NomEmpEmisor;
    l_PaisEmisor := Self.PaisEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisEmisor', l_PaisEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisEmisor']));
    end;
    Self.PaisEmisor := l_PaisEmisor;
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisExpedidoEn']));
    end;
    Self.PaisExpedidoEn := l_PaisExpedidoEn;
    l_PaisReceptor := Self.PaisReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisReceptor', l_PaisReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisReceptor']));
    end;
    Self.PaisReceptor := l_PaisReceptor;
    l_RFCEmisor := Self.RFCEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCEmisor', l_RFCEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCEmisor']));
    end;
    Self.RFCEmisor := l_RFCEmisor;
    l_RFCReceptor := Self.RFCReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCReceptor', l_RFCReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCReceptor']));
    end;
    Self.RFCReceptor := l_RFCReceptor;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_SubTotal := Self.SubTotal;
    try
      TROSerializer(ASerializer).ReadAnsiString('SubTotal', l_SubTotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SubTotal']));
    end;
    Self.SubTotal := l_SubTotal;
    l_Sucursal := Self.Sucursal;
    try
      TROSerializer(ASerializer).ReadEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sucursal']));
    end;
    Self.Sucursal := l_Sucursal;
    l_tipoComprobante := Self.tipoComprobante;
    try
      TROSerializer(ASerializer).ReadAnsiString('tipoComprobante', l_tipoComprobante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'tipoComprobante']));
    end;
    Self.tipoComprobante := l_tipoComprobante;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadAnsiString('Total', l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_TotalImpuesto := Self.TotalImpuesto;
    try
      TROSerializer(ASerializer).ReadAnsiString('TotalImpuesto', l_TotalImpuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalImpuesto']));
    end;
    Self.TotalImpuesto := l_TotalImpuesto;
  end;
end;

procedure TFacturaElectronica.WriteComplex(ASerializer: TObject);
var
  l_anoAprobacion: AnsiString;
  l_CalleExpedidoEn: AnsiString;
  l_CodigoPostalEmisor: AnsiString;
  l_CodigoPostalExpedidoEn: AnsiString;
  l_CodigoPostalReceptor: AnsiString;
  l_ColoniaEmisor: AnsiString;
  l_ColoniaExpedidoEn: AnsiString;
  l_ColoniaReceptor: AnsiString;
  l_DireccionEm: AnsiString;
  l_DomicilioReceptor: AnsiString;
  l_email: AnsiString;
  l_EstadoEmisor: AnsiString;
  l_EstadoExpedidoEn: AnsiString;
  l_EstadoReceptor: AnsiString;
  l_FacturaElectronicaDetalleImportes: ATFacturaElectronicaDetalleImportes;
  l_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  l_Fecha: AnsiString;
  l_Folio: AnsiString;
  l_formaDePago: AnsiString;
  l_LocalidadExpedidoEn: AnsiString;
  l_LocalidadReceptor: AnsiString;
  l_MunicipioEmisor: AnsiString;
  l_MunicipioExpedidoEn: AnsiString;
  l_MunicipioReceptor: AnsiString;
  l_noAprobacion: AnsiString;
  l_NoExterioExpedidoEn: AnsiString;
  l_NoExteriorEm: AnsiString;
  l_NoExteriorReceptor: AnsiString;
  l_NombreReceptor: AnsiString;
  l_NomEmpEmisor: AnsiString;
  l_PaisEmisor: AnsiString;
  l_PaisExpedidoEn: AnsiString;
  l_PaisReceptor: AnsiString;
  l_RFCEmisor: AnsiString;
  l_RFCReceptor: AnsiString;
  l_Serie: AnsiString;
  l_SubTotal: AnsiString;
  l_Sucursal: Boolean;
  l_tipoComprobante: AnsiString;
  l_Total: AnsiString;
  l_TotalImpuesto: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFacturaElectronica);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteAnsiString('Folio', l_Folio);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteAnsiString('Fecha', l_Fecha);
    l_noAprobacion := Self.noAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('noAprobacion', l_noAprobacion);
    l_anoAprobacion := Self.anoAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('anoAprobacion', l_anoAprobacion);
    l_tipoComprobante := Self.tipoComprobante;
    TROSerializer(ASerializer).WriteAnsiString('tipoComprobante', l_tipoComprobante);
    l_formaDePago := Self.formaDePago;
    TROSerializer(ASerializer).WriteAnsiString('formaDePago', l_formaDePago);
    l_SubTotal := Self.SubTotal;
    TROSerializer(ASerializer).WriteAnsiString('SubTotal', l_SubTotal);
    l_MunicipioEmisor := Self.MunicipioEmisor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteAnsiString('Total', l_Total);
    l_RFCEmisor := Self.RFCEmisor;
    TROSerializer(ASerializer).WriteAnsiString('RFCEmisor', l_RFCEmisor);
    l_NomEmpEmisor := Self.NomEmpEmisor;
    TROSerializer(ASerializer).WriteAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    l_DireccionEm := Self.DireccionEm;
    TROSerializer(ASerializer).WriteAnsiString('DireccionEm', l_DireccionEm);
    l_NoExteriorEm := Self.NoExteriorEm;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorEm', l_NoExteriorEm);
    l_ColoniaEmisor := Self.ColoniaEmisor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    l_EstadoEmisor := Self.EstadoEmisor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoEmisor', l_EstadoEmisor);
    l_PaisEmisor := Self.PaisEmisor;
    TROSerializer(ASerializer).WriteAnsiString('PaisEmisor', l_PaisEmisor);
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    l_RFCReceptor := Self.RFCReceptor;
    TROSerializer(ASerializer).WriteAnsiString('RFCReceptor', l_RFCReceptor);
    l_NombreReceptor := Self.NombreReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NombreReceptor', l_NombreReceptor);
    l_DomicilioReceptor := Self.DomicilioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    l_ColoniaReceptor := Self.ColoniaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    l_LocalidadReceptor := Self.LocalidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    l_MunicipioReceptor := Self.MunicipioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    l_EstadoReceptor := Self.EstadoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoReceptor', l_EstadoReceptor);
    l_PaisReceptor := Self.PaisReceptor;
    TROSerializer(ASerializer).WriteAnsiString('PaisReceptor', l_PaisReceptor);
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    l_FacturaElectronicaDetalleImportes := int_FacturaElectronicaDetalleImportes;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImportes', l_FacturaElectronicaDetalleImportes, ATFacturaElectronicaDetalleImportes);
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImpuestos', l_FacturaElectronicaDetalleImpuestos, ATFacturaElectronicaDetalleImpuestos);
    l_TotalImpuesto := Self.TotalImpuesto;
    TROSerializer(ASerializer).WriteAnsiString('TotalImpuesto', l_TotalImpuesto);
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    l_NoExterioExpedidoEn := Self.NoExterioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('NoExterioExpedidoEn', l_NoExterioExpedidoEn);
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    l_Sucursal := Self.Sucursal;
    TROSerializer(ASerializer).WriteEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
  end
  else begin
    l_anoAprobacion := Self.anoAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('anoAprobacion', l_anoAprobacion);
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    l_ColoniaEmisor := Self.ColoniaEmisor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    l_ColoniaReceptor := Self.ColoniaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    l_DireccionEm := Self.DireccionEm;
    TROSerializer(ASerializer).WriteAnsiString('DireccionEm', l_DireccionEm);
    l_DomicilioReceptor := Self.DomicilioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
    l_EstadoEmisor := Self.EstadoEmisor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoEmisor', l_EstadoEmisor);
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    l_EstadoReceptor := Self.EstadoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoReceptor', l_EstadoReceptor);
    l_FacturaElectronicaDetalleImportes := int_FacturaElectronicaDetalleImportes;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImportes', l_FacturaElectronicaDetalleImportes, ATFacturaElectronicaDetalleImportes);
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImpuestos', l_FacturaElectronicaDetalleImpuestos, ATFacturaElectronicaDetalleImpuestos);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteAnsiString('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteAnsiString('Folio', l_Folio);
    l_formaDePago := Self.formaDePago;
    TROSerializer(ASerializer).WriteAnsiString('formaDePago', l_formaDePago);
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    l_LocalidadReceptor := Self.LocalidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    l_MunicipioEmisor := Self.MunicipioEmisor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    l_MunicipioReceptor := Self.MunicipioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    l_noAprobacion := Self.noAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('noAprobacion', l_noAprobacion);
    l_NoExterioExpedidoEn := Self.NoExterioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('NoExterioExpedidoEn', l_NoExterioExpedidoEn);
    l_NoExteriorEm := Self.NoExteriorEm;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorEm', l_NoExteriorEm);
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    l_NombreReceptor := Self.NombreReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NombreReceptor', l_NombreReceptor);
    l_NomEmpEmisor := Self.NomEmpEmisor;
    TROSerializer(ASerializer).WriteAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    l_PaisEmisor := Self.PaisEmisor;
    TROSerializer(ASerializer).WriteAnsiString('PaisEmisor', l_PaisEmisor);
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    l_PaisReceptor := Self.PaisReceptor;
    TROSerializer(ASerializer).WriteAnsiString('PaisReceptor', l_PaisReceptor);
    l_RFCEmisor := Self.RFCEmisor;
    TROSerializer(ASerializer).WriteAnsiString('RFCEmisor', l_RFCEmisor);
    l_RFCReceptor := Self.RFCReceptor;
    TROSerializer(ASerializer).WriteAnsiString('RFCReceptor', l_RFCReceptor);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_SubTotal := Self.SubTotal;
    TROSerializer(ASerializer).WriteAnsiString('SubTotal', l_SubTotal);
    l_Sucursal := Self.Sucursal;
    TROSerializer(ASerializer).WriteEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    l_tipoComprobante := Self.tipoComprobante;
    TROSerializer(ASerializer).WriteAnsiString('tipoComprobante', l_tipoComprobante);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteAnsiString('Total', l_Total);
    l_TotalImpuesto := Self.TotalImpuesto;
    TROSerializer(ASerializer).WriteAnsiString('TotalImpuesto', l_TotalImpuesto);
  end;
end;

{ TFacturaElectronicaCollection }
constructor TFacturaElectronicaCollection.Create;
begin
  inherited Create(TFacturaElectronica);
end;

constructor TFacturaElectronicaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFacturaElectronicaCollection.Add: TFacturaElectronica;
begin
  Result := TFacturaElectronica(inherited Add);
end;

function TFacturaElectronicaCollection.GetItems(aIndex: Integer): TFacturaElectronica;
begin
  Result := TFacturaElectronica(inherited Items[aIndex]);
end;

procedure TFacturaElectronicaCollection.SetItems(aIndex: Integer; const Value: TFacturaElectronica);
begin
  TFacturaElectronica(inherited Items[aIndex]).Assign(Value);
end;

{ TFacturaElectronicaDetalleImportes }

procedure TFacturaElectronicaDetalleImportes.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFacturaElectronicaDetalleImportes;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFacturaElectronicaDetalleImportes) then begin
    lSource := LibraryValesCupones_Intf.TFacturaElectronicaDetalleImportes(iSource);

    Self.CantidadReceptor := lSource.CantidadReceptor;
    Self.UnidadReceptor := lSource.UnidadReceptor;
    Self.DescripcionReceptor := lSource.DescripcionReceptor;
    Self.ValorUnitarioReceptor := lSource.ValorUnitarioReceptor;
    Self.ImporteReceptor := lSource.ImporteReceptor;
  end;
end;

procedure TFacturaElectronicaDetalleImportes.ReadComplex(ASerializer: TObject);
var
  l_CantidadReceptor: AnsiString;
  l_DescripcionReceptor: AnsiString;
  l_ImporteReceptor: AnsiString;
  l_UnidadReceptor: AnsiString;
  l_ValorUnitarioReceptor: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_CantidadReceptor := Self.CantidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CantidadReceptor', l_CantidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CantidadReceptor']));
    end;
    Self.CantidadReceptor := l_CantidadReceptor;
    l_UnidadReceptor := Self.UnidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('UnidadReceptor', l_UnidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UnidadReceptor']));
    end;
    Self.UnidadReceptor := l_UnidadReceptor;
    l_DescripcionReceptor := Self.DescripcionReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DescripcionReceptor', l_DescripcionReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DescripcionReceptor']));
    end;
    Self.DescripcionReceptor := l_DescripcionReceptor;
    l_ValorUnitarioReceptor := Self.ValorUnitarioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ValorUnitarioReceptor', l_ValorUnitarioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ValorUnitarioReceptor']));
    end;
    Self.ValorUnitarioReceptor := l_ValorUnitarioReceptor;
    l_ImporteReceptor := Self.ImporteReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteReceptor', l_ImporteReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteReceptor']));
    end;
    Self.ImporteReceptor := l_ImporteReceptor;
  end
  else begin
    l_CantidadReceptor := Self.CantidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CantidadReceptor', l_CantidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CantidadReceptor']));
    end;
    Self.CantidadReceptor := l_CantidadReceptor;
    l_DescripcionReceptor := Self.DescripcionReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DescripcionReceptor', l_DescripcionReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DescripcionReceptor']));
    end;
    Self.DescripcionReceptor := l_DescripcionReceptor;
    l_ImporteReceptor := Self.ImporteReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteReceptor', l_ImporteReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteReceptor']));
    end;
    Self.ImporteReceptor := l_ImporteReceptor;
    l_UnidadReceptor := Self.UnidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('UnidadReceptor', l_UnidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UnidadReceptor']));
    end;
    Self.UnidadReceptor := l_UnidadReceptor;
    l_ValorUnitarioReceptor := Self.ValorUnitarioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ValorUnitarioReceptor', l_ValorUnitarioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ValorUnitarioReceptor']));
    end;
    Self.ValorUnitarioReceptor := l_ValorUnitarioReceptor;
  end;
end;

procedure TFacturaElectronicaDetalleImportes.WriteComplex(ASerializer: TObject);
var
  l_CantidadReceptor: AnsiString;
  l_DescripcionReceptor: AnsiString;
  l_ImporteReceptor: AnsiString;
  l_UnidadReceptor: AnsiString;
  l_ValorUnitarioReceptor: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFacturaElectronicaDetalleImportes);
    l_CantidadReceptor := Self.CantidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CantidadReceptor', l_CantidadReceptor);
    l_UnidadReceptor := Self.UnidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('UnidadReceptor', l_UnidadReceptor);
    l_DescripcionReceptor := Self.DescripcionReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DescripcionReceptor', l_DescripcionReceptor);
    l_ValorUnitarioReceptor := Self.ValorUnitarioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ValorUnitarioReceptor', l_ValorUnitarioReceptor);
    l_ImporteReceptor := Self.ImporteReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImporteReceptor', l_ImporteReceptor);
  end
  else begin
    l_CantidadReceptor := Self.CantidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CantidadReceptor', l_CantidadReceptor);
    l_DescripcionReceptor := Self.DescripcionReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DescripcionReceptor', l_DescripcionReceptor);
    l_ImporteReceptor := Self.ImporteReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImporteReceptor', l_ImporteReceptor);
    l_UnidadReceptor := Self.UnidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('UnidadReceptor', l_UnidadReceptor);
    l_ValorUnitarioReceptor := Self.ValorUnitarioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ValorUnitarioReceptor', l_ValorUnitarioReceptor);
  end;
end;

{ TFacturaElectronicaDetalleImportesCollection }
constructor TFacturaElectronicaDetalleImportesCollection.Create;
begin
  inherited Create(TFacturaElectronicaDetalleImportes);
end;

constructor TFacturaElectronicaDetalleImportesCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFacturaElectronicaDetalleImportesCollection.Add: TFacturaElectronicaDetalleImportes;
begin
  Result := TFacturaElectronicaDetalleImportes(inherited Add);
end;

function TFacturaElectronicaDetalleImportesCollection.GetItems(aIndex: Integer): TFacturaElectronicaDetalleImportes;
begin
  Result := TFacturaElectronicaDetalleImportes(inherited Items[aIndex]);
end;

procedure TFacturaElectronicaDetalleImportesCollection.LoadFromArray(anArray: ATFacturaElectronicaDetalleImportes);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TFacturaElectronicaDetalleImportesCollection.SaveToArray(anArray: ATFacturaElectronicaDetalleImportes);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TFacturaElectronicaDetalleImportes(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TFacturaElectronicaDetalleImportesCollection.SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImportes);
begin
  TFacturaElectronicaDetalleImportes(inherited Items[aIndex]).Assign(Value);
end;

{ TFacturaElectronicaDetalleImpuestos }

procedure TFacturaElectronicaDetalleImpuestos.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFacturaElectronicaDetalleImpuestos;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFacturaElectronicaDetalleImpuestos) then begin
    lSource := LibraryValesCupones_Intf.TFacturaElectronicaDetalleImpuestos(iSource);

    Self.ImpuestoReceptor := lSource.ImpuestoReceptor;
    Self.TasaReceptor := lSource.TasaReceptor;
    Self.ImporteImpReceptor := lSource.ImporteImpReceptor;
  end;
end;

procedure TFacturaElectronicaDetalleImpuestos.ReadComplex(ASerializer: TObject);
var
  l_ImporteImpReceptor: AnsiString;
  l_ImpuestoReceptor: AnsiString;
  l_TasaReceptor: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ImpuestoReceptor := Self.ImpuestoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImpuestoReceptor', l_ImpuestoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoReceptor']));
    end;
    Self.ImpuestoReceptor := l_ImpuestoReceptor;
    l_TasaReceptor := Self.TasaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('TasaReceptor', l_TasaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TasaReceptor']));
    end;
    Self.TasaReceptor := l_TasaReceptor;
    l_ImporteImpReceptor := Self.ImporteImpReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteImpReceptor', l_ImporteImpReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteImpReceptor']));
    end;
    Self.ImporteImpReceptor := l_ImporteImpReceptor;
  end
  else begin
    l_ImporteImpReceptor := Self.ImporteImpReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteImpReceptor', l_ImporteImpReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteImpReceptor']));
    end;
    Self.ImporteImpReceptor := l_ImporteImpReceptor;
    l_ImpuestoReceptor := Self.ImpuestoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImpuestoReceptor', l_ImpuestoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoReceptor']));
    end;
    Self.ImpuestoReceptor := l_ImpuestoReceptor;
    l_TasaReceptor := Self.TasaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('TasaReceptor', l_TasaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TasaReceptor']));
    end;
    Self.TasaReceptor := l_TasaReceptor;
  end;
end;

procedure TFacturaElectronicaDetalleImpuestos.WriteComplex(ASerializer: TObject);
var
  l_ImporteImpReceptor: AnsiString;
  l_ImpuestoReceptor: AnsiString;
  l_TasaReceptor: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFacturaElectronicaDetalleImpuestos);
    l_ImpuestoReceptor := Self.ImpuestoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImpuestoReceptor', l_ImpuestoReceptor);
    l_TasaReceptor := Self.TasaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('TasaReceptor', l_TasaReceptor);
    l_ImporteImpReceptor := Self.ImporteImpReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImporteImpReceptor', l_ImporteImpReceptor);
  end
  else begin
    l_ImporteImpReceptor := Self.ImporteImpReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImporteImpReceptor', l_ImporteImpReceptor);
    l_ImpuestoReceptor := Self.ImpuestoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ImpuestoReceptor', l_ImpuestoReceptor);
    l_TasaReceptor := Self.TasaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('TasaReceptor', l_TasaReceptor);
  end;
end;

{ TFacturaElectronicaDetalleImpuestosCollection }
constructor TFacturaElectronicaDetalleImpuestosCollection.Create;
begin
  inherited Create(TFacturaElectronicaDetalleImpuestos);
end;

constructor TFacturaElectronicaDetalleImpuestosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFacturaElectronicaDetalleImpuestosCollection.Add: TFacturaElectronicaDetalleImpuestos;
begin
  Result := TFacturaElectronicaDetalleImpuestos(inherited Add);
end;

function TFacturaElectronicaDetalleImpuestosCollection.GetItems(aIndex: Integer): TFacturaElectronicaDetalleImpuestos;
begin
  Result := TFacturaElectronicaDetalleImpuestos(inherited Items[aIndex]);
end;

procedure TFacturaElectronicaDetalleImpuestosCollection.LoadFromArray(anArray: ATFacturaElectronicaDetalleImpuestos);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TFacturaElectronicaDetalleImpuestosCollection.SaveToArray(anArray: ATFacturaElectronicaDetalleImpuestos);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TFacturaElectronicaDetalleImpuestos(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TFacturaElectronicaDetalleImpuestosCollection.SetItems(aIndex: Integer; const Value: TFacturaElectronicaDetalleImpuestos);
begin
  TFacturaElectronicaDetalleImpuestos(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosFacturaPemex }

procedure TDatosFacturaPemex.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosFacturaPemex;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosFacturaPemex) then begin
    lSource := LibraryValesCupones_Intf.TDatosFacturaPemex(iSource);

    Self.TurnoID := lSource.TurnoID;
    Self.EstacionID := lSource.EstacionID;
    Self.Fecha := lSource.Fecha;
    Self.Nombre := lSource.Nombre;
    Self.FacturaPemexID := lSource.FacturaPemexID;
    Self.Serie := lSource.Serie;
    Self.Folio := lSource.Folio;
    Self.Total := lSource.Total;
    if System.Assigned(lSource.fDetalleFactura) then
      Self.DetalleFactura.Assign(lSource.fDetalleFactura)
    else
      SysUtils.FreeAndNil(fDetalleFactura);
    Self.ProductoID := lSource.ProductoID;
  end;
end;

function TDatosFacturaPemex.GetDetalleFactura: ADetalleFacturaPemex;
begin
  if (fDetalleFactura = nil) then fDetalleFactura := ADetalleFacturaPemex.Create();
  Result := fDetalleFactura;
end;

procedure TDatosFacturaPemex.ReadComplex(ASerializer: TObject);
var
  l_DetalleFactura: ADetalleFacturaPemex;
  l_EstacionID: Integer;
  l_FacturaPemexID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Nombre: AnsiString;
  l_ProductoID: Integer;
  l_Serie: AnsiString;
  l_Total: Double;
  l_TurnoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_FacturaPemexID := Self.FacturaPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaPemexID']));
    end;
    Self.FacturaPemexID := l_FacturaPemexID;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_DetalleFactura := int_DetalleFactura;
    try
      TROSerializer(ASerializer).ReadArray('DetalleFactura', ADetalleFacturaPemex, l_DetalleFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFactura']));
    end;
    if int_DetalleFactura <> l_DetalleFactura then Self.DetalleFactura.Free;
    Self.DetalleFactura := l_DetalleFactura;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
  end
  else begin
    l_DetalleFactura := int_DetalleFactura;
    try
      TROSerializer(ASerializer).ReadArray('DetalleFactura', ADetalleFacturaPemex, l_DetalleFactura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DetalleFactura']));
    end;
    if int_DetalleFactura <> l_DetalleFactura then DetalleFactura.Free;
    Self.DetalleFactura := l_DetalleFactura;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FacturaPemexID := Self.FacturaPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaPemexID']));
    end;
    Self.FacturaPemexID := l_FacturaPemexID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadInteger('Folio', TypInfo.otSLong, l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadDouble('Total', TypInfo.ftDouble, l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
  end;
end;

procedure TDatosFacturaPemex.WriteComplex(ASerializer: TObject);
var
  l_DetalleFactura: ADetalleFacturaPemex;
  l_EstacionID: Integer;
  l_FacturaPemexID: Integer;
  l_Fecha: DateTime;
  l_Folio: Integer;
  l_Nombre: AnsiString;
  l_ProductoID: Integer;
  l_Serie: AnsiString;
  l_Total: Double;
  l_TurnoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosFacturaPemex);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_FacturaPemexID := Self.FacturaPemexID;
    TROSerializer(ASerializer).WriteInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_DetalleFactura := int_DetalleFactura;
    TROSerializer(ASerializer).WriteArray('DetalleFactura', l_DetalleFactura, ADetalleFacturaPemex);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
  end
  else begin
    l_DetalleFactura := int_DetalleFactura;
    TROSerializer(ASerializer).WriteArray('DetalleFactura', l_DetalleFactura, ADetalleFacturaPemex);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FacturaPemexID := Self.FacturaPemexID;
    TROSerializer(ASerializer).WriteInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteInteger('Folio', TypInfo.otSLong, l_Folio);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteDouble('Total', TypInfo.ftDouble, l_Total);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
  end;
end;

{ TDatosFacturaPemexCollection }
constructor TDatosFacturaPemexCollection.Create;
begin
  inherited Create(TDatosFacturaPemex);
end;

constructor TDatosFacturaPemexCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosFacturaPemexCollection.Add: TDatosFacturaPemex;
begin
  Result := TDatosFacturaPemex(inherited Add);
end;

function TDatosFacturaPemexCollection.GetItems(aIndex: Integer): TDatosFacturaPemex;
begin
  Result := TDatosFacturaPemex(inherited Items[aIndex]);
end;

procedure TDatosFacturaPemexCollection.SetItems(aIndex: Integer; const Value: TDatosFacturaPemex);
begin
  TDatosFacturaPemex(inherited Items[aIndex]).Assign(Value);
end;

{ TDetalleFacturaPemex }

procedure TDetalleFacturaPemex.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDetalleFacturaPemex;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDetalleFacturaPemex) then begin
    lSource := LibraryValesCupones_Intf.TDetalleFacturaPemex(iSource);

    Self.FacturaPemexID := lSource.FacturaPemexID;
    Self.TipoValorPemexID := lSource.TipoValorPemexID;
    Self.Cantidad := lSource.Cantidad;
    Self.Importe := lSource.Importe;
    Self.Precio := lSource.Precio;
  end;
end;

procedure TDetalleFacturaPemex.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_FacturaPemexID: Integer;
  l_Importe: Double;
  l_Precio: Double;
  l_TipoValorPemexID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FacturaPemexID := Self.FacturaPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaPemexID']));
    end;
    Self.FacturaPemexID := l_FacturaPemexID;
    l_TipoValorPemexID := Self.TipoValorPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorPemexID', TypInfo.otSLong, l_TipoValorPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorPemexID']));
    end;
    Self.TipoValorPemexID := l_TipoValorPemexID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_FacturaPemexID := Self.FacturaPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaPemexID']));
    end;
    Self.FacturaPemexID := l_FacturaPemexID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_TipoValorPemexID := Self.TipoValorPemexID;
    try
      TROSerializer(ASerializer).ReadInteger('TipoValorPemexID', TypInfo.otSLong, l_TipoValorPemexID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoValorPemexID']));
    end;
    Self.TipoValorPemexID := l_TipoValorPemexID;
  end;
end;

procedure TDetalleFacturaPemex.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_FacturaPemexID: Integer;
  l_Importe: Double;
  l_Precio: Double;
  l_TipoValorPemexID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDetalleFacturaPemex);
    l_FacturaPemexID := Self.FacturaPemexID;
    TROSerializer(ASerializer).WriteInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    l_TipoValorPemexID := Self.TipoValorPemexID;
    TROSerializer(ASerializer).WriteInteger('TipoValorPemexID', TypInfo.otSLong, l_TipoValorPemexID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_FacturaPemexID := Self.FacturaPemexID;
    TROSerializer(ASerializer).WriteInteger('FacturaPemexID', TypInfo.otSLong, l_FacturaPemexID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_TipoValorPemexID := Self.TipoValorPemexID;
    TROSerializer(ASerializer).WriteInteger('TipoValorPemexID', TypInfo.otSLong, l_TipoValorPemexID);
  end;
end;

{ TDetalleFacturaPemexCollection }
constructor TDetalleFacturaPemexCollection.Create;
begin
  inherited Create(TDetalleFacturaPemex);
end;

constructor TDetalleFacturaPemexCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDetalleFacturaPemexCollection.Add: TDetalleFacturaPemex;
begin
  Result := TDetalleFacturaPemex(inherited Add);
end;

function TDetalleFacturaPemexCollection.GetItems(aIndex: Integer): TDetalleFacturaPemex;
begin
  Result := TDetalleFacturaPemex(inherited Items[aIndex]);
end;

procedure TDetalleFacturaPemexCollection.LoadFromArray(anArray: ADetalleFacturaPemex);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDetalleFacturaPemexCollection.SaveToArray(anArray: ADetalleFacturaPemex);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDetalleFacturaPemex(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDetalleFacturaPemexCollection.SetItems(aIndex: Integer; const Value: TDetalleFacturaPemex);
begin
  TDetalleFacturaPemex(inherited Items[aIndex]).Assign(Value);
end;

{ TDataFacturaElectronicaF }

procedure TDataFacturaElectronicaF.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDataFacturaElectronicaF;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDataFacturaElectronicaF) then begin
    lSource := LibraryValesCupones_Intf.TDataFacturaElectronicaF(iSource);

    Self.Folio := lSource.Folio;
    Self.Fecha := lSource.Fecha;
    Self.noAprobacion := lSource.noAprobacion;
    Self.tipoComprobante := lSource.tipoComprobante;
    Self.anoAprobacion := lSource.anoAprobacion;
    Self.FormaPago := lSource.FormaPago;
    Self.SubTotal := lSource.SubTotal;
    Self.Total := lSource.Total;
    Self.RFCEmisor := lSource.RFCEmisor;
    Self.NomEmpEmisor := lSource.NomEmpEmisor;
    Self.DireccionEm := lSource.DireccionEm;
    Self.NoExteriorEm := lSource.NoExteriorEm;
    Self.ColoniaEmisor := lSource.ColoniaEmisor;
    Self.MunicipioEmisor := lSource.MunicipioEmisor;
    Self.EstadoEmisor := lSource.EstadoEmisor;
    Self.PaisEmisor := lSource.PaisEmisor;
    Self.CodigoPostalEmisor := lSource.CodigoPostalEmisor;
    Self.RFCReceptor := lSource.RFCReceptor;
    Self.NombreReceptor := lSource.NombreReceptor;
    Self.DomicilioReceptor := lSource.DomicilioReceptor;
    Self.NoExteriorReceptor := lSource.NoExteriorReceptor;
    Self.ColoniaReceptor := lSource.ColoniaReceptor;
    Self.LocalidadReceptor := lSource.LocalidadReceptor;
    Self.MunicipioReceptor := lSource.MunicipioReceptor;
    Self.EstadoReceptor := lSource.EstadoReceptor;
    Self.PaisReceptor := lSource.PaisReceptor;
    Self.CodigoPostalReceptor := lSource.CodigoPostalReceptor;
    if System.Assigned(lSource.fFacturaElectronicaDetalleImporte) then
      Self.FacturaElectronicaDetalleImporte.Assign(lSource.fFacturaElectronicaDetalleImporte)
    else
      SysUtils.FreeAndNil(fFacturaElectronicaDetalleImporte);
    if System.Assigned(lSource.fFacturaElectronicaDetalleImpuestos) then
      Self.FacturaElectronicaDetalleImpuestos.Assign(lSource.fFacturaElectronicaDetalleImpuestos)
    else
      SysUtils.FreeAndNil(fFacturaElectronicaDetalleImpuestos);
    Self.TotalImpuesto := lSource.TotalImpuesto;
    Self.CalleExpedidoEn := lSource.CalleExpedidoEn;
    Self.NoExteriorExpedidaEn := lSource.NoExteriorExpedidaEn;
    Self.ColoniaExpedidoEn := lSource.ColoniaExpedidoEn;
    Self.CodigoPostalExpedidoEn := lSource.CodigoPostalExpedidoEn;
    Self.LocalidadExpedidoEn := lSource.LocalidadExpedidoEn;
    Self.MunicipioExpedidoEn := lSource.MunicipioExpedidoEn;
    Self.EstadoExpedidoEn := lSource.EstadoExpedidoEn;
    Self.PaisExpedidoEn := lSource.PaisExpedidoEn;
    Self.Sucursal := lSource.Sucursal;
    Self.email := lSource.email;
  end;
end;

function TDataFacturaElectronicaF.GetFacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes;
begin
  if (fFacturaElectronicaDetalleImporte = nil) then fFacturaElectronicaDetalleImporte := ATFacturaElectronicaDetalleImportes.Create();
  Result := fFacturaElectronicaDetalleImporte;
end;

function TDataFacturaElectronicaF.GetFacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
begin
  if (fFacturaElectronicaDetalleImpuestos = nil) then fFacturaElectronicaDetalleImpuestos := ATFacturaElectronicaDetalleImpuestos.Create();
  Result := fFacturaElectronicaDetalleImpuestos;
end;

procedure TDataFacturaElectronicaF.ReadComplex(ASerializer: TObject);
var
  l_anoAprobacion: AnsiString;
  l_CalleExpedidoEn: AnsiString;
  l_CodigoPostalEmisor: AnsiString;
  l_CodigoPostalExpedidoEn: AnsiString;
  l_CodigoPostalReceptor: AnsiString;
  l_ColoniaEmisor: AnsiString;
  l_ColoniaExpedidoEn: AnsiString;
  l_ColoniaReceptor: AnsiString;
  l_DireccionEm: AnsiString;
  l_DomicilioReceptor: AnsiString;
  l_email: AnsiString;
  l_EstadoEmisor: AnsiString;
  l_EstadoExpedidoEn: AnsiString;
  l_EstadoReceptor: AnsiString;
  l_FacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes;
  l_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  l_Fecha: AnsiString;
  l_Folio: AnsiString;
  l_FormaPago: AnsiString;
  l_LocalidadExpedidoEn: AnsiString;
  l_LocalidadReceptor: AnsiString;
  l_MunicipioEmisor: AnsiString;
  l_MunicipioExpedidoEn: AnsiString;
  l_MunicipioReceptor: AnsiString;
  l_noAprobacion: AnsiString;
  l_NoExteriorEm: AnsiString;
  l_NoExteriorExpedidaEn: AnsiString;
  l_NoExteriorReceptor: AnsiString;
  l_NombreReceptor: AnsiString;
  l_NomEmpEmisor: AnsiString;
  l_PaisEmisor: AnsiString;
  l_PaisExpedidoEn: AnsiString;
  l_PaisReceptor: AnsiString;
  l_RFCEmisor: AnsiString;
  l_RFCReceptor: AnsiString;
  l_SubTotal: AnsiString;
  l_Sucursal: Boolean;
  l_tipoComprobante: AnsiString;
  l_Total: AnsiString;
  l_TotalImpuesto: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Folio', l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadAnsiString('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_noAprobacion := Self.noAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('noAprobacion', l_noAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'noAprobacion']));
    end;
    Self.noAprobacion := l_noAprobacion;
    l_tipoComprobante := Self.tipoComprobante;
    try
      TROSerializer(ASerializer).ReadAnsiString('tipoComprobante', l_tipoComprobante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'tipoComprobante']));
    end;
    Self.tipoComprobante := l_tipoComprobante;
    l_anoAprobacion := Self.anoAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('anoAprobacion', l_anoAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'anoAprobacion']));
    end;
    Self.anoAprobacion := l_anoAprobacion;
    l_FormaPago := Self.FormaPago;
    try
      TROSerializer(ASerializer).ReadAnsiString('FormaPago', l_FormaPago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPago']));
    end;
    Self.FormaPago := l_FormaPago;
    l_SubTotal := Self.SubTotal;
    try
      TROSerializer(ASerializer).ReadAnsiString('SubTotal', l_SubTotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SubTotal']));
    end;
    Self.SubTotal := l_SubTotal;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadAnsiString('Total', l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_RFCEmisor := Self.RFCEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCEmisor', l_RFCEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCEmisor']));
    end;
    Self.RFCEmisor := l_RFCEmisor;
    l_NomEmpEmisor := Self.NomEmpEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NomEmpEmisor']));
    end;
    Self.NomEmpEmisor := l_NomEmpEmisor;
    l_DireccionEm := Self.DireccionEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('DireccionEm', l_DireccionEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DireccionEm']));
    end;
    Self.DireccionEm := l_DireccionEm;
    l_NoExteriorEm := Self.NoExteriorEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorEm', l_NoExteriorEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorEm']));
    end;
    Self.NoExteriorEm := l_NoExteriorEm;
    l_ColoniaEmisor := Self.ColoniaEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaEmisor']));
    end;
    Self.ColoniaEmisor := l_ColoniaEmisor;
    l_MunicipioEmisor := Self.MunicipioEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioEmisor']));
    end;
    Self.MunicipioEmisor := l_MunicipioEmisor;
    l_EstadoEmisor := Self.EstadoEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoEmisor', l_EstadoEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoEmisor']));
    end;
    Self.EstadoEmisor := l_EstadoEmisor;
    l_PaisEmisor := Self.PaisEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisEmisor', l_PaisEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisEmisor']));
    end;
    Self.PaisEmisor := l_PaisEmisor;
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalEmisor']));
    end;
    Self.CodigoPostalEmisor := l_CodigoPostalEmisor;
    l_RFCReceptor := Self.RFCReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCReceptor', l_RFCReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCReceptor']));
    end;
    Self.RFCReceptor := l_RFCReceptor;
    l_NombreReceptor := Self.NombreReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreReceptor', l_NombreReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreReceptor']));
    end;
    Self.NombreReceptor := l_NombreReceptor;
    l_DomicilioReceptor := Self.DomicilioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DomicilioReceptor']));
    end;
    Self.DomicilioReceptor := l_DomicilioReceptor;
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorReceptor']));
    end;
    Self.NoExteriorReceptor := l_NoExteriorReceptor;
    l_ColoniaReceptor := Self.ColoniaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaReceptor']));
    end;
    Self.ColoniaReceptor := l_ColoniaReceptor;
    l_LocalidadReceptor := Self.LocalidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadReceptor']));
    end;
    Self.LocalidadReceptor := l_LocalidadReceptor;
    l_MunicipioReceptor := Self.MunicipioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioReceptor']));
    end;
    Self.MunicipioReceptor := l_MunicipioReceptor;
    l_EstadoReceptor := Self.EstadoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoReceptor', l_EstadoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoReceptor']));
    end;
    Self.EstadoReceptor := l_EstadoReceptor;
    l_PaisReceptor := Self.PaisReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisReceptor', l_PaisReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisReceptor']));
    end;
    Self.PaisReceptor := l_PaisReceptor;
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalReceptor']));
    end;
    Self.CodigoPostalReceptor := l_CodigoPostalReceptor;
    l_FacturaElectronicaDetalleImporte := int_FacturaElectronicaDetalleImporte;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImporte', ATFacturaElectronicaDetalleImportes, l_FacturaElectronicaDetalleImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImporte']));
    end;
    if int_FacturaElectronicaDetalleImporte <> l_FacturaElectronicaDetalleImporte then Self.FacturaElectronicaDetalleImporte.Free;
    Self.FacturaElectronicaDetalleImporte := l_FacturaElectronicaDetalleImporte;
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImpuestos', ATFacturaElectronicaDetalleImpuestos, l_FacturaElectronicaDetalleImpuestos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImpuestos']));
    end;
    if int_FacturaElectronicaDetalleImpuestos <> l_FacturaElectronicaDetalleImpuestos then Self.FacturaElectronicaDetalleImpuestos.Free;
    Self.FacturaElectronicaDetalleImpuestos := l_FacturaElectronicaDetalleImpuestos;
    l_TotalImpuesto := Self.TotalImpuesto;
    try
      TROSerializer(ASerializer).ReadAnsiString('TotalImpuesto', l_TotalImpuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalImpuesto']));
    end;
    Self.TotalImpuesto := l_TotalImpuesto;
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CalleExpedidoEn']));
    end;
    Self.CalleExpedidoEn := l_CalleExpedidoEn;
    l_NoExteriorExpedidaEn := Self.NoExteriorExpedidaEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorExpedidaEn', l_NoExteriorExpedidaEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorExpedidaEn']));
    end;
    Self.NoExteriorExpedidaEn := l_NoExteriorExpedidaEn;
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaExpedidoEn']));
    end;
    Self.ColoniaExpedidoEn := l_ColoniaExpedidoEn;
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalExpedidoEn']));
    end;
    Self.CodigoPostalExpedidoEn := l_CodigoPostalExpedidoEn;
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadExpedidoEn']));
    end;
    Self.LocalidadExpedidoEn := l_LocalidadExpedidoEn;
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioExpedidoEn']));
    end;
    Self.MunicipioExpedidoEn := l_MunicipioExpedidoEn;
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoExpedidoEn']));
    end;
    Self.EstadoExpedidoEn := l_EstadoExpedidoEn;
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisExpedidoEn']));
    end;
    Self.PaisExpedidoEn := l_PaisExpedidoEn;
    l_Sucursal := Self.Sucursal;
    try
      TROSerializer(ASerializer).ReadEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sucursal']));
    end;
    Self.Sucursal := l_Sucursal;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
  end
  else begin
    l_anoAprobacion := Self.anoAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('anoAprobacion', l_anoAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'anoAprobacion']));
    end;
    Self.anoAprobacion := l_anoAprobacion;
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CalleExpedidoEn']));
    end;
    Self.CalleExpedidoEn := l_CalleExpedidoEn;
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalEmisor']));
    end;
    Self.CodigoPostalEmisor := l_CodigoPostalEmisor;
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalExpedidoEn']));
    end;
    Self.CodigoPostalExpedidoEn := l_CodigoPostalExpedidoEn;
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoPostalReceptor']));
    end;
    Self.CodigoPostalReceptor := l_CodigoPostalReceptor;
    l_ColoniaEmisor := Self.ColoniaEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaEmisor']));
    end;
    Self.ColoniaEmisor := l_ColoniaEmisor;
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaExpedidoEn']));
    end;
    Self.ColoniaExpedidoEn := l_ColoniaExpedidoEn;
    l_ColoniaReceptor := Self.ColoniaReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ColoniaReceptor']));
    end;
    Self.ColoniaReceptor := l_ColoniaReceptor;
    l_DireccionEm := Self.DireccionEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('DireccionEm', l_DireccionEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DireccionEm']));
    end;
    Self.DireccionEm := l_DireccionEm;
    l_DomicilioReceptor := Self.DomicilioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DomicilioReceptor']));
    end;
    Self.DomicilioReceptor := l_DomicilioReceptor;
    l_email := Self.email;
    try
      TROSerializer(ASerializer).ReadAnsiString('email', l_email);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'email']));
    end;
    Self.email := l_email;
    l_EstadoEmisor := Self.EstadoEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoEmisor', l_EstadoEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoEmisor']));
    end;
    Self.EstadoEmisor := l_EstadoEmisor;
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoExpedidoEn']));
    end;
    Self.EstadoExpedidoEn := l_EstadoExpedidoEn;
    l_EstadoReceptor := Self.EstadoReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('EstadoReceptor', l_EstadoReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoReceptor']));
    end;
    Self.EstadoReceptor := l_EstadoReceptor;
    l_FacturaElectronicaDetalleImporte := int_FacturaElectronicaDetalleImporte;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImporte', ATFacturaElectronicaDetalleImportes, l_FacturaElectronicaDetalleImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImporte']));
    end;
    if int_FacturaElectronicaDetalleImporte <> l_FacturaElectronicaDetalleImporte then FacturaElectronicaDetalleImporte.Free;
    Self.FacturaElectronicaDetalleImporte := l_FacturaElectronicaDetalleImporte;
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    try
      TROSerializer(ASerializer).ReadArray('FacturaElectronicaDetalleImpuestos', ATFacturaElectronicaDetalleImpuestos, l_FacturaElectronicaDetalleImpuestos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaElectronicaDetalleImpuestos']));
    end;
    if int_FacturaElectronicaDetalleImpuestos <> l_FacturaElectronicaDetalleImpuestos then FacturaElectronicaDetalleImpuestos.Free;
    Self.FacturaElectronicaDetalleImpuestos := l_FacturaElectronicaDetalleImpuestos;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadAnsiString('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Folio := Self.Folio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Folio', l_Folio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Folio']));
    end;
    Self.Folio := l_Folio;
    l_FormaPago := Self.FormaPago;
    try
      TROSerializer(ASerializer).ReadAnsiString('FormaPago', l_FormaPago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaPago']));
    end;
    Self.FormaPago := l_FormaPago;
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadExpedidoEn']));
    end;
    Self.LocalidadExpedidoEn := l_LocalidadExpedidoEn;
    l_LocalidadReceptor := Self.LocalidadReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LocalidadReceptor']));
    end;
    Self.LocalidadReceptor := l_LocalidadReceptor;
    l_MunicipioEmisor := Self.MunicipioEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioEmisor']));
    end;
    Self.MunicipioEmisor := l_MunicipioEmisor;
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioExpedidoEn']));
    end;
    Self.MunicipioExpedidoEn := l_MunicipioExpedidoEn;
    l_MunicipioReceptor := Self.MunicipioReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MunicipioReceptor']));
    end;
    Self.MunicipioReceptor := l_MunicipioReceptor;
    l_noAprobacion := Self.noAprobacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('noAprobacion', l_noAprobacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'noAprobacion']));
    end;
    Self.noAprobacion := l_noAprobacion;
    l_NoExteriorEm := Self.NoExteriorEm;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorEm', l_NoExteriorEm);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorEm']));
    end;
    Self.NoExteriorEm := l_NoExteriorEm;
    l_NoExteriorExpedidaEn := Self.NoExteriorExpedidaEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorExpedidaEn', l_NoExteriorExpedidaEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorExpedidaEn']));
    end;
    Self.NoExteriorExpedidaEn := l_NoExteriorExpedidaEn;
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoExteriorReceptor']));
    end;
    Self.NoExteriorReceptor := l_NoExteriorReceptor;
    l_NombreReceptor := Self.NombreReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreReceptor', l_NombreReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreReceptor']));
    end;
    Self.NombreReceptor := l_NombreReceptor;
    l_NomEmpEmisor := Self.NomEmpEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NomEmpEmisor']));
    end;
    Self.NomEmpEmisor := l_NomEmpEmisor;
    l_PaisEmisor := Self.PaisEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisEmisor', l_PaisEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisEmisor']));
    end;
    Self.PaisEmisor := l_PaisEmisor;
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisExpedidoEn']));
    end;
    Self.PaisExpedidoEn := l_PaisExpedidoEn;
    l_PaisReceptor := Self.PaisReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('PaisReceptor', l_PaisReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PaisReceptor']));
    end;
    Self.PaisReceptor := l_PaisReceptor;
    l_RFCEmisor := Self.RFCEmisor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCEmisor', l_RFCEmisor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCEmisor']));
    end;
    Self.RFCEmisor := l_RFCEmisor;
    l_RFCReceptor := Self.RFCReceptor;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFCReceptor', l_RFCReceptor);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFCReceptor']));
    end;
    Self.RFCReceptor := l_RFCReceptor;
    l_SubTotal := Self.SubTotal;
    try
      TROSerializer(ASerializer).ReadAnsiString('SubTotal', l_SubTotal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SubTotal']));
    end;
    Self.SubTotal := l_SubTotal;
    l_Sucursal := Self.Sucursal;
    try
      TROSerializer(ASerializer).ReadEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Sucursal']));
    end;
    Self.Sucursal := l_Sucursal;
    l_tipoComprobante := Self.tipoComprobante;
    try
      TROSerializer(ASerializer).ReadAnsiString('tipoComprobante', l_tipoComprobante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'tipoComprobante']));
    end;
    Self.tipoComprobante := l_tipoComprobante;
    l_Total := Self.Total;
    try
      TROSerializer(ASerializer).ReadAnsiString('Total', l_Total);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Total']));
    end;
    Self.Total := l_Total;
    l_TotalImpuesto := Self.TotalImpuesto;
    try
      TROSerializer(ASerializer).ReadAnsiString('TotalImpuesto', l_TotalImpuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalImpuesto']));
    end;
    Self.TotalImpuesto := l_TotalImpuesto;
  end;
end;

procedure TDataFacturaElectronicaF.WriteComplex(ASerializer: TObject);
var
  l_anoAprobacion: AnsiString;
  l_CalleExpedidoEn: AnsiString;
  l_CodigoPostalEmisor: AnsiString;
  l_CodigoPostalExpedidoEn: AnsiString;
  l_CodigoPostalReceptor: AnsiString;
  l_ColoniaEmisor: AnsiString;
  l_ColoniaExpedidoEn: AnsiString;
  l_ColoniaReceptor: AnsiString;
  l_DireccionEm: AnsiString;
  l_DomicilioReceptor: AnsiString;
  l_email: AnsiString;
  l_EstadoEmisor: AnsiString;
  l_EstadoExpedidoEn: AnsiString;
  l_EstadoReceptor: AnsiString;
  l_FacturaElectronicaDetalleImporte: ATFacturaElectronicaDetalleImportes;
  l_FacturaElectronicaDetalleImpuestos: ATFacturaElectronicaDetalleImpuestos;
  l_Fecha: AnsiString;
  l_Folio: AnsiString;
  l_FormaPago: AnsiString;
  l_LocalidadExpedidoEn: AnsiString;
  l_LocalidadReceptor: AnsiString;
  l_MunicipioEmisor: AnsiString;
  l_MunicipioExpedidoEn: AnsiString;
  l_MunicipioReceptor: AnsiString;
  l_noAprobacion: AnsiString;
  l_NoExteriorEm: AnsiString;
  l_NoExteriorExpedidaEn: AnsiString;
  l_NoExteriorReceptor: AnsiString;
  l_NombreReceptor: AnsiString;
  l_NomEmpEmisor: AnsiString;
  l_PaisEmisor: AnsiString;
  l_PaisExpedidoEn: AnsiString;
  l_PaisReceptor: AnsiString;
  l_RFCEmisor: AnsiString;
  l_RFCReceptor: AnsiString;
  l_SubTotal: AnsiString;
  l_Sucursal: Boolean;
  l_tipoComprobante: AnsiString;
  l_Total: AnsiString;
  l_TotalImpuesto: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDataFacturaElectronicaF);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteAnsiString('Folio', l_Folio);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteAnsiString('Fecha', l_Fecha);
    l_noAprobacion := Self.noAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('noAprobacion', l_noAprobacion);
    l_tipoComprobante := Self.tipoComprobante;
    TROSerializer(ASerializer).WriteAnsiString('tipoComprobante', l_tipoComprobante);
    l_anoAprobacion := Self.anoAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('anoAprobacion', l_anoAprobacion);
    l_FormaPago := Self.FormaPago;
    TROSerializer(ASerializer).WriteAnsiString('FormaPago', l_FormaPago);
    l_SubTotal := Self.SubTotal;
    TROSerializer(ASerializer).WriteAnsiString('SubTotal', l_SubTotal);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteAnsiString('Total', l_Total);
    l_RFCEmisor := Self.RFCEmisor;
    TROSerializer(ASerializer).WriteAnsiString('RFCEmisor', l_RFCEmisor);
    l_NomEmpEmisor := Self.NomEmpEmisor;
    TROSerializer(ASerializer).WriteAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    l_DireccionEm := Self.DireccionEm;
    TROSerializer(ASerializer).WriteAnsiString('DireccionEm', l_DireccionEm);
    l_NoExteriorEm := Self.NoExteriorEm;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorEm', l_NoExteriorEm);
    l_ColoniaEmisor := Self.ColoniaEmisor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    l_MunicipioEmisor := Self.MunicipioEmisor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    l_EstadoEmisor := Self.EstadoEmisor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoEmisor', l_EstadoEmisor);
    l_PaisEmisor := Self.PaisEmisor;
    TROSerializer(ASerializer).WriteAnsiString('PaisEmisor', l_PaisEmisor);
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    l_RFCReceptor := Self.RFCReceptor;
    TROSerializer(ASerializer).WriteAnsiString('RFCReceptor', l_RFCReceptor);
    l_NombreReceptor := Self.NombreReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NombreReceptor', l_NombreReceptor);
    l_DomicilioReceptor := Self.DomicilioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    l_ColoniaReceptor := Self.ColoniaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    l_LocalidadReceptor := Self.LocalidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    l_MunicipioReceptor := Self.MunicipioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    l_EstadoReceptor := Self.EstadoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoReceptor', l_EstadoReceptor);
    l_PaisReceptor := Self.PaisReceptor;
    TROSerializer(ASerializer).WriteAnsiString('PaisReceptor', l_PaisReceptor);
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    l_FacturaElectronicaDetalleImporte := int_FacturaElectronicaDetalleImporte;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImporte', l_FacturaElectronicaDetalleImporte, ATFacturaElectronicaDetalleImportes);
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImpuestos', l_FacturaElectronicaDetalleImpuestos, ATFacturaElectronicaDetalleImpuestos);
    l_TotalImpuesto := Self.TotalImpuesto;
    TROSerializer(ASerializer).WriteAnsiString('TotalImpuesto', l_TotalImpuesto);
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    l_NoExteriorExpedidaEn := Self.NoExteriorExpedidaEn;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorExpedidaEn', l_NoExteriorExpedidaEn);
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    l_Sucursal := Self.Sucursal;
    TROSerializer(ASerializer).WriteEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
  end
  else begin
    l_anoAprobacion := Self.anoAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('anoAprobacion', l_anoAprobacion);
    l_CalleExpedidoEn := Self.CalleExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CalleExpedidoEn', l_CalleExpedidoEn);
    l_CodigoPostalEmisor := Self.CodigoPostalEmisor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalEmisor', l_CodigoPostalEmisor);
    l_CodigoPostalExpedidoEn := Self.CodigoPostalExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalExpedidoEn', l_CodigoPostalExpedidoEn);
    l_CodigoPostalReceptor := Self.CodigoPostalReceptor;
    TROSerializer(ASerializer).WriteAnsiString('CodigoPostalReceptor', l_CodigoPostalReceptor);
    l_ColoniaEmisor := Self.ColoniaEmisor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaEmisor', l_ColoniaEmisor);
    l_ColoniaExpedidoEn := Self.ColoniaExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaExpedidoEn', l_ColoniaExpedidoEn);
    l_ColoniaReceptor := Self.ColoniaReceptor;
    TROSerializer(ASerializer).WriteAnsiString('ColoniaReceptor', l_ColoniaReceptor);
    l_DireccionEm := Self.DireccionEm;
    TROSerializer(ASerializer).WriteAnsiString('DireccionEm', l_DireccionEm);
    l_DomicilioReceptor := Self.DomicilioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('DomicilioReceptor', l_DomicilioReceptor);
    l_email := Self.email;
    TROSerializer(ASerializer).WriteAnsiString('email', l_email);
    l_EstadoEmisor := Self.EstadoEmisor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoEmisor', l_EstadoEmisor);
    l_EstadoExpedidoEn := Self.EstadoExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('EstadoExpedidoEn', l_EstadoExpedidoEn);
    l_EstadoReceptor := Self.EstadoReceptor;
    TROSerializer(ASerializer).WriteAnsiString('EstadoReceptor', l_EstadoReceptor);
    l_FacturaElectronicaDetalleImporte := int_FacturaElectronicaDetalleImporte;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImporte', l_FacturaElectronicaDetalleImporte, ATFacturaElectronicaDetalleImportes);
    l_FacturaElectronicaDetalleImpuestos := int_FacturaElectronicaDetalleImpuestos;
    TROSerializer(ASerializer).WriteArray('FacturaElectronicaDetalleImpuestos', l_FacturaElectronicaDetalleImpuestos, ATFacturaElectronicaDetalleImpuestos);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteAnsiString('Fecha', l_Fecha);
    l_Folio := Self.Folio;
    TROSerializer(ASerializer).WriteAnsiString('Folio', l_Folio);
    l_FormaPago := Self.FormaPago;
    TROSerializer(ASerializer).WriteAnsiString('FormaPago', l_FormaPago);
    l_LocalidadExpedidoEn := Self.LocalidadExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadExpedidoEn', l_LocalidadExpedidoEn);
    l_LocalidadReceptor := Self.LocalidadReceptor;
    TROSerializer(ASerializer).WriteAnsiString('LocalidadReceptor', l_LocalidadReceptor);
    l_MunicipioEmisor := Self.MunicipioEmisor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioEmisor', l_MunicipioEmisor);
    l_MunicipioExpedidoEn := Self.MunicipioExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioExpedidoEn', l_MunicipioExpedidoEn);
    l_MunicipioReceptor := Self.MunicipioReceptor;
    TROSerializer(ASerializer).WriteAnsiString('MunicipioReceptor', l_MunicipioReceptor);
    l_noAprobacion := Self.noAprobacion;
    TROSerializer(ASerializer).WriteAnsiString('noAprobacion', l_noAprobacion);
    l_NoExteriorEm := Self.NoExteriorEm;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorEm', l_NoExteriorEm);
    l_NoExteriorExpedidaEn := Self.NoExteriorExpedidaEn;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorExpedidaEn', l_NoExteriorExpedidaEn);
    l_NoExteriorReceptor := Self.NoExteriorReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NoExteriorReceptor', l_NoExteriorReceptor);
    l_NombreReceptor := Self.NombreReceptor;
    TROSerializer(ASerializer).WriteAnsiString('NombreReceptor', l_NombreReceptor);
    l_NomEmpEmisor := Self.NomEmpEmisor;
    TROSerializer(ASerializer).WriteAnsiString('NomEmpEmisor', l_NomEmpEmisor);
    l_PaisEmisor := Self.PaisEmisor;
    TROSerializer(ASerializer).WriteAnsiString('PaisEmisor', l_PaisEmisor);
    l_PaisExpedidoEn := Self.PaisExpedidoEn;
    TROSerializer(ASerializer).WriteAnsiString('PaisExpedidoEn', l_PaisExpedidoEn);
    l_PaisReceptor := Self.PaisReceptor;
    TROSerializer(ASerializer).WriteAnsiString('PaisReceptor', l_PaisReceptor);
    l_RFCEmisor := Self.RFCEmisor;
    TROSerializer(ASerializer).WriteAnsiString('RFCEmisor', l_RFCEmisor);
    l_RFCReceptor := Self.RFCReceptor;
    TROSerializer(ASerializer).WriteAnsiString('RFCReceptor', l_RFCReceptor);
    l_SubTotal := Self.SubTotal;
    TROSerializer(ASerializer).WriteAnsiString('SubTotal', l_SubTotal);
    l_Sucursal := Self.Sucursal;
    TROSerializer(ASerializer).WriteEnumerated('Sucursal',System.TypeInfo(boolean), l_Sucursal);
    l_tipoComprobante := Self.tipoComprobante;
    TROSerializer(ASerializer).WriteAnsiString('tipoComprobante', l_tipoComprobante);
    l_Total := Self.Total;
    TROSerializer(ASerializer).WriteAnsiString('Total', l_Total);
    l_TotalImpuesto := Self.TotalImpuesto;
    TROSerializer(ASerializer).WriteAnsiString('TotalImpuesto', l_TotalImpuesto);
  end;
end;

{ TDataFacturaElectronicaFCollection }
constructor TDataFacturaElectronicaFCollection.Create;
begin
  inherited Create(TDataFacturaElectronicaF);
end;

constructor TDataFacturaElectronicaFCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDataFacturaElectronicaFCollection.Add: TDataFacturaElectronicaF;
begin
  Result := TDataFacturaElectronicaF(inherited Add);
end;

function TDataFacturaElectronicaFCollection.GetItems(aIndex: Integer): TDataFacturaElectronicaF;
begin
  Result := TDataFacturaElectronicaF(inherited Items[aIndex]);
end;

procedure TDataFacturaElectronicaFCollection.SetItems(aIndex: Integer; const Value: TDataFacturaElectronicaF);
begin
  TDataFacturaElectronicaF(inherited Items[aIndex]).Assign(Value);
end;

{ TGeneraCupon }

procedure TGeneraCupon.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TGeneraCupon;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TGeneraCupon) then begin
    lSource := LibraryValesCupones_Intf.TGeneraCupon(iSource);

    Self.Cantidad := lSource.Cantidad;
    Self.Denominacion := lSource.Denominacion;
    Self.Referencia := lSource.Referencia;
  end;
end;

procedure TGeneraCupon.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Integer;
  l_Denominacion: Double;
  l_Referencia: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Denominacion := Self.Denominacion;
    try
      TROSerializer(ASerializer).ReadDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Denominacion']));
    end;
    Self.Denominacion := l_Denominacion;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Denominacion := Self.Denominacion;
    try
      TROSerializer(ASerializer).ReadDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Denominacion']));
    end;
    Self.Denominacion := l_Denominacion;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
  end;
end;

procedure TGeneraCupon.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Integer;
  l_Denominacion: Double;
  l_Referencia: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TGeneraCupon);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    l_Denominacion := Self.Denominacion;
    TROSerializer(ASerializer).WriteDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteInteger('Cantidad', TypInfo.otSLong, l_Cantidad);
    l_Denominacion := Self.Denominacion;
    TROSerializer(ASerializer).WriteDouble('Denominacion', TypInfo.ftDouble, l_Denominacion);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
  end;
end;

{ TGeneraCuponCollection }
constructor TGeneraCuponCollection.Create;
begin
  inherited Create(TGeneraCupon);
end;

constructor TGeneraCuponCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TGeneraCuponCollection.Add: TGeneraCupon;
begin
  Result := TGeneraCupon(inherited Add);
end;

function TGeneraCuponCollection.GetItems(aIndex: Integer): TGeneraCupon;
begin
  Result := TGeneraCupon(inherited Items[aIndex]);
end;

procedure TGeneraCuponCollection.LoadFromArray(anArray: AGeneraCupon);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TGeneraCuponCollection.SaveToArray(anArray: AGeneraCupon);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TGeneraCupon(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TGeneraCuponCollection.SetItems(aIndex: Integer; const Value: TGeneraCupon);
begin
  TGeneraCupon(inherited Items[aIndex]).Assign(Value);
end;

{ TDocumentosConSaldo }

procedure TDocumentosConSaldo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDocumentosConSaldo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDocumentosConSaldo) then begin
    lSource := LibraryValesCupones_Intf.TDocumentosConSaldo(iSource);

    Self.Referencia := lSource.Referencia;
    Self.Importe := lSource.Importe;
    Self.Saldo := lSource.Saldo;
    Self.Fecha := lSource.Fecha;
    Self.Vencimiento := lSource.Vencimiento;
    Self.DocumentoID := lSource.DocumentoID;
    Self.EnRecibo := lSource.EnRecibo;
    Self.MovimientoID := lSource.MovimientoID;
  end;
end;

procedure TDocumentosConSaldo.ReadComplex(ASerializer: TObject);
var
  l_DocumentoID: Integer;
  l_EnRecibo: Double;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_MovimientoID: Integer;
  l_Referencia: AnsiString;
  l_Saldo: Double;
  l_Vencimiento: DateTime;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Vencimiento := Self.Vencimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('Vencimiento', l_Vencimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Vencimiento']));
    end;
    Self.Vencimiento := l_Vencimiento;
    l_DocumentoID := Self.DocumentoID;
    try
      TROSerializer(ASerializer).ReadInteger('DocumentoID', TypInfo.otSLong, l_DocumentoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DocumentoID']));
    end;
    Self.DocumentoID := l_DocumentoID;
    l_EnRecibo := Self.EnRecibo;
    try
      TROSerializer(ASerializer).ReadDouble('EnRecibo', TypInfo.ftDouble, l_EnRecibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EnRecibo']));
    end;
    Self.EnRecibo := l_EnRecibo;
    l_MovimientoID := Self.MovimientoID;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoID']));
    end;
    Self.MovimientoID := l_MovimientoID;
  end
  else begin
    l_DocumentoID := Self.DocumentoID;
    try
      TROSerializer(ASerializer).ReadInteger('DocumentoID', TypInfo.otSLong, l_DocumentoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DocumentoID']));
    end;
    Self.DocumentoID := l_DocumentoID;
    l_EnRecibo := Self.EnRecibo;
    try
      TROSerializer(ASerializer).ReadDouble('EnRecibo', TypInfo.ftDouble, l_EnRecibo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EnRecibo']));
    end;
    Self.EnRecibo := l_EnRecibo;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_MovimientoID := Self.MovimientoID;
    try
      TROSerializer(ASerializer).ReadInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MovimientoID']));
    end;
    Self.MovimientoID := l_MovimientoID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Vencimiento := Self.Vencimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('Vencimiento', l_Vencimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Vencimiento']));
    end;
    Self.Vencimiento := l_Vencimiento;
  end;
end;

procedure TDocumentosConSaldo.WriteComplex(ASerializer: TObject);
var
  l_DocumentoID: Integer;
  l_EnRecibo: Double;
  l_Fecha: DateTime;
  l_Importe: Double;
  l_MovimientoID: Integer;
  l_Referencia: AnsiString;
  l_Saldo: Double;
  l_Vencimiento: DateTime;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDocumentosConSaldo);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Vencimiento := Self.Vencimiento;
    TROSerializer(ASerializer).WriteDateTime('Vencimiento', l_Vencimiento);
    l_DocumentoID := Self.DocumentoID;
    TROSerializer(ASerializer).WriteInteger('DocumentoID', TypInfo.otSLong, l_DocumentoID);
    l_EnRecibo := Self.EnRecibo;
    TROSerializer(ASerializer).WriteDouble('EnRecibo', TypInfo.ftDouble, l_EnRecibo);
    l_MovimientoID := Self.MovimientoID;
    TROSerializer(ASerializer).WriteInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
  end
  else begin
    l_DocumentoID := Self.DocumentoID;
    TROSerializer(ASerializer).WriteInteger('DocumentoID', TypInfo.otSLong, l_DocumentoID);
    l_EnRecibo := Self.EnRecibo;
    TROSerializer(ASerializer).WriteDouble('EnRecibo', TypInfo.ftDouble, l_EnRecibo);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_MovimientoID := Self.MovimientoID;
    TROSerializer(ASerializer).WriteInteger('MovimientoID', TypInfo.otSLong, l_MovimientoID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Vencimiento := Self.Vencimiento;
    TROSerializer(ASerializer).WriteDateTime('Vencimiento', l_Vencimiento);
  end;
end;

{ TDocumentosConSaldoCollection }
constructor TDocumentosConSaldoCollection.Create;
begin
  inherited Create(TDocumentosConSaldo);
end;

constructor TDocumentosConSaldoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDocumentosConSaldoCollection.Add: TDocumentosConSaldo;
begin
  Result := TDocumentosConSaldo(inherited Add);
end;

function TDocumentosConSaldoCollection.GetItems(aIndex: Integer): TDocumentosConSaldo;
begin
  Result := TDocumentosConSaldo(inherited Items[aIndex]);
end;

procedure TDocumentosConSaldoCollection.LoadFromArray(anArray: TDocumentosConSaldoArray);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDocumentosConSaldoCollection.SaveToArray(anArray: TDocumentosConSaldoArray);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDocumentosConSaldo(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDocumentosConSaldoCollection.SetItems(aIndex: Integer; const Value: TDocumentosConSaldo);
begin
  TDocumentosConSaldo(inherited Items[aIndex]).Assign(Value);
end;

{ TAutoriza }

procedure TAutoriza.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TAutoriza;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TAutoriza) then begin
    lSource := LibraryValesCupones_Intf.TAutoriza(iSource);

    Self.EstacionID := lSource.EstacionID;
    Self.Referencia := lSource.Referencia;
    Self.DigitoSeguridad := lSource.DigitoSeguridad;
    Self.PosicionCarga := lSource.PosicionCarga;
    Self.Terminal := lSource.Terminal;
    Self.NIP := lSource.NIP;
    Self.Odometro := lSource.Odometro;
    Self.Magna := lSource.Magna;
    Self.Premium := lSource.Premium;
    Self.Diesel := lSource.Diesel;
    Self.SeleccionDeProducto := lSource.SeleccionDeProducto;
    Self.AutorizacionID := lSource.AutorizacionID;
    Self.GasolineroID := lSource.GasolineroID;
    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.Cantidad := lSource.Cantidad;
    Self.ImporteCantidad := lSource.ImporteCantidad;
    Self.Fecha := lSource.Fecha;
    Self.Importe := lSource.Importe;
    Self.Tipo := lSource.Tipo;
    Self.PresetMN := lSource.PresetMN;
    Self.PresetLT := lSource.PresetLT;
    Self.KMS := lSource.KMS;
  end;
end;

procedure TAutoriza.ReadComplex(ASerializer: TObject);
var
  l_AutorizacionID: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_Diesel: Double;
  l_DigitoSeguridad: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_GasolineroID: Integer;
  l_Importe: Double;
  l_ImporteCantidad: AnsiString;
  l_KMS: Integer;
  l_Magna: Double;
  l_NIP: AnsiString;
  l_Odometro: Integer;
  l_PosicionCarga: Integer;
  l_Premium: Double;
  l_PresetLT: Double;
  l_PresetMN: Double;
  l_Referencia: AnsiString;
  l_SeleccionDeProducto: AnsiString;
  l_Terminal: AnsiString;
  l_Tipo: Integer;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_DigitoSeguridad := Self.DigitoSeguridad;
    try
      TROSerializer(ASerializer).ReadInteger('DigitoSeguridad', TypInfo.otSLong, l_DigitoSeguridad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DigitoSeguridad']));
    end;
    Self.DigitoSeguridad := l_DigitoSeguridad;
    l_PosicionCarga := Self.PosicionCarga;
    try
      TROSerializer(ASerializer).ReadInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PosicionCarga']));
    end;
    Self.PosicionCarga := l_PosicionCarga;
    l_Terminal := Self.Terminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('Terminal', l_Terminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Terminal']));
    end;
    Self.Terminal := l_Terminal;
    l_NIP := Self.NIP;
    try
      TROSerializer(ASerializer).ReadAnsiString('NIP', l_NIP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NIP']));
    end;
    Self.NIP := l_NIP;
    l_Odometro := Self.Odometro;
    try
      TROSerializer(ASerializer).ReadInteger('Odometro', TypInfo.otSLong, l_Odometro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Odometro']));
    end;
    Self.Odometro := l_Odometro;
    l_Magna := Self.Magna;
    try
      TROSerializer(ASerializer).ReadDouble('Magna', TypInfo.ftDouble, l_Magna);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Magna']));
    end;
    Self.Magna := l_Magna;
    l_Premium := Self.Premium;
    try
      TROSerializer(ASerializer).ReadDouble('Premium', TypInfo.ftDouble, l_Premium);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Premium']));
    end;
    Self.Premium := l_Premium;
    l_Diesel := Self.Diesel;
    try
      TROSerializer(ASerializer).ReadDouble('Diesel', TypInfo.ftDouble, l_Diesel);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diesel']));
    end;
    Self.Diesel := l_Diesel;
    l_SeleccionDeProducto := Self.SeleccionDeProducto;
    try
      TROSerializer(ASerializer).ReadAnsiString('SeleccionDeProducto', l_SeleccionDeProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SeleccionDeProducto']));
    end;
    Self.SeleccionDeProducto := l_SeleccionDeProducto;
    l_AutorizacionID := Self.AutorizacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AutorizacionID', TypInfo.otSLong, l_AutorizacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionID']));
    end;
    Self.AutorizacionID := l_AutorizacionID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_ImporteCantidad := Self.ImporteCantidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteCantidad', l_ImporteCantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteCantidad']));
    end;
    Self.ImporteCantidad := l_ImporteCantidad;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_PresetMN := Self.PresetMN;
    try
      TROSerializer(ASerializer).ReadDouble('PresetMN', TypInfo.ftDouble, l_PresetMN);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PresetMN']));
    end;
    Self.PresetMN := l_PresetMN;
    l_PresetLT := Self.PresetLT;
    try
      TROSerializer(ASerializer).ReadDouble('PresetLT', TypInfo.ftDouble, l_PresetLT);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PresetLT']));
    end;
    Self.PresetLT := l_PresetLT;
    l_KMS := Self.KMS;
    try
      TROSerializer(ASerializer).ReadInteger('KMS', TypInfo.otSLong, l_KMS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'KMS']));
    end;
    Self.KMS := l_KMS;
  end
  else begin
    l_AutorizacionID := Self.AutorizacionID;
    try
      TROSerializer(ASerializer).ReadInteger('AutorizacionID', TypInfo.otSLong, l_AutorizacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionID']));
    end;
    Self.AutorizacionID := l_AutorizacionID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Diesel := Self.Diesel;
    try
      TROSerializer(ASerializer).ReadDouble('Diesel', TypInfo.ftDouble, l_Diesel);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Diesel']));
    end;
    Self.Diesel := l_Diesel;
    l_DigitoSeguridad := Self.DigitoSeguridad;
    try
      TROSerializer(ASerializer).ReadInteger('DigitoSeguridad', TypInfo.otSLong, l_DigitoSeguridad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DigitoSeguridad']));
    end;
    Self.DigitoSeguridad := l_DigitoSeguridad;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_ImporteCantidad := Self.ImporteCantidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('ImporteCantidad', l_ImporteCantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteCantidad']));
    end;
    Self.ImporteCantidad := l_ImporteCantidad;
    l_KMS := Self.KMS;
    try
      TROSerializer(ASerializer).ReadInteger('KMS', TypInfo.otSLong, l_KMS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'KMS']));
    end;
    Self.KMS := l_KMS;
    l_Magna := Self.Magna;
    try
      TROSerializer(ASerializer).ReadDouble('Magna', TypInfo.ftDouble, l_Magna);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Magna']));
    end;
    Self.Magna := l_Magna;
    l_NIP := Self.NIP;
    try
      TROSerializer(ASerializer).ReadAnsiString('NIP', l_NIP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NIP']));
    end;
    Self.NIP := l_NIP;
    l_Odometro := Self.Odometro;
    try
      TROSerializer(ASerializer).ReadInteger('Odometro', TypInfo.otSLong, l_Odometro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Odometro']));
    end;
    Self.Odometro := l_Odometro;
    l_PosicionCarga := Self.PosicionCarga;
    try
      TROSerializer(ASerializer).ReadInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PosicionCarga']));
    end;
    Self.PosicionCarga := l_PosicionCarga;
    l_Premium := Self.Premium;
    try
      TROSerializer(ASerializer).ReadDouble('Premium', TypInfo.ftDouble, l_Premium);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Premium']));
    end;
    Self.Premium := l_Premium;
    l_PresetLT := Self.PresetLT;
    try
      TROSerializer(ASerializer).ReadDouble('PresetLT', TypInfo.ftDouble, l_PresetLT);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PresetLT']));
    end;
    Self.PresetLT := l_PresetLT;
    l_PresetMN := Self.PresetMN;
    try
      TROSerializer(ASerializer).ReadDouble('PresetMN', TypInfo.ftDouble, l_PresetMN);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PresetMN']));
    end;
    Self.PresetMN := l_PresetMN;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_SeleccionDeProducto := Self.SeleccionDeProducto;
    try
      TROSerializer(ASerializer).ReadAnsiString('SeleccionDeProducto', l_SeleccionDeProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SeleccionDeProducto']));
    end;
    Self.SeleccionDeProducto := l_SeleccionDeProducto;
    l_Terminal := Self.Terminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('Terminal', l_Terminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Terminal']));
    end;
    Self.Terminal := l_Terminal;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TAutoriza.WriteComplex(ASerializer: TObject);
var
  l_AutorizacionID: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_Diesel: Double;
  l_DigitoSeguridad: Integer;
  l_EstacionID: Integer;
  l_Fecha: DateTime;
  l_GasolineroID: Integer;
  l_Importe: Double;
  l_ImporteCantidad: AnsiString;
  l_KMS: Integer;
  l_Magna: Double;
  l_NIP: AnsiString;
  l_Odometro: Integer;
  l_PosicionCarga: Integer;
  l_Premium: Double;
  l_PresetLT: Double;
  l_PresetMN: Double;
  l_Referencia: AnsiString;
  l_SeleccionDeProducto: AnsiString;
  l_Terminal: AnsiString;
  l_Tipo: Integer;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TAutoriza);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_DigitoSeguridad := Self.DigitoSeguridad;
    TROSerializer(ASerializer).WriteInteger('DigitoSeguridad', TypInfo.otSLong, l_DigitoSeguridad);
    l_PosicionCarga := Self.PosicionCarga;
    TROSerializer(ASerializer).WriteInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    l_Terminal := Self.Terminal;
    TROSerializer(ASerializer).WriteAnsiString('Terminal', l_Terminal);
    l_NIP := Self.NIP;
    TROSerializer(ASerializer).WriteAnsiString('NIP', l_NIP);
    l_Odometro := Self.Odometro;
    TROSerializer(ASerializer).WriteInteger('Odometro', TypInfo.otSLong, l_Odometro);
    l_Magna := Self.Magna;
    TROSerializer(ASerializer).WriteDouble('Magna', TypInfo.ftDouble, l_Magna);
    l_Premium := Self.Premium;
    TROSerializer(ASerializer).WriteDouble('Premium', TypInfo.ftDouble, l_Premium);
    l_Diesel := Self.Diesel;
    TROSerializer(ASerializer).WriteDouble('Diesel', TypInfo.ftDouble, l_Diesel);
    l_SeleccionDeProducto := Self.SeleccionDeProducto;
    TROSerializer(ASerializer).WriteAnsiString('SeleccionDeProducto', l_SeleccionDeProducto);
    l_AutorizacionID := Self.AutorizacionID;
    TROSerializer(ASerializer).WriteInteger('AutorizacionID', TypInfo.otSLong, l_AutorizacionID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_ImporteCantidad := Self.ImporteCantidad;
    TROSerializer(ASerializer).WriteAnsiString('ImporteCantidad', l_ImporteCantidad);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
    l_PresetMN := Self.PresetMN;
    TROSerializer(ASerializer).WriteDouble('PresetMN', TypInfo.ftDouble, l_PresetMN);
    l_PresetLT := Self.PresetLT;
    TROSerializer(ASerializer).WriteDouble('PresetLT', TypInfo.ftDouble, l_PresetLT);
    l_KMS := Self.KMS;
    TROSerializer(ASerializer).WriteInteger('KMS', TypInfo.otSLong, l_KMS);
  end
  else begin
    l_AutorizacionID := Self.AutorizacionID;
    TROSerializer(ASerializer).WriteInteger('AutorizacionID', TypInfo.otSLong, l_AutorizacionID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Diesel := Self.Diesel;
    TROSerializer(ASerializer).WriteDouble('Diesel', TypInfo.ftDouble, l_Diesel);
    l_DigitoSeguridad := Self.DigitoSeguridad;
    TROSerializer(ASerializer).WriteInteger('DigitoSeguridad', TypInfo.otSLong, l_DigitoSeguridad);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_ImporteCantidad := Self.ImporteCantidad;
    TROSerializer(ASerializer).WriteAnsiString('ImporteCantidad', l_ImporteCantidad);
    l_KMS := Self.KMS;
    TROSerializer(ASerializer).WriteInteger('KMS', TypInfo.otSLong, l_KMS);
    l_Magna := Self.Magna;
    TROSerializer(ASerializer).WriteDouble('Magna', TypInfo.ftDouble, l_Magna);
    l_NIP := Self.NIP;
    TROSerializer(ASerializer).WriteAnsiString('NIP', l_NIP);
    l_Odometro := Self.Odometro;
    TROSerializer(ASerializer).WriteInteger('Odometro', TypInfo.otSLong, l_Odometro);
    l_PosicionCarga := Self.PosicionCarga;
    TROSerializer(ASerializer).WriteInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    l_Premium := Self.Premium;
    TROSerializer(ASerializer).WriteDouble('Premium', TypInfo.ftDouble, l_Premium);
    l_PresetLT := Self.PresetLT;
    TROSerializer(ASerializer).WriteDouble('PresetLT', TypInfo.ftDouble, l_PresetLT);
    l_PresetMN := Self.PresetMN;
    TROSerializer(ASerializer).WriteDouble('PresetMN', TypInfo.ftDouble, l_PresetMN);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_SeleccionDeProducto := Self.SeleccionDeProducto;
    TROSerializer(ASerializer).WriteAnsiString('SeleccionDeProducto', l_SeleccionDeProducto);
    l_Terminal := Self.Terminal;
    TROSerializer(ASerializer).WriteAnsiString('Terminal', l_Terminal);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TAutorizaCollection }
constructor TAutorizaCollection.Create;
begin
  inherited Create(TAutoriza);
end;

constructor TAutorizaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TAutorizaCollection.Add: TAutoriza;
begin
  Result := TAutoriza(inherited Add);
end;

function TAutorizaCollection.GetItems(aIndex: Integer): TAutoriza;
begin
  Result := TAutoriza(inherited Items[aIndex]);
end;

procedure TAutorizaCollection.SetItems(aIndex: Integer; const Value: TAutoriza);
begin
  TAutoriza(inherited Items[aIndex]).Assign(Value);
end;

{ TCuponValido }

procedure TCuponValido.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCuponValido;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCuponValido) then begin
    lSource := LibraryValesCupones_Intf.TCuponValido(iSource);

    Self.CuponID := lSource.CuponID;
    Self.Importe := lSource.Importe;
    Self.Status := lSource.Status;
    Self.Referencia := lSource.Referencia;
    Self.Nombre := lSource.Nombre;
    Self.RazonSocial := lSource.RazonSocial;
    Self.Domicilio := lSource.Domicilio;
    Self.RFC := lSource.RFC;
    Self.CuentaContable := lSource.CuentaContable;
    Self.TarjetaID := lSource.TarjetaID;
    Self.MsnError := lSource.MsnError;
    Self.Valido := lSource.Valido;
    Self.ClienteID := lSource.ClienteID;
    Self.GasolineroID := lSource.GasolineroID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.Grupo := lSource.Grupo;
  end;
end;

procedure TCuponValido.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_CuentaContable: AnsiString;
  l_CuponID: Integer;
  l_Domicilio: AnsiString;
  l_GasolineroID: Integer;
  l_Grupo: Integer;
  l_Importe: Double;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_RazonSocial: AnsiString;
  l_Referencia: AnsiString;
  l_RFC: AnsiString;
  l_Status: AnsiString;
  l_TarjetaID: Integer;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_CuponID := Self.CuponID;
    try
      TROSerializer(ASerializer).ReadInteger('CuponID', TypInfo.otSLong, l_CuponID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuponID']));
    end;
    Self.CuponID := l_CuponID;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_Domicilio := Self.Domicilio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Domicilio', l_Domicilio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Domicilio']));
    end;
    Self.Domicilio := l_Domicilio;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_TarjetaID := Self.TarjetaID;
    try
      TROSerializer(ASerializer).ReadInteger('TarjetaID', TypInfo.otSLong, l_TarjetaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TarjetaID']));
    end;
    Self.TarjetaID := l_TarjetaID;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadInteger('Grupo', TypInfo.otSLong, l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_CuponID := Self.CuponID;
    try
      TROSerializer(ASerializer).ReadInteger('CuponID', TypInfo.otSLong, l_CuponID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuponID']));
    end;
    Self.CuponID := l_CuponID;
    l_Domicilio := Self.Domicilio;
    try
      TROSerializer(ASerializer).ReadAnsiString('Domicilio', l_Domicilio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Domicilio']));
    end;
    Self.Domicilio := l_Domicilio;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadInteger('Grupo', TypInfo.otSLong, l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadAnsiString('Status', l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_TarjetaID := Self.TarjetaID;
    try
      TROSerializer(ASerializer).ReadInteger('TarjetaID', TypInfo.otSLong, l_TarjetaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TarjetaID']));
    end;
    Self.TarjetaID := l_TarjetaID;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TCuponValido.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_CuentaContable: AnsiString;
  l_CuponID: Integer;
  l_Domicilio: AnsiString;
  l_GasolineroID: Integer;
  l_Grupo: Integer;
  l_Importe: Double;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_RazonSocial: AnsiString;
  l_Referencia: AnsiString;
  l_RFC: AnsiString;
  l_Status: AnsiString;
  l_TarjetaID: Integer;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCuponValido);
    l_CuponID := Self.CuponID;
    TROSerializer(ASerializer).WriteInteger('CuponID', TypInfo.otSLong, l_CuponID);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_Domicilio := Self.Domicilio;
    TROSerializer(ASerializer).WriteAnsiString('Domicilio', l_Domicilio);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_TarjetaID := Self.TarjetaID;
    TROSerializer(ASerializer).WriteInteger('TarjetaID', TypInfo.otSLong, l_TarjetaID);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteInteger('Grupo', TypInfo.otSLong, l_Grupo);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_CuponID := Self.CuponID;
    TROSerializer(ASerializer).WriteInteger('CuponID', TypInfo.otSLong, l_CuponID);
    l_Domicilio := Self.Domicilio;
    TROSerializer(ASerializer).WriteAnsiString('Domicilio', l_Domicilio);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteInteger('Grupo', TypInfo.otSLong, l_Grupo);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteAnsiString('Status', l_Status);
    l_TarjetaID := Self.TarjetaID;
    TROSerializer(ASerializer).WriteInteger('TarjetaID', TypInfo.otSLong, l_TarjetaID);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TCuponValidoCollection }
constructor TCuponValidoCollection.Create;
begin
  inherited Create(TCuponValido);
end;

constructor TCuponValidoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCuponValidoCollection.Add: TCuponValido;
begin
  Result := TCuponValido(inherited Add);
end;

function TCuponValidoCollection.GetItems(aIndex: Integer): TCuponValido;
begin
  Result := TCuponValido(inherited Items[aIndex]);
end;

procedure TCuponValidoCollection.SetItems(aIndex: Integer; const Value: TCuponValido);
begin
  TCuponValido(inherited Items[aIndex]).Assign(Value);
end;

{ TCupon }

procedure TCupon.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCupon;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCupon) then begin
    lSource := LibraryValesCupones_Intf.TCupon(iSource);

    Self.Valido := lSource.Valido;
    Self.Mensaje := lSource.Mensaje;
    Self.Nombre := lSource.Nombre;
    Self.RazonSocial := lSource.RazonSocial;
    Self.RFC := lSource.RFC;
    Self.Referencia := lSource.Referencia;
    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.GasolineroID := lSource.GasolineroID;
    Self.Grupo := lSource.Grupo;
    Self.Importe := lSource.Importe;
    Self.EstacionID := lSource.EstacionID;
  end;
end;

procedure TCupon.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_GasolineroID: Integer;
  l_Grupo: Integer;
  l_Importe: Double;
  l_Mensaje: AnsiString;
  l_Nombre: AnsiString;
  l_RazonSocial: AnsiString;
  l_Referencia: AnsiString;
  l_RFC: AnsiString;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadInteger('Grupo', TypInfo.otSLong, l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Grupo := Self.Grupo;
    try
      TROSerializer(ASerializer).ReadInteger('Grupo', TypInfo.otSLong, l_Grupo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Grupo']));
    end;
    Self.Grupo := l_Grupo;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TCupon.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_EstacionID: Integer;
  l_GasolineroID: Integer;
  l_Grupo: Integer;
  l_Importe: Double;
  l_Mensaje: AnsiString;
  l_Nombre: AnsiString;
  l_RazonSocial: AnsiString;
  l_Referencia: AnsiString;
  l_RFC: AnsiString;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCupon);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteInteger('Grupo', TypInfo.otSLong, l_Grupo);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Grupo := Self.Grupo;
    TROSerializer(ASerializer).WriteInteger('Grupo', TypInfo.otSLong, l_Grupo);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TCuponCollection }
constructor TCuponCollection.Create;
begin
  inherited Create(TCupon);
end;

constructor TCuponCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCuponCollection.Add: TCupon;
begin
  Result := TCupon(inherited Add);
end;

function TCuponCollection.GetItems(aIndex: Integer): TCupon;
begin
  Result := TCupon(inherited Items[aIndex]);
end;

procedure TCuponCollection.SetItems(aIndex: Integer; const Value: TCupon);
begin
  TCupon(inherited Items[aIndex]).Assign(Value);
end;

{ TRegistroVenta }

procedure TRegistroVenta.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TRegistroVenta;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TRegistroVenta) then begin
    lSource := LibraryValesCupones_Intf.TRegistroVenta(iSource);

    Self.Avituallamiento := lSource.Avituallamiento;
    Self.Bomba := lSource.Bomba;
    Self.CentroCostos := lSource.CentroCostos;
    Self.ClavePemex := lSource.ClavePemex;
    Self.Codigo := lSource.Codigo;
    Self.Comision := lSource.Comision;
    Self.CuentaContable := lSource.CuentaContable;
    Self.Descripcion := lSource.Descripcion;
    Self.Empresa := lSource.Empresa;
    Self.Estacion := lSource.Estacion;
    Self.FechaCarga := lSource.FechaCarga;
    Self.FechaContable := lSource.FechaContable;
    Self.FormaCompra := lSource.FormaCompra;
    Self.Hora := lSource.Hora;
    Self.Identificador := lSource.Identificador;
    Self.Identificacion := lSource.Identificacion;
    Self.ImporteServido := lSource.ImporteServido;
    Self.Impreso := lSource.Impreso;
    Self.Impuesto := lSource.Impuesto;
    Self.Kms := lSource.Kms;
    Self.Manguera := lSource.Manguera;
    Self.Mensaje := lSource.Mensaje;
    Self.Movimiento := lSource.Movimiento;
    Self.Nip := lSource.Nip;
    Self.NoEconomico := lSource.NoEconomico;
    Self.Nombre := lSource.Nombre;
    Self.NoTerminal := lSource.NoTerminal;
    Self.Odometro := lSource.Odometro;
    Self.PrecioServido := lSource.PrecioServido;
    Self.Preset := lSource.Preset;
    Self.Registro := lSource.Registro;
    Self.Saldo := lSource.Saldo;
    Self.SaldoTarjeta := lSource.SaldoTarjeta;
    Self.Secuencia := lSource.Secuencia;
    Self.Site := lSource.Site;
    Self.Tarjeta := lSource.Tarjeta;
    Self.Tipo := lSource.Tipo;
    Self.TipoTarjeta := lSource.TipoTarjeta;
    Self.TipoTrama := lSource.TipoTrama;
    Self.Turno := lSource.Turno;
    Self.Unidad := lSource.Unidad;
    Self.VentaAutorizada := lSource.VentaAutorizada;
    Self.VentaVolumenImporte := lSource.VentaVolumenImporte;
    Self.VentaExhibida := lSource.VentaExhibida;
    Self.VolumenServido := lSource.VolumenServido;
    Self.GasolineroID := lSource.GasolineroID;
    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.IEPS := lSource.IEPS;
  end;
end;

procedure TRegistroVenta.ReadComplex(ASerializer: TObject);
var
  l_Avituallamiento: Double;
  l_Bomba: Integer;
  l_CentroCostos: AnsiString;
  l_ClavePemex: AnsiString;
  l_ClienteID: Integer;
  l_Codigo: AnsiString;
  l_Comision: Double;
  l_CuentaContable: AnsiString;
  l_Descripcion: AnsiString;
  l_Empresa: AnsiString;
  l_Estacion: Integer;
  l_FechaCarga: DateTime;
  l_FechaContable: DateTime;
  l_FormaCompra: Integer;
  l_GasolineroID: Integer;
  l_Hora: AnsiString;
  l_Identificacion: AnsiString;
  l_Identificador: AnsiString;
  l_IEPS: Double;
  l_ImporteServido: Double;
  l_Impreso: Integer;
  l_Impuesto: Double;
  l_Kms: Integer;
  l_Manguera: Integer;
  l_Mensaje: AnsiString;
  l_Movimiento: AnsiString;
  l_Nip: AnsiString;
  l_NoEconomico: AnsiString;
  l_Nombre: AnsiString;
  l_NoTerminal: AnsiString;
  l_Odometro: Integer;
  l_PrecioServido: Double;
  l_Preset: eTipoPreset;
  l_Registro: AnsiString;
  l_Saldo: Double;
  l_SaldoTarjeta: Double;
  l_Secuencia: Integer;
  l_Site: AnsiString;
  l_Tarjeta: AnsiString;
  l_Tipo: Integer;
  l_TipoTarjeta: AnsiString;
  l_TipoTrama: AnsiString;
  l_Turno: Integer;
  l_Unidad: AnsiString;
  l_VehiculoID: Integer;
  l_VentaAutorizada: Double;
  l_VentaExhibida: Double;
  l_VentaVolumenImporte: AnsiString;
  l_VolumenServido: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Avituallamiento := Self.Avituallamiento;
    try
      TROSerializer(ASerializer).ReadDouble('Avituallamiento', TypInfo.ftDouble, l_Avituallamiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Avituallamiento']));
    end;
    Self.Avituallamiento := l_Avituallamiento;
    l_Bomba := Self.Bomba;
    try
      TROSerializer(ASerializer).ReadInteger('Bomba', TypInfo.otSLong, l_Bomba);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bomba']));
    end;
    Self.Bomba := l_Bomba;
    l_CentroCostos := Self.CentroCostos;
    try
      TROSerializer(ASerializer).ReadAnsiString('CentroCostos', l_CentroCostos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CentroCostos']));
    end;
    Self.CentroCostos := l_CentroCostos;
    l_ClavePemex := Self.ClavePemex;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClavePemex', l_ClavePemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClavePemex']));
    end;
    Self.ClavePemex := l_ClavePemex;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Comision := Self.Comision;
    try
      TROSerializer(ASerializer).ReadDouble('Comision', TypInfo.ftDouble, l_Comision);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Comision']));
    end;
    Self.Comision := l_Comision;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_Empresa := Self.Empresa;
    try
      TROSerializer(ASerializer).ReadAnsiString('Empresa', l_Empresa);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Empresa']));
    end;
    Self.Empresa := l_Empresa;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_FechaCarga := Self.FechaCarga;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaCarga', l_FechaCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaCarga']));
    end;
    Self.FechaCarga := l_FechaCarga;
    l_FechaContable := Self.FechaContable;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaContable', l_FechaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaContable']));
    end;
    Self.FechaContable := l_FechaContable;
    l_FormaCompra := Self.FormaCompra;
    try
      TROSerializer(ASerializer).ReadInteger('FormaCompra', TypInfo.otSLong, l_FormaCompra);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaCompra']));
    end;
    Self.FormaCompra := l_FormaCompra;
    l_Hora := Self.Hora;
    try
      TROSerializer(ASerializer).ReadAnsiString('Hora', l_Hora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Hora']));
    end;
    Self.Hora := l_Hora;
    l_Identificador := Self.Identificador;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificador', l_Identificador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificador']));
    end;
    Self.Identificador := l_Identificador;
    l_Identificacion := Self.Identificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificacion', l_Identificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificacion']));
    end;
    Self.Identificacion := l_Identificacion;
    l_ImporteServido := Self.ImporteServido;
    try
      TROSerializer(ASerializer).ReadDouble('ImporteServido', TypInfo.ftDouble, l_ImporteServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteServido']));
    end;
    Self.ImporteServido := l_ImporteServido;
    l_Impreso := Self.Impreso;
    try
      TROSerializer(ASerializer).ReadInteger('Impreso', TypInfo.otSLong, l_Impreso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impreso']));
    end;
    Self.Impreso := l_Impreso;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Kms := Self.Kms;
    try
      TROSerializer(ASerializer).ReadInteger('Kms', TypInfo.otSLong, l_Kms);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kms']));
    end;
    Self.Kms := l_Kms;
    l_Manguera := Self.Manguera;
    try
      TROSerializer(ASerializer).ReadInteger('Manguera', TypInfo.otSLong, l_Manguera);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Manguera']));
    end;
    Self.Manguera := l_Manguera;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Movimiento := Self.Movimiento;
    try
      TROSerializer(ASerializer).ReadAnsiString('Movimiento', l_Movimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Movimiento']));
    end;
    Self.Movimiento := l_Movimiento;
    l_Nip := Self.Nip;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nip', l_Nip);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nip']));
    end;
    Self.Nip := l_Nip;
    l_NoEconomico := Self.NoEconomico;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoEconomico', l_NoEconomico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoEconomico']));
    end;
    Self.NoEconomico := l_NoEconomico;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_NoTerminal := Self.NoTerminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoTerminal', l_NoTerminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoTerminal']));
    end;
    Self.NoTerminal := l_NoTerminal;
    l_Odometro := Self.Odometro;
    try
      TROSerializer(ASerializer).ReadInteger('Odometro', TypInfo.otSLong, l_Odometro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Odometro']));
    end;
    Self.Odometro := l_Odometro;
    l_PrecioServido := Self.PrecioServido;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioServido', TypInfo.ftDouble, l_PrecioServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioServido']));
    end;
    Self.PrecioServido := l_PrecioServido;
    l_Preset := Self.Preset;
    try
      TROSerializer(ASerializer).ReadEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Preset']));
    end;
    Self.Preset := l_Preset;
    l_Registro := Self.Registro;
    try
      TROSerializer(ASerializer).ReadAnsiString('Registro', l_Registro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Registro']));
    end;
    Self.Registro := l_Registro;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_SaldoTarjeta := Self.SaldoTarjeta;
    try
      TROSerializer(ASerializer).ReadDouble('SaldoTarjeta', TypInfo.ftDouble, l_SaldoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SaldoTarjeta']));
    end;
    Self.SaldoTarjeta := l_SaldoTarjeta;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Site := Self.Site;
    try
      TROSerializer(ASerializer).ReadAnsiString('Site', l_Site);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Site']));
    end;
    Self.Site := l_Site;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTarjeta', l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_TipoTrama := Self.TipoTrama;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTrama', l_TipoTrama);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTrama']));
    end;
    Self.TipoTrama := l_TipoTrama;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_Unidad := Self.Unidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Unidad', l_Unidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Unidad']));
    end;
    Self.Unidad := l_Unidad;
    l_VentaAutorizada := Self.VentaAutorizada;
    try
      TROSerializer(ASerializer).ReadDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAutorizada']));
    end;
    Self.VentaAutorizada := l_VentaAutorizada;
    l_VentaVolumenImporte := Self.VentaVolumenImporte;
    try
      TROSerializer(ASerializer).ReadAnsiString('VentaVolumenImporte', l_VentaVolumenImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaVolumenImporte']));
    end;
    Self.VentaVolumenImporte := l_VentaVolumenImporte;
    l_VentaExhibida := Self.VentaExhibida;
    try
      TROSerializer(ASerializer).ReadDouble('VentaExhibida', TypInfo.ftDouble, l_VentaExhibida);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaExhibida']));
    end;
    Self.VentaExhibida := l_VentaExhibida;
    l_VolumenServido := Self.VolumenServido;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenServido', TypInfo.ftDouble, l_VolumenServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenServido']));
    end;
    Self.VolumenServido := l_VolumenServido;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
  end
  else begin
    l_Avituallamiento := Self.Avituallamiento;
    try
      TROSerializer(ASerializer).ReadDouble('Avituallamiento', TypInfo.ftDouble, l_Avituallamiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Avituallamiento']));
    end;
    Self.Avituallamiento := l_Avituallamiento;
    l_Bomba := Self.Bomba;
    try
      TROSerializer(ASerializer).ReadInteger('Bomba', TypInfo.otSLong, l_Bomba);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bomba']));
    end;
    Self.Bomba := l_Bomba;
    l_CentroCostos := Self.CentroCostos;
    try
      TROSerializer(ASerializer).ReadAnsiString('CentroCostos', l_CentroCostos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CentroCostos']));
    end;
    Self.CentroCostos := l_CentroCostos;
    l_ClavePemex := Self.ClavePemex;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClavePemex', l_ClavePemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClavePemex']));
    end;
    Self.ClavePemex := l_ClavePemex;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_Codigo := Self.Codigo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Codigo', l_Codigo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Codigo']));
    end;
    Self.Codigo := l_Codigo;
    l_Comision := Self.Comision;
    try
      TROSerializer(ASerializer).ReadDouble('Comision', TypInfo.ftDouble, l_Comision);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Comision']));
    end;
    Self.Comision := l_Comision;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_Descripcion := Self.Descripcion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Descripcion', l_Descripcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Descripcion']));
    end;
    Self.Descripcion := l_Descripcion;
    l_Empresa := Self.Empresa;
    try
      TROSerializer(ASerializer).ReadAnsiString('Empresa', l_Empresa);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Empresa']));
    end;
    Self.Empresa := l_Empresa;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_FechaCarga := Self.FechaCarga;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaCarga', l_FechaCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaCarga']));
    end;
    Self.FechaCarga := l_FechaCarga;
    l_FechaContable := Self.FechaContable;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaContable', l_FechaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaContable']));
    end;
    Self.FechaContable := l_FechaContable;
    l_FormaCompra := Self.FormaCompra;
    try
      TROSerializer(ASerializer).ReadInteger('FormaCompra', TypInfo.otSLong, l_FormaCompra);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FormaCompra']));
    end;
    Self.FormaCompra := l_FormaCompra;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Hora := Self.Hora;
    try
      TROSerializer(ASerializer).ReadAnsiString('Hora', l_Hora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Hora']));
    end;
    Self.Hora := l_Hora;
    l_Identificacion := Self.Identificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificacion', l_Identificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificacion']));
    end;
    Self.Identificacion := l_Identificacion;
    l_Identificador := Self.Identificador;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificador', l_Identificador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificador']));
    end;
    Self.Identificador := l_Identificador;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_ImporteServido := Self.ImporteServido;
    try
      TROSerializer(ASerializer).ReadDouble('ImporteServido', TypInfo.ftDouble, l_ImporteServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteServido']));
    end;
    Self.ImporteServido := l_ImporteServido;
    l_Impreso := Self.Impreso;
    try
      TROSerializer(ASerializer).ReadInteger('Impreso', TypInfo.otSLong, l_Impreso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impreso']));
    end;
    Self.Impreso := l_Impreso;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Kms := Self.Kms;
    try
      TROSerializer(ASerializer).ReadInteger('Kms', TypInfo.otSLong, l_Kms);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kms']));
    end;
    Self.Kms := l_Kms;
    l_Manguera := Self.Manguera;
    try
      TROSerializer(ASerializer).ReadInteger('Manguera', TypInfo.otSLong, l_Manguera);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Manguera']));
    end;
    Self.Manguera := l_Manguera;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_Movimiento := Self.Movimiento;
    try
      TROSerializer(ASerializer).ReadAnsiString('Movimiento', l_Movimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Movimiento']));
    end;
    Self.Movimiento := l_Movimiento;
    l_Nip := Self.Nip;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nip', l_Nip);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nip']));
    end;
    Self.Nip := l_Nip;
    l_NoEconomico := Self.NoEconomico;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoEconomico', l_NoEconomico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoEconomico']));
    end;
    Self.NoEconomico := l_NoEconomico;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_NoTerminal := Self.NoTerminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoTerminal', l_NoTerminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoTerminal']));
    end;
    Self.NoTerminal := l_NoTerminal;
    l_Odometro := Self.Odometro;
    try
      TROSerializer(ASerializer).ReadInteger('Odometro', TypInfo.otSLong, l_Odometro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Odometro']));
    end;
    Self.Odometro := l_Odometro;
    l_PrecioServido := Self.PrecioServido;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioServido', TypInfo.ftDouble, l_PrecioServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioServido']));
    end;
    Self.PrecioServido := l_PrecioServido;
    l_Preset := Self.Preset;
    try
      TROSerializer(ASerializer).ReadEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Preset']));
    end;
    Self.Preset := l_Preset;
    l_Registro := Self.Registro;
    try
      TROSerializer(ASerializer).ReadAnsiString('Registro', l_Registro);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Registro']));
    end;
    Self.Registro := l_Registro;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_SaldoTarjeta := Self.SaldoTarjeta;
    try
      TROSerializer(ASerializer).ReadDouble('SaldoTarjeta', TypInfo.ftDouble, l_SaldoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SaldoTarjeta']));
    end;
    Self.SaldoTarjeta := l_SaldoTarjeta;
    l_Secuencia := Self.Secuencia;
    try
      TROSerializer(ASerializer).ReadInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Secuencia']));
    end;
    Self.Secuencia := l_Secuencia;
    l_Site := Self.Site;
    try
      TROSerializer(ASerializer).ReadAnsiString('Site', l_Site);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Site']));
    end;
    Self.Site := l_Site;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTarjeta', l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_TipoTrama := Self.TipoTrama;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTrama', l_TipoTrama);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTrama']));
    end;
    Self.TipoTrama := l_TipoTrama;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_Unidad := Self.Unidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Unidad', l_Unidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Unidad']));
    end;
    Self.Unidad := l_Unidad;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_VentaAutorizada := Self.VentaAutorizada;
    try
      TROSerializer(ASerializer).ReadDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAutorizada']));
    end;
    Self.VentaAutorizada := l_VentaAutorizada;
    l_VentaExhibida := Self.VentaExhibida;
    try
      TROSerializer(ASerializer).ReadDouble('VentaExhibida', TypInfo.ftDouble, l_VentaExhibida);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaExhibida']));
    end;
    Self.VentaExhibida := l_VentaExhibida;
    l_VentaVolumenImporte := Self.VentaVolumenImporte;
    try
      TROSerializer(ASerializer).ReadAnsiString('VentaVolumenImporte', l_VentaVolumenImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaVolumenImporte']));
    end;
    Self.VentaVolumenImporte := l_VentaVolumenImporte;
    l_VolumenServido := Self.VolumenServido;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenServido', TypInfo.ftDouble, l_VolumenServido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenServido']));
    end;
    Self.VolumenServido := l_VolumenServido;
  end;
end;

procedure TRegistroVenta.WriteComplex(ASerializer: TObject);
var
  l_Avituallamiento: Double;
  l_Bomba: Integer;
  l_CentroCostos: AnsiString;
  l_ClavePemex: AnsiString;
  l_ClienteID: Integer;
  l_Codigo: AnsiString;
  l_Comision: Double;
  l_CuentaContable: AnsiString;
  l_Descripcion: AnsiString;
  l_Empresa: AnsiString;
  l_Estacion: Integer;
  l_FechaCarga: DateTime;
  l_FechaContable: DateTime;
  l_FormaCompra: Integer;
  l_GasolineroID: Integer;
  l_Hora: AnsiString;
  l_Identificacion: AnsiString;
  l_Identificador: AnsiString;
  l_IEPS: Double;
  l_ImporteServido: Double;
  l_Impreso: Integer;
  l_Impuesto: Double;
  l_Kms: Integer;
  l_Manguera: Integer;
  l_Mensaje: AnsiString;
  l_Movimiento: AnsiString;
  l_Nip: AnsiString;
  l_NoEconomico: AnsiString;
  l_Nombre: AnsiString;
  l_NoTerminal: AnsiString;
  l_Odometro: Integer;
  l_PrecioServido: Double;
  l_Preset: eTipoPreset;
  l_Registro: AnsiString;
  l_Saldo: Double;
  l_SaldoTarjeta: Double;
  l_Secuencia: Integer;
  l_Site: AnsiString;
  l_Tarjeta: AnsiString;
  l_Tipo: Integer;
  l_TipoTarjeta: AnsiString;
  l_TipoTrama: AnsiString;
  l_Turno: Integer;
  l_Unidad: AnsiString;
  l_VehiculoID: Integer;
  l_VentaAutorizada: Double;
  l_VentaExhibida: Double;
  l_VentaVolumenImporte: AnsiString;
  l_VolumenServido: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TRegistroVenta);
    l_Avituallamiento := Self.Avituallamiento;
    TROSerializer(ASerializer).WriteDouble('Avituallamiento', TypInfo.ftDouble, l_Avituallamiento);
    l_Bomba := Self.Bomba;
    TROSerializer(ASerializer).WriteInteger('Bomba', TypInfo.otSLong, l_Bomba);
    l_CentroCostos := Self.CentroCostos;
    TROSerializer(ASerializer).WriteAnsiString('CentroCostos', l_CentroCostos);
    l_ClavePemex := Self.ClavePemex;
    TROSerializer(ASerializer).WriteAnsiString('ClavePemex', l_ClavePemex);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Comision := Self.Comision;
    TROSerializer(ASerializer).WriteDouble('Comision', TypInfo.ftDouble, l_Comision);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_Empresa := Self.Empresa;
    TROSerializer(ASerializer).WriteAnsiString('Empresa', l_Empresa);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_FechaCarga := Self.FechaCarga;
    TROSerializer(ASerializer).WriteDateTime('FechaCarga', l_FechaCarga);
    l_FechaContable := Self.FechaContable;
    TROSerializer(ASerializer).WriteDateTime('FechaContable', l_FechaContable);
    l_FormaCompra := Self.FormaCompra;
    TROSerializer(ASerializer).WriteInteger('FormaCompra', TypInfo.otSLong, l_FormaCompra);
    l_Hora := Self.Hora;
    TROSerializer(ASerializer).WriteAnsiString('Hora', l_Hora);
    l_Identificador := Self.Identificador;
    TROSerializer(ASerializer).WriteAnsiString('Identificador', l_Identificador);
    l_Identificacion := Self.Identificacion;
    TROSerializer(ASerializer).WriteAnsiString('Identificacion', l_Identificacion);
    l_ImporteServido := Self.ImporteServido;
    TROSerializer(ASerializer).WriteDouble('ImporteServido', TypInfo.ftDouble, l_ImporteServido);
    l_Impreso := Self.Impreso;
    TROSerializer(ASerializer).WriteInteger('Impreso', TypInfo.otSLong, l_Impreso);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Kms := Self.Kms;
    TROSerializer(ASerializer).WriteInteger('Kms', TypInfo.otSLong, l_Kms);
    l_Manguera := Self.Manguera;
    TROSerializer(ASerializer).WriteInteger('Manguera', TypInfo.otSLong, l_Manguera);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Movimiento := Self.Movimiento;
    TROSerializer(ASerializer).WriteAnsiString('Movimiento', l_Movimiento);
    l_Nip := Self.Nip;
    TROSerializer(ASerializer).WriteAnsiString('Nip', l_Nip);
    l_NoEconomico := Self.NoEconomico;
    TROSerializer(ASerializer).WriteAnsiString('NoEconomico', l_NoEconomico);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_NoTerminal := Self.NoTerminal;
    TROSerializer(ASerializer).WriteAnsiString('NoTerminal', l_NoTerminal);
    l_Odometro := Self.Odometro;
    TROSerializer(ASerializer).WriteInteger('Odometro', TypInfo.otSLong, l_Odometro);
    l_PrecioServido := Self.PrecioServido;
    TROSerializer(ASerializer).WriteDouble('PrecioServido', TypInfo.ftDouble, l_PrecioServido);
    l_Preset := Self.Preset;
    TROSerializer(ASerializer).WriteEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    l_Registro := Self.Registro;
    TROSerializer(ASerializer).WriteAnsiString('Registro', l_Registro);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_SaldoTarjeta := Self.SaldoTarjeta;
    TROSerializer(ASerializer).WriteDouble('SaldoTarjeta', TypInfo.ftDouble, l_SaldoTarjeta);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Site := Self.Site;
    TROSerializer(ASerializer).WriteAnsiString('Site', l_Site);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteAnsiString('TipoTarjeta', l_TipoTarjeta);
    l_TipoTrama := Self.TipoTrama;
    TROSerializer(ASerializer).WriteAnsiString('TipoTrama', l_TipoTrama);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_Unidad := Self.Unidad;
    TROSerializer(ASerializer).WriteAnsiString('Unidad', l_Unidad);
    l_VentaAutorizada := Self.VentaAutorizada;
    TROSerializer(ASerializer).WriteDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    l_VentaVolumenImporte := Self.VentaVolumenImporte;
    TROSerializer(ASerializer).WriteAnsiString('VentaVolumenImporte', l_VentaVolumenImporte);
    l_VentaExhibida := Self.VentaExhibida;
    TROSerializer(ASerializer).WriteDouble('VentaExhibida', TypInfo.ftDouble, l_VentaExhibida);
    l_VolumenServido := Self.VolumenServido;
    TROSerializer(ASerializer).WriteDouble('VolumenServido', TypInfo.ftDouble, l_VolumenServido);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
  end
  else begin
    l_Avituallamiento := Self.Avituallamiento;
    TROSerializer(ASerializer).WriteDouble('Avituallamiento', TypInfo.ftDouble, l_Avituallamiento);
    l_Bomba := Self.Bomba;
    TROSerializer(ASerializer).WriteInteger('Bomba', TypInfo.otSLong, l_Bomba);
    l_CentroCostos := Self.CentroCostos;
    TROSerializer(ASerializer).WriteAnsiString('CentroCostos', l_CentroCostos);
    l_ClavePemex := Self.ClavePemex;
    TROSerializer(ASerializer).WriteAnsiString('ClavePemex', l_ClavePemex);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_Codigo := Self.Codigo;
    TROSerializer(ASerializer).WriteAnsiString('Codigo', l_Codigo);
    l_Comision := Self.Comision;
    TROSerializer(ASerializer).WriteDouble('Comision', TypInfo.ftDouble, l_Comision);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_Descripcion := Self.Descripcion;
    TROSerializer(ASerializer).WriteAnsiString('Descripcion', l_Descripcion);
    l_Empresa := Self.Empresa;
    TROSerializer(ASerializer).WriteAnsiString('Empresa', l_Empresa);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_FechaCarga := Self.FechaCarga;
    TROSerializer(ASerializer).WriteDateTime('FechaCarga', l_FechaCarga);
    l_FechaContable := Self.FechaContable;
    TROSerializer(ASerializer).WriteDateTime('FechaContable', l_FechaContable);
    l_FormaCompra := Self.FormaCompra;
    TROSerializer(ASerializer).WriteInteger('FormaCompra', TypInfo.otSLong, l_FormaCompra);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Hora := Self.Hora;
    TROSerializer(ASerializer).WriteAnsiString('Hora', l_Hora);
    l_Identificacion := Self.Identificacion;
    TROSerializer(ASerializer).WriteAnsiString('Identificacion', l_Identificacion);
    l_Identificador := Self.Identificador;
    TROSerializer(ASerializer).WriteAnsiString('Identificador', l_Identificador);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_ImporteServido := Self.ImporteServido;
    TROSerializer(ASerializer).WriteDouble('ImporteServido', TypInfo.ftDouble, l_ImporteServido);
    l_Impreso := Self.Impreso;
    TROSerializer(ASerializer).WriteInteger('Impreso', TypInfo.otSLong, l_Impreso);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Kms := Self.Kms;
    TROSerializer(ASerializer).WriteInteger('Kms', TypInfo.otSLong, l_Kms);
    l_Manguera := Self.Manguera;
    TROSerializer(ASerializer).WriteInteger('Manguera', TypInfo.otSLong, l_Manguera);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_Movimiento := Self.Movimiento;
    TROSerializer(ASerializer).WriteAnsiString('Movimiento', l_Movimiento);
    l_Nip := Self.Nip;
    TROSerializer(ASerializer).WriteAnsiString('Nip', l_Nip);
    l_NoEconomico := Self.NoEconomico;
    TROSerializer(ASerializer).WriteAnsiString('NoEconomico', l_NoEconomico);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_NoTerminal := Self.NoTerminal;
    TROSerializer(ASerializer).WriteAnsiString('NoTerminal', l_NoTerminal);
    l_Odometro := Self.Odometro;
    TROSerializer(ASerializer).WriteInteger('Odometro', TypInfo.otSLong, l_Odometro);
    l_PrecioServido := Self.PrecioServido;
    TROSerializer(ASerializer).WriteDouble('PrecioServido', TypInfo.ftDouble, l_PrecioServido);
    l_Preset := Self.Preset;
    TROSerializer(ASerializer).WriteEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    l_Registro := Self.Registro;
    TROSerializer(ASerializer).WriteAnsiString('Registro', l_Registro);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_SaldoTarjeta := Self.SaldoTarjeta;
    TROSerializer(ASerializer).WriteDouble('SaldoTarjeta', TypInfo.ftDouble, l_SaldoTarjeta);
    l_Secuencia := Self.Secuencia;
    TROSerializer(ASerializer).WriteInteger('Secuencia', TypInfo.otSLong, l_Secuencia);
    l_Site := Self.Site;
    TROSerializer(ASerializer).WriteAnsiString('Site', l_Site);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteAnsiString('TipoTarjeta', l_TipoTarjeta);
    l_TipoTrama := Self.TipoTrama;
    TROSerializer(ASerializer).WriteAnsiString('TipoTrama', l_TipoTrama);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_Unidad := Self.Unidad;
    TROSerializer(ASerializer).WriteAnsiString('Unidad', l_Unidad);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_VentaAutorizada := Self.VentaAutorizada;
    TROSerializer(ASerializer).WriteDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    l_VentaExhibida := Self.VentaExhibida;
    TROSerializer(ASerializer).WriteDouble('VentaExhibida', TypInfo.ftDouble, l_VentaExhibida);
    l_VentaVolumenImporte := Self.VentaVolumenImporte;
    TROSerializer(ASerializer).WriteAnsiString('VentaVolumenImporte', l_VentaVolumenImporte);
    l_VolumenServido := Self.VolumenServido;
    TROSerializer(ASerializer).WriteDouble('VolumenServido', TypInfo.ftDouble, l_VolumenServido);
  end;
end;

{ TRegistroVentaCollection }
constructor TRegistroVentaCollection.Create;
begin
  inherited Create(TRegistroVenta);
end;

constructor TRegistroVentaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TRegistroVentaCollection.Add: TRegistroVenta;
begin
  Result := TRegistroVenta(inherited Add);
end;

function TRegistroVentaCollection.GetItems(aIndex: Integer): TRegistroVenta;
begin
  Result := TRegistroVenta(inherited Items[aIndex]);
end;

procedure TRegistroVentaCollection.LoadFromArray(anArray: aRegistroVenta);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TRegistroVentaCollection.SaveToArray(anArray: aRegistroVenta);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TRegistroVenta(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TRegistroVentaCollection.SetItems(aIndex: Integer; const Value: TRegistroVenta);
begin
  TRegistroVenta(inherited Items[aIndex]).Assign(Value);
end;

{ TConsumo }

procedure TConsumo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TConsumo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TConsumo) then begin
    lSource := LibraryValesCupones_Intf.TConsumo(iSource);

    Self.SecuenciaVenta := lSource.SecuenciaVenta;
    Self.Serie := lSource.Serie;
    Self.EstacionID := lSource.EstacionID;
    Self.FechaMovimiento := lSource.FechaMovimiento;
    Self.FechaCarga := lSource.FechaCarga;
    Self.Ejercicio := lSource.Ejercicio;
    Self.Periodo := lSource.Periodo;
    Self.Dia := lSource.Dia;
    Self.Turno := lSource.Turno;
    Self.Kilometraje := lSource.Kilometraje;
    Self.Referencia := lSource.Referencia;
    Self.Tarjeta := lSource.Tarjeta;
    Self.PosicionCarga := lSource.PosicionCarga;
    Self.Cantidad := lSource.Cantidad;
    Self.Precio := lSource.Precio;
    Self.Importe := lSource.Importe;
    Self.ImpuestoImporte := lSource.ImpuestoImporte;
    Self.ImpuestoPorcentaje := lSource.ImpuestoPorcentaje;
    Self.CuentaContable := lSource.CuentaContable;
    Self.ManualAutomatico := lSource.ManualAutomatico;
    Self.Facturado := lSource.Facturado;
    Self.EnIngresos := lSource.EnIngresos;
    Self.Auditado := lSource.Auditado;
    Self.FacturaID := lSource.FacturaID;
    Self.ProductoID := lSource.ProductoID;
    Self.GasolineroID := lSource.GasolineroID;
    Self.ClienteID := lSource.ClienteID;
    Self.IEPS := lSource.IEPS;
    Self.UsuarioID := lSource.UsuarioID;
    Self.VehiculoID := lSource.VehiculoID;
  end;
end;

procedure TConsumo.ReadComplex(ASerializer: TObject);
var
  l_Auditado: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_CuentaContable: AnsiString;
  l_Dia: Integer;
  l_Ejercicio: Integer;
  l_EnIngresos: Integer;
  l_EstacionID: Integer;
  l_Facturado: Integer;
  l_FacturaID: Integer;
  l_FechaCarga: DateTime;
  l_FechaMovimiento: DateTime;
  l_GasolineroID: Integer;
  l_IEPS: Double;
  l_Importe: Double;
  l_ImpuestoImporte: Double;
  l_ImpuestoPorcentaje: Double;
  l_Kilometraje: Integer;
  l_ManualAutomatico: AnsiString;
  l_Periodo: Integer;
  l_PosicionCarga: Integer;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_Referencia: AnsiString;
  l_SecuenciaVenta: Integer;
  l_Serie: AnsiString;
  l_Tarjeta: AnsiString;
  l_Turno: Integer;
  l_UsuarioID: Integer;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_SecuenciaVenta := Self.SecuenciaVenta;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaVenta', TypInfo.otSLong, l_SecuenciaVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaVenta']));
    end;
    Self.SecuenciaVenta := l_SecuenciaVenta;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_FechaMovimiento := Self.FechaMovimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaMovimiento', l_FechaMovimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaMovimiento']));
    end;
    Self.FechaMovimiento := l_FechaMovimiento;
    l_FechaCarga := Self.FechaCarga;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaCarga', l_FechaCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaCarga']));
    end;
    Self.FechaCarga := l_FechaCarga;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_Kilometraje := Self.Kilometraje;
    try
      TROSerializer(ASerializer).ReadInteger('Kilometraje', TypInfo.otSLong, l_Kilometraje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kilometraje']));
    end;
    Self.Kilometraje := l_Kilometraje;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_PosicionCarga := Self.PosicionCarga;
    try
      TROSerializer(ASerializer).ReadInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PosicionCarga']));
    end;
    Self.PosicionCarga := l_PosicionCarga;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_ImpuestoImporte := Self.ImpuestoImporte;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoImporte', TypInfo.ftDouble, l_ImpuestoImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoImporte']));
    end;
    Self.ImpuestoImporte := l_ImpuestoImporte;
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoPorcentaje']));
    end;
    Self.ImpuestoPorcentaje := l_ImpuestoPorcentaje;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_ManualAutomatico := Self.ManualAutomatico;
    try
      TROSerializer(ASerializer).ReadAnsiString('ManualAutomatico', l_ManualAutomatico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ManualAutomatico']));
    end;
    Self.ManualAutomatico := l_ManualAutomatico;
    l_Facturado := Self.Facturado;
    try
      TROSerializer(ASerializer).ReadInteger('Facturado', TypInfo.otSLong, l_Facturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Facturado']));
    end;
    Self.Facturado := l_Facturado;
    l_EnIngresos := Self.EnIngresos;
    try
      TROSerializer(ASerializer).ReadInteger('EnIngresos', TypInfo.otSLong, l_EnIngresos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EnIngresos']));
    end;
    Self.EnIngresos := l_EnIngresos;
    l_Auditado := Self.Auditado;
    try
      TROSerializer(ASerializer).ReadInteger('Auditado', TypInfo.otSLong, l_Auditado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Auditado']));
    end;
    Self.Auditado := l_Auditado;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end
  else begin
    l_Auditado := Self.Auditado;
    try
      TROSerializer(ASerializer).ReadInteger('Auditado', TypInfo.otSLong, l_Auditado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Auditado']));
    end;
    Self.Auditado := l_Auditado;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_CuentaContable := Self.CuentaContable;
    try
      TROSerializer(ASerializer).ReadAnsiString('CuentaContable', l_CuentaContable);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CuentaContable']));
    end;
    Self.CuentaContable := l_CuentaContable;
    l_Dia := Self.Dia;
    try
      TROSerializer(ASerializer).ReadInteger('Dia', TypInfo.otSLong, l_Dia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dia']));
    end;
    Self.Dia := l_Dia;
    l_Ejercicio := Self.Ejercicio;
    try
      TROSerializer(ASerializer).ReadInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ejercicio']));
    end;
    Self.Ejercicio := l_Ejercicio;
    l_EnIngresos := Self.EnIngresos;
    try
      TROSerializer(ASerializer).ReadInteger('EnIngresos', TypInfo.otSLong, l_EnIngresos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EnIngresos']));
    end;
    Self.EnIngresos := l_EnIngresos;
    l_EstacionID := Self.EstacionID;
    try
      TROSerializer(ASerializer).ReadInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstacionID']));
    end;
    Self.EstacionID := l_EstacionID;
    l_Facturado := Self.Facturado;
    try
      TROSerializer(ASerializer).ReadInteger('Facturado', TypInfo.otSLong, l_Facturado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Facturado']));
    end;
    Self.Facturado := l_Facturado;
    l_FacturaID := Self.FacturaID;
    try
      TROSerializer(ASerializer).ReadInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FacturaID']));
    end;
    Self.FacturaID := l_FacturaID;
    l_FechaCarga := Self.FechaCarga;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaCarga', l_FechaCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaCarga']));
    end;
    Self.FechaCarga := l_FechaCarga;
    l_FechaMovimiento := Self.FechaMovimiento;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaMovimiento', l_FechaMovimiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaMovimiento']));
    end;
    Self.FechaMovimiento := l_FechaMovimiento;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_ImpuestoImporte := Self.ImpuestoImporte;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoImporte', TypInfo.ftDouble, l_ImpuestoImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoImporte']));
    end;
    Self.ImpuestoImporte := l_ImpuestoImporte;
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    try
      TROSerializer(ASerializer).ReadDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImpuestoPorcentaje']));
    end;
    Self.ImpuestoPorcentaje := l_ImpuestoPorcentaje;
    l_Kilometraje := Self.Kilometraje;
    try
      TROSerializer(ASerializer).ReadInteger('Kilometraje', TypInfo.otSLong, l_Kilometraje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kilometraje']));
    end;
    Self.Kilometraje := l_Kilometraje;
    l_ManualAutomatico := Self.ManualAutomatico;
    try
      TROSerializer(ASerializer).ReadAnsiString('ManualAutomatico', l_ManualAutomatico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ManualAutomatico']));
    end;
    Self.ManualAutomatico := l_ManualAutomatico;
    l_Periodo := Self.Periodo;
    try
      TROSerializer(ASerializer).ReadInteger('Periodo', TypInfo.otSLong, l_Periodo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Periodo']));
    end;
    Self.Periodo := l_Periodo;
    l_PosicionCarga := Self.PosicionCarga;
    try
      TROSerializer(ASerializer).ReadInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PosicionCarga']));
    end;
    Self.PosicionCarga := l_PosicionCarga;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_SecuenciaVenta := Self.SecuenciaVenta;
    try
      TROSerializer(ASerializer).ReadInteger('SecuenciaVenta', TypInfo.otSLong, l_SecuenciaVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SecuenciaVenta']));
    end;
    Self.SecuenciaVenta := l_SecuenciaVenta;
    l_Serie := Self.Serie;
    try
      TROSerializer(ASerializer).ReadAnsiString('Serie', l_Serie);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Serie']));
    end;
    Self.Serie := l_Serie;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_Turno := Self.Turno;
    try
      TROSerializer(ASerializer).ReadInteger('Turno', TypInfo.otSLong, l_Turno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Turno']));
    end;
    Self.Turno := l_Turno;
    l_UsuarioID := Self.UsuarioID;
    try
      TROSerializer(ASerializer).ReadInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UsuarioID']));
    end;
    Self.UsuarioID := l_UsuarioID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TConsumo.WriteComplex(ASerializer: TObject);
var
  l_Auditado: Integer;
  l_Cantidad: Double;
  l_ClienteID: Integer;
  l_CuentaContable: AnsiString;
  l_Dia: Integer;
  l_Ejercicio: Integer;
  l_EnIngresos: Integer;
  l_EstacionID: Integer;
  l_Facturado: Integer;
  l_FacturaID: Integer;
  l_FechaCarga: DateTime;
  l_FechaMovimiento: DateTime;
  l_GasolineroID: Integer;
  l_IEPS: Double;
  l_Importe: Double;
  l_ImpuestoImporte: Double;
  l_ImpuestoPorcentaje: Double;
  l_Kilometraje: Integer;
  l_ManualAutomatico: AnsiString;
  l_Periodo: Integer;
  l_PosicionCarga: Integer;
  l_Precio: Double;
  l_ProductoID: Integer;
  l_Referencia: AnsiString;
  l_SecuenciaVenta: Integer;
  l_Serie: AnsiString;
  l_Tarjeta: AnsiString;
  l_Turno: Integer;
  l_UsuarioID: Integer;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TConsumo);
    l_SecuenciaVenta := Self.SecuenciaVenta;
    TROSerializer(ASerializer).WriteInteger('SecuenciaVenta', TypInfo.otSLong, l_SecuenciaVenta);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_FechaMovimiento := Self.FechaMovimiento;
    TROSerializer(ASerializer).WriteDateTime('FechaMovimiento', l_FechaMovimiento);
    l_FechaCarga := Self.FechaCarga;
    TROSerializer(ASerializer).WriteDateTime('FechaCarga', l_FechaCarga);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_Kilometraje := Self.Kilometraje;
    TROSerializer(ASerializer).WriteInteger('Kilometraje', TypInfo.otSLong, l_Kilometraje);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_PosicionCarga := Self.PosicionCarga;
    TROSerializer(ASerializer).WriteInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_ImpuestoImporte := Self.ImpuestoImporte;
    TROSerializer(ASerializer).WriteDouble('ImpuestoImporte', TypInfo.ftDouble, l_ImpuestoImporte);
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    TROSerializer(ASerializer).WriteDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_ManualAutomatico := Self.ManualAutomatico;
    TROSerializer(ASerializer).WriteAnsiString('ManualAutomatico', l_ManualAutomatico);
    l_Facturado := Self.Facturado;
    TROSerializer(ASerializer).WriteInteger('Facturado', TypInfo.otSLong, l_Facturado);
    l_EnIngresos := Self.EnIngresos;
    TROSerializer(ASerializer).WriteInteger('EnIngresos', TypInfo.otSLong, l_EnIngresos);
    l_Auditado := Self.Auditado;
    TROSerializer(ASerializer).WriteInteger('Auditado', TypInfo.otSLong, l_Auditado);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end
  else begin
    l_Auditado := Self.Auditado;
    TROSerializer(ASerializer).WriteInteger('Auditado', TypInfo.otSLong, l_Auditado);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_CuentaContable := Self.CuentaContable;
    TROSerializer(ASerializer).WriteAnsiString('CuentaContable', l_CuentaContable);
    l_Dia := Self.Dia;
    TROSerializer(ASerializer).WriteInteger('Dia', TypInfo.otSLong, l_Dia);
    l_Ejercicio := Self.Ejercicio;
    TROSerializer(ASerializer).WriteInteger('Ejercicio', TypInfo.otSLong, l_Ejercicio);
    l_EnIngresos := Self.EnIngresos;
    TROSerializer(ASerializer).WriteInteger('EnIngresos', TypInfo.otSLong, l_EnIngresos);
    l_EstacionID := Self.EstacionID;
    TROSerializer(ASerializer).WriteInteger('EstacionID', TypInfo.otSLong, l_EstacionID);
    l_Facturado := Self.Facturado;
    TROSerializer(ASerializer).WriteInteger('Facturado', TypInfo.otSLong, l_Facturado);
    l_FacturaID := Self.FacturaID;
    TROSerializer(ASerializer).WriteInteger('FacturaID', TypInfo.otSLong, l_FacturaID);
    l_FechaCarga := Self.FechaCarga;
    TROSerializer(ASerializer).WriteDateTime('FechaCarga', l_FechaCarga);
    l_FechaMovimiento := Self.FechaMovimiento;
    TROSerializer(ASerializer).WriteDateTime('FechaMovimiento', l_FechaMovimiento);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_ImpuestoImporte := Self.ImpuestoImporte;
    TROSerializer(ASerializer).WriteDouble('ImpuestoImporte', TypInfo.ftDouble, l_ImpuestoImporte);
    l_ImpuestoPorcentaje := Self.ImpuestoPorcentaje;
    TROSerializer(ASerializer).WriteDouble('ImpuestoPorcentaje', TypInfo.ftDouble, l_ImpuestoPorcentaje);
    l_Kilometraje := Self.Kilometraje;
    TROSerializer(ASerializer).WriteInteger('Kilometraje', TypInfo.otSLong, l_Kilometraje);
    l_ManualAutomatico := Self.ManualAutomatico;
    TROSerializer(ASerializer).WriteAnsiString('ManualAutomatico', l_ManualAutomatico);
    l_Periodo := Self.Periodo;
    TROSerializer(ASerializer).WriteInteger('Periodo', TypInfo.otSLong, l_Periodo);
    l_PosicionCarga := Self.PosicionCarga;
    TROSerializer(ASerializer).WriteInteger('PosicionCarga', TypInfo.otSLong, l_PosicionCarga);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_SecuenciaVenta := Self.SecuenciaVenta;
    TROSerializer(ASerializer).WriteInteger('SecuenciaVenta', TypInfo.otSLong, l_SecuenciaVenta);
    l_Serie := Self.Serie;
    TROSerializer(ASerializer).WriteAnsiString('Serie', l_Serie);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_Turno := Self.Turno;
    TROSerializer(ASerializer).WriteInteger('Turno', TypInfo.otSLong, l_Turno);
    l_UsuarioID := Self.UsuarioID;
    TROSerializer(ASerializer).WriteInteger('UsuarioID', TypInfo.otSLong, l_UsuarioID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TConsumoCollection }
constructor TConsumoCollection.Create;
begin
  inherited Create(TConsumo);
end;

constructor TConsumoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TConsumoCollection.Add: TConsumo;
begin
  Result := TConsumo(inherited Add);
end;

function TConsumoCollection.GetItems(aIndex: Integer): TConsumo;
begin
  Result := TConsumo(inherited Items[aIndex]);
end;

procedure TConsumoCollection.LoadFromArray(anArray: aConsumo);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TConsumoCollection.SaveToArray(anArray: aConsumo);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TConsumo(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TConsumoCollection.SetItems(aIndex: Integer; const Value: TConsumo);
begin
  TConsumo(inherited Items[aIndex]).Assign(Value);
end;

{ TTarjeta }

procedure TTarjeta.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTarjeta;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTarjeta) then begin
    lSource := LibraryValesCupones_Intf.TTarjeta(iSource);

    Self.GasolineroID := lSource.GasolineroID;
    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.Cantidad := lSource.Cantidad;
    Self.LitrosImporte := lSource.LitrosImporte;
    Self.MsnError := lSource.MsnError;
    Self.Status := lSource.Status;
    Self.Valido := lSource.Valido;
    Self.Nombre := lSource.Nombre;
    Self.RazonSocial := lSource.RazonSocial;
    Self.RFC := lSource.RFC;
    Self.Identificacion := lSource.Identificacion;
    Self.Saldo := lSource.Saldo;
    Self.Mensaje := lSource.Mensaje;
    Self.CentroCostos := lSource.CentroCostos;
    Self.Clasificacion := lSource.Clasificacion;
    Self.Productos := lSource.Productos;
  end;
end;

procedure TTarjeta.ReadComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_CentroCostos: AnsiString;
  l_Clasificacion: AnsiString;
  l_ClienteID: Integer;
  l_GasolineroID: Integer;
  l_Identificacion: AnsiString;
  l_LitrosImporte: AnsiString;
  l_Mensaje: AnsiString;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_Productos: AnsiString;
  l_RazonSocial: AnsiString;
  l_RFC: AnsiString;
  l_Saldo: Double;
  l_Status: Integer;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_LitrosImporte := Self.LitrosImporte;
    try
      TROSerializer(ASerializer).ReadAnsiString('LitrosImporte', l_LitrosImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LitrosImporte']));
    end;
    Self.LitrosImporte := l_LitrosImporte;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadInteger('Status', TypInfo.otSLong, l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Identificacion := Self.Identificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificacion', l_Identificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificacion']));
    end;
    Self.Identificacion := l_Identificacion;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_CentroCostos := Self.CentroCostos;
    try
      TROSerializer(ASerializer).ReadAnsiString('CentroCostos', l_CentroCostos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CentroCostos']));
    end;
    Self.CentroCostos := l_CentroCostos;
    l_Clasificacion := Self.Clasificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Clasificacion', l_Clasificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Clasificacion']));
    end;
    Self.Clasificacion := l_Clasificacion;
    l_Productos := Self.Productos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Productos', l_Productos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Productos']));
    end;
    Self.Productos := l_Productos;
  end
  else begin
    l_Cantidad := Self.Cantidad;
    try
      TROSerializer(ASerializer).ReadDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cantidad']));
    end;
    Self.Cantidad := l_Cantidad;
    l_CentroCostos := Self.CentroCostos;
    try
      TROSerializer(ASerializer).ReadAnsiString('CentroCostos', l_CentroCostos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CentroCostos']));
    end;
    Self.CentroCostos := l_CentroCostos;
    l_Clasificacion := Self.Clasificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Clasificacion', l_Clasificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Clasificacion']));
    end;
    Self.Clasificacion := l_Clasificacion;
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_GasolineroID := Self.GasolineroID;
    try
      TROSerializer(ASerializer).ReadInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'GasolineroID']));
    end;
    Self.GasolineroID := l_GasolineroID;
    l_Identificacion := Self.Identificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Identificacion', l_Identificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Identificacion']));
    end;
    Self.Identificacion := l_Identificacion;
    l_LitrosImporte := Self.LitrosImporte;
    try
      TROSerializer(ASerializer).ReadAnsiString('LitrosImporte', l_LitrosImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LitrosImporte']));
    end;
    Self.LitrosImporte := l_LitrosImporte;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_MsnError := Self.MsnError;
    try
      TROSerializer(ASerializer).ReadAnsiString('MsnError', l_MsnError);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MsnError']));
    end;
    Self.MsnError := l_MsnError;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Productos := Self.Productos;
    try
      TROSerializer(ASerializer).ReadAnsiString('Productos', l_Productos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Productos']));
    end;
    Self.Productos := l_Productos;
    l_RazonSocial := Self.RazonSocial;
    try
      TROSerializer(ASerializer).ReadAnsiString('RazonSocial', l_RazonSocial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RazonSocial']));
    end;
    Self.RazonSocial := l_RazonSocial;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Status := Self.Status;
    try
      TROSerializer(ASerializer).ReadInteger('Status', TypInfo.otSLong, l_Status);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Status']));
    end;
    Self.Status := l_Status;
    l_Valido := Self.Valido;
    try
      TROSerializer(ASerializer).ReadEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Valido']));
    end;
    Self.Valido := l_Valido;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TTarjeta.WriteComplex(ASerializer: TObject);
var
  l_Cantidad: Double;
  l_CentroCostos: AnsiString;
  l_Clasificacion: AnsiString;
  l_ClienteID: Integer;
  l_GasolineroID: Integer;
  l_Identificacion: AnsiString;
  l_LitrosImporte: AnsiString;
  l_Mensaje: AnsiString;
  l_MsnError: AnsiString;
  l_Nombre: AnsiString;
  l_Productos: AnsiString;
  l_RazonSocial: AnsiString;
  l_RFC: AnsiString;
  l_Saldo: Double;
  l_Status: Integer;
  l_Valido: Boolean;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTarjeta);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_LitrosImporte := Self.LitrosImporte;
    TROSerializer(ASerializer).WriteAnsiString('LitrosImporte', l_LitrosImporte);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteInteger('Status', TypInfo.otSLong, l_Status);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Identificacion := Self.Identificacion;
    TROSerializer(ASerializer).WriteAnsiString('Identificacion', l_Identificacion);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_CentroCostos := Self.CentroCostos;
    TROSerializer(ASerializer).WriteAnsiString('CentroCostos', l_CentroCostos);
    l_Clasificacion := Self.Clasificacion;
    TROSerializer(ASerializer).WriteAnsiString('Clasificacion', l_Clasificacion);
    l_Productos := Self.Productos;
    TROSerializer(ASerializer).WriteAnsiString('Productos', l_Productos);
  end
  else begin
    l_Cantidad := Self.Cantidad;
    TROSerializer(ASerializer).WriteDouble('Cantidad', TypInfo.ftDouble, l_Cantidad);
    l_CentroCostos := Self.CentroCostos;
    TROSerializer(ASerializer).WriteAnsiString('CentroCostos', l_CentroCostos);
    l_Clasificacion := Self.Clasificacion;
    TROSerializer(ASerializer).WriteAnsiString('Clasificacion', l_Clasificacion);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_GasolineroID := Self.GasolineroID;
    TROSerializer(ASerializer).WriteInteger('GasolineroID', TypInfo.otSLong, l_GasolineroID);
    l_Identificacion := Self.Identificacion;
    TROSerializer(ASerializer).WriteAnsiString('Identificacion', l_Identificacion);
    l_LitrosImporte := Self.LitrosImporte;
    TROSerializer(ASerializer).WriteAnsiString('LitrosImporte', l_LitrosImporte);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_MsnError := Self.MsnError;
    TROSerializer(ASerializer).WriteAnsiString('MsnError', l_MsnError);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Productos := Self.Productos;
    TROSerializer(ASerializer).WriteAnsiString('Productos', l_Productos);
    l_RazonSocial := Self.RazonSocial;
    TROSerializer(ASerializer).WriteAnsiString('RazonSocial', l_RazonSocial);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Status := Self.Status;
    TROSerializer(ASerializer).WriteInteger('Status', TypInfo.otSLong, l_Status);
    l_Valido := Self.Valido;
    TROSerializer(ASerializer).WriteEnumerated('Valido',System.TypeInfo(boolean), l_Valido);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TTarjetaCollection }
constructor TTarjetaCollection.Create;
begin
  inherited Create(TTarjeta);
end;

constructor TTarjetaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTarjetaCollection.Add: TTarjeta;
begin
  Result := TTarjeta(inherited Add);
end;

function TTarjetaCollection.GetItems(aIndex: Integer): TTarjeta;
begin
  Result := TTarjeta(inherited Items[aIndex]);
end;

procedure TTarjetaCollection.SetItems(aIndex: Integer; const Value: TTarjeta);
begin
  TTarjeta(inherited Items[aIndex]).Assign(Value);
end;

{ TTarjetaManual }

procedure TTarjetaManual.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTarjetaManual;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTarjetaManual) then begin
    lSource := LibraryValesCupones_Intf.TTarjetaManual(iSource);

    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.ClienteNombre := lSource.ClienteNombre;
    Self.Referencia := lSource.Referencia;
  end;
end;

procedure TTarjetaManual.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_ClienteNombre: AnsiString;
  l_Referencia: AnsiString;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_ClienteNombre := Self.ClienteNombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClienteNombre', l_ClienteNombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteNombre']));
    end;
    Self.ClienteNombre := l_ClienteNombre;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_ClienteNombre := Self.ClienteNombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClienteNombre', l_ClienteNombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteNombre']));
    end;
    Self.ClienteNombre := l_ClienteNombre;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TTarjetaManual.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_ClienteNombre: AnsiString;
  l_Referencia: AnsiString;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTarjetaManual);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_ClienteNombre := Self.ClienteNombre;
    TROSerializer(ASerializer).WriteAnsiString('ClienteNombre', l_ClienteNombre);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_ClienteNombre := Self.ClienteNombre;
    TROSerializer(ASerializer).WriteAnsiString('ClienteNombre', l_ClienteNombre);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TTarjetaManualCollection }
constructor TTarjetaManualCollection.Create;
begin
  inherited Create(TTarjetaManual);
end;

constructor TTarjetaManualCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTarjetaManualCollection.Add: TTarjetaManual;
begin
  Result := TTarjetaManual(inherited Add);
end;

function TTarjetaManualCollection.GetItems(aIndex: Integer): TTarjetaManual;
begin
  Result := TTarjetaManual(inherited Items[aIndex]);
end;

procedure TTarjetaManualCollection.SetItems(aIndex: Integer; const Value: TTarjetaManual);
begin
  TTarjetaManual(inherited Items[aIndex]).Assign(Value);
end;

{ TNodo }

procedure TNodo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TNodo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TNodo) then begin
    lSource := LibraryValesCupones_Intf.TNodo(iSource);

    Self.Nombre := lSource.Nombre;
    Self.Tipo := lSource.Tipo;
    Self.Gasolinero := lSource.Gasolinero;
    Self.Host := lSource.Host;
  end;
end;

procedure TNodo.ReadComplex(ASerializer: TObject);
var
  l_Gasolinero: Integer;
  l_Host: AnsiString;
  l_Nombre: AnsiString;
  l_Tipo: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
    l_Gasolinero := Self.Gasolinero;
    try
      TROSerializer(ASerializer).ReadInteger('Gasolinero', TypInfo.otSLong, l_Gasolinero);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolinero']));
    end;
    Self.Gasolinero := l_Gasolinero;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
  end
  else begin
    l_Gasolinero := Self.Gasolinero;
    try
      TROSerializer(ASerializer).ReadInteger('Gasolinero', TypInfo.otSLong, l_Gasolinero);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolinero']));
    end;
    Self.Gasolinero := l_Gasolinero;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Tipo := Self.Tipo;
    try
      TROSerializer(ASerializer).ReadInteger('Tipo', TypInfo.otSLong, l_Tipo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tipo']));
    end;
    Self.Tipo := l_Tipo;
  end;
end;

procedure TNodo.WriteComplex(ASerializer: TObject);
var
  l_Gasolinero: Integer;
  l_Host: AnsiString;
  l_Nombre: AnsiString;
  l_Tipo: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TNodo);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
    l_Gasolinero := Self.Gasolinero;
    TROSerializer(ASerializer).WriteInteger('Gasolinero', TypInfo.otSLong, l_Gasolinero);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
  end
  else begin
    l_Gasolinero := Self.Gasolinero;
    TROSerializer(ASerializer).WriteInteger('Gasolinero', TypInfo.otSLong, l_Gasolinero);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Tipo := Self.Tipo;
    TROSerializer(ASerializer).WriteInteger('Tipo', TypInfo.otSLong, l_Tipo);
  end;
end;

{ TNodoCollection }
constructor TNodoCollection.Create;
begin
  inherited Create(TNodo);
end;

constructor TNodoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TNodoCollection.Add: TNodo;
begin
  Result := TNodo(inherited Add);
end;

function TNodoCollection.GetItems(aIndex: Integer): TNodo;
begin
  Result := TNodo(inherited Items[aIndex]);
end;

procedure TNodoCollection.LoadFromArray(anArray: aNodos);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TNodoCollection.SaveToArray(anArray: aNodos);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TNodo(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TNodoCollection.SetItems(aIndex: Integer; const Value: TNodo);
begin
  TNodo(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosVehiculo }

procedure TDatosVehiculo.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosVehiculo;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosVehiculo) then begin
    lSource := LibraryValesCupones_Intf.TDatosVehiculo(iSource);

    Self.ClienteID := lSource.ClienteID;
    Self.VehiculoID := lSource.VehiculoID;
    Self.ClienteNombre := lSource.ClienteNombre;
    Self.Referencia := lSource.Referencia;
  end;
end;

procedure TDatosVehiculo.ReadComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_ClienteNombre: AnsiString;
  l_Referencia: AnsiString;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
    l_ClienteNombre := Self.ClienteNombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClienteNombre', l_ClienteNombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteNombre']));
    end;
    Self.ClienteNombre := l_ClienteNombre;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
  end
  else begin
    l_ClienteID := Self.ClienteID;
    try
      TROSerializer(ASerializer).ReadInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteID']));
    end;
    Self.ClienteID := l_ClienteID;
    l_ClienteNombre := Self.ClienteNombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClienteNombre', l_ClienteNombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClienteNombre']));
    end;
    Self.ClienteNombre := l_ClienteNombre;
    l_Referencia := Self.Referencia;
    try
      TROSerializer(ASerializer).ReadAnsiString('Referencia', l_Referencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Referencia']));
    end;
    Self.Referencia := l_Referencia;
    l_VehiculoID := Self.VehiculoID;
    try
      TROSerializer(ASerializer).ReadInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VehiculoID']));
    end;
    Self.VehiculoID := l_VehiculoID;
  end;
end;

procedure TDatosVehiculo.WriteComplex(ASerializer: TObject);
var
  l_ClienteID: Integer;
  l_ClienteNombre: AnsiString;
  l_Referencia: AnsiString;
  l_VehiculoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosVehiculo);
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
    l_ClienteNombre := Self.ClienteNombre;
    TROSerializer(ASerializer).WriteAnsiString('ClienteNombre', l_ClienteNombre);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
  end
  else begin
    l_ClienteID := Self.ClienteID;
    TROSerializer(ASerializer).WriteInteger('ClienteID', TypInfo.otSLong, l_ClienteID);
    l_ClienteNombre := Self.ClienteNombre;
    TROSerializer(ASerializer).WriteAnsiString('ClienteNombre', l_ClienteNombre);
    l_Referencia := Self.Referencia;
    TROSerializer(ASerializer).WriteAnsiString('Referencia', l_Referencia);
    l_VehiculoID := Self.VehiculoID;
    TROSerializer(ASerializer).WriteInteger('VehiculoID', TypInfo.otSLong, l_VehiculoID);
  end;
end;

{ TDatosVehiculoCollection }
constructor TDatosVehiculoCollection.Create;
begin
  inherited Create(TDatosVehiculo);
end;

constructor TDatosVehiculoCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosVehiculoCollection.Add: TDatosVehiculo;
begin
  Result := TDatosVehiculo(inherited Add);
end;

function TDatosVehiculoCollection.GetItems(aIndex: Integer): TDatosVehiculo;
begin
  Result := TDatosVehiculo(inherited Items[aIndex]);
end;

procedure TDatosVehiculoCollection.SetItems(aIndex: Integer; const Value: TDatosVehiculo);
begin
  TDatosVehiculo(inherited Items[aIndex]).Assign(Value);
end;

{ TDatosVerifica }

procedure TDatosVerifica.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDatosVerifica;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDatosVerifica) then begin
    lSource := LibraryValesCupones_Intf.TDatosVerifica(iSource);

    Self.AfectarHistorico := lSource.AfectarHistorico;
    Self.KMS := lSource.KMS;
  end;
end;

procedure TDatosVerifica.ReadComplex(ASerializer: TObject);
var
  l_AfectarHistorico: Boolean;
  l_KMS: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_AfectarHistorico := Self.AfectarHistorico;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarHistorico',System.TypeInfo(boolean), l_AfectarHistorico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarHistorico']));
    end;
    Self.AfectarHistorico := l_AfectarHistorico;
    l_KMS := Self.KMS;
    try
      TROSerializer(ASerializer).ReadInteger('KMS', TypInfo.otSLong, l_KMS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'KMS']));
    end;
    Self.KMS := l_KMS;
  end
  else begin
    l_AfectarHistorico := Self.AfectarHistorico;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarHistorico',System.TypeInfo(boolean), l_AfectarHistorico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarHistorico']));
    end;
    Self.AfectarHistorico := l_AfectarHistorico;
    l_KMS := Self.KMS;
    try
      TROSerializer(ASerializer).ReadInteger('KMS', TypInfo.otSLong, l_KMS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'KMS']));
    end;
    Self.KMS := l_KMS;
  end;
end;

procedure TDatosVerifica.WriteComplex(ASerializer: TObject);
var
  l_AfectarHistorico: Boolean;
  l_KMS: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDatosVerifica);
    l_AfectarHistorico := Self.AfectarHistorico;
    TROSerializer(ASerializer).WriteEnumerated('AfectarHistorico',System.TypeInfo(boolean), l_AfectarHistorico);
    l_KMS := Self.KMS;
    TROSerializer(ASerializer).WriteInteger('KMS', TypInfo.otSLong, l_KMS);
  end
  else begin
    l_AfectarHistorico := Self.AfectarHistorico;
    TROSerializer(ASerializer).WriteEnumerated('AfectarHistorico',System.TypeInfo(boolean), l_AfectarHistorico);
    l_KMS := Self.KMS;
    TROSerializer(ASerializer).WriteInteger('KMS', TypInfo.otSLong, l_KMS);
  end;
end;

{ TDatosVerificaCollection }
constructor TDatosVerificaCollection.Create;
begin
  inherited Create(TDatosVerifica);
end;

constructor TDatosVerificaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDatosVerificaCollection.Add: TDatosVerifica;
begin
  Result := TDatosVerifica(inherited Add);
end;

function TDatosVerificaCollection.GetItems(aIndex: Integer): TDatosVerifica;
begin
  Result := TDatosVerifica(inherited Items[aIndex]);
end;

procedure TDatosVerificaCollection.SetItems(aIndex: Integer; const Value: TDatosVerifica);
begin
  TDatosVerifica(inherited Items[aIndex]).Assign(Value);
end;

{ TGlobal }

procedure TGlobal.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TGlobal;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TGlobal) then begin
    lSource := LibraryValesCupones_Intf.TGlobal(iSource);

    Self.AfectarDecimal := lSource.AfectarDecimal;
    Self.AfectarLitros := lSource.AfectarLitros;
    Self.AutorizacionPorManguera := lSource.AutorizacionPorManguera;
    Self.blnProdArriba := lSource.blnProdArriba;
    Self.bolStrop := lSource.bolStrop;
    if System.Assigned(lSource.fBombas) then
      Self.Bombas.Assign(lSource.fBombas)
    else
      SysUtils.FreeAndNil(fBombas);
    Self.cAck := lSource.cAck;
    if System.Assigned(lSource.fCambioPrecio) then
      Self.CambioPrecio.Assign(lSource.fCambioPrecio)
    else
      SysUtils.FreeAndNil(fCambioPrecio);
    Self.cCR := lSource.cCR;
    Self.cEsc := lSource.cEsc;
    Self.cExt := lSource.cExt;
    Self.cLF := lSource.cLF;
    Self.cNak := lSource.cNak;
    if System.Assigned(lSource.fConexiones) then
      Self.Conexiones.Assign(lSource.fConexiones)
    else
      SysUtils.FreeAndNil(fConexiones);
    Self.CortarSuministrosSinAut := lSource.CortarSuministrosSinAut;
    Self.eStx := lSource.eStx;
    Self.cUsuario := lSource.cUsuario;
    if System.Assigned(lSource.fDispositivos) then
      Self.Dispositivos.Assign(lSource.fDispositivos)
    else
      SysUtils.FreeAndNil(fDispositivos);
    Self.Estacion := lSource.Estacion;
    Self.Fabricante := lSource.Fabricante;
    Self.FlujoActual := lSource.FlujoActual;
    if System.Assigned(lSource.fGasolinas) then
      Self.Gasolinas.Assign(lSource.fGasolinas)
    else
      SysUtils.FreeAndNil(fGasolinas);
    Self.idNivelUsuario := lSource.idNivelUsuario;
    Self.idUsuario := lSource.idUsuario;
    Self.lPoleoHora := lSource.lPoleoHora;
    Self.Maquina := lSource.Maquina;
    Self.MetodoOperacion := lSource.MetodoOperacion;
    Self.MetodoTurno := lSource.MetodoTurno;
    Self.Monitoreo := lSource.Monitoreo;
    Self.MSG_CISTEMGAS := lSource.MSG_CISTEMGAS;
    Self.NombreEstacion := lSource.NombreEstacion;
    Self.NumPuerto := lSource.NumPuerto;
    Self.NumSite := lSource.NumSite;
    Self.nVueltasGenerales := lSource.nVueltasGenerales;
    Self.PermitirCorteSinVenta := lSource.PermitirCorteSinVenta;
    Self.PrecioInferior := lSource.PrecioInferior;
    Self.PrecioSuperior := lSource.PrecioSuperior;
    if System.Assigned(lSource.fProtecciones) then
      Self.Protecciones.Assign(lSource.fProtecciones)
    else
      SysUtils.FreeAndNil(fProtecciones);
    Self.RegistroVenta := lSource.RegistroVenta;
    Self.Respuesta := lSource.Respuesta;
    Self.RFC := lSource.RFC;
    Self.SIIC := lSource.SIIC;
    Self.SinAfectar := lSource.SinAfectar;
    Self.StatusCtrolVol := lSource.StatusCtrolVol;
    Self.StrCmdPendiente := lSource.StrCmdPendiente;
    if System.Assigned(lSource.fTanques) then
      Self.Tanques.Assign(lSource.fTanques)
    else
      SysUtils.FreeAndNil(fTanques);
    Self.Telemedicion := lSource.Telemedicion;
    Self.TipoEstacion := lSource.TipoEstacion;
    Self.TotalBombas := lSource.TotalBombas;
    Self.TotalCiclos := lSource.TotalCiclos;
    Self.TurnoActual := lSource.TurnoActual;
    Self.TurnosActuales := lSource.TurnosActuales;
    Self.VentaSeleccionada := lSource.VentaSeleccionada;
    if System.Assigned(lSource.fLicencia) then
      Self.Licencia.Assign(lSource.fLicencia)
    else
      SysUtils.FreeAndNil(fLicencia);
  end;
end;

function TGlobal.GetBombas: aBombas;
begin
  if (fBombas = nil) then fBombas := aBombas.Create();
  Result := fBombas;
end;

function TGlobal.GetCambioPrecio: TCambio;
begin
  if (fCambioPrecio = nil) then fCambioPrecio := TCambio.Create();
  Result := fCambioPrecio;
end;

function TGlobal.GetConexiones: aDispositivos;
begin
  if (fConexiones = nil) then fConexiones := aDispositivos.Create();
  Result := fConexiones;
end;

function TGlobal.GetDispositivos: aDispositivos;
begin
  if (fDispositivos = nil) then fDispositivos := aDispositivos.Create();
  Result := fDispositivos;
end;

function TGlobal.GetGasolinas: aGas;
begin
  if (fGasolinas = nil) then fGasolinas := aGas.Create();
  Result := fGasolinas;
end;

function TGlobal.GetProtecciones: TProtecciones;
begin
  if (fProtecciones = nil) then fProtecciones := TProtecciones.Create();
  Result := fProtecciones;
end;

function TGlobal.GetTanques: aTanques;
begin
  if (fTanques = nil) then fTanques := aTanques.Create();
  Result := fTanques;
end;

function TGlobal.GetLicencia: TLicencia;
begin
  if (fLicencia = nil) then fLicencia := TLicencia.Create();
  Result := fLicencia;
end;

procedure TGlobal.ReadComplex(ASerializer: TObject);
var
  l_AfectarDecimal: Boolean;
  l_AfectarLitros: Boolean;
  l_AutorizacionPorManguera: Boolean;
  l_blnProdArriba: Boolean;
  l_bolStrop: Boolean;
  l_Bombas: aBombas;
  l_cAck: AnsiString;
  l_CambioPrecio: TCambio;
  l_cCR: AnsiString;
  l_cEsc: AnsiString;
  l_cExt: AnsiString;
  l_cLF: AnsiString;
  l_cNak: AnsiString;
  l_Conexiones: aDispositivos;
  l_CortarSuministrosSinAut: Boolean;
  l_cUsuario: AnsiString;
  l_Dispositivos: aDispositivos;
  l_Estacion: Integer;
  l_eStx: AnsiString;
  l_Fabricante: eFabricante;
  l_FlujoActual: Integer;
  l_Gasolinas: aGas;
  l_idNivelUsuario: Integer;
  l_idUsuario: Integer;
  l_Licencia: TLicencia;
  l_lPoleoHora: Boolean;
  l_Maquina: AnsiString;
  l_MetodoOperacion: Integer;
  l_MetodoTurno: Integer;
  l_Monitoreo: eMonitoreo;
  l_MSG_CISTEMGAS: Integer;
  l_NombreEstacion: AnsiString;
  l_NumPuerto: Integer;
  l_NumSite: AnsiString;
  l_nVueltasGenerales: Integer;
  l_PermitirCorteSinVenta: Boolean;
  l_PrecioInferior: Double;
  l_PrecioSuperior: Double;
  l_Protecciones: TProtecciones;
  l_RegistroVenta: Integer;
  l_Respuesta: AnsiString;
  l_RFC: AnsiString;
  l_SIIC: AnsiString;
  l_SinAfectar: Boolean;
  l_StatusCtrolVol: Integer;
  l_StrCmdPendiente: AnsiString;
  l_Tanques: aTanques;
  l_Telemedicion: Integer;
  l_TipoEstacion: Integer;
  l_TotalBombas: Integer;
  l_TotalCiclos: Integer;
  l_TurnoActual: Integer;
  l_TurnosActuales: Integer;
  l_VentaSeleccionada: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_AfectarDecimal := Self.AfectarDecimal;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarDecimal',System.TypeInfo(boolean), l_AfectarDecimal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarDecimal']));
    end;
    Self.AfectarDecimal := l_AfectarDecimal;
    l_AfectarLitros := Self.AfectarLitros;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarLitros',System.TypeInfo(boolean), l_AfectarLitros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarLitros']));
    end;
    Self.AfectarLitros := l_AfectarLitros;
    l_AutorizacionPorManguera := Self.AutorizacionPorManguera;
    try
      TROSerializer(ASerializer).ReadEnumerated('AutorizacionPorManguera',System.TypeInfo(boolean), l_AutorizacionPorManguera);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionPorManguera']));
    end;
    Self.AutorizacionPorManguera := l_AutorizacionPorManguera;
    l_blnProdArriba := Self.blnProdArriba;
    try
      TROSerializer(ASerializer).ReadEnumerated('blnProdArriba',System.TypeInfo(boolean), l_blnProdArriba);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'blnProdArriba']));
    end;
    Self.blnProdArriba := l_blnProdArriba;
    l_bolStrop := Self.bolStrop;
    try
      TROSerializer(ASerializer).ReadEnumerated('bolStrop',System.TypeInfo(boolean), l_bolStrop);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'bolStrop']));
    end;
    Self.bolStrop := l_bolStrop;
    l_Bombas := int_Bombas;
    try
      TROSerializer(ASerializer).ReadArray('Bombas', aBombas, l_Bombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bombas']));
    end;
    if int_Bombas <> l_Bombas then Self.Bombas.Free;
    Self.Bombas := l_Bombas;
    l_cAck := Self.cAck;
    try
      TROSerializer(ASerializer).ReadAnsiString('cAck', l_cAck);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cAck']));
    end;
    Self.cAck := l_cAck;
    l_CambioPrecio := int_CambioPrecio;
    try
      TROSerializer(ASerializer).ReadStruct('CambioPrecio', TCambio, l_CambioPrecio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CambioPrecio']));
    end;
    if int_CambioPrecio <> l_CambioPrecio then Self.CambioPrecio.Free;
    Self.CambioPrecio := l_CambioPrecio;
    l_cCR := Self.cCR;
    try
      TROSerializer(ASerializer).ReadAnsiString('cCR', l_cCR);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cCR']));
    end;
    Self.cCR := l_cCR;
    l_cEsc := Self.cEsc;
    try
      TROSerializer(ASerializer).ReadAnsiString('cEsc', l_cEsc);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cEsc']));
    end;
    Self.cEsc := l_cEsc;
    l_cExt := Self.cExt;
    try
      TROSerializer(ASerializer).ReadAnsiString('cExt', l_cExt);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cExt']));
    end;
    Self.cExt := l_cExt;
    l_cLF := Self.cLF;
    try
      TROSerializer(ASerializer).ReadAnsiString('cLF', l_cLF);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cLF']));
    end;
    Self.cLF := l_cLF;
    l_cNak := Self.cNak;
    try
      TROSerializer(ASerializer).ReadAnsiString('cNak', l_cNak);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cNak']));
    end;
    Self.cNak := l_cNak;
    l_Conexiones := int_Conexiones;
    try
      TROSerializer(ASerializer).ReadArray('Conexiones', aDispositivos, l_Conexiones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Conexiones']));
    end;
    if int_Conexiones <> l_Conexiones then Self.Conexiones.Free;
    Self.Conexiones := l_Conexiones;
    l_CortarSuministrosSinAut := Self.CortarSuministrosSinAut;
    try
      TROSerializer(ASerializer).ReadEnumerated('CortarSuministrosSinAut',System.TypeInfo(boolean), l_CortarSuministrosSinAut);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CortarSuministrosSinAut']));
    end;
    Self.CortarSuministrosSinAut := l_CortarSuministrosSinAut;
    l_eStx := Self.eStx;
    try
      TROSerializer(ASerializer).ReadAnsiString('eStx', l_eStx);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'eStx']));
    end;
    Self.eStx := l_eStx;
    l_cUsuario := Self.cUsuario;
    try
      TROSerializer(ASerializer).ReadAnsiString('cUsuario', l_cUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cUsuario']));
    end;
    Self.cUsuario := l_cUsuario;
    l_Dispositivos := int_Dispositivos;
    try
      TROSerializer(ASerializer).ReadArray('Dispositivos', aDispositivos, l_Dispositivos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dispositivos']));
    end;
    if int_Dispositivos <> l_Dispositivos then Self.Dispositivos.Free;
    Self.Dispositivos := l_Dispositivos;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_Fabricante := Self.Fabricante;
    try
      TROSerializer(ASerializer).ReadEnumerated('Fabricante',System.TypeInfo(eFabricante), l_Fabricante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fabricante']));
    end;
    Self.Fabricante := l_Fabricante;
    l_FlujoActual := Self.FlujoActual;
    try
      TROSerializer(ASerializer).ReadInteger('FlujoActual', TypInfo.otSLong, l_FlujoActual);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FlujoActual']));
    end;
    Self.FlujoActual := l_FlujoActual;
    l_Gasolinas := int_Gasolinas;
    try
      TROSerializer(ASerializer).ReadArray('Gasolinas', aGas, l_Gasolinas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolinas']));
    end;
    if int_Gasolinas <> l_Gasolinas then Self.Gasolinas.Free;
    Self.Gasolinas := l_Gasolinas;
    l_idNivelUsuario := Self.idNivelUsuario;
    try
      TROSerializer(ASerializer).ReadInteger('idNivelUsuario', TypInfo.otSLong, l_idNivelUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'idNivelUsuario']));
    end;
    Self.idNivelUsuario := l_idNivelUsuario;
    l_idUsuario := Self.idUsuario;
    try
      TROSerializer(ASerializer).ReadInteger('idUsuario', TypInfo.otSLong, l_idUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'idUsuario']));
    end;
    Self.idUsuario := l_idUsuario;
    l_lPoleoHora := Self.lPoleoHora;
    try
      TROSerializer(ASerializer).ReadEnumerated('lPoleoHora',System.TypeInfo(boolean), l_lPoleoHora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'lPoleoHora']));
    end;
    Self.lPoleoHora := l_lPoleoHora;
    l_Maquina := Self.Maquina;
    try
      TROSerializer(ASerializer).ReadAnsiString('Maquina', l_Maquina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Maquina']));
    end;
    Self.Maquina := l_Maquina;
    l_MetodoOperacion := Self.MetodoOperacion;
    try
      TROSerializer(ASerializer).ReadInteger('MetodoOperacion', TypInfo.otSLong, l_MetodoOperacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MetodoOperacion']));
    end;
    Self.MetodoOperacion := l_MetodoOperacion;
    l_MetodoTurno := Self.MetodoTurno;
    try
      TROSerializer(ASerializer).ReadInteger('MetodoTurno', TypInfo.otSLong, l_MetodoTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MetodoTurno']));
    end;
    Self.MetodoTurno := l_MetodoTurno;
    l_Monitoreo := Self.Monitoreo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Monitoreo',System.TypeInfo(eMonitoreo), l_Monitoreo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Monitoreo']));
    end;
    Self.Monitoreo := l_Monitoreo;
    l_MSG_CISTEMGAS := Self.MSG_CISTEMGAS;
    try
      TROSerializer(ASerializer).ReadInteger('MSG_CISTEMGAS', TypInfo.otSLong, l_MSG_CISTEMGAS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MSG_CISTEMGAS']));
    end;
    Self.MSG_CISTEMGAS := l_MSG_CISTEMGAS;
    l_NombreEstacion := Self.NombreEstacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreEstacion', l_NombreEstacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreEstacion']));
    end;
    Self.NombreEstacion := l_NombreEstacion;
    l_NumPuerto := Self.NumPuerto;
    try
      TROSerializer(ASerializer).ReadInteger('NumPuerto', TypInfo.otSLong, l_NumPuerto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NumPuerto']));
    end;
    Self.NumPuerto := l_NumPuerto;
    l_NumSite := Self.NumSite;
    try
      TROSerializer(ASerializer).ReadAnsiString('NumSite', l_NumSite);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NumSite']));
    end;
    Self.NumSite := l_NumSite;
    l_nVueltasGenerales := Self.nVueltasGenerales;
    try
      TROSerializer(ASerializer).ReadInteger('nVueltasGenerales', TypInfo.otSLong, l_nVueltasGenerales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'nVueltasGenerales']));
    end;
    Self.nVueltasGenerales := l_nVueltasGenerales;
    l_PermitirCorteSinVenta := Self.PermitirCorteSinVenta;
    try
      TROSerializer(ASerializer).ReadEnumerated('PermitirCorteSinVenta',System.TypeInfo(boolean), l_PermitirCorteSinVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PermitirCorteSinVenta']));
    end;
    Self.PermitirCorteSinVenta := l_PermitirCorteSinVenta;
    l_PrecioInferior := Self.PrecioInferior;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioInferior', TypInfo.ftDouble, l_PrecioInferior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioInferior']));
    end;
    Self.PrecioInferior := l_PrecioInferior;
    l_PrecioSuperior := Self.PrecioSuperior;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioSuperior', TypInfo.ftDouble, l_PrecioSuperior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioSuperior']));
    end;
    Self.PrecioSuperior := l_PrecioSuperior;
    l_Protecciones := int_Protecciones;
    try
      TROSerializer(ASerializer).ReadStruct('Protecciones', TProtecciones, l_Protecciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Protecciones']));
    end;
    if int_Protecciones <> l_Protecciones then Self.Protecciones.Free;
    Self.Protecciones := l_Protecciones;
    l_RegistroVenta := Self.RegistroVenta;
    try
      TROSerializer(ASerializer).ReadInteger('RegistroVenta', TypInfo.otSLong, l_RegistroVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RegistroVenta']));
    end;
    Self.RegistroVenta := l_RegistroVenta;
    l_Respuesta := Self.Respuesta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Respuesta', l_Respuesta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Respuesta']));
    end;
    Self.Respuesta := l_Respuesta;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_SIIC := Self.SIIC;
    try
      TROSerializer(ASerializer).ReadAnsiString('SIIC', l_SIIC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SIIC']));
    end;
    Self.SIIC := l_SIIC;
    l_SinAfectar := Self.SinAfectar;
    try
      TROSerializer(ASerializer).ReadEnumerated('SinAfectar',System.TypeInfo(boolean), l_SinAfectar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SinAfectar']));
    end;
    Self.SinAfectar := l_SinAfectar;
    l_StatusCtrolVol := Self.StatusCtrolVol;
    try
      TROSerializer(ASerializer).ReadInteger('StatusCtrolVol', TypInfo.otSLong, l_StatusCtrolVol);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StatusCtrolVol']));
    end;
    Self.StatusCtrolVol := l_StatusCtrolVol;
    l_StrCmdPendiente := Self.StrCmdPendiente;
    try
      TROSerializer(ASerializer).ReadAnsiString('StrCmdPendiente', l_StrCmdPendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StrCmdPendiente']));
    end;
    Self.StrCmdPendiente := l_StrCmdPendiente;
    l_Tanques := int_Tanques;
    try
      TROSerializer(ASerializer).ReadArray('Tanques', aTanques, l_Tanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanques']));
    end;
    if int_Tanques <> l_Tanques then Self.Tanques.Free;
    Self.Tanques := l_Tanques;
    l_Telemedicion := Self.Telemedicion;
    try
      TROSerializer(ASerializer).ReadInteger('Telemedicion', TypInfo.otSLong, l_Telemedicion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telemedicion']));
    end;
    Self.Telemedicion := l_Telemedicion;
    l_TipoEstacion := Self.TipoEstacion;
    try
      TROSerializer(ASerializer).ReadInteger('TipoEstacion', TypInfo.otSLong, l_TipoEstacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoEstacion']));
    end;
    Self.TipoEstacion := l_TipoEstacion;
    l_TotalBombas := Self.TotalBombas;
    try
      TROSerializer(ASerializer).ReadInteger('TotalBombas', TypInfo.otSLong, l_TotalBombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalBombas']));
    end;
    Self.TotalBombas := l_TotalBombas;
    l_TotalCiclos := Self.TotalCiclos;
    try
      TROSerializer(ASerializer).ReadInteger('TotalCiclos', TypInfo.otSLong, l_TotalCiclos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalCiclos']));
    end;
    Self.TotalCiclos := l_TotalCiclos;
    l_TurnoActual := Self.TurnoActual;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoActual', TypInfo.otSLong, l_TurnoActual);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoActual']));
    end;
    Self.TurnoActual := l_TurnoActual;
    l_TurnosActuales := Self.TurnosActuales;
    try
      TROSerializer(ASerializer).ReadInteger('TurnosActuales', TypInfo.otSLong, l_TurnosActuales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnosActuales']));
    end;
    Self.TurnosActuales := l_TurnosActuales;
    l_VentaSeleccionada := Self.VentaSeleccionada;
    try
      TROSerializer(ASerializer).ReadInteger('VentaSeleccionada', TypInfo.otSLong, l_VentaSeleccionada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaSeleccionada']));
    end;
    Self.VentaSeleccionada := l_VentaSeleccionada;
    l_Licencia := int_Licencia;
    try
      TROSerializer(ASerializer).ReadStruct('Licencia', TLicencia, l_Licencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Licencia']));
    end;
    if int_Licencia <> l_Licencia then Self.Licencia.Free;
    Self.Licencia := l_Licencia;
  end
  else begin
    l_AfectarDecimal := Self.AfectarDecimal;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarDecimal',System.TypeInfo(boolean), l_AfectarDecimal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarDecimal']));
    end;
    Self.AfectarDecimal := l_AfectarDecimal;
    l_AfectarLitros := Self.AfectarLitros;
    try
      TROSerializer(ASerializer).ReadEnumerated('AfectarLitros',System.TypeInfo(boolean), l_AfectarLitros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AfectarLitros']));
    end;
    Self.AfectarLitros := l_AfectarLitros;
    l_AutorizacionPorManguera := Self.AutorizacionPorManguera;
    try
      TROSerializer(ASerializer).ReadEnumerated('AutorizacionPorManguera',System.TypeInfo(boolean), l_AutorizacionPorManguera);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionPorManguera']));
    end;
    Self.AutorizacionPorManguera := l_AutorizacionPorManguera;
    l_blnProdArriba := Self.blnProdArriba;
    try
      TROSerializer(ASerializer).ReadEnumerated('blnProdArriba',System.TypeInfo(boolean), l_blnProdArriba);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'blnProdArriba']));
    end;
    Self.blnProdArriba := l_blnProdArriba;
    l_bolStrop := Self.bolStrop;
    try
      TROSerializer(ASerializer).ReadEnumerated('bolStrop',System.TypeInfo(boolean), l_bolStrop);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'bolStrop']));
    end;
    Self.bolStrop := l_bolStrop;
    l_Bombas := int_Bombas;
    try
      TROSerializer(ASerializer).ReadArray('Bombas', aBombas, l_Bombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Bombas']));
    end;
    if int_Bombas <> l_Bombas then Bombas.Free;
    Self.Bombas := l_Bombas;
    l_cAck := Self.cAck;
    try
      TROSerializer(ASerializer).ReadAnsiString('cAck', l_cAck);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cAck']));
    end;
    Self.cAck := l_cAck;
    l_CambioPrecio := int_CambioPrecio;
    try
      TROSerializer(ASerializer).ReadStruct('CambioPrecio', TCambio, l_CambioPrecio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CambioPrecio']));
    end;
    if int_CambioPrecio <> l_CambioPrecio then CambioPrecio.Free;
    Self.CambioPrecio := l_CambioPrecio;
    l_cCR := Self.cCR;
    try
      TROSerializer(ASerializer).ReadAnsiString('cCR', l_cCR);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cCR']));
    end;
    Self.cCR := l_cCR;
    l_cEsc := Self.cEsc;
    try
      TROSerializer(ASerializer).ReadAnsiString('cEsc', l_cEsc);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cEsc']));
    end;
    Self.cEsc := l_cEsc;
    l_cExt := Self.cExt;
    try
      TROSerializer(ASerializer).ReadAnsiString('cExt', l_cExt);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cExt']));
    end;
    Self.cExt := l_cExt;
    l_cLF := Self.cLF;
    try
      TROSerializer(ASerializer).ReadAnsiString('cLF', l_cLF);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cLF']));
    end;
    Self.cLF := l_cLF;
    l_cNak := Self.cNak;
    try
      TROSerializer(ASerializer).ReadAnsiString('cNak', l_cNak);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cNak']));
    end;
    Self.cNak := l_cNak;
    l_Conexiones := int_Conexiones;
    try
      TROSerializer(ASerializer).ReadArray('Conexiones', aDispositivos, l_Conexiones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Conexiones']));
    end;
    if int_Conexiones <> l_Conexiones then Conexiones.Free;
    Self.Conexiones := l_Conexiones;
    l_CortarSuministrosSinAut := Self.CortarSuministrosSinAut;
    try
      TROSerializer(ASerializer).ReadEnumerated('CortarSuministrosSinAut',System.TypeInfo(boolean), l_CortarSuministrosSinAut);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CortarSuministrosSinAut']));
    end;
    Self.CortarSuministrosSinAut := l_CortarSuministrosSinAut;
    l_cUsuario := Self.cUsuario;
    try
      TROSerializer(ASerializer).ReadAnsiString('cUsuario', l_cUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'cUsuario']));
    end;
    Self.cUsuario := l_cUsuario;
    l_Dispositivos := int_Dispositivos;
    try
      TROSerializer(ASerializer).ReadArray('Dispositivos', aDispositivos, l_Dispositivos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Dispositivos']));
    end;
    if int_Dispositivos <> l_Dispositivos then Dispositivos.Free;
    Self.Dispositivos := l_Dispositivos;
    l_Estacion := Self.Estacion;
    try
      TROSerializer(ASerializer).ReadInteger('Estacion', TypInfo.otSLong, l_Estacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estacion']));
    end;
    Self.Estacion := l_Estacion;
    l_eStx := Self.eStx;
    try
      TROSerializer(ASerializer).ReadAnsiString('eStx', l_eStx);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'eStx']));
    end;
    Self.eStx := l_eStx;
    l_Fabricante := Self.Fabricante;
    try
      TROSerializer(ASerializer).ReadEnumerated('Fabricante',System.TypeInfo(eFabricante), l_Fabricante);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fabricante']));
    end;
    Self.Fabricante := l_Fabricante;
    l_FlujoActual := Self.FlujoActual;
    try
      TROSerializer(ASerializer).ReadInteger('FlujoActual', TypInfo.otSLong, l_FlujoActual);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FlujoActual']));
    end;
    Self.FlujoActual := l_FlujoActual;
    l_Gasolinas := int_Gasolinas;
    try
      TROSerializer(ASerializer).ReadArray('Gasolinas', aGas, l_Gasolinas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolinas']));
    end;
    if int_Gasolinas <> l_Gasolinas then Gasolinas.Free;
    Self.Gasolinas := l_Gasolinas;
    l_idNivelUsuario := Self.idNivelUsuario;
    try
      TROSerializer(ASerializer).ReadInteger('idNivelUsuario', TypInfo.otSLong, l_idNivelUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'idNivelUsuario']));
    end;
    Self.idNivelUsuario := l_idNivelUsuario;
    l_idUsuario := Self.idUsuario;
    try
      TROSerializer(ASerializer).ReadInteger('idUsuario', TypInfo.otSLong, l_idUsuario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'idUsuario']));
    end;
    Self.idUsuario := l_idUsuario;
    l_Licencia := int_Licencia;
    try
      TROSerializer(ASerializer).ReadStruct('Licencia', TLicencia, l_Licencia);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Licencia']));
    end;
    if int_Licencia <> l_Licencia then Licencia.Free;
    Self.Licencia := l_Licencia;
    l_lPoleoHora := Self.lPoleoHora;
    try
      TROSerializer(ASerializer).ReadEnumerated('lPoleoHora',System.TypeInfo(boolean), l_lPoleoHora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'lPoleoHora']));
    end;
    Self.lPoleoHora := l_lPoleoHora;
    l_Maquina := Self.Maquina;
    try
      TROSerializer(ASerializer).ReadAnsiString('Maquina', l_Maquina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Maquina']));
    end;
    Self.Maquina := l_Maquina;
    l_MetodoOperacion := Self.MetodoOperacion;
    try
      TROSerializer(ASerializer).ReadInteger('MetodoOperacion', TypInfo.otSLong, l_MetodoOperacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MetodoOperacion']));
    end;
    Self.MetodoOperacion := l_MetodoOperacion;
    l_MetodoTurno := Self.MetodoTurno;
    try
      TROSerializer(ASerializer).ReadInteger('MetodoTurno', TypInfo.otSLong, l_MetodoTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MetodoTurno']));
    end;
    Self.MetodoTurno := l_MetodoTurno;
    l_Monitoreo := Self.Monitoreo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Monitoreo',System.TypeInfo(eMonitoreo), l_Monitoreo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Monitoreo']));
    end;
    Self.Monitoreo := l_Monitoreo;
    l_MSG_CISTEMGAS := Self.MSG_CISTEMGAS;
    try
      TROSerializer(ASerializer).ReadInteger('MSG_CISTEMGAS', TypInfo.otSLong, l_MSG_CISTEMGAS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MSG_CISTEMGAS']));
    end;
    Self.MSG_CISTEMGAS := l_MSG_CISTEMGAS;
    l_NombreEstacion := Self.NombreEstacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreEstacion', l_NombreEstacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreEstacion']));
    end;
    Self.NombreEstacion := l_NombreEstacion;
    l_NumPuerto := Self.NumPuerto;
    try
      TROSerializer(ASerializer).ReadInteger('NumPuerto', TypInfo.otSLong, l_NumPuerto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NumPuerto']));
    end;
    Self.NumPuerto := l_NumPuerto;
    l_NumSite := Self.NumSite;
    try
      TROSerializer(ASerializer).ReadAnsiString('NumSite', l_NumSite);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NumSite']));
    end;
    Self.NumSite := l_NumSite;
    l_nVueltasGenerales := Self.nVueltasGenerales;
    try
      TROSerializer(ASerializer).ReadInteger('nVueltasGenerales', TypInfo.otSLong, l_nVueltasGenerales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'nVueltasGenerales']));
    end;
    Self.nVueltasGenerales := l_nVueltasGenerales;
    l_PermitirCorteSinVenta := Self.PermitirCorteSinVenta;
    try
      TROSerializer(ASerializer).ReadEnumerated('PermitirCorteSinVenta',System.TypeInfo(boolean), l_PermitirCorteSinVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PermitirCorteSinVenta']));
    end;
    Self.PermitirCorteSinVenta := l_PermitirCorteSinVenta;
    l_PrecioInferior := Self.PrecioInferior;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioInferior', TypInfo.ftDouble, l_PrecioInferior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioInferior']));
    end;
    Self.PrecioInferior := l_PrecioInferior;
    l_PrecioSuperior := Self.PrecioSuperior;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioSuperior', TypInfo.ftDouble, l_PrecioSuperior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioSuperior']));
    end;
    Self.PrecioSuperior := l_PrecioSuperior;
    l_Protecciones := int_Protecciones;
    try
      TROSerializer(ASerializer).ReadStruct('Protecciones', TProtecciones, l_Protecciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Protecciones']));
    end;
    if int_Protecciones <> l_Protecciones then Protecciones.Free;
    Self.Protecciones := l_Protecciones;
    l_RegistroVenta := Self.RegistroVenta;
    try
      TROSerializer(ASerializer).ReadInteger('RegistroVenta', TypInfo.otSLong, l_RegistroVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RegistroVenta']));
    end;
    Self.RegistroVenta := l_RegistroVenta;
    l_Respuesta := Self.Respuesta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Respuesta', l_Respuesta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Respuesta']));
    end;
    Self.Respuesta := l_Respuesta;
    l_RFC := Self.RFC;
    try
      TROSerializer(ASerializer).ReadAnsiString('RFC', l_RFC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RFC']));
    end;
    Self.RFC := l_RFC;
    l_SIIC := Self.SIIC;
    try
      TROSerializer(ASerializer).ReadAnsiString('SIIC', l_SIIC);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SIIC']));
    end;
    Self.SIIC := l_SIIC;
    l_SinAfectar := Self.SinAfectar;
    try
      TROSerializer(ASerializer).ReadEnumerated('SinAfectar',System.TypeInfo(boolean), l_SinAfectar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'SinAfectar']));
    end;
    Self.SinAfectar := l_SinAfectar;
    l_StatusCtrolVol := Self.StatusCtrolVol;
    try
      TROSerializer(ASerializer).ReadInteger('StatusCtrolVol', TypInfo.otSLong, l_StatusCtrolVol);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StatusCtrolVol']));
    end;
    Self.StatusCtrolVol := l_StatusCtrolVol;
    l_StrCmdPendiente := Self.StrCmdPendiente;
    try
      TROSerializer(ASerializer).ReadAnsiString('StrCmdPendiente', l_StrCmdPendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StrCmdPendiente']));
    end;
    Self.StrCmdPendiente := l_StrCmdPendiente;
    l_Tanques := int_Tanques;
    try
      TROSerializer(ASerializer).ReadArray('Tanques', aTanques, l_Tanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanques']));
    end;
    if int_Tanques <> l_Tanques then Tanques.Free;
    Self.Tanques := l_Tanques;
    l_Telemedicion := Self.Telemedicion;
    try
      TROSerializer(ASerializer).ReadInteger('Telemedicion', TypInfo.otSLong, l_Telemedicion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Telemedicion']));
    end;
    Self.Telemedicion := l_Telemedicion;
    l_TipoEstacion := Self.TipoEstacion;
    try
      TROSerializer(ASerializer).ReadInteger('TipoEstacion', TypInfo.otSLong, l_TipoEstacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoEstacion']));
    end;
    Self.TipoEstacion := l_TipoEstacion;
    l_TotalBombas := Self.TotalBombas;
    try
      TROSerializer(ASerializer).ReadInteger('TotalBombas', TypInfo.otSLong, l_TotalBombas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalBombas']));
    end;
    Self.TotalBombas := l_TotalBombas;
    l_TotalCiclos := Self.TotalCiclos;
    try
      TROSerializer(ASerializer).ReadInteger('TotalCiclos', TypInfo.otSLong, l_TotalCiclos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalCiclos']));
    end;
    Self.TotalCiclos := l_TotalCiclos;
    l_TurnoActual := Self.TurnoActual;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoActual', TypInfo.otSLong, l_TurnoActual);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoActual']));
    end;
    Self.TurnoActual := l_TurnoActual;
    l_TurnosActuales := Self.TurnosActuales;
    try
      TROSerializer(ASerializer).ReadInteger('TurnosActuales', TypInfo.otSLong, l_TurnosActuales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnosActuales']));
    end;
    Self.TurnosActuales := l_TurnosActuales;
    l_VentaSeleccionada := Self.VentaSeleccionada;
    try
      TROSerializer(ASerializer).ReadInteger('VentaSeleccionada', TypInfo.otSLong, l_VentaSeleccionada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaSeleccionada']));
    end;
    Self.VentaSeleccionada := l_VentaSeleccionada;
  end;
end;

procedure TGlobal.WriteComplex(ASerializer: TObject);
var
  l_AfectarDecimal: Boolean;
  l_AfectarLitros: Boolean;
  l_AutorizacionPorManguera: Boolean;
  l_blnProdArriba: Boolean;
  l_bolStrop: Boolean;
  l_Bombas: aBombas;
  l_cAck: AnsiString;
  l_CambioPrecio: TCambio;
  l_cCR: AnsiString;
  l_cEsc: AnsiString;
  l_cExt: AnsiString;
  l_cLF: AnsiString;
  l_cNak: AnsiString;
  l_Conexiones: aDispositivos;
  l_CortarSuministrosSinAut: Boolean;
  l_cUsuario: AnsiString;
  l_Dispositivos: aDispositivos;
  l_Estacion: Integer;
  l_eStx: AnsiString;
  l_Fabricante: eFabricante;
  l_FlujoActual: Integer;
  l_Gasolinas: aGas;
  l_idNivelUsuario: Integer;
  l_idUsuario: Integer;
  l_Licencia: TLicencia;
  l_lPoleoHora: Boolean;
  l_Maquina: AnsiString;
  l_MetodoOperacion: Integer;
  l_MetodoTurno: Integer;
  l_Monitoreo: eMonitoreo;
  l_MSG_CISTEMGAS: Integer;
  l_NombreEstacion: AnsiString;
  l_NumPuerto: Integer;
  l_NumSite: AnsiString;
  l_nVueltasGenerales: Integer;
  l_PermitirCorteSinVenta: Boolean;
  l_PrecioInferior: Double;
  l_PrecioSuperior: Double;
  l_Protecciones: TProtecciones;
  l_RegistroVenta: Integer;
  l_Respuesta: AnsiString;
  l_RFC: AnsiString;
  l_SIIC: AnsiString;
  l_SinAfectar: Boolean;
  l_StatusCtrolVol: Integer;
  l_StrCmdPendiente: AnsiString;
  l_Tanques: aTanques;
  l_Telemedicion: Integer;
  l_TipoEstacion: Integer;
  l_TotalBombas: Integer;
  l_TotalCiclos: Integer;
  l_TurnoActual: Integer;
  l_TurnosActuales: Integer;
  l_VentaSeleccionada: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TGlobal);
    l_AfectarDecimal := Self.AfectarDecimal;
    TROSerializer(ASerializer).WriteEnumerated('AfectarDecimal',System.TypeInfo(boolean), l_AfectarDecimal);
    l_AfectarLitros := Self.AfectarLitros;
    TROSerializer(ASerializer).WriteEnumerated('AfectarLitros',System.TypeInfo(boolean), l_AfectarLitros);
    l_AutorizacionPorManguera := Self.AutorizacionPorManguera;
    TROSerializer(ASerializer).WriteEnumerated('AutorizacionPorManguera',System.TypeInfo(boolean), l_AutorizacionPorManguera);
    l_blnProdArriba := Self.blnProdArriba;
    TROSerializer(ASerializer).WriteEnumerated('blnProdArriba',System.TypeInfo(boolean), l_blnProdArriba);
    l_bolStrop := Self.bolStrop;
    TROSerializer(ASerializer).WriteEnumerated('bolStrop',System.TypeInfo(boolean), l_bolStrop);
    l_Bombas := int_Bombas;
    TROSerializer(ASerializer).WriteArray('Bombas', l_Bombas, aBombas);
    l_cAck := Self.cAck;
    TROSerializer(ASerializer).WriteAnsiString('cAck', l_cAck);
    l_CambioPrecio := int_CambioPrecio;
    TROSerializer(ASerializer).WriteStruct('CambioPrecio', l_CambioPrecio, TCambio);
    l_cCR := Self.cCR;
    TROSerializer(ASerializer).WriteAnsiString('cCR', l_cCR);
    l_cEsc := Self.cEsc;
    TROSerializer(ASerializer).WriteAnsiString('cEsc', l_cEsc);
    l_cExt := Self.cExt;
    TROSerializer(ASerializer).WriteAnsiString('cExt', l_cExt);
    l_cLF := Self.cLF;
    TROSerializer(ASerializer).WriteAnsiString('cLF', l_cLF);
    l_cNak := Self.cNak;
    TROSerializer(ASerializer).WriteAnsiString('cNak', l_cNak);
    l_Conexiones := int_Conexiones;
    TROSerializer(ASerializer).WriteArray('Conexiones', l_Conexiones, aDispositivos);
    l_CortarSuministrosSinAut := Self.CortarSuministrosSinAut;
    TROSerializer(ASerializer).WriteEnumerated('CortarSuministrosSinAut',System.TypeInfo(boolean), l_CortarSuministrosSinAut);
    l_eStx := Self.eStx;
    TROSerializer(ASerializer).WriteAnsiString('eStx', l_eStx);
    l_cUsuario := Self.cUsuario;
    TROSerializer(ASerializer).WriteAnsiString('cUsuario', l_cUsuario);
    l_Dispositivos := int_Dispositivos;
    TROSerializer(ASerializer).WriteArray('Dispositivos', l_Dispositivos, aDispositivos);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_Fabricante := Self.Fabricante;
    TROSerializer(ASerializer).WriteEnumerated('Fabricante',System.TypeInfo(eFabricante), l_Fabricante);
    l_FlujoActual := Self.FlujoActual;
    TROSerializer(ASerializer).WriteInteger('FlujoActual', TypInfo.otSLong, l_FlujoActual);
    l_Gasolinas := int_Gasolinas;
    TROSerializer(ASerializer).WriteArray('Gasolinas', l_Gasolinas, aGas);
    l_idNivelUsuario := Self.idNivelUsuario;
    TROSerializer(ASerializer).WriteInteger('idNivelUsuario', TypInfo.otSLong, l_idNivelUsuario);
    l_idUsuario := Self.idUsuario;
    TROSerializer(ASerializer).WriteInteger('idUsuario', TypInfo.otSLong, l_idUsuario);
    l_lPoleoHora := Self.lPoleoHora;
    TROSerializer(ASerializer).WriteEnumerated('lPoleoHora',System.TypeInfo(boolean), l_lPoleoHora);
    l_Maquina := Self.Maquina;
    TROSerializer(ASerializer).WriteAnsiString('Maquina', l_Maquina);
    l_MetodoOperacion := Self.MetodoOperacion;
    TROSerializer(ASerializer).WriteInteger('MetodoOperacion', TypInfo.otSLong, l_MetodoOperacion);
    l_MetodoTurno := Self.MetodoTurno;
    TROSerializer(ASerializer).WriteInteger('MetodoTurno', TypInfo.otSLong, l_MetodoTurno);
    l_Monitoreo := Self.Monitoreo;
    TROSerializer(ASerializer).WriteEnumerated('Monitoreo',System.TypeInfo(eMonitoreo), l_Monitoreo);
    l_MSG_CISTEMGAS := Self.MSG_CISTEMGAS;
    TROSerializer(ASerializer).WriteInteger('MSG_CISTEMGAS', TypInfo.otSLong, l_MSG_CISTEMGAS);
    l_NombreEstacion := Self.NombreEstacion;
    TROSerializer(ASerializer).WriteAnsiString('NombreEstacion', l_NombreEstacion);
    l_NumPuerto := Self.NumPuerto;
    TROSerializer(ASerializer).WriteInteger('NumPuerto', TypInfo.otSLong, l_NumPuerto);
    l_NumSite := Self.NumSite;
    TROSerializer(ASerializer).WriteAnsiString('NumSite', l_NumSite);
    l_nVueltasGenerales := Self.nVueltasGenerales;
    TROSerializer(ASerializer).WriteInteger('nVueltasGenerales', TypInfo.otSLong, l_nVueltasGenerales);
    l_PermitirCorteSinVenta := Self.PermitirCorteSinVenta;
    TROSerializer(ASerializer).WriteEnumerated('PermitirCorteSinVenta',System.TypeInfo(boolean), l_PermitirCorteSinVenta);
    l_PrecioInferior := Self.PrecioInferior;
    TROSerializer(ASerializer).WriteDouble('PrecioInferior', TypInfo.ftDouble, l_PrecioInferior);
    l_PrecioSuperior := Self.PrecioSuperior;
    TROSerializer(ASerializer).WriteDouble('PrecioSuperior', TypInfo.ftDouble, l_PrecioSuperior);
    l_Protecciones := int_Protecciones;
    TROSerializer(ASerializer).WriteStruct('Protecciones', l_Protecciones, TProtecciones);
    l_RegistroVenta := Self.RegistroVenta;
    TROSerializer(ASerializer).WriteInteger('RegistroVenta', TypInfo.otSLong, l_RegistroVenta);
    l_Respuesta := Self.Respuesta;
    TROSerializer(ASerializer).WriteAnsiString('Respuesta', l_Respuesta);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_SIIC := Self.SIIC;
    TROSerializer(ASerializer).WriteAnsiString('SIIC', l_SIIC);
    l_SinAfectar := Self.SinAfectar;
    TROSerializer(ASerializer).WriteEnumerated('SinAfectar',System.TypeInfo(boolean), l_SinAfectar);
    l_StatusCtrolVol := Self.StatusCtrolVol;
    TROSerializer(ASerializer).WriteInteger('StatusCtrolVol', TypInfo.otSLong, l_StatusCtrolVol);
    l_StrCmdPendiente := Self.StrCmdPendiente;
    TROSerializer(ASerializer).WriteAnsiString('StrCmdPendiente', l_StrCmdPendiente);
    l_Tanques := int_Tanques;
    TROSerializer(ASerializer).WriteArray('Tanques', l_Tanques, aTanques);
    l_Telemedicion := Self.Telemedicion;
    TROSerializer(ASerializer).WriteInteger('Telemedicion', TypInfo.otSLong, l_Telemedicion);
    l_TipoEstacion := Self.TipoEstacion;
    TROSerializer(ASerializer).WriteInteger('TipoEstacion', TypInfo.otSLong, l_TipoEstacion);
    l_TotalBombas := Self.TotalBombas;
    TROSerializer(ASerializer).WriteInteger('TotalBombas', TypInfo.otSLong, l_TotalBombas);
    l_TotalCiclos := Self.TotalCiclos;
    TROSerializer(ASerializer).WriteInteger('TotalCiclos', TypInfo.otSLong, l_TotalCiclos);
    l_TurnoActual := Self.TurnoActual;
    TROSerializer(ASerializer).WriteInteger('TurnoActual', TypInfo.otSLong, l_TurnoActual);
    l_TurnosActuales := Self.TurnosActuales;
    TROSerializer(ASerializer).WriteInteger('TurnosActuales', TypInfo.otSLong, l_TurnosActuales);
    l_VentaSeleccionada := Self.VentaSeleccionada;
    TROSerializer(ASerializer).WriteInteger('VentaSeleccionada', TypInfo.otSLong, l_VentaSeleccionada);
    l_Licencia := int_Licencia;
    TROSerializer(ASerializer).WriteStruct('Licencia', l_Licencia, TLicencia);
  end
  else begin
    l_AfectarDecimal := Self.AfectarDecimal;
    TROSerializer(ASerializer).WriteEnumerated('AfectarDecimal',System.TypeInfo(boolean), l_AfectarDecimal);
    l_AfectarLitros := Self.AfectarLitros;
    TROSerializer(ASerializer).WriteEnumerated('AfectarLitros',System.TypeInfo(boolean), l_AfectarLitros);
    l_AutorizacionPorManguera := Self.AutorizacionPorManguera;
    TROSerializer(ASerializer).WriteEnumerated('AutorizacionPorManguera',System.TypeInfo(boolean), l_AutorizacionPorManguera);
    l_blnProdArriba := Self.blnProdArriba;
    TROSerializer(ASerializer).WriteEnumerated('blnProdArriba',System.TypeInfo(boolean), l_blnProdArriba);
    l_bolStrop := Self.bolStrop;
    TROSerializer(ASerializer).WriteEnumerated('bolStrop',System.TypeInfo(boolean), l_bolStrop);
    l_Bombas := int_Bombas;
    TROSerializer(ASerializer).WriteArray('Bombas', l_Bombas, aBombas);
    l_cAck := Self.cAck;
    TROSerializer(ASerializer).WriteAnsiString('cAck', l_cAck);
    l_CambioPrecio := int_CambioPrecio;
    TROSerializer(ASerializer).WriteStruct('CambioPrecio', l_CambioPrecio, TCambio);
    l_cCR := Self.cCR;
    TROSerializer(ASerializer).WriteAnsiString('cCR', l_cCR);
    l_cEsc := Self.cEsc;
    TROSerializer(ASerializer).WriteAnsiString('cEsc', l_cEsc);
    l_cExt := Self.cExt;
    TROSerializer(ASerializer).WriteAnsiString('cExt', l_cExt);
    l_cLF := Self.cLF;
    TROSerializer(ASerializer).WriteAnsiString('cLF', l_cLF);
    l_cNak := Self.cNak;
    TROSerializer(ASerializer).WriteAnsiString('cNak', l_cNak);
    l_Conexiones := int_Conexiones;
    TROSerializer(ASerializer).WriteArray('Conexiones', l_Conexiones, aDispositivos);
    l_CortarSuministrosSinAut := Self.CortarSuministrosSinAut;
    TROSerializer(ASerializer).WriteEnumerated('CortarSuministrosSinAut',System.TypeInfo(boolean), l_CortarSuministrosSinAut);
    l_cUsuario := Self.cUsuario;
    TROSerializer(ASerializer).WriteAnsiString('cUsuario', l_cUsuario);
    l_Dispositivos := int_Dispositivos;
    TROSerializer(ASerializer).WriteArray('Dispositivos', l_Dispositivos, aDispositivos);
    l_Estacion := Self.Estacion;
    TROSerializer(ASerializer).WriteInteger('Estacion', TypInfo.otSLong, l_Estacion);
    l_eStx := Self.eStx;
    TROSerializer(ASerializer).WriteAnsiString('eStx', l_eStx);
    l_Fabricante := Self.Fabricante;
    TROSerializer(ASerializer).WriteEnumerated('Fabricante',System.TypeInfo(eFabricante), l_Fabricante);
    l_FlujoActual := Self.FlujoActual;
    TROSerializer(ASerializer).WriteInteger('FlujoActual', TypInfo.otSLong, l_FlujoActual);
    l_Gasolinas := int_Gasolinas;
    TROSerializer(ASerializer).WriteArray('Gasolinas', l_Gasolinas, aGas);
    l_idNivelUsuario := Self.idNivelUsuario;
    TROSerializer(ASerializer).WriteInteger('idNivelUsuario', TypInfo.otSLong, l_idNivelUsuario);
    l_idUsuario := Self.idUsuario;
    TROSerializer(ASerializer).WriteInteger('idUsuario', TypInfo.otSLong, l_idUsuario);
    l_Licencia := int_Licencia;
    TROSerializer(ASerializer).WriteStruct('Licencia', l_Licencia, TLicencia);
    l_lPoleoHora := Self.lPoleoHora;
    TROSerializer(ASerializer).WriteEnumerated('lPoleoHora',System.TypeInfo(boolean), l_lPoleoHora);
    l_Maquina := Self.Maquina;
    TROSerializer(ASerializer).WriteAnsiString('Maquina', l_Maquina);
    l_MetodoOperacion := Self.MetodoOperacion;
    TROSerializer(ASerializer).WriteInteger('MetodoOperacion', TypInfo.otSLong, l_MetodoOperacion);
    l_MetodoTurno := Self.MetodoTurno;
    TROSerializer(ASerializer).WriteInteger('MetodoTurno', TypInfo.otSLong, l_MetodoTurno);
    l_Monitoreo := Self.Monitoreo;
    TROSerializer(ASerializer).WriteEnumerated('Monitoreo',System.TypeInfo(eMonitoreo), l_Monitoreo);
    l_MSG_CISTEMGAS := Self.MSG_CISTEMGAS;
    TROSerializer(ASerializer).WriteInteger('MSG_CISTEMGAS', TypInfo.otSLong, l_MSG_CISTEMGAS);
    l_NombreEstacion := Self.NombreEstacion;
    TROSerializer(ASerializer).WriteAnsiString('NombreEstacion', l_NombreEstacion);
    l_NumPuerto := Self.NumPuerto;
    TROSerializer(ASerializer).WriteInteger('NumPuerto', TypInfo.otSLong, l_NumPuerto);
    l_NumSite := Self.NumSite;
    TROSerializer(ASerializer).WriteAnsiString('NumSite', l_NumSite);
    l_nVueltasGenerales := Self.nVueltasGenerales;
    TROSerializer(ASerializer).WriteInteger('nVueltasGenerales', TypInfo.otSLong, l_nVueltasGenerales);
    l_PermitirCorteSinVenta := Self.PermitirCorteSinVenta;
    TROSerializer(ASerializer).WriteEnumerated('PermitirCorteSinVenta',System.TypeInfo(boolean), l_PermitirCorteSinVenta);
    l_PrecioInferior := Self.PrecioInferior;
    TROSerializer(ASerializer).WriteDouble('PrecioInferior', TypInfo.ftDouble, l_PrecioInferior);
    l_PrecioSuperior := Self.PrecioSuperior;
    TROSerializer(ASerializer).WriteDouble('PrecioSuperior', TypInfo.ftDouble, l_PrecioSuperior);
    l_Protecciones := int_Protecciones;
    TROSerializer(ASerializer).WriteStruct('Protecciones', l_Protecciones, TProtecciones);
    l_RegistroVenta := Self.RegistroVenta;
    TROSerializer(ASerializer).WriteInteger('RegistroVenta', TypInfo.otSLong, l_RegistroVenta);
    l_Respuesta := Self.Respuesta;
    TROSerializer(ASerializer).WriteAnsiString('Respuesta', l_Respuesta);
    l_RFC := Self.RFC;
    TROSerializer(ASerializer).WriteAnsiString('RFC', l_RFC);
    l_SIIC := Self.SIIC;
    TROSerializer(ASerializer).WriteAnsiString('SIIC', l_SIIC);
    l_SinAfectar := Self.SinAfectar;
    TROSerializer(ASerializer).WriteEnumerated('SinAfectar',System.TypeInfo(boolean), l_SinAfectar);
    l_StatusCtrolVol := Self.StatusCtrolVol;
    TROSerializer(ASerializer).WriteInteger('StatusCtrolVol', TypInfo.otSLong, l_StatusCtrolVol);
    l_StrCmdPendiente := Self.StrCmdPendiente;
    TROSerializer(ASerializer).WriteAnsiString('StrCmdPendiente', l_StrCmdPendiente);
    l_Tanques := int_Tanques;
    TROSerializer(ASerializer).WriteArray('Tanques', l_Tanques, aTanques);
    l_Telemedicion := Self.Telemedicion;
    TROSerializer(ASerializer).WriteInteger('Telemedicion', TypInfo.otSLong, l_Telemedicion);
    l_TipoEstacion := Self.TipoEstacion;
    TROSerializer(ASerializer).WriteInteger('TipoEstacion', TypInfo.otSLong, l_TipoEstacion);
    l_TotalBombas := Self.TotalBombas;
    TROSerializer(ASerializer).WriteInteger('TotalBombas', TypInfo.otSLong, l_TotalBombas);
    l_TotalCiclos := Self.TotalCiclos;
    TROSerializer(ASerializer).WriteInteger('TotalCiclos', TypInfo.otSLong, l_TotalCiclos);
    l_TurnoActual := Self.TurnoActual;
    TROSerializer(ASerializer).WriteInteger('TurnoActual', TypInfo.otSLong, l_TurnoActual);
    l_TurnosActuales := Self.TurnosActuales;
    TROSerializer(ASerializer).WriteInteger('TurnosActuales', TypInfo.otSLong, l_TurnosActuales);
    l_VentaSeleccionada := Self.VentaSeleccionada;
    TROSerializer(ASerializer).WriteInteger('VentaSeleccionada', TypInfo.otSLong, l_VentaSeleccionada);
  end;
end;

{ TGlobalCollection }
constructor TGlobalCollection.Create;
begin
  inherited Create(TGlobal);
end;

constructor TGlobalCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TGlobalCollection.Add: TGlobal;
begin
  Result := TGlobal(inherited Add);
end;

function TGlobalCollection.GetItems(aIndex: Integer): TGlobal;
begin
  Result := TGlobal(inherited Items[aIndex]);
end;

procedure TGlobalCollection.SetItems(aIndex: Integer; const Value: TGlobal);
begin
  TGlobal(inherited Items[aIndex]).Assign(Value);
end;

{ TBomba }

procedure TBomba.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TBomba;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TBomba) then begin
    lSource := LibraryValesCupones_Intf.TBomba(iSource);

    Self.AutoImprimir := lSource.AutoImprimir;
    Self.Autorizacion := lSource.Autorizacion;
    Self.AutorizacionAutomatica := lSource.AutorizacionAutomatica;
    Self.Clasificacion := lSource.Clasificacion;
    Self.CodigoProducto := lSource.CodigoProducto;
    Self.ConectLCD := lSource.ConectLCD;
    Self.Despachador := lSource.Despachador;
    Self.DobleTarjeta := lSource.DobleTarjeta;
    Self.Estado := lSource.Estado;
    Self.EstadoNuevo := lSource.EstadoNuevo;
    Self.EstadoVenta := lSource.EstadoVenta;
    if System.Assigned(lSource.fFactorDecimal) then
      Self.FactorDecimal.Assign(lSource.fFactorDecimal)
    else
      SysUtils.FreeAndNil(fFactorDecimal);
    if System.Assigned(lSource.fFaltaPago) then
      Self.FaltaPago.Assign(lSource.fFaltaPago)
    else
      SysUtils.FreeAndNil(fFaltaPago);
    if System.Assigned(lSource.fFlujo) then
      Self.Flujo.Assign(lSource.fFlujo)
    else
      SysUtils.FreeAndNil(fFlujo);
    Self.Gasolina := lSource.Gasolina;
    Self.Guardando := lSource.Guardando;
    Self.ImporteMinimo := lSource.ImporteMinimo;
    Self.IntentoPrepago := lSource.IntentoPrepago;
    if System.Assigned(lSource.fLecturasFinales) then
      Self.LecturasFinales.Assign(lSource.fLecturasFinales)
    else
      SysUtils.FreeAndNil(fLecturasFinales);
    if System.Assigned(lSource.fLecturasIniciales) then
      Self.LecturasIniciales.Assign(lSource.fLecturasIniciales)
    else
      SysUtils.FreeAndNil(fLecturasIniciales);
    if System.Assigned(lSource.fLecturasTurno) then
      Self.LecturasTurno.Assign(lSource.fLecturasTurno)
    else
      SysUtils.FreeAndNil(fLecturasTurno);
    if System.Assigned(lSource.fMangueras) then
      Self.Mangueras.Assign(lSource.fMangueras)
    else
      SysUtils.FreeAndNil(fMangueras);
    Self.MaximoLecturasImporte := lSource.MaximoLecturasImporte;
    Self.MaximoLecturasVolumen := lSource.MaximoLecturasVolumen;
    Self.Modo := lSource.Modo;
    Self.NoMangueras := lSource.NoMangueras;
    Self.NombreImpresora := lSource.NombreImpresora;
    Self.NuevoFlujo := lSource.NuevoFlujo;
    Self.Pendiente := lSource.Pendiente;
    Self.PendienteGrabar := lSource.PendienteGrabar;
    if System.Assigned(lSource.fPendientesAplicar) then
      Self.PendientesAplicar.Assign(lSource.fPendientesAplicar)
    else
      SysUtils.FreeAndNil(fPendientesAplicar);
    if System.Assigned(lSource.fPosicionamiento) then
      Self.Posicionamiento.Assign(lSource.fPosicionamiento)
    else
      SysUtils.FreeAndNil(fPosicionamiento);
    Self.Preset := lSource.Preset;
    Self.Proceso := lSource.Proceso;
    Self.PuntosRegistrados := lSource.PuntosRegistrados;
    if System.Assigned(lSource.fPuntoVenta) then
      Self.PuntoVenta.Assign(lSource.fPuntoVenta)
    else
      SysUtils.FreeAndNil(fPuntoVenta);
    Self.RegresaModo := lSource.RegresaModo;
    Self.Servicio := lSource.Servicio;
    Self.Ticket := lSource.Ticket;
    Self.TiempoAutorizado := lSource.TiempoAutorizado;
    Self.TipoDispensario := lSource.TipoDispensario;
    Self.TipoTarjeta := lSource.TipoTarjeta;
    Self.TotalizacionAutomatica := lSource.TotalizacionAutomatica;
    Self.TurnoID := lSource.TurnoID;
    Self.UltimoFlujo := lSource.UltimoFlujo;
    Self.UltimoLimiteImporte := lSource.UltimoLimiteImporte;
    Self.UltimoLimiteVolumen := lSource.UltimoLimiteVolumen;
    if System.Assigned(lSource.fVentaProceso) then
      Self.VentaProceso.Assign(lSource.fVentaProceso)
    else
      SysUtils.FreeAndNil(fVentaProceso);
    if System.Assigned(lSource.fVentaUltima) then
      Self.VentaUltima.Assign(lSource.fVentaUltima)
    else
      SysUtils.FreeAndNil(fVentaUltima);
    if System.Assigned(lSource.fVentaPenultima) then
      Self.VentaPenultima.Assign(lSource.fVentaPenultima)
    else
      SysUtils.FreeAndNil(fVentaPenultima);
    if System.Assigned(lSource.fVentaAntePenultima) then
      Self.VentaAntePenultima.Assign(lSource.fVentaAntePenultima)
    else
      SysUtils.FreeAndNil(fVentaAntePenultima);
    if System.Assigned(lSource.fVentasTurno) then
      Self.VentasTurno.Assign(lSource.fVentasTurno)
    else
      SysUtils.FreeAndNil(fVentasTurno);
    Self.Host := lSource.Host;
  end;
end;

function TBomba.GetFactorDecimal: TFactorDecimal;
begin
  if (fFactorDecimal = nil) then fFactorDecimal := TFactorDecimal.Create();
  Result := fFactorDecimal;
end;

function TBomba.GetFaltaPago: aBoolean;
begin
  if (fFaltaPago = nil) then fFaltaPago := aBoolean.Create();
  Result := fFaltaPago;
end;

function TBomba.GetFlujo: TFlujos;
begin
  if (fFlujo = nil) then fFlujo := TFlujos.Create();
  Result := fFlujo;
end;

function TBomba.GetLecturasFinales: aLecturas;
begin
  if (fLecturasFinales = nil) then fLecturasFinales := aLecturas.Create();
  Result := fLecturasFinales;
end;

function TBomba.GetLecturasIniciales: aLecturas;
begin
  if (fLecturasIniciales = nil) then fLecturasIniciales := aLecturas.Create();
  Result := fLecturasIniciales;
end;

function TBomba.GetLecturasTurno: aLecturas;
begin
  if (fLecturasTurno = nil) then fLecturasTurno := aLecturas.Create();
  Result := fLecturasTurno;
end;

function TBomba.GetMangueras: aMaguera;
begin
  if (fMangueras = nil) then fMangueras := aMaguera.Create();
  Result := fMangueras;
end;

function TBomba.GetPendientesAplicar: aContador;
begin
  if (fPendientesAplicar = nil) then fPendientesAplicar := aContador.Create();
  Result := fPendientesAplicar;
end;

function TBomba.GetPosicionamiento: TCanal;
begin
  if (fPosicionamiento = nil) then fPosicionamiento := TCanal.Create();
  Result := fPosicionamiento;
end;

function TBomba.GetPuntoVenta: TRegistroPuntoVenta;
begin
  if (fPuntoVenta = nil) then fPuntoVenta := TRegistroPuntoVenta.Create();
  Result := fPuntoVenta;
end;

function TBomba.GetVentaProceso: TRegistroVenta;
begin
  if (fVentaProceso = nil) then fVentaProceso := TRegistroVenta.Create();
  Result := fVentaProceso;
end;

function TBomba.GetVentaUltima: TRegistroVenta;
begin
  if (fVentaUltima = nil) then fVentaUltima := TRegistroVenta.Create();
  Result := fVentaUltima;
end;

function TBomba.GetVentaPenultima: TRegistroVenta;
begin
  if (fVentaPenultima = nil) then fVentaPenultima := TRegistroVenta.Create();
  Result := fVentaPenultima;
end;

function TBomba.GetVentaAntePenultima: TRegistroVenta;
begin
  if (fVentaAntePenultima = nil) then fVentaAntePenultima := TRegistroVenta.Create();
  Result := fVentaAntePenultima;
end;

function TBomba.GetVentasTurno: aContador;
begin
  if (fVentasTurno = nil) then fVentasTurno := aContador.Create();
  Result := fVentasTurno;
end;

procedure TBomba.ReadComplex(ASerializer: TObject);
var
  l_AutoImprimir: Boolean;
  l_Autorizacion: eTipoAutorizacion;
  l_AutorizacionAutomatica: Integer;
  l_Clasificacion: AnsiString;
  l_CodigoProducto: eCodigoProducto;
  l_ConectLCD: Integer;
  l_Despachador: Integer;
  l_DobleTarjeta: Boolean;
  l_Estado: eEstadoBomba;
  l_EstadoNuevo: eEstadoBomba;
  l_EstadoVenta: eEstadoVenta;
  l_FactorDecimal: TFactorDecimal;
  l_FaltaPago: aBoolean;
  l_Flujo: TFlujos;
  l_Gasolina: Integer;
  l_Guardando: Boolean;
  l_Host: AnsiString;
  l_ImporteMinimo: Double;
  l_IntentoPrepago: Boolean;
  l_LecturasFinales: aLecturas;
  l_LecturasIniciales: aLecturas;
  l_LecturasTurno: aLecturas;
  l_Mangueras: aMaguera;
  l_MaximoLecturasImporte: Double;
  l_MaximoLecturasVolumen: Double;
  l_Modo: eModoOperacion;
  l_NoMangueras: Integer;
  l_NombreImpresora: AnsiString;
  l_NuevoFlujo: AnsiString;
  l_Pendiente: Boolean;
  l_PendienteGrabar: Boolean;
  l_PendientesAplicar: aContador;
  l_Posicionamiento: TCanal;
  l_Preset: eTipoPreset;
  l_Proceso: eProceso;
  l_PuntosRegistrados: Boolean;
  l_PuntoVenta: TRegistroPuntoVenta;
  l_RegresaModo: Boolean;
  l_Servicio: Double;
  l_Ticket: eImpresionTicket;
  l_TiempoAutorizado: Integer;
  l_TipoDispensario: Integer;
  l_TipoTarjeta: Integer;
  l_TotalizacionAutomatica: Integer;
  l_TurnoID: Integer;
  l_UltimoFlujo: AnsiString;
  l_UltimoLimiteImporte: Double;
  l_UltimoLimiteVolumen: Double;
  l_VentaAntePenultima: TRegistroVenta;
  l_VentaPenultima: TRegistroVenta;
  l_VentaProceso: TRegistroVenta;
  l_VentasTurno: aContador;
  l_VentaUltima: TRegistroVenta;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_AutoImprimir := Self.AutoImprimir;
    try
      TROSerializer(ASerializer).ReadEnumerated('AutoImprimir',System.TypeInfo(boolean), l_AutoImprimir);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutoImprimir']));
    end;
    Self.AutoImprimir := l_AutoImprimir;
    l_Autorizacion := Self.Autorizacion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Autorizacion',System.TypeInfo(eTipoAutorizacion), l_Autorizacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Autorizacion']));
    end;
    Self.Autorizacion := l_Autorizacion;
    l_AutorizacionAutomatica := Self.AutorizacionAutomatica;
    try
      TROSerializer(ASerializer).ReadInteger('AutorizacionAutomatica', TypInfo.otSLong, l_AutorizacionAutomatica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionAutomatica']));
    end;
    Self.AutorizacionAutomatica := l_AutorizacionAutomatica;
    l_Clasificacion := Self.Clasificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Clasificacion', l_Clasificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Clasificacion']));
    end;
    Self.Clasificacion := l_Clasificacion;
    l_CodigoProducto := Self.CodigoProducto;
    try
      TROSerializer(ASerializer).ReadEnumerated('CodigoProducto',System.TypeInfo(eCodigoProducto), l_CodigoProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoProducto']));
    end;
    Self.CodigoProducto := l_CodigoProducto;
    l_ConectLCD := Self.ConectLCD;
    try
      TROSerializer(ASerializer).ReadInteger('ConectLCD', TypInfo.otSLong, l_ConectLCD);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConectLCD']));
    end;
    Self.ConectLCD := l_ConectLCD;
    l_Despachador := Self.Despachador;
    try
      TROSerializer(ASerializer).ReadInteger('Despachador', TypInfo.otSLong, l_Despachador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Despachador']));
    end;
    Self.Despachador := l_Despachador;
    l_DobleTarjeta := Self.DobleTarjeta;
    try
      TROSerializer(ASerializer).ReadEnumerated('DobleTarjeta',System.TypeInfo(boolean), l_DobleTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DobleTarjeta']));
    end;
    Self.DobleTarjeta := l_DobleTarjeta;
    l_Estado := Self.Estado;
    try
      TROSerializer(ASerializer).ReadEnumerated('Estado',System.TypeInfo(eEstadoBomba), l_Estado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estado']));
    end;
    Self.Estado := l_Estado;
    l_EstadoNuevo := Self.EstadoNuevo;
    try
      TROSerializer(ASerializer).ReadEnumerated('EstadoNuevo',System.TypeInfo(eEstadoBomba), l_EstadoNuevo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoNuevo']));
    end;
    Self.EstadoNuevo := l_EstadoNuevo;
    l_EstadoVenta := Self.EstadoVenta;
    try
      TROSerializer(ASerializer).ReadEnumerated('EstadoVenta',System.TypeInfo(eEstadoVenta), l_EstadoVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoVenta']));
    end;
    Self.EstadoVenta := l_EstadoVenta;
    l_FactorDecimal := int_FactorDecimal;
    try
      TROSerializer(ASerializer).ReadStruct('FactorDecimal', TFactorDecimal, l_FactorDecimal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FactorDecimal']));
    end;
    if int_FactorDecimal <> l_FactorDecimal then Self.FactorDecimal.Free;
    Self.FactorDecimal := l_FactorDecimal;
    l_FaltaPago := int_FaltaPago;
    try
      TROSerializer(ASerializer).ReadArray('FaltaPago', aBoolean, l_FaltaPago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FaltaPago']));
    end;
    if int_FaltaPago <> l_FaltaPago then Self.FaltaPago.Free;
    Self.FaltaPago := l_FaltaPago;
    l_Flujo := int_Flujo;
    try
      TROSerializer(ASerializer).ReadStruct('Flujo', TFlujos, l_Flujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flujo']));
    end;
    if int_Flujo <> l_Flujo then Self.Flujo.Free;
    Self.Flujo := l_Flujo;
    l_Gasolina := Self.Gasolina;
    try
      TROSerializer(ASerializer).ReadInteger('Gasolina', TypInfo.otSLong, l_Gasolina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolina']));
    end;
    Self.Gasolina := l_Gasolina;
    l_Guardando := Self.Guardando;
    try
      TROSerializer(ASerializer).ReadEnumerated('Guardando',System.TypeInfo(boolean), l_Guardando);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Guardando']));
    end;
    Self.Guardando := l_Guardando;
    l_ImporteMinimo := Self.ImporteMinimo;
    try
      TROSerializer(ASerializer).ReadDouble('ImporteMinimo', TypInfo.ftDouble, l_ImporteMinimo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteMinimo']));
    end;
    Self.ImporteMinimo := l_ImporteMinimo;
    l_IntentoPrepago := Self.IntentoPrepago;
    try
      TROSerializer(ASerializer).ReadEnumerated('IntentoPrepago',System.TypeInfo(boolean), l_IntentoPrepago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IntentoPrepago']));
    end;
    Self.IntentoPrepago := l_IntentoPrepago;
    l_LecturasFinales := int_LecturasFinales;
    try
      TROSerializer(ASerializer).ReadArray('LecturasFinales', aLecturas, l_LecturasFinales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasFinales']));
    end;
    if int_LecturasFinales <> l_LecturasFinales then Self.LecturasFinales.Free;
    Self.LecturasFinales := l_LecturasFinales;
    l_LecturasIniciales := int_LecturasIniciales;
    try
      TROSerializer(ASerializer).ReadArray('LecturasIniciales', aLecturas, l_LecturasIniciales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasIniciales']));
    end;
    if int_LecturasIniciales <> l_LecturasIniciales then Self.LecturasIniciales.Free;
    Self.LecturasIniciales := l_LecturasIniciales;
    l_LecturasTurno := int_LecturasTurno;
    try
      TROSerializer(ASerializer).ReadArray('LecturasTurno', aLecturas, l_LecturasTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasTurno']));
    end;
    if int_LecturasTurno <> l_LecturasTurno then Self.LecturasTurno.Free;
    Self.LecturasTurno := l_LecturasTurno;
    l_Mangueras := int_Mangueras;
    try
      TROSerializer(ASerializer).ReadArray('Mangueras', aMaguera, l_Mangueras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mangueras']));
    end;
    if int_Mangueras <> l_Mangueras then Self.Mangueras.Free;
    Self.Mangueras := l_Mangueras;
    l_MaximoLecturasImporte := Self.MaximoLecturasImporte;
    try
      TROSerializer(ASerializer).ReadDouble('MaximoLecturasImporte', TypInfo.ftDouble, l_MaximoLecturasImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MaximoLecturasImporte']));
    end;
    Self.MaximoLecturasImporte := l_MaximoLecturasImporte;
    l_MaximoLecturasVolumen := Self.MaximoLecturasVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('MaximoLecturasVolumen', TypInfo.ftDouble, l_MaximoLecturasVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MaximoLecturasVolumen']));
    end;
    Self.MaximoLecturasVolumen := l_MaximoLecturasVolumen;
    l_Modo := Self.Modo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Modo',System.TypeInfo(eModoOperacion), l_Modo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Modo']));
    end;
    Self.Modo := l_Modo;
    l_NoMangueras := Self.NoMangueras;
    try
      TROSerializer(ASerializer).ReadInteger('NoMangueras', TypInfo.otSLong, l_NoMangueras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoMangueras']));
    end;
    Self.NoMangueras := l_NoMangueras;
    l_NombreImpresora := Self.NombreImpresora;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreImpresora', l_NombreImpresora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreImpresora']));
    end;
    Self.NombreImpresora := l_NombreImpresora;
    l_NuevoFlujo := Self.NuevoFlujo;
    try
      TROSerializer(ASerializer).ReadAnsiString('NuevoFlujo', l_NuevoFlujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NuevoFlujo']));
    end;
    Self.NuevoFlujo := l_NuevoFlujo;
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(boolean), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_PendienteGrabar := Self.PendienteGrabar;
    try
      TROSerializer(ASerializer).ReadEnumerated('PendienteGrabar',System.TypeInfo(boolean), l_PendienteGrabar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PendienteGrabar']));
    end;
    Self.PendienteGrabar := l_PendienteGrabar;
    l_PendientesAplicar := int_PendientesAplicar;
    try
      TROSerializer(ASerializer).ReadArray('PendientesAplicar', aContador, l_PendientesAplicar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PendientesAplicar']));
    end;
    if int_PendientesAplicar <> l_PendientesAplicar then Self.PendientesAplicar.Free;
    Self.PendientesAplicar := l_PendientesAplicar;
    l_Posicionamiento := int_Posicionamiento;
    try
      TROSerializer(ASerializer).ReadStruct('Posicionamiento', TCanal, l_Posicionamiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Posicionamiento']));
    end;
    if int_Posicionamiento <> l_Posicionamiento then Self.Posicionamiento.Free;
    Self.Posicionamiento := l_Posicionamiento;
    l_Preset := Self.Preset;
    try
      TROSerializer(ASerializer).ReadEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Preset']));
    end;
    Self.Preset := l_Preset;
    l_Proceso := Self.Proceso;
    try
      TROSerializer(ASerializer).ReadEnumerated('Proceso',System.TypeInfo(eProceso), l_Proceso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Proceso']));
    end;
    Self.Proceso := l_Proceso;
    l_PuntosRegistrados := Self.PuntosRegistrados;
    try
      TROSerializer(ASerializer).ReadEnumerated('PuntosRegistrados',System.TypeInfo(boolean), l_PuntosRegistrados);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosRegistrados']));
    end;
    Self.PuntosRegistrados := l_PuntosRegistrados;
    l_PuntoVenta := int_PuntoVenta;
    try
      TROSerializer(ASerializer).ReadStruct('PuntoVenta', TRegistroPuntoVenta, l_PuntoVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntoVenta']));
    end;
    if int_PuntoVenta <> l_PuntoVenta then Self.PuntoVenta.Free;
    Self.PuntoVenta := l_PuntoVenta;
    l_RegresaModo := Self.RegresaModo;
    try
      TROSerializer(ASerializer).ReadEnumerated('RegresaModo',System.TypeInfo(boolean), l_RegresaModo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RegresaModo']));
    end;
    Self.RegresaModo := l_RegresaModo;
    l_Servicio := Self.Servicio;
    try
      TROSerializer(ASerializer).ReadDouble('Servicio', TypInfo.ftDouble, l_Servicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Servicio']));
    end;
    Self.Servicio := l_Servicio;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadEnumerated('Ticket',System.TypeInfo(eImpresionTicket), l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_TiempoAutorizado := Self.TiempoAutorizado;
    try
      TROSerializer(ASerializer).ReadInteger('TiempoAutorizado', TypInfo.otSLong, l_TiempoAutorizado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TiempoAutorizado']));
    end;
    Self.TiempoAutorizado := l_TiempoAutorizado;
    l_TipoDispensario := Self.TipoDispensario;
    try
      TROSerializer(ASerializer).ReadInteger('TipoDispensario', TypInfo.otSLong, l_TipoDispensario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoDispensario']));
    end;
    Self.TipoDispensario := l_TipoDispensario;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadInteger('TipoTarjeta', TypInfo.otSLong, l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_TotalizacionAutomatica := Self.TotalizacionAutomatica;
    try
      TROSerializer(ASerializer).ReadInteger('TotalizacionAutomatica', TypInfo.otSLong, l_TotalizacionAutomatica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalizacionAutomatica']));
    end;
    Self.TotalizacionAutomatica := l_TotalizacionAutomatica;
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
    l_UltimoFlujo := Self.UltimoFlujo;
    try
      TROSerializer(ASerializer).ReadAnsiString('UltimoFlujo', l_UltimoFlujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoFlujo']));
    end;
    Self.UltimoFlujo := l_UltimoFlujo;
    l_UltimoLimiteImporte := Self.UltimoLimiteImporte;
    try
      TROSerializer(ASerializer).ReadDouble('UltimoLimiteImporte', TypInfo.ftDouble, l_UltimoLimiteImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoLimiteImporte']));
    end;
    Self.UltimoLimiteImporte := l_UltimoLimiteImporte;
    l_UltimoLimiteVolumen := Self.UltimoLimiteVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('UltimoLimiteVolumen', TypInfo.ftDouble, l_UltimoLimiteVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoLimiteVolumen']));
    end;
    Self.UltimoLimiteVolumen := l_UltimoLimiteVolumen;
    l_VentaProceso := int_VentaProceso;
    try
      TROSerializer(ASerializer).ReadStruct('VentaProceso', TRegistroVenta, l_VentaProceso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaProceso']));
    end;
    if int_VentaProceso <> l_VentaProceso then Self.VentaProceso.Free;
    Self.VentaProceso := l_VentaProceso;
    l_VentaUltima := int_VentaUltima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaUltima', TRegistroVenta, l_VentaUltima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaUltima']));
    end;
    if int_VentaUltima <> l_VentaUltima then Self.VentaUltima.Free;
    Self.VentaUltima := l_VentaUltima;
    l_VentaPenultima := int_VentaPenultima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaPenultima', TRegistroVenta, l_VentaPenultima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaPenultima']));
    end;
    if int_VentaPenultima <> l_VentaPenultima then Self.VentaPenultima.Free;
    Self.VentaPenultima := l_VentaPenultima;
    l_VentaAntePenultima := int_VentaAntePenultima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaAntePenultima', TRegistroVenta, l_VentaAntePenultima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAntePenultima']));
    end;
    if int_VentaAntePenultima <> l_VentaAntePenultima then Self.VentaAntePenultima.Free;
    Self.VentaAntePenultima := l_VentaAntePenultima;
    l_VentasTurno := int_VentasTurno;
    try
      TROSerializer(ASerializer).ReadArray('VentasTurno', aContador, l_VentasTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTurno']));
    end;
    if int_VentasTurno <> l_VentasTurno then Self.VentasTurno.Free;
    Self.VentasTurno := l_VentasTurno;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
  end
  else begin
    l_AutoImprimir := Self.AutoImprimir;
    try
      TROSerializer(ASerializer).ReadEnumerated('AutoImprimir',System.TypeInfo(boolean), l_AutoImprimir);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutoImprimir']));
    end;
    Self.AutoImprimir := l_AutoImprimir;
    l_Autorizacion := Self.Autorizacion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Autorizacion',System.TypeInfo(eTipoAutorizacion), l_Autorizacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Autorizacion']));
    end;
    Self.Autorizacion := l_Autorizacion;
    l_AutorizacionAutomatica := Self.AutorizacionAutomatica;
    try
      TROSerializer(ASerializer).ReadInteger('AutorizacionAutomatica', TypInfo.otSLong, l_AutorizacionAutomatica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'AutorizacionAutomatica']));
    end;
    Self.AutorizacionAutomatica := l_AutorizacionAutomatica;
    l_Clasificacion := Self.Clasificacion;
    try
      TROSerializer(ASerializer).ReadAnsiString('Clasificacion', l_Clasificacion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Clasificacion']));
    end;
    Self.Clasificacion := l_Clasificacion;
    l_CodigoProducto := Self.CodigoProducto;
    try
      TROSerializer(ASerializer).ReadEnumerated('CodigoProducto',System.TypeInfo(eCodigoProducto), l_CodigoProducto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CodigoProducto']));
    end;
    Self.CodigoProducto := l_CodigoProducto;
    l_ConectLCD := Self.ConectLCD;
    try
      TROSerializer(ASerializer).ReadInteger('ConectLCD', TypInfo.otSLong, l_ConectLCD);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConectLCD']));
    end;
    Self.ConectLCD := l_ConectLCD;
    l_Despachador := Self.Despachador;
    try
      TROSerializer(ASerializer).ReadInteger('Despachador', TypInfo.otSLong, l_Despachador);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Despachador']));
    end;
    Self.Despachador := l_Despachador;
    l_DobleTarjeta := Self.DobleTarjeta;
    try
      TROSerializer(ASerializer).ReadEnumerated('DobleTarjeta',System.TypeInfo(boolean), l_DobleTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DobleTarjeta']));
    end;
    Self.DobleTarjeta := l_DobleTarjeta;
    l_Estado := Self.Estado;
    try
      TROSerializer(ASerializer).ReadEnumerated('Estado',System.TypeInfo(eEstadoBomba), l_Estado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Estado']));
    end;
    Self.Estado := l_Estado;
    l_EstadoNuevo := Self.EstadoNuevo;
    try
      TROSerializer(ASerializer).ReadEnumerated('EstadoNuevo',System.TypeInfo(eEstadoBomba), l_EstadoNuevo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoNuevo']));
    end;
    Self.EstadoNuevo := l_EstadoNuevo;
    l_EstadoVenta := Self.EstadoVenta;
    try
      TROSerializer(ASerializer).ReadEnumerated('EstadoVenta',System.TypeInfo(eEstadoVenta), l_EstadoVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'EstadoVenta']));
    end;
    Self.EstadoVenta := l_EstadoVenta;
    l_FactorDecimal := int_FactorDecimal;
    try
      TROSerializer(ASerializer).ReadStruct('FactorDecimal', TFactorDecimal, l_FactorDecimal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FactorDecimal']));
    end;
    if int_FactorDecimal <> l_FactorDecimal then FactorDecimal.Free;
    Self.FactorDecimal := l_FactorDecimal;
    l_FaltaPago := int_FaltaPago;
    try
      TROSerializer(ASerializer).ReadArray('FaltaPago', aBoolean, l_FaltaPago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FaltaPago']));
    end;
    if int_FaltaPago <> l_FaltaPago then FaltaPago.Free;
    Self.FaltaPago := l_FaltaPago;
    l_Flujo := int_Flujo;
    try
      TROSerializer(ASerializer).ReadStruct('Flujo', TFlujos, l_Flujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flujo']));
    end;
    if int_Flujo <> l_Flujo then Flujo.Free;
    Self.Flujo := l_Flujo;
    l_Gasolina := Self.Gasolina;
    try
      TROSerializer(ASerializer).ReadInteger('Gasolina', TypInfo.otSLong, l_Gasolina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Gasolina']));
    end;
    Self.Gasolina := l_Gasolina;
    l_Guardando := Self.Guardando;
    try
      TROSerializer(ASerializer).ReadEnumerated('Guardando',System.TypeInfo(boolean), l_Guardando);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Guardando']));
    end;
    Self.Guardando := l_Guardando;
    l_Host := Self.Host;
    try
      TROSerializer(ASerializer).ReadAnsiString('Host', l_Host);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Host']));
    end;
    Self.Host := l_Host;
    l_ImporteMinimo := Self.ImporteMinimo;
    try
      TROSerializer(ASerializer).ReadDouble('ImporteMinimo', TypInfo.ftDouble, l_ImporteMinimo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ImporteMinimo']));
    end;
    Self.ImporteMinimo := l_ImporteMinimo;
    l_IntentoPrepago := Self.IntentoPrepago;
    try
      TROSerializer(ASerializer).ReadEnumerated('IntentoPrepago',System.TypeInfo(boolean), l_IntentoPrepago);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IntentoPrepago']));
    end;
    Self.IntentoPrepago := l_IntentoPrepago;
    l_LecturasFinales := int_LecturasFinales;
    try
      TROSerializer(ASerializer).ReadArray('LecturasFinales', aLecturas, l_LecturasFinales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasFinales']));
    end;
    if int_LecturasFinales <> l_LecturasFinales then LecturasFinales.Free;
    Self.LecturasFinales := l_LecturasFinales;
    l_LecturasIniciales := int_LecturasIniciales;
    try
      TROSerializer(ASerializer).ReadArray('LecturasIniciales', aLecturas, l_LecturasIniciales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasIniciales']));
    end;
    if int_LecturasIniciales <> l_LecturasIniciales then LecturasIniciales.Free;
    Self.LecturasIniciales := l_LecturasIniciales;
    l_LecturasTurno := int_LecturasTurno;
    try
      TROSerializer(ASerializer).ReadArray('LecturasTurno', aLecturas, l_LecturasTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasTurno']));
    end;
    if int_LecturasTurno <> l_LecturasTurno then LecturasTurno.Free;
    Self.LecturasTurno := l_LecturasTurno;
    l_Mangueras := int_Mangueras;
    try
      TROSerializer(ASerializer).ReadArray('Mangueras', aMaguera, l_Mangueras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mangueras']));
    end;
    if int_Mangueras <> l_Mangueras then Mangueras.Free;
    Self.Mangueras := l_Mangueras;
    l_MaximoLecturasImporte := Self.MaximoLecturasImporte;
    try
      TROSerializer(ASerializer).ReadDouble('MaximoLecturasImporte', TypInfo.ftDouble, l_MaximoLecturasImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MaximoLecturasImporte']));
    end;
    Self.MaximoLecturasImporte := l_MaximoLecturasImporte;
    l_MaximoLecturasVolumen := Self.MaximoLecturasVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('MaximoLecturasVolumen', TypInfo.ftDouble, l_MaximoLecturasVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'MaximoLecturasVolumen']));
    end;
    Self.MaximoLecturasVolumen := l_MaximoLecturasVolumen;
    l_Modo := Self.Modo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Modo',System.TypeInfo(eModoOperacion), l_Modo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Modo']));
    end;
    Self.Modo := l_Modo;
    l_NoMangueras := Self.NoMangueras;
    try
      TROSerializer(ASerializer).ReadInteger('NoMangueras', TypInfo.otSLong, l_NoMangueras);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoMangueras']));
    end;
    Self.NoMangueras := l_NoMangueras;
    l_NombreImpresora := Self.NombreImpresora;
    try
      TROSerializer(ASerializer).ReadAnsiString('NombreImpresora', l_NombreImpresora);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NombreImpresora']));
    end;
    Self.NombreImpresora := l_NombreImpresora;
    l_NuevoFlujo := Self.NuevoFlujo;
    try
      TROSerializer(ASerializer).ReadAnsiString('NuevoFlujo', l_NuevoFlujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NuevoFlujo']));
    end;
    Self.NuevoFlujo := l_NuevoFlujo;
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(boolean), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_PendienteGrabar := Self.PendienteGrabar;
    try
      TROSerializer(ASerializer).ReadEnumerated('PendienteGrabar',System.TypeInfo(boolean), l_PendienteGrabar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PendienteGrabar']));
    end;
    Self.PendienteGrabar := l_PendienteGrabar;
    l_PendientesAplicar := int_PendientesAplicar;
    try
      TROSerializer(ASerializer).ReadArray('PendientesAplicar', aContador, l_PendientesAplicar);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PendientesAplicar']));
    end;
    if int_PendientesAplicar <> l_PendientesAplicar then PendientesAplicar.Free;
    Self.PendientesAplicar := l_PendientesAplicar;
    l_Posicionamiento := int_Posicionamiento;
    try
      TROSerializer(ASerializer).ReadStruct('Posicionamiento', TCanal, l_Posicionamiento);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Posicionamiento']));
    end;
    if int_Posicionamiento <> l_Posicionamiento then Posicionamiento.Free;
    Self.Posicionamiento := l_Posicionamiento;
    l_Preset := Self.Preset;
    try
      TROSerializer(ASerializer).ReadEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Preset']));
    end;
    Self.Preset := l_Preset;
    l_Proceso := Self.Proceso;
    try
      TROSerializer(ASerializer).ReadEnumerated('Proceso',System.TypeInfo(eProceso), l_Proceso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Proceso']));
    end;
    Self.Proceso := l_Proceso;
    l_PuntosRegistrados := Self.PuntosRegistrados;
    try
      TROSerializer(ASerializer).ReadEnumerated('PuntosRegistrados',System.TypeInfo(boolean), l_PuntosRegistrados);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntosRegistrados']));
    end;
    Self.PuntosRegistrados := l_PuntosRegistrados;
    l_PuntoVenta := int_PuntoVenta;
    try
      TROSerializer(ASerializer).ReadStruct('PuntoVenta', TRegistroPuntoVenta, l_PuntoVenta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PuntoVenta']));
    end;
    if int_PuntoVenta <> l_PuntoVenta then PuntoVenta.Free;
    Self.PuntoVenta := l_PuntoVenta;
    l_RegresaModo := Self.RegresaModo;
    try
      TROSerializer(ASerializer).ReadEnumerated('RegresaModo',System.TypeInfo(boolean), l_RegresaModo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RegresaModo']));
    end;
    Self.RegresaModo := l_RegresaModo;
    l_Servicio := Self.Servicio;
    try
      TROSerializer(ASerializer).ReadDouble('Servicio', TypInfo.ftDouble, l_Servicio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Servicio']));
    end;
    Self.Servicio := l_Servicio;
    l_Ticket := Self.Ticket;
    try
      TROSerializer(ASerializer).ReadEnumerated('Ticket',System.TypeInfo(eImpresionTicket), l_Ticket);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Ticket']));
    end;
    Self.Ticket := l_Ticket;
    l_TiempoAutorizado := Self.TiempoAutorizado;
    try
      TROSerializer(ASerializer).ReadInteger('TiempoAutorizado', TypInfo.otSLong, l_TiempoAutorizado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TiempoAutorizado']));
    end;
    Self.TiempoAutorizado := l_TiempoAutorizado;
    l_TipoDispensario := Self.TipoDispensario;
    try
      TROSerializer(ASerializer).ReadInteger('TipoDispensario', TypInfo.otSLong, l_TipoDispensario);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoDispensario']));
    end;
    Self.TipoDispensario := l_TipoDispensario;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadInteger('TipoTarjeta', TypInfo.otSLong, l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_TotalizacionAutomatica := Self.TotalizacionAutomatica;
    try
      TROSerializer(ASerializer).ReadInteger('TotalizacionAutomatica', TypInfo.otSLong, l_TotalizacionAutomatica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TotalizacionAutomatica']));
    end;
    Self.TotalizacionAutomatica := l_TotalizacionAutomatica;
    l_TurnoID := Self.TurnoID;
    try
      TROSerializer(ASerializer).ReadInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TurnoID']));
    end;
    Self.TurnoID := l_TurnoID;
    l_UltimoFlujo := Self.UltimoFlujo;
    try
      TROSerializer(ASerializer).ReadAnsiString('UltimoFlujo', l_UltimoFlujo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoFlujo']));
    end;
    Self.UltimoFlujo := l_UltimoFlujo;
    l_UltimoLimiteImporte := Self.UltimoLimiteImporte;
    try
      TROSerializer(ASerializer).ReadDouble('UltimoLimiteImporte', TypInfo.ftDouble, l_UltimoLimiteImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoLimiteImporte']));
    end;
    Self.UltimoLimiteImporte := l_UltimoLimiteImporte;
    l_UltimoLimiteVolumen := Self.UltimoLimiteVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('UltimoLimiteVolumen', TypInfo.ftDouble, l_UltimoLimiteVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'UltimoLimiteVolumen']));
    end;
    Self.UltimoLimiteVolumen := l_UltimoLimiteVolumen;
    l_VentaAntePenultima := int_VentaAntePenultima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaAntePenultima', TRegistroVenta, l_VentaAntePenultima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAntePenultima']));
    end;
    if int_VentaAntePenultima <> l_VentaAntePenultima then VentaAntePenultima.Free;
    Self.VentaAntePenultima := l_VentaAntePenultima;
    l_VentaPenultima := int_VentaPenultima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaPenultima', TRegistroVenta, l_VentaPenultima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaPenultima']));
    end;
    if int_VentaPenultima <> l_VentaPenultima then VentaPenultima.Free;
    Self.VentaPenultima := l_VentaPenultima;
    l_VentaProceso := int_VentaProceso;
    try
      TROSerializer(ASerializer).ReadStruct('VentaProceso', TRegistroVenta, l_VentaProceso);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaProceso']));
    end;
    if int_VentaProceso <> l_VentaProceso then VentaProceso.Free;
    Self.VentaProceso := l_VentaProceso;
    l_VentasTurno := int_VentasTurno;
    try
      TROSerializer(ASerializer).ReadArray('VentasTurno', aContador, l_VentasTurno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentasTurno']));
    end;
    if int_VentasTurno <> l_VentasTurno then VentasTurno.Free;
    Self.VentasTurno := l_VentasTurno;
    l_VentaUltima := int_VentaUltima;
    try
      TROSerializer(ASerializer).ReadStruct('VentaUltima', TRegistroVenta, l_VentaUltima);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaUltima']));
    end;
    if int_VentaUltima <> l_VentaUltima then VentaUltima.Free;
    Self.VentaUltima := l_VentaUltima;
  end;
end;

procedure TBomba.WriteComplex(ASerializer: TObject);
var
  l_AutoImprimir: Boolean;
  l_Autorizacion: eTipoAutorizacion;
  l_AutorizacionAutomatica: Integer;
  l_Clasificacion: AnsiString;
  l_CodigoProducto: eCodigoProducto;
  l_ConectLCD: Integer;
  l_Despachador: Integer;
  l_DobleTarjeta: Boolean;
  l_Estado: eEstadoBomba;
  l_EstadoNuevo: eEstadoBomba;
  l_EstadoVenta: eEstadoVenta;
  l_FactorDecimal: TFactorDecimal;
  l_FaltaPago: aBoolean;
  l_Flujo: TFlujos;
  l_Gasolina: Integer;
  l_Guardando: Boolean;
  l_Host: AnsiString;
  l_ImporteMinimo: Double;
  l_IntentoPrepago: Boolean;
  l_LecturasFinales: aLecturas;
  l_LecturasIniciales: aLecturas;
  l_LecturasTurno: aLecturas;
  l_Mangueras: aMaguera;
  l_MaximoLecturasImporte: Double;
  l_MaximoLecturasVolumen: Double;
  l_Modo: eModoOperacion;
  l_NoMangueras: Integer;
  l_NombreImpresora: AnsiString;
  l_NuevoFlujo: AnsiString;
  l_Pendiente: Boolean;
  l_PendienteGrabar: Boolean;
  l_PendientesAplicar: aContador;
  l_Posicionamiento: TCanal;
  l_Preset: eTipoPreset;
  l_Proceso: eProceso;
  l_PuntosRegistrados: Boolean;
  l_PuntoVenta: TRegistroPuntoVenta;
  l_RegresaModo: Boolean;
  l_Servicio: Double;
  l_Ticket: eImpresionTicket;
  l_TiempoAutorizado: Integer;
  l_TipoDispensario: Integer;
  l_TipoTarjeta: Integer;
  l_TotalizacionAutomatica: Integer;
  l_TurnoID: Integer;
  l_UltimoFlujo: AnsiString;
  l_UltimoLimiteImporte: Double;
  l_UltimoLimiteVolumen: Double;
  l_VentaAntePenultima: TRegistroVenta;
  l_VentaPenultima: TRegistroVenta;
  l_VentaProceso: TRegistroVenta;
  l_VentasTurno: aContador;
  l_VentaUltima: TRegistroVenta;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TBomba);
    l_AutoImprimir := Self.AutoImprimir;
    TROSerializer(ASerializer).WriteEnumerated('AutoImprimir',System.TypeInfo(boolean), l_AutoImprimir);
    l_Autorizacion := Self.Autorizacion;
    TROSerializer(ASerializer).WriteEnumerated('Autorizacion',System.TypeInfo(eTipoAutorizacion), l_Autorizacion);
    l_AutorizacionAutomatica := Self.AutorizacionAutomatica;
    TROSerializer(ASerializer).WriteInteger('AutorizacionAutomatica', TypInfo.otSLong, l_AutorizacionAutomatica);
    l_Clasificacion := Self.Clasificacion;
    TROSerializer(ASerializer).WriteAnsiString('Clasificacion', l_Clasificacion);
    l_CodigoProducto := Self.CodigoProducto;
    TROSerializer(ASerializer).WriteEnumerated('CodigoProducto',System.TypeInfo(eCodigoProducto), l_CodigoProducto);
    l_ConectLCD := Self.ConectLCD;
    TROSerializer(ASerializer).WriteInteger('ConectLCD', TypInfo.otSLong, l_ConectLCD);
    l_Despachador := Self.Despachador;
    TROSerializer(ASerializer).WriteInteger('Despachador', TypInfo.otSLong, l_Despachador);
    l_DobleTarjeta := Self.DobleTarjeta;
    TROSerializer(ASerializer).WriteEnumerated('DobleTarjeta',System.TypeInfo(boolean), l_DobleTarjeta);
    l_Estado := Self.Estado;
    TROSerializer(ASerializer).WriteEnumerated('Estado',System.TypeInfo(eEstadoBomba), l_Estado);
    l_EstadoNuevo := Self.EstadoNuevo;
    TROSerializer(ASerializer).WriteEnumerated('EstadoNuevo',System.TypeInfo(eEstadoBomba), l_EstadoNuevo);
    l_EstadoVenta := Self.EstadoVenta;
    TROSerializer(ASerializer).WriteEnumerated('EstadoVenta',System.TypeInfo(eEstadoVenta), l_EstadoVenta);
    l_FactorDecimal := int_FactorDecimal;
    TROSerializer(ASerializer).WriteStruct('FactorDecimal', l_FactorDecimal, TFactorDecimal);
    l_FaltaPago := int_FaltaPago;
    TROSerializer(ASerializer).WriteArray('FaltaPago', l_FaltaPago, aBoolean);
    l_Flujo := int_Flujo;
    TROSerializer(ASerializer).WriteStruct('Flujo', l_Flujo, TFlujos);
    l_Gasolina := Self.Gasolina;
    TROSerializer(ASerializer).WriteInteger('Gasolina', TypInfo.otSLong, l_Gasolina);
    l_Guardando := Self.Guardando;
    TROSerializer(ASerializer).WriteEnumerated('Guardando',System.TypeInfo(boolean), l_Guardando);
    l_ImporteMinimo := Self.ImporteMinimo;
    TROSerializer(ASerializer).WriteDouble('ImporteMinimo', TypInfo.ftDouble, l_ImporteMinimo);
    l_IntentoPrepago := Self.IntentoPrepago;
    TROSerializer(ASerializer).WriteEnumerated('IntentoPrepago',System.TypeInfo(boolean), l_IntentoPrepago);
    l_LecturasFinales := int_LecturasFinales;
    TROSerializer(ASerializer).WriteArray('LecturasFinales', l_LecturasFinales, aLecturas);
    l_LecturasIniciales := int_LecturasIniciales;
    TROSerializer(ASerializer).WriteArray('LecturasIniciales', l_LecturasIniciales, aLecturas);
    l_LecturasTurno := int_LecturasTurno;
    TROSerializer(ASerializer).WriteArray('LecturasTurno', l_LecturasTurno, aLecturas);
    l_Mangueras := int_Mangueras;
    TROSerializer(ASerializer).WriteArray('Mangueras', l_Mangueras, aMaguera);
    l_MaximoLecturasImporte := Self.MaximoLecturasImporte;
    TROSerializer(ASerializer).WriteDouble('MaximoLecturasImporte', TypInfo.ftDouble, l_MaximoLecturasImporte);
    l_MaximoLecturasVolumen := Self.MaximoLecturasVolumen;
    TROSerializer(ASerializer).WriteDouble('MaximoLecturasVolumen', TypInfo.ftDouble, l_MaximoLecturasVolumen);
    l_Modo := Self.Modo;
    TROSerializer(ASerializer).WriteEnumerated('Modo',System.TypeInfo(eModoOperacion), l_Modo);
    l_NoMangueras := Self.NoMangueras;
    TROSerializer(ASerializer).WriteInteger('NoMangueras', TypInfo.otSLong, l_NoMangueras);
    l_NombreImpresora := Self.NombreImpresora;
    TROSerializer(ASerializer).WriteAnsiString('NombreImpresora', l_NombreImpresora);
    l_NuevoFlujo := Self.NuevoFlujo;
    TROSerializer(ASerializer).WriteAnsiString('NuevoFlujo', l_NuevoFlujo);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(boolean), l_Pendiente);
    l_PendienteGrabar := Self.PendienteGrabar;
    TROSerializer(ASerializer).WriteEnumerated('PendienteGrabar',System.TypeInfo(boolean), l_PendienteGrabar);
    l_PendientesAplicar := int_PendientesAplicar;
    TROSerializer(ASerializer).WriteArray('PendientesAplicar', l_PendientesAplicar, aContador);
    l_Posicionamiento := int_Posicionamiento;
    TROSerializer(ASerializer).WriteStruct('Posicionamiento', l_Posicionamiento, TCanal);
    l_Preset := Self.Preset;
    TROSerializer(ASerializer).WriteEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    l_Proceso := Self.Proceso;
    TROSerializer(ASerializer).WriteEnumerated('Proceso',System.TypeInfo(eProceso), l_Proceso);
    l_PuntosRegistrados := Self.PuntosRegistrados;
    TROSerializer(ASerializer).WriteEnumerated('PuntosRegistrados',System.TypeInfo(boolean), l_PuntosRegistrados);
    l_PuntoVenta := int_PuntoVenta;
    TROSerializer(ASerializer).WriteStruct('PuntoVenta', l_PuntoVenta, TRegistroPuntoVenta);
    l_RegresaModo := Self.RegresaModo;
    TROSerializer(ASerializer).WriteEnumerated('RegresaModo',System.TypeInfo(boolean), l_RegresaModo);
    l_Servicio := Self.Servicio;
    TROSerializer(ASerializer).WriteDouble('Servicio', TypInfo.ftDouble, l_Servicio);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteEnumerated('Ticket',System.TypeInfo(eImpresionTicket), l_Ticket);
    l_TiempoAutorizado := Self.TiempoAutorizado;
    TROSerializer(ASerializer).WriteInteger('TiempoAutorizado', TypInfo.otSLong, l_TiempoAutorizado);
    l_TipoDispensario := Self.TipoDispensario;
    TROSerializer(ASerializer).WriteInteger('TipoDispensario', TypInfo.otSLong, l_TipoDispensario);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteInteger('TipoTarjeta', TypInfo.otSLong, l_TipoTarjeta);
    l_TotalizacionAutomatica := Self.TotalizacionAutomatica;
    TROSerializer(ASerializer).WriteInteger('TotalizacionAutomatica', TypInfo.otSLong, l_TotalizacionAutomatica);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    l_UltimoFlujo := Self.UltimoFlujo;
    TROSerializer(ASerializer).WriteAnsiString('UltimoFlujo', l_UltimoFlujo);
    l_UltimoLimiteImporte := Self.UltimoLimiteImporte;
    TROSerializer(ASerializer).WriteDouble('UltimoLimiteImporte', TypInfo.ftDouble, l_UltimoLimiteImporte);
    l_UltimoLimiteVolumen := Self.UltimoLimiteVolumen;
    TROSerializer(ASerializer).WriteDouble('UltimoLimiteVolumen', TypInfo.ftDouble, l_UltimoLimiteVolumen);
    l_VentaProceso := int_VentaProceso;
    TROSerializer(ASerializer).WriteStruct('VentaProceso', l_VentaProceso, TRegistroVenta);
    l_VentaUltima := int_VentaUltima;
    TROSerializer(ASerializer).WriteStruct('VentaUltima', l_VentaUltima, TRegistroVenta);
    l_VentaPenultima := int_VentaPenultima;
    TROSerializer(ASerializer).WriteStruct('VentaPenultima', l_VentaPenultima, TRegistroVenta);
    l_VentaAntePenultima := int_VentaAntePenultima;
    TROSerializer(ASerializer).WriteStruct('VentaAntePenultima', l_VentaAntePenultima, TRegistroVenta);
    l_VentasTurno := int_VentasTurno;
    TROSerializer(ASerializer).WriteArray('VentasTurno', l_VentasTurno, aContador);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
  end
  else begin
    l_AutoImprimir := Self.AutoImprimir;
    TROSerializer(ASerializer).WriteEnumerated('AutoImprimir',System.TypeInfo(boolean), l_AutoImprimir);
    l_Autorizacion := Self.Autorizacion;
    TROSerializer(ASerializer).WriteEnumerated('Autorizacion',System.TypeInfo(eTipoAutorizacion), l_Autorizacion);
    l_AutorizacionAutomatica := Self.AutorizacionAutomatica;
    TROSerializer(ASerializer).WriteInteger('AutorizacionAutomatica', TypInfo.otSLong, l_AutorizacionAutomatica);
    l_Clasificacion := Self.Clasificacion;
    TROSerializer(ASerializer).WriteAnsiString('Clasificacion', l_Clasificacion);
    l_CodigoProducto := Self.CodigoProducto;
    TROSerializer(ASerializer).WriteEnumerated('CodigoProducto',System.TypeInfo(eCodigoProducto), l_CodigoProducto);
    l_ConectLCD := Self.ConectLCD;
    TROSerializer(ASerializer).WriteInteger('ConectLCD', TypInfo.otSLong, l_ConectLCD);
    l_Despachador := Self.Despachador;
    TROSerializer(ASerializer).WriteInteger('Despachador', TypInfo.otSLong, l_Despachador);
    l_DobleTarjeta := Self.DobleTarjeta;
    TROSerializer(ASerializer).WriteEnumerated('DobleTarjeta',System.TypeInfo(boolean), l_DobleTarjeta);
    l_Estado := Self.Estado;
    TROSerializer(ASerializer).WriteEnumerated('Estado',System.TypeInfo(eEstadoBomba), l_Estado);
    l_EstadoNuevo := Self.EstadoNuevo;
    TROSerializer(ASerializer).WriteEnumerated('EstadoNuevo',System.TypeInfo(eEstadoBomba), l_EstadoNuevo);
    l_EstadoVenta := Self.EstadoVenta;
    TROSerializer(ASerializer).WriteEnumerated('EstadoVenta',System.TypeInfo(eEstadoVenta), l_EstadoVenta);
    l_FactorDecimal := int_FactorDecimal;
    TROSerializer(ASerializer).WriteStruct('FactorDecimal', l_FactorDecimal, TFactorDecimal);
    l_FaltaPago := int_FaltaPago;
    TROSerializer(ASerializer).WriteArray('FaltaPago', l_FaltaPago, aBoolean);
    l_Flujo := int_Flujo;
    TROSerializer(ASerializer).WriteStruct('Flujo', l_Flujo, TFlujos);
    l_Gasolina := Self.Gasolina;
    TROSerializer(ASerializer).WriteInteger('Gasolina', TypInfo.otSLong, l_Gasolina);
    l_Guardando := Self.Guardando;
    TROSerializer(ASerializer).WriteEnumerated('Guardando',System.TypeInfo(boolean), l_Guardando);
    l_Host := Self.Host;
    TROSerializer(ASerializer).WriteAnsiString('Host', l_Host);
    l_ImporteMinimo := Self.ImporteMinimo;
    TROSerializer(ASerializer).WriteDouble('ImporteMinimo', TypInfo.ftDouble, l_ImporteMinimo);
    l_IntentoPrepago := Self.IntentoPrepago;
    TROSerializer(ASerializer).WriteEnumerated('IntentoPrepago',System.TypeInfo(boolean), l_IntentoPrepago);
    l_LecturasFinales := int_LecturasFinales;
    TROSerializer(ASerializer).WriteArray('LecturasFinales', l_LecturasFinales, aLecturas);
    l_LecturasIniciales := int_LecturasIniciales;
    TROSerializer(ASerializer).WriteArray('LecturasIniciales', l_LecturasIniciales, aLecturas);
    l_LecturasTurno := int_LecturasTurno;
    TROSerializer(ASerializer).WriteArray('LecturasTurno', l_LecturasTurno, aLecturas);
    l_Mangueras := int_Mangueras;
    TROSerializer(ASerializer).WriteArray('Mangueras', l_Mangueras, aMaguera);
    l_MaximoLecturasImporte := Self.MaximoLecturasImporte;
    TROSerializer(ASerializer).WriteDouble('MaximoLecturasImporte', TypInfo.ftDouble, l_MaximoLecturasImporte);
    l_MaximoLecturasVolumen := Self.MaximoLecturasVolumen;
    TROSerializer(ASerializer).WriteDouble('MaximoLecturasVolumen', TypInfo.ftDouble, l_MaximoLecturasVolumen);
    l_Modo := Self.Modo;
    TROSerializer(ASerializer).WriteEnumerated('Modo',System.TypeInfo(eModoOperacion), l_Modo);
    l_NoMangueras := Self.NoMangueras;
    TROSerializer(ASerializer).WriteInteger('NoMangueras', TypInfo.otSLong, l_NoMangueras);
    l_NombreImpresora := Self.NombreImpresora;
    TROSerializer(ASerializer).WriteAnsiString('NombreImpresora', l_NombreImpresora);
    l_NuevoFlujo := Self.NuevoFlujo;
    TROSerializer(ASerializer).WriteAnsiString('NuevoFlujo', l_NuevoFlujo);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(boolean), l_Pendiente);
    l_PendienteGrabar := Self.PendienteGrabar;
    TROSerializer(ASerializer).WriteEnumerated('PendienteGrabar',System.TypeInfo(boolean), l_PendienteGrabar);
    l_PendientesAplicar := int_PendientesAplicar;
    TROSerializer(ASerializer).WriteArray('PendientesAplicar', l_PendientesAplicar, aContador);
    l_Posicionamiento := int_Posicionamiento;
    TROSerializer(ASerializer).WriteStruct('Posicionamiento', l_Posicionamiento, TCanal);
    l_Preset := Self.Preset;
    TROSerializer(ASerializer).WriteEnumerated('Preset',System.TypeInfo(eTipoPreset), l_Preset);
    l_Proceso := Self.Proceso;
    TROSerializer(ASerializer).WriteEnumerated('Proceso',System.TypeInfo(eProceso), l_Proceso);
    l_PuntosRegistrados := Self.PuntosRegistrados;
    TROSerializer(ASerializer).WriteEnumerated('PuntosRegistrados',System.TypeInfo(boolean), l_PuntosRegistrados);
    l_PuntoVenta := int_PuntoVenta;
    TROSerializer(ASerializer).WriteStruct('PuntoVenta', l_PuntoVenta, TRegistroPuntoVenta);
    l_RegresaModo := Self.RegresaModo;
    TROSerializer(ASerializer).WriteEnumerated('RegresaModo',System.TypeInfo(boolean), l_RegresaModo);
    l_Servicio := Self.Servicio;
    TROSerializer(ASerializer).WriteDouble('Servicio', TypInfo.ftDouble, l_Servicio);
    l_Ticket := Self.Ticket;
    TROSerializer(ASerializer).WriteEnumerated('Ticket',System.TypeInfo(eImpresionTicket), l_Ticket);
    l_TiempoAutorizado := Self.TiempoAutorizado;
    TROSerializer(ASerializer).WriteInteger('TiempoAutorizado', TypInfo.otSLong, l_TiempoAutorizado);
    l_TipoDispensario := Self.TipoDispensario;
    TROSerializer(ASerializer).WriteInteger('TipoDispensario', TypInfo.otSLong, l_TipoDispensario);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteInteger('TipoTarjeta', TypInfo.otSLong, l_TipoTarjeta);
    l_TotalizacionAutomatica := Self.TotalizacionAutomatica;
    TROSerializer(ASerializer).WriteInteger('TotalizacionAutomatica', TypInfo.otSLong, l_TotalizacionAutomatica);
    l_TurnoID := Self.TurnoID;
    TROSerializer(ASerializer).WriteInteger('TurnoID', TypInfo.otSLong, l_TurnoID);
    l_UltimoFlujo := Self.UltimoFlujo;
    TROSerializer(ASerializer).WriteAnsiString('UltimoFlujo', l_UltimoFlujo);
    l_UltimoLimiteImporte := Self.UltimoLimiteImporte;
    TROSerializer(ASerializer).WriteDouble('UltimoLimiteImporte', TypInfo.ftDouble, l_UltimoLimiteImporte);
    l_UltimoLimiteVolumen := Self.UltimoLimiteVolumen;
    TROSerializer(ASerializer).WriteDouble('UltimoLimiteVolumen', TypInfo.ftDouble, l_UltimoLimiteVolumen);
    l_VentaAntePenultima := int_VentaAntePenultima;
    TROSerializer(ASerializer).WriteStruct('VentaAntePenultima', l_VentaAntePenultima, TRegistroVenta);
    l_VentaPenultima := int_VentaPenultima;
    TROSerializer(ASerializer).WriteStruct('VentaPenultima', l_VentaPenultima, TRegistroVenta);
    l_VentaProceso := int_VentaProceso;
    TROSerializer(ASerializer).WriteStruct('VentaProceso', l_VentaProceso, TRegistroVenta);
    l_VentasTurno := int_VentasTurno;
    TROSerializer(ASerializer).WriteArray('VentasTurno', l_VentasTurno, aContador);
    l_VentaUltima := int_VentaUltima;
    TROSerializer(ASerializer).WriteStruct('VentaUltima', l_VentaUltima, TRegistroVenta);
  end;
end;

{ TBombaCollection }
constructor TBombaCollection.Create;
begin
  inherited Create(TBomba);
end;

constructor TBombaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TBombaCollection.Add: TBomba;
begin
  Result := TBomba(inherited Add);
end;

function TBombaCollection.GetItems(aIndex: Integer): TBomba;
begin
  Result := TBomba(inherited Items[aIndex]);
end;

procedure TBombaCollection.LoadFromArray(anArray: aBombas);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TBombaCollection.SaveToArray(anArray: aBombas);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TBomba(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TBombaCollection.SetItems(aIndex: Integer; const Value: TBomba);
begin
  TBomba(inherited Items[aIndex]).Assign(Value);
end;

{ TFactorDecimal }

procedure TFactorDecimal.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFactorDecimal;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFactorDecimal) then begin
    lSource := LibraryValesCupones_Intf.TFactorDecimal(iSource);

    Self.CambioPrecios := lSource.CambioPrecios;
    Self.VentaPrecio := lSource.VentaPrecio;
    Self.VentaVolumen := lSource.VentaVolumen;
    Self.VentaImporte := lSource.VentaImporte;
    Self.LecturasVolumen := lSource.LecturasVolumen;
    Self.LecturasImporte := lSource.LecturasImporte;
    Self.DespliegaVolumen := lSource.DespliegaVolumen;
    Self.DespliegaImporte := lSource.DespliegaImporte;
    Self.PrefijarVolumen := lSource.PrefijarVolumen;
    Self.PrefijarImporte := lSource.PrefijarImporte;
  end;
end;

procedure TFactorDecimal.ReadComplex(ASerializer: TObject);
var
  l_CambioPrecios: Double;
  l_DespliegaImporte: Double;
  l_DespliegaVolumen: Double;
  l_LecturasImporte: Double;
  l_LecturasVolumen: Double;
  l_PrefijarImporte: Double;
  l_PrefijarVolumen: Double;
  l_VentaImporte: Double;
  l_VentaPrecio: Double;
  l_VentaVolumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_CambioPrecios := Self.CambioPrecios;
    try
      TROSerializer(ASerializer).ReadDouble('CambioPrecios', TypInfo.ftDouble, l_CambioPrecios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CambioPrecios']));
    end;
    Self.CambioPrecios := l_CambioPrecios;
    l_VentaPrecio := Self.VentaPrecio;
    try
      TROSerializer(ASerializer).ReadDouble('VentaPrecio', TypInfo.ftDouble, l_VentaPrecio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaPrecio']));
    end;
    Self.VentaPrecio := l_VentaPrecio;
    l_VentaVolumen := Self.VentaVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('VentaVolumen', TypInfo.ftDouble, l_VentaVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaVolumen']));
    end;
    Self.VentaVolumen := l_VentaVolumen;
    l_VentaImporte := Self.VentaImporte;
    try
      TROSerializer(ASerializer).ReadDouble('VentaImporte', TypInfo.ftDouble, l_VentaImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaImporte']));
    end;
    Self.VentaImporte := l_VentaImporte;
    l_LecturasVolumen := Self.LecturasVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('LecturasVolumen', TypInfo.ftDouble, l_LecturasVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasVolumen']));
    end;
    Self.LecturasVolumen := l_LecturasVolumen;
    l_LecturasImporte := Self.LecturasImporte;
    try
      TROSerializer(ASerializer).ReadDouble('LecturasImporte', TypInfo.ftDouble, l_LecturasImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasImporte']));
    end;
    Self.LecturasImporte := l_LecturasImporte;
    l_DespliegaVolumen := Self.DespliegaVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('DespliegaVolumen', TypInfo.ftDouble, l_DespliegaVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespliegaVolumen']));
    end;
    Self.DespliegaVolumen := l_DespliegaVolumen;
    l_DespliegaImporte := Self.DespliegaImporte;
    try
      TROSerializer(ASerializer).ReadDouble('DespliegaImporte', TypInfo.ftDouble, l_DespliegaImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespliegaImporte']));
    end;
    Self.DespliegaImporte := l_DespliegaImporte;
    l_PrefijarVolumen := Self.PrefijarVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('PrefijarVolumen', TypInfo.ftDouble, l_PrefijarVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrefijarVolumen']));
    end;
    Self.PrefijarVolumen := l_PrefijarVolumen;
    l_PrefijarImporte := Self.PrefijarImporte;
    try
      TROSerializer(ASerializer).ReadDouble('PrefijarImporte', TypInfo.ftDouble, l_PrefijarImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrefijarImporte']));
    end;
    Self.PrefijarImporte := l_PrefijarImporte;
  end
  else begin
    l_CambioPrecios := Self.CambioPrecios;
    try
      TROSerializer(ASerializer).ReadDouble('CambioPrecios', TypInfo.ftDouble, l_CambioPrecios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CambioPrecios']));
    end;
    Self.CambioPrecios := l_CambioPrecios;
    l_DespliegaImporte := Self.DespliegaImporte;
    try
      TROSerializer(ASerializer).ReadDouble('DespliegaImporte', TypInfo.ftDouble, l_DespliegaImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespliegaImporte']));
    end;
    Self.DespliegaImporte := l_DespliegaImporte;
    l_DespliegaVolumen := Self.DespliegaVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('DespliegaVolumen', TypInfo.ftDouble, l_DespliegaVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DespliegaVolumen']));
    end;
    Self.DespliegaVolumen := l_DespliegaVolumen;
    l_LecturasImporte := Self.LecturasImporte;
    try
      TROSerializer(ASerializer).ReadDouble('LecturasImporte', TypInfo.ftDouble, l_LecturasImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasImporte']));
    end;
    Self.LecturasImporte := l_LecturasImporte;
    l_LecturasVolumen := Self.LecturasVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('LecturasVolumen', TypInfo.ftDouble, l_LecturasVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturasVolumen']));
    end;
    Self.LecturasVolumen := l_LecturasVolumen;
    l_PrefijarImporte := Self.PrefijarImporte;
    try
      TROSerializer(ASerializer).ReadDouble('PrefijarImporte', TypInfo.ftDouble, l_PrefijarImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrefijarImporte']));
    end;
    Self.PrefijarImporte := l_PrefijarImporte;
    l_PrefijarVolumen := Self.PrefijarVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('PrefijarVolumen', TypInfo.ftDouble, l_PrefijarVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrefijarVolumen']));
    end;
    Self.PrefijarVolumen := l_PrefijarVolumen;
    l_VentaImporte := Self.VentaImporte;
    try
      TROSerializer(ASerializer).ReadDouble('VentaImporte', TypInfo.ftDouble, l_VentaImporte);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaImporte']));
    end;
    Self.VentaImporte := l_VentaImporte;
    l_VentaPrecio := Self.VentaPrecio;
    try
      TROSerializer(ASerializer).ReadDouble('VentaPrecio', TypInfo.ftDouble, l_VentaPrecio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaPrecio']));
    end;
    Self.VentaPrecio := l_VentaPrecio;
    l_VentaVolumen := Self.VentaVolumen;
    try
      TROSerializer(ASerializer).ReadDouble('VentaVolumen', TypInfo.ftDouble, l_VentaVolumen);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaVolumen']));
    end;
    Self.VentaVolumen := l_VentaVolumen;
  end;
end;

procedure TFactorDecimal.WriteComplex(ASerializer: TObject);
var
  l_CambioPrecios: Double;
  l_DespliegaImporte: Double;
  l_DespliegaVolumen: Double;
  l_LecturasImporte: Double;
  l_LecturasVolumen: Double;
  l_PrefijarImporte: Double;
  l_PrefijarVolumen: Double;
  l_VentaImporte: Double;
  l_VentaPrecio: Double;
  l_VentaVolumen: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFactorDecimal);
    l_CambioPrecios := Self.CambioPrecios;
    TROSerializer(ASerializer).WriteDouble('CambioPrecios', TypInfo.ftDouble, l_CambioPrecios);
    l_VentaPrecio := Self.VentaPrecio;
    TROSerializer(ASerializer).WriteDouble('VentaPrecio', TypInfo.ftDouble, l_VentaPrecio);
    l_VentaVolumen := Self.VentaVolumen;
    TROSerializer(ASerializer).WriteDouble('VentaVolumen', TypInfo.ftDouble, l_VentaVolumen);
    l_VentaImporte := Self.VentaImporte;
    TROSerializer(ASerializer).WriteDouble('VentaImporte', TypInfo.ftDouble, l_VentaImporte);
    l_LecturasVolumen := Self.LecturasVolumen;
    TROSerializer(ASerializer).WriteDouble('LecturasVolumen', TypInfo.ftDouble, l_LecturasVolumen);
    l_LecturasImporte := Self.LecturasImporte;
    TROSerializer(ASerializer).WriteDouble('LecturasImporte', TypInfo.ftDouble, l_LecturasImporte);
    l_DespliegaVolumen := Self.DespliegaVolumen;
    TROSerializer(ASerializer).WriteDouble('DespliegaVolumen', TypInfo.ftDouble, l_DespliegaVolumen);
    l_DespliegaImporte := Self.DespliegaImporte;
    TROSerializer(ASerializer).WriteDouble('DespliegaImporte', TypInfo.ftDouble, l_DespliegaImporte);
    l_PrefijarVolumen := Self.PrefijarVolumen;
    TROSerializer(ASerializer).WriteDouble('PrefijarVolumen', TypInfo.ftDouble, l_PrefijarVolumen);
    l_PrefijarImporte := Self.PrefijarImporte;
    TROSerializer(ASerializer).WriteDouble('PrefijarImporte', TypInfo.ftDouble, l_PrefijarImporte);
  end
  else begin
    l_CambioPrecios := Self.CambioPrecios;
    TROSerializer(ASerializer).WriteDouble('CambioPrecios', TypInfo.ftDouble, l_CambioPrecios);
    l_DespliegaImporte := Self.DespliegaImporte;
    TROSerializer(ASerializer).WriteDouble('DespliegaImporte', TypInfo.ftDouble, l_DespliegaImporte);
    l_DespliegaVolumen := Self.DespliegaVolumen;
    TROSerializer(ASerializer).WriteDouble('DespliegaVolumen', TypInfo.ftDouble, l_DespliegaVolumen);
    l_LecturasImporte := Self.LecturasImporte;
    TROSerializer(ASerializer).WriteDouble('LecturasImporte', TypInfo.ftDouble, l_LecturasImporte);
    l_LecturasVolumen := Self.LecturasVolumen;
    TROSerializer(ASerializer).WriteDouble('LecturasVolumen', TypInfo.ftDouble, l_LecturasVolumen);
    l_PrefijarImporte := Self.PrefijarImporte;
    TROSerializer(ASerializer).WriteDouble('PrefijarImporte', TypInfo.ftDouble, l_PrefijarImporte);
    l_PrefijarVolumen := Self.PrefijarVolumen;
    TROSerializer(ASerializer).WriteDouble('PrefijarVolumen', TypInfo.ftDouble, l_PrefijarVolumen);
    l_VentaImporte := Self.VentaImporte;
    TROSerializer(ASerializer).WriteDouble('VentaImporte', TypInfo.ftDouble, l_VentaImporte);
    l_VentaPrecio := Self.VentaPrecio;
    TROSerializer(ASerializer).WriteDouble('VentaPrecio', TypInfo.ftDouble, l_VentaPrecio);
    l_VentaVolumen := Self.VentaVolumen;
    TROSerializer(ASerializer).WriteDouble('VentaVolumen', TypInfo.ftDouble, l_VentaVolumen);
  end;
end;

{ TFactorDecimalCollection }
constructor TFactorDecimalCollection.Create;
begin
  inherited Create(TFactorDecimal);
end;

constructor TFactorDecimalCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFactorDecimalCollection.Add: TFactorDecimal;
begin
  Result := TFactorDecimal(inherited Add);
end;

function TFactorDecimalCollection.GetItems(aIndex: Integer): TFactorDecimal;
begin
  Result := TFactorDecimal(inherited Items[aIndex]);
end;

procedure TFactorDecimalCollection.SetItems(aIndex: Integer; const Value: TFactorDecimal);
begin
  TFactorDecimal(inherited Items[aIndex]).Assign(Value);
end;

{ TFlujos }

procedure TFlujos.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TFlujos;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TFlujos) then begin
    lSource := LibraryValesCupones_Intf.TFlujos(iSource);

    Self.Minimo := lSource.Minimo;
    Self.Maximo := lSource.Maximo;
  end;
end;

procedure TFlujos.ReadComplex(ASerializer: TObject);
var
  l_Maximo: AnsiString;
  l_Minimo: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Minimo := Self.Minimo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Minimo', l_Minimo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Minimo']));
    end;
    Self.Minimo := l_Minimo;
    l_Maximo := Self.Maximo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Maximo', l_Maximo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Maximo']));
    end;
    Self.Maximo := l_Maximo;
  end
  else begin
    l_Maximo := Self.Maximo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Maximo', l_Maximo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Maximo']));
    end;
    Self.Maximo := l_Maximo;
    l_Minimo := Self.Minimo;
    try
      TROSerializer(ASerializer).ReadAnsiString('Minimo', l_Minimo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Minimo']));
    end;
    Self.Minimo := l_Minimo;
  end;
end;

procedure TFlujos.WriteComplex(ASerializer: TObject);
var
  l_Maximo: AnsiString;
  l_Minimo: AnsiString;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TFlujos);
    l_Minimo := Self.Minimo;
    TROSerializer(ASerializer).WriteAnsiString('Minimo', l_Minimo);
    l_Maximo := Self.Maximo;
    TROSerializer(ASerializer).WriteAnsiString('Maximo', l_Maximo);
  end
  else begin
    l_Maximo := Self.Maximo;
    TROSerializer(ASerializer).WriteAnsiString('Maximo', l_Maximo);
    l_Minimo := Self.Minimo;
    TROSerializer(ASerializer).WriteAnsiString('Minimo', l_Minimo);
  end;
end;

{ TFlujosCollection }
constructor TFlujosCollection.Create;
begin
  inherited Create(TFlujos);
end;

constructor TFlujosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TFlujosCollection.Add: TFlujos;
begin
  Result := TFlujos(inherited Add);
end;

function TFlujosCollection.GetItems(aIndex: Integer): TFlujos;
begin
  Result := TFlujos(inherited Items[aIndex]);
end;

procedure TFlujosCollection.SetItems(aIndex: Integer; const Value: TFlujos);
begin
  TFlujos(inherited Items[aIndex]).Assign(Value);
end;

{ TLecturas }

procedure TLecturas.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLecturas;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLecturas) then begin
    lSource := LibraryValesCupones_Intf.TLecturas(iSource);

    Self.LecturaCorrecta := lSource.LecturaCorrecta;
    Self.Precio := lSource.Precio;
    Self.CashMoney := lSource.CashMoney;
    Self.CashVolume := lSource.CashVolume;
    Self.CreditMoney := lSource.CreditMoney;
    Self.CreditVolume := lSource.CreditVolume;
  end;
end;

procedure TLecturas.ReadComplex(ASerializer: TObject);
var
  l_CashMoney: Double;
  l_CashVolume: Double;
  l_CreditMoney: Double;
  l_CreditVolume: Double;
  l_LecturaCorrecta: Boolean;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_LecturaCorrecta := Self.LecturaCorrecta;
    try
      TROSerializer(ASerializer).ReadEnumerated('LecturaCorrecta',System.TypeInfo(boolean), l_LecturaCorrecta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturaCorrecta']));
    end;
    Self.LecturaCorrecta := l_LecturaCorrecta;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_CashMoney := Self.CashMoney;
    try
      TROSerializer(ASerializer).ReadDouble('CashMoney', TypInfo.ftDouble, l_CashMoney);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CashMoney']));
    end;
    Self.CashMoney := l_CashMoney;
    l_CashVolume := Self.CashVolume;
    try
      TROSerializer(ASerializer).ReadDouble('CashVolume', TypInfo.ftDouble, l_CashVolume);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CashVolume']));
    end;
    Self.CashVolume := l_CashVolume;
    l_CreditMoney := Self.CreditMoney;
    try
      TROSerializer(ASerializer).ReadDouble('CreditMoney', TypInfo.ftDouble, l_CreditMoney);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CreditMoney']));
    end;
    Self.CreditMoney := l_CreditMoney;
    l_CreditVolume := Self.CreditVolume;
    try
      TROSerializer(ASerializer).ReadDouble('CreditVolume', TypInfo.ftDouble, l_CreditVolume);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CreditVolume']));
    end;
    Self.CreditVolume := l_CreditVolume;
  end
  else begin
    l_CashMoney := Self.CashMoney;
    try
      TROSerializer(ASerializer).ReadDouble('CashMoney', TypInfo.ftDouble, l_CashMoney);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CashMoney']));
    end;
    Self.CashMoney := l_CashMoney;
    l_CashVolume := Self.CashVolume;
    try
      TROSerializer(ASerializer).ReadDouble('CashVolume', TypInfo.ftDouble, l_CashVolume);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CashVolume']));
    end;
    Self.CashVolume := l_CashVolume;
    l_CreditMoney := Self.CreditMoney;
    try
      TROSerializer(ASerializer).ReadDouble('CreditMoney', TypInfo.ftDouble, l_CreditMoney);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CreditMoney']));
    end;
    Self.CreditMoney := l_CreditMoney;
    l_CreditVolume := Self.CreditVolume;
    try
      TROSerializer(ASerializer).ReadDouble('CreditVolume', TypInfo.ftDouble, l_CreditVolume);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CreditVolume']));
    end;
    Self.CreditVolume := l_CreditVolume;
    l_LecturaCorrecta := Self.LecturaCorrecta;
    try
      TROSerializer(ASerializer).ReadEnumerated('LecturaCorrecta',System.TypeInfo(boolean), l_LecturaCorrecta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'LecturaCorrecta']));
    end;
    Self.LecturaCorrecta := l_LecturaCorrecta;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end;
end;

procedure TLecturas.WriteComplex(ASerializer: TObject);
var
  l_CashMoney: Double;
  l_CashVolume: Double;
  l_CreditMoney: Double;
  l_CreditVolume: Double;
  l_LecturaCorrecta: Boolean;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLecturas);
    l_LecturaCorrecta := Self.LecturaCorrecta;
    TROSerializer(ASerializer).WriteEnumerated('LecturaCorrecta',System.TypeInfo(boolean), l_LecturaCorrecta);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_CashMoney := Self.CashMoney;
    TROSerializer(ASerializer).WriteDouble('CashMoney', TypInfo.ftDouble, l_CashMoney);
    l_CashVolume := Self.CashVolume;
    TROSerializer(ASerializer).WriteDouble('CashVolume', TypInfo.ftDouble, l_CashVolume);
    l_CreditMoney := Self.CreditMoney;
    TROSerializer(ASerializer).WriteDouble('CreditMoney', TypInfo.ftDouble, l_CreditMoney);
    l_CreditVolume := Self.CreditVolume;
    TROSerializer(ASerializer).WriteDouble('CreditVolume', TypInfo.ftDouble, l_CreditVolume);
  end
  else begin
    l_CashMoney := Self.CashMoney;
    TROSerializer(ASerializer).WriteDouble('CashMoney', TypInfo.ftDouble, l_CashMoney);
    l_CashVolume := Self.CashVolume;
    TROSerializer(ASerializer).WriteDouble('CashVolume', TypInfo.ftDouble, l_CashVolume);
    l_CreditMoney := Self.CreditMoney;
    TROSerializer(ASerializer).WriteDouble('CreditMoney', TypInfo.ftDouble, l_CreditMoney);
    l_CreditVolume := Self.CreditVolume;
    TROSerializer(ASerializer).WriteDouble('CreditVolume', TypInfo.ftDouble, l_CreditVolume);
    l_LecturaCorrecta := Self.LecturaCorrecta;
    TROSerializer(ASerializer).WriteEnumerated('LecturaCorrecta',System.TypeInfo(boolean), l_LecturaCorrecta);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end;
end;

{ TLecturasCollection }
constructor TLecturasCollection.Create;
begin
  inherited Create(TLecturas);
end;

constructor TLecturasCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLecturasCollection.Add: TLecturas;
begin
  Result := TLecturas(inherited Add);
end;

function TLecturasCollection.GetItems(aIndex: Integer): TLecturas;
begin
  Result := TLecturas(inherited Items[aIndex]);
end;

procedure TLecturasCollection.LoadFromArray(anArray: aLecturas);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TLecturasCollection.SaveToArray(anArray: aLecturas);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TLecturas(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TLecturasCollection.SetItems(aIndex: Integer; const Value: TLecturas);
begin
  TLecturas(inherited Items[aIndex]).Assign(Value);
end;

{ TManguera }

procedure TManguera.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TManguera;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TManguera) then begin
    lSource := LibraryValesCupones_Intf.TManguera(iSource);

    Self.NoGasolina := lSource.NoGasolina;
  end;
end;

procedure TManguera.ReadComplex(ASerializer: TObject);
var
  l_NoGasolina: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_NoGasolina := Self.NoGasolina;
    try
      TROSerializer(ASerializer).ReadInteger('NoGasolina', TypInfo.otSLong, l_NoGasolina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoGasolina']));
    end;
    Self.NoGasolina := l_NoGasolina;
  end
  else begin
    l_NoGasolina := Self.NoGasolina;
    try
      TROSerializer(ASerializer).ReadInteger('NoGasolina', TypInfo.otSLong, l_NoGasolina);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoGasolina']));
    end;
    Self.NoGasolina := l_NoGasolina;
  end;
end;

procedure TManguera.WriteComplex(ASerializer: TObject);
var
  l_NoGasolina: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TManguera);
    l_NoGasolina := Self.NoGasolina;
    TROSerializer(ASerializer).WriteInteger('NoGasolina', TypInfo.otSLong, l_NoGasolina);
  end
  else begin
    l_NoGasolina := Self.NoGasolina;
    TROSerializer(ASerializer).WriteInteger('NoGasolina', TypInfo.otSLong, l_NoGasolina);
  end;
end;

{ TMangueraCollection }
constructor TMangueraCollection.Create;
begin
  inherited Create(TManguera);
end;

constructor TMangueraCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TMangueraCollection.Add: TManguera;
begin
  Result := TManguera(inherited Add);
end;

function TMangueraCollection.GetItems(aIndex: Integer): TManguera;
begin
  Result := TManguera(inherited Items[aIndex]);
end;

procedure TMangueraCollection.LoadFromArray(anArray: aMaguera);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TMangueraCollection.SaveToArray(anArray: aMaguera);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TManguera(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TMangueraCollection.SetItems(aIndex: Integer; const Value: TManguera);
begin
  TManguera(inherited Items[aIndex]).Assign(Value);
end;

{ TCanal }

procedure TCanal.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCanal;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCanal) then begin
    lSource := LibraryValesCupones_Intf.TCanal(iSource);

    Self.Pendiente := lSource.Pendiente;
    Self.Litros := lSource.Litros;
    Self.Importe := lSource.Importe;
    Self.Precio := lSource.Precio;
    Self.Canal := lSource.Canal;
    Self.CanalAnterior := lSource.CanalAnterior;
    Self.Direccion := lSource.Direccion;
  end;
end;

procedure TCanal.ReadComplex(ASerializer: TObject);
var
  l_Canal: Integer;
  l_CanalAnterior: Integer;
  l_Direccion: Integer;
  l_Importe: Double;
  l_Litros: Double;
  l_Pendiente: eEstadoVenta;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_Litros := Self.Litros;
    try
      TROSerializer(ASerializer).ReadDouble('Litros', TypInfo.ftDouble, l_Litros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Litros']));
    end;
    Self.Litros := l_Litros;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_Canal := Self.Canal;
    try
      TROSerializer(ASerializer).ReadInteger('Canal', TypInfo.otSLong, l_Canal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Canal']));
    end;
    Self.Canal := l_Canal;
    l_CanalAnterior := Self.CanalAnterior;
    try
      TROSerializer(ASerializer).ReadInteger('CanalAnterior', TypInfo.otSLong, l_CanalAnterior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CanalAnterior']));
    end;
    Self.CanalAnterior := l_CanalAnterior;
    l_Direccion := Self.Direccion;
    try
      TROSerializer(ASerializer).ReadInteger('Direccion', TypInfo.otSLong, l_Direccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Direccion']));
    end;
    Self.Direccion := l_Direccion;
  end
  else begin
    l_Canal := Self.Canal;
    try
      TROSerializer(ASerializer).ReadInteger('Canal', TypInfo.otSLong, l_Canal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Canal']));
    end;
    Self.Canal := l_Canal;
    l_CanalAnterior := Self.CanalAnterior;
    try
      TROSerializer(ASerializer).ReadInteger('CanalAnterior', TypInfo.otSLong, l_CanalAnterior);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'CanalAnterior']));
    end;
    Self.CanalAnterior := l_CanalAnterior;
    l_Direccion := Self.Direccion;
    try
      TROSerializer(ASerializer).ReadInteger('Direccion', TypInfo.otSLong, l_Direccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Direccion']));
    end;
    Self.Direccion := l_Direccion;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Litros := Self.Litros;
    try
      TROSerializer(ASerializer).ReadDouble('Litros', TypInfo.ftDouble, l_Litros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Litros']));
    end;
    Self.Litros := l_Litros;
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end;
end;

procedure TCanal.WriteComplex(ASerializer: TObject);
var
  l_Canal: Integer;
  l_CanalAnterior: Integer;
  l_Direccion: Integer;
  l_Importe: Double;
  l_Litros: Double;
  l_Pendiente: eEstadoVenta;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCanal);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    l_Litros := Self.Litros;
    TROSerializer(ASerializer).WriteDouble('Litros', TypInfo.ftDouble, l_Litros);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_Canal := Self.Canal;
    TROSerializer(ASerializer).WriteInteger('Canal', TypInfo.otSLong, l_Canal);
    l_CanalAnterior := Self.CanalAnterior;
    TROSerializer(ASerializer).WriteInteger('CanalAnterior', TypInfo.otSLong, l_CanalAnterior);
    l_Direccion := Self.Direccion;
    TROSerializer(ASerializer).WriteInteger('Direccion', TypInfo.otSLong, l_Direccion);
  end
  else begin
    l_Canal := Self.Canal;
    TROSerializer(ASerializer).WriteInteger('Canal', TypInfo.otSLong, l_Canal);
    l_CanalAnterior := Self.CanalAnterior;
    TROSerializer(ASerializer).WriteInteger('CanalAnterior', TypInfo.otSLong, l_CanalAnterior);
    l_Direccion := Self.Direccion;
    TROSerializer(ASerializer).WriteInteger('Direccion', TypInfo.otSLong, l_Direccion);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Litros := Self.Litros;
    TROSerializer(ASerializer).WriteDouble('Litros', TypInfo.ftDouble, l_Litros);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end;
end;

{ TCanalCollection }
constructor TCanalCollection.Create;
begin
  inherited Create(TCanal);
end;

constructor TCanalCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCanalCollection.Add: TCanal;
begin
  Result := TCanal(inherited Add);
end;

function TCanalCollection.GetItems(aIndex: Integer): TCanal;
begin
  Result := TCanal(inherited Items[aIndex]);
end;

procedure TCanalCollection.SetItems(aIndex: Integer; const Value: TCanal);
begin
  TCanal(inherited Items[aIndex]).Assign(Value);
end;

{ TContador }

procedure TContador.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TContador;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TContador) then begin
    lSource := LibraryValesCupones_Intf.TContador(iSource);

    Self.Pendiente := lSource.Pendiente;
    Self.Litros := lSource.Litros;
    Self.Importe := lSource.Importe;
    Self.Precio := lSource.Precio;
  end;
end;

procedure TContador.ReadComplex(ASerializer: TObject);
var
  l_Importe: Double;
  l_Litros: Double;
  l_Pendiente: eEstadoVenta;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_Litros := Self.Litros;
    try
      TROSerializer(ASerializer).ReadDouble('Litros', TypInfo.ftDouble, l_Litros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Litros']));
    end;
    Self.Litros := l_Litros;
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end
  else begin
    l_Importe := Self.Importe;
    try
      TROSerializer(ASerializer).ReadDouble('Importe', TypInfo.ftDouble, l_Importe);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Importe']));
    end;
    Self.Importe := l_Importe;
    l_Litros := Self.Litros;
    try
      TROSerializer(ASerializer).ReadDouble('Litros', TypInfo.ftDouble, l_Litros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Litros']));
    end;
    Self.Litros := l_Litros;
    l_Pendiente := Self.Pendiente;
    try
      TROSerializer(ASerializer).ReadEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Pendiente']));
    end;
    Self.Pendiente := l_Pendiente;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
  end;
end;

procedure TContador.WriteComplex(ASerializer: TObject);
var
  l_Importe: Double;
  l_Litros: Double;
  l_Pendiente: eEstadoVenta;
  l_Precio: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TContador);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    l_Litros := Self.Litros;
    TROSerializer(ASerializer).WriteDouble('Litros', TypInfo.ftDouble, l_Litros);
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end
  else begin
    l_Importe := Self.Importe;
    TROSerializer(ASerializer).WriteDouble('Importe', TypInfo.ftDouble, l_Importe);
    l_Litros := Self.Litros;
    TROSerializer(ASerializer).WriteDouble('Litros', TypInfo.ftDouble, l_Litros);
    l_Pendiente := Self.Pendiente;
    TROSerializer(ASerializer).WriteEnumerated('Pendiente',System.TypeInfo(eEstadoVenta), l_Pendiente);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
  end;
end;

{ TContadorCollection }
constructor TContadorCollection.Create;
begin
  inherited Create(TContador);
end;

constructor TContadorCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TContadorCollection.Add: TContador;
begin
  Result := TContador(inherited Add);
end;

function TContadorCollection.GetItems(aIndex: Integer): TContador;
begin
  Result := TContador(inherited Items[aIndex]);
end;

procedure TContadorCollection.LoadFromArray(anArray: aContador);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TContadorCollection.SaveToArray(anArray: aContador);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TContador(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TContadorCollection.SetItems(aIndex: Integer; const Value: TContador);
begin
  TContador(inherited Items[aIndex]).Assign(Value);
end;

{ TRegistroPuntoVenta }

procedure TRegistroPuntoVenta.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TRegistroPuntoVenta;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TRegistroPuntoVenta) then begin
    lSource := LibraryValesCupones_Intf.TRegistroPuntoVenta(iSource);

    Self.NoTerminal := lSource.NoTerminal;
    Self.TipoTrama := lSource.TipoTrama;
    Self.TipoTarjeta := lSource.TipoTarjeta;
    Self.Tarjeta := lSource.Tarjeta;
    Self.Unidad := lSource.Unidad;
    Self.NIP := lSource.NIP;
    Self.Kms := lSource.Kms;
    Self.Comision := lSource.Comision;
    Self.VentaAutorizada := lSource.VentaAutorizada;
    Self.Saldo := lSource.Saldo;
    Self.Mensaje := lSource.Mensaje;
  end;
end;

procedure TRegistroPuntoVenta.ReadComplex(ASerializer: TObject);
var
  l_Comision: AnsiString;
  l_Kms: Integer;
  l_Mensaje: AnsiString;
  l_NIP: AnsiString;
  l_NoTerminal: AnsiString;
  l_Saldo: Double;
  l_Tarjeta: AnsiString;
  l_TipoTarjeta: AnsiString;
  l_TipoTrama: AnsiString;
  l_Unidad: AnsiString;
  l_VentaAutorizada: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_NoTerminal := Self.NoTerminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoTerminal', l_NoTerminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoTerminal']));
    end;
    Self.NoTerminal := l_NoTerminal;
    l_TipoTrama := Self.TipoTrama;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTrama', l_TipoTrama);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTrama']));
    end;
    Self.TipoTrama := l_TipoTrama;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTarjeta', l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_Unidad := Self.Unidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Unidad', l_Unidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Unidad']));
    end;
    Self.Unidad := l_Unidad;
    l_NIP := Self.NIP;
    try
      TROSerializer(ASerializer).ReadAnsiString('NIP', l_NIP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NIP']));
    end;
    Self.NIP := l_NIP;
    l_Kms := Self.Kms;
    try
      TROSerializer(ASerializer).ReadInteger('Kms', TypInfo.otSLong, l_Kms);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kms']));
    end;
    Self.Kms := l_Kms;
    l_Comision := Self.Comision;
    try
      TROSerializer(ASerializer).ReadAnsiString('Comision', l_Comision);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Comision']));
    end;
    Self.Comision := l_Comision;
    l_VentaAutorizada := Self.VentaAutorizada;
    try
      TROSerializer(ASerializer).ReadDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAutorizada']));
    end;
    Self.VentaAutorizada := l_VentaAutorizada;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
  end
  else begin
    l_Comision := Self.Comision;
    try
      TROSerializer(ASerializer).ReadAnsiString('Comision', l_Comision);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Comision']));
    end;
    Self.Comision := l_Comision;
    l_Kms := Self.Kms;
    try
      TROSerializer(ASerializer).ReadInteger('Kms', TypInfo.otSLong, l_Kms);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Kms']));
    end;
    Self.Kms := l_Kms;
    l_Mensaje := Self.Mensaje;
    try
      TROSerializer(ASerializer).ReadAnsiString('Mensaje', l_Mensaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Mensaje']));
    end;
    Self.Mensaje := l_Mensaje;
    l_NIP := Self.NIP;
    try
      TROSerializer(ASerializer).ReadAnsiString('NIP', l_NIP);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NIP']));
    end;
    Self.NIP := l_NIP;
    l_NoTerminal := Self.NoTerminal;
    try
      TROSerializer(ASerializer).ReadAnsiString('NoTerminal', l_NoTerminal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NoTerminal']));
    end;
    Self.NoTerminal := l_NoTerminal;
    l_Saldo := Self.Saldo;
    try
      TROSerializer(ASerializer).ReadDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Saldo']));
    end;
    Self.Saldo := l_Saldo;
    l_Tarjeta := Self.Tarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('Tarjeta', l_Tarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tarjeta']));
    end;
    Self.Tarjeta := l_Tarjeta;
    l_TipoTarjeta := Self.TipoTarjeta;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTarjeta', l_TipoTarjeta);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTarjeta']));
    end;
    Self.TipoTarjeta := l_TipoTarjeta;
    l_TipoTrama := Self.TipoTrama;
    try
      TROSerializer(ASerializer).ReadAnsiString('TipoTrama', l_TipoTrama);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TipoTrama']));
    end;
    Self.TipoTrama := l_TipoTrama;
    l_Unidad := Self.Unidad;
    try
      TROSerializer(ASerializer).ReadAnsiString('Unidad', l_Unidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Unidad']));
    end;
    Self.Unidad := l_Unidad;
    l_VentaAutorizada := Self.VentaAutorizada;
    try
      TROSerializer(ASerializer).ReadDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VentaAutorizada']));
    end;
    Self.VentaAutorizada := l_VentaAutorizada;
  end;
end;

procedure TRegistroPuntoVenta.WriteComplex(ASerializer: TObject);
var
  l_Comision: AnsiString;
  l_Kms: Integer;
  l_Mensaje: AnsiString;
  l_NIP: AnsiString;
  l_NoTerminal: AnsiString;
  l_Saldo: Double;
  l_Tarjeta: AnsiString;
  l_TipoTarjeta: AnsiString;
  l_TipoTrama: AnsiString;
  l_Unidad: AnsiString;
  l_VentaAutorizada: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TRegistroPuntoVenta);
    l_NoTerminal := Self.NoTerminal;
    TROSerializer(ASerializer).WriteAnsiString('NoTerminal', l_NoTerminal);
    l_TipoTrama := Self.TipoTrama;
    TROSerializer(ASerializer).WriteAnsiString('TipoTrama', l_TipoTrama);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteAnsiString('TipoTarjeta', l_TipoTarjeta);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_Unidad := Self.Unidad;
    TROSerializer(ASerializer).WriteAnsiString('Unidad', l_Unidad);
    l_NIP := Self.NIP;
    TROSerializer(ASerializer).WriteAnsiString('NIP', l_NIP);
    l_Kms := Self.Kms;
    TROSerializer(ASerializer).WriteInteger('Kms', TypInfo.otSLong, l_Kms);
    l_Comision := Self.Comision;
    TROSerializer(ASerializer).WriteAnsiString('Comision', l_Comision);
    l_VentaAutorizada := Self.VentaAutorizada;
    TROSerializer(ASerializer).WriteDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
  end
  else begin
    l_Comision := Self.Comision;
    TROSerializer(ASerializer).WriteAnsiString('Comision', l_Comision);
    l_Kms := Self.Kms;
    TROSerializer(ASerializer).WriteInteger('Kms', TypInfo.otSLong, l_Kms);
    l_Mensaje := Self.Mensaje;
    TROSerializer(ASerializer).WriteAnsiString('Mensaje', l_Mensaje);
    l_NIP := Self.NIP;
    TROSerializer(ASerializer).WriteAnsiString('NIP', l_NIP);
    l_NoTerminal := Self.NoTerminal;
    TROSerializer(ASerializer).WriteAnsiString('NoTerminal', l_NoTerminal);
    l_Saldo := Self.Saldo;
    TROSerializer(ASerializer).WriteDouble('Saldo', TypInfo.ftDouble, l_Saldo);
    l_Tarjeta := Self.Tarjeta;
    TROSerializer(ASerializer).WriteAnsiString('Tarjeta', l_Tarjeta);
    l_TipoTarjeta := Self.TipoTarjeta;
    TROSerializer(ASerializer).WriteAnsiString('TipoTarjeta', l_TipoTarjeta);
    l_TipoTrama := Self.TipoTrama;
    TROSerializer(ASerializer).WriteAnsiString('TipoTrama', l_TipoTrama);
    l_Unidad := Self.Unidad;
    TROSerializer(ASerializer).WriteAnsiString('Unidad', l_Unidad);
    l_VentaAutorizada := Self.VentaAutorizada;
    TROSerializer(ASerializer).WriteDouble('VentaAutorizada', TypInfo.ftDouble, l_VentaAutorizada);
  end;
end;

{ TRegistroPuntoVentaCollection }
constructor TRegistroPuntoVentaCollection.Create;
begin
  inherited Create(TRegistroPuntoVenta);
end;

constructor TRegistroPuntoVentaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TRegistroPuntoVentaCollection.Add: TRegistroPuntoVenta;
begin
  Result := TRegistroPuntoVenta(inherited Add);
end;

function TRegistroPuntoVentaCollection.GetItems(aIndex: Integer): TRegistroPuntoVenta;
begin
  Result := TRegistroPuntoVenta(inherited Items[aIndex]);
end;

procedure TRegistroPuntoVentaCollection.SetItems(aIndex: Integer; const Value: TRegistroPuntoVenta);
begin
  TRegistroPuntoVenta(inherited Items[aIndex]).Assign(Value);
end;

{ TCambio }

procedure TCambio.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCambio;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCambio) then begin
    lSource := LibraryValesCupones_Intf.TCambio(iSource);

    Self.Fecha := lSource.Fecha;
    Self.Activo := lSource.Activo;
    if System.Assigned(lSource.fPrecios) then
      Self.Precios.Assign(lSource.fPrecios)
    else
      SysUtils.FreeAndNil(fPrecios);
  end;
end;

function TCambio.GetPrecios: aCambioPrecios;
begin
  if (fPrecios = nil) then fPrecios := aCambioPrecios.Create();
  Result := fPrecios;
end;

procedure TCambio.ReadComplex(ASerializer: TObject);
var
  l_Activo: Boolean;
  l_Fecha: DateTime;
  l_Precios: aCambioPrecios;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Activo := Self.Activo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Activo']));
    end;
    Self.Activo := l_Activo;
    l_Precios := int_Precios;
    try
      TROSerializer(ASerializer).ReadArray('Precios', aCambioPrecios, l_Precios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precios']));
    end;
    if int_Precios <> l_Precios then Self.Precios.Free;
    Self.Precios := l_Precios;
  end
  else begin
    l_Activo := Self.Activo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Activo']));
    end;
    Self.Activo := l_Activo;
    l_Fecha := Self.Fecha;
    try
      TROSerializer(ASerializer).ReadDateTime('Fecha', l_Fecha);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fecha']));
    end;
    Self.Fecha := l_Fecha;
    l_Precios := int_Precios;
    try
      TROSerializer(ASerializer).ReadArray('Precios', aCambioPrecios, l_Precios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precios']));
    end;
    if int_Precios <> l_Precios then Precios.Free;
    Self.Precios := l_Precios;
  end;
end;

procedure TCambio.WriteComplex(ASerializer: TObject);
var
  l_Activo: Boolean;
  l_Fecha: DateTime;
  l_Precios: aCambioPrecios;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCambio);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Activo := Self.Activo;
    TROSerializer(ASerializer).WriteEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    l_Precios := int_Precios;
    TROSerializer(ASerializer).WriteArray('Precios', l_Precios, aCambioPrecios);
  end
  else begin
    l_Activo := Self.Activo;
    TROSerializer(ASerializer).WriteEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    l_Fecha := Self.Fecha;
    TROSerializer(ASerializer).WriteDateTime('Fecha', l_Fecha);
    l_Precios := int_Precios;
    TROSerializer(ASerializer).WriteArray('Precios', l_Precios, aCambioPrecios);
  end;
end;

{ TCambioCollection }
constructor TCambioCollection.Create;
begin
  inherited Create(TCambio);
end;

constructor TCambioCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCambioCollection.Add: TCambio;
begin
  Result := TCambio(inherited Add);
end;

function TCambioCollection.GetItems(aIndex: Integer): TCambio;
begin
  Result := TCambio(inherited Items[aIndex]);
end;

procedure TCambioCollection.SetItems(aIndex: Integer; const Value: TCambio);
begin
  TCambio(inherited Items[aIndex]).Assign(Value);
end;

{ TCambioPrecios }

procedure TCambioPrecios.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TCambioPrecios;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TCambioPrecios) then begin
    lSource := LibraryValesCupones_Intf.TCambioPrecios(iSource);

    Self.ProductoID := lSource.ProductoID;
    Self.Precio := lSource.Precio;
    Self.IEPS := lSource.IEPS;
  end;
end;

procedure TCambioPrecios.ReadComplex(ASerializer: TObject);
var
  l_IEPS: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
  end
  else begin
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
  end;
end;

procedure TCambioPrecios.WriteComplex(ASerializer: TObject);
var
  l_IEPS: Double;
  l_Precio: Double;
  l_ProductoID: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TCambioPrecios);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
  end
  else begin
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
  end;
end;

{ TCambioPreciosCollection }
constructor TCambioPreciosCollection.Create;
begin
  inherited Create(TCambioPrecios);
end;

constructor TCambioPreciosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TCambioPreciosCollection.Add: TCambioPrecios;
begin
  Result := TCambioPrecios(inherited Add);
end;

function TCambioPreciosCollection.GetItems(aIndex: Integer): TCambioPrecios;
begin
  Result := TCambioPrecios(inherited Items[aIndex]);
end;

procedure TCambioPreciosCollection.LoadFromArray(anArray: aCambioPrecios);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TCambioPreciosCollection.SaveToArray(anArray: aCambioPrecios);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TCambioPrecios(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TCambioPreciosCollection.SetItems(aIndex: Integer; const Value: TCambioPrecios);
begin
  TCambioPrecios(inherited Items[aIndex]).Assign(Value);
end;

{ TDispositivos }

procedure TDispositivos.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TDispositivos;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TDispositivos) then begin
    lSource := LibraryValesCupones_Intf.TDispositivos(iSource);

    Self.Activo := lSource.Activo;
    Self.Nombre := lSource.Nombre;
    Self.Puerto := lSource.Puerto;
    Self.Speed := lSource.Speed;
    Self.DataBits := lSource.DataBits;
    Self.Parity := lSource.Parity;
    Self.StopBits := lSource.StopBits;
    Self.Echo := lSource.Echo;
    Self.Flow := lSource.Flow;
    Self.HandShake := lSource.HandShake;
    Self.DispLector := lSource.DispLector;
    Self.DispComunicaciones := lSource.DispComunicaciones;
    Self.RetardoLectura := lSource.RetardoLectura;
    Self.RetardoCiclo := lSource.RetardoCiclo;
  end;
end;

procedure TDispositivos.ReadComplex(ASerializer: TObject);
var
  l_Activo: Boolean;
  l_DataBits: Integer;
  l_DispComunicaciones: Integer;
  l_DispLector: Integer;
  l_Echo: Integer;
  l_Flow: AnsiString;
  l_HandShake: Integer;
  l_Nombre: AnsiString;
  l_Parity: Integer;
  l_Puerto: Integer;
  l_RetardoCiclo: Integer;
  l_RetardoLectura: Integer;
  l_Speed: Integer;
  l_StopBits: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Activo := Self.Activo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Activo']));
    end;
    Self.Activo := l_Activo;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Puerto := Self.Puerto;
    try
      TROSerializer(ASerializer).ReadInteger('Puerto', TypInfo.otSLong, l_Puerto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puerto']));
    end;
    Self.Puerto := l_Puerto;
    l_Speed := Self.Speed;
    try
      TROSerializer(ASerializer).ReadInteger('Speed', TypInfo.otSLong, l_Speed);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Speed']));
    end;
    Self.Speed := l_Speed;
    l_DataBits := Self.DataBits;
    try
      TROSerializer(ASerializer).ReadInteger('DataBits', TypInfo.otSLong, l_DataBits);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DataBits']));
    end;
    Self.DataBits := l_DataBits;
    l_Parity := Self.Parity;
    try
      TROSerializer(ASerializer).ReadInteger('Parity', TypInfo.otSLong, l_Parity);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Parity']));
    end;
    Self.Parity := l_Parity;
    l_StopBits := Self.StopBits;
    try
      TROSerializer(ASerializer).ReadInteger('StopBits', TypInfo.otSLong, l_StopBits);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StopBits']));
    end;
    Self.StopBits := l_StopBits;
    l_Echo := Self.Echo;
    try
      TROSerializer(ASerializer).ReadInteger('Echo', TypInfo.otSLong, l_Echo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Echo']));
    end;
    Self.Echo := l_Echo;
    l_Flow := Self.Flow;
    try
      TROSerializer(ASerializer).ReadAnsiString('Flow', l_Flow);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flow']));
    end;
    Self.Flow := l_Flow;
    l_HandShake := Self.HandShake;
    try
      TROSerializer(ASerializer).ReadInteger('HandShake', TypInfo.otSLong, l_HandShake);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'HandShake']));
    end;
    Self.HandShake := l_HandShake;
    l_DispLector := Self.DispLector;
    try
      TROSerializer(ASerializer).ReadInteger('DispLector', TypInfo.otSLong, l_DispLector);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DispLector']));
    end;
    Self.DispLector := l_DispLector;
    l_DispComunicaciones := Self.DispComunicaciones;
    try
      TROSerializer(ASerializer).ReadInteger('DispComunicaciones', TypInfo.otSLong, l_DispComunicaciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DispComunicaciones']));
    end;
    Self.DispComunicaciones := l_DispComunicaciones;
    l_RetardoLectura := Self.RetardoLectura;
    try
      TROSerializer(ASerializer).ReadInteger('RetardoLectura', TypInfo.otSLong, l_RetardoLectura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RetardoLectura']));
    end;
    Self.RetardoLectura := l_RetardoLectura;
    l_RetardoCiclo := Self.RetardoCiclo;
    try
      TROSerializer(ASerializer).ReadInteger('RetardoCiclo', TypInfo.otSLong, l_RetardoCiclo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RetardoCiclo']));
    end;
    Self.RetardoCiclo := l_RetardoCiclo;
  end
  else begin
    l_Activo := Self.Activo;
    try
      TROSerializer(ASerializer).ReadEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Activo']));
    end;
    Self.Activo := l_Activo;
    l_DataBits := Self.DataBits;
    try
      TROSerializer(ASerializer).ReadInteger('DataBits', TypInfo.otSLong, l_DataBits);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DataBits']));
    end;
    Self.DataBits := l_DataBits;
    l_DispComunicaciones := Self.DispComunicaciones;
    try
      TROSerializer(ASerializer).ReadInteger('DispComunicaciones', TypInfo.otSLong, l_DispComunicaciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DispComunicaciones']));
    end;
    Self.DispComunicaciones := l_DispComunicaciones;
    l_DispLector := Self.DispLector;
    try
      TROSerializer(ASerializer).ReadInteger('DispLector', TypInfo.otSLong, l_DispLector);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'DispLector']));
    end;
    Self.DispLector := l_DispLector;
    l_Echo := Self.Echo;
    try
      TROSerializer(ASerializer).ReadInteger('Echo', TypInfo.otSLong, l_Echo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Echo']));
    end;
    Self.Echo := l_Echo;
    l_Flow := Self.Flow;
    try
      TROSerializer(ASerializer).ReadAnsiString('Flow', l_Flow);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flow']));
    end;
    Self.Flow := l_Flow;
    l_HandShake := Self.HandShake;
    try
      TROSerializer(ASerializer).ReadInteger('HandShake', TypInfo.otSLong, l_HandShake);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'HandShake']));
    end;
    Self.HandShake := l_HandShake;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Parity := Self.Parity;
    try
      TROSerializer(ASerializer).ReadInteger('Parity', TypInfo.otSLong, l_Parity);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Parity']));
    end;
    Self.Parity := l_Parity;
    l_Puerto := Self.Puerto;
    try
      TROSerializer(ASerializer).ReadInteger('Puerto', TypInfo.otSLong, l_Puerto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Puerto']));
    end;
    Self.Puerto := l_Puerto;
    l_RetardoCiclo := Self.RetardoCiclo;
    try
      TROSerializer(ASerializer).ReadInteger('RetardoCiclo', TypInfo.otSLong, l_RetardoCiclo);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RetardoCiclo']));
    end;
    Self.RetardoCiclo := l_RetardoCiclo;
    l_RetardoLectura := Self.RetardoLectura;
    try
      TROSerializer(ASerializer).ReadInteger('RetardoLectura', TypInfo.otSLong, l_RetardoLectura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'RetardoLectura']));
    end;
    Self.RetardoLectura := l_RetardoLectura;
    l_Speed := Self.Speed;
    try
      TROSerializer(ASerializer).ReadInteger('Speed', TypInfo.otSLong, l_Speed);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Speed']));
    end;
    Self.Speed := l_Speed;
    l_StopBits := Self.StopBits;
    try
      TROSerializer(ASerializer).ReadInteger('StopBits', TypInfo.otSLong, l_StopBits);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StopBits']));
    end;
    Self.StopBits := l_StopBits;
  end;
end;

procedure TDispositivos.WriteComplex(ASerializer: TObject);
var
  l_Activo: Boolean;
  l_DataBits: Integer;
  l_DispComunicaciones: Integer;
  l_DispLector: Integer;
  l_Echo: Integer;
  l_Flow: AnsiString;
  l_HandShake: Integer;
  l_Nombre: AnsiString;
  l_Parity: Integer;
  l_Puerto: Integer;
  l_RetardoCiclo: Integer;
  l_RetardoLectura: Integer;
  l_Speed: Integer;
  l_StopBits: Integer;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TDispositivos);
    l_Activo := Self.Activo;
    TROSerializer(ASerializer).WriteEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Puerto := Self.Puerto;
    TROSerializer(ASerializer).WriteInteger('Puerto', TypInfo.otSLong, l_Puerto);
    l_Speed := Self.Speed;
    TROSerializer(ASerializer).WriteInteger('Speed', TypInfo.otSLong, l_Speed);
    l_DataBits := Self.DataBits;
    TROSerializer(ASerializer).WriteInteger('DataBits', TypInfo.otSLong, l_DataBits);
    l_Parity := Self.Parity;
    TROSerializer(ASerializer).WriteInteger('Parity', TypInfo.otSLong, l_Parity);
    l_StopBits := Self.StopBits;
    TROSerializer(ASerializer).WriteInteger('StopBits', TypInfo.otSLong, l_StopBits);
    l_Echo := Self.Echo;
    TROSerializer(ASerializer).WriteInteger('Echo', TypInfo.otSLong, l_Echo);
    l_Flow := Self.Flow;
    TROSerializer(ASerializer).WriteAnsiString('Flow', l_Flow);
    l_HandShake := Self.HandShake;
    TROSerializer(ASerializer).WriteInteger('HandShake', TypInfo.otSLong, l_HandShake);
    l_DispLector := Self.DispLector;
    TROSerializer(ASerializer).WriteInteger('DispLector', TypInfo.otSLong, l_DispLector);
    l_DispComunicaciones := Self.DispComunicaciones;
    TROSerializer(ASerializer).WriteInteger('DispComunicaciones', TypInfo.otSLong, l_DispComunicaciones);
    l_RetardoLectura := Self.RetardoLectura;
    TROSerializer(ASerializer).WriteInteger('RetardoLectura', TypInfo.otSLong, l_RetardoLectura);
    l_RetardoCiclo := Self.RetardoCiclo;
    TROSerializer(ASerializer).WriteInteger('RetardoCiclo', TypInfo.otSLong, l_RetardoCiclo);
  end
  else begin
    l_Activo := Self.Activo;
    TROSerializer(ASerializer).WriteEnumerated('Activo',System.TypeInfo(boolean), l_Activo);
    l_DataBits := Self.DataBits;
    TROSerializer(ASerializer).WriteInteger('DataBits', TypInfo.otSLong, l_DataBits);
    l_DispComunicaciones := Self.DispComunicaciones;
    TROSerializer(ASerializer).WriteInteger('DispComunicaciones', TypInfo.otSLong, l_DispComunicaciones);
    l_DispLector := Self.DispLector;
    TROSerializer(ASerializer).WriteInteger('DispLector', TypInfo.otSLong, l_DispLector);
    l_Echo := Self.Echo;
    TROSerializer(ASerializer).WriteInteger('Echo', TypInfo.otSLong, l_Echo);
    l_Flow := Self.Flow;
    TROSerializer(ASerializer).WriteAnsiString('Flow', l_Flow);
    l_HandShake := Self.HandShake;
    TROSerializer(ASerializer).WriteInteger('HandShake', TypInfo.otSLong, l_HandShake);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Parity := Self.Parity;
    TROSerializer(ASerializer).WriteInteger('Parity', TypInfo.otSLong, l_Parity);
    l_Puerto := Self.Puerto;
    TROSerializer(ASerializer).WriteInteger('Puerto', TypInfo.otSLong, l_Puerto);
    l_RetardoCiclo := Self.RetardoCiclo;
    TROSerializer(ASerializer).WriteInteger('RetardoCiclo', TypInfo.otSLong, l_RetardoCiclo);
    l_RetardoLectura := Self.RetardoLectura;
    TROSerializer(ASerializer).WriteInteger('RetardoLectura', TypInfo.otSLong, l_RetardoLectura);
    l_Speed := Self.Speed;
    TROSerializer(ASerializer).WriteInteger('Speed', TypInfo.otSLong, l_Speed);
    l_StopBits := Self.StopBits;
    TROSerializer(ASerializer).WriteInteger('StopBits', TypInfo.otSLong, l_StopBits);
  end;
end;

{ TDispositivosCollection }
constructor TDispositivosCollection.Create;
begin
  inherited Create(TDispositivos);
end;

constructor TDispositivosCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TDispositivosCollection.Add: TDispositivos;
begin
  Result := TDispositivos(inherited Add);
end;

function TDispositivosCollection.GetItems(aIndex: Integer): TDispositivos;
begin
  Result := TDispositivos(inherited Items[aIndex]);
end;

procedure TDispositivosCollection.LoadFromArray(anArray: aDispositivos);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TDispositivosCollection.SaveToArray(anArray: aDispositivos);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TDispositivos(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TDispositivosCollection.SetItems(aIndex: Integer; const Value: TDispositivos);
begin
  TDispositivos(inherited Items[aIndex]).Assign(Value);
end;

{ TGas }

procedure TGas.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TGas;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TGas) then begin
    lSource := LibraryValesCupones_Intf.TGas(iSource);

    Self.Aplica := lSource.Aplica;
    Self.ClavePemex := lSource.ClavePemex;
    Self.FactorDecimalPrecios := lSource.FactorDecimalPrecios;
    Self.IEPS := lSource.IEPS;
    Self.IEPSProgramado := lSource.IEPSProgramado;
    Self.Impuesto := lSource.Impuesto;
    Self.Nombre := lSource.Nombre;
    Self.Precio := lSource.Precio;
    Self.PrecioProgramado := lSource.PrecioProgramado;
  end;
end;

procedure TGas.ReadComplex(ASerializer: TObject);
var
  l_Aplica: Boolean;
  l_ClavePemex: AnsiString;
  l_FactorDecimalPrecios: Integer;
  l_IEPS: Double;
  l_IEPSProgramado: Double;
  l_Impuesto: Double;
  l_Nombre: AnsiString;
  l_Precio: Double;
  l_PrecioProgramado: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Aplica := Self.Aplica;
    try
      TROSerializer(ASerializer).ReadEnumerated('Aplica',System.TypeInfo(boolean), l_Aplica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Aplica']));
    end;
    Self.Aplica := l_Aplica;
    l_ClavePemex := Self.ClavePemex;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClavePemex', l_ClavePemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClavePemex']));
    end;
    Self.ClavePemex := l_ClavePemex;
    l_FactorDecimalPrecios := Self.FactorDecimalPrecios;
    try
      TROSerializer(ASerializer).ReadInteger('FactorDecimalPrecios', TypInfo.otSLong, l_FactorDecimalPrecios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FactorDecimalPrecios']));
    end;
    Self.FactorDecimalPrecios := l_FactorDecimalPrecios;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_IEPSProgramado := Self.IEPSProgramado;
    try
      TROSerializer(ASerializer).ReadDouble('IEPSProgramado', TypInfo.ftDouble, l_IEPSProgramado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPSProgramado']));
    end;
    Self.IEPSProgramado := l_IEPSProgramado;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_PrecioProgramado := Self.PrecioProgramado;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioProgramado', TypInfo.ftDouble, l_PrecioProgramado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioProgramado']));
    end;
    Self.PrecioProgramado := l_PrecioProgramado;
  end
  else begin
    l_Aplica := Self.Aplica;
    try
      TROSerializer(ASerializer).ReadEnumerated('Aplica',System.TypeInfo(boolean), l_Aplica);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Aplica']));
    end;
    Self.Aplica := l_Aplica;
    l_ClavePemex := Self.ClavePemex;
    try
      TROSerializer(ASerializer).ReadAnsiString('ClavePemex', l_ClavePemex);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ClavePemex']));
    end;
    Self.ClavePemex := l_ClavePemex;
    l_FactorDecimalPrecios := Self.FactorDecimalPrecios;
    try
      TROSerializer(ASerializer).ReadInteger('FactorDecimalPrecios', TypInfo.otSLong, l_FactorDecimalPrecios);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FactorDecimalPrecios']));
    end;
    Self.FactorDecimalPrecios := l_FactorDecimalPrecios;
    l_IEPS := Self.IEPS;
    try
      TROSerializer(ASerializer).ReadDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPS']));
    end;
    Self.IEPS := l_IEPS;
    l_IEPSProgramado := Self.IEPSProgramado;
    try
      TROSerializer(ASerializer).ReadDouble('IEPSProgramado', TypInfo.ftDouble, l_IEPSProgramado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'IEPSProgramado']));
    end;
    Self.IEPSProgramado := l_IEPSProgramado;
    l_Impuesto := Self.Impuesto;
    try
      TROSerializer(ASerializer).ReadDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Impuesto']));
    end;
    Self.Impuesto := l_Impuesto;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_Precio := Self.Precio;
    try
      TROSerializer(ASerializer).ReadDouble('Precio', TypInfo.ftDouble, l_Precio);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Precio']));
    end;
    Self.Precio := l_Precio;
    l_PrecioProgramado := Self.PrecioProgramado;
    try
      TROSerializer(ASerializer).ReadDouble('PrecioProgramado', TypInfo.ftDouble, l_PrecioProgramado);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'PrecioProgramado']));
    end;
    Self.PrecioProgramado := l_PrecioProgramado;
  end;
end;

procedure TGas.WriteComplex(ASerializer: TObject);
var
  l_Aplica: Boolean;
  l_ClavePemex: AnsiString;
  l_FactorDecimalPrecios: Integer;
  l_IEPS: Double;
  l_IEPSProgramado: Double;
  l_Impuesto: Double;
  l_Nombre: AnsiString;
  l_Precio: Double;
  l_PrecioProgramado: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TGas);
    l_Aplica := Self.Aplica;
    TROSerializer(ASerializer).WriteEnumerated('Aplica',System.TypeInfo(boolean), l_Aplica);
    l_ClavePemex := Self.ClavePemex;
    TROSerializer(ASerializer).WriteAnsiString('ClavePemex', l_ClavePemex);
    l_FactorDecimalPrecios := Self.FactorDecimalPrecios;
    TROSerializer(ASerializer).WriteInteger('FactorDecimalPrecios', TypInfo.otSLong, l_FactorDecimalPrecios);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_IEPSProgramado := Self.IEPSProgramado;
    TROSerializer(ASerializer).WriteDouble('IEPSProgramado', TypInfo.ftDouble, l_IEPSProgramado);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_PrecioProgramado := Self.PrecioProgramado;
    TROSerializer(ASerializer).WriteDouble('PrecioProgramado', TypInfo.ftDouble, l_PrecioProgramado);
  end
  else begin
    l_Aplica := Self.Aplica;
    TROSerializer(ASerializer).WriteEnumerated('Aplica',System.TypeInfo(boolean), l_Aplica);
    l_ClavePemex := Self.ClavePemex;
    TROSerializer(ASerializer).WriteAnsiString('ClavePemex', l_ClavePemex);
    l_FactorDecimalPrecios := Self.FactorDecimalPrecios;
    TROSerializer(ASerializer).WriteInteger('FactorDecimalPrecios', TypInfo.otSLong, l_FactorDecimalPrecios);
    l_IEPS := Self.IEPS;
    TROSerializer(ASerializer).WriteDouble('IEPS', TypInfo.ftDouble, l_IEPS);
    l_IEPSProgramado := Self.IEPSProgramado;
    TROSerializer(ASerializer).WriteDouble('IEPSProgramado', TypInfo.ftDouble, l_IEPSProgramado);
    l_Impuesto := Self.Impuesto;
    TROSerializer(ASerializer).WriteDouble('Impuesto', TypInfo.ftDouble, l_Impuesto);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_Precio := Self.Precio;
    TROSerializer(ASerializer).WriteDouble('Precio', TypInfo.ftDouble, l_Precio);
    l_PrecioProgramado := Self.PrecioProgramado;
    TROSerializer(ASerializer).WriteDouble('PrecioProgramado', TypInfo.ftDouble, l_PrecioProgramado);
  end;
end;

{ TGasCollection }
constructor TGasCollection.Create;
begin
  inherited Create(TGas);
end;

constructor TGasCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TGasCollection.Add: TGas;
begin
  Result := TGas(inherited Add);
end;

function TGasCollection.GetItems(aIndex: Integer): TGas;
begin
  Result := TGas(inherited Items[aIndex]);
end;

procedure TGasCollection.LoadFromArray(anArray: aGas);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TGasCollection.SaveToArray(anArray: aGas);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TGas(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TGasCollection.SetItems(aIndex: Integer; const Value: TGas);
begin
  TGas(inherited Items[aIndex]).Assign(Value);
end;

{ TProtecciones }

procedure TProtecciones.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TProtecciones;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TProtecciones) then begin
    lSource := LibraryValesCupones_Intf.TProtecciones(iSource);

    Self.ConPreset := lSource.ConPreset;
    Self.ConLitros := lSource.ConLitros;
    Self.ConMultiplos := lSource.ConMultiplos;
    Self.ConDecimales := lSource.ConDecimales;
    Self.ConLLeno := lSource.ConLLeno;
    Self.ObligarPreset := lSource.ObligarPreset;
  end;
end;

procedure TProtecciones.ReadComplex(ASerializer: TObject);
var
  l_ConDecimales: Boolean;
  l_ConLitros: Boolean;
  l_ConLLeno: Boolean;
  l_ConMultiplos: Boolean;
  l_ConPreset: Boolean;
  l_ObligarPreset: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_ConPreset := Self.ConPreset;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConPreset',System.TypeInfo(boolean), l_ConPreset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConPreset']));
    end;
    Self.ConPreset := l_ConPreset;
    l_ConLitros := Self.ConLitros;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConLitros',System.TypeInfo(boolean), l_ConLitros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConLitros']));
    end;
    Self.ConLitros := l_ConLitros;
    l_ConMultiplos := Self.ConMultiplos;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConMultiplos',System.TypeInfo(boolean), l_ConMultiplos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConMultiplos']));
    end;
    Self.ConMultiplos := l_ConMultiplos;
    l_ConDecimales := Self.ConDecimales;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConDecimales',System.TypeInfo(boolean), l_ConDecimales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConDecimales']));
    end;
    Self.ConDecimales := l_ConDecimales;
    l_ConLLeno := Self.ConLLeno;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConLLeno',System.TypeInfo(boolean), l_ConLLeno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConLLeno']));
    end;
    Self.ConLLeno := l_ConLLeno;
    l_ObligarPreset := Self.ObligarPreset;
    try
      TROSerializer(ASerializer).ReadEnumerated('ObligarPreset',System.TypeInfo(boolean), l_ObligarPreset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ObligarPreset']));
    end;
    Self.ObligarPreset := l_ObligarPreset;
  end
  else begin
    l_ConDecimales := Self.ConDecimales;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConDecimales',System.TypeInfo(boolean), l_ConDecimales);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConDecimales']));
    end;
    Self.ConDecimales := l_ConDecimales;
    l_ConLitros := Self.ConLitros;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConLitros',System.TypeInfo(boolean), l_ConLitros);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConLitros']));
    end;
    Self.ConLitros := l_ConLitros;
    l_ConLLeno := Self.ConLLeno;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConLLeno',System.TypeInfo(boolean), l_ConLLeno);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConLLeno']));
    end;
    Self.ConLLeno := l_ConLLeno;
    l_ConMultiplos := Self.ConMultiplos;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConMultiplos',System.TypeInfo(boolean), l_ConMultiplos);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConMultiplos']));
    end;
    Self.ConMultiplos := l_ConMultiplos;
    l_ConPreset := Self.ConPreset;
    try
      TROSerializer(ASerializer).ReadEnumerated('ConPreset',System.TypeInfo(boolean), l_ConPreset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ConPreset']));
    end;
    Self.ConPreset := l_ConPreset;
    l_ObligarPreset := Self.ObligarPreset;
    try
      TROSerializer(ASerializer).ReadEnumerated('ObligarPreset',System.TypeInfo(boolean), l_ObligarPreset);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ObligarPreset']));
    end;
    Self.ObligarPreset := l_ObligarPreset;
  end;
end;

procedure TProtecciones.WriteComplex(ASerializer: TObject);
var
  l_ConDecimales: Boolean;
  l_ConLitros: Boolean;
  l_ConLLeno: Boolean;
  l_ConMultiplos: Boolean;
  l_ConPreset: Boolean;
  l_ObligarPreset: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TProtecciones);
    l_ConPreset := Self.ConPreset;
    TROSerializer(ASerializer).WriteEnumerated('ConPreset',System.TypeInfo(boolean), l_ConPreset);
    l_ConLitros := Self.ConLitros;
    TROSerializer(ASerializer).WriteEnumerated('ConLitros',System.TypeInfo(boolean), l_ConLitros);
    l_ConMultiplos := Self.ConMultiplos;
    TROSerializer(ASerializer).WriteEnumerated('ConMultiplos',System.TypeInfo(boolean), l_ConMultiplos);
    l_ConDecimales := Self.ConDecimales;
    TROSerializer(ASerializer).WriteEnumerated('ConDecimales',System.TypeInfo(boolean), l_ConDecimales);
    l_ConLLeno := Self.ConLLeno;
    TROSerializer(ASerializer).WriteEnumerated('ConLLeno',System.TypeInfo(boolean), l_ConLLeno);
    l_ObligarPreset := Self.ObligarPreset;
    TROSerializer(ASerializer).WriteEnumerated('ObligarPreset',System.TypeInfo(boolean), l_ObligarPreset);
  end
  else begin
    l_ConDecimales := Self.ConDecimales;
    TROSerializer(ASerializer).WriteEnumerated('ConDecimales',System.TypeInfo(boolean), l_ConDecimales);
    l_ConLitros := Self.ConLitros;
    TROSerializer(ASerializer).WriteEnumerated('ConLitros',System.TypeInfo(boolean), l_ConLitros);
    l_ConLLeno := Self.ConLLeno;
    TROSerializer(ASerializer).WriteEnumerated('ConLLeno',System.TypeInfo(boolean), l_ConLLeno);
    l_ConMultiplos := Self.ConMultiplos;
    TROSerializer(ASerializer).WriteEnumerated('ConMultiplos',System.TypeInfo(boolean), l_ConMultiplos);
    l_ConPreset := Self.ConPreset;
    TROSerializer(ASerializer).WriteEnumerated('ConPreset',System.TypeInfo(boolean), l_ConPreset);
    l_ObligarPreset := Self.ObligarPreset;
    TROSerializer(ASerializer).WriteEnumerated('ObligarPreset',System.TypeInfo(boolean), l_ObligarPreset);
  end;
end;

{ TProteccionesCollection }
constructor TProteccionesCollection.Create;
begin
  inherited Create(TProtecciones);
end;

constructor TProteccionesCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TProteccionesCollection.Add: TProtecciones;
begin
  Result := TProtecciones(inherited Add);
end;

function TProteccionesCollection.GetItems(aIndex: Integer): TProtecciones;
begin
  Result := TProtecciones(inherited Items[aIndex]);
end;

procedure TProteccionesCollection.SetItems(aIndex: Integer; const Value: TProtecciones);
begin
  TProtecciones(inherited Items[aIndex]).Assign(Value);
end;

{ TLicencia }

procedure TLicencia.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TLicencia;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TLicencia) then begin
    lSource := LibraryValesCupones_Intf.TLicencia(iSource);

    Self.Volumetrico := lSource.Volumetrico;
    Self.Tanques := lSource.Tanques;
    Self.Cualli := lSource.Cualli;
    Self.Produccion := lSource.Produccion;
    Self.Administracion := lSource.Administracion;
    Self.Flotillas := lSource.Flotillas;
    Self.NA := lSource.NA;
    Self.Definitiva := lSource.Definitiva;
  end;
end;

procedure TLicencia.ReadComplex(ASerializer: TObject);
var
  l_Administracion: Boolean;
  l_Cualli: Boolean;
  l_Definitiva: Boolean;
  l_Flotillas: Boolean;
  l_NA: Boolean;
  l_Produccion: Boolean;
  l_Tanques: Boolean;
  l_Volumetrico: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_Volumetrico := Self.Volumetrico;
    try
      TROSerializer(ASerializer).ReadEnumerated('Volumetrico',System.TypeInfo(boolean), l_Volumetrico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumetrico']));
    end;
    Self.Volumetrico := l_Volumetrico;
    l_Tanques := Self.Tanques;
    try
      TROSerializer(ASerializer).ReadEnumerated('Tanques',System.TypeInfo(boolean), l_Tanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanques']));
    end;
    Self.Tanques := l_Tanques;
    l_Cualli := Self.Cualli;
    try
      TROSerializer(ASerializer).ReadEnumerated('Cualli',System.TypeInfo(boolean), l_Cualli);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cualli']));
    end;
    Self.Cualli := l_Cualli;
    l_Produccion := Self.Produccion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Produccion',System.TypeInfo(boolean), l_Produccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Produccion']));
    end;
    Self.Produccion := l_Produccion;
    l_Administracion := Self.Administracion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Administracion',System.TypeInfo(boolean), l_Administracion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Administracion']));
    end;
    Self.Administracion := l_Administracion;
    l_Flotillas := Self.Flotillas;
    try
      TROSerializer(ASerializer).ReadEnumerated('Flotillas',System.TypeInfo(boolean), l_Flotillas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flotillas']));
    end;
    Self.Flotillas := l_Flotillas;
    l_NA := Self.NA;
    try
      TROSerializer(ASerializer).ReadEnumerated('NA',System.TypeInfo(boolean), l_NA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NA']));
    end;
    Self.NA := l_NA;
    l_Definitiva := Self.Definitiva;
    try
      TROSerializer(ASerializer).ReadEnumerated('Definitiva',System.TypeInfo(boolean), l_Definitiva);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Definitiva']));
    end;
    Self.Definitiva := l_Definitiva;
  end
  else begin
    l_Administracion := Self.Administracion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Administracion',System.TypeInfo(boolean), l_Administracion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Administracion']));
    end;
    Self.Administracion := l_Administracion;
    l_Cualli := Self.Cualli;
    try
      TROSerializer(ASerializer).ReadEnumerated('Cualli',System.TypeInfo(boolean), l_Cualli);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Cualli']));
    end;
    Self.Cualli := l_Cualli;
    l_Definitiva := Self.Definitiva;
    try
      TROSerializer(ASerializer).ReadEnumerated('Definitiva',System.TypeInfo(boolean), l_Definitiva);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Definitiva']));
    end;
    Self.Definitiva := l_Definitiva;
    l_Flotillas := Self.Flotillas;
    try
      TROSerializer(ASerializer).ReadEnumerated('Flotillas',System.TypeInfo(boolean), l_Flotillas);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Flotillas']));
    end;
    Self.Flotillas := l_Flotillas;
    l_NA := Self.NA;
    try
      TROSerializer(ASerializer).ReadEnumerated('NA',System.TypeInfo(boolean), l_NA);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'NA']));
    end;
    Self.NA := l_NA;
    l_Produccion := Self.Produccion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Produccion',System.TypeInfo(boolean), l_Produccion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Produccion']));
    end;
    Self.Produccion := l_Produccion;
    l_Tanques := Self.Tanques;
    try
      TROSerializer(ASerializer).ReadEnumerated('Tanques',System.TypeInfo(boolean), l_Tanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanques']));
    end;
    Self.Tanques := l_Tanques;
    l_Volumetrico := Self.Volumetrico;
    try
      TROSerializer(ASerializer).ReadEnumerated('Volumetrico',System.TypeInfo(boolean), l_Volumetrico);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Volumetrico']));
    end;
    Self.Volumetrico := l_Volumetrico;
  end;
end;

procedure TLicencia.WriteComplex(ASerializer: TObject);
var
  l_Administracion: Boolean;
  l_Cualli: Boolean;
  l_Definitiva: Boolean;
  l_Flotillas: Boolean;
  l_NA: Boolean;
  l_Produccion: Boolean;
  l_Tanques: Boolean;
  l_Volumetrico: Boolean;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TLicencia);
    l_Volumetrico := Self.Volumetrico;
    TROSerializer(ASerializer).WriteEnumerated('Volumetrico',System.TypeInfo(boolean), l_Volumetrico);
    l_Tanques := Self.Tanques;
    TROSerializer(ASerializer).WriteEnumerated('Tanques',System.TypeInfo(boolean), l_Tanques);
    l_Cualli := Self.Cualli;
    TROSerializer(ASerializer).WriteEnumerated('Cualli',System.TypeInfo(boolean), l_Cualli);
    l_Produccion := Self.Produccion;
    TROSerializer(ASerializer).WriteEnumerated('Produccion',System.TypeInfo(boolean), l_Produccion);
    l_Administracion := Self.Administracion;
    TROSerializer(ASerializer).WriteEnumerated('Administracion',System.TypeInfo(boolean), l_Administracion);
    l_Flotillas := Self.Flotillas;
    TROSerializer(ASerializer).WriteEnumerated('Flotillas',System.TypeInfo(boolean), l_Flotillas);
    l_NA := Self.NA;
    TROSerializer(ASerializer).WriteEnumerated('NA',System.TypeInfo(boolean), l_NA);
    l_Definitiva := Self.Definitiva;
    TROSerializer(ASerializer).WriteEnumerated('Definitiva',System.TypeInfo(boolean), l_Definitiva);
  end
  else begin
    l_Administracion := Self.Administracion;
    TROSerializer(ASerializer).WriteEnumerated('Administracion',System.TypeInfo(boolean), l_Administracion);
    l_Cualli := Self.Cualli;
    TROSerializer(ASerializer).WriteEnumerated('Cualli',System.TypeInfo(boolean), l_Cualli);
    l_Definitiva := Self.Definitiva;
    TROSerializer(ASerializer).WriteEnumerated('Definitiva',System.TypeInfo(boolean), l_Definitiva);
    l_Flotillas := Self.Flotillas;
    TROSerializer(ASerializer).WriteEnumerated('Flotillas',System.TypeInfo(boolean), l_Flotillas);
    l_NA := Self.NA;
    TROSerializer(ASerializer).WriteEnumerated('NA',System.TypeInfo(boolean), l_NA);
    l_Produccion := Self.Produccion;
    TROSerializer(ASerializer).WriteEnumerated('Produccion',System.TypeInfo(boolean), l_Produccion);
    l_Tanques := Self.Tanques;
    TROSerializer(ASerializer).WriteEnumerated('Tanques',System.TypeInfo(boolean), l_Tanques);
    l_Volumetrico := Self.Volumetrico;
    TROSerializer(ASerializer).WriteEnumerated('Volumetrico',System.TypeInfo(boolean), l_Volumetrico);
  end;
end;

{ TLicenciaCollection }
constructor TLicenciaCollection.Create;
begin
  inherited Create(TLicencia);
end;

constructor TLicenciaCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TLicenciaCollection.Add: TLicencia;
begin
  Result := TLicencia(inherited Add);
end;

function TLicenciaCollection.GetItems(aIndex: Integer): TLicencia;
begin
  Result := TLicencia(inherited Items[aIndex]);
end;

procedure TLicenciaCollection.SetItems(aIndex: Integer; const Value: TLicencia);
begin
  TLicencia(inherited Items[aIndex]).Assign(Value);
end;

{ TTanque }

procedure TTanque.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TTanque;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TTanque) then begin
    lSource := LibraryValesCupones_Intf.TTanque(iSource);

    Self.TanqueID := lSource.TanqueID;
    Self.Tanque := lSource.Tanque;
    Self.Capacidad := lSource.Capacidad;
    Self.Fondaje := lSource.Fondaje;
    Self.ProductoID := lSource.ProductoID;
    Self.Inicial := lSource.Inicial;
    Self.Temperatura := lSource.Temperatura;
    Self.VolumenAgua := lSource.VolumenAgua;
    Self.VolumenInicial := lSource.VolumenInicial;
    Self.VolumenDisponible := lSource.VolumenDisponible;
    Self.Conexion := lSource.Conexion;
    Self.StringTanques := lSource.StringTanques;
    Self.StringRecepciones := lSource.StringRecepciones;
    Self.Recepciones := lSource.Recepciones;
    Self.Nombre := lSource.Nombre;
  end;
end;

procedure TTanque.ReadComplex(ASerializer: TObject);
var
  l_Capacidad: Double;
  l_Conexion: Boolean;
  l_Fondaje: Double;
  l_Inicial: Double;
  l_Nombre: AnsiString;
  l_ProductoID: Integer;
  l_Recepciones: AnsiString;
  l_StringRecepciones: AnsiString;
  l_StringTanques: AnsiString;
  l_Tanque: Integer;
  l_TanqueID: Integer;
  l_Temperatura: Double;
  l_VolumenAgua: Double;
  l_VolumenDisponible: Double;
  l_VolumenInicial: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_TanqueID := Self.TanqueID;
    try
      TROSerializer(ASerializer).ReadInteger('TanqueID', TypInfo.otSLong, l_TanqueID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TanqueID']));
    end;
    Self.TanqueID := l_TanqueID;
    l_Tanque := Self.Tanque;
    try
      TROSerializer(ASerializer).ReadInteger('Tanque', TypInfo.otSLong, l_Tanque);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanque']));
    end;
    Self.Tanque := l_Tanque;
    l_Capacidad := Self.Capacidad;
    try
      TROSerializer(ASerializer).ReadDouble('Capacidad', TypInfo.ftDouble, l_Capacidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Capacidad']));
    end;
    Self.Capacidad := l_Capacidad;
    l_Fondaje := Self.Fondaje;
    try
      TROSerializer(ASerializer).ReadDouble('Fondaje', TypInfo.ftDouble, l_Fondaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fondaje']));
    end;
    Self.Fondaje := l_Fondaje;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Inicial := Self.Inicial;
    try
      TROSerializer(ASerializer).ReadDouble('Inicial', TypInfo.ftDouble, l_Inicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Inicial']));
    end;
    Self.Inicial := l_Inicial;
    l_Temperatura := Self.Temperatura;
    try
      TROSerializer(ASerializer).ReadDouble('Temperatura', TypInfo.ftDouble, l_Temperatura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Temperatura']));
    end;
    Self.Temperatura := l_Temperatura;
    l_VolumenAgua := Self.VolumenAgua;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenAgua', TypInfo.ftDouble, l_VolumenAgua);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenAgua']));
    end;
    Self.VolumenAgua := l_VolumenAgua;
    l_VolumenInicial := Self.VolumenInicial;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenInicial']));
    end;
    Self.VolumenInicial := l_VolumenInicial;
    l_VolumenDisponible := Self.VolumenDisponible;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenDisponible', TypInfo.ftDouble, l_VolumenDisponible);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenDisponible']));
    end;
    Self.VolumenDisponible := l_VolumenDisponible;
    l_Conexion := Self.Conexion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Conexion',System.TypeInfo(boolean), l_Conexion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Conexion']));
    end;
    Self.Conexion := l_Conexion;
    l_StringTanques := Self.StringTanques;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringTanques', l_StringTanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringTanques']));
    end;
    Self.StringTanques := l_StringTanques;
    l_StringRecepciones := Self.StringRecepciones;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringRecepciones', l_StringRecepciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringRecepciones']));
    end;
    Self.StringRecepciones := l_StringRecepciones;
    l_Recepciones := Self.Recepciones;
    try
      TROSerializer(ASerializer).ReadAnsiString('Recepciones', l_Recepciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recepciones']));
    end;
    Self.Recepciones := l_Recepciones;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
  end
  else begin
    l_Capacidad := Self.Capacidad;
    try
      TROSerializer(ASerializer).ReadDouble('Capacidad', TypInfo.ftDouble, l_Capacidad);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Capacidad']));
    end;
    Self.Capacidad := l_Capacidad;
    l_Conexion := Self.Conexion;
    try
      TROSerializer(ASerializer).ReadEnumerated('Conexion',System.TypeInfo(boolean), l_Conexion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Conexion']));
    end;
    Self.Conexion := l_Conexion;
    l_Fondaje := Self.Fondaje;
    try
      TROSerializer(ASerializer).ReadDouble('Fondaje', TypInfo.ftDouble, l_Fondaje);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Fondaje']));
    end;
    Self.Fondaje := l_Fondaje;
    l_Inicial := Self.Inicial;
    try
      TROSerializer(ASerializer).ReadDouble('Inicial', TypInfo.ftDouble, l_Inicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Inicial']));
    end;
    Self.Inicial := l_Inicial;
    l_Nombre := Self.Nombre;
    try
      TROSerializer(ASerializer).ReadAnsiString('Nombre', l_Nombre);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Nombre']));
    end;
    Self.Nombre := l_Nombre;
    l_ProductoID := Self.ProductoID;
    try
      TROSerializer(ASerializer).ReadInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'ProductoID']));
    end;
    Self.ProductoID := l_ProductoID;
    l_Recepciones := Self.Recepciones;
    try
      TROSerializer(ASerializer).ReadAnsiString('Recepciones', l_Recepciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Recepciones']));
    end;
    Self.Recepciones := l_Recepciones;
    l_StringRecepciones := Self.StringRecepciones;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringRecepciones', l_StringRecepciones);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringRecepciones']));
    end;
    Self.StringRecepciones := l_StringRecepciones;
    l_StringTanques := Self.StringTanques;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringTanques', l_StringTanques);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringTanques']));
    end;
    Self.StringTanques := l_StringTanques;
    l_Tanque := Self.Tanque;
    try
      TROSerializer(ASerializer).ReadInteger('Tanque', TypInfo.otSLong, l_Tanque);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Tanque']));
    end;
    Self.Tanque := l_Tanque;
    l_TanqueID := Self.TanqueID;
    try
      TROSerializer(ASerializer).ReadInteger('TanqueID', TypInfo.otSLong, l_TanqueID);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TanqueID']));
    end;
    Self.TanqueID := l_TanqueID;
    l_Temperatura := Self.Temperatura;
    try
      TROSerializer(ASerializer).ReadDouble('Temperatura', TypInfo.ftDouble, l_Temperatura);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'Temperatura']));
    end;
    Self.Temperatura := l_Temperatura;
    l_VolumenAgua := Self.VolumenAgua;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenAgua', TypInfo.ftDouble, l_VolumenAgua);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenAgua']));
    end;
    Self.VolumenAgua := l_VolumenAgua;
    l_VolumenDisponible := Self.VolumenDisponible;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenDisponible', TypInfo.ftDouble, l_VolumenDisponible);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenDisponible']));
    end;
    Self.VolumenDisponible := l_VolumenDisponible;
    l_VolumenInicial := Self.VolumenInicial;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenInicial']));
    end;
    Self.VolumenInicial := l_VolumenInicial;
  end;
end;

procedure TTanque.WriteComplex(ASerializer: TObject);
var
  l_Capacidad: Double;
  l_Conexion: Boolean;
  l_Fondaje: Double;
  l_Inicial: Double;
  l_Nombre: AnsiString;
  l_ProductoID: Integer;
  l_Recepciones: AnsiString;
  l_StringRecepciones: AnsiString;
  l_StringTanques: AnsiString;
  l_Tanque: Integer;
  l_TanqueID: Integer;
  l_Temperatura: Double;
  l_VolumenAgua: Double;
  l_VolumenDisponible: Double;
  l_VolumenInicial: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TTanque);
    l_TanqueID := Self.TanqueID;
    TROSerializer(ASerializer).WriteInteger('TanqueID', TypInfo.otSLong, l_TanqueID);
    l_Tanque := Self.Tanque;
    TROSerializer(ASerializer).WriteInteger('Tanque', TypInfo.otSLong, l_Tanque);
    l_Capacidad := Self.Capacidad;
    TROSerializer(ASerializer).WriteDouble('Capacidad', TypInfo.ftDouble, l_Capacidad);
    l_Fondaje := Self.Fondaje;
    TROSerializer(ASerializer).WriteDouble('Fondaje', TypInfo.ftDouble, l_Fondaje);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Inicial := Self.Inicial;
    TROSerializer(ASerializer).WriteDouble('Inicial', TypInfo.ftDouble, l_Inicial);
    l_Temperatura := Self.Temperatura;
    TROSerializer(ASerializer).WriteDouble('Temperatura', TypInfo.ftDouble, l_Temperatura);
    l_VolumenAgua := Self.VolumenAgua;
    TROSerializer(ASerializer).WriteDouble('VolumenAgua', TypInfo.ftDouble, l_VolumenAgua);
    l_VolumenInicial := Self.VolumenInicial;
    TROSerializer(ASerializer).WriteDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    l_VolumenDisponible := Self.VolumenDisponible;
    TROSerializer(ASerializer).WriteDouble('VolumenDisponible', TypInfo.ftDouble, l_VolumenDisponible);
    l_Conexion := Self.Conexion;
    TROSerializer(ASerializer).WriteEnumerated('Conexion',System.TypeInfo(boolean), l_Conexion);
    l_StringTanques := Self.StringTanques;
    TROSerializer(ASerializer).WriteAnsiString('StringTanques', l_StringTanques);
    l_StringRecepciones := Self.StringRecepciones;
    TROSerializer(ASerializer).WriteAnsiString('StringRecepciones', l_StringRecepciones);
    l_Recepciones := Self.Recepciones;
    TROSerializer(ASerializer).WriteAnsiString('Recepciones', l_Recepciones);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
  end
  else begin
    l_Capacidad := Self.Capacidad;
    TROSerializer(ASerializer).WriteDouble('Capacidad', TypInfo.ftDouble, l_Capacidad);
    l_Conexion := Self.Conexion;
    TROSerializer(ASerializer).WriteEnumerated('Conexion',System.TypeInfo(boolean), l_Conexion);
    l_Fondaje := Self.Fondaje;
    TROSerializer(ASerializer).WriteDouble('Fondaje', TypInfo.ftDouble, l_Fondaje);
    l_Inicial := Self.Inicial;
    TROSerializer(ASerializer).WriteDouble('Inicial', TypInfo.ftDouble, l_Inicial);
    l_Nombre := Self.Nombre;
    TROSerializer(ASerializer).WriteAnsiString('Nombre', l_Nombre);
    l_ProductoID := Self.ProductoID;
    TROSerializer(ASerializer).WriteInteger('ProductoID', TypInfo.otSLong, l_ProductoID);
    l_Recepciones := Self.Recepciones;
    TROSerializer(ASerializer).WriteAnsiString('Recepciones', l_Recepciones);
    l_StringRecepciones := Self.StringRecepciones;
    TROSerializer(ASerializer).WriteAnsiString('StringRecepciones', l_StringRecepciones);
    l_StringTanques := Self.StringTanques;
    TROSerializer(ASerializer).WriteAnsiString('StringTanques', l_StringTanques);
    l_Tanque := Self.Tanque;
    TROSerializer(ASerializer).WriteInteger('Tanque', TypInfo.otSLong, l_Tanque);
    l_TanqueID := Self.TanqueID;
    TROSerializer(ASerializer).WriteInteger('TanqueID', TypInfo.otSLong, l_TanqueID);
    l_Temperatura := Self.Temperatura;
    TROSerializer(ASerializer).WriteDouble('Temperatura', TypInfo.ftDouble, l_Temperatura);
    l_VolumenAgua := Self.VolumenAgua;
    TROSerializer(ASerializer).WriteDouble('VolumenAgua', TypInfo.ftDouble, l_VolumenAgua);
    l_VolumenDisponible := Self.VolumenDisponible;
    TROSerializer(ASerializer).WriteDouble('VolumenDisponible', TypInfo.ftDouble, l_VolumenDisponible);
    l_VolumenInicial := Self.VolumenInicial;
    TROSerializer(ASerializer).WriteDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
  end;
end;

{ TTanqueCollection }
constructor TTanqueCollection.Create;
begin
  inherited Create(TTanque);
end;

constructor TTanqueCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TTanqueCollection.Add: TTanque;
begin
  Result := TTanque(inherited Add);
end;

function TTanqueCollection.GetItems(aIndex: Integer): TTanque;
begin
  Result := TTanque(inherited Items[aIndex]);
end;

procedure TTanqueCollection.LoadFromArray(anArray: aTanques);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TTanqueCollection.SaveToArray(anArray: aTanques);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TTanque(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TTanqueCollection.SetItems(aIndex: Integer; const Value: TTanque);
begin
  TTanque(inherited Items[aIndex]).Assign(Value);
end;

{ TRecepcion }

procedure TRecepcion.Assign(iSource: TPersistent);
var
  lSource: LibraryValesCupones_Intf.TRecepcion;
begin
  inherited Assign(iSource);
  if (iSource is LibraryValesCupones_Intf.TRecepcion) then begin
    lSource := LibraryValesCupones_Intf.TRecepcion(iSource);

    Self.FechaRecepcion := lSource.FechaRecepcion;
    Self.FechaInicial := lSource.FechaInicial;
    Self.FechaFinal := lSource.FechaFinal;
    Self.VolumenInicial := lSource.VolumenInicial;
    Self.VolumenFinal := lSource.VolumenFinal;
    Self.TemperaturaInicial := lSource.TemperaturaInicial;
    Self.TemperaturaFinal := lSource.TemperaturaFinal;
    Self.VolumenRecibido := lSource.VolumenRecibido;
    Self.StringRecepocion := lSource.StringRecepocion;
  end;
end;

procedure TRecepcion.ReadComplex(ASerializer: TObject);
var
  l_FechaFinal: DateTime;
  l_FechaInicial: DateTime;
  l_FechaRecepcion: DateTime;
  l_StringRecepocion: AnsiString;
  l_TemperaturaFinal: Double;
  l_TemperaturaInicial: Double;
  l_VolumenFinal: Double;
  l_VolumenInicial: Double;
  l_VolumenRecibido: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    l_FechaRecepcion := Self.FechaRecepcion;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaRecepcion', l_FechaRecepcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaRecepcion']));
    end;
    Self.FechaRecepcion := l_FechaRecepcion;
    l_FechaInicial := Self.FechaInicial;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaInicial', l_FechaInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaInicial']));
    end;
    Self.FechaInicial := l_FechaInicial;
    l_FechaFinal := Self.FechaFinal;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFinal', l_FechaFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFinal']));
    end;
    Self.FechaFinal := l_FechaFinal;
    l_VolumenInicial := Self.VolumenInicial;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenInicial']));
    end;
    Self.VolumenInicial := l_VolumenInicial;
    l_VolumenFinal := Self.VolumenFinal;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenFinal', TypInfo.ftDouble, l_VolumenFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenFinal']));
    end;
    Self.VolumenFinal := l_VolumenFinal;
    l_TemperaturaInicial := Self.TemperaturaInicial;
    try
      TROSerializer(ASerializer).ReadDouble('TemperaturaInicial', TypInfo.ftDouble, l_TemperaturaInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TemperaturaInicial']));
    end;
    Self.TemperaturaInicial := l_TemperaturaInicial;
    l_TemperaturaFinal := Self.TemperaturaFinal;
    try
      TROSerializer(ASerializer).ReadDouble('TemperaturaFinal', TypInfo.ftDouble, l_TemperaturaFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TemperaturaFinal']));
    end;
    Self.TemperaturaFinal := l_TemperaturaFinal;
    l_VolumenRecibido := Self.VolumenRecibido;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenRecibido', TypInfo.ftDouble, l_VolumenRecibido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenRecibido']));
    end;
    Self.VolumenRecibido := l_VolumenRecibido;
    l_StringRecepocion := Self.StringRecepocion;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringRecepocion', l_StringRecepocion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringRecepocion']));
    end;
    Self.StringRecepocion := l_StringRecepocion;
  end
  else begin
    l_FechaFinal := Self.FechaFinal;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaFinal', l_FechaFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaFinal']));
    end;
    Self.FechaFinal := l_FechaFinal;
    l_FechaInicial := Self.FechaInicial;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaInicial', l_FechaInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaInicial']));
    end;
    Self.FechaInicial := l_FechaInicial;
    l_FechaRecepcion := Self.FechaRecepcion;
    try
      TROSerializer(ASerializer).ReadDateTime('FechaRecepcion', l_FechaRecepcion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'FechaRecepcion']));
    end;
    Self.FechaRecepcion := l_FechaRecepcion;
    l_StringRecepocion := Self.StringRecepocion;
    try
      TROSerializer(ASerializer).ReadAnsiString('StringRecepocion', l_StringRecepocion);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'StringRecepocion']));
    end;
    Self.StringRecepocion := l_StringRecepocion;
    l_TemperaturaFinal := Self.TemperaturaFinal;
    try
      TROSerializer(ASerializer).ReadDouble('TemperaturaFinal', TypInfo.ftDouble, l_TemperaturaFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TemperaturaFinal']));
    end;
    Self.TemperaturaFinal := l_TemperaturaFinal;
    l_TemperaturaInicial := Self.TemperaturaInicial;
    try
      TROSerializer(ASerializer).ReadDouble('TemperaturaInicial', TypInfo.ftDouble, l_TemperaturaInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'TemperaturaInicial']));
    end;
    Self.TemperaturaInicial := l_TemperaturaInicial;
    l_VolumenFinal := Self.VolumenFinal;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenFinal', TypInfo.ftDouble, l_VolumenFinal);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenFinal']));
    end;
    Self.VolumenFinal := l_VolumenFinal;
    l_VolumenInicial := Self.VolumenInicial;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenInicial']));
    end;
    Self.VolumenInicial := l_VolumenInicial;
    l_VolumenRecibido := Self.VolumenRecibido;
    try
      TROSerializer(ASerializer).ReadDouble('VolumenRecibido', TypInfo.ftDouble, l_VolumenRecibido);
    except
      on E: SysUtils.Exception do
        uROClasses.RaiseError(SysUtils.Format('Exception "%s" with message "%s" happens during reading field "%s".', [E.ClassName, E.Message, 'VolumenRecibido']));
    end;
    Self.VolumenRecibido := l_VolumenRecibido;
  end;
end;

procedure TRecepcion.WriteComplex(ASerializer: TObject);
var
  l_FechaFinal: DateTime;
  l_FechaInicial: DateTime;
  l_FechaRecepcion: DateTime;
  l_StringRecepocion: AnsiString;
  l_TemperaturaFinal: Double;
  l_TemperaturaInicial: Double;
  l_VolumenFinal: Double;
  l_VolumenInicial: Double;
  l_VolumenRecibido: Double;
begin
  if TROSerializer(ASerializer).RecordStrictOrder then begin
    TROSerializer(ASerializer).ChangeClass(TRecepcion);
    l_FechaRecepcion := Self.FechaRecepcion;
    TROSerializer(ASerializer).WriteDateTime('FechaRecepcion', l_FechaRecepcion);
    l_FechaInicial := Self.FechaInicial;
    TROSerializer(ASerializer).WriteDateTime('FechaInicial', l_FechaInicial);
    l_FechaFinal := Self.FechaFinal;
    TROSerializer(ASerializer).WriteDateTime('FechaFinal', l_FechaFinal);
    l_VolumenInicial := Self.VolumenInicial;
    TROSerializer(ASerializer).WriteDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    l_VolumenFinal := Self.VolumenFinal;
    TROSerializer(ASerializer).WriteDouble('VolumenFinal', TypInfo.ftDouble, l_VolumenFinal);
    l_TemperaturaInicial := Self.TemperaturaInicial;
    TROSerializer(ASerializer).WriteDouble('TemperaturaInicial', TypInfo.ftDouble, l_TemperaturaInicial);
    l_TemperaturaFinal := Self.TemperaturaFinal;
    TROSerializer(ASerializer).WriteDouble('TemperaturaFinal', TypInfo.ftDouble, l_TemperaturaFinal);
    l_VolumenRecibido := Self.VolumenRecibido;
    TROSerializer(ASerializer).WriteDouble('VolumenRecibido', TypInfo.ftDouble, l_VolumenRecibido);
    l_StringRecepocion := Self.StringRecepocion;
    TROSerializer(ASerializer).WriteAnsiString('StringRecepocion', l_StringRecepocion);
  end
  else begin
    l_FechaFinal := Self.FechaFinal;
    TROSerializer(ASerializer).WriteDateTime('FechaFinal', l_FechaFinal);
    l_FechaInicial := Self.FechaInicial;
    TROSerializer(ASerializer).WriteDateTime('FechaInicial', l_FechaInicial);
    l_FechaRecepcion := Self.FechaRecepcion;
    TROSerializer(ASerializer).WriteDateTime('FechaRecepcion', l_FechaRecepcion);
    l_StringRecepocion := Self.StringRecepocion;
    TROSerializer(ASerializer).WriteAnsiString('StringRecepocion', l_StringRecepocion);
    l_TemperaturaFinal := Self.TemperaturaFinal;
    TROSerializer(ASerializer).WriteDouble('TemperaturaFinal', TypInfo.ftDouble, l_TemperaturaFinal);
    l_TemperaturaInicial := Self.TemperaturaInicial;
    TROSerializer(ASerializer).WriteDouble('TemperaturaInicial', TypInfo.ftDouble, l_TemperaturaInicial);
    l_VolumenFinal := Self.VolumenFinal;
    TROSerializer(ASerializer).WriteDouble('VolumenFinal', TypInfo.ftDouble, l_VolumenFinal);
    l_VolumenInicial := Self.VolumenInicial;
    TROSerializer(ASerializer).WriteDouble('VolumenInicial', TypInfo.ftDouble, l_VolumenInicial);
    l_VolumenRecibido := Self.VolumenRecibido;
    TROSerializer(ASerializer).WriteDouble('VolumenRecibido', TypInfo.ftDouble, l_VolumenRecibido);
  end;
end;

{ TRecepcionCollection }
constructor TRecepcionCollection.Create;
begin
  inherited Create(TRecepcion);
end;

constructor TRecepcionCollection.Create(aItemClass: TCollectionItemClass);
begin
  inherited Create(aItemClass);
end;

function TRecepcionCollection.Add: TRecepcion;
begin
  Result := TRecepcion(inherited Add);
end;

function TRecepcionCollection.GetItems(aIndex: Integer): TRecepcion;
begin
  Result := TRecepcion(inherited Items[aIndex]);
end;

procedure TRecepcionCollection.LoadFromArray(anArray: aRecepciones);
var
  i : Integer;
begin
  Self.Clear;
  for i := 0 to (anArray.Count-1) do
    if anArray[i] <> nil then
      anArray[i].Clone.Collection := Self;
end;

procedure TRecepcionCollection.SaveToArray(anArray: aRecepciones);
var
  i : Integer;
begin
  anArray.Clear;
  for i := 0 to (Self.Count-1) do begin
    if System.Assigned(Self.Items[i]) then
      anArray.Add(TRecepcion(Self.Items[i].Clone))
    else
      anArray.Add(nil);
  end;
end;

procedure TRecepcionCollection.SetItems(aIndex: Integer; const Value: TRecepcion);
begin
  TRecepcion(inherited Items[aIndex]).Assign(Value);
end;

{ CoServiceValesCupones }

class function CoServiceValesCupones.Create(const aMessage: IROMessage; aTransportChannel: IROTransportChannel): IServiceValesCupones;
begin
  Result := TServiceValesCupones_Proxy.Create(aMessage, aTransportChannel);
end;

class function CoServiceValesCupones.Create(const aUri: TROUri): IServiceValesCupones;
begin
  Result := TServiceValesCupones_Proxy.Create(aUri);
end;

class function CoServiceValesCupones.Create(const aUrl: string): IServiceValesCupones;
begin
  Result := TServiceValesCupones_Proxy.Create(aUrl);
end;

{ TServiceValesCupones_Proxy }

function TServiceValesCupones_Proxy.__GetInterfaceName:string;
begin
  Result := 'ServiceValesCupones';
end;

function TServiceValesCupones_Proxy.AbreDataSetReporte(const SQL: AnsiString; const Parametros: TParametros): Binary;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AbreDataSetReporte');
    lMessage.Write('SQL', System.TypeInfo(AnsiString), SQL, []);
    lMessage.Write('Parametros', System.TypeInfo(LibraryValesCupones_Intf.TParametros), Parametros, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Binary), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FacturaCupon(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                                                 const SerieFactura: AnsiString): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FacturaCupon');
    lMessage.Write('Cliente', System.TypeInfo(Integer), Cliente, []);
    lMessage.Write('Importe', System.TypeInfo(Double), Importe, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('EmpleadoID', System.TypeInfo(Integer), EmpleadoID, []);
    lMessage.Write('SerieFactura', System.TypeInfo(AnsiString), SerieFactura, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizaCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                                    const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Liquidacion: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaCupon');
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Write('DiaConsumo', System.TypeInfo(Integer), DiaConsumo, []);
    lMessage.Write('EjercicioConsumo', System.TypeInfo(Integer), EjercicioConsumo, []);
    lMessage.Write('FechaConsumo', System.TypeInfo(DateTime), FechaConsumo, [paIsDateTime]);
    lMessage.Write('PeriodoConsumo', System.TypeInfo(Integer), PeriodoConsumo, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Write('Ticket', System.TypeInfo(Integer), Ticket, []);
    lMessage.Write('Liquidacion', System.TypeInfo(Integer), Liquidacion, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizaCuponConReferencia(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                                                 const PeriodoConsumo: Integer; const Secuencia: Integer; const Ticket: Integer; const Referencia: AnsiString);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaCuponConReferencia');
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Write('DiaConsumo', System.TypeInfo(Integer), DiaConsumo, []);
    lMessage.Write('EjercicioConsumo', System.TypeInfo(Integer), EjercicioConsumo, []);
    lMessage.Write('FechaConsumo', System.TypeInfo(DateTime), FechaConsumo, [paIsDateTime]);
    lMessage.Write('PeriodoConsumo', System.TypeInfo(Integer), PeriodoConsumo, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Write('Ticket', System.TypeInfo(Integer), Ticket, []);
    lMessage.Write('Referencia', System.TypeInfo(AnsiString), Referencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizaLiquidacionCorte(const Liquidacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaLiquidacionCorte');
    lMessage.Write('Liquidacion', System.TypeInfo(Integer), Liquidacion, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Write('AgrupacionID', System.TypeInfo(Integer), AgrupacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizarStatusValeCredito(const CarteraValeCreditoID: Integer; const Status: AnsiString; const FacturaID: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizarStatusValeCredito');
    lMessage.Write('CarteraValeCreditoID', System.TypeInfo(Integer), CarteraValeCreditoID, []);
    lMessage.Write('Status', System.TypeInfo(AnsiString), Status, []);
    lMessage.Write('FacturaID', System.TypeInfo(Integer), FacturaID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizaStatusCupon(const Barras: AnsiString; const DiaConsumo: Integer; const EjercicioConsumo: Integer; const FechaConsumo: DateTime;
                                                          const LiquidacionID: Integer; const PeriodoConsumo: Integer; const Secuencia: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaStatusCupon');
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Write('DiaConsumo', System.TypeInfo(Integer), DiaConsumo, []);
    lMessage.Write('EjercicioConsumo', System.TypeInfo(Integer), EjercicioConsumo, []);
    lMessage.Write('FechaConsumo', System.TypeInfo(DateTime), FechaConsumo, [paIsDateTime]);
    lMessage.Write('LiquidacionID', System.TypeInfo(Integer), LiquidacionID, []);
    lMessage.Write('PeriodoConsumo', System.TypeInfo(Integer), PeriodoConsumo, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaHost(const Tipo: Integer; const Gasolinero: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaHost');
    lMessage.Write('Tipo', System.TypeInfo(Integer), Tipo, []);
    lMessage.Write('Gasolinero', System.TypeInfo(Integer), Gasolinero, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaReporte(const Nombre: AnsiString; const TipoTemplate: Integer): TReporte;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaReporte');
    lMessage.Write('Nombre', System.TypeInfo(AnsiString), Nombre, []);
    lMessage.Write('TipoTemplate', System.TypeInfo(Integer), TipoTemplate, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TReporte), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CambiaFolioFactura(const Serie: AnsiString; const Folio: Integer; const NewSerie: AnsiString; const NewFolio: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CambiaFolioFactura');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Write('NewSerie', System.TypeInfo(AnsiString), NewSerie, []);
    lMessage.Write('NewFolio', System.TypeInfo(Integer), NewFolio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CancelaFactura(const Folio: Integer; const Serie: AnsiString; const Fecha: DateTime; const EstacionID: Integer; const UsuarioID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CancelaFactura');
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Fecha', System.TypeInfo(DateTime), Fecha, [paIsDateTime]);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CancelarCupones(const Lista: AnsiString; const UsuarioID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CancelarCupones');
    lMessage.Write('Lista', System.TypeInfo(AnsiString), Lista, []);
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CierraLiquidacion(const LiquidacionID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CierraLiquidacion');
    lMessage.Write('LiquidacionID', System.TypeInfo(Integer), LiquidacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CostoProducto(const ProductoID: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CostoProducto');
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosCliente(const ClienteID: Integer; const Referencia: AnsiString): TDatosCliente;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosCliente');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('Referencia', System.TypeInfo(AnsiString), Referencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosCliente), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.Existencia(const EstacionID: Integer; const AlmacenID: Integer; const ProductoID: Integer; const Fecha: DateTime): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'Existencia');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('AlmacenID', System.TypeInfo(Integer), AlmacenID, []);
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Write('Fecha', System.TypeInfo(DateTime), Fecha, [paIsDateTime]);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FacturaExpress(const Serie: AnsiString; const Folio: Integer): TFacturaExpress;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FacturaExpress');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TFacturaExpress), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FacturaID(const Serie: AnsiString; const Folio: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FacturaID');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FacturaYLiquidacion(const ClienteID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FacturaYLiquidacion');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.Fecha: DateTime;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'Fecha');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(DateTime), Result, [paIsDateTime]);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.Folio(const Campo: AnsiString; const Serie: AnsiString): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'Folio');
    lMessage.Write('Campo', System.TypeInfo(AnsiString), Campo, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FolioActual(const Campo: AnsiString; const Serie: AnsiString): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FolioActual');
    lMessage.Write('Campo', System.TypeInfo(AnsiString), Campo, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FolioActual2(const serie: AnsiString; const folio: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FolioActual2');
    lMessage.Write('serie', System.TypeInfo(AnsiString), serie, []);
    lMessage.Write('folio', System.TypeInfo(Integer), folio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardaAccesos(const UsuarioID: Integer; const Lista: AnsiString);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaAccesos');
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Write('Lista', System.TypeInfo(AnsiString), Lista, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GuardaConsumoExpress(const Consumo: TConsumoExpress): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaConsumoExpress');
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumoExpress), Consumo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardaLimiteFactura(const UsuarioID: Int64; const Cantidad: Double);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaLimiteFactura');
    lMessage.Write('UsuarioID', System.TypeInfo(Int64), UsuarioID, []);
    lMessage.Write('Cantidad', System.TypeInfo(Double), Cantidad, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardarDatosFactura(const DatosFactura: TDatosFactura);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarDatosFactura');
    lMessage.Write('DatosFactura', System.TypeInfo(LibraryValesCupones_Intf.TDatosFactura), DatosFactura, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardarDatosLiquidacion(const DatosLiquidacion: TDatosLiquidacion);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarDatosLiquidacion');
    lMessage.Write('DatosLiquidacion', System.TypeInfo(LibraryValesCupones_Intf.TDatosLiquidacion), DatosLiquidacion, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardarDatosReciboPago(const DatosReciboPago: TDatosReciboPago);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarDatosReciboPago');
    lMessage.Write('DatosReciboPago', System.TypeInfo(LibraryValesCupones_Intf.TDatosReciboPago), DatosReciboPago, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GuardarFacturaExpress(const ClienteID: Integer; const Serie: AnsiString; const EstacionID: Integer; const TicketID: Integer): TFacturaExpress;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarFacturaExpress');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TicketID', System.TypeInfo(Integer), TicketID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TFacturaExpress), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.InsertaDeposito(const DepositoID: Integer; const Cantidad: Double; const Usuario: Integer; const Fecha: DateTime; const Secuencia: Integer;
                                                     const EstacionID: Integer; const Descripcion: AnsiString; const Ejercicio: Integer; const Periodo: Integer;
                                                     const Dia: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'InsertaDeposito');
    lMessage.Write('DepositoID', System.TypeInfo(Integer), DepositoID, []);
    lMessage.Write('Cantidad', System.TypeInfo(Double), Cantidad, []);
    lMessage.Write('Usuario', System.TypeInfo(Integer), Usuario, []);
    lMessage.Write('Fecha', System.TypeInfo(DateTime), Fecha, [paIsDateTime]);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('Descripcion', System.TypeInfo(AnsiString), Descripcion, []);
    lMessage.Write('Ejercicio', System.TypeInfo(Integer), Ejercicio, []);
    lMessage.Write('Periodo', System.TypeInfo(Integer), Periodo, []);
    lMessage.Write('Dia', System.TypeInfo(Integer), Dia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardaDatosFactura(const DatosFactura: TDatosFactura);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaDatosFactura');
    lMessage.Write('DatosFactura', System.TypeInfo(LibraryValesCupones_Intf.TDatosFactura), DatosFactura, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.InsertaProductoPrecio(const ProductoID: Integer; const EstacionID: Integer; const Precio: Double);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'InsertaProductoPrecio');
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('Precio', System.TypeInfo(Double), Precio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.Login(const Usuario: AnsiString; const Clave: AnsiString): TLoginInfo;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'Login');
    lMessage.Write('Usuario', System.TypeInfo(AnsiString), Usuario, []);
    lMessage.Write('Clave', System.TypeInfo(AnsiString), Clave, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TLoginInfo), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ModificarFolioActual(const Campo: AnsiString; const Serie: AnsiString; const Folio: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ModificarFolioActual');
    lMessage.Write('Campo', System.TypeInfo(AnsiString), Campo, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ObtenerStatusCupon(const Barras: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ObtenerStatusCupon');
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PrefijaCupon(const nBomba: Integer; const Barras: AnsiString; const Terminal: AnsiString; const Grupo: Integer; const ClienteID: Integer): TCupon;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PrefijaCupon');
    lMessage.Write('nBomba', System.TypeInfo(Integer), nBomba, []);
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Write('Terminal', System.TypeInfo(AnsiString), Terminal, []);
    lMessage.Write('Grupo', System.TypeInfo(Integer), Grupo, []);
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TCupon), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ObtenerTipoCambioIDPorEstacion(const EstacionID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ObtenerTipoCambioIDPorEstacion');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ObtenerTipoCambioPorEstacion(const EstacionID: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ObtenerTipoCambioPorEstacion');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PrecioProducto(const EstacionID: Integer; const ProductoID: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PrecioProducto');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.StatusTicket(const EstacionID: Integer; const TicketID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'StatusTicket');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TicketID', System.TypeInfo(Integer), TicketID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.SumaAnticipo(const Estacion: Integer; const Secuencia: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'SumaAnticipo');
    lMessage.Write('Estacion', System.TypeInfo(Integer), Estacion, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.SumaVentasCorte(const Estacion: Integer; const Secuencia: Integer; const AgrupacionID: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'SumaVentasCorte');
    lMessage.Write('Estacion', System.TypeInfo(Integer), Estacion, []);
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Write('AgrupacionID', System.TypeInfo(Integer), AgrupacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ValoresTurno(const EstacionID: Integer; const TurnoID: Integer): TValoresTurno;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValoresTurno');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TValoresTurno), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.VersionServer: AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'VersionServer');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.LiquidacionCerrada(const EstacionID: Integer; const TurnoID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'LiquidacionCerrada');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ClienteValido(const ClienteID: Integer; const NewClienteID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ClienteValido');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('NewClienteID', System.TypeInfo(Integer), NewClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaOtroProducto(const EstacionID: Integer; const Codigo: AnsiString): TOtroProducto;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaOtroProducto');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('Codigo', System.TypeInfo(AnsiString), Codigo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TOtroProducto), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.TurnoALiquidacionID(const TurnoID: Integer; const EstacionID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'TurnoALiquidacionID');
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaSagarpa(const EstacionID: Integer): TSagarpa;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaSagarpa');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TSagarpa), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PuntosCalculaSaldo(const ClienteID: Integer): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosCalculaSaldo');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PuntosCalculaPuntos(const ClienteID: Integer; const ProductoID: Integer; const Volumen: Double): Double;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosCalculaPuntos');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Write('Volumen', System.TypeInfo(Double), Volumen, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Double), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.PuntosGuardaDatos(const Datos: TDatosPuntos);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosGuardaDatos');
    lMessage.Write('Datos', System.TypeInfo(LibraryValesCupones_Intf.TDatosPuntos), Datos, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PuntosDatos(const ClienteID: Integer): TDatosPuntos;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosDatos');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosPuntos), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.EliminaAutomaticosLiquidacion(const EstacionID: Integer; const TurnoID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EliminaAutomaticosLiquidacion');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaDespachadorLiquidacion(const BombaID: Integer; const EstacionID: Integer; const TurnoID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaDespachadorLiquidacion');
    lMessage.Write('BombaID', System.TypeInfo(Integer), BombaID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.EntregaPremio(const ClienteID: Integer; const PremioID: Integer; const Cantidad: Integer): TEntregaPremio;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EntregaPremio');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('PremioID', System.TypeInfo(Integer), PremioID, []);
    lMessage.Write('Cantidad', System.TypeInfo(Integer), Cantidad, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TEntregaPremio), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosPremio(const PremioID: Integer): TDatosPremio;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosPremio');
    lMessage.Write('PremioID', System.TypeInfo(Integer), PremioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosPremio), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ValidaConsumo(const Consumo: TConsumoExpress): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValidaConsumo');
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumoExpress), Consumo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.AgrupacionesBomba(const EstacionID: Integer): AAgrupacion;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AgrupacionesBomba');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.AAgrupacion), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosCerrarLiquidacion(const EstacionID: Integer; const TurnoID: Integer): TDatosCerrarLiquidacion;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosCerrarLiquidacion');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosCerrarLiquidacion), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ProcesaVentasLiquidacion(const Datos: TDatosCerrarLiquidacion);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ProcesaVentasLiquidacion');
    lMessage.Write('Datos', System.TypeInfo(LibraryValesCupones_Intf.TDatosCerrarLiquidacion), Datos, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.TicketsLiquidacion(const LiquidacionID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'TicketsLiquidacion');
    lMessage.Write('LiquidacionID', System.TypeInfo(Integer), LiquidacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ValidaFolioFactura(const Serie: AnsiString; const Folio: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValidaFolioFactura');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.PuntosGuardaCriterios(const PuntosCriterioID: Integer; const Datos: AnsiString);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosGuardaCriterios');
    lMessage.Write('PuntosCriterioID', System.TypeInfo(Integer), PuntosCriterioID, []);
    lMessage.Write('Datos', System.TypeInfo(AnsiString), Datos, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosFacturaElectronica(const FacturaID: Integer; const EstacionID: Integer): TFacturaElectronica;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosFacturaElectronica');
    lMessage.Write('FacturaID', System.TypeInfo(Integer), FacturaID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TFacturaElectronica), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.LlavePrivaCertificado(const LlavePrivada: AnsiString; const Certificado: AnsiString; const GasolineroID: Integer; const Password: AnsiString;
                                                          const EstacionID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'LlavePrivaCertificado');
    lMessage.Write('LlavePrivada', System.TypeInfo(AnsiString), LlavePrivada, []);
    lMessage.Write('Certificado', System.TypeInfo(AnsiString), Certificado, []);
    lMessage.Write('GasolineroID', System.TypeInfo(Integer), GasolineroID, []);
    lMessage.Write('Password', System.TypeInfo(AnsiString), Password, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.SELLOPEMs(const Texto: UnicodeString; const EstacionID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'SELLOPEMs');
    lMessage.Write('Texto', System.TypeInfo(UnicodeString), Texto, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CERs(const DIR: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CERs');
    lMessage.Write('DIR', System.TypeInfo(AnsiString), DIR, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ValidaLiquidacionDespachador(const LiquidacionID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValidaLiquidacionDespachador');
    lMessage.Write('LiquidacionID', System.TypeInfo(Integer), LiquidacionID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ActualizaLiquidacionProd(const Estacion: Integer; const FechaIni: DateTime; const FechaFin: DateTime; const TurnoID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaLiquidacionProd');
    lMessage.Write('Estacion', System.TypeInfo(Integer), Estacion, []);
    lMessage.Write('FechaIni', System.TypeInfo(DateTime), FechaIni, [paIsDateTime]);
    lMessage.Write('FechaFin', System.TypeInfo(DateTime), FechaFin, [paIsDateTime]);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.PuntosDespachador(const TurnoID: Integer; const EstacionID: Integer; const UsuarioID: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PuntosDespachador');
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GuardarDatosFacturaPemex(const DatosFacturaPemex: TDatosFacturaPemex);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarDatosFacturaPemex');
    lMessage.Write('DatosFacturaPemex', System.TypeInfo(LibraryValesCupones_Intf.TDatosFacturaPemex), DatosFacturaPemex, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.AfectaInventarios(const LiquidacionID: Integer; const UsuarioID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AfectaInventarios');
    lMessage.Write('LiquidacionID', System.TypeInfo(Integer), LiquidacionID, []);
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.InsertaFacturaElectronica(const FacturaElectronicaID: Integer; const CadenaOriginal: UnicodeString; const SelloDigital: UnicodeString;
                                                               const FacturaID: Integer; const Vigencia: Boolean; const Enviado: Boolean; const NoCertificado: AnsiString;
                                                               const NoAprobacion: AnsiString; const FechaAprobacion: DateTime);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'InsertaFacturaElectronica');
    lMessage.Write('FacturaElectronicaID', System.TypeInfo(Integer), FacturaElectronicaID, []);
    lMessage.Write('CadenaOriginal', System.TypeInfo(UnicodeString), CadenaOriginal, []);
    lMessage.Write('SelloDigital', System.TypeInfo(UnicodeString), SelloDigital, []);
    lMessage.Write('FacturaID', System.TypeInfo(Integer), FacturaID, []);
    lMessage.Write('Vigencia', System.TypeInfo(Boolean), Vigencia, []);
    lMessage.Write('Enviado', System.TypeInfo(Boolean), Enviado, []);
    lMessage.Write('NoCertificado', System.TypeInfo(AnsiString), NoCertificado, []);
    lMessage.Write('NoAprobacion', System.TypeInfo(AnsiString), NoAprobacion, []);
    lMessage.Write('FechaAprobacion', System.TypeInfo(DateTime), FechaAprobacion, [paIsDateTime]);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CERsNUM(const DIR: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CERsNUM');
    lMessage.Write('DIR', System.TypeInfo(AnsiString), DIR, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ActualizaFirmaDefault(const UsuarioID: Integer; const FIRMA: Binary): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaFirmaDefault');
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Write('FIRMA', System.TypeInfo(Binary), FIRMA, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.obtendatosempleados(const EstacionID: AnsiString; const Turno: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'obtendatosempleados');
    lMessage.Write('EstacionID', System.TypeInfo(AnsiString), EstacionID, []);
    lMessage.Write('Turno', System.TypeInfo(AnsiString), Turno, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ProgramaMantenimiento(const IDPROGRAMAMANTENIMIENTO: Integer; const IDESTACION: Integer; const FECHA: DateTime);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ProgramaMantenimiento');
    lMessage.Write('IDPROGRAMAMANTENIMIENTO', System.TypeInfo(Integer), IDPROGRAMAMANTENIMIENTO, []);
    lMessage.Write('IDESTACION', System.TypeInfo(Integer), IDESTACION, []);
    lMessage.Write('FECHA', System.TypeInfo(DateTime), FECHA, [paIsDateTime]);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.obtendatosActividadesProgramadas(const IDESTACION: Integer; const TIPO: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'obtendatosActividadesProgramadas');
    lMessage.Write('IDESTACION', System.TypeInfo(Integer), IDESTACION, []);
    lMessage.Write('TIPO', System.TypeInfo(Integer), TIPO, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.obtenNotificacionesDiarias(const IDESTACION: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'obtenNotificacionesDiarias');
    lMessage.Write('IDESTACION', System.TypeInfo(Integer), IDESTACION, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.registraactividadrealizada(const IDACTIVIDADMANTENIMIENTO: Integer; const NOTA: AnsiString; const STATUS: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'registraactividadrealizada');
    lMessage.Write('IDACTIVIDADMANTENIMIENTO', System.TypeInfo(Integer), IDACTIVIDADMANTENIMIENTO, []);
    lMessage.Write('NOTA', System.TypeInfo(AnsiString), NOTA, []);
    lMessage.Write('STATUS', System.TypeInfo(AnsiString), STATUS, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CancelarLote(const LoteID: Integer; const UsuarioID: Integer): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CancelarLote');
    lMessage.Write('LoteID', System.TypeInfo(Integer), LoteID, []);
    lMessage.Write('UsuarioID', System.TypeInfo(Integer), UsuarioID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.StatusCupon(const Codigo: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'StatusCupon');
    lMessage.Write('Codigo', System.TypeInfo(AnsiString), Codigo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscarReporte(const Nombre: AnsiString): TReporte;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscarReporte');
    lMessage.Write('Nombre', System.TypeInfo(AnsiString), Nombre, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TReporte), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.GeneraCupones(const LoteCuponID: Integer; const ClienteID: Integer; const EstacionID: Integer; const Identificador: Integer;
                                                   const Cupones: AGeneraCupon; const TipoCupon: Boolean; const Serie: AnsiString; const aFolio: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GeneraCupones');
    lMessage.Write('LoteCuponID', System.TypeInfo(Integer), LoteCuponID, []);
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('Identificador', System.TypeInfo(Integer), Identificador, []);
    lMessage.Write('Cupones', System.TypeInfo(LibraryValesCupones_Intf.AGeneraCupon), Cupones, []);
    lMessage.Write('TipoCupon', System.TypeInfo(Boolean), TipoCupon, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('aFolio', System.TypeInfo(Integer), aFolio, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DocumentosConSaldo(const ClienteID: Integer): TDocumentosConSaldoArray;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DocumentosConSaldo');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDocumentosConSaldoArray), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.FacturaRecibo(const Cliente: Integer; const Importe: Double; const EstacionID: Integer; const EmpleadoID: Integer;
                                                  const SerieFactura: AnsiString; const ProductoID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'FacturaRecibo');
    lMessage.Write('Cliente', System.TypeInfo(Integer), Cliente, []);
    lMessage.Write('Importe', System.TypeInfo(Double), Importe, []);
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('EmpleadoID', System.TypeInfo(Integer), EmpleadoID, []);
    lMessage.Write('SerieFactura', System.TypeInfo(AnsiString), SerieFactura, []);
    lMessage.Write('ProductoID', System.TypeInfo(Integer), ProductoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CaracteresCupon: AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CaracteresCupon');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ObtenCuponValido(const Barras: AnsiString; const Grupo: Integer; const CuponAutoriza: TAutoriza): TCuponValido;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ObtenCuponValido');
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Write('Grupo', System.TypeInfo(Integer), Grupo, []);
    lMessage.Write('CuponAutoriza', System.TypeInfo(LibraryValesCupones_Intf.TAutoriza), CuponAutoriza, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TCuponValido), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GuardaAutorizacion(const Autorizacion: TAutoriza): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaAutorizacion');
    lMessage.Write('Autorizacion', System.TypeInfo(LibraryValesCupones_Intf.TAutoriza), Autorizacion, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosVehiculo(const VehiculoID: Integer): TDatosVehiculo;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosVehiculo');
    lMessage.Write('VehiculoID', System.TypeInfo(Integer), VehiculoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosVehiculo), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CancelaGrupo(const Grupo: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CancelaGrupo');
    lMessage.Write('Grupo', System.TypeInfo(Integer), Grupo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.QuemarCupon(const Consumo: TConsumo; const Barras: AnsiString): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'QuemarCupon');
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumo), Consumo, []);
    lMessage.Write('Barras', System.TypeInfo(AnsiString), Barras, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GuardarConsumo(const Consumo: TConsumo): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardarConsumo');
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumo), Consumo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosVerifica(const VehiculoID: Integer): TDatosVerifica;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosVerifica');
    lMessage.Write('VehiculoID', System.TypeInfo(Integer), VehiculoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosVerifica), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosVerificaReferencia(const Referencia: AnsiString): TDatosVerifica;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosVerificaReferencia');
    lMessage.Write('Referencia', System.TypeInfo(AnsiString), Referencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TDatosVerifica), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.TarjetaConsumo(const Tarjeta: AnsiString; const Consumo: TConsumo): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'TarjetaConsumo');
    lMessage.Write('Tarjeta', System.TypeInfo(AnsiString), Tarjeta, []);
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumo), Consumo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ActualizaHistorial(const Consumo: TConsumo);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ActualizaHistorial');
    lMessage.Write('Consumo', System.TypeInfo(LibraryValesCupones_Intf.TConsumo), Consumo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.EliminaAutorizacionVehiculo(const Referencia: AnsiString): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EliminaAutorizacionVehiculo');
    lMessage.Write('Referencia', System.TypeInfo(AnsiString), Referencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.AgregaSecuencias(const Consumos: aConsumo): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AgregaSecuencias');
    lMessage.Write('Consumos', System.TypeInfo(LibraryValesCupones_Intf.aConsumo), Consumos, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.AutorizaTarjeta(const Autoriza: TAutoriza): TTarjeta;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AutorizaTarjeta');
    lMessage.Write('Autoriza', System.TypeInfo(LibraryValesCupones_Intf.TAutoriza), Autoriza, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TTarjeta), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.BuscaAutorizacionTarjeta(const ClienteID: Integer; const VehiculoID: Integer; const GasolineroID: Integer): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'BuscaAutorizacionTarjeta');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('VehiculoID', System.TypeInfo(Integer), VehiculoID, []);
    lMessage.Write('GasolineroID', System.TypeInfo(Integer), GasolineroID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.EstadoDispensario(const Dispensario: Integer; const Estado: AnsiString);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EstadoDispensario');
    lMessage.Write('Dispensario', System.TypeInfo(Integer), Dispensario, []);
    lMessage.Write('Estado', System.TypeInfo(AnsiString), Estado, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.EntradaBitacora(const Descripcion: AnsiString; const Tipo: eTipoBitacora; const Alerta: Boolean);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EntradaBitacora');
    lMessage.Write('Descripcion', System.TypeInfo(AnsiString), Descripcion, []);
    lMessage.Write('Tipo', System.TypeInfo(LibraryValesCupones_Intf.eTipoBitacora), Tipo, []);
    lMessage.Write('Alerta', System.TypeInfo(Boolean), Alerta, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.AutoCambioPrecio;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AutoCambioPrecio');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.CierraTurno(const BombaID: Integer; const TurnoID: Integer; const FinLecturas: aLecturas);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CierraTurno');
    lMessage.Write('BombaID', System.TypeInfo(Integer), BombaID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Write('FinLecturas', System.TypeInfo(LibraryValesCupones_Intf.aLecturas), FinLecturas, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.CreaTurno(const BombaID: Integer; const TurnoID: Integer; const InicioLecturas: aLecturas);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CreaTurno');
    lMessage.Write('BombaID', System.TypeInfo(Integer), BombaID, []);
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Write('InicioLecturas', System.TypeInfo(LibraryValesCupones_Intf.aLecturas), InicioLecturas, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GuardaVenta(const nBomba: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GuardaVenta');
    lMessage.Write('nBomba', System.TypeInfo(Integer), nBomba, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.UltimoTurnoBomba: aTurnoBomba;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'UltimoTurnoBomba');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.aTurnoBomba), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.ValidaSecuencias(const TurnoID: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValidaSecuencias');
    lMessage.Write('TurnoID', System.TypeInfo(Integer), TurnoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.ValidaSecuenciasC(const EstacionID: Integer; const Secuencias: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'ValidaSecuenciasC');
    lMessage.Write('EstacionID', System.TypeInfo(Integer), EstacionID, []);
    lMessage.Write('Secuencias', System.TypeInfo(AnsiString), Secuencias, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.Datos: TGlobal;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    Result := nil;
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'Datos');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(LibraryValesCupones_Intf.TGlobal), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.CancelaGrupoC(const BombaID: Integer; const Grupo: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CancelaGrupoC');
    lMessage.Write('BombaID', System.TypeInfo(Integer), BombaID, []);
    lMessage.Write('Grupo', System.TypeInfo(Integer), Grupo, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PrefijaGrupo(const Bomba: Integer; const Grupo: Integer; const Terminal: AnsiString; const Cupon: TCupon): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PrefijaGrupo');
    lMessage.Write('Bomba', System.TypeInfo(Integer), Bomba, []);
    lMessage.Write('Grupo', System.TypeInfo(Integer), Grupo, []);
    lMessage.Write('Terminal', System.TypeInfo(AnsiString), Terminal, []);
    lMessage.Write('Cupon', System.TypeInfo(LibraryValesCupones_Intf.TCupon), Cupon, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PrefijaTarjeta(const nBomba: Integer; const Tarjeta: AnsiString; const NIP: AnsiString; const Odometro: AnsiString;
                                                   const Terminal: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PrefijaTarjeta');
    lMessage.Write('nBomba', System.TypeInfo(Integer), nBomba, []);
    lMessage.Write('Tarjeta', System.TypeInfo(AnsiString), Tarjeta, []);
    lMessage.Write('NIP', System.TypeInfo(AnsiString), NIP, []);
    lMessage.Write('Odometro', System.TypeInfo(AnsiString), Odometro, []);
    lMessage.Write('Terminal', System.TypeInfo(AnsiString), Terminal, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.AsignaBombas;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AsignaBombas');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.AsignaProductos;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AsignaProductos');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.AsignaDispositivo;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AsignaDispositivo');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.AsignaTanques;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'AsignaTanques');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

procedure TServiceValesCupones_Proxy.IncrementarImpreso(const Secuencia: Integer);
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'IncrementarImpreso');
    lMessage.Write('Secuencia', System.TypeInfo(Integer), Secuencia, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.PrefijaTarjetaFrecuente(const nBomba: Integer; const Tarjeta: AnsiString; const Odometro: AnsiString; const Terminal: AnsiString): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'PrefijaTarjetaFrecuente');
    lMessage.Write('nBomba', System.TypeInfo(Integer), nBomba, []);
    lMessage.Write('Tarjeta', System.TypeInfo(AnsiString), Tarjeta, []);
    lMessage.Write('Odometro', System.TypeInfo(AnsiString), Odometro, []);
    lMessage.Write('Terminal', System.TypeInfo(AnsiString), Terminal, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GenerarFacturas(const Serie: AnsiString; const fol: Integer; const FechaCorte: DateTime; const FechaContable: DateTime;
                                                    const DiaFacturar: Integer; const Magna: Boolean; const Premium: Boolean; const Diesel: Boolean; const EmpleadoID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GenerarFacturas');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('fol', System.TypeInfo(Integer), fol, []);
    lMessage.Write('FechaCorte', System.TypeInfo(DateTime), FechaCorte, [paIsDateTime]);
    lMessage.Write('FechaContable', System.TypeInfo(DateTime), FechaContable, [paIsDateTime]);
    lMessage.Write('DiaFacturar', System.TypeInfo(Integer), DiaFacturar, []);
    lMessage.Write('Magna', System.TypeInfo(Boolean), Magna, []);
    lMessage.Write('Premium', System.TypeInfo(Boolean), Premium, []);
    lMessage.Write('Diesel', System.TypeInfo(Boolean), Diesel, []);
    lMessage.Write('EmpleadoID', System.TypeInfo(Integer), EmpleadoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.EliminaFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'EliminaFactura');
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.CambioFactura(const Serie: AnsiString; const Folio: Integer; const NuevaSerie: AnsiString; const NuevoFolio: Integer;
                                                  const FechaImpresion: DateTime): AnsiString;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'CambioFactura');
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Write('NuevaSerie', System.TypeInfo(AnsiString), NuevaSerie, []);
    lMessage.Write('NuevoFolio', System.TypeInfo(Integer), NuevoFolio, []);
    lMessage.Write('FechaImpresion', System.TypeInfo(DateTime), FechaImpresion, [paIsDateTime]);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(AnsiString), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.DatosFactura(const Folio: Integer; const Serie: AnsiString): Boolean;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'DatosFactura');
    lMessage.Write('Folio', System.TypeInfo(Integer), Folio, []);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Boolean), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GeneraFacturasXCliente(const ClienteID: Integer; const Productos: AnsiString; const FechaCorte: DateTime; const FechaContable: DateTime;
                                                           const Serie: AnsiString; const aFolio: Integer; const EmpleadoID: Integer): Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GeneraFacturasXCliente');
    lMessage.Write('ClienteID', System.TypeInfo(Integer), ClienteID, []);
    lMessage.Write('Productos', System.TypeInfo(AnsiString), Productos, []);
    lMessage.Write('FechaCorte', System.TypeInfo(DateTime), FechaCorte, [paIsDateTime]);
    lMessage.Write('FechaContable', System.TypeInfo(DateTime), FechaContable, [paIsDateTime]);
    lMessage.Write('Serie', System.TypeInfo(AnsiString), Serie, []);
    lMessage.Write('aFolio', System.TypeInfo(Integer), aFolio, []);
    lMessage.Write('EmpleadoID', System.TypeInfo(Integer), EmpleadoID, []);
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

function TServiceValesCupones_Proxy.GasolineroID: Integer;
var
  lMessage: IROMessage;
  lTransportChannel: IROTransportChannel;
begin
  lMessage := __GetMessage;
  lTransportChannel := __TransportChannel;
  try
    lMessage.InitializeRequestMessage(lTransportChannel, 'LibraryValesCupones', __InterfaceName, 'GasolineroID');
    lMessage.Finalize;

    lTransportChannel.Dispatch(lMessage);

    lMessage.Read('Result', System.TypeInfo(Integer), Result, []);
  finally
    lMessage.UnsetAttributes(lTransportChannel);
    lMessage.FreeStream;
    lMessage := nil;
    lTransportChannel := nil;
  end;
end;

initialization
  RegisterROClass(TParametros);
  RegisterROClass(TFactura);
  RegisterROClass(TDetalleFactura);
  RegisterROClass(TTicketFactura);
  RegisterROClass(TDetalleCupon);
  RegisterROClass(TDetalleValeCreditoFactura);
  RegisterROClass(TDatosFactura);
  RegisterROClass(TLiquidacion);
  RegisterROClass(TLiquidacionDetalle);
  RegisterROClass(TLiquidacionProducto);
  RegisterROClass(TTransaccion);
  RegisterROClass(TDetalleTransaccion);
  RegisterROClass(TCarteraVale);
  RegisterROClass(TDatosLiquidacion);
  RegisterROClass(TReporte);
  RegisterROClass(TRecibo);
  RegisterROClass(TDetalleReciboPago);
  RegisterROClass(TDatosReciboPago);
  RegisterROClass(TDatosRecibo);
  RegisterROClass(TLoginInfo);
  RegisterROClass(TOpcion);
  RegisterROClass(TConsumoExpress);
  RegisterROClass(TDatosCliente);
  RegisterROClass(TFacturaExpress);
  RegisterROClass(TDetalleExpress);
  RegisterROClass(TValoresTurno);
  RegisterROClass(TOtroProducto);
  RegisterROClass(TAgrupacion);
  RegisterROClass(TSagarpa);
  RegisterROClass(TDatosPuntos);
  RegisterROClass(TDatosCelular);
  RegisterROClass(TEntregaPremio);
  RegisterROClass(TDatosPremio);
  RegisterROClass(TConfirmaEnvioA);
  RegisterROClass(TDatosCerrarLiquidacion);
  RegisterROClass(TDespachadorLiquidacion);
  RegisterROClass(TDetalleLiquidacion);
  RegisterROClass(TTipoValor);
  RegisterROClass(TPerfilPuntos);
  RegisterROClass(TFacturaElectronica);
  RegisterROClass(TFacturaElectronicaDetalleImportes);
  RegisterROClass(TFacturaElectronicaDetalleImpuestos);
  RegisterROClass(TDatosFacturaPemex);
  RegisterROClass(TDetalleFacturaPemex);
  RegisterROClass(TDataFacturaElectronicaF);
  RegisterROClass(TGeneraCupon);
  RegisterROClass(TDocumentosConSaldo);
  RegisterROClass(TAutoriza);
  RegisterROClass(TCuponValido);
  RegisterROClass(TCupon);
  RegisterROClass(TRegistroVenta);
  RegisterROClass(TConsumo);
  RegisterROClass(TTarjeta);
  RegisterROClass(TTarjetaManual);
  RegisterROClass(TNodo);
  RegisterROClass(TDatosVehiculo);
  RegisterROClass(TDatosVerifica);
  RegisterROClass(TGlobal);
  RegisterROClass(TBomba);
  RegisterROClass(TFactorDecimal);
  RegisterROClass(TFlujos);
  RegisterROClass(TLecturas);
  RegisterROClass(TManguera);
  RegisterROClass(TCanal);
  RegisterROClass(TContador);
  RegisterROClass(TRegistroPuntoVenta);
  RegisterROClass(TCambio);
  RegisterROClass(TCambioPrecios);
  RegisterROClass(TDispositivos);
  RegisterROClass(TGas);
  RegisterROClass(TProtecciones);
  RegisterROClass(TLicencia);
  RegisterROClass(TTanque);
  RegisterROClass(TRecepcion);
  RegisterROClass(AAgrupacion);
  RegisterROClass(AAgrupacionBomba);
  RegisterROClass(aBombas);
  RegisterROClass(aBoolean);
  RegisterROClass(aCambioPrecios);
  RegisterROClass(aConsumo);
  RegisterROClass(aContador);
  RegisterROClass(ADatosCelular);
  RegisterROClass(ADespachadorLiquidacion);
  RegisterROClass(ADetalleExpress);
  RegisterROClass(ADetalleFacturaPemex);
  RegisterROClass(ADetalleLiquidacion);
  RegisterROClass(aDispositivos);
  RegisterROClass(aGas);
  RegisterROClass(AGeneraCupon);
  RegisterROClass(aLecturas);
  RegisterROClass(aMaguera);
  RegisterROClass(aNodos);
  RegisterROClass(aRecepciones);
  RegisterROClass(aRegistroVenta);
  RegisterROClass(ArrayOpcion);
  RegisterROClass(aTanques);
  RegisterROClass(ATCarteraVale);
  RegisterROClass(ATDetalleCupon);
  RegisterROClass(ATDetalleFactura);
  RegisterROClass(ATDetalleRecibo);
  RegisterROClass(ATDetalleTransaccion);
  RegisterROClass(ATDetalleValeCreditoFactura);
  RegisterROClass(ATFacturaElectronicaDetalleImportes);
  RegisterROClass(ATFacturaElectronicaDetalleImpuestos);
  RegisterROClass(ATipoValor);
  RegisterROClass(ATLiquidacionDetalle);
  RegisterROClass(ATLiquidacionProducto);
  RegisterROClass(ATTicketFactura);
  RegisterROClass(aTurnoBomba);
  RegisterROClass(TDocumentosConSaldoArray);
  RegisterProxyClass(IServiceValesCupones_IID, TServiceValesCupones_Proxy);

  RegisterROEnum('eCodeTrama',System.TypeInfo(LibraryValesCupones_Intf.eCodeTrama));
  RegisterEnumMapping('eCodeTrama', 'ctContinue', 'ctContinue');
  RegisterEnumMapping('eCodeTrama', 'ctOut', 'ctOut');
  RegisterEnumMapping('eCodeTrama', 'ctAkc', 'ctAkc');
  RegisterEnumMapping('eCodeTrama', 'ctNak', 'ctNak');
  RegisterEnumMapping('eCodeTrama', 'ctFrame', 'ctFrame');
  RegisterEnumMapping('eCodeTrama', 'ctOverRun', 'ctOverRun');
  RegisterROEnum('eCodigoProducto',System.TypeInfo(LibraryValesCupones_Intf.eCodigoProducto));
  RegisterEnumMapping('eCodigoProducto', 'cpPorProducto', 'cpPorProducto');
  RegisterEnumMapping('eCodigoProducto', 'cpPorPosicion', 'cpPorPosicion');
  RegisterROEnum('eEstadoBomba',System.TypeInfo(LibraryValesCupones_Intf.eEstadoBomba));
  RegisterEnumMapping('eEstadoBomba', 'ebOffLine', 'ebOffLine');
  RegisterEnumMapping('eEstadoBomba', 'ebIdle', 'ebIdle');
  RegisterEnumMapping('eEstadoBomba', 'ebBusy', 'ebBusy');
  RegisterEnumMapping('eEstadoBomba', 'ebPago', 'ebPago');
  RegisterEnumMapping('eEstadoBomba', 'ebUnknown', 'ebUnknown');
  RegisterEnumMapping('eEstadoBomba', 'ebCall', 'ebCall');
  RegisterEnumMapping('eEstadoBomba', 'ebClose', 'ebClose');
  RegisterEnumMapping('eEstadoBomba', 'ebError', 'ebError');
  RegisterEnumMapping('eEstadoBomba', 'ebStop', 'ebStop');
  RegisterEnumMapping('eEstadoBomba', 'ebAuto', 'ebAuto');
  RegisterROEnum('eEstadoVenta',System.TypeInfo(LibraryValesCupones_Intf.eEstadoVenta));
  RegisterEnumMapping('eEstadoVenta', 'evSinAutorizadoConsola', 'evSinAutorizadoConsola');
  RegisterEnumMapping('eEstadoVenta', 'evCambio', 'evCambio');
  RegisterEnumMapping('eEstadoVenta', 'evCobro', 'evCobro');
  RegisterEnumMapping('eEstadoVenta', 'evFullServer', 'evFullServer');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizadoConsolaVolumen', 'evAutorizadoConsolaVolumen');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizadoConsolaEfectivo', 'evAutorizadoConsolaEfectivo');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizadoConsolaLibre', 'evAutorizadoConsolaLibre');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizadoCistemFlotillas', 'evAutorizadoCistemFlotillas');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizadoCistemProtocol', 'evAutorizadoCistemProtocol');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizacionTPV', 'evAutorizacionTPV');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizacionLDC', 'evAutorizacionLDC');
  RegisterEnumMapping('eEstadoVenta', 'evAutorizacionPendiente', 'evAutorizacionPendiente');
  RegisterROEnum('eFabricante',System.TypeInfo(LibraryValesCupones_Intf.eFabricante));
  RegisterEnumMapping('eFabricante', 'fTokheim', 'fTokheim');
  RegisterEnumMapping('eFabricante', 'fWayne', 'fWayne');
  RegisterEnumMapping('eFabricante', 'fGilbarco', 'fGilbarco');
  RegisterEnumMapping('eFabricante', 'fBennett', 'fBennett');
  RegisterEnumMapping('eFabricante', 'fSchlumberger', 'fSchlumberger');
  RegisterEnumMapping('eFabricante', 'fCistemGas', 'fCistemGas');
  RegisterEnumMapping('eFabricante', 'fCistemGasRed', 'fCistemGasRed');
  RegisterEnumMapping('eFabricante', 'fIGM', 'fIGM');
  RegisterEnumMapping('eFabricante', 'fTransactor', 'fTransactor');
  RegisterROEnum('eImpresionTicket',System.TypeInfo(LibraryValesCupones_Intf.eImpresionTicket));
  RegisterEnumMapping('eImpresionTicket', 'itNoImprime', 'itNoImprime');
  RegisterEnumMapping('eImpresionTicket', 'itCorteTurno', 'itCorteTurno');
  RegisterEnumMapping('eImpresionTicket', 'itParcial', 'itParcial');
  RegisterROEnum('eModoOperacion',System.TypeInfo(LibraryValesCupones_Intf.eModoOperacion));
  RegisterEnumMapping('eModoOperacion', 'moFullService', 'moFullService');
  RegisterEnumMapping('eModoOperacion', 'moConsola', 'moConsola');
  RegisterEnumMapping('eModoOperacion', 'moDeshabilitada', 'moDeshabilitada');
  RegisterROEnum('eMonitoreo',System.TypeInfo(LibraryValesCupones_Intf.eMonitoreo));
  RegisterEnumMapping('eMonitoreo', 'mVeederRoot', 'mVeederRoot');
  RegisterEnumMapping('eMonitoreo', 'mVeederRoot2', 'mVeederRoot2');
  RegisterEnumMapping('eMonitoreo', 'mAutoStikJR', 'mAutoStikJR');
  RegisterEnumMapping('eMonitoreo', 'mAutoStickII', 'mAutoStickII');
  RegisterEnumMapping('eMonitoreo', 'mIncon', 'mIncon');
  RegisterEnumMapping('eMonitoreo', 'mPetrovend', 'mPetrovend');
  RegisterEnumMapping('eMonitoreo', 'mEeco', 'mEeco');
  RegisterROEnum('eProceso',System.TypeInfo(LibraryValesCupones_Intf.eProceso));
  RegisterEnumMapping('eProceso', 'pSinProceso', 'pSinProceso');
  RegisterEnumMapping('eProceso', 'pPrecio', 'pPrecio');
  RegisterEnumMapping('eProceso', 'pMaximo', 'pMaximo');
  RegisterEnumMapping('eProceso', 'pCorte', 'pCorte');
  RegisterEnumMapping('eProceso', 'pCambioPrecio', 'pCambioPrecio');
  RegisterEnumMapping('eProceso', 'pMinimo', 'pMinimo');
  RegisterROEnum('eResult',System.TypeInfo(LibraryValesCupones_Intf.eResult));
  RegisterEnumMapping('eResult', 'eOK', 'eOK');
  RegisterEnumMapping('eResult', 'eFail', 'eFail');
  RegisterROEnum('eTipoAutorizacion',System.TypeInfo(LibraryValesCupones_Intf.eTipoAutorizacion));
  RegisterEnumMapping('eTipoAutorizacion', 'taSinAutorizacion', 'taSinAutorizacion');
  RegisterEnumMapping('eTipoAutorizacion', 'taAutorizacionPreset', 'taAutorizacionPreset');
  RegisterEnumMapping('eTipoAutorizacion', 'taTarjetaClub', 'taTarjetaClub');
  RegisterEnumMapping('eTipoAutorizacion', 'taCupon', 'taCupon');
  RegisterEnumMapping('eTipoAutorizacion', 'taFacturacionExpress', 'taFacturacionExpress');
  RegisterEnumMapping('eTipoAutorizacion', 'taTarjetaBancaria', 'taTarjetaBancaria');
  RegisterROEnum('eTipoBitacora',System.TypeInfo(LibraryValesCupones_Intf.eTipoBitacora));
  RegisterEnumMapping('eTipoBitacora', 'tbUnkown', 'tbUnkown');
  RegisterEnumMapping('eTipoBitacora', 'tbBitacora', 'tbBitacora');
  RegisterEnumMapping('eTipoBitacora', 'tbAlertas', 'tbAlertas');
  RegisterEnumMapping('eTipoBitacora', 'tbDispensarios', 'tbDispensarios');
  RegisterEnumMapping('eTipoBitacora', 'tbTanques', 'tbTanques');
  RegisterEnumMapping('eTipoBitacora', 'tbAccesos', 'tbAccesos');
  RegisterEnumMapping('eTipoBitacora', 'tbJarreos', 'tbJarreos');
  RegisterEnumMapping('eTipoBitacora', 'tbCapturas', 'tbCapturas');
  RegisterEnumMapping('eTipoBitacora', 'tbConexiones', 'tbConexiones');
  RegisterROEnum('eTipoPago',System.TypeInfo(LibraryValesCupones_Intf.eTipoPago));
  RegisterEnumMapping('eTipoPago', 'tpgoEfectivo', 'tpgoEfectivo');
  RegisterEnumMapping('eTipoPago', 'tpgoCheque', 'tpgoCheque');
  RegisterEnumMapping('eTipoPago', 'tpgoTarjeta', 'tpgoTarjeta');
  RegisterROEnum('eTipoPreset',System.TypeInfo(LibraryValesCupones_Intf.eTipoPreset));
  RegisterEnumMapping('eTipoPreset', 'tpSinPreset', 'tpSinPreset');
  RegisterEnumMapping('eTipoPreset', 'tpImporteSinProducto', 'tpImporteSinProducto');
  RegisterEnumMapping('eTipoPreset', 'tpLitrosSinProducto', 'tpLitrosSinProducto');
  RegisterEnumMapping('eTipoPreset', 'tpLibreSinProducto', 'tpLibreSinProducto');
  RegisterEnumMapping('eTipoPreset', 'tpImporteConProducto', 'tpImporteConProducto');
  RegisterEnumMapping('eTipoPreset', 'tpLitrosConProducto', 'tpLitrosConProducto');
  RegisterEnumMapping('eTipoPreset', 'tpLibreConProducto', 'tpLibreConProducto');

finalization
  UnregisterROClass(TParametros);
  UnregisterROClass(TFactura);
  UnregisterROClass(TDetalleFactura);
  UnregisterROClass(TTicketFactura);
  UnregisterROClass(TDetalleCupon);
  UnregisterROClass(TDetalleValeCreditoFactura);
  UnregisterROClass(TDatosFactura);
  UnregisterROClass(TLiquidacion);
  UnregisterROClass(TLiquidacionDetalle);
  UnregisterROClass(TLiquidacionProducto);
  UnregisterROClass(TTransaccion);
  UnregisterROClass(TDetalleTransaccion);
  UnregisterROClass(TCarteraVale);
  UnregisterROClass(TDatosLiquidacion);
  UnregisterROClass(TReporte);
  UnregisterROClass(TRecibo);
  UnregisterROClass(TDetalleReciboPago);
  UnregisterROClass(TDatosReciboPago);
  UnregisterROClass(TDatosRecibo);
  UnregisterROClass(TLoginInfo);
  UnregisterROClass(TOpcion);
  UnregisterROClass(TConsumoExpress);
  UnregisterROClass(TDatosCliente);
  UnregisterROClass(TFacturaExpress);
  UnregisterROClass(TDetalleExpress);
  UnregisterROClass(TValoresTurno);
  UnregisterROClass(TOtroProducto);
  UnregisterROClass(TAgrupacion);
  UnregisterROClass(TSagarpa);
  UnregisterROClass(TDatosPuntos);
  UnregisterROClass(TDatosCelular);
  UnregisterROClass(TEntregaPremio);
  UnregisterROClass(TDatosPremio);
  UnregisterROClass(TConfirmaEnvioA);
  UnregisterROClass(TDatosCerrarLiquidacion);
  UnregisterROClass(TDespachadorLiquidacion);
  UnregisterROClass(TDetalleLiquidacion);
  UnregisterROClass(TTipoValor);
  UnregisterROClass(TPerfilPuntos);
  UnregisterROClass(TFacturaElectronica);
  UnregisterROClass(TFacturaElectronicaDetalleImportes);
  UnregisterROClass(TFacturaElectronicaDetalleImpuestos);
  UnregisterROClass(TDatosFacturaPemex);
  UnregisterROClass(TDetalleFacturaPemex);
  UnregisterROClass(TDataFacturaElectronicaF);
  UnregisterROClass(TGeneraCupon);
  UnregisterROClass(TDocumentosConSaldo);
  UnregisterROClass(TAutoriza);
  UnregisterROClass(TCuponValido);
  UnregisterROClass(TCupon);
  UnregisterROClass(TRegistroVenta);
  UnregisterROClass(TConsumo);
  UnregisterROClass(TTarjeta);
  UnregisterROClass(TTarjetaManual);
  UnregisterROClass(TNodo);
  UnregisterROClass(TDatosVehiculo);
  UnregisterROClass(TDatosVerifica);
  UnregisterROClass(TGlobal);
  UnregisterROClass(TBomba);
  UnregisterROClass(TFactorDecimal);
  UnregisterROClass(TFlujos);
  UnregisterROClass(TLecturas);
  UnregisterROClass(TManguera);
  UnregisterROClass(TCanal);
  UnregisterROClass(TContador);
  UnregisterROClass(TRegistroPuntoVenta);
  UnregisterROClass(TCambio);
  UnregisterROClass(TCambioPrecios);
  UnregisterROClass(TDispositivos);
  UnregisterROClass(TGas);
  UnregisterROClass(TProtecciones);
  UnregisterROClass(TLicencia);
  UnregisterROClass(TTanque);
  UnregisterROClass(TRecepcion);
  UnregisterROClass(AAgrupacion);
  UnregisterROClass(AAgrupacionBomba);
  UnregisterROClass(aBombas);
  UnregisterROClass(aBoolean);
  UnregisterROClass(aCambioPrecios);
  UnregisterROClass(aConsumo);
  UnregisterROClass(aContador);
  UnregisterROClass(ADatosCelular);
  UnregisterROClass(ADespachadorLiquidacion);
  UnregisterROClass(ADetalleExpress);
  UnregisterROClass(ADetalleFacturaPemex);
  UnregisterROClass(ADetalleLiquidacion);
  UnregisterROClass(aDispositivos);
  UnregisterROClass(aGas);
  UnregisterROClass(AGeneraCupon);
  UnregisterROClass(aLecturas);
  UnregisterROClass(aMaguera);
  UnregisterROClass(aNodos);
  UnregisterROClass(aRecepciones);
  UnregisterROClass(aRegistroVenta);
  UnregisterROClass(ArrayOpcion);
  UnregisterROClass(aTanques);
  UnregisterROClass(ATCarteraVale);
  UnregisterROClass(ATDetalleCupon);
  UnregisterROClass(ATDetalleFactura);
  UnregisterROClass(ATDetalleRecibo);
  UnregisterROClass(ATDetalleTransaccion);
  UnregisterROClass(ATDetalleValeCreditoFactura);
  UnregisterROClass(ATFacturaElectronicaDetalleImportes);
  UnregisterROClass(ATFacturaElectronicaDetalleImpuestos);
  UnregisterROClass(ATipoValor);
  UnregisterROClass(ATLiquidacionDetalle);
  UnregisterROClass(ATLiquidacionProducto);
  UnregisterROClass(ATTicketFactura);
  UnregisterROClass(aTurnoBomba);
  UnregisterROClass(TDocumentosConSaldoArray);
  UnregisterProxyClass(IServiceValesCupones_IID);


  UnRegisterEnumMappings('eCodeTrama');
  UnregisterROEnum('eCodeTrama');
  UnRegisterEnumMappings('eCodigoProducto');
  UnregisterROEnum('eCodigoProducto');
  UnRegisterEnumMappings('eEstadoBomba');
  UnregisterROEnum('eEstadoBomba');
  UnRegisterEnumMappings('eEstadoVenta');
  UnregisterROEnum('eEstadoVenta');
  UnRegisterEnumMappings('eFabricante');
  UnregisterROEnum('eFabricante');
  UnRegisterEnumMappings('eImpresionTicket');
  UnregisterROEnum('eImpresionTicket');
  UnRegisterEnumMappings('eModoOperacion');
  UnregisterROEnum('eModoOperacion');
  UnRegisterEnumMappings('eMonitoreo');
  UnregisterROEnum('eMonitoreo');
  UnRegisterEnumMappings('eProceso');
  UnregisterROEnum('eProceso');
  UnRegisterEnumMappings('eResult');
  UnregisterROEnum('eResult');
  UnRegisterEnumMappings('eTipoAutorizacion');
  UnregisterROEnum('eTipoAutorizacion');
  UnRegisterEnumMappings('eTipoBitacora');
  UnregisterROEnum('eTipoBitacora');
  UnRegisterEnumMappings('eTipoPago');
  UnregisterROEnum('eTipoPago');
  UnRegisterEnumMappings('eTipoPreset');
  UnregisterROEnum('eTipoPreset');
end.
